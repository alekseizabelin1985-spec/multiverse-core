# The example file scripts/compose-lint.sh reads instead of .env.example when it
# lints bad-env-example-comment.yml. Values are placeholders, never credentials.
COMPOSE_PROJECT_NAME=multiverse
MV_IMAGE_TAG=fixture
MINIO_ROOT_USER=                     # [required] the inline comment is the defect
