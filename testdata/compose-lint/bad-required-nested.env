# The example file scripts/compose-lint.sh reads instead of .env.example when it
# lints bad-required-nested.yml. Values are placeholders, never credentials.
COMPOSE_PROJECT_NAME=multiverse
MV_IMAGE_TAG=fixture
# the world; the marker the README asks for is missing on purpose
MV_WORLD_ID=dark-forest-world
