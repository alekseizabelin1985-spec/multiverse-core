
---

# 🌌 Живой Мультиверсум: Общая архитектура

> **Цель системы**: создать мир, который не просто реагирует на действия игроков, а **эволюционирует вместе с ними**, сохраняя внутреннюю целостность и повествовательную глубину.

---

## 🧠 Ключевые принципы

1. **Событийно-ориентированная архитектура (EDA)**  
   Все взаимодействия происходят через события в единой шине (Redpanda).

2. **Слабая связанность**  
   Сервисы не знают друг о друге — только о событиях.

3. **Stateful-сервисы с восстановлением**  
   Каждый сервис хранит свой state и восстанавливается через snapshot + replay.

4. **Иерархическая организация миров**  
   От уникальных базовых миров к единому Первоисточнику.

5. **Генеративность вместо скриптов**  
   ИИ (Qwen3) создаёт уникальные исходы, а не выбирает из списка.

6. **Память в сущностях**  
   История хранится в самих объектах мира, а не в отдельной системе.

7. **Динамические повествователи (GM)**  
   GM создаются/удаляются по событиям и работают с любой областью.

---

## 📡 Событийная модель

### Топики Redpanda

| Топик | Назначение | Примеры событий |
|-------|-----------|----------------|
| `player_events` | Действия игроков | `player.used_skill`, `player.sent_message` |
| `world_events` | Реакция мира | `violation.detected`, `narrative.generated` |
| `game_events` | Игровая логика | `quest.assigned`, `city.reputation.changed` |
| `system_events` | Инфраструктурные события | `world.generated`, `snapshot.requested` |

### Формат события

```json
{
  "event_id": "uuid4",
  "event_type": "player.used_skill",
  "timestamp": "2025-10-28T12:00:00Z",
  "source": "player-777",
  "world_id": "pain-realm",
  "scope_id": "city-ashes",
  "payload": {
    "spell_id": "fireball",
    "state_changes": [ /* операции для EntityManager */ ],
    "entity_snapshots": [ /* полный граф для путешествий */ ],
    "description": "Вы выпустили огненный шар..."
  }
}
```

> 🔑 **`world_id` — ключ шардинга**: определяет партицию в Redpanda и бакет в MinIO.

---

## 🏗️ Компоненты системы

### 1. **EntityManager**
- **Роль**: управление сущностями через ленивые операции в MinIO.
- **Особенности**:
  - Не хранит данные в памяти,
  - Бакеты в MinIO: `entities-{world_id}`,
  - Поддержка путешествий через `entity_snapshots`.

### 2. **NarrativeOrchestrator (GM)**
- **Роль**: динамическое повествование для любой области.
- **Особенности**:
  - Создаётся/удаляется по событиям,
  - Использует RAG из ChromaDB/Neo4j,
  - Вызывает Ascension Oracle (Qwen3).

### 3. **WorldGenerator**
- **Роль**: генерация миров и онтологий.
- **Процесс**:
  - Получает семя → вызывает Qwen3 → создаёт сущности через EntityManager.

### 4. **BanOfWorld (Запрет Мира)**
- **Роль**: защита целостности мира через резонанс с Ядром.
- **Реакции**: трансформация, наказание, игнорирование.

### 5. **CityGovernor**
- **Роль**: управление городами, NPC, квестами.
- **Особенности**: генерация квестов на основе состояния города.

### 6. **CultivationModule**
- **Роль**: управление системами культивации.
- **Особенности**: уникальные системы на Плане 0, гибридные на высших планах.

### 7. **Reality Monitor**
- **Роль**: агрегация метрик здоровья мира.
- **Метрики**: `dao_stability`, `spatial_integrity`, `pain_flow`.

### 8. **Plan Manager**
- **Роль**: управление иерархией планов.
- **Функции**: маршрутизация вознесений, активация зон слияния.

### 9. **Ascension Oracle**
- **Роль**: генеративный ИИ (Qwen3) для ключевых решений.
- **Используется в**: генерация онтологии, исходы вознесения, трансформация умений.

### 10. **Semantic Memory Builder**
- **Роль**: построение контекста для ИИ.
- **Выход**: векторная БД (ChromaDB) + граф знаний (Neo4j).

### 11. **OntologicalArchivist**
- **Роль**: хранение версий онтологий.
- **Формат**: версионируемые JSON-объекты в MinIO.

---

## 💾 Хранение данных

| Данные | Технология | Назначение |
|-------|-----------|-----------|
| События | Redpanda | Полная история, replay |
| Сущности | MinIO | Бакеты `entities-{world_id}` |
| Онтологии | MinIO | Версионирование правил мира |
| Векторы | ChromaDB | RAG для ИИ |
| Граф знаний | Neo4j | Семантические связи |
| Метрики | TimescaleDB | Аналитика здоровья мира |

---

## 🔄 Потоки данных

### Создание мира
```
WorldGenerator 
  → system_events (создание сущностей)
    → EntityManager (сохранение в MinIO)
    → BanOfWorld (загрузка Ядра)
    → CityGovernor (запуск для локаций)
```

### Нарушение Запрета
```
Игрок использует чужое умение
  → player_events
    → BanOfWorld (анализ резонанса)
      → Semantic Memory Builder (контекст)
        → Ascension Oracle (решение)
          → world_events (трансформация/наказание)
            → CityGovernor (реакция города)
```

### Путешествие между мирами
```
PlanManager генерирует событие entity.travelled
  → world_events (с world_id = новый-мир)
    → EntityManager (сохраняет сущности в entities-новый-мир)
```

### Повествование GM
```
Событие в мире
  → GM группы: генерирует "внутреннее" описание
  → GM города: генерирует "внешнее" описание
  → narrative_output → клиент
```

---

## 🌐 Иерархия планов и миров

```
План Ω (Первоисточник / Хаос)
│
└── План N-1 (Абстрактный)
    │
    └── План 1 (Мир Слияния)
        │
        └── План 0 (Базовые миры)
            ├── Мир Боли
            ├── Мир Памяти
            ├── Мир Механизмов
            └── ...
```

**Особенности**:
- **План 0**: тысячи уникальных миров с собственными онтологиями,
- **План 1**: зоны слияния, где пути объединяются,
- **Высшие планы**: всё более абстрактные формы,
- **План Ω**: все различия стираются, культивация = чистая воля.

---

## 🛠️ Технологический стек

| Уровень | Технология |
|--------|-----------|
| **Язык** | Go 1.23+ |
| **Событийная шина** | Redpanda |
| **Хранение объектов** | MinIO |
| **Векторная БД** | ChromaDB |
| **Граф БД** | Neo4j |
| **Временные ряды** | TimescaleDB |
| **ИИ** | Qwen3 (через vLLM/Ollama) |
| **Оркестрация** | Docker Compose / Kubernetes |
| **Мониторинг** | OpenTelemetry + Prometheus |

---

## ✅ Преимущества архитектуры

- **Масштабируемость**: шардинг по `world_id`, горизонтальное масштабирование.
- **Отказоустойчивость**: восстановление через snapshot + replay.
- **Гибкость**: поддержка любых онтологий и эволюции миров.
- **Иммерсивность**: генеративное повествование вместо скриптов.
- **Целостность**: защита через резонанс с Ядром, а не правила.

---

> **Живой Мультиверсум — это не просто игра, а симуляция живой метафизики,  
> где архитектура служит онтологии, а код — поэзии.**