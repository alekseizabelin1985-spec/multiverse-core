# Журнал решений

- 2026-09-09 · PROJECT · Интейк · цель: аудит проекта, сборка архитектуры и эпиков, поиск открытых вопросов · язык артефактов ru · autonomy gates · git: branches=true, commits=ask · artifactsDir Docs/dev-team · hasUI=false (HTTP API, без UI).
- 2026-09-09 · PROJECT · Допущение · проект уже содержит код (Go workspace, 15 сервисов, shared/agent на ветке feature/agent-gm-core) → перед discovery выполняется аудит текущего состояния (system-architect).
- 2026-09-09 · PROJECT · A0 аудит завершён (system-architect) · 22 модуля собираются; 3 сервиса реализованы, 6 частично, 4 in-memory прототипа, 2 неработоспособны; ~30 разрывов издатель→потребитель, 6 фантомных топиков; Agent GM Core собран, но не активирован; секреты в git-истории; CI на Go отсутствует · файлы: architecture/overview.md, architecture/audit-facts.md.
- 2026-09-09 · PROJECT · A1 discovery-исследование завершено (product-owner, business-analyst, domain-expert) · research.md, vision.md (черновик, варианты A/B/C, рекомендация: B цель + A как MVP), stakeholders.md, requirements/inventory.md (40 требований), requirements/scenarios.md (10 сценариев, ни один игровой не проходит), glossary.md, domain-notes.md.
- 2026-09-09 · PROJECT · Инцидент процесса · open-questions.md перезаписан system-architect при параллельной записи; разделы BA/PO/DE восстановлены оркестратором из транскриптов агентов без потерь (61 вопрос). Решение: далее общие файлы пишет только одна роль за раз, либо каждой роли — свой файл.
- 2026-09-09 · PROJECT · Этап A1 · сводка discovery показана, раунд 1 вопросов задан.
- 2026-09-09 · PROJECT · Discovery раунд 1, решения пользователя · (1) Продукт — бэкенд-платформа живых миров, игровые домены как расширения; первый игрок — сам разработчик, затем узкий круг тестеров; успех = регулярные сессии без поломок мира (OQ-A-01, OQ-P-01, OQ-P-02, OQ-R-01..03). (2) Модель GM — Agent GM Core единственный «мозг», narrative-orchestrator → тонкий рендер/выводится, Rule Engine — механика, временный фича-флаг на миграцию (OQ-A-02, OQ-P-10, OQ-R-13). (3) Режим — соло + малая группа 2–6 СРАЗУ в MVP (отклонение от рекомендации «только соло»); среда — одна машина с GPU, границы под рост, «1000/шард» убрать из NFR (OQ-P-03, OQ-R-05, OQ-A-04). (4) MVP-сценарий — «Тёмный лес» (вход → встреча → бой → нарратив, 30 ходов без поломки) + Telegram/Discord-бот как эталонный клиент; база — сценарии оператора и исследователя (OQ-R-04, OQ-R-06, OQ-P-04).
- 2026-09-09 · PROJECT · Discovery раунд 2, решения пользователя · (1) Living Worlds ВКЛЮЧИТЬ в целевое видение (отклонение от рекомендации «заморозить»): Entity-Actor и эволюция правил — отдельный эпик после MVP, архитектура резервирует контракты (OQ-P-09, OQ-A-06, OQ-R-12). (2) Состояние: entity-manager — единственный писатель, снапшот — истина, выводы LLM и броски записываются как события (record-replay), replay не вызывает LLM повторно (OQ-A-03, OQ-D-01). (3) Консистентность — слоями (законы неизменны → канон с утверждением → сиюминутное свободно), НО с механизмом «пробоя законов»: LLM может породить неординарное событие, разрушающее старые законы и образующее новые; контроль постфактум (страж/ревью). Это ключевое доменное требование — «эволюция законов мира через катаклизм» (OQ-R-11, OQ-D-09, OQ-D-12). (4) LLM: допустить облачные модели (OpenAI/Anthropic/DeepSeek) через абстракцию провайдера, локальный Ollama по умолчанию; выбор векторной БД делегирован архитектору (OQ-A-05, OQ-P-07, OQ-D-13).
- 2026-09-09 · PROJECT · Discovery раунд 3, решения пользователя · (1) Контент: без фильтров в MVP, но с точкой вставки фильтра на выходе нарратива и списком абсолютных запретов в системном промпте; целевой рейтинг 18+ (OQ-D-03, OQ-P-08, OQ-R-16). (2) Культивация: гибрид — ступени (реалмы) внутри мира с бутылочными горлышками + «портрет дао» для перехода между Планами; достижение Ω — финал персонажа; не в MVP «Тёмный лес» (OQ-D-02, OQ-D-06). (3) ПДн: псевдонимизация на входе в game-service — внутри только player_id, связка с Telegram/Discord ID хранится отдельно и удаляема; уведомление «контент создан ИИ» в боте; юрисдикция РФ по умолчанию (OQ-D-04, OQ-D-14, OQ-A-14). (4) Секреты: пользователь отзывает токены сам; команда — удалить .mcp.env/.env из индекса, .gitignore, шаблон .mcp.env.example; очистка истории (git filter-repo) — отдельная задача, выполняется только по явной команде пользователя (OQ-A-11).
- 2026-09-09 · PROJECT · Допущения оркестратора по неблокирующим вопросам (подтвердить на G1) · язык контента и документов — русский, английский как вторичный для промптов при необходимости (OQ-R-08, OQ-P-06); латентность — механика ≤ 0,5 с без LLM, нарратив асинхронно ≤ 5 с, цифры фиксируются после замера на целевом GPU (OQ-R-09); стоимость LLM — локальный Ollama по умолчанию, облако опционально через абстракцию (OQ-R-10); мир «живёт» только в активных scope (тики по расписанию для регионов с игроками), фоновые симуляции — эпик после MVP (OQ-R-15); роль «автор мира» в MVP = оператор через блупринты и CLI, без отдельного UI (OQ-R-17, OQ-P-11); открытость репозитория — без изменений в этом цикле (OQ-P-13); горизонт — один разработчик, инкременты по 2–4 недели, дедлайна нет (OQ-P-12).
- 2026-09-09 · PROJECT · A1 шаг 3 «Уточнение» · запущены product-owner (vision.md, stakeholders.md) ∥ business-analyst (prd.md, user-stories.md, nfr.md; единственный писатель open-questions.md — проставляет статусы «решено»).
- 2026-09-09 · PROJECT · A1 шаг 3 завершён · vision.md v1.0 «К подтверждению (G1)» (PO); requirements/prd.md, user-stories.md, nfr.md (BA); open-questions.md — статусы всех 61 вопросов: 41 решено, 8 допущений, 12 открытых (владельцы: архитектура/планирование). Запущены domain-expert (ревью требований, глоссарий) ∥ product-analyst (metrics.md); пользователю показан текст подтверждения видения.
- 2026-09-09 · PROJECT · Подтверждение видения, раунд 4 · пользователь: «Почти, есть уточнения» (текст уточнений запрошен отдельно). Решено: первый мессенджер — Telegram, Discord следующим инкрементом; ввод — команды enter/look/attack/flee/say/leave с опциональным текстом, распознавание свободного текста — целевое (OQ-R-07, PRD A-1, A-6). Список абсолютных запретов BR-08 принят как есть (4 пункта). Целевое железо — пользователь укажет модель GPU и VRAM.
- 2026-09-09 · PROJECT · Уточнения к видению (раунд 5), решения пользователя · (1) GM — НЕ один агент, а РОЙ иерархических GM: глобальный GM (события мира — погода и т.п.), GM региона, GM города (следит за происходящим и кидает события), персональный GM игрока (следит за событиями в видимости игрока). «Один GM» в видении трактовать как «одна архитектура — Agent GM Core», реализующая иерархию Global → Region/City (Domain) → Player (Task/Monitor). (2) Фоновая жизнь мира НУЖНА УЖЕ В MVP — допущение «мир живёт только в активных scope» ОТКЛОНЕНО: глобальный и региональные GM генерируют события и без игроков (OQ-R-15). (3) Целевое железо: RTX 4090 24 ГБ VRAM, 128 ГБ RAM, i9-13900 — две модели Qwen3 (8b + 30b-a3b/32b) помещаются одновременно; цифры S4 фиксируются после замера на этой машине. (4) product-analyst: metrics.md v0.1 — северная звезда clean_human_turns_weekly, 5 событий analytics.*, 6 гипотез; предложение: сессия = простой ≥ 30 мин, в S7 считаются только живые игроки — допущение, подтвердить на G1.
- 2026-09-09 · PROJECT · A1 шаг 4 · domain-expert: requirements/domain-review.md (DR-01..27, 10 существенных), glossary.md v0.2; рекомендация «на G1 после правок». Запущен business-analyst на сводные правки: рой GM + фоновая жизнь (раунд 5), DR-01..10 и замечания, предложения product-analyst (metrics.md §8).
- 2026-09-09 · PROJECT · ВИДЕНИЕ ПОДТВЕРЖДЕНО ЯВНО · пользователь: «Да, именно так» по vision.md v1.1 (рой GM на Agent GM Core, фоновая жизнь мира в MVP). Решено: состав роя в MVP — глобальный + региональный «Тёмный лес» + персональный GM игрока (городской — целевое); пороги S14 — на решение архитектора после замера; сессия = ходы с простоем ≤ 30 мин, только живые игроки; S7 = 8 сессий / 3 групповых за 4 недели; все допущения vision.md приняты (русский язык, автор = оператор через файлы, инкременты 2–4 недели, репозиторий без изменений).
- 2026-09-09 · PROJECT · A1 завершён · business-analyst: prd.md v0.2 (рой GM §5.13, FR-120..128, BR-02 полная редакция, BR-16/17, правила боя v0.1), user-stories.md v0.2 (38 историй), nfr.md v0.2; open-questions.md — 69 вопросов: 43 решено, 7 допущений, 11 на архитектуру, 8 новых к G1. product-analyst: metrics.md v0.2. Чек-лист G1 проверен оркестратором: все пункты выполнены. Сводка G1 показана пользователю.
- 2026-09-09 · PROJECT · G1 УТВЕРЖДЁН пользователем · замечаний нет. Решения на гейте: OQ-D-16 — фильтр в MVP-1 = минимальная проверка категории (a) fail-closed, задержанный ответ → quarantined без хранения response_raw; OQ-D-17 — абсолютные запреты расширены до 7 пунктов (+ самоповреждение с протоколом вне игры, ПДн третьих лиц, «мир вымышлен»); OQ-R-18/19/20, OQ-D-18/19, словарь команд — приняты по рекомендациям как стартовые значения, уточняются замером на этапе архитектуры.
- 2026-09-09 · PROJECT · Этап A2 «Анализ» · запущены system-analyst (use-cases, data-model, api-contracts, integrations) ∥ business-analyst (внесение решений G1 в prd/nfr/open-questions). hasUI=false — ux-designer не подключается.
- 2026-09-09 · PROJECT · business-analyst внёс решения G1: prd/nfr/user-stories v0.3, open-questions (51 решено, 7 допущений, 11 на архитектуру). Оркестратор точечно: устаревшие пометки «подтвердить на G1» в разделах P/D реестра → «подтверждено на G1»; S2 в vision.md → «30 раундов группы из 3 (≥ 60 действий)» по OQ-R-18.
- 2026-09-09 · PROJECT · A2 «Анализ» завершён · system-analyst: analysis/use-cases.md (UC-001..035 MVP-1, UC-101..113 целевые), data-model.md (три класса данных, ER, 11 машин состояний), api-contracts.md (HTTP API бота, 45+ событий с картой топиков, контракт блупринта), integrations.md. Замечания к требованиям (приложение Б) — передать BA на этапе планирования. Открытых вопросов к пользователю нет; развилки переданы архитектору.
- 2026-09-09 · PROJECT · Этап A3 «Архитектура» · запущен system-architect: целевая архитектура, ADR, контракты между блоками, план миграции, предложение декомпозиции на эпики.
- 2026-09-09 · PROJECT · A3 шаг 1 завершён · system-architect: overview.md «Целевое состояние» (модульный монолит, контексты gateway/state/mechanics/swarm/llm/laws/memory/replay, 3 процесса + telegram-bot; 3 сервиса переписать, 3 вывести, 8 заморозить), ADR-001..010 (counters.adr=10), contracts.md (C-01..C-15), plan/epics.md (EPIC-001..013, TEAM-1..3, волна 0 «фундамент»), plan/ownership.md. Находка: пара qwen3:8b + 30b-a3b в Q4 с KV-кэшем не помещается в 24 ГБ — нужна матрица замера (F-8). Вопросы пользователю: OQ-A-17 (удалить/заморозить заменяемый код), OQ-A-18 (конфигурация моделей после замера), OQ-A-19 (retention), число команд (3 или 2).
- 2026-09-09 · PROJECT · Решения пользователя по архитектурным развилкам · OQ-A-17: весь заменяемый код (ban-of-world, reality-monitor, shared/schema/redis/config/minio/oracle, rules/intent/tinyml/spatial) — в services/_archive/ вне сборки, НИЧЕГО не удалять (отклонение от рекомендации «удалить нерабочее»). OQ-A-18: базовая конфигурация — одна qwen3:30b-a3b на всё, если проходит замер NFR-002/NFR-090; иначе 8b + 14b; решение фиксируется после матрицы F-8. OQ-A-19: retention 30/90/180 дней (доменные/llm_records/analytics), полные промпты в MinIO по флагу. Команды: 3 (TEAM-1 фундамент+состояние+память, TEAM-2 рой/LLM/законы, TEAM-3 gateway/бот), окончательно на G2.
- 2026-09-09 · PROJECT · A3 шаги 2–3 · запущены architect#1 (EPIC-002 + каркас EPIC-001), architect#2 (EPIC-003), architect#3 (EPIC-004), security-engineer (threat-model.md), devops-engineer (infrastructure.md).
- 2026-09-09 · PROJECT · security-engineer: architecture/threat-model.md (40 угроз, SEC-01..33, чек-лист security-review). 5 Major-замечаний к контрактам (allowlist Telegram user id; идемпотентность без external_id в gateway.db; непубликация всех портов; редакция токена бота в логах; записи LLM для golden только с actor_kind=ci) — передать system-architect. Находки: .mcp.env всё ещё в индексе; в shared/oracle/README.md значение вида sk-… (возможно реальный ключ); .claude/worktrees/* в индексе. Вопросы пользователю: OneDrive в Documents; ключ в README реальный?; allowlist vs инвайт-коды.
- 2026-09-09 · PROJECT · A3 шаги 2–3 завершены (агенты прерваны лимитом сессии на этапе отчёта, файлы записаны полностью) · architect#1: components/state-and-mechanics.md, foundation.md, ADR-011..013; architect#2: components/swarm-llm-laws.md, ADR-014..017; architect#3: components/gateway-and-bot.md, ADR-018..020; devops: infrastructure.md (14 замечаний к архитектуре); counters.adr=20. Запросы на изменение контрактов собраны из §14 документов; сводятся system-architect на шаге 4.
- 2026-09-09 · PROJECT · Решения пользователя (безопасность) · ключ sk-… в shared/oracle/README.md — РЕАЛЬНЫЙ, пользователь отзывает сам; команда заменяет на плейсхолдер в задаче гигиены F-1 (до этого считать скомпрометированным). OneDrive Documents не синхронизирует — данные (links.db, .env, бэкапы) можно держать в томах Docker/рядом с проектом. Допуск тестеров — allowlist Telegram user id в конфиге бота (SEC-06/07).
- 2026-09-09 · PROJECT · A3 шаги 4–5 · запущены system-architect (сведение запросов на изменение контрактов из components/*.md §14, Major-замечаний threat-model, замечаний infrastructure.md §11; обновление contracts.md/ADR/overview/ownership) ∥ tech-lead#1 (проверка декомпозиции, plan/teams.md, замечания к epics.md).
- 2026-09-09 · PROJECT · tech-lead#1: plan/decomposition-review.md (покрытие 22 US MVP-1, размеры, критический путь EPIC-003 XL → I1a/I1b и I1-α «соло на шаблонах», заглушки F-10, объём ≈ 95–100 задач / 9–11 недель), plan/teams.md (TEAM-1..3, integration/mvp-1 от feature/agent-gm-core, слоты 2/3/1). Замечания к epics.md/ownership.md/F-задачам — внести после возврата system-architect. Вопросы пользователю на G2: подготовительный коммит F-0; отрезаемость EPIC-005 memory.
- 2026-09-09 · PROJECT · A3 шаг 4 завершён · system-architect: 63 запроса/замечания — 46 принято, 8 с изменением, 0 отклонено, 3 переданы BA; contracts.md v0.2 (C-01 Journal/Dedup, C-05 round{}, C-08 коды/лимиты, C-11 glob, C-14 указатель); ADR-001/004/005/006/007/009/010 дополнены; ADR-021 объектное хранилище (MinIO upstream архивирован → сборка из исходников, триггеры замены); overview §13/15/16/18/19/21; ownership.md v0.2; consolidation.md; counters.adr=21. Новый вопрос OQ-A-20 (MinIO сборка / SeaweedFS / форк) — на G2. Оркестратор: epics.md обновляется под consolidation §9 + decomposition-review §5 до показа G2.
- 2026-09-09 · PROJECT · По просьбе пользователя создана ветка feat/PROJECT-audit-architecture от feature/agent-gm-core; артефакты Docs/dev-team/** остаются незакоммиченными (commits=ask). git status по-прежнему падает из-за указателей .claude/worktrees/* в индексе (задача F-0).
- 2026-09-09 · PROJECT · A3 завершён · epics.md v0.2 (F-0, F-10, 005-ops/005-memory, I1a/I1b/I1-α, порядок интеграции 002→004→003 для I1; инициализация мира — фикстуры, npc_table только респаун). Чек-лист G2 проверен оркестратором: все пункты выполнены (оговорки: правки api-contracts.md и infrastructure.md по сведению — задачи на A4). Сводка G2 показана пользователю.
- 2026-09-09 · PROJECT · G2 УТВЕРЖДЁН пользователем · без замечаний; утверждены архитектура, 21 ADR, контракты v0.2, 13 эпиков, 3 команды. Решения: OQ-A-20 — MinIO из исходников на MVP-1, замена позже по триггерам ADR-021; F-0 разрешён от feature/agent-gm-core; допущения приняты — 005-memory отрезаема на G3/G4, инициализация мира из фикстур (npc_table только респаун). counters: epic=13, team=3.
- 2026-09-09 · PROJECT · F-0 выполнен · git worktree prune; 13 gitlinks .claude/worktrees/* удалены из индекса (git status восстановлен); .gitignore += .claude/worktrees/, Docs/dev-team/.dashboard.port; коммит 744fb10 в feat/PROJECT-audit-architecture (.dev-team.json, Docs/dev-team/**); ветка integration/mvp-1 создана от feature/agent-gm-core. Без push.
- 2026-09-09 · PROJECT · Этап A4 «Планирование», волна 1 · architect#1 (EPIC-001/002/005 design.md), architect#2 (EPIC-003 design.md), architect#3 (EPIC-004 design.md) ∥ system-analyst (api-contracts.md под contracts v0.2) ∥ business-analyst (FR-025, BR-13, NFR-020, FR-009) ∥ devops-engineer (infrastructure.md под сведение).
- 2026-09-09 · PROJECT · business-analyst: prd/nfr/user-stories v0.4 по сведению G2 (FR-025 say/look вне раунда, 409 already_acted; BR-13 лидерство при смерти/forget; FR-034 enum reason из ADR-017; FR-009 текст согласия; FR-061 abandoned; FR-124 активный тик 60 с; FR-130/131 allowlist и личные чаты; NFR-049 лимиты). Допущения (не блокируют): abandoned без возврата; текст /start краткий; look вне раунда. Замечания к схемам (end_reason=forget, статус abandoned, leader:null) — system-analyst/architect#1/#3 при нарезке.
- 2026-09-09 · EPIC-003 · architect#2: epics/EPIC-003-swarm-llm-laws/design.md v0.1 (три независимых потока I1a, FakeEncounter как первая единица для I1-α, стендовые задачи), components/swarm-llm-laws.md v0.2. Запросы к процессу: ранний merge shared/testkit/swarm в integration/mvp-1 (решить на G3, рекомендация — разрешить); флаг MV_SWARM_FAKE; совладение раздела admin в gateway.openapi.yaml; BA — US-014 критерий про две модели → «по baseline.md»; SA — UC-028/029/020 устарели после фикстур/group-narrator.
- 2026-09-09 · EPIC-004 · architect#3: epics/EPIC-004-gateway-bot/design.md v0.1, components/gateway-and-bot.md v0.2 (allowlist access.Gate, link_id, лимиты, коды C-08 v1.1, MV_ env, InputFilter noop), ADR-018/019/020 дополнены. Имена веток: канон — plan/teams.md (epic/EPIC-002-state, -003-swarm, -004-gateway); state.js приведён; design.md EPIC-004 поправит tech-lead#3 при нарезке.
- 2026-09-09 · PROJECT · Указание пользователя · модели агентов: Fable 5.1 — только тяжёлые размышления (system-architect, architect, security, сведение); tech-lead, developer, code-reviewer, QA, PM — Opus 5; tester, tech-writer, мелкие правки — Sonnet 5. Применяется со второй волны A4.
- 2026-09-09 · PROJECT · system-analyst: analysis/api-contracts.md, data-model.md, use-cases.md → v0.2 (meta-конверт, §2.0 шина/журнал, коды C-08, link_id/CharacterRequest, UC-036 фикстуры, §4 сверка с contracts v0.2 — расхождений нет). Допущения: link_id клиенту не отдаётся; character_status=none; 200 при creating.
- 2026-09-09 · EPIC-001/002/005 · architect#1: design.md для трёх эпиков, foundation.md v0.2, state-and-mechanics.md v0.2 (§4.10 инициализация из фикстур). Запросы к system-architect (свести перед G3): импорт internal/memory → internal/llm (Embed); схема analytics.consistency.violated в F-4b; mvctl trace в 005-ops (Must); FakeNarrator.WithEncounterStub издаёт чужие типы в тестовом режиме — допустимость; ранний merge shared/testkit/swarm (от architect#2).
- 2026-09-09 · PROJECT · A4 волна 2 (Opus) · tech-lead#2 (EPIC-003 tasks.md) ∥ tech-lead#3 (EPIC-004 tasks.md) ∥ qa-engineer (testing/strategy.md). tech-lead#1 — после devops.
- 2026-09-09 · EPIC-001 · devops-engineer: infrastructure.md v0.2 (F-1 список команд по факту git ls-files, F-3 структура _archive, build/minio.Dockerfile, compose-lint, pre-commit для Windows, llm-bench.ps1, runbook-карточки, чек-лист F-0..F-9); goose вместо golang-migrate по ADR-019. Вопросы пользователю: Ollama нативно/контейнер; GitHub-логин для CODEOWNERS и приватность репо; форк minio/minio как страховка; GPU-замер вручную (допущение: вручную); вывод IDE/AI-каталогов из индекса.
- 2026-09-09 · PROJECT · A4 волна 2 · tech-lead#1 (Opus): tasks.md для EPIC-001 (F-0..F-10 → T-0NN), EPIC-002, EPIC-005.
- 2026-09-09 · PROJECT · Решения пользователя (DevOps) · LLM на целевой машине: в основном НАТИВНО через llama.cpp llama-server (D:\Models\llama\llama\llama-server.exe, модель D:\Models\unsloth\Qwen3.8\Qwen3.8-27B-UD-Q3_K_XL.gguf, --host 127.0.0.1 --port 1234, --n-gpu-layers 99, --flash-attn, --jinja, --reasoning on, --tools all, kv-offload/unified, slot-save), Ollama тоже есть → провайдер по умолчанию должен быть OpenAI-совместимый llama.cpp, Ollama — альтернатива; кандидат модели для замера — Qwen3.8-27B (UD-Q3_K_XL). CODEOWNERS: alekseizabelin1985-spec, репозиторий приватный личный. Форк minio/minio в аккаунт пользователя в F-6. IDE/AI-каталоги в индексе — оставить, решить в F-9. GPU-замер — вручную (допущение).
- 2026-09-09 · PROJECT · A4 сведение 2 (Fable) · system-architect: ADR-005/C-15 — провайдер openai_compat (llama-server) как основной; F-8 матрица с Qwen3.8-27B; запросы architect#1/#2 (импорт memory→llm, схема consistency.violated в F-4b, mvctl trace в 005-ops, WithEncounterStub, ранний merge testkit/swarm, goose).
- 2026-09-09 · PROJECT · qa-engineer: testing/strategy.md v0.1 (contract-тест как один набор для заглушки и реализации; интеграция по волнам I1-α/I1/I2, INT-01..14; пороги «после замера» = наблюдения до baseline). Пробелы: G-7 (рестарт между Phase 1/2 — INT-06, задача в EPIC-003 I1b), G-1 (sim-клиенты в Harness, EPIC-004 I2), G-4 (негативы облака, EPIC-003) — передать тимлидам на G3. Вопросы пользователю на G3: GPU-раннер (допущение: вручную), три живых Telegram-аккаунта для S2, интеграционные планы по волнам (принять).
- 2026-09-09 · EPIC-004 · tech-lead#3: epics/EPIC-004-gateway-bot/tasks.md (T-301..320 I1, T-350..357 I2, T-390..393 stand; критический путь T-301→302→303→305→307→308→313→314; I1-α к подволне 1.9). Блокирующие замечания к подволне 1.2: З-1 end_reason=forget vs CHECK миграции (SA/architect#3), З-2 abandoned при /forget — кто публикует (architect#1/#3), З-3 облако в C-08 (system-architect), З-4 leader:null (SA/architect#1), З-5 имя env allowlist (BA/security: принять MV_TELEGRAM_ALLOWED_USER_IDS). Вопросы на G3: ранний merge T-301/T-308 и testkit/swarm; T-320 (облако) оставить/перенести; два тестера с Telegram к волне 2.
- 2026-09-09 · EPIC-003 · tech-lead#2: epics/EPIC-003-swarm-llm-laws/tasks.md (T-201..253 + стенд T-260..266, 19 подволн; интерфейсы Phase2Runner/TickRunner отвязывают роли от Pipeline). Оценка I1 5–6 нед. против 4–5 — меры: e2e/golden tester#2 вне слотов (−0,5 нед.), отрезать Should I2. Замечания для сведения 3: validation_status enum (data-model 11 vs ADR-017 7) — решить до T-215; издатель dice.rolled (EPIC-002 по §0, публикует агент встречи) — оговорка в C-03; процедура при расхождении levels.go ↔ OwnershipRules; encounter.started от FakeEncounter — исключение на I1-α; догон роя без replay.completed от FakeState v0; enum tick-схем; mvctl laws bump доступ к шине (devops). Вопросы на G3: ранний merge testkit/swarm + схем; MV_SWARM_FAKE; срок I1 5–6 нед. или мера №1; Should I2 отрезаема.
- 2026-09-09 · EPIC-001/002/005 · tech-lead#1: tasks.md трёх эпиков TEAM-1 (T-001…T-020 волна 0; T-050…T-071 EPIC-002 I1/I2; T-130…T-148 EPIC-005 ops/memory), раздел «Волны проекта» в EPIC-001/tasks.md §8 (подволны 0.x/1.x/2.x по трём командам, ≤ 6 слотов, точки I1-α/I1/I2, порядок интеграции 002→004→003 и 002→003→004→005, ранние merge F-10/T-a/C4/B2/A5-A6). Разбито: F-6 → F-6a/F-6b, F-4b → F-4b-1/F-4b-2, F-10 → 5 задач. Уточнения к дизайну: build/versions.env создаёт F-6a (0.2), а не F-5; волна 0 — 8 подволн вместо 5. counters.task=148 (диапазоны: TEAM-1 T-001..199, TEAM-2 T-200..299, TEAM-3 T-300..399).
- 2026-09-09 · PROJECT · Сведение 2 завершено (system-architect) · ADR-005 «Дополнение 2»: провайдер по умолчанию openai_compat (llama-server), ollama второй, облако — тот же адаптер с гейтом по host; вариант E Qwen3.8-27B UD-Q3_K_XL (13,1 ГБ + KV 2–4 ГБ — помещается), критерий E → C → A; llama.cpp#20345 → enable_thinking=false на запросах со схемой. ADR-001 п.7/8 (импорт memory→llm, хук MV_SWARM_FAKE в cmd/multiverse), ADR-004 goose. contracts.md v0.3 (C-15 v1.1, FakeEncounter — единственная заглушка боя в EPIC-003 C4 с ранним merge testkit/swarm; F-10 — FakeNarrator v0 без боя), ownership v0.3, epics v0.3, consolidation §10–13, OQ-A-21 решён (scripts/llm-server.ps1 + make llm-up). Запросы к ролям: tech-lead#2/architect#2 (B3a openai_compat, модель E), tech-lead#1 (FakeNarrator без WithEncounterStub, F-8 E), tech-lead#3 (goose), devops (§6 llama-server, CODEOWNERS, форк), BA (US-014, NFR-076), security (SEC-15 llama-server), QA (CT-06).
- 2026-09-09 · PROJECT · A4 волна 3a · system-architect (Fable, сведение 3: З-1..З-8 tech-lead#3 и замечания 1–8 tech-lead#2) ∥ devops (Opus, infrastructure v0.3) ∥ tech-lead#2 (Opus, EPIC-003 design/tasks под сведение 2).
- 2026-09-09 · PROJECT · Указание пользователя · число одновременно работающих команд — 1 (экономия лимитов). .dev-team.json: parallelism = {maxTeams: 1, maxAgentsPerRole: 1, maxParallelAgents: 2}. Следствия: команды TEAM-1..3 сохраняются как логические владельцы эпиков, но работают ПОСЛЕДОВАТЕЛЬНО одной командой; порядок эпиков по DAG: EPIC-001 → EPIC-002 (I1) → EPIC-004 (I1) → EPIC-003 (I1a/I1b) → I2 по эпикам → EPIC-005; ранние merge-поставки и заглушки для параллельной работы становятся необязательными (оставить только те, что нужны точке I1-α). Уже запущенные агенты волны 3a (system-architect, devops, tech-lead#2) и tech-lead#1 доработают; далее — не более 2 агентов одновременно. PM на A4 строит последовательную дорожную карту.
- 2026-09-09 · EPIC-001/002/005 · tech-lead#1: tasks.md × 3 (61 задача TEAM-1; F-10 → 5 задач, F-6 → 6a/6b; T-014 contract-тест шины — ворота волны 1; §8 карта волн трёх команд). Итого MVP-1 ≈ 153 задачи (vs 95–100). counters.task → 393 (оркестратор; max по трём диапазонам). ВАЖНО: карта волн §8 построена на 3 параллельные команды — после решения пользователя «1 команда» PM пересобирает план как последовательный; ранние merge-вопросы снимаются. Замечания к архитектору (импорт memory→llm, mvctl trace, levels.go ↔ OwnershipRules) уже закрыты сведением 2/3; F-4b «создаёт схемы, владельцы ревизуют» — принять.
- 2026-09-09 · EPIC-003 · tech-lead#2: design v0.2, tasks v0.2 под сведение 2 (B3a openai_compat T-208; T-254 ollama условная до F-8; T-255/T-256 хук MV_SWARM_FAKE в cmd/multiverse, удаление — критерий I1; модель E). Вопросы на G3: снять T-254 (ollama) совсем?; волны под последовательную работу — пересобирает PM. Мелкое: «C-05 v1.2» → в contracts.md версия C-05 не поднята — system-architect поправит одной строкой при сведении 3.
- 2026-09-09 · EPIC-001 · devops-engineer: infrastructure.md v0.3 (llama-server вне compose, scripts/llm-server.ps1 без семплинга, --no-webui, только loopback; MV_LLM_* env; bench E/E+/C/A; CODEOWNERS @alekseizabelin1985-spec; форк MinIO). Действия владельца до F-6: форк minio/minio; вывод `llama-server --version` для пина; branch protection в GitHub. Имя брокера — MV_KAFKA_BROKERS (не MV_KAFKA_BROKERS). Изменения F-задач: F-6 +0,5, F-5 новые env, F-7 без Code Owners review на integration.
- 2026-09-09 · PROJECT · A4 · project-manager (Opus): последовательная дорожная карта под 1 команду, risks.md, покрытие US → T.
- 2026-09-09 · PROJECT · Сведение 3 завершено (system-architect) · contracts.md v0.4 (§0 Spec.Publishers; C-02 v1.2 abandoned/cause=forget; C-04 v1.1 leader null, 409 no_leader; C-06 v1.1 config.cloud_enabled при старте; C-07 v1.2 validation_status 6 значений; C-08 v1.2 worlds[].llm.cloud_enabled; C-10 v1.1 end_reason forget; §16 п.6 levels.go истина), ADR-017 доп.1, data-model v0.2.1, api-contracts v0.2.1, ownership v0.4, consolidation §14. Решения: З-2 abandoned только из alive, публикует gateway; TL2-6 FakeState v0 публикует replay.completed + MV_SWARM_REPLAY_WAIT. Оркестратор: sed-унификация MV_BOT_ALLOWED_USER_IDS → MV_TELEGRAM_ALLOWED_USER_IDS и MV_BUS_BROKERS → MV_KAFKA_BROKERS по Docs/dev-team. Остальные запросы к ролям (BA, tech-lead#1/2/3, architect#N, devops, QA) — сводной задачей после PM.
- 2026-09-09 · PROJECT · business-analyst (Sonnet): prd/user-stories/nfr v0.5 по сведениям 2–3; metrics.md §4.2 end_reason + forget; open-questions R обновлён.
- 2026-09-09 · PROJECT · project-manager (Opus): plan/roadmap.md — последовательный план на 1 команду, 13 вех (M0 фундамент → M0+ мост → M1 → M2 → M3 I1-α → M4 → M5 → M6 → M7 I1 → M8–M10 группа → M11 → M12 G4 → M13 Should), 156 задач, срок 26–35 нед. (при maxAgentsPerRole=2 −25–30 %); мостовой блок T-214/215/219/220/255 в конец волны 0 (e2e соло нуждаются в FakeEncounter/FakeNarrator); 005-ops ядро перед интеграцией I1; I1-α оставить. plan/risks.md R-01..35; plan/backlog.md (покрытие полное, пробелы П-1..П-10). Примечание оркестратора: З-1/З-2/З-4/З-5 и validation_status уже решены сведением 3 (PM работал параллельно); T-320 разблокирована.
- 2026-09-09 · PROJECT · tech-lead#1 (Opus): сведение 3 внесено в tasks.md EPIC-001/002/003/004 (T-006 Spec.Publishers, T-017 FakeState публикует replay.completed, T-215 validation_status, T-212 config.cloud_enabled при старте, T-237 таймаут ожидания, T-301/302/314/320/352/355 по C-04/C-08/C-10), разделы-указатели в трёх компонентных документах, strategy.md CT-02a/E2E-10; counters.task=393 подтверждён (окна свободных номеров по эпикам).
- 2026-09-09 · PROJECT · Решение оркестратора · T-018 (F-10e): FakeNarrator v0 БЕЗ WithEncounterStub (по сведению 2); e2e заглушек v0 — сценарий без боя; бой на I1-α обеспечивает мостовой блок M0+ (T-214/215/219/220/255) в конце волны 0 по roadmap.md §4.1. Правку T-018 вносит tech-lead при старте подволны 0.7.
- 2026-09-09 · PROJECT · A4 завершён · чек-лист G3 проверен оркестратором: все пункты выполнены (покрытие US — backlog.md; T-NNN с DoD/оценкой/зависимостями — tasks.md × 5; порядок и MVP — roadmap.md; первая задача — T-001 гигиена/инициализация; тесты и документация — задачами; риски — risks.md; стратегия — testing/strategy.md). Сводка G3 показана пользователю.
- 2026-09-09 · PROJECT · G3 УТВЕРЖДЁН пользователем · без замечаний. Решения: режим — второй developer разрешён на независимых (файлово не пересекающихся) задачах в пределах maxParallelAgents=2 (maxAgentsPerRole → 2); T-254 (провайдер Ollama) снята из MVP-1 — llama-server единственный рантайм, реестр провайдеров остаётся; I1-α остаётся вехой M3; S2 — пользователь найдёт двух тестеров с Telegram к вехе M10. Допущение оркестратора: коммиты — один запрос на подволну (рекомендация PM), подтверждается фактом первого запроса. Волна 0: задачи гигиены/каркаса трогают корень репозитория и общий рабочий каталог → выполняются ПОСЛЕДОВАТЕЛЬНО одним developer, второй слот — code-reviewer/tester; параллельные разработчики — с волны 1 на разных пакетах.
- 2026-09-09 · PROJECT · Этап A5 «Разработка» · ветка epic/EPIC-001-foundation от integration/mvp-1; первая задача T-001.
- 2026-09-09 · EPIC-001 · developer#1 (Opus): T-001 выполнена (gitleaks v8.30.1, pre-commit 4.6.2, .gitleaks.toml/.gitleaksignore, .pre-commit-config.yaml, .gitignore/.gitattributes/.env.example/.mcp.env.example, плейсхолдеры в shared/oracle/README.md, .mcp.env и settings.local.json из индекса, .exe удалены, .log из индекса; dev-log.md). Решения оркестратора по замечаниям к DoD: критерий негативного теста хука — «токен, не совпадающий с allowlist-плейсхолдером» (принято); критерий gitleaks — `gitleaks git` по диапазону + `gitleaks dir` по содержимому git ls-files (make secrets-scan в T-008); pre-commit --all-files не зелёный на as-is коде — закрывается T-002/003/004/008. Вопросы пользователю: удалить остатки .claude/worktrees/* (копии ключа и .env); ключ ORACLE_API_KEY отзывает пользователь.
- 2026-09-09 · PROJECT · По решению пользователя удалены с диска 12 мёртвых каталогов .claude/worktrees/* (копии старого README с ключом и .env); frosty-bell (живой worktree ветки claude/frosty-bell) сохранён. Git-ветки claude/* не тронуты.
- 2026-09-09 · EPIC-001 · code-reviewer#1 (Opus): T-001 — ПРИНЯТЬ (Critical 0, Major 0, Minor 5, Nit 6); review.md. Оркестратор: M-1 (комментарий .gitleaksignore) исправлен; M-2/M-3 — критерии DoD переформулированы (см. запись выше), M-4 — плейсхолдер `123456789:AA[x]{33}` остаётся в allowlist только для *.example (уточнить в T-008), M-5 — exclude `^services/_archive/` в pre-commit добавить первым шагом T-002; Nit — в бэклог T-019. Приёмка T-001 по DoD выполнена оркестратором (экономия лимитов; DoD независимо проверен ревьюером). Подволна 0.1 закрыта — запрошен коммит.
- 2026-09-09 · EPIC-001 · Коммит подволны 0.1: 04a3a15 «T-001: repo hygiene …» через pre-commit (gitleaks Passed). Инцидент: хук был записан с искажённой кириллицей в INSTALL_PYTHON → заменён на 8.3-путь C:/Users/CD86~1/…/python.exe (локальный .git/hooks, вне git); ложные срабатывания generic-api-key в review.md (цитата) и .gitleaksignore (комментарий) погашены inline-пометкой и перефразировкой. Для runbook F-9: на машинах с кириллическим именем пользователя `pre-commit install` нужно перезапускать с проверкой INSTALL_PYTHON (или использовать 8.3-путь).
- 2026-09-09 · EPIC-001 · T-002 (F-3 архив заменяемого кода) → developer#1 (Opus). Первым шагом — exclude ^services/_archive/ в .pre-commit-config.yaml (M-5 ревью T-001).
- 2026-09-09 · PROJECT · Указание пользователя · при исчерпании лимита модели агент запускается на ступень ниже (fable → opus → sonnet), работа не останавливается; факт понижения фиксируется в отчёте задачи.
- 2026-09-09 · EPIC-001 · developer#1 (Opus): T-002 выполнена — 60 путей перенесены git mv (100 % rename), services/_archive/{go.mod,README.md}, 15 ARCHIVED.md, 8 FROZEN.md, go.work сокращён, build/Dockerfile → build/legacy.Dockerfile, exclude ^services/_archive/ + always_run для gitleaks в pre-commit; корневые go build/vet/test зелёные, gitleaks 0.
- 2026-09-09 · EPIC-001 · Решения оркестратора по ОВ T-002 (в границах foundation.md §11 и ADR-012; подтвердить architect#1 при следующей ревизии) · ОВ-1: shared/agent/filter.go уходит в архив вместе с shared/rules — в единый модуль по §11 переносятся только типы/парсер/валидатор блупринтов; логика фильтра реализуется заново в EPIC-003 (internal/llm/filter, задача C1). Вариант «вернуть shared/rules» отклонён (противоречит §11 и ADR-012). ОВ-2: narrative-orchestrator (нужен профилю legacy до S5) получает replace на ../../services/_archive/shared/… в T-008 вместе с build/legacy.Dockerfile; rule-engine заморожен (FROZEN.md), действий не требуется. ОВ-3: неработающая as-is сборка образов до T-004/T-008 — ожидаемо, дефект не заводится. ОВ-4: хэш коммита переноса в ARCHIVED.md не проставляется (отдельный коммит ради поля не делаем); ссылка на T-002 и базу 04a3a15 достаточна. ОВ-5: `go test -race` — только в CI (Linux); общий DoD §1 п. 2 читать как «-race в CI, локально без него», внести в tasks.md §1 при следующей правке.
- 2026-09-09 · EPIC-001 · code-reviewer#1 (Opus): T-002 — ПРИНЯТЬ (Critical 0, Major 0, Minor 4, Nit 6); review.md дополнен. Оркестратор внёс: M-1 — exclude перенесён пофайлово на 4 автофиксера, always_run у gitleaks убран (check-added-large-files/check-yaml снова действуют на архив); M-3 — формулировка «Правила» п. 1 в services/_archive/README.md переведена в будущее время с привязкой к T-003/T-004/T-008/T-012; M-2 — в README добавлено правило «возврат только git mv обратно; подкаталоги без go.mod через replace не подключаются» (5 go.mod не создаём). M-4 → назначено в T-003: перенести в архив shared/agent/filter.go И shared/agent/e2e_dark_forest_test.go (оба зависят от shared/rules); Nit N-1..N-6 — в бэклог задач T-003/T-004/T-008/T-012 и T-019. Уточнение: rule-engine не в списке 8 замороженных — он источник для переписывания в EPIC-002 T-064 (формулировка ОВ-2 поправлена).
- 2026-09-09 · EPIC-001 · Коммит T-002: 5a20bb8 (90 файлов, 60 переименований 100 %). Далее T-003 (F-2).
- 2026-09-09 · EPIC-001 · developer#1 (Opus): T-003 (F-2) выполнена — go.work удалён, shared/{eventbus,agent,agent/tools} слиты в корневой модуль (go 1.26, toolchain go1.26.8), созданы shared/clock, shared/runtime, каркас cmd/multiverse с 7 контекстами и /health, .golangci.yml (depguard по ADR-001, forbidigo на os.Getenv/time.Now/log), build/Dockerfile; M-4 исполнено (filter.go и e2e_dark_forest_test.go в архив). DoD: build/vet/lint/tests зелёные, docker build + /health ok, gitleaks 0; -race — в CI (T-012).
- 2026-09-09 · EPIC-001 · Решения оркестратора по ОВ T-003 · ОВ-6: строка `*.go text eol=lf` в .gitattributes подтверждена (без неё gofmt/линтер локально нерабочие; содержимое в индексе уже LF). ОВ-7: снятие временных исключений as-is пакетов из .golangci.yml — явным критерием DoD в T-005 (shared/eventbus) и в задачах EPIC-003 (shared/agent); внести при следующей правке tasks.md. ОВ-8: `make secrets-scan` = gitleaks git по диапазону + gitleaks dir по содержимому `git ls-files` (не по рабочей копии) — зафиксировать в T-008; каталог .claude/worktrees/frosty-bell оставлен по решению пользователя. ОВ-9: сверка `toolchain` в go.mod и GO_VERSION в build/versions.env — в job `unit` (T-012). ОВ-10: тесты shared/entity — в F-10/T-011, как и планировалось.
- 2026-09-09 · EPIC-001 · code-reviewer#1 (Opus): T-003 — ВЕРНУТЬ (Critical 0, Major 1, Minor 8, Nit 11). Major M-1: shared/runtime/http.go AdminOnly пускает по X-Actor-Kind ∈ {ci,operator}, но `operator` — это client_id, а actor_kind оператора = human (C-01/ADR-007 enum human|ci|sim|system) → штатный доступ оператора через прокси gateway получил бы 403.
- 2026-09-09 · EPIC-001 · Решение оркестратора по M-1 (подтвердить system-architect при ревизии C-06/ADR-009 п.9) · админ-доступ решается по КЛИЕНТУ, а не по виду актора: AdminOnly проверяет X-Client-Id против списка MV_CORE_ADMIN_CLIENTS (по умолчанию `operator`), X-Actor-Kind только валидируется на принадлежность enum human|ci|sim|system и не является допуском; при отсутствии X-Client-Id — 403. Итерация 2 T-003 также закрывает Mi-1 (forbidigo на shared/{clock,runtime,env}, а не на весь shared), Mi-3 (depguard fail-closed для новых internal/*), Mi-4 (digest вместо тега nonroot), Mi-5 (HEALTHCHECK из образа убрать — живёт в compose, T-004), Mi-8 (гонка realTicker.stopped). Mi-2, Mi-6, Mi-7 и Nit — в T-004/T-012 и бэклог T-019.
- 2026-09-09 · EPIC-001 · developer#1 (Opus): T-003 итерация 2 — M-1 (AdminOnly по X-Client-Id из MV_CORE_ADMIN_CLIENTS, X-Actor-Kind только валидируется по enum), Mi-1 (forbidigo сужен до shared/{clock,runtime,env}; временные исключения shared/{eventbus,agent,entity} со снятием в T-005/EPIC-003/T-011), Mi-3 (depguard fail-closed + ловушка internal-unlisted, проверено на временных пакетах), Mi-4 (distroless по digest), Mi-5 (HEALTHCHECK убран, живёт в compose), Mi-8 (atomic.Bool в realTicker). Новые ОВ: ОВ-11 (уточнить C-06/ADR-009 п.9 — system-architect), ОВ-12 (снятие forbidigo для shared/entity — критерий DoD T-011), ОВ-13 (Dependabot на digest в build/ — T-004).
- 2026-09-09 · EPIC-001 · Проверка итерации 2 оркестратором (вместо повторного полного ревью, экономия лимитов): AdminOnly читается по клиенту и валидирует enum; Dockerfile — digest, HEALTHCHECK убран; realTicker на atomic.Bool; go build/vet/test -short зелёные (6 пакетов ok), golangci-lint 0 issues. T-003 ПРИНЯТА по DoD. Умолчание MV_CORE_ADMIN_CLIENTS=operator — fail-closed: добавить ci-harness (T-012) и mvctl (T-010) в их DoD.
- 2026-09-09 · EPIC-001 · Коммит T-003: b7df900. Волна 0 продолжается: параллельно developer#1 → T-005 (F-4a shared/eventbus) и developer#2 → T-004 (F-6a build/versions.env, minio.Dockerfile, ядро compose, .dockerignore) — файлово независимы, второй разработчик разрешён пользователем.
- 2026-09-09 · EPIC-001 · developer#2 (Opus): T-004 (F-6a) выполнена — build/versions.env (пины), build/minio.Dockerfile (образ собран из тега, `--version` = RELEASE.2025-10-15T17-29-55Z, mc admin info ok), ядро docker-compose.yml (redpanda, minio, gateway, core, ollama в профиле gpu; все порты на 127.0.0.1), .dockerignore, .github/ci.env, Makefile +image/minio-image; as-is compose перенесён в services/_archive/build/docker-compose.as-is.yml (U-1). ОВ-14: форка minio/minio у пользователя ещё нет — MINIO_REPO указывает на upstream, страховка — тарбол в backups/ (вне git). ОВ-15: infrastructure.md §2.5 дополнить ENV MINIO_RELEASE и аргументом версии gen-ldflags (devops). ОВ-16: CHROMA_IMAGE пустая до T-008. Также: build/legacy.Dockerfile копирует удалённый go.work — починить в T-008; .env владельца нужно пересобрать из .env.example перед первым make up.
- 2026-09-09 · EPIC-001 · developer#1 (Opus): T-005 (F-4a) выполнена — shared/eventbus переписан по C-01 v1.1: конверт meta (NewRoot/Derive), Bus/Journal/Position/PositionFromContext/Dedup, kafka-адаптер (kafka-go v0.4.51), DLQ после 3 повторов, политики топиков, валидация при чтении; правила публикации/чтения вынесены транспортно-независимо (membus в T-014 переиспользует их); исключение shared/eventbus из .golangci.yml снято (ОВ-7 закрыт). Покрытие 69,7 %, lint 0 issues.
- 2026-09-09 · EPIC-001 · Решения оркестратора по ОВ T-005 (нумерация сдвинута — у T-004 уже были ОВ-14..16) · ОВ-17: contracts.Spec встраивает eventbus.TypeSpec (без дублирования Topic/Policy/SchemaVersion) — принято, вход для T-006; подтвердить system-architect при ревизии foundation §6. ОВ-18: проверка `source ∈ Spec.Publishers` остаётся статической (job contracts / mvctl contracts check, T-010) — принято. ОВ-19: shared/eventbus/{MIGRATION.md,docs,examples} и дрейф примеров в CLAUDE.md/AGENTS.md — в T-019 (F-9). Трактовка «повтор ×3» = 4 вызова обработчика, DeadLetter.Attempts=4 — принята, contract-тест T-014 ожидает то же.
- 2026-09-09 · EPIC-001 · code-reviewer#1 (Opus): T-004 — ПРИНЯТЬ (Critical 0, Major 0, Minor 5, Nit 8). Оркестратор внёс правки по замечаниям безопасности: Mi-1 — рекурсивные глобы в .dockerignore (**/*.exe, **/*.log и т.д.); Mi-2 — тома переименованы (redpanda-data, minio-data, gateway-data, ollama-data), чтобы первый `up` не примонтировал данные as-is MinIO со старыми учётными данными; Mi-4 — админ-порт core больше НЕ публикуется (expose вместо ports; доступ через прокси gateway, ADR-009 п. 9 и threat-model T-14; расхождение infrastructure.md §1.4 vs threat-model разрешено в пользу модели угроз — внести в infrastructure.md при следующей правке devops); Mi-5 — MinIO пиннится по коммиту (ARG MINIO_COMMIT=9e49d5e7a648 + fail-closed git rev-parse, MINIO_COMMIT в versions.env). Mi-3 (CHROMA_IMAGE пуста) — решение: оставить пустой, в T-008 у сервиса профиля legacy использовать ${CHROMA_IMAGE:?} (падать громко). Nit — в T-008/T-012. Проверка после правок: `docker compose config -q` ok, публикации только на 127.0.0.1, у core портов нет.
- 2026-09-09 · EPIC-001 · code-reviewer#2 (Opus): T-005 — ВЕРНУТЬ (Critical 0, Major 3, Minor 7, Nit 4). M-1: kafka writer без BatchSize/BatchTimeout → каждая публикация ждёт ~1 с (ломает NFR-001). M-2: ValidateOnRead fail-open (нулевое значение выключает валидацию), C-01/SEC-16 требуют «по умолчанию включено». M-3: NewRoot не заполняет meta.gm_path — после T-006 (`required` в _envelope.json) все события уйдут в dead_letters.
- 2026-09-09 · EPIC-001 · Решения оркестратора по ревью T-005 · M-1: BatchSize=1 и BatchTimeout=10ms явными константами; проверку задержки публикации внести в DoD T-014 (contract-тест). M-2: поле инвертировать в SkipValidateOnRead (fail-closed по умолчанию), умолчание «валидировать» не зависит от вызывающего. M-3: NewRoot проставляет gm_path=agent по умолчанию с возможностью переопределить опцией; ValidateEnvelope требует непустое значение. Mi-1: Subscribe при штатной остановке возвращает nil (единая семантика с membus, contract-тест T-014). Mi-2: Option → DeriveOption по C-01. Mi-3: размер dead letter ограничить (усечение Raw с пометкой), чтобы топик не вставал. Mi-6: расширить forbidigo (time.After/Tick/NewTimer/NewTicker/Since/Until, os.Environ/ExpandEnv) — закрыть здесь. Mi-7: правка shared/runtime принята (поля Bus/Journal были предусмотрены в T-003). Mi-4, Mi-5, Nit — в T-014/T-010 и бэклог T-019.
- 2026-09-09 · EPIC-001 · developer#1 (Opus): T-005 итерация 2 — M-1 (kafkaBatchSize=1, kafkaBatchTimeout=10ms), M-2 (SkipValidateOnRead, fail-closed), M-3 (NewRoot ставит gm_path=agent, ValidateEnvelope требует agent|legacy), Mi-1 (Subscribe/Tail/ReadRange возвращают nil при штатной остановке), Mi-2 (DeriveOption), Mi-3 (MaxDeadLetterRaw 512 КиБ + raw_truncated; DeadLetter.Raw стал []byte — иначе запись не сериализовалась), Mi-6 (forbidigo расширен на time.After/Tick/NewTimer/NewTicker/Since/Until и os.Environ/ExpandEnv). Покрытие 76,1 %, lint 0 issues.
- 2026-09-09 · EPIC-001 · Проверка итерации 2 оркестратором: build/vet/test зелёные (6 ok), покрытие 76,1 %, lint 0 issues, ключевые правки на месте. T-005 ПРИНЯТА по DoD. Требования к смежным задачам: T-014 — проверить фактическую задержку публикации и семантику io.EOF/остановки на брокере; T-007/T-010/T-018 — передавать SkipValidateOnRead = !MV_BUS_VALIDATE_ON_READ; T-006 — в _envelope.json объявить gm_path required с enum agent|legacy. Публичный API шины изменён (Option→DeriveOption, ValidateOnRead→SkipValidateOnRead, DeadLetter.Raw []byte) — коммит помечается как contract-change (contracts.md §16 п. 3).
- 2026-09-09 · EPIC-001 · Коммиты: 59b5af0 (T-004), dfc0498 (T-005, помечен contract-change). Дальше параллельно: developer#1 → T-006 (F-4b-1 shared/contracts + схемы конверта), developer#2 → T-007 (F-5 shared/env, objstore).
- 2026-09-09 · EPIC-001 · developer#1 (Opus): T-006 (F-4b-1) выполнена — shared/contracts (Spec со встроенным eventbus.TypeSpec + Publishers, Lookup/Validate/Topics/All, статичная копия OwnershipRules), пакет schemas с go:embed, _common.json и _envelope.json (gm_path required, enum agent|legacy), 25 схем типов блоков «а» и «б»; legacy-типы внесены как Deprecated без схемы (иначе профиль legacy не опубликует ничего). Покрытие 79,6 %, lint 0 issues, 17 тестов / 107 подтестов. Схемы блока «в» — T-009.
- 2026-09-09 · EPIC-001 · Решения оркестратора по ОВ T-006 · ОВ-20: enum `cause` в схемах C-02 РАСШИРИТЬ до объединения api-contracts §2.3.4 и state-and-mechanics §4.6 (добавить leave, flee, death, resolve, init, author) — иначе State не сможет публиковать штатные причины; правку вносит T-009 или EPIC-002 T-052, в contracts.md/api-contracts фиксирует system-architect (совместимое расширение enum). ОВ-21: proposal_id в entity.create.proposed остаётся опциональным, решение об обязательности — EPIC-002 T-052. ОВ-22: `scope` в payload round.opened не добавлять (конверт уже несёт scope) — правка перечня полей в api-contracts §2.3.3 за system-analyst. ОВ-23: legacy-типы в T-009 не дублировать. ОВ-24: `go mod tidy` — после приёмки подволны 0.3 (общий go.mod, две задачи).
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-007 (F-5) выполнена — shared/env (манифест 55 MV_* + 11 внешних, Declare/Validate/CheckExample, секреты не выводятся), shared/objstore (Put/Get/Stat/List/Delete/EnsureBucket/Capabilities, Memory и minio-go, versioning+ILM при создании бакета, prompts-* без versioning), shared/logging (slog JSON, редакция, BusMiddleware, LogDeadLetter); os.Getenv вне shared/env устранён (runtime.EnvAddr удалён, AdminOnly и cmd/multiverse читают манифест). Покрытие: env 94,6 %, objstore 65,0 %, logging 98,4 %; integration-тест objstore на testcontainers с собранным образом MinIO — зелёный.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ОВ T-007 · ОВ-25: имя переменной — MV_MINIO_USE_SSL (infrastructure.md §4.2 и .env.example — источник истины); foundation.md §7 и design.md §8 поправит devops/architect при ревизии. ОВ-26: MV_CONTEXTS и MV_ID_SOURCE остаются флагами командной строки, в манифест env не вносятся. ОВ-27: env.Validate вызывает cmd/multiverse при старте, но проверяет только переменные, обязательные для ВКЛЮЧЁННЫХ контекстов (ключи MinIO — только если поднят state или memory); полная проверка — `mvctl env check` (T-010). Внести в DoD T-010 и в задачу подключения контекстов EPIC-002. ОВ-28: mvctl и ci-harness добавляют себя в MV_CORE_ADMIN_CLIENTS в своих задачах. ОВ-29/ОВ-24: `go mod tidy` — оркестратор выполнит после приёмки подволны 0.3. Отклонение от foundation §8 (централизованный vars.go вместо Load()/Env.String) принято: иначе `mvctl env check` не увидит переменные неимпортированных пакетов; подтвердить architect#1 при ревизии §8.
- 2026-09-10 · EPIC-001 · code-reviewer#1 (Opus): T-006 — ВЕРНУТЬ (Critical 0, Major 1, Minor 10, Nit 6). M-1: в схемах player.entered_region/left_region/said потеряно общее поле action{type,key_hash} (§2.3.1 даёт его всем player.*), при additionalProperties:false штатный издатель из POST /v1/players/{id}/actions получит отказ.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ревью T-006 · M-1: добавить action{type,key_hash} в три схемы. Mi-1: Validate для deprecated-типов обязан проверять конверт (не возвращать nil сразу). Mi-2: All()/Spec() возвращают глубокие копии Publishers/Consumers (как OwnershipRules). Mi-7: добавить поле Contracts в runtime.Deps (назначено этой задаче ещё в T-003). ОВ-20/Mi-4: enum `cause` в схемах entity.* расширить до объединения api-contracts §2.3.4 и state-and-mechanics §4.6 (+leave, flee, death, resolve, init, author) — делаем здесь, а не в T-009, файлы те же; тест-«часовой» (каждое значение Causes из OwnershipRules есть в enum схем) внести в DoD T-009. Mi-3, Mi-5, Mi-6, Mi-8, Mi-9, Mi-10 и Nit — в T-009/T-014 и бэклог. Предложения ревьюера в бэклог (T-010 проверка Publishers на фикстурах, T-014 бенчмарк Validate и сверка двух ValidateEnvelope, T-052/T-301 version/proposal_id, вопрос по eventbus.Entity к system-architect) приняты.
- 2026-09-10 · EPIC-001 · code-reviewer#2 (Opus): T-007 — ВЕРНУТЬ (Critical 0, Major 1, Minor 10, Nit 6). M-1: objstore.Delete несуществующего объекта — Memory возвращает ErrNotFound, minio возвращает nil (S3 идемпотентен); проверено на живом сервере. Это тот класс расхождений, из-за которого unit-тесты на Memory зелёные там, где продакшен ведёт себя иначе.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ревью T-007 · M-1: принять семантику S3 — Delete идемпотентен (nil на отсутствующий объект), поправить Memory, документацию интерфейса и тесты, ЗАКРЕПИТЬ кейс в integration_test. Mi-1: дефолт --url в health при пустом хосте использовать 127.0.0.1 (сейчас http://:8090 уходит через прокси и даёт 503). Mi-3: добавить chat_id, first_name, last_name, update_id в список чувствительных ключей (threat-model T-06/T-08). Mi-4: MINIO_ROOT_USER/PASSWORD и NEO4J_PASSWORD пометить Required. Mi-5: Validate проверяет тип значения (Bool/Int/Duration), а не только присутствие. Mi-2: быстрый выход в Redact (MatchString перед заменой). Mi-6: пустое значение переменной ДОЛЖНО перебивать непустой дефолт (иначе MV_CORE_ADMIN_CLIENTS= открывает админ-маршруты вместо закрытия). Mi-9: ключ времени привести к `ts` через ReplaceAttr и заполнять `version` в Init. Mi-7: коммит T-007 пометить contract-change (менялся публичный API shared/runtime). Mi-8/ОВ-29: `go mod tidy` — оркестратор после приёмки. Mi-10 (сходимость EnsureBucket и minio-init) — в T-008. Nit — в бэклог T-019/T-012. Замечание ревьюера: решение ОВ-27 (валидация по включённым контекстам) требует расширения API shared/env признаком «нужна контексту X» — внести в объём T-010/EPIC-002.
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-006 итерация 2 — M-1 (action{type,key_hash} в трёх схемах player.* + тест-«часовой» по всем восьми), ОВ-20 (enum cause = объединение §2.3.4 и §4.6 + «часовой» по Causes из OwnershipRules), Mi-1 (Validate для deprecated проверяет конверт через Event.ValidateEnvelope), Mi-2 (cloneSpec: глубокие копии Publishers/Consumers/Policy.ActorKinds), Mi-7 (поле Contracts *contracts.Registry в runtime.Deps). Покрытие 81,4 %, lint 0 issues. Проверено оркестратором — T-006 ПРИНЯТА.
- 2026-09-10 · EPIC-001 · Решения оркестратора · ОВ-30: Deps.Contracts заполняет cmd/multiverse при старте (одна строка) — внести в задачу подключения контекстов EPIC-002; до тех пор контекст, которому нужен реестр, падает в Start. ОВ-31: `action` остаётся разрешённым во всех player.* и обязательным только там, где так было (player.attacked); правило записать в api-contracts §2.3.1 (system-analyst). Факт: коммит T-006 унесёт «хвост» T-005 в shared/runtime/runtime.go (поля Bus/Journal не попали в dfc0498, файл один) — отражено в сообщении коммита.
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-007 итерация 2 — M-1 (Delete идемпотентен по семантике S3, закреплено unit + integration на живом MinIO), Mi-1 (health --url подставляет 127.0.0.1 вместо пустого хоста/0.0.0.0/::), Mi-3 (chat_id, first_name, last_name, update_id в чувствительных ключах), Mi-4 (Required на MINIO_ROOT_USER/PASSWORD, NEO4J_PASSWORD), Mi-5 (ValueKind: Validate и CheckExample разбирают значение), Mi-2 (Redact: 2634→18,5 ns без совпадений), Mi-6 (пустое значение перебивает дефолт; MV_CORE_ADMIN_CLIENTS= закрывает админ-маршруты), Mi-9 (ключ времени ts, поле version). Покрытие env 94,9 %, objstore 65,1 %, logging 98,5 %. Проверено оркестратором — T-007 ПРИНЯТА. Выполнен `go mod tidy` (ОВ-24/ОВ-29): сборка и тесты остались зелёными.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ОВ T-007 итерации 2 · ОВ-30: сведение переменных версии (main.version в Dockerfile vs logging.Version) — в T-012 (CI) добавить второй -X либо свести к одной; отметить в T-004 при следующей правке. ОВ-31: перевод cmd/multiverse/serve.go на logging.Init (сейчас логи процесса без редакции и без ts) — в задачу подключения контекстов EPIC-002, продублировать в DoD T-010. ОВ-32: смена поведения «VAR= больше не даёт дефолт» — упомянуть в migration-guide (T-019).
- 2026-09-10 · EPIC-001 · Коммит 27cabe9 — T-006 и T-007 ВМЕСТЕ (пользователь просил двумя). Причина: хук pre-commit запускает golangci-lint по всему модулю и прячет незастейдженные файлы; при коммите только T-006 дерево не компилировалось (T-007 удалил runtime.EnvAddr, которым пользовался cmd/multiverse), первый коммит упал, файлы остались в индексе и ушли вторым. Сообщение коммита исправлено амендом: перечислены обе задачи, помечен contract-change. Вывод для процесса (внести в T-012 и в tasks.md §1): при взаимозависимых правках общего кода коммитить подволну целиком, либо в hook-конфигурации ограничить golangci-lint изменёнными пакетами.
- 2026-09-10 · EPIC-001 · Подволна 0.3/0.4 продолжается: developer#1 → T-009 (F-4b-2 схемы блока «в», политики топиков), developer#2 → T-008 (F-6b профили compose, redpanda-init/minio-init, Makefile, scripts/compose-lint.sh). Файлово независимы.
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-009 (F-4b-2) выполнена — 31 схема блока «в» (рой, LLM, нарратив, встреча/бой, законы, аналитика, config.cloud_enabled), политики топиков закреплены тестами; реестр покрывает всю карту §2.2 (56 типов + 9 legacy), добавлено поле Spec.Reserved (машинная форма исключения §16 п. 4 для world.law_breach.*). Покрытие 81,5 %, lint 0 issues, 25 негативных фикстур.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ОВ T-009 · ОВ-33: `response_raw` НЕ сохраняется и при `filter_error` (не только при `quarantined`) — приватность важнее отладки, для разбора достаточно incident-записи без сырого текста; C-07 остаётся истиной, ADR-016 п. 3 поправит system-architect. ОВ-34: имя поля — `external_players_ack` (api-contracts §2.3.16 — эталон payload), C-06 v1.1 поправить. ОВ-35: `world`/`scope` в payload аналитики остаются опциональными (конверт их уже несёт). ОВ-36: `rules.change.*` не регистрируем (C-13, эпик E-A); механизм Reserved готов. ОВ-37: `max.message.bytes=4 МиБ` для llm_records внести в TopicSpec в T-010 (единый источник для redpanda-init и `mvctl contracts topics`). ОВ-38: `tick.aborted.reason` и `narrative.output.fallback_reason` остаются строками, сужение до enum — совместимое, позже. Предложение EventRef в _common.json — в бэклог (правка общего файла через system-architect).
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-008 (F-6b) выполнена — 5 профилей compose (gpu/memory/dev/legacy/bot), init-контейнеры redpanda-init (8 топиков, retention 30/90/180, segment.ms=1d, llm_records max.message.bytes=4 МиБ) и minio-init (бакеты, versioning/ILM, сервисный пользователь, сходимость с objstore.EnsureBucket), scripts/compose-lint.sh (6 правил, поймал 10/10 подложенных нарушений), scripts/llm-server.ps1/.sh, Makefile 23 цели, build/legacy.Dockerfile чинён (сборка из архивного коммита e6103e4 через make legacy-src). Проверено на живых контейнерах: полный up ядра зелёный, повторный запуск init идемпотентен; образ narrative-orchestrator профиля legacy собирается.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ОВ T-008 · ОВ-40: консоли MinIO 9001 и Neo4j 7474 НЕ публикуются (строгое прочтение SEC-33); при необходимости локальной отладки — отдельный оверлей build/compose.dev.yml вторым -f, заводить по требованию, не сейчас. ОВ-41: профиль legacy собирается из архивного коммита — принято; уведомить tech-lead#2 (EPIC-003): до стендовой проверки semantic-memory и Chroma путь MV_GM_PATH=legacy считать непроверенным, критерий S5 — с запасным вариантом. ОВ-42: критерий «git grep -i minioadmin пуст» переформулировать — проверяется только единый модуль, compose и scripts; замороженные сервисы, services/_archive и Docs исключены; QWEN.md — в T-019. ОВ-43: PowerShell 7 на машине нет — нужна установка пользователем (winget install Microsoft.PowerShell), иначе make llm-up/llm-health и замер F-8 недоступны; альтернатива — .sh через Git Bash. ОВ-44/ОВ-45: telegram-bot без бинарника (EPIC-004) и make bench без скриптов (T-013) — ожидаемо.
- 2026-09-10 · EPIC-001 · code-reviewer#1 (Opus): T-009 — ВЕРНУТЬ (Critical 0, Major 1, Minor 7, Nit 4). M-1: решение по ОВ-33 не попало в схему — условие запрета `response_raw` было завязано только на `quarantined`, запись с `filter_error` и полным сырым ответом проходила и жила бы 90 дней в llm_records.
- 2026-09-10 · EPIC-001 · Оркестратор внёс правки вместо итерации разработчика (объём — три строки): M-1 — условие расширено до enum [quarantined, filter_error], добавлены два тест-кейса (без текста принимается, с текстом отклоняется); Mi-1 — у analytics.consistency.violated убран лишний издатель testkit/state (по EPIC-005 §7 единственный источник — mvctl). Проверено: тесты shared/contracts зелёные, покрытие 81,5 %, lint 0 issues. T-009 ПРИНЯТА. Mi-2 (условная обязательность response_raw/filter), Mi-3 (SwarmPolicy шире C-01 §2.0 — нужна правка контракта и метка contract-change), Mi-4 (Spec.Reserved отразить в C-01 и foundation §6), Mi-5 (scope в encounter.started vs tick.* — единое правило), Mi-6 (негативные фикстуры combat.decided), Mi-7 ($defs для EventRef), N-1..N-4 — в T-010/T-014, ревизии владельцев (T-052, T-214/T-215, T-301) и к system-architect.
- 2026-09-10 · EPIC-001 · code-reviewer#2 (Opus): T-008 — ВЕРНУТЬ (Critical 0, Major 5, Minor 7, Nit 8). M-1: OLLAMA_ORIGINS добавлен в .env.example, но не объявлен в манифесте shared/env → `go test ./shared/env/` красный (общий DoD §1 п. 2 нарушен). M-2: healthcheck qdrant использует /dev/tcp, а в образе sh=dash → сервис никогда не станет healthy, профиль memory не поднимется. M-3: `make up` возвращает ненулевой код, когда LLM не поднят, вопреки §6.3 (деградация FR-080/NFR-072); без pwsh падает всегда. M-4: правило 3 compose-lint (SEC-14) имеет два подтверждённых ложных пропуска — литеральные пароли в environment списком и в любом якоре кроме x-platform-env; это единственная автоматическая защита в CI. M-5: `.env` не читается ни одной целью — `make llm-up` в чистой оболочке падает на MV_LLM_BIN.
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-008 итерация 2 — M-1 (OLLAMA_ORIGINS в манифесте, тест сверки зелёный), M-2 (healthcheck qdrant на bash -c, доведён до healthy на стенде), M-3 (make up возвращает 0 при недоступном LLM, LLM_STRICT), M-4 (правило 3 compose-lint одним проходом по файлу + фикстуры testdata/compose-lint/bad-*.yml, 4/4 ловятся), M-5 (load_dotenv в целях LLM), Mi-1 (health_code), Mi-2 (ошибка mc admin user add больше не глушится — проверено сценарием, который раньше молча проходил), Mi-3 (секреты в mc через stdin: MC_HOST_local отвергнут — mc не декодирует percent-encoding, пароль со спецсимволами ломал разбор URL), Mi-6 (пин debian и sha256 ONNX). Проверено оркестратором: тесты и lint зелёные, compose config 6/6, линтер 0 на боевом файле и 1 на каждой фикстуре. T-008 ПРИНЯТА.
- 2026-09-10 · EPIC-001 · Коммит eda1b3b (T-008 + T-009, contract-change). Подволна 0.4: developer#1 → T-010 (F-4c cmd/mvctl), developer#2 → T-011 (F-10a shared/entity v2). Файлово независимы.
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-010 (F-4c) выполнена — каркас cmd/mvctl (реестр подкоманд, единый вывод, коды 0/1/2), contracts check (проверки а–д, включая сверку политики типа с политикой топика — первая редакция не ловила главный случай и была переписана), contracts topics (+ --format=rpk как единый источник для redpanda-init), env check, storage init, version; 10 имён зарезервировано за EPIC-002/003/005 с кодом 2. Внесены ОВ-37 (TopicSpec.MaxMessageBytes 4 МиБ для llm_records + константы топиков) и ОВ-28 (MV_CORE_ADMIN_CLIENTS=operator,mvctl), добавлен contracts.KnownSources() для проверки реальности источников. Покрытие 75–94 % по пакетам; integration storage init на живом MinIO зелёный.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ОВ T-010 · ОВ-46: имя подкоманды — `mvctl llm usage` (группа `llm`, действие `usage`); в design.md §4.1 поправить при ревизии. ОВ-47: минимальную реализацию `mvctl privacy scan` (grep по фикстурам) делает T-012 — она нужна job `security`; EPIC-005 (T-139) заменит полноценной. ОВ-48: строку `mvctl blueprint validate blueprints/` вернуть в цель `contracts` вместе с командой — внести в DoD задачи EPIC-003 (A4/T-204). ОВ-49: перевод cmd/multiverse/serve.go на logging.Init остаётся в задаче подключения контекстов EPIC-002. Признак «переменная нужна контексту X» в shared/env — тоже EPIC-002 (потребитель — старт cmd/multiverse). Предложение генерировать redpanda-init.sh из `contracts topics --format=rpk` — в бэклог devops (T-012 или позже).
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-011 (F-10a) выполнена — shared/entity v2 переписан по state-and-mechanics §3: типизированная сущность с оптимистичной версией, четыре операции (set/inc/append/remove) с разбором ошибок и no-op, канонический JSON и state_hash, типизированные геттеры атрибутов, согласование со схемами entity.* в обе стороны, таблица терминальных статусов (alive→abandoned разрешён). Свойства, закреплённые тестами: нет часов, нет записи за спиной вызывающего (ApplyOps на копии, двигает Commit), нет политики (владение и инварианты — internal/state). Покрытие 83,9 %, lint 0 issues; исключение forbidigo для shared/entity снято (ОВ-12 закрыт).
- 2026-09-10 · EPIC-001 · Решения оркестратора по ОВ T-011 · (1) Атрибуты региона: канон — data-model.md §3.2 (`npc_ids[]`, `players_present[]`) плюс `encounter_chance`; фикстуры T-016 следуют ему, таблицу state-and-mechanics §4.10 поправит architect#1. (2) `scope` в атрибутах сущности — канонически ОБЪЕКТ {id,type} (_common.json); строковая форма `solo:{id}` допустима только как сокращение в блупринтах, в фикстурах T-016 — объект. (3) Порядок ключей в CanonicalJSON — сортировка на всех уровнях (принято, порядок §3.3 не является частью контракта). (4) `name` не входит в зарезервированные пути: атрибут `name` и поле Entity.Name — разные вещи (подтвердить architect#1 в ревизии §3.2). (5) HistoryEntry по §3.1 (Version, EventID, ProposalID, At) — принято. (6) No-op по канонической равности, а не DeepEqual — принято (иначе inc -3 и +3 дали бы ложное изменение версии). Предложение свести грамматику путей в shared/jsonpath — в бэклог EPIC-002.
- 2026-09-10 · EPIC-001 · code-reviewer#1 (Opus): T-010 — ПРИНЯТЬ (Critical 0, Major 0, Minor 8, Nit 5). Существенных нет; проверено, что коды возврата, разделение потоков, Spec.Reserved, 8 фикстур политик и отсутствие вывода секретов работают. Оркестратор назначил короткую доработку по Mi-1 (фантом схемы прошлой версии невидим: файлы кладутся в map по типу), Mi-2 (сверка с redpanda-init только по именам и retention — расширить на segment.ms, max.message.bytes, партиции, cleanup.policy), Mi-3 (вывод --format=rpk не готовый скрипт: нет брокера, создание существующего топика даёт ненулевой код), Mi-4 (заметка «правило пропущено» печатается всегда), Mi-6 (knownSources не связан тестом с константами Source*), Mi-8 (описание MV_CORE_ADMIN_CLIENTS противоречит значению). Mi-5, Mi-7 (пометка contract-change для констант топиков и KnownSources — ставится при коммите) и Nit — в бэклог.
- 2026-09-10 · EPIC-001 · code-reviewer#2 (Opus): T-011 — ВЕРНУТЬ (Critical 0, Major 3, Minor 11, Nit 5); находки получены прогоном на модуле-зонде, не чтением. M-1: три способа изменить атрибуты с пустым changed[] (append null, set отсутствующего ключа в null, remove ключа со значением null) — state_hash при этом сдвигается, Version не растёт; ломается гарантия C-02 и пересчёт хэша в audit. Причина: sameCanonical сравнивает значения, но не факт существования пути. M-2: `set members[0]` при отсутствующем members даёт map с ключом "0", а `set tags[0]` над []string теряет весь список; тест согласованности грамматик поймать это не может, потому что jsonpath.navigate тоже разрешает [0] как ключ — обе половины ошибаются согласованно. M-3: `remove inventory[0]` пишет change с new=null, хотя список сдвинулся — потребитель, применяющий changed[] буквально, запишет null в нулевой элемент.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ревью T-011 · M-1: учитывать факт существования пути (oldExists) — «не было» и «было null» перестают совпадать. M-2: отвергать целочисленный токен на map в setIn и нормализовать не-[]any срезы; тесты обязаны проверять ТИП контейнера, а не только чтение по тому же пути; чинить обе половины грамматики (запись здесь, чтение — заявка в EPIC-002 на shared/jsonpath). M-3: для удаления по индексу писать изменение на РОДИТЕЛЬСКОМ пути (old/new — список целиком), чтобы буквальное применение changed[] давало тот же результат. Дополнительно в ту же итерацию: Mi-1 (фантомная запись при append+remove одного элемента), Mi-2 (golden-значения CanonicalJSON/StateHash в тесте), Mi-7 (Commit копирует карту вызывающего), Mi-8 (геттеры epoch, spawned_by, opened_by_event_id, closed_by_event_id), Mi-10 (обратное направление «схема → модель» для entity.create.proposed). Остальные Minor и Nit — в бэклог/EPIC-002; вопросы к architect#1 (ReservedPaths по первому сегменту, HistoryEntry при пустом changed, C-03 ChangesFor/ActorFromEntity) — на ревизию контрактов.
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-010 доработка — Mi-1 (файлы схем по имени файла: лишняя схема прошлой версии теперь видна; проверено подложенным player.looked.v2.json), Mi-2 (TestRPKMatchesTheInitScript сверяет шесть значений по каждому топику; расхождений не нашлось, ловля дрейфа проверена намеренной порчей), Mi-3 (вывод --format=rpk сделан исполнимым: --brokers, -X brokers, `|| true`), Mi-4 (заметка о деградации только когда бакет действительно просил правила), Mi-6 (sources_test.go читает константы Source* через go/ast и сверяет в обе стороны), Mi-8 (описание MV_CORE_ADMIN_CLIENTS приведено в соответствие). Покрытие contracts 94,5 %, storage 96,8 %, shared/contracts 81,7 %; lint 0 issues. T-010 ПРИНЯТА. Флаг --brokers локален для contracts topics — конфликта с планами EPIC-005 нет (подтверждено оркестратором).
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-011 итерация 2 — M-1 (трекер переписан в упорядоченный набор путей: changes(before, after) читает каждый путь из «как было» и «как стало», запись отбрасывается только при hadOld==hasNew и канонической равности; property-тест на 17 наборах «len(changed)>0 ⟺ StateHash сдвинулся»; Mi-1 закрылся тем же решением, хак ревьюера дал бы неверный old), M-2 (setIn отвергает целочисленный токен на map — ReasonIndexOnPath, нормализация не-[]any срезов; тесты проверяют ТИП контейнера), M-3 (remove по индексу отчитывается по родительскому пути; тест применяет changed[] буквально и сверяет StateHash), Mi-2 (golden CanonicalJSON/StateHash), Mi-7 (Commit копирует карту), Mi-8 (геттеры epoch, spawned_by, opened/closed_by_event_id), Mi-10 (обратное направление для entity.create.proposed). Мутационная проверка: снятие каждой правки валит соответствующие подтесты. Покрытие 86,9 %, lint 0 issues. Проверено оркестратором — T-011 ПРИНЯТА.
- 2026-09-10 · EPIC-001 · Коммит fd93a6a (T-010 + T-011, contract-change). Подволна 0.4/0.5: developer#1 → T-012 (F-7 CI), developer#2 → T-013 (F-8 подготовка замера; сам замер — стенд с участием пользователя, требуется PowerShell 7 либо запуск .sh через Git Bash).
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-012 (F-7) выполнена — .github/workflows/go.yml (7 job'ов: unit с -race, integration на testcontainers, e2e, contracts, security, compose-lint, image без push), permissions минимальные, concurrency с отменой, все действия по SHA, версии из versions.env; dependabot.yml (gomod/actions/docker), .github/CODEOWNERS, scripts/coverage-gate.sh; удалён устаревший validate-blueprints.yml; реализована минимальная `mvctl privacy scan` (ОВ-47) с покрытием 86,8 %. Побочно: gitleaks по содержимому индекса давал находку на имя переменной в документе — добавлен узкий allowlist (по значению, только имена переменных проекта), Docs/testdata из проверки НЕ исключены. actionlint 0 замечаний, govulncheck без уязвимостей.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ОВ T-012 · (1) Двойной прогон CI на PR из ветки эпика: сузить триггер `push` до main и integration/** (ветки эпиков покрываются триггером pull_request) — экономия примерно половины минут Actions; внести в этой же задаче. (2) Фильтр путей только на push — принято: пропущенный по фильтру workflow не публикует checks, а required check без публикации блокирует PR навсегда. (3) hadolint на пороге error и замечания к build/minio.Dockerfile (DL4006/DL3062/DL3025 — следствия ADR-021) — в бэклог T-019. (4) `make secrets-scan` должен сканировать содержимое индекса, а не рабочую копию (ОВ-8) — доделать здесь же, если дёшево, иначе в T-019. (5) Настройки репозитория (branch protection, required checks, Dependabot alerts) — действия пользователя после первого зелёного прогона.
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-013 (F-8) подготовительная часть выполнена — testdata/bench/prompts.jsonl (30 промптов: 10 ситуаций «Тёмного леса» × tick/phase2/phase2-group3, схемы 2020-12, статы согласованы с rules/dark-forest.yaml), ops/metrics/bench-matrix.json (E/E+/C/A, пороги NFR-002/090, decision_order E→C→A, 3 прогона), scripts/llm-bench.sh (основной, Git Bash, без jq — curl+python3) и .ps1, ops/metrics/{baseline.md,README.md}, ops/models.txt. Логика .sh проверена сквозным прогоном против макета llama-server (30 запросов → CSV/JSON/таблица) и по всем веткам отказа; .ps1 проверен только разбором AST (PS7 нет). Отклонения: n_per_cell 10 вместо 20 (кэш префикса занижал бы p95), python3 вместо jq, проверка языка по text_paths.
- 2026-09-10 · EPIC-001 · T-013 принята оркестратором без отдельного ревью (стендовая оснастка, не код продукта): синтаксис .sh чист, матрица валидна (4 конфигурации, порядок E→C→A), 30 промптов с равным делением по фазам, отсутствие сервера даёт код 2 и внятное сообщение. Замер остаётся за пользователем: `bash scripts/llm-bench.sh --configs E` в Git Bash с MV_LLM_URL=http://127.0.0.1:1234, три прогона, прислать ops/metrics/bench-*.csv|json. PowerShell 7 для замера НЕ требуется.
- 2026-09-10 · EPIC-001 · code-reviewer#1 (Opus): T-012 — ВЕРНУТЬ (Critical 1, Major 3, Minor 9, Nit 6). C-1: scripts/coverage-gate.sh в индексе с режимом 100644 — на Linux-раннере запуск даёт Permission denied (воспроизведено в контейнере, exit 126), job unit и `make test` сломаны. M-2: решение оркестратора сузить push до main и integration/** не внесено — двойной счёт минут остаётся. M-3: job security на pull_request упрётся в 403 (gitleaks-action читает коммиты PR, а permissions обнуляют pull-requests; плюс комментарии включены по умолчанию). M-4: новый глобальный allowlist в .gitleaks.toml не нужен и противоречит §4.5 п. 1 — находка гасится уже существующим fingerprint, а срабатывала только из-за абсолютного пути в шаге job'а.
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-014 (F-5t, ВОРОТА ВОЛНЫ 1) выполнена — shared/testkit (Versions из build/versions.env, детерминированные помощники, StartRedpanda на testcontainers), testkit/membus (переиспользует Route/Delivery из eventbus), contract-тест из 15 поведенческих проверок ОДНИМ файлом против membus и живой Redpanda; оба прогона зелёные дважды подряд. Вскрыты и починены 3 расхождения: (1) ReadRange за концом журнала висел на брокере до истечения контекста — обрезка по End, иначе догоняющее чтение при replay не заканчивается; (2) Subscribe не закрывал reader — он оставался в consumer group и держал партицию, следующая подписка ждала ребаланс (флак 3,3 с → 60 с); починка убрала и утечку горутин при каждой штатной остановке; (3) семантика Close у membus приведена к kafka (nil, а не ErrClosed). Замер задержки публикации на живом брокере: 4,0 / 9,1 / 17,0 мс при бюджете 300 мс — правка M-1 из ревью T-005 подтверждена. Покрытие: testkit 81,1 %, membus 83,6 %, contract 81,3 %.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ОВ T-014 · (1) Лишний round-trip End в ReadRange принят; вариант с уже известным End — в бэклог EPIC-002. (2) Правки shared/eventbus/kafka.go меняют поведение общего кода — коммит помечается contract-change. (3) containers.go только Redpanda — принято, MinIO уже поднимается в тестах T-007, qdrant/neo4j добавит EPIC-005. (4) Размещение contract-теста в shared/testkit/contract — принято (иначе eventbus знал бы про testkit). (5) Мягкий режим membus без списка топиков — принят, документирован.
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-012 итерация 2 — C-1 (бит исполнения coverage-gate.sh в индексе 100755, вызовы через bash; проверено распаковкой дерева индекса в контейнере), M-2 (push сужен до main и integration/**), M-3 (pull-requests: read, комментарии gitleaks выключены), M-4 (глобальный allowlist откачен, gitleaks запускается с относительным путём — fingerprint из .gitleaksignore снова работает), Mi-1 (убран неиспользуемый security-events), Mi-2 (MINIO_COMMIT в build-args), Mi-3/Mi-4 (сканер видит каноническую форму Telegram-апдейта и выровнен с shared/logging; покрытие 89 %), Mi-5 (второй -X для версии логгера), Mi-6 (ci-harness в MV_CORE_ADMIN_CLIENTS), Mi-7 (make secrets-scan по содержимому индекса), Mi-9 (ошибка ввода-вывода → код 2). actionlint 0, lint 0 issues, тесты зелёные. T-012 ПРИНЯТА.
- 2026-09-10 · EPIC-001 · Оркестратор: у scripts/llm-bench.sh (T-013) был тот же дефект режима — выставлен 100755. Открытые вопросы T-012 приняты: экспорт единого списка чувствительных ключей из shared/logging — за T-139 (EPIC-005); строка MV_CORE_ADMIN_CLIENTS в .github/ci.env расширяется в T-018, если e2e пойдут через compose; правки общих файлов (Makefile, build/Dockerfile, shared/env, .env.example) согласованы как часть волны 0.
- 2026-09-10 · EPIC-001 · code-reviewer#2 (Opus): T-014 — ВЕРНУТЬ (Critical 0, Major 2, Minor 7, Nit 5). ВОРОТА ВОЛНЫ 1 НЕ ЗАКРЫТЫ. Major-1: membus проверяет ctx.Err() только на пустом хвосте — отменённая подписка дочитывает весь хвост; зонд на обеих реализациях: membus 30 вызовов на момент отмены и 100 после возврата, kafka 26 → 26. Major-2: Bus.Close() вообще не входит в контрактный набор — мутация «membus возвращает ErrClosed вместо nil» оставляет весь набор зелёным. Из 6 мутаций рецензента 5 пойманы (подгонки под реализацию нет), задержка публикации на брокере 3,0 мс.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ревью T-014 · Итерация 2 закрывает: Major-1 (проверка ctx.Err() в начале итерации цикла membus + кейс контракта «отмена при непустом хвосте»), Major-2 (Target.Close + кейс «Close → Subscribe вернул nil → Publish вернул ErrClosed» на обеих реализациях), Minor-1 (stop() не выбрасывает результат Subscribe — иначе отказ подписки виден только как таймаут), Minor-2 (перевыдача некоммитнутого события: доставка прервана отменой → новая подписка той же группы получает его снова), Minor-5 (усечение большого тела dead letter на живом брокере), Minor-6 (кейс «валидация выключена → событие доходит до обработчика», иначе перепутанный знак SkipValidateOnRead контракт не поймает), Minor-7 (записать известные расхождения: несуществующий топик и начало журнала при retention), Minor-3/4 (ручка хаоса в Target и запись отклонения от ADR-010 в dev-log), Nit-5 (снизить таймаут кейса — 15 × 60 с не укладывается в бюджет CI 10 минут).
- 2026-09-10 · EPIC-001 · Известный «шум» рабочего дерева: `go work sync` переписал go.mod у 15 legacy-сервисов (`go 1.24` → `go 1.24.0`, свёрнутые блоки require, у части сервисов удалён неиспользуемый kafka-go). Изменения НЕ индексируются и ни в один коммит волны 0 не попали — коммиты собираются пофайлово. Решение отложено до T-020 (приёмка волны): либо отдельный коммит «workspace sync for legacy services», либо откат вместе с переносом остатка сервисов в `services/_archive/` (OQ-A-17: ничего не удалять).
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-015 выполнена — `internal/mechanics` (правила как данные, грамматика формул, детерминированный RNG по ADR-003, инварианты inv-01…inv-10, типы C-03) + `rules/dark-forest.yaml` v0.1 по приложению A PRD. Покрытие 94,9 %, lint 0 issues, 40 тестов и 103 подтеста, `-race` недоступен. `Resolve`/`ChangesFor`/`NPCTarget`/`Invariant.Check` — заглушки для EPIC-002 (T-053/T-054), как и планировалось.
- 2026-09-10 · EPIC-001 · ДЕФЕКТ КОНТРАКТА, найден на T-015 и исправлен оркестратором: `dice.rolled.roll.seed` был `integer`, а `state-and-mechanics.md` §5.6 требовал десятичную строку. Шина декодирует payload в `map[string]any`, где число JSON становится `float64`; измерено: seed 17918835045097094771 читается как 17918835045097095168. Сверка `events_hash_match` при воспроизведении по этому полю не сошлась бы. Правка: схема → `string` с шаблоном `^(0|[1-9][0-9]{0,19})$`, `DiceRolledPayload` → `strconv.FormatUint`, три фикстуры в `shared/contracts/validate_test.go`, уточнение в `api-contracts.md` §2.3.5; добавлен тест «seed через обобщённый разбор остаётся точным». `go test -short ./...` зелёный.
- 2026-09-10 · EPIC-001 · Открытые вопросы T-015 к системному архитектору (в работу на T-020/бридж-блоке): расхождение C-03 и `state-and-mechanics.md` §5.1 в четырёх сигнатурах (`Roll`, `ActorFromEntity`, `ChangesFor`, `DiceRolledPayload`) — реализованы формы §5.1; `NPCTarget` без канала ошибки не отличает «некого кусать» от заглушки; `laws/dark-forest-world.v1.yaml` (EPIC-003) ещё нет, поэтому сверка идентификаторов инвариантов с законами отложена.
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-014 итерация 2 — контрактный набор 15 → 20 проверок, зелёный на обеих реализациях (membus 0,9 с, Redpanda 7,7–10,4 с), интеграционный прогон зелёный дважды подряд. Закрыты Major-1, Major-2 и все Minor/Nit из списка решений. Каждый новый кейс доказан мутацией.
- 2026-09-10 · EPIC-001 · ДЕФЕКТ ОБЩЕГО КОДА, найден кейсом Major-2: `Kafka.Close()` навсегда подвешивал подписку, пойманную между обработчиком и коммитом (4 падения из 4; дамп горутин — `kafka-go/reader.go:907` в `CommitMessages`: канал `commits` буферизован, запрос уходит и после `Close`, отвечать на `errch` уже некому, контекст подписки не отменён). Штатная остановка процесса не завершалась. Починено контекстом `closing` в `kafka.go`; `membus` в той же ситуации возвращал `nil` сразу. Плюс третье расхождение того же класса: `membus` не замечал закрытие шины при непустом хвосте.
- 2026-09-10 · EPIC-001 · Решения оркестратора по открытым вопросам T-014 итерации 2: (1) `membus.SetChaos` оставить — экземпляр шины создаётся на тест, разделяемого стенда нет; в комментарии зафиксировать «только для тестов, не для параллельного использования одной шины». (2) Правку `kafka.go` пометить `[contract-change]` при коммите: публичный API не менялся, но поведение `Close` под идущей подпиской изменилось с зависания на возврат `nil` — это часть контракта C-01. (3) Поведение `ReadRange` левее начала журнала и (5) закрытие шины под работающим `Tail` — заявки architect#1 на уточнение C-01, в бридж-блок волны 1. (4) `ReorderTopics` (ADR-010 п. 4) — потребитель EPIC-003 (ожидание по `correlation_id`), закрепить в `foundation.md` §9.
- 2026-09-10 · EPIC-001 · code-reviewer#1 (Opus): T-015 — ВЕРНУТЬ (Critical 0, Major 1, Minor 7, Nit 4). Major-1: поток RNG не закреплён ни одним тестом — подмена `rand.NewPCG(seed, 0)` на `rand.NewPCG(0, seed)` оставляет все 40 тестов зелёными, потому что ожидания выводятся из той же функции, которую проверяют. На этой константе висят ADR-003 п. 4–5 и сверка `events_hash_match`/`dice_rolled_new = 0` (C-14). Из 16 мутаций 14 убиты; детерминизм проверен на 12 800 сверках в 16 горутинах трижды подряд, 0 расхождений; золотые числа правил сверены с приложением A PRD построчно; 13 враждебных YAML не вызвали ни паники, ни зависания. Правка seed оркестратором признана полной в исполняемой части.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ревью T-015 · Итерация 2 (developer#1): Major-1 — таблица золотых бросков (числа в review.md), закрепляющая поток PCG, разделитель seed и канонизацию `Roll.Formula` (Minor-6); Minor-2 — `Document()` перестаёт течь через `StatsDoc.Flee *int`; Minor-3 — `Load` при нескольких битых записях называет ключ детерминированно (сортировка); Minor-4 — верхняя граница числа кубов в формуле (`200000000d6` грузится молча, один бросок 466 мс); Minor-5 — второй документ YAML отвергается; Minor-7 — обновить устаревшие §7 и §10 п. 3 записи T-015 в dev-log. Документы `data-model.md` §7.3 и `api-contracts.md` §2.3.5 оркестратор поправил сам.
- 2026-09-10 · EPIC-001 · Решение по расхождению C-03 и §5.1 (рекомендация code-reviewer#1 принята): править НАДО контракт, а не код. `Roll` → `(Roll, error)`; `ActorFromEntity(e, enc *entity.Entity) (*Actor, error)` — `Participation`/`LastDamager` суть факты встречи, C-03 v1.1 противоречил сам себе; `DiceRolledPayload(roll, roller entity.Ref)` — схема требует `entity{id,type}`; `ChangesFor` → `([]ProposedChange, error)` в ОБОИХ документах (`entity.Change` из C-03 — факт `entity.updated`, а не предложение ops, C-02; канал ошибки нужен и после T-053). `NPCTarget` → `(*Actor, error)` в C-03 v1.2, иначе `nil` заглушки неотличим от «целей нет» (UC-008 A2). Исполнитель — architect#1 в бридж-блоке волны 1; код T-015 не переделывать.
- 2026-09-10 · EPIC-001 · Снято: замечания developer#2 в записи T-014 («красный `internal/mechanics`», «2 issues в rules.go») относились к временным зондам и мутациям code-reviewer#1, работавшего параллельно. Зонды удалены, дерево сверено по md5 с индексом.
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-015 итерация 2 — `TestGoldenRolls` (пять строк литеральных чисел) закрепляет адрес броска, поток генератора, каноническую форму формулы на проводе и десятичную строку seed. Числа пересчитаны независимо вне Go (SHA-256 BE, PCG-DXSM, отбор грани) и совпали с числами ревьюера, полученными третьим путём. Все пять подмен из ревью роняют тест (перестановка аргументов PCG, разделитель seed, порядок байт, hex на проводе, канонизация формулы). Закрыты Minor-2…Minor-7: глубокая копия в `Document()`, детерминированный порядок ключей в сообщениях `Load`, предел 1000 на кубы/грани/модификатор, отказ второму документу YAML. Покрытие 94,9 %, lint 0 issues.
- 2026-09-10 · EPIC-001 · Правка seed оркестратора внесена в индекс (`schemas/events/dice.rolled.v1.json`, `internal/mechanics/dice_event*.go`, `shared/contracts/validate_test.go`, `api-contracts.md` §2.3.5, `data-model.md` §7.3) — ОВ-3 developer#1 закрыт.
- 2026-09-10 · EPIC-001 · Принято к сведению (в бридж-блок волны 1): фикстуры `shared/contracts/validate_test.go` пишут формулу как `"1d20"`, каноническая форма на проводе — `"d20"`; схема формой не ограничивает, поломки нет, привести при следующей правке C-01. Предел 1000 кубов/граней/модификатора живёт константами пакета — перенести в `state-and-mechanics.md` §5.3 как контракт грамматики для блупринтов EPIC-003.
- 2026-09-10 · EPIC-001 · code-reviewer#2 (Opus): T-014 ревью #2 — ВЕРНУТЬ (Critical 0, Major 3, Minor 5, Nit 2). ВОРОТА ВОЛНЫ 1 ПО-ПРЕЖНЕМУ НЕ ЗАКРЫТЫ. Правка `kafka.go` проверена отдельно на живом брокере и признана верной без замечаний (`Close` завершает подписку и в обработчике, и в коммите; необработанное событие перевыдаётся; повторный `Close` безопасен; гонка «Close во время Publish» — 30 из 30 `ErrClosed`). Не держат три якоря: (1) мутация, воспроизводящая `kafka.go` до правки, роняет набор лишь 4 прогона из 8 — кейс закрывает шину в произвольный момент, а зависает только подписка между обработчиком и коммитом; в dev-log заявлено «4 из 4»; (2) мутация 6 ревью #1 (`return ErrClosed` вместо `nil` в припаркованной ветке `membus.go:292`) снова оставляет все 20 кейсов зелёными — не покрыта самая частая форма остановки: простаивающий потребитель плюс `Close`; (3) `DedupDropsTheRepeatedDelivery` проходит, даже если дубля не было вовсе (на обеих реализациях), то есть пункт DoD про at-least-once набором не держится. Подтверждено: 20/20 на обеих реализациях, покрытие совпадает с заявленным, худший красный прогон ~10 минут укладывается в бюджет.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ревью #2 T-014 · Итерация 3 (developer#2, объём тестовый, ~20–30 строк): Major-1 — сузить окно кейса `Close` до детерминированного (сигнал из обработчика последней строкой, `Close` по сигналу), чтобы мутация «до правки» падала 8 прогонов из 8; Major-2 — две подписки на одной шине (одна на хвосте, одна припаркованная) и один `Close`; Major-3 — считать все доставки повторяемого события до дедупа и требовать не менее двух, а `SetChaos`/`Duplicate` обязаны реально порождать дубль на обеих реализациях; Minor-3 — `if b.stopping(ctx)` вместо `if ctx.Err() != nil` в путях записи dead letter; Nit-2 — утверждение строже, чем «меньше 40»; Minor-5 — поднять таймаут кейса перевыдачи (18,8 с под нагрузкой при пороге 15 с), сохранив общий бюджет.
- 2026-09-10 · EPIC-001 · Решения по открытым вопросам ревью #2: (1) Minor-1 и Minor-2 (`stopping` в `ReadRange`/`Tail` не покрыт; под `Close` membus даёт «3 of 40», Redpanda «40 of 40» — дочитывает буфер до ~`QueueCapacity`=100) — кейс НЕ добавлять, пока C-01 не скажет, какое поведение правильное: набор не может закреплять расхождение. Цифры внести в заявку architect#1 (она принималась на утверждении «дефекта там нет» — утверждение снято). Записать оба расхождения в dev-log как известные и непокрытые. То же для Minor-4: обработчик, игнорирующий отмену и вернувший `nil`, — `membus` коммитит всегда, kafka даёт 3 перевыдачи из 5 (случайный `select` в kafka-go). (2) Предложение коммитить обработанное событие через `context.WithoutCancel` — общий код, решение system-architect, пометка `contract-change`, в бридж-блок волны 1. (3) Собственный интеграционный тест адаптера в `shared/eventbus` — отдельная задача T-394 волны 1 (интеграционных тестов в этом пакете нет вовсе); в T-014 Major-1 закрывается сужением окна.
- 2026-09-10 · EPIC-001 · Создана задача T-394 (волна 1, бридж-блок): интеграционные тесты адаптера Kafka в `shared/eventbus` на живом брокере — жизненный цикл `Close` под подпиской в каждой точке (обработчик, коммит, парковка), перевыдача некоммитнутого, повторный `Close`, гонка с `Publish`. Причина: сейчас в пакете нет ни одного интеграционного теста, и единственным якорем правки `kafka.go` служит контрактный набор из `testkit`, который ловит регрессию не на каждом прогоне. Счётчик задач 393 → 394.
- 2026-09-10 · EPIC-001 · code-reviewer#1 (Opus): T-015 ревью #2 — ПРИНЯТЬ (Critical 0, Major 0, Minor 1, Nit 1). Major-1 закрыт по существу: золотые числа пересчитаны ревьюером третьим путём (SHA-256 BE → PCG-DXSM → `uint64n`), все пять строк и шесть векторов совпали; числа в тесте — литералы, ни `Seed`, ни `NewRNG`, ни `String()` в теле нет; каждая из четырёх колонок ожиданий проверена мутацией одной цифры — мёртвых колонок нет; две подмены роняют ровно новую таблицу и ничего больше. Minor-2…Minor-7 закрыты и проверены экспериментом (утечка — зонд по семи ссылочным полям; порядок ключей — 200 прогонов; границы предела — 20 выражений и 7 подстановок через `Load`, стоимость самой широкой разрешённой формулы 2,7 мкс на бросок против 466 мс до правки). Регрессия детерминизма 800 сверок — 0 расхождений. Покрытие 94,9 %.
- 2026-09-10 · EPIC-001 · Minor-8 закрыт оркестратором сразу: константное слагаемое проверки не имело границы и молча переполнялось — `d20 >= 9223372036854775807 + living_enemies` разбиралось, порог сворачивался в −9223372036854775808, и проверка становилась всегда истинной; `Load` принимал такой файл молча, хотя весь его контракт — «жаловаться здесь, а не в бою». Введена `maxTermConst = 1000` (та же величина, что у остальных чисел грамматики), четыре отказа в таблицу `TestParseCheckRejects` и `TestCheckConstBounds` с проверкой через `Load` на настоящем файле правил. Проверено мутацией: со снятой границей падают все пять новых кейсов, с границей — покрытие 94,9 %, lint 0 issues.
- 2026-09-10 · EPIC-001 · T-015 ПРИНЯТА (итерация 2 + правка Minor-8). Nit-1 (строка `3d6+2` в золотой таблице бросается с `PurposeHit`, читалось бы ровнее с `PurposeDamage`) — в бэклог, попутно при следующей правке файла.
- 2026-09-10 · EPIC-001 · developer#2 (Opus): T-014 итерация 3 — закрыты все шесть пунктов, три Major доказаны мутациями. Существенное отклонение от плана правки Major-1: одного сужения окна не хватило. Замер показал, что `k.Close()` закрывает читателей последовательно (порядок обхода карты случаен), и если первым закрывается припаркованный читатель, его выход из группы занимает ~80 мс — за это время коммит подписки с хвостом успевает пройти. С одним читателем мутация роняла набор 4 из 4, с двумя — 3 из 4. Добавлено второе утверждение: счёт вызовов обработчика после входа в `Close` с допуском одно событие в полёте (исправный адаптер — 3 и 3 в шести прогонах из шести, под мутацией 3 → 6…7). Вместе два утверждения дают 8 из 8. Набор 20/20 на обеих реализациях, интеграционный прогон зелёный дважды подряд, покрытие `contract` 81,2 → 81,4 %.
- 2026-09-10 · EPIC-001 · Решения оркестратора по открытым вопросам итерации 3: (1) Minor-3 (`Close` под падающим обработчиком: `membus` жалуется «bus is closed» при записи dead letter, kafka возвращает `nil`) сознательно остаётся без якоря — детерминированный кейс требует третьей подписки с блокировкой на последней попытке, это ~25 строк сверх объёма и заново ставит под вопрос якорь 8 из 8; правка внесена, форма кейса выписана в dev-log. Создана задача T-395 волны 1 рядом с T-394: обе про одну точку жизненного цикла. (2) Последовательное закрытие читателей — на исправном коде не дефект (`stopReaders` отменяет контексты всех циклов до того, как тронут первый читатель), но остановка с N подписками стоит N сетевых выходов из группы подряд, при 10 подписках ~0,8 с: заявка system-architect на NFR о времени штатной остановки, бридж-блок волны 1. Счётчик задач 394 → 395.
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-016 выполнена — фикстуры мира «Тёмный лес» (6 сущностей), указатель `latest.json` и сам объект снапшота seq 0, тест на 11 функций и 13 подтестов в `test/fixtures/`. Все производные числа выводятся, а не повторяются: статы и трофей сверяются с `Rules.Stats`/`Rules.Loot` целиком (новый стат в правилах не проедет непроверенным), проекции региона — с позициями сущностей, `state_hash`, `size_bytes`, `entities_count`, `applied_proposals` пересчитываются. Тест проверен десятью подменами. Privacy-scan по `testdata/` зелёный, 13 файлов. Дефектов в принятой T-015 сверка не вскрыла.
- 2026-09-10 · EPIC-001 · Тест фикстур живёт в `test/fixtures/`, а не в `testdata/`: каталог `testdata/` инструменты Go игнорируют и тест бы не запускался, а новый пакет под `internal/` упёрся бы в правило `internal-unlisted` в `.golangci.yml` (импорт `internal/mechanics` запрещён), то есть потребовал бы правки общего файла. Каталог `test/` уже назначен планом под сквозные тесты T-018. Пакет состоит только из `_test.go`, `go build ./...` его пропускает.
- 2026-09-10 · EPIC-001 · Решения оркестратора по открытым вопросам T-016: (1) `snapshot.created.v1.json` остаётся закрытой схемой (`additionalProperties: false`) — событие строится из указателя снятием трёх полей (`rules_version`, `entities_count`, `reason`), которые живут только в указателе; закрепить фразой в §4.4, схему не расширять: принцип fail-closed важнее удобства. (2) `size_bytes` есть только в указателе — объект не может назвать собственную длину, не изменив её; фраза в §4.4. (3) Объект снапшота сверх списка §4.10 — ПОДТВЕРЖДЁН и вносится в таблицу: без него порядок чтения «указатель → объект по ключу → сверка `state_hash`» не проходит и `size_bytes` нечем проверить. (4) `encounter_chance: 0.25` остаётся предварительным, владелец числа — блупринт `domain-dark-forest` EPIC-003; тест проверяет только принадлежность интервалу. (5) Имя мира «Мир Тёмного леса» принято. (6) Формат `blueprint_ref` сверить, когда EPIC-003 напишет блупринты. Правки §4.4 и §4.10 — architect#1, бридж-блок волны 1.
- 2026-09-10 · EPIC-001 · code-reviewer#1 (Opus): T-016 — ПРИНЯТЬ (Critical 0, Major 0, Minor 2, Nit 5). Заявление «производные числа выводятся» проверено буквально и подтверждено: в теле теста нет ни одной константы, повторяющей правила или фикстуру. Сверка статов идёт целиком структурой — доказано зондом: по-настоящему добавленный стат роняет три подтеста. Из 17 мутаций фикстур непойманной оказалась одна. `state_hash` независим в обе стороны; набор зелёный и на LF-дереве, и на CRLF-дереве; privacy-scan чист; схема снапшота закрыта и не менялась с T-006; размещение теста границ импортов не ломает, он попадает в задание CI `unit`.
- 2026-09-10 · EPIC-001 · Оба Minor T-016 закрыты оркестратором. Minor-1: `snapshot.key` был единственным производным полем, оставленным литералом — сдвиг `taken_at` на 5 марта при неизменном имени файла оставлял набор зелёным, хотя указатель говорил одну дату, а объект другую. Ключ теперь выводится из `taken_at` и `seq` (§4.3), а объект читается по ключу из указателя, как его читает потребитель. Проверено мутацией ревьюера: тест падает с внятным сообщением. Minor-2: `.gitattributes` не нормализовал `*.json`, и после обычного `git checkout` на Windows объект снапшота весил 4686 байт против обещанных указателем 4531 — потребитель read-model, который не вызывает нормализацию теста, получил бы не тот файл. Добавлена строка `testdata/fixtures/** text eol=lf`, выполнен `git add --renormalize`; проверено удалением и повторной выкладкой файла: на диске 4531 байт, возвратов каретки нет.
- 2026-09-10 · EPIC-001 · T-016 ПРИНЯТА. В бэклог из ревью: правило `depguard` для `test/**` (каталог не покрыт ничем, с приходом сквозных тестов в подволне 0.7 это станет дырой), развернуть причины в сообщении `contracts.Validate`, один источник словарей `data-model.md` §3.1; Nit-1…5 (закрытый словарь `weather`, no-op `DisallowUnknownFields` при разборе в карту, длина имени игрока, молчаливый пропуск сверки `laws_version`, `test/**` в `ownership.md` §1).
- 2026-09-10 · EPIC-001 · code-reviewer#2 (Opus): T-014 ревью #3 — ВЕРНУТЬ (Critical 0, Major 1, Minor 0, Nit 3). ВОРОТА ВОЛНЫ 1 НЕ ЗАКРЫТЫ, причина одна. Якорь мутации H не детерминирован: 35 полных прогонов под мутацией дали 6 зелёных (17 %), а не заявленные «8 из 8». Механизм показан зондом: все зелёные — `Close` 20,6 / 39,1 / 51,6 мс и `after = before + 1` (внутри допуска «одно событие в полёте»), все красные — `Close` ≥ 61 мс. Допуск стоит 25 мс работы обработчика, а `Close` на брокере замерен в диапазоне 20,6…193 мс: порог допуска лежит внутри рабочего разброса, в dev-log была узкая выборка 72–111 мс. Major-2 закрыт (мутация 6 ревью #1 — 8 красных из 8), Major-3 закрыт (3 из 3 и 2 из 2). Ложных красных на исправном коде не найдено за 33 прогона, включая 14 под нагрузкой. Цифры автора по покрытию, времени и бюджету подтверждены.
- 2026-09-10 · EPIC-001 · Решение оркестратора по ревью #3 · Итерация 4 (developer#2), с ЯВНЫМ УСЛОВИЕМ ВЫХОДА: основной вариант — сделать якорь детерминированным по построению, а не по времени: обработчик сигнального события блокируется до возврата `Close`, утверждение становится точным (`after == before`). Это безопасно: `Close` (`kafka.go:311-341`) не дожидается горутин подписок, он отменяет контексты и закрывает читателей, поэтому блокировка обработчика его не запирает; мутант же после разблокировки дочитывает буфер читателя (тот же механизм, что даёт «40 из 40» в известном расхождении по `Tail`), поэтому расходится с исправным кодом при любой скорости машины. Запасной вариант — пауза 2–3 мс на событие при хвосте 200–400 событий (порог уезжает в 7 раз ниже самого быстрого замеренного `Close`). Доказательство обязательно: не менее 30 прогонов под мутацией H — все красные, и не менее 20 прогонов на исправном коде, включая прогоны под нагрузкой, — все зелёные. ЕСЛИ ПОСЛЕ ИТЕРАЦИИ 4 ЯКОРЬ ВСЁ ЕЩЁ НЕ ДЕТЕРМИНИРОВАН — принимается третий вариант ревьюера: якорь переносится в T-394 (интеграционный тест адаптера с повторами), а из dev-log снимается утверждение, что кейс `Close` держит правку `kafka.go`; итерации 5 не будет. Попутно: Nit-1 (ограничить ожидание в `stop`, иначе зависшая подписка стоит не 15 с, а всего прогона), Nit-2 (своя группа потребителей для припаркованной подписки), Nit-3 (поправить устаревшее утверждение о кандидате на флак).
- 2026-09-10 · EPIC-001 · developer#1 (Opus): T-017 выполнена — `shared/testkit/state.FakeState` v0 и `shared/testkit/mechanics.FixedMechanics`. Покрытие 87,2 % и 93,2 % при пороге 70 %. Совпадение сигнатур с C-03 доказано не утверждением, а подменой: тест-потребитель объявляет интерфейс на своей стороне и прогоняет один и тот же ход и на заглушке, и на загруженных правилах — на первой ход проходит, на вторых приходит `ErrNotImplemented`. Совпадение с C-02 проверено проекцией потребителя, построенной только на реестре и путях события, без единого упоминания `testkit`, плюс сверкой, что `testkit/state` числится издателем всех четырёх типов. Двенадцать подмен прогнаны фактически, каждая красит ожидаемый тест. Заглушка сознательно не делает: проверку уровня и инвариантов, персист сущностей, догон журнала, дедуп по последнему изменению, публикацию `snapshot.created`.
- 2026-09-10 · EPIC-001 · Решение оркестратора по блокеру T-017: правило `shared` в `.golangci.yml` запрещало `shared/testkit/mechanics → internal/mechanics`, но заглушка обязана говорить типами C-03, иначе она и настоящие правила не удовлетворяют одному интерфейсу и смысл двойника исчезает. Введено узкое исключение по образцу правил `internal-*`: каталог `shared/testkit/mechanics/**` вынут из правила `shared` и получил собственное правило `shared-testkit-mechanics` с `list-mode: lax` и единственным разрешением `internal/mechanics`. Граница проверена зондом: файл в `shared/objstore`, импортирующий `internal/mechanics`, линтер по-прежнему отвергает с прежним текстом. ОСТАТОЧНЫЙ РИСК, вынесен на ратификацию architect#1 в бридж-блоке: контекст может дотянуться до `internal/mechanics` через двойник (`internal/gateway → shared/testkit/mechanics → internal/mechanics`), потому что запрета на импорт `shared/testkit` вне тестов у нас нет и depguard не умеет отличать тестовые файлы. Альтернативы (перенос двойника в `internal/mechanics/testkit`, подъём типов C-03 в `shared/`) ломают `ownership.md` §1 либо меняют C-03 — выбор за архитектором.
- 2026-09-10 · EPIC-001 · Открытые вопросы T-017 в бридж-блок волны 1: (1) `Outcome.Loot` нечем заполнить — трофей берётся по виду существа, а `mechanics.Actor` несёт только идентификатор и тип; настоящая реализация T-053 столкнётся с тем же, развилка «поле `Actor.Kind` (совместимое дополнение C-03)» против «отдельный аргумент». (2) `details.batch_size` требуется §4.5 п. 9, но схема `entity.update.rejected.v1.json` закрыта и такого поля не знает — поправить документ или схему. (3) `contracts.md` §17 называет двойник механики в `shared/testkit/state`, тогда как дизайн, задача и карта владения — в `shared/testkit/mechanics`; поправить §17. (4) Константа пути трупа `loot_claimed_by` отсутствует в `shared/entity/types.go`, три остальных есть. (5) Формат идентификатора прогона в `analytics.replay.completed` не закреплён. (6) Заглушка не проверяет, что `abandoned` предлагает именно шлюз (это `level_violation`, вне матрицы v0) — если рою и сквозным тестам это важно, нужна шестая причина либо ограничение в C-02 §17.
- 2026-09-10 · EPIC-001 · code-reviewer#1 (Opus): T-017 — ВЕРНУТЬ (Critical 0, Major 1, Minor 5, Nit 3). Ядро задачи сделано честно: подмена C-03 держится компилятором и одним потребителем, прогнанным на обеих реализациях (дрейф на один аргумент ломает сборку — проверено); матрица из пяти причин закреплена шестью мутациями поштучно; `atomic=true` мир не двигает; сигнал старта валидируется реестром, а не тестом. Major-1: два набора изменений на одну сущность в одном предложении — первый молча теряется. Зонд: два набора по `player-A` (`inc hp -1`, `inc hp -2`) дали hp 8, версию 2 и ДВА факта с одной версией, первый из которых сообщает 10→9, второй 10→8; нарушены C-02 (версия строго +1 на сущность) и §4.5 п. 12 (один факт на сущность). Ветка достижима через шину: схема повтор сущности в `changes[]` не запрещает.
- 2026-09-10 · EPIC-001 · Защита `no-testkit-in-production` внесена оркестратором по рекомендации ревьюера — она закрывает остаточный риск исключения для двойника механики. `ownership.md` §1 уже называл `cmd/multiverse/fake_contexts.go` «единственным импортом `shared/testkit` в production-бинарнике», то есть запрет подразумевался, но записан не был. Добавлено правило depguard для `**/internal/**` и `**/cmd/**` с единственным исключением по этому файлу плюс исключение по пути `_test\.go$` — та же пара «правило и исключение», что уже используется для `shared/(clock|env)` и forbidigo. Проверено зондами: производственный файл в контексте, импортирующий двойник, отвергается с внятным текстом; тестовый файл того же пакета не отвергается; на всём модуле по-прежнему 0 issues. Возражение «depguard не отличает тесты» снято. Ратификация ADR-001 — за architect#1.
- 2026-09-10 · EPIC-001 · Решения оркестратора по ревью T-017 · Итерация 2 (developer#1): Major-1 — отказ `invalid_op` при повторе сущности в одном предложении (молчаливая потеря недопустима; настоящий State в T-056 столкнётся с тем же, вопрос ратифицирует архитектор; выразить ограничение схемой нельзя — JSON Schema не умеет «уникальность по вложенному полю»); Minor-1 — закрепить порядок «объект, потом указатель» декоратором хранилища, а не именем теста; Minor-2 — сигнал старта после подписки, а не до; Minor-3 — режим сверять с литералом `recovery`, а не с собственной константой; Minor-4 — предложения чужого мира не применять (зонд изменил игрока предложением с другим миром); Minor-5 — задействовать `entity.StatusTransitionAllowed` из T-011 и проверять причину `forget`, а зазор по издателю (`level_violation` вне матрицы v0) записать явно; Nit-2 — золотой вектор на таблицу исходов, иначе перестановка строк молча изменит запись сценария T-018. Nit-1 закрыт оркестратором: `ownership.md` §1 дополнена подпакетом `…/mechanics`.
- 2026-09-11 · EPIC-001 · developer#1 (Opus): T-017 итерация 2 — исправлены все семь обязательных пунктов плюс Nit-3, каждая правка закреплена мутацией (девять мутаций, каждая красит ожидаемый тест). Major-1: отказ `invalid_op` один на пакет, до взятия замка и независимо от `atomic`; пояснение ушло в лог, потому что `details` в схеме отказа закрыт для новых полей — открытый вопрос ревью «дешевле закрыть в схеме» закрывается отрицательно. Minor-4: выбран пропуск без факта и без отказа, потому что отказ был бы ответом от имени чужого State и попал бы к потребителям другого мира как настоящий; курсор при пропуске двигается. Minor-5: задействован `entity.StatusTransitionAllowed` из T-011, `abandoned` требует причины `forget`, шестая причина не введена, зазор по издателю записан в коде и в dev-log. Покрытие `state` 87,3 %, `mechanics` 93,2 %.
- 2026-09-11 · EPIC-001 · Отклонение по Minor-2, вынесено на оценку ревьюеру: строгий порядок «группа зарегистрирована → сигнал» средствами C-01 недостижим — `Subscribe` блокируется до конца подписки, а момент регистрации группы находится внутри невернувшегося вызова. Реализован опциональный интерфейс `readySubscriber`: шина, знающая момент регистрации, отчитывается каналом; `membus` его не реализует, там названа и закреплена зондом гарантия «новая группа читает с первого офсета». Дополнительно `Start` теперь возвращает ошибку подписки вместо публикации сигнала поверх сломанной. Предложение в C-01 (канал готовности в `Subscribe` либо чтение State по офсетам через `Journal.Tail` с закреплённым офсетом) — заявка architect#1 в бридж-блок волны 1.
- 2026-09-11 · EPIC-001 · `pre-commit` для файлов T-017 прогнан оркестратором (исполнитель не нашёл его в PATH и сделал ручные эквиваленты): все восемь хуков Passed, код возврата 0. Пробел готовности закрыт фактическим прогоном, а не заменой.
- 2026-09-11 · EPIC-001 · В бэклог из итерации 2 T-017: правило «одна сущность — один набор изменений» записать явным пунктом в `state-and-mechanics.md` §4.5, чтобы T-056 не выводила его из журнала заглушки; константа `loot_claimed_by` по-прежнему отсутствует в `shared/entity/types.go`.
- 2026-09-11 · EPIC-001 · developer#2 (Opus): T-014 итерация 4 — якорь мутации H детерминирован на практике: 34 прогона итогового кода под мутацией дали 34 красных (30 обычных и 4 под нагрузкой), плюс 60 из 60 на двух промежуточных версиях, итого 94 из 94; на исправном коде 20 зелёных из 20, из них 8 под нагрузкой; отдельный замер дал 0 дополнительных вызовов обработчика в 64 измерениях из 64. Условие выхода не наступило, перенос якоря в T-394 не потребовался.
- 2026-09-11 · EPIC-001 · ВАЖНО: разбор механизма, положенный оркестратором в основу решения, оказался НЕВЕРЕН, и исполнитель это доказал, а не обошёл. Предпосылка подтвердилась (`Close` не ждёт горутин подписок), но вывод «после разблокировки мутант дочитает буфер читателя» неверен: цикл упирается не в чтение, а в коммит. После возврата `Close` коммит пройти не может — цикл коммитов при отмене поколения разбирает очередь, отвечает всем и выходит, а `Close` дожидается именно его; `CommitMessages` выбирает из двух готовых ветвей: отправка в буфер, где отвечать уже некому (цикл встаёт навсегда), либо ошибка закрытого канала, которую шина считает штатной остановкой и возвращает `nil`. Это ровно половина на подписку — хуже наблюдавшихся ревьюером 0,83. Настоящая причина зелёных прогонов тоже не в скорости `Close`: цикл разбирает хвост ровно пока ЕГО читатель ещё в группе, а `Close` закрывает читателей последовательно в порядке обхода карты, поэтому подписка, чей читатель закрыт первым, останавливается мёртвым читателем и молчит при любом `Close` (зелёный прогон зонда: `Close` 161,9 мс, последний вызов через 0,5 мс после входа).
- 2026-09-11 · EPIC-001 · Итоговое решение исполнителя: три подписки на хвосте, каждая в своей группе, плюс припаркованная. Первым может закрыться только один читатель, у остальных впереди целое закрытие читателя, и требование к машине сведено к одному свойству транспорта: закрытие одного читателя дольше двух событий работы обработчика, то есть дольше ~9 мс при типичных 40–90 мс (выход из группы идёт по сети, на медленном раннере свойство усиливается). Запасной вариант применён вместе с основным, а не вместо. Исполнитель честно НЕ утверждает «детерминирован по построению»: строго детерминированного утверждения, отличающего исправный код от мутанта без обращения ко времени, в границах C-01 не существует — единственный кандидат «контекст обработчика отменяется при `Close`» был бы расширением контракта; заявка system-architect в бридж-блок волны 1. Попутно закрыты Nit-1…3, набор стал быстрее (9,6–12,7 с против 13,3 с), покрытие `contract` 81,4 → 81,9 %.
- 2026-09-11 · EPIC-001 · code-reviewer#1 (Opus): T-017 ревью #2 — ПРИНЯТЬ (Critical 0, Major 0, Minor 3, Nit 2). Major-1 закрыт: зонд повторён — два набора по одной сущности в обоих режимах дают ноль фактов, один отказ, версию и хеш без движения; законное предложение с тремя разными сущностями по-прежнему проходит целиком. Шесть остальных замечаний закрыты и держатся мутациями (13 мутаций итерации 2: одиннадцать красят ожидаемое). Ослабления прежних тестов от рефакторинга помощников не найдено, подмена C-03 по-прежнему держится компилятором.
- 2026-09-11 · EPIC-001 · Поправка ревьюера к формулировке итерации 2: «недостижимо средствами C-01» верно лишь наполовину — момент регистрации группы невыразим через `Bus`, но у C-01 есть журнальная половина (`End`/`Tail`/`ReadRange`), которую реализуют ОБЕ шины, и связка «закрепить офсет → просигналить → читать с него» окна потери не имеет вовсе и правки контракта не требует. Кроме того, адаптер Kafka подписывается с первого офсета, то есть свойство «новая группа читает с начала» принадлежит обеим реализациям, а не только шине в памяти. Рекомендация ревьюера архитектору (принимается как основной вариант): признать это свойство гарантией C-01 и закрепить контрактным тестом — тогда рукопожатие не нужно никому и `readySubscriber` удаляется целиком; запасные варианты — поднять интерфейс готовности в `shared/eventbus` (если появится брокер без свойства) или добавить канал готовности в `Subscribe` (самый дорогой). В бридж-блок волны 1.
- 2026-09-11 · EPIC-001 · Три новых Minor T-017 переданы developer#1 вместе с T-018 (тот же код, тот же исполнитель, отдельная итерация не нужна): Minor-6 — тест порядка «подписка, затем сигнал» красит собственную мутацию лишь 2 раза из 100, доказательство держится на удаче планировщика, нужны ворота в тестовой шине; Minor-7 — в dev-log записана гарантия, которой нет: `Start` сообщает ошибку подписки 0 раз из 300, сигнал уходит 300 из 300, потому что в запасной ветке канал готовности закрывается до подписки — сузить формулировку; Minor-8 — движение курсора над чужим миром в коде верно, но мутация «перенести обновление курсора ниже фильтра» оставляет пакет зелёным. Nit-4 (ветка выбирается проверкой типа, смена сигнатуры пройдёт молча) и Nit-5 (комментарий приписывает платформе брокер, стартующий с конца журнала) — туда же.
- 2026-09-11 · EPIC-001 · T-017 ПРИНЯТА.
- 2026-09-11 · EPIC-001 · РЕШЕНИЕ по неподтверждённому пункту T-018: решение сведения 2 подтверждается, `WithEncounterStub` в F-10 не делается. Действующая редакция контракта называет единственной заглушкой боя Phase 1 `FakeEncounter` из EPIC-003, а дизайн прямо перечисляет риск «`WithEncounterStub` прижилась» — строить второй боевой двойник, который потом придётся удалять, значит навлекать этот риск сознательно. Состав T-018 сокращён до `Harness` v0 и `FakeNarrator` v0 (только нарратив) плюс сквозной тест без боя; боевой сквозной тест с 30 `narrative.output` переносится в I1-α волны 1, сразу после T-219/T-220, которые и так стоят первым блоком. Правка внесена в `tasks.md` оркестратором, прежний текст сохранён для истории.
- 2026-09-11 · EPIC-001 · developer#1 (Opus): T-018 выполнена в сокращённом составе плюс закрыты все пять замечаний ревью #2 T-017. Minor-6: ворота в тестовой шине подняли долю красных под мутацией с 7 из 100 до 100 из 100, на исправном коде 0 из 100; ложных красных окно дать не может, потому что при исправном коде `Start` за воротами не проходит вообще. Minor-7: код не менялся, сужена формулировка — ошибку подписки вместо сигнала `Start` возвращает только на шине, реализующей интерфейс готовности, а такой в дереве нет; зонд ревьюера приведён рядом. Minor-8: курсор закреплён подачей события с позицией, мутация «перенести обновление курсора ниже фильтра» теперь валит тест. Nit-4: допущение проверяется рефлексией по имени метода, а не приведением к интерфейсу, поэтому падает и случай «метод завели с другой сигнатурой».
- 2026-09-11 · EPIC-001 · Решение исполнителя по `combat.decided`, принимается: подписку в рассказчике v0 НЕ объявлять. В Phase 1 единственный издатель — `FakeEncounter`, которого ещё нет, поэтому ветка была бы кодом, до которого не дотягивается ни один тест; объявленная, но не питаемая подписка — тот же класс «гарантии, которой нет», из-за которого возникли Minor-6 и Minor-7. Отсутствие названо вслух и закреплено тестом, который падает и при появлении типа в таблице, и при его пропаже. Строку добавит тот, кто принесёт издателя (EPIC-003 T-220), одним изменением.
- 2026-09-11 · EPIC-001 · Существенное свойство `Harness` v0: каждое действие, предлагающее изменение, ждёт ответа State по своему `proposal_id` и возвращает отказ ошибкой. Это не удобство теста: `expected_version` обязателен по ADR-013 п. 1, а версию харнесс знает только из фактов, которые читает подпиской. Прямое следствие закреплено тестом — изменение, о котором харнесс ещё не услышал, даёт `version_conflict`, ровно как у настоящего шлюза с его моделью чтения. Известное ограничение v0: настоящему шлюзу блокироваться нельзя (HTTP отвечает 202), для нагрузочного переиспользования в EPIC-004 понадобится неблокирующий режим.
- 2026-09-11 · EPIC-001 · Решение оркестратора по расхождению документов (открытый вопрос T-018 №1): `player.looked` порождает нарратив вида `entry`, а не `turn`. Дизайн §5 и задача говорят `entry`, блок «Заглушка» C-05 говорит `turn`; по существу прав дизайн — осмотр не является ходом боя, а `turn` в C-05 описывает исход схватки. Исполнитель сделал `entry`. Правка C-05 — architect#1 в бридж-блоке волны 1, чтобы EPIC-003 не переоткрывал вопрос при замене заглушки. Вопросы №2 (нужен ли `scope` в предложении при движении — строка в C-04 и правка EPIC-004 T-301) и №3 (`kind=turn`/`death`/`world_event` заводит EPIC-003 T-220 вместе с издателем) — туда же.
- 2026-09-11 · EPIC-001 · code-reviewer#2 (Opus): T-014 ревью #4 — ПРИНЯТЬ (Critical 0, Major 0, Minor 1, Nit 2). **ВОРОТА ВОЛНЫ 1 СО СТОРОНЫ T-014 ЗАКРЫТЫ**: якорь мутации H красен в 27 прогонах из 27 (6 + 16 обычных, 5 под нагрузкой), ложных красных 0 в 21 прогоне, из них 6 под нагрузкой; во всех 27 падал ровно один подтест — кейс `Close` на настоящем брокере, соседние кейсы зелёные всегда. Мутации B1 и B2 — 4 из 4 каждая. Ворота волны целиком дополнительно зависят от T-017 (принята) и T-018 (на ревью).
- 2026-09-11 · EPIC-001 · Разбор механизма исполнителя проверен ревьюером по исходнику клиента брокера и измерением — верен по всем трём утверждениям и точнее разбора оркестратора. Вероятность зависания измерена распределением имени зависшей подписки в 27 прогонах: 14 / 8 / 2 и 3 без зависания против ожидаемых при половине 13,5 / 6,75 / 3,375 / 3,375 — гипотеза 0,83 из ревью #3 не подтверждается, половина подтверждается. Причина прежних зелёных подтверждена прямым зондом: на исправном коде первое закрытие читателя 5,5–43,5 мс, все последующие ноль, потому что циклы закрывают своих читателей сами. Оценка вероятности зелёного прогона упала с 17 % до менее 1 %.
- 2026-09-11 · EPIC-001 · Три отклонения исполнителя от решения оркестратора ревьюером приняты, причём два — с доказательством, что решение оркестратора было хуже: (1) отпускать обработчик ПЕРЕД `Close` усиливает якорь, потому что в варианте оркестратора счётчик вырасти не может вообще и детектор «счёт» исчезает, оставляя одну лишь половину на подписку; в принятом варианте работают оба детектора — 24 красных зависанием, 3 счётом; (2) допуск в одно событие оставить, строку `slack = 0` НЕ вносить — прямая проверка дала 1 ложный красный из 8 прогонов на исправном коде, и на шине в памяти, где между отпусканием обработчика и отменой нет сетевого коммита; (3) запасной вариант вместе с основным обоснован: пауза и длинный хвост лечат порог «закрытие быстрее события», три подписки — слепой участок «мой читатель закрыт первым», наблюдавшийся прямо.
- 2026-09-11 · EPIC-001 · Замечания ревью #4 закрыты оркестратором без микро-итерации. Minor-1: бюджет худшего красного прогона был занижен на четверть — `stop` теперь ограничен сроком и сообщает через `t.Errorf`, то есть кейс не прерывает, поэтому отложенная остановка добавляет 15 с поверх упавшего ожидания, а четыре кейса держат по три независимых срока; худший красный ≈ 14,5 мин на пакет против `-timeout 15m`. Дефект не спрятался бы, но диагностика превратилась бы в панику рантайма вместо упавшего утверждения. Срок задания `integration` поднят до 20 минут (предел самого задания 25 минут не трогался). Nit-1 и Nit-2: обе формулировки в dev-log поправлены пометками оркестратора — запас надо считать по сумме закрытий, а не по одиночному (одиночные опускаются до 5,5 мс при стоимости события 5,8 мс), и «0 из 64» дополнено контрпримером ревьюера (82 нуля и две единицы из 84).
- 2026-09-11 · EPIC-001 · T-014 ПРИНЯТА. Хвост, не блокирующий ворота: заявка system-architect на расширение C-01 «контекст обработчика отменяется при `Close`» (без неё строго детерминированного якоря в границах контракта не существует), T-394 как второй независимый якорь, T-395; отдельно — кейс `Close` сегодня не попадает НИ ПОД ОДНО задание CI с детектором гонок (`unit` идёт с `-short`, `integration` без `-race`), это в бэклог волны 1.
- 2026-09-11 · EPIC-001 · tech-writer (Sonnet): T-019 выполнена — `README.md`, `CLAUDE.md`, `AGENTS.md`, `Docs/ops/runbook.md`, дополнение `services/_archive/README.md`, поле `stack` в `.dev-team.json`. Убрано как устаревшее: описание пятнадцати микросервисов и рабочего пространства `go.work`, цели `make build-service`/`build-all`/`run SERVICE=`/`logs-service` (их в Makefile больше нет), пятнадцать ссылок на несуществующие `services/<имя>/AGENTS.md`, старый образец доступа к событию через цепочки запасных путей внутри полезной нагрузки (заменён описанием настоящего конверта), Chroma/TimescaleDB/Redis как часть основного стека. Команды сверены чтением `Makefile`, `docker-compose.yml`, `cmd/*` и скриптов; то, чего ещё нет (`mvctl world`, `session-report`, `memory rebuild`, `multiverse db backup`), помечено как зарезервированное, а не выдано за работающее.
- 2026-09-11 · EPIC-001 · Решение оркестратора по расхождению `docs/` и `Docs/`: канонический каталог — `Docs/`. Проверено: все 89 отслеживаемых файлов лежат под `Docs/`, отдельного `docs/` в индексе нет; на этой машине регистр в путях не различается, поэтому файл, созданный как `docs/ops/runbook.md`, реально лёг в `Docs/ops/`. Ссылки в README, CLAUDE.md и AGENTS.md исправлены оркестратором на `Docs/ops/runbook.md` (восемь ссылок) — иначе на сборщике с чувствительным к регистру путём они дали бы 404. Формулировки «docs/» в `infrastructure.md` §9, `epics.md` и `tasks.md` — правка architect#1 в бридж-блоке волны 1.
- 2026-09-11 · EPIC-001 · Найдена и исправлена оркестратором настоящая помеха первому запуску: `.env.example` держал в наборе профилей по умолчанию `bot`, тогда как сервиса `telegram-bot` не существует (в `cmd/` только `multiverse` и `mvctl`), то есть `make up` на чистой машине падал бы на сборке — прямо против пункта готовности «команды README выполняются на чистой машине». Набор по умолчанию сокращён до `memory`, причина записана в самом файле; профиль вернётся вместе с ботом в EPIC-004.
- 2026-09-11 · EPIC-001 · Создана задача T-396 (волна 1): закрыть U-11 — решение по каталогам среды разработки и ассистентов в индексе (`.idea/`, `.vscode/`, `.kilo*`, `.roo*`, `.qwen/`, `memory/`, `plans/`, `reports/`, `event-model.pdf`). В `tasks.md` пункт был поручен техническому писателю в T-019, но исполнение требует правки `.gitignore` и снятия файлов с отслеживания, что выходит за рамки документарной задачи и, главное, меняет состав репозитория. Пользователь ранее решил «оставить проиндексированными до F-9» — F-9 наступил, но снятие с отслеживания я выполнять без прямого подтверждения не буду: файлы останутся на диске, однако из свежего клона исчезнут. Счётчик задач 395 → 396.
- 2026-09-11 · EPIC-001 · code-reviewer#1 (Opus): T-018 — ПРИНЯТЬ (Critical 0, Major 0, Minor 3, Nit 4). Сокращение состава по решению оркестратора проведено честно: `Attack`/`Flee`, боевой сценарий, `WithEncounterStub` и виды нарратива `turn`/`death`/`world_event` не заглушены пустышками, а отсутствуют, и каждое отсутствие закреплено тестом. Главное свойство — ожидания сквозного теста выводятся из скрипта — подтверждено четырьмя мутациями: добавленный шаг, потерянный шаг, переставленный порядок и отдых без предложения дают точные сообщения вида «14 действий, в скрипте 15». Подстановка здоровья настоящая: константа вместо проекции красит тест. Пятнадцать прогонов сквозного теста дали одинаковые тексты. Пять замечаний ревью #2 T-017 закрыты: ворота подняли долю красных с 2 из 100 до 60 из 60 при 0 ложных из 140, включая 80 под восьмикратной нагрузкой.
- 2026-09-11 · EPIC-001 · ИСПРАВЛЕНИЕ МОЕЙ СОБСТВЕННОЙ ЗАПИСИ: в записи от 2026-09-11 о `Harness` я написал, что следствие «изменение, о котором харнесс не слышал, даёт `version_conflict`» закреплено тестом. Ревьюер проверил — не закреплено ни одним: слово встречается один раз в комментарии, а тест, который мог бы это проверить, закрывает нужное окно ожиданием. Само свойство реально (зонд «глухой харнесс» дал отказ по конфликту версий на шине, зонд без ожидания — 50 из 50), но записи о том, что чего-то держит тест, обязаны быть верными, иначе следующий снимет ожидание и ничего не заметит. Тест добавляется в текущей правке.
- 2026-09-11 · EPIC-001 · Решение оркестратора по открытому вопросу ревью T-018: `cmd/multiverse/fake_contexts.go` и `MV_SWARM_FAKE` в волне 0 НЕ делаются. Карта владения относит хук к EPIC-001, но он монтирует `testkit/swarm.FakeContext`, которого не существует и который приходит вместе с `FakeEncounter` из EPIC-003 (ранний merge C4, T-219/T-220 — первый же блок волны 1). Хук без того, что он монтирует, — пустая обёртка того самого класса «объявлено, но нечем питать», от которого мы отказались в рассказчике. Именное исключение `depguard` для этого файла в `.golangci.yml` безвредно: это исключение по пути, а не требование существования файла. Записать в задачу EPIC-003, приносящую `FakeContext`.
- 2026-09-11 · EPIC-001 · tech-lead#1 (Opus): T-019 — ВЕРНУТЬ (Critical 0, Major 2, Minor 4, Nit 7). Старая картина вычищена полностью и это проверено: ни `go.work`, ни снятых целей `make`, ни Chroma как основной базы, ни ссылок на несуществующие файлы инструкций сервисов; таблица статусов сходится с деревом до каталога (13 = 3 переписываемых + 2 legacy + 8 замороженных, ровно 8 файлов заморозки; в архиве 15 файлов архивации); ни одной несуществующей цели или подкоманды в документах нет, а «зарезервированное» действительно отвечает ошибкой с именем эпика; поле `stack` сходится с `go.mod`, `versions.env` и compose. Правка регистра `Docs/` признана верной.
- 2026-09-11 · EPIC-001 · **Ma-1: МОЯ ПРАВКА `.env.example` ПРОБЛЕМУ НЕ РЕШАЕТ.** `docker compose` интерполирует весь файл ДО фильтрации по профилям, а в compose две обязательные переменные вида `${VAR:?}` принадлежат сервисам вне набора по умолчанию: токен телеграм-бота (пуст в примере) и образ Chroma (намеренно пуст в `versions.env` по решению D-3). Поэтому `make up` на чистой машине падает, не запустив ни одного контейнера, независимо от состава профилей: `docker compose config -q` даёт «required variable is missing a value», `up --dry-run` падает до создания контейнеров. С подменным файлом переменных та же команда проходит и даёт ровно девять сервисов. Под тем же ударом критерий готовности эпика в T-020. Сокращение набора профилей остаётся верным по существу (сервиса бота нет), но проблему запуска не закрывает.
- 2026-09-11 · EPIC-001 · Создана задача T-397 (волна 1, ДО T-020, исполнитель devops-engineer): починить запуск на чистой машине. Варианты тимлида: (а) заменить `:?` на `:-` и поставить громкую проверку в точке использования; (б) подмешивать файл переменных с заглушками в `make up` — хуже, тихо убивает смысл громкого отказа; (в) вынести телеграм-бота и Chroma в отдельные compose-файлы, подключаемые по профилю. Выбор за исполнителем с обоснованием; требование одно — громкий отказ обязан остаться там, где переменная действительно нужна.
- 2026-09-11 · EPIC-001 · **ИСПРАВЛЕНИЕ МОЕЙ ЗАПИСИ О КАТАЛОГЕ ДОКУМЕНТАЦИИ.** Я написал, что все 89 отслеживаемых файлов лежат под `Docs/`. Неверно: под `Docs/` их 83, под строчным `docs/` — 6 (наследие Living Worlds). На этой машине регистр в путях не различается и раскол не виден, на файловой системе с различением регистра будет два каталога. На ссылки T-019 это не влияет (они проверены по индексу и ведут в `Docs/`), но вывод «отдельного `docs/` в индексе нет» был ошибочным.
- 2026-09-11 · EPIC-001 · Создана задача T-398 (волна 1): свести раскол каталога документации и вычистить устаревшие файлы-инструкции вне области T-019. Тимлид нашёл ту же болезнь по другим адресам: `QWEN.md` (723 строки — Chroma и TimescaleDB, снятые цели сборки, таблица пятнадцати сервисов) — файл того же класса, что `CLAUDE.md`, то есть инструкция, которую читают агенты; плюс `AI_AGENT_INSTRUCTIONS.md`, `README_LIVING_WORLDS.md`, `AUTOMATION-SETUP.md` и шесть файлов `docs/LIVING_WORLDS_*.md` — они же и есть содержимое строчного `docs/`. Ничего не удалять (OQ-A-17): переносить в архив документации либо переписывать. Счётчик задач 396 → 398.
- 2026-09-11 · EPIC-001 · Ma-2 закрыт оркестратором: врезка README описывала `.env.example` в том виде, в каком он был ДО моей же правки, и требовала убрать `bot` из набора, которого там уже нет. Текст переписан на фактическое состояние, перекрёстная ссылка из блока команд проверена и цела. Mi-1…Mi-4 и мелкие замечания уходят техническому писателю итерацией 2.
- 2026-09-11 · EPIC-001 · developer#1 (Opus): доводка T-018 — закрыты все три Minor и три Nit; реализация не менялась нигде, кроме порядка проверок в сквозном тесте, остальное чинило доказательство, а не поведение. Minor-1: новый тест ставит второй харнесс, который НЕ запускается вовсе, получает ровно один факт о создании персонажа, после чего персонажа ранят мимо него; отдых прикалывает версию, которой мир уже не живёт, и тест утверждает и тип ответа, и причину отказа, и адресата. Глухота выбрана ради детерминизма: зонд ревьюера со слушающим харнессом давал конфликт 50 из 50, но это гонка, а не свойство. Мутация «снять проверку версии» до правки оставляла весь пакет шлюза зелёным, после правки роняет его за 0,30 с с внятным текстом. Minor-2: список из семи типов заменён обходом всего реестра плюс сверкой числа отвечающих типов. Minor-3: проверка недоставленных поднята выше ожидания нарративов — та же поломка теперь сообщает о себе за 0,03 с полной ошибкой схемы вместо 10,03 с и слов «истекло ожидание». Проверено: дерево чистое, следов мутаций нет, все пакеты тестового набора зелёные.
- 2026-09-11 · EPIC-001 · Замечание о параллельной работе в одном дереве: `pre-commit` дважды сообщил «файлы изменены хуком», хотя ни один файл Go не менялся — хук сравнивает состояние ВСЕГО дерева до и после, а в те же минуты devops-engineer правил compose, Makefile и скрипты по T-397. Контрольный замер при неизменном дереве дал зелёный. Вывод на будущее: `pre-commit` в общем дереве при двух работающих агентах даёт ложные срабатывания; доверять только прогону, у которого состояние дерева до и после совпало.
- 2026-09-11 · EPIC-001 · devops-engineer (Opus): T-397 закрыт вариантом (в) — сервисы `bot` и `legacy` вынесены в отдельные compose-файлы, подключаемые из Makefile ровно тогда, когда профиль запрошен. Громкий отказ сохранён дословно и переехал вместе с переменной: теперь его видит только тот, кто поднимает соответствующий профиль. Вариант (а) отвергнут с разбором: «точка использования» токена бота — бинарник, которого нет до EPIC-004, а образ платформы не содержит ни оболочки, ни curl, поэтому проверку негде поставить и контейнер стартовал бы молча; пустой образ Chroma даёт ошибку, в которой имя переменной не упоминается, а подставной тег — это ровно то, что запрещает решение D-3. Граница проведена по зацеплению якорями YAML: якорь не пересекает файл. Побочный выигрыш: профиль `legacy` целиком живёт в одном файле, и его удаление в EPIC-003 станет удалением файла, а не хирургией по общему compose.
- 2026-09-11 · EPIC-001 · ВТОРОЙ ДЕФЕКТ, найденный по дороге и опаснее исходного: в `.env.example` инлайн-комментарий после ПУСТОГО значения становился значением переменной. Разбор dotenv у compose обрезает хвостовой комментарий только после непустого значения. Оператор, скопировавший пример и ничего не заполнивший, получал логин MinIO, равный тексту комментария: значение непустое, поэтому громкий отказ молчал, и хранилище стартовало с мусорным логином. При этом Makefile читал ту же строку как пустую — то есть compose и make расходились в значении одной переменной, а правило линтера «секрет только через обязательную переменную» было обесценено форматом файла. Комментарии перенесены на строку выше у всех 13 пустых переменных, запрет записан в шапку и охраняется новым правилом 7 линтера с двумя фикстурами. Проверено: нетронутая копия примера теперь отказывается стартовать с внятным текстом вместо подстановки комментария.
- 2026-09-11 · EPIC-001 · Решения оркестратора по открытым вопросам T-397: (1) голый `docker compose --profile bot up` без Makefile официально НЕ поддерживается — принимается вариант (а) исполнителя: Makefile объявлен точкой входа оператора, профиль `legacy` и так требует отдельной цели подготовки исходников; известная цена — такой вызов молча поднимет стек без бота, и это задокументировано в примере настроек и шапках файлов. (2) Правило 7 разрешает обязательную переменную у профильного сервиса, если она помечена как заполняемая в примере настроек — принимается: пример поставляется с профилем `memory`, и README велит заполнить обе переменные Neo4j; вынос профиля `memory` в отдельный файл потребовал бы дублировать якоря и в задачу не входит.
- 2026-09-11 · EPIC-001 · **ПРОБЕЛ ПРОВЕРКИ, требует действия владельца: GNU make на машине не установлен**, и ни один агент не смог выполнить ни одной цели. Правки Makefile по T-397 (вычисление активного набора профилей, подключение файлов профиля, экранирование в вызове sed) синтаксически не исполнялись — проверены только эквивалентной эмуляцией оболочкой. CI цели make тоже не вызывает (в рабочем процессе make упоминается лишь в комментариях), то есть автоматической страховки нет. README называет `ezwinports.make` в списке требований. Действие владельца: установить make и прогнать `make -n up PROFILES=bot`, `make -n up PROFILES=memory,legacy`, `make compose-lint`, затем реальный `make up` на чистой машине — это и есть недостающая часть критерия готовности эпика.
- 2026-09-11 · EPIC-001 · tech-writer (Sonnet): T-019 итерация 2 — исправлены Mi-1…Mi-4 и все шесть мелких замечаний, каждое подтверждено запуском или поиском по файлу. Существенное: в `CLAUDE.md` раздела о запретах линтера не было вовсе, теперь перечислены все четыре (окружение, часы, таймеры, журналирование) — это файл-инструкция, который читают агенты, и неполный список запретов был прямым источником будущих правок мимо правил. Ссылка на несуществующую подкоманду исправлена по фактическому реестру команд; утверждение архива о том, что файлов исключения ещё нет, переписано на факт с честным пояснением там, где исключение не требовалось вовсе.
- 2026-09-11 · EPIC-001 · tech-writer (Sonnet): T-019 итерация 3 — README и runbook приведены к механизму T-397. Существенно, что список из шести переменных заменён ПРАВИЛОМ «заполнять помеченные как обязательные»: текст больше не разойдётся с файлом при следующей правке. Добавлены раздел о профилях с прямым предупреждением, что голый вызов compose без Makefile молча поднимет стек без бота, уточнение о снятии стека и явное предупреждение, что порядок первого запуска целиком не прогонялся из-за отсутствия make. Заголовок «Запуск за 5 команд» пересчитан, а не подогнан: команд по-прежнему пять.
- 2026-09-11 · EPIC-001 · tech-lead#1 (Opus): T-019 приёмка #2 — ВЕРНУТЬ (Critical 0, Major 2, Minor 5, Nit 5), объём итерации 4 заморожен семью пунктами. Все прежние замечания закрыты и это доказано собственными прогонами тимлида, а не отчётом исполнителя: он синтезировал «чистую машину» из примера настроек, заполнив только помеченные переменные, и прогнал три проверки состава — набор по умолчанию, профиль бота, устаревший профиль; громкий отказ переехал, а не исчез. Возврат по двум существенным: (1) файл-инструкция `CLAUDE.md` в итерации 3 не правился вовсе и сохранил карту профилей ДО T-397 — он по-прежнему утверждает, что все профили живут в одном compose-файле, и ни один из двух новых файлов не назван; это ведёт агентов следующих эпиков ровно туда, откуда дефект только что убрали. (2) Записи о T-019 в dev-log нет ни одной, хотя общий DoD её требует явно — ход работы восстанавливается только по журналу оркестратора.
- 2026-09-11 · EPIC-001 · Существенное среди мелких замечаний T-019: README и runbook утверждают, что устаревший профиль требует ручной подготовки исходников перед первым запуском. Неверно — Makefile объявляет эту цель зависимостью, она выполняется сама, и шапка соответствующего compose-файла говорит то же. Два документа разошлись сразу с двумя источниками истины. Ещё: предупреждение о неисполненных целях стоит только в runbook, а в README блок первого запуска его не имеет и ссылается на runbook ПОСЛЕ команд, обещая «полный порядок и диагностику», а не «эти команды никто не выполнял»; правило про обязательные переменные, введённое в итерации 3, до `CLAUDE.md` не дошло, и там остался список из пяти вместо шести. По цели `ci` исходное замечание закрыто не до конца: инструмент предкоммитных проверок в ней не участвует, утверждение «в CI ставятся автоматически» ложно, а `python3`, без которого линтер композиции красный, не назван вовсе.
- 2026-09-11 · EPIC-001 · Позиция тимлида по приёмке волны, принимается: T-020 нельзя закрывать, пока владелец не выполнит стендовый пункт готовности — команды README на чистой машине. Это и есть недостающая часть готовности эпика, и без установленного make её не выполнить.
- 2026-09-11 · EPIC-001 · code-reviewer#2 (Opus): T-397 — ПРИНЯТЬ (Critical 0, Major 0, Minor 3, Nit 4). Оба дефекта проверены сравнением «до и после», причём состояние «до» доставалось из индекса и прогонялось теми же командами: старый файл на собранной вручную чистой машине падает на образе Chroma, новый даёт код 0 и ровно девять сервисов. Второй дефект воспроизведён в обе стороны: старый пример дословно давал логин MinIO, равный тексту комментария, и даже пару «логин/пароль» Neo4j с комментарием внутри, тогда как оболочка читала ту же строку как пустую. Инлайн-комментариев после пустого значения было ровно 13, сейчас ни одного; проверены все 15 пустых переменных, других таких не осталось. Полностью разрешённые модели всех пяти профилей до и после совпадают дословно — при переносе не потеряно ничего.
- 2026-09-11 · EPIC-001 · Minor-1 ревью T-397 закрыт оркестратором: разбор `.env` в Makefile расходился с разбором compose в трёх случаях, и всегда в одну сторону — make подключал МЕНЬШЕ файлов, поэтому compose считал профиль активным, файл профиля не подключался, и стек молча поднимался без нужного сервиса. Случаи: дубль ключа (compose берёт последнее присваивание, make брал первое), строка вида `export COMPOSE_PROFILES=…` (валидная для dotenv), ведущие пробелы перед именем. Исправлено одной строкой по рецепту ревьюера; проверено эмуляцией на шести формах: три прежде ломавшихся теперь дают тот же ответ, что compose, три прежде работавших (инлайн-комментарий, кавычки, простое значение) не сломаны. Причина каждой детали записана в комментарии рядом, чтобы следующий не «упростил» шаблон обратно.
- 2026-09-11 · EPIC-001 · Найдено оркестратором при проверке: пять файлов T-397 (`docker-compose.bot.yml`, `docker-compose.legacy.yml` и три фикстуры линтера) НЕ БЫЛИ добавлены в индекс — исполнитель проиндексировал только изменённые файлы, а новые оставил неотслеживаемыми. При коммите волны они бы просто не попали в историю, и ветка приехала бы с Makefile, ссылающимся на несуществующие файлы. Добавлены; переносы строк в индексе нормализованы (в них не осталось возвратов каретки); линтер зелёный, все шесть «плохих» фикстур по-прежнему отвергаются. Правило на будущее: после каждой задачи, создающей файлы, проверять `git status --untracked=all`, а не доверять отчёту.
- 2026-09-11 · EPIC-001 · Кольцевая ссылка в `.env.example` («те же шесть, что перечисляет README») закрыта оркестратором: README после итерации 3 списка не содержит, там правило. Пометка в примере объявлена единственным источником списка, и это сказано прямо.
- 2026-09-11 · EPIC-001 · Заведены карточки задач T-394…T-399 в `tasks.md` отдельным разделом. Причина — процессное замечание, прозвучавшее дважды (Mi-3 ревью T-397 и N-2 приёмки T-019): задача, живущая только в журнале и живом состоянии, невоспроизводима из артефакта эпика. Создана T-399: привести `infrastructure.md` к состоянию после T-397 — §1.3, §2.2, §3.1.1 описывают прежнюю топологию, а §4.2 хранит эталон примера настроек ровно в запрещённом теперь формате, то есть следующий исполнитель воспроизвёл бы дефект «по дизайну». Счётчик задач 398 → 399.
- 2026-09-11 · EPIC-001 · ПОЛНАЯ ПРОВЕРКА ВЕТКИ оркестратором перед приёмкой волны, все прогоны свои, не по отчётам:
  · `go build ./...` и `go vet ./...` — чисто.
  · `go test -short -count=1 ./...` — все пакеты зелёные.
  · `golangci-lint run ./...` — 0 issues; с тегами `integration,e2e` — 0 issues.
  · `go test -tags e2e -count=1 ./test/...` — зелёный (сквозные тесты и фикстуры).
  · `go test -tags integration -count=1 ./shared/testkit/... ./shared/eventbus/...` — зелёный; контрактный набор на живом брокере отдельно 9,77 с — зелёный.
  · `mvctl contracts check` — 65 типов, 8 топиков, 58 схем; `mvctl env check` — 69 переменных сверены с примером; `mvctl privacy scan testdata/` — внешних идентификаторов нет, 16 файлов.
  · `scripts/compose-lint.sh` — 15 сервисов в 3 файлах, 7 правил; все шесть «плохих» фикстур отвергаются своими правилами.
  · `gitleaks git --staged --redact .` — утечек нет, просканировано 1,26 МБ.
  · В индексе 106 файлов. Неотслеживаемого из волны не осталось: вне каталога ассистента таких файлов три, и все три — наследие прежнего состояния проекта (`Docs/user-stories/**`), к волне отношения не имеют; каталог `.claude/**` — территория задачи T-396 (U-11).
  · НЕ проверено и проверено быть не может без действия владельца: ни одна цель `make` (GNU make не установлен) и стендовый прогон команд README на чистой машине. Это единственный незакрытый пункт готовности эпика.
- 2026-09-11 · EPIC-001 · tech-lead#1 (Opus): T-019 приёмка #3 — ПРИНЯТЬ (Critical 0, Major 0, Minor 2, Nit 4). Все семь пунктов заморозки и четыре мелких закрыты, каждый проверен собственным прогоном тимлида. Показательная проверка N-1: он не читал карту профилей, а сравнил состав сервисов по одному compose-файлу и по трём — 7 против 11, разница ровно четыре вынесенных сервиса. Тимлид отдельно перепроверил цифру версии сборщика композиции «потому что она выглядит неправдоподобно» и подтвердил её, а также убедился, что найденный на машине `python3` — настоящий интерпретатор, а не заглушка магазина приложений.
- 2026-09-11 · EPIC-001 · Два остатка T-019 закрыты оркестратором, задача пятый раз не возвращалась. Nov-1: три ссылки вида «файл:номер строки» указывали не туда — номера сдвинулись после моей же правки разбора профилей в Makefile, и исполнитель скопировал их из текста приёмки #2. Заменены ссылками на имена цели и переменной. Nov-2: утверждение файла архива «ни один compose-файл не собирает `services/*`» верно для двух файлов из трёх — устаревший профиль собирает два сервиса, но не из рабочего дерева: цель подготовки исходников выкладывает их из закреплённой ссылки в отдельный каталог сборки, и архив при этом всё равно не участвует. Формулировка исправлена по существу.
- 2026-09-11 · EPIC-001 · ПРАВИЛО ЭПИКА, предложено tech-lead#1 и принимается: в документах и записях ссылаться на имена целей, переменных и разделов, а не на пары «файл:номер строки». За три приёмки устаревшие номера всплыли дважды, оба раза из-за правок в чужой задаче. Номер строки в живом документе — ссылка, которая гниёт молча.
- 2026-09-11 · EPIC-001 · T-019 ПРИНЯТА. Незакрытым остаётся один пункт готовности эпика: стендовый прогон команд README на чистой машине владельцем (нужен GNU make). T-020 без него закрывать нельзя — позиция тимлида, подтверждённая дважды.
- 2026-09-11 · EPIC-001 · tech-lead#1 (Opus): T-020 — **ВОЛНА 0 ПРИНЯТА С УСЛОВИЯМИ**. Инженерное содержание сделано и доказано, ворота волны 1 закрыты ревью #4, формально закрытых задач нет: у каждой есть артефакт в дереве, ревью и запись. Условия не про качество кода: стендовый прогон владельцем, коммит владельца и артефактная гигиена. Тимлид не повторял прогоны оркестратора, а перепроверил своими руками то, от чего зависит вердикт, и выборочно сверил утверждения журналов с кодом, а не с записями о правках.
- 2026-09-11 · EPIC-001 · Три места, где бридж-блок волны 1 упрётся, найдены приёмкой: (1) T-219 не сможет выполнить свой DoD «сквозной сценарий на 30 ходов» — заглушка встречи подписана на действия атаки и бегства, а у харнесса волны 0 таких методов нет, они сняты решением о сокращении T-018; владельца у правки сегодня нет, потому что каталог принадлежит EPIC-004, чьи задачи идут позже. (2) T-220 придёт с видом нарратива на осмотр, противоречащим тому, что волна 0 сделала и закрепила тестом. (3) Контракт C-03 расходится с кодом в четырёх сигнатурах, и в одном месте после правки контракта придётся тронуть код. Заведены карточки T-400…T-403.
- 2026-09-11 · EPIC-001 · Артефактная гигиена закрыта оркестратором: (1) утверждение «вместе они дают 8 из 8» в записи итерации 3 T-014 было опровергнуто ревью #3 замером, но пометки не имело, тогда как у более мелких утверждений в том же файле пометки есть — добавлена, с указанием, что 8 из 8 были выборкой, а не свойством, и что детерминированным на практике якорь стал только в итерации 4. (2) Записи T-397 в dev-log не было вовсе (второй случай за волну) — восстановлена по отчёту исполнителя и журналу, с честной пометкой о восстановлении и с разделом «чего проверка не покрывает».
- 2026-09-11 · EPIC-001 · Решение по 15 неиндексированным `go.mod`/`go.sum` устаревших сервисов: тимлид рекомендует откатить, я оставляю решение владельцу и НЕ откатываю сам. Причина: это правка файлов замороженных и архивных сервисов, а действующее указание владельца — ничего в архиве не трогать без его слова (OQ-A-17). Файлы не в индексе и в коммит волны не попадут при любом исходе. Владельцу на выбор: откатить (`git checkout -- services/`) либо сохранить отдельным коммитом `chore`, вне коммита волны.
- 2026-09-11 · EPIC-001 · Оценка процесса от tech-lead#1, в ретроспективу: возвраты получили 13 задач из 20, и в каждом возврате был настоящий Major или Critical — это цена постановки, где доказательство не входило в задание, а не качества работы. Недостатки постановки: определение готовности описывало состав, а не доказательство, и критерий силы якоря изобретался по ходу трёх ревью; для детерминированных алгоритмов не требовался золотой вектор, посчитанный вне реализации; документарная задача шла параллельно задаче, переставлявшей ровно то, что документ описывает; пункт «журнал разработчика заполнен» не проверялся на входе в приёмку, хотя это проверка на десять секунд; заглушки ставились без матрицы отказов; общее рабочее дерево на двух агентов дало ложные срабатывания проверок и новые файлы вне индекса.
- 2026-09-11 · EPIC-001 · КОММИТ ВОЛНЫ 0 сделан по подтверждению владельца: `69d1aed` «Wave 0 of EPIC-001: the contract set, the mechanics, the fixtures and the doubles [contract-change]» на ветке `epic/EPIC-001-foundation`, 108 файлов, +28671 −1141. Все восемь хуков перед коммитом пройдены. После коммита дерево перепроверено: сборка, 27 пакетов тестов и линтер зелёные. Пометка `contract-change` относится к двум изменениям: поведение `Close` под идущей подпиской (было зависание, стало возврат nil, публичный интерфейс не менялся) и тип поля `seed` в схеме `dice.rolled` (целое → десятичная строка). Push, слияние и тег НЕ делались — по действующему указанию владельца.
- 2026-09-11 · EPIC-001 · Вне коммита осталось ровно то, что и планировалось: 15 файлов модулей замороженных и архивных сервисов (решение за владельцем, см. запись выше), настройки среды разработки и ассистентов (задача T-396, требует подтверждения владельца), три файла `Docs/user-stories/**` — наследие прежнего состояния проекта.
- 2026-09-11 · EPIC-001 · T-403, прогон целей `make` владельцем состоялся (make установлен). Результаты, все прогоны мои:
  · `make -n up PROFILES=bot` — набор файлов верен: `-f docker-compose.yml -f docker-compose.bot.yml --profile bot`. Логика T-397 подтверждена ИСПОЛНЕНИЕМ, а не эмуляцией.
  · `make -n up PROFILES=memory,legacy` — верно: `-f docker-compose.yml -f docker-compose.legacy.yml --profile memory --profile legacy`, с автоматической подготовкой исходников устаревшего профиля впереди (то самое, про что документы ошибочно писали «вручную» — правка N-3 подтвердилась на деле).
  · `make compose-lint` — код 0: 15 сервисов в 3 файлах, 7 правил; все шесть «плохих» фикстур отвергнуты.
  · `make ci` — сначала КРАСНЫЙ, после правки ЗЕЛЁНЫЙ (код 0): линт, тесты, контракты, секреты, персональные данные, уязвимости, линтер композиции, сквозные тесты.
- 2026-09-11 · EPIC-001 · ДЕФЕКТ, найденный первым же настоящим прогоном `make ci`: цель `test` жёстко прописывала `-race`, а детектор гонок требует cgo и компилятора C, которых на машине нет — `go: -race requires cgo`. Цель падала ДО первого теста. Это прямо противоречило записанному решению ОВ-5 (ADR-012): «`go test -race` — только в CI (Linux); общий DoD читать как “-race в CI, локально без него”». То есть Makefile всё это время противоречил решению команды, и обнаружить это можно было только исполнением — ни один агент за всю волну не мог, потому что make не был установлен. Исправлено: флаг подставляется, только когда доступны cgo и компилятор; цель ВСЛУХ печатает, какой из двух прогонов состоялся, чтобы зелёный локальный прогон никто не прочитал как проверенный детектором гонок. Молчаливое снятие флага было бы хуже самого падения.
- 2026-09-11 · EPIC-001 · `make up` не прогонялся: в `.env` владельца пусты пять из шести обязательных переменных (заполнен только один пароль Neo4j), и compose отказал громко, назвав первую недостающую — ровно то поведение, ради которого делался T-397. Учётные данные я не заполняю. Это единственный незакрытый пункт готовности эпика: критерий «`make up` поднимает инфраструктуру и пустые контексты с ответом на проверке здоровья».
- 2026-09-11 · EPIC-001 · T-403 ЗАКРЫТ: стек поднят на машине владельца, `make up` возвращает 0. Все контейнеры здоровы (redpanda, minio, qdrant, neo4j, core, gateway, memory; оба init-контейнера отработали и вышли), все три контекста отвечают `ok` на проверке здоровья. Недоступность LLM даёт предупреждение и не роняет запуск — ровно как предписано FR-080. Критерии готовности эпика «`make ci` зелёный» и «`make up` поднимает инфраструктуру и пустые контексты с ответом на проверке здоровья» закрыты. Проект в compose называется `multiverse-mvp`.
- 2026-09-11 · EPIC-001 · ТРИ ДЕФЕКТА, найденных ТОЛЬКО исполнением на машине владельца — ни один не мог быть найден агентами, потому что make не был установлен:
  1. Цель `test` жёстко требовала `-race`, а он требует cgo и компилятора C. `make ci` падал ДО первого теста, прямо вопреки записанному решению ОВ-5 «-race в CI, локально без него». Флаг теперь подставляется только при доступных cgo и компиляторе, и цель ВСЛУХ печатает, какой из двух прогонов состоялся: молчаливое снятие флага хуже падения, потому что зелёный локальный прогон прочитали бы как проверенный детектором гонок.
  2. Проверка ядра сообщала FAIL при полностью исправном ядре. Причина не в сервисе: под Git Bash аргумент `/multiverse` — абсолютный путь ВНУТРИ контейнера — переписывался MSYS в путь Windows, и docker получал `C:/Program Files/Git/multiverse`. Лечится `MSYS_NO_PATHCONV=1`, переменная неизвестна оболочкам Linux и там игнорируется. Это худший класс дефекта: инструмент диагностики врал про здоровую систему.
  3. Предупреждение оператору о недоступности LLM было написано по-русски, а консоль Windows читает UTF-8 файла как CP1251 — оператор видел кракозябры ровно там, где сообщение важно. Текст переведён на английский по правилу «код на английском»; русская формулировка остаётся в руководстве по эксплуатации, где она отображается верно.
- 2026-09-11 · EPIC-001 · Правка `.env` владельца оркестратором (одна строка, не учётные данные): `COMPOSE_PROJECT_NAME` содержал `multiverse-` плюс кириллические «ьмз» — это `mvp`, набранное на русской раскладке. Compose отвергает такое имя проекта. Заменено на `multiverse-mvp`. Учётные данные владелец заполнил сам, я их не вводил и не читал.
- 2026-09-11 · EPIC-001 · Повторная проверка после правок Makefile: `make ci` — 0, `make up` — 0, `make compose-lint` — 0, `make health` при поднятом стеке — три контекста `ok`.
- 2026-09-11 · EPIC-001 · LLM владельца поднят на порту 8888 (не 1234). Настроено и проверено оркестратором:
  · `MV_LLM_PORT` в `.env` был 1234 — заменён на 8888. `MV_LLM_URL` владелец уже указал верно (`http://host.docker.internal:8888`), `MV_LLM_API_KEY` заполнен.
  · **Найдено исполнением: у сервера владельца НЕТ `/health`.** Замер: `/health` и `/v1/health` дают 404, `/props` и `/v1/models` — 200. Это сервер семейства llama.cpp (есть `/props`), но с другим набором точек: `owned_by` моделей — `unsloth-studio`. Проверка здоровья опиралась на `/health`, то есть на точку самого llama.cpp, а не на контракт: платформа зависит от совместимого с OpenAI интерфейса (ADR-005), и сервер может его давать без `/health`. Итог был бы «стек мёртв» при живом сервере — тот же класс дефекта, что ложный FAIL ядра. Правка: при 404 проверка переходит на `/v1/models` — ту точку, которая названа контрактом; 503 по-прежнему означает «модель ещё грузится», прочие коды сохраняют свой смысл. Сообщение печатает, по какой точке вынесено суждение, чтобы никто не считал, что `/health` ответил.
  · **Путь «контейнер → LLM» проверен по-настоящему**, а не по конфигурации: из контейнера в сети проекта анонимный запрос даёт 401, с ключом платформы — полный список моделей. То есть связность есть, авторизация нужна, ключ у платформы верный. Сервер требует авторизацию для запросов не с loopback — при проверке с хоста этого не видно.
  · `make health` в строгом режиме (с LLM) — код 0: gateway, memory, core и LLM отвечают.
- 2026-09-11 · EPIC-001 · Остаточное замечание, не чинил: `MV_LLM_BIN не задан, сравнить нельзя` — закреплённая сборка llama.cpp (b10441) не сверяется с работающим сервером. Для сервера семейства unsloth-studio сверка по бинарнику llama.cpp неприменима в принципе. Вопрос архитектору: чем закреплять версию runtime, если сервер не llama-server. В бридж-блок волны 1.
- 2026-09-11 · EPIC-001 · УТОЧНЕНИЕ ВЛАДЕЛЬЦА о провайдере LLM: им может быть что угодно, совместимое с OpenAI. Сейчас локальный unsloth на базе llama.cpp, но равно допустимы официальное облачное API, Qwen, Claude и любое другое. Это согласуется с решением этапа видения («допустить облачные LLM как вариант») и уже отражено в среде выполнения: `MV_LLM_PROVIDER` перечисляет `openai_compat`, `ollama`, `anthropic`, `recorded`, `fake`; адрес и ключ вынесены в переменные; нелокальный адрес открывается отдельным флагом, причём шлюз стоит на адресе, а не на имени провайдера.
- 2026-09-11 · EPIC-001 · Следствие уточнения, зафиксировано задачей T-404: провайдер-независима среда выполнения, но НЕ эксплуатационная обвязка. Цели запуска и остановки LLM поднимают нативный процесс — для облака останавливать нечего; сверка закреплённой сборки llama.cpp неприменима к другому провайдеру и требует иного способа закрепить версию (для облака — модель и её версия из ответа, а не бинарник); отчёт о видеопамяти осмыслен только для локального процесса; точка `/health` принадлежит llama.cpp, а не контракту, и правка T-403 сделана пока только в PowerShell-варианте; наконец, у адреса ДВА источника истины — платформа берёт `MV_LLM_URL`, скрипт собирает из `MV_LLM_PORT`, и ровно из-за этого проверка стучалась в 1234, когда сервер слушал 8888. Счётчик задач 403 → 404.
- 2026-09-11 · EPIC-001 · Коммит `18cf577` «Three defects only a real run could find (T-403)»: 5 файлов, +137 −17. Все хуки пройдены. Push, слияние и тег не делались.
- 2026-09-11 · EPIC-001 · devops-engineer (Opus): T-404, главная часть — единственным источником адреса LLM стала `MV_LLM_URL` (для `ollama` — `MV_OLLAMA_URL`). Выбор обоснован не вкусом: это ЕДИНСТВЕННАЯ из трёх переменных, которую читает сама платформа, и на её хосте стоит шлюз облака по ADR-005 доп. 2. `MV_LLM_HOST` оставлена и описана как интерфейс прослушивания: «что слушать» и «куда стучаться» законно различаются, дублировался именно порт — он и убран. Переменная `MV_LLM_PORT` не просто удалена, а объявлена выведенной из обращения, чтобы оператор со старым файлом получил указание удалить строку, а не молчание. Манифест 69 → 68 переменных, сверка с примером двусторонняя и зелёная.
- 2026-09-11 · EPIC-001 · Сверх задания исполнитель сделал обвязку провайдер-зависимой, как и требовало уточнение владельца: для облачного адреса, `anthropic`, `ollama`, `recorded` и `fake` цели запуска и остановки честно печатают, что отсюда ничего не запускается, и вместо запуска проверяют эндпоинт там, где он есть. Правка T-403 перенесена во второй скрипт и расширена: судить по контрактной точке теперь и при 401, 403, 405, 501, а не только при 404 — облачный эндпоинт отвечает на `/health` ровно так же, как сервер владельца. Ключ в варианте для оболочки передаётся через конфиг curl на стандартном вводе, чтобы не попасть в список процессов. Билд-пин и отчёт о видеопамяти печатаются только для локального рантайма.
- 2026-09-11 · EPIC-001 · Проверено оркестратором после правки: строка `MV_LLM_PORT` удалена из `.env` владельца (файл правил я, агент к нему не прикасался); `mvctl env check` без сверки окружения — 68 переменных, код 0; со сверкой окружения из `.env` — код 0; `make llm-health` — код 0, в выводе прямо сказано, из какой переменной взят адрес и по какой точке вынесено суждение; строгий `make health` — код 0, все три контекста и LLM отвечают. Расхождение, из-за которого задача затевалась, воспроизвести нечем: адрес читается в одном месте на каждую реализацию.
- 2026-09-11 · EPIC-001 · Открытые вопросы T-404 к архитектору: (1) содержит ли `MV_LLM_URL` суффикс `/v1` — документация облачных вендоров обычно даёт адрес именно с ним, а скрипт дописывает `/v1/models` сам; решить НАДО до реализации провайдера в EPIC-003, иначе получится `/v1/v1/...`. (2) Правило «запускается отсюда» сейчас считает адрес локальной подсети нелокальным для запуска, но локальным для шлюза облака — так же, как ADR-005; если появится стенд с сервером на другой машине сети, правило придётся расширить. (3) Предупреждение о закрытом шлюзе облака намеренно не меняет код возврата: сам шлюз — правило платформы, которое появится в EPIC-003, и ронять проверку за нереализованное преждевременно. (4) `infrastructure.md` §4.2 и §6.2 всё ещё описывают удалённую переменную как источник порта — добавить в T-399.
- 2026-09-11 · EPIC-001 · code-reviewer#2 (Opus): T-404 — ВЕРНУТЬ (Critical 1, Major 8, Minor 12, Nit 3). Заявленное автором подтверждено исполнением: порт снят с НАСТОЯЩЕЙ командной строки процесса, подменяется только имя хоста Docker, старая переменная игнорируется обеими реализациями, ключ отсутствует в списке процессов, откат на контрактную точку работает, все цели зелёные. Но ревью вскрыло то, чего задание не предусматривало.
- 2026-09-11 · EPIC-001 · ГЛАВНЫЙ ВЫВОД РЕВЬЮ T-404: единой стала ПЕРЕМЕННАЯ, а не ПРАВИЛО. Адрес выводится в четырёх местах — двух скриптах сервера и двух скриптах замера, — и правила расходятся: замер срезает `/v1`, сервер нет; замер не подменяет имя хоста Docker и не имеет отката на контрактную точку. Прямое следствие, измеренное на стенде: `make bench` умер бы с «процесс LLM не запущен» при живом сервере. То есть исходный дефект не устранён, а перенесён в соседний файл. Настоящее решение — один модуль вывода адреса на все четыре точки.
- 2026-09-11 · EPIC-001 · Critical ревью T-404: пустой `MV_LLM_URL` даёт ЗЕЛЁНУЮ проверку в варианте PowerShell — оператор `??` не реагирует на пустую строку, и цель печатает «нечего проверять» с кодом 0, тогда как вариант для оболочки возвращает 1. Строгий `make health` был бы зелёным при ненастроенном LLM на основной машине проекта. Тот же класс ложно-зелёного, что и вчерашний ложный отказ ядра, только в обратную сторону.
- 2026-09-11 · EPIC-001 · Решения оркестратора по ревью T-404 · Итерация 2: (1) M-1 делается ВНУТРИ T-404, отдельной карточки не будет: суть задачи — «один источник», а пока правило продублировано, задача не выполнена; извлечь вывод адреса в общий модуль и использовать во всех четырёх скриптах. (2) Вопрос про `/v1` закрывается так: `MV_LLM_URL` — базовый адрес БЕЗ `/v1`; общий модуль срезает завершающий `/v1`, если оператор его указал, потому что документация облачных вендоров обычно даёт адрес именно с ним; правило записать в `.env.example` и руководство. Решение не изобретено — оно уже воплощено в скриптах замера, просто не было общим. (3) Предупреждение о закрытом шлюзе облака ОБЯЗАНО менять код возврата: эндпоинт отвечает, а провайдер откажется стартовать — это тот же ложно-зелёный, что Critical. (4) SEC-15: предупреждать и всё равно привязываться к нелокальному интерфейсу — не контроль, а его имитация; при нелокальном значении запуск ОТКЛОНЯТЬ. (5) `infrastructure.md` остаётся за architect#1 в T-399, но пункт про удалённую переменную поднимается в приоритете: на этот документ ссылаются шапки обоих скриптов. (6) Записи T-404 в dev-log нет — третий случай за волну; написать в этой итерации.
- 2026-09-11 · EPIC-001 · УТОЧНЕНИЕ ВЛАДЕЛЬЦА по адресу LLM (принимается, уточняет решение оркестратора): проверять наличие `/v1` и дописывать, только если его нет; если есть — не дописывать. То есть принимать ОБЕ формы, какую бы ни дала документация вендора, а не требовать от оператора нормализовать адрес руками. Это лучше прежней формулировки «базовый адрес без `/v1`»: она перекладывала работу на человека там, где её делает одна строка кода.
- 2026-09-11 · EPIC-001 · ТЕХНИЧЕСКАЯ ТОНКОСТЬ, которую обязана учесть реализация и которую надо проверить в приёмке: «дописать `/v1`, если его нет» верно для точек ПОД `/v1` (`/v1/models`, `/v1/chat/completions`), но `/health` живёт НЕ под `/v1`. Для адреса вида `https://api.вендор/v1` проверка здоровья обязана идти на `https://api.вендор/health`, а не на `.../v1/health`. Поэтому внутреннее представление одно: базовый адрес без `/v1`, а `/v1` дописывается там и только там, где точка действительно под ним. Внешне это и есть требование владельца: на входе принимаются обе формы, удвоения `/v1/v1/...` не возникает ни при какой. Если реализация итерации 2 сделала иначе — правится в приёмке.
- 2026-09-11 · EPIC-001 · devops-engineer (Opus): T-404 итерация 2 — правило вывода адреса вынесено в один модуль на реализацию, все четыре скрипта им пользуются, собственного вывода адреса не осталось ни в одном. Стенд паритета на 32 входа: 0 расхождений против 10 из 24 в ревью. Закрыты Critical и все Major кроме документа архитектора. Сверх ревью найден и закрыт ещё один дефект того же класса: скрипт замера передавал ключ в аргументах curl, то есть ключ был виден в списке процессов.
- 2026-09-11 · EPIC-001 · Показательная деталь итерации 2: разбор адреса в варианте PowerShell сделан вручную, а не встроенным типом. Причина названа исполнителем прямо: встроенный тип оставляет скобки у литерала IPv6 (из-за чего петлевой адрес объявлялся облаком), молча теряет userinfo там, где оболочка его сохраняет, и принимает то, чего не примет HTTP-клиент платформы. Паритет проще держать, чем доказывать.
- 2026-09-11 · EPIC-001 · Требование владельца по `/v1` проверено оркестратором на живом сервере в трёх формах адреса: без суффикса, с ним и с ним же плюс завершающая косая черта. Во всех трёх итоговый запрос идёт в `/v1/models` ровно один раз, удвоения нет, а проверка здоровья идёт по базовому адресу, а не под `/v1`. Тонкость, записанная в журнал заранее, реализацией учтена.
- 2026-09-11 · EPIC-001 · Побочное подтверждение на живом сервере: список моделей теперь печатается полностью, 22 модели. До правки печаталась одна, последняя — жадный разбор. Это был не косметический дефект: именно список моделей задача назначила заменой закрепления версии рантайма для не-llama.cpp провайдеров.
- 2026-09-11 · EPIC-001 · Создана задача T-405: стенд паритета собран во временном каталоге и при следующей правке скриптов исчезнет. Паритет двух реализаций дважды за одну задачу оказывался нарушенным, причём с противоположными кодами возврата на одном входе, — свойство обязано держаться прогоном в CI, а не аккуратностью исполнителя. Счётчик задач 404 → 405.
- 2026-09-11 · EPIC-001 · **ПРОИСШЕСТВИЕ: живой LLM владельца остановлен агентом.** code-reviewer#2 при проверке доверия к PID-файлу выбрал «подопытный» процесс по имени `python.exe`, тогда как его собственный назывался `python3.13.exe`; выбранный номер принадлежал процессу владельца, и `llm-server.ps1` при `up` выполнил по нему принудительную остановку. Проверено оркестратором: порт 8888 никем не слушается, обращения уходят в никуда. Несколько процессов `python` в системе остались, но сервер поднимался владельцем вручную и `MV_LLM_BIN` не задан — обвязкой его не поднять, и я не пытался: чужой процесс на GPU запускать не моё дело. Требуется ручной перезапуск владельцем.
- 2026-09-11 · EPIC-001 · Происшествие — натурное подтверждение замечания M-5 того же ревью, а не случайность: скрипты доверяют номеру из PID-файла БЕЗ проверки, что процесс наш, и выполняют по нему принудительную остановку. M-6 первого ревью был закрыт наполовину — добавили проверку «занят ли адрес», но не проверку «наш ли процесс». Пока это не исправлено, запускать цели `llm-up` и `llm-down` агентам запрещено: правило внесено в постановку итерации 3.
- 2026-09-11 · EPIC-001 · code-reviewer#2: T-404 ревью #2 — ВЕРНУТЬ (Critical 0, Major 5, Minor 12, Nit 4). Блокирующее замечание первого ревью закрыто по-настоящему и подтверждено исполнением; главное заявленное — «правило одно на реализацию» — сделано, замер на самом злом входе доходит до конца, удвоения пути не возникает ни разу; требование владельца по `/v1` реализовано ровно как записано, включая тонкость про проверку здоровья не под `/v1`. Ревьюер собрал СВОЙ стенд на 69 входов и нашёл шесть расхождений, которых не было в наборе автора: невидимые символы Unicode в адресе дают противоположные коды возврата, причём вариант PowerShell отвечает зелёным по адресу, которого в переменной нет; ключ с управляющим символом в PowerShell молча обрезается и добавляет на сервер посторонний заголовок, собранный из хвоста значения.
- 2026-09-11 · EPIC-001 · LLM владельца перезапущен, полная проверка оркестратором после перезапуска: `make llm-health` — 0, строгий `make health` — 0 (шлюз, память, ядро и LLM), `make ci` — 0, `mvctl env check` — 68 переменных, 0.
- 2026-09-11 · EPIC-001 · Наблюдение при перезапуске, объясняющее весь сюжет с `/health`: под сервером владельца работает НАСТОЯЩИЙ `llama-server` на служебном порту, у которого `/health` есть и отвечает 200, а наружу на 8888 выставлен фасад `unsloth-studio`, у которого этой точки нет. То есть откат на контрактную точку нужен не потому, что «llama.cpp другой», а потому что между платформой и llama.cpp стоит чужой фасад. Это ровно тот случай, ради которого владелец и сказал «провайдером может быть что угодно, совместимое с OpenAI».
- 2026-09-11 · EPIC-001 · Решение оркестратора по M-4 ревью #2: снять умолчание у `MV_LLM_URL` и объявить переменную обязательной. Адрес LLM невозможно угадать, а тихое умолчание на `127.0.0.1:1234` — ровно та ловушка, с которой задача и началась: платформа считала бы, что адрес есть, обвязка — что его нет.
- 2026-09-11 · EPIC-001 · В постановку итерации 3 внесён ЗАПРЕТ агентам запускать цели старта и остановки LLM в рабочем дереве, с указанием причины и правила: опыты с процессами только в изолированной копии и только над процессами, созданными самим агентом, опознаваемыми по уникальному аргументу командной строки, а не по имени. Опознание по имени и есть механизм происшествия.
- 2026-09-11 · EPIC-001 · devops-engineer (Opus): T-404 итерация 3 — закрыты все пять существенных замечаний, двенадцать мелких и три дефекта, которых в ревью не было: их нашёл собственный стенд исполнителя. Стенд паритета собран заново, 70 входов, ноль расхождений (в первом ревью было 10 из 24, во втором 6 из 69). Опознание своего процесса: в файл пишется номер, путь программы и время старта; перед остановкой сверяется путь, а расхождение времени старта больше двух секунд означает переиспользованный номер. Три исхода вместо одного, и ни один из них не «убить». Сценарий происшествия проверен в изолированной копии: чужой процесс остаётся живым.
- 2026-09-11 · EPIC-001 · Третий источник истины закрыт оркестратором: `docker-compose.yml` держал у адреса LLM умолчание на порт 1234, и после того как переменная стала обязательной, контейнеры получали бы адрес, в который не верят ни платформа, ни обвязка. Умолчание снято, поставлен громкий отказ; значение взято в кавычки, потому что двоеточие в тексте сообщения YAML читает как вложенное отображение. Проверены обе стороны: с файлом настроек владельца конфигурация собирается, без переменной — отказ с внятным текстом.
- 2026-09-11 · EPIC-001 · Правка линтера композиции оркестратором, вскрытая предыдущей: разбор примера настроек запоминал только ПОСЛЕДНЮЮ строку блока комментария, поэтому пометка обязательности, стоявшая на шестнадцать строк выше переменной, была невидима, и правило 7 объявляло заполненную чистую машину незаполненной. Это ровно то замечание, которое ревьюер отметил в первом ревью как мелкое. Починен разбор, а не подогнан файл: теперь читается весь непрерывный блок, и позиция пометки внутри него значения не имеет. Проверено, что ложных срабатываний нет — обязательными считаются ровно семь настоящих переменных, а пояснительный блок про саму пометку никого не задевает.
- 2026-09-11 · EPIC-001 · Коммит `1493b3d` «T-404: one source of truth for the LLM address, and it is not llama.cpp-shaped»: 19 файлов, +4114 −300. Все восемь хуков пройдены. После коммита дерево перепроверено: проверка LLM, строгая проверка здоровья, сборка и линтер — всё зелёное. Push, слияние и тег не делались.
- 2026-09-11 · EPIC-001/003 · **ВОЛНА 1 НАЧАТА** по решению владельца. Порядок взят из рекомендации приёмки волны 0: сперва заявки архитектору по расхождениям контрактов (это предусловия, без них две задачи моста придут с требованиями, противоречащими тому, что волна 0 закрепила тестами), параллельно — схемы событий бридж-блока, которые от этих правок не зависят. Дальше по цепочке: T-215, затем T-219 вместе с T-400 (методы атаки и бегства у харнесса), T-220, T-255; вторым потоком независимые T-394 и T-395.
- 2026-09-11 · EPIC-001/003 · Волна 1, состав первой пары: architect#1 — сведение всех накопленных расхождений контрактов (C-01, C-03, C-05, §17); developer#3 — T-214 «Схемы событий EPIC-003, часть 1: рой, тики, мир, регион, NPC, законы», размер M. Задачи независимы по файлам: архитектор правит `contracts.md`, разработчик создаёт схемы и регистрирует типы.
- 2026-09-11 · EPIC-003 · Лимит модели Fable исчерпан: architect#1 упал сразу после старта, не успев записать ни одного файла (проверено — правок в `architecture/` и `analysis/` нет). По действующему указанию владельца от 2026-09-09 «лимит исчерпан — спускаться на ступень ниже, не останавливать работу» задача перезапущена на Opus с тем же заданием и пометкой в отчёте о работе на модели ниже плановой. Маршрутизация моделей на остаток волны 1: тяжёлое сведение контрактов и сложный разбор — Opus вместо Fable, разработка и ревью — Opus, документы и статусы — Sonnet.
- 2026-09-11 · EPIC-003 · architect#1 (Opus, ступень ниже плановой): сведение расхождений контрактов выполнено. C-01 → v1.2, C-02 → v1.3, C-03 → v1.2, C-04 → v1.2, C-05 → v1.2, `state-and-mechanics.md` → v0.3 с новым сводным разделом. Заведены ADR-022 (гарантия первого офсета), ADR-023 (границы `Close`), ADR-024 (канал ошибки в интерфейсе механики); счётчик ADR 21 → 24. В шапку контрактов внесено сквозное ПРАВИЛО ПРИОРИТЕТА: при расхождении контракта и принятого кода спор решается доказательством — побеждает код, правится документ; побеждает контракт, заводится задача с пометкой об изменении контракта; молча оставлять расхождение нельзя. Это ровно то, чего не хватало всю волну 0.
- 2026-09-11 · EPIC-003 · Самостоятельное решение архитектора, принимается: заявка «контекст обработчика отменяется при `Close`» ОТКЛОНЕНА на MVP-1, и довод сильнее, чем цена правки. C-02 требует, чтобы State публиковал факт только после успешной записи в хранилище; обработчик, прерванный посреди записи, оставил бы мир в состоянии, которое эта гарантия запрещает. Отмена обязана приходить через контекст вызывающего, а не через закрытие шины. Пересмотр разрешён только вместе с правилом «начатую запись доводить через контекст без отмены». Взамен назначен детерминированный якорь для тестов: чтение диапазона от конца журнала, а не закрытие. Это прямо адресует то, что стоило T-014 четырёх ревью.
- 2026-09-11 · EPIC-003 · Границы `Close` определены НАМЕРЕННО НЕОПРЕДЕЛЁННЫМИ там, где обе реализации законны: число доставок после закрытия не специфицируется (3 из 40 против 40 из 40), коммит обработчика, вернувшего успех под отменённым контекстом, не гарантирован (чистая доставка хотя бы один раз, идемпотентность обработчика и так обязательна), контекст обработчика при закрытии не отменяется. Вместо чисел набору назван список свойств, которые он вправе проверять, — в том числе для T-395. Это снимает вопрос, из-за которого набор не мог закреплять расхождение.
- 2026-09-11 · EPIC-003 · Мест, где правка контракта требует правки кода, оказалось ДВА, а не одно. Первое известно: канал ошибки у выбора цели, задача T-053 в EPIC-002. Второе вскрылось при сведении: раз гарантия первого офсета признана контрактом, необязательный интерфейс готовности в заглушке состояния становится лишним и подлежит удалению вместе с его тестом, а взамен нужен кейс контракта «группа, созданная после публикации, получает событие с первого офсета». Владельца у этой правки не было — заведена карточка T-406, отдельной задачей по рекомендации архитектора: другой пакет, другой эпик-владелец, другая метка. Счётчик задач 405 → 406.
- 2026-09-11 · EPIC-003 · В область T-399 (ревизия `infrastructure.md`) добавлен пункт: гарантия первого офсета сужает выбор политики топиков — `auto.offset.reset=latest` для тем платформы применять нельзя, и это должно быть записано там, где настраиваются топики.
- 2026-09-11 · EPIC-003 · Оставлено неопределённым сознательно: чем заполнять трофей в исходе боя — поле вида существа в действующем лице против отдельного аргумента; обе формы совместимы с зафиксированным контрактом, выбор делает T-053 на настоящей реализации, потому что сегодня это была бы догадка. ADR-023 стоит перечитать системному архитектору при следующей ревизии: это единственное место, где архитектор принял решение, а не зафиксировал уже принятое.
- 2026-09-11 · EPIC-003 · developer#3 (Opus): T-214 — реальный объём задачи оказался ДРУГИМ, чем в постановке. Все 22 схемы и вся регистрация уже существовали: они пришли волной 0 в блоке «в» и вошли в коммит `69d1aed`; `mvctl contracts check` был зелёным ДО первой правки. Исполнитель сверил все 22 схемы построчно с контрактом API и расхождений не нашёл, включая три пункта, вынесенные в задачу отдельно. Механизм регистрации «без издателя» в реестре уже есть и используется — обходить не понадобилось. Недоставало трети задачи: фикстур не было вовсе. Сделано 44 фикстуры, тест с мутационной проверкой (четыре подмены, все убиты), правка описаний в двух схемах. PR в реестр не потребовался, ревью системного архитектора по этому пункту снимается.
- 2026-09-11 · EPIC-003 · Три решения исполнителя T-214, которые стоит перенести на остальные фикстуры: фикстура содержит только полезную нагрузку, а конверт тест строит из реестра — тест, повторивший бы контракт у себя, продолжал бы проходить после сдвига контракта; версия схемы вынесена в имя файла, потому что фикстура под схему, которую реестр перерос, опаснее отсутствующей; проверка идёт чтением через дозапись, а не публикацией, потому что публикация тоже валидирует и читающая сторона проверялась бы против сообщений, которых не бывает. Третий шаг — та же невалидная фикстура читается повторно в мягком режиме и ДОХОДИТ до обработчика: без него шаг «не доходит» доказывал бы лишь, что событие не пришло, и не отличал бы работающую схему от потерявшей его шины.
- 2026-09-11 · EPIC-003 · Ловушка, закреплённая исполнителем T-214 не правкой, а невалидной фикстурой: у механики пять видов действия (включая отдых), а у события решения боя — четыре. Издатель, который пройдётся циклом по списку видов, произведёт значение, которого схема не знает. Это не дефект — отдых меняет состояние другим путём, — но теперь ловушка стоит в тесте, а не в чьей-то памяти. Тем же способом закреплена ловушка с точностью чисел: невалидная фикстура несёт хеш содержимого тем самым числом из журнала, которое не переживает разбор.
- 2026-09-11 · EPIC-003 · ПРОБЛЕМА ПЛАНИРОВАНИЯ, поднята исполнителем и подтверждена оркестратором: постановки бридж-блока писались до того, как волна 0 выдала блок «в», и описывают уже сделанную работу. Проверено по T-215: все пять её схем существуют, а фикстур для них нет ни одной. То есть T-215 — это не «создать схемы», а «сверить схемы с решениями сведения 3 и добавить фикстуры». Остальные задачи блока (T-219, T-220, T-255) проверю тем же способом перед постановкой, а не после.
- 2026-09-11 · EPIC-003 · Предложение исполнителя о переносах строк ПРОВЕРЕНО И ОТКЛОНЕНО: он сообщил, что рабочее дерево держит файлы Go с возвратами каретки вопреки правилу нормализации, и предложил общую правку репозитория. Замер: и в индексе, и на диске все 157 отслеживаемых файлов Go вне архива чистые, возвратов каретки нет ни одного. Проблема была в файлах, созданных инструментами самого агента, а не в репозитории; общая правка не нужна.
- 2026-09-11 · EPIC-003 · developer#3 (Opus): T-215 — сверка пяти схем с решениями сведения 3 нашла ОДНО расхождение, и оно того рода, что проявилось бы только в бою. Блок разбора в выводе модели закрыт для новых полей и не оставлял места длине отброшенного рассуждения, которую контракт C-15 требует туда писать: шлюз физически не смог бы её записать, и событие отвергалось бы при публикации. Исправлено опциональным полем. Остальные пять пунктов совпали, реестр править не пришлось. Добавлено десять фикстур; восемь подмен по фикстурам и схемам и две по реестру — все убиты. Итог по дереву: 27 типов, 54 файла фикстур, 81 подтест.
- 2026-09-11 · EPIC-003 · Приём, который стоит перенять: равенство перечня причин отказа с будущим пакетом охраны закреплено тестом-заглушкой, который читает файл как исходник и включается САМ, когда пакет появится, а пока печатает в пропуске требуемый перечень. Мутация «создать пакет с девятью причинами вместо десяти» его будит. Это лучше, чем пометка «сделать потом»: пометку не исполняют, а тест сам себя предъявит.
- 2026-09-11 · EPIC-003 · Риск утечки в событиях, названный исполнителем и закрытый оркестратором наполовину. Поле имени провайдера было объявлено строкой без ограничения, тогда как проза требовала «только имя и ничего больше»: туда помещался адрес, а событие живёт в топике записей модели весь срок хранения. Сужено шаблоном в обеих схемах; проверено — все пять настоящих имён проходят, оба вида адреса отвергаются, включая тот, что несёт ключ в параметрах запроса. Сделано сейчас потому, что издателя ещё нет: это самый дешёвый момент сузить схему.
- 2026-09-11 · EPIC-003 · Вторая половина риска утечки НЕ закрывается схемой и уходит требованием в T-211: свободная строка сообщения об ошибке — именно туда попадает ошибка HTTP-клиента, а она в Go выглядит как «Post "http://адрес/v1/chat/completions": …», то есть несёт адрес провайдера, а при ключе в параметрах запроса и ключ. Схема тут ни при чём, сообщение нужно; требование в том, чтобы шлюз строил его через редактор секретов, а не отдавал текст ошибки провайдера как есть.
- 2026-09-11 · EPIC-003 · Открытые вопросы T-215 к архитектору, в отдельную задачу: два пункта ADR-016 устарели после дополнения к ADR-017 (называют статус, которого больше нет, и требуют хранить сырой ответ там, где решение оркестратора велит не хранить); имя третьего поля в событии облака расходится между контрактом и анализом; блок ошибки при статусе «ошибка» объявлен обязательным в модели данных, но схемой не требуется, а запись со статусом ошибки без кода бесполезна для разбора.
- 2026-09-11 · EPIC-003 · Проверка состава перед постановкой (правило, введённое после T-214): T-219 — настоящая новая работа, файлов заглушки встречи и контекста в дереве нет. T-220 частично сделана волной 0: рассказчик существует, но только с нарративом входа и раунда; задача добавляет виды «ход», «событие мира» и «смерть». T-255 — хук, которого нет, как и решено при сокращении T-018.
- 2026-09-11 · EPIC-003 · ВАЖНО для будущей постановки T-220: её текст велит отвечать на осмотр местности видом «ход», но C-05 v1.2 после сведения контрактов говорит «вход», и код волны 0 закрепил это тестом. Постановка устарела так же, как у T-214 и T-215. При запуске T-220 указать прямо: вид «вход», текст задачи в этом месте не исполнять.
- 2026-09-11 · EPIC-003 · Запущены параллельно T-400 (методы атаки и бегства у харнесса, developer#1, каталог шлюза) и T-219 (заглушка встречи, developer#3, каталог роя). Каталоги не пересекаются. Зависимость честно разведена: пока методов харнесса нет, тесты заглушки публикуют действия игрока прямо в шину, а полный сквозной сценарий на 30 ходов вынесен на приёмку, когда обе задачи лягут. Отдельно поставлен вопрос, которого нет в исходной задаче: чего должна ждать атака, если предложение об изменении публикует не харнесс, а заглушка встречи, — без явного ответа сценарий станет гонкой, а такие места волна 0 ловила.
- 2026-09-11 · EPIC-003 · **РЕШЕНИЕ ОРКЕСТРАТОРА по срочному вопросу T-400 → T-219: вариант (б).** Вопрос: что делает заглушка встречи, когда и удар игрока, и ответ существа прошли мимо (по таблице двойника механики это около 9 % ходов)? Если предложение не публикуется вовсе, ожидание атаки упирается в срок, и сценарий на тридцать ходов падает почти наверняка; при этом пакет без операций хранитель состояния отвергает, так что «публиковать пустое» решением не является. Принято: **каждое разрешённое встречей действие порождает ровно одно атомарное предложение**, и это не подгонка под харнесс. Основание в принятой модели данных: сущность встречи несёт участников с их состоянием и нанесённым уроном и существ с последним ударившим. Промах не меняет здоровья, но меняет УЧАСТИЕ — а без записи участия раунд невозможно закрыть ни по «все походили», ни по порогу пропущенных ходов, то есть по тем самым полям срока и порога, которые задача T-219 требует класть в событие начала встречи. Промах расходует ход, и это доменная правда, а не удобство теста.
- 2026-09-11 · EPIC-003 · Следствия решения: терминальный маркер конца реакции не нужен, правило «одно действие — один пакет» остаётся (оно же ответ на второй открытый вопрос T-400); если когда-нибудь встреча ответит на одно действие двумя пакетами — например изменением и созданием трофея, — это правка контракта, а не догадка заглушки, и до неё харнесс вправе возвращаться по первому. Записать в C-05 при следующей ревизии контрактов.
- 2026-09-11 · EPIC-001 · developer#1 (Opus): T-400 — харнесс получил атаку и бегство, сценарий получил боевой шаг, покрытие 89,5 → 91,1 %. Ожидания сквозного теста по-прежнему выводятся из скрипта, включая бой: мутация «добавить в скрипт ещё один удар» оставляет набор зелёным, мутация «боевой шаг не сообщает свой тип события» роняет три теста.
- 2026-09-11 · EPIC-001 · Разбор исполнителя, чего должна ждать атака, — образец того, как надо отвечать на такие вопросы. Три варианта взвешены замером, а не рассуждением. «Не ждать» — гонка не гипотетическая: под мутацией сценарий падает на шаге выхода с конфликтом версий, потому что встреча сдвинула версию персонажа за спиной харнесса. «Ждать решения боя» отвергнуто, потому что решение публикуется ДО изменения и версии не несёт — это сказано в описании самой схемы, — и потому что потребовало бы подписки на тип, у которого до T-219 нет ни одного издателя, то есть ровно той «объявленной, но не питаемой подписки», от которой отказались в T-018. Осталось «ждать факт изменения» — единственная точка, где модель чтения харнесса держит то же, что держит хранитель состояния.
- 2026-09-11 · EPIC-001 · Механика ожидания сделана без знания внутренностей встречи: действие открывает слот ожидания по идентификатору соответствия ДО публикации, чужое предложение с тем же соответствием сообщает, какие сущности сейчас сдвинутся, и действие возвращается, когда по КАЖДОЙ названной пришёл факт. Порядок прихода не важен, решает разность множеств. Возврат по первому факту был бы дефектом: пакет атомарный и называет двоих, а факты публикуются по возрастанию идентификатора.
- 2026-09-11 · EPIC-001 · Мутация нашла слабое утверждение в тесте самого исполнителя: проверка «в бою должен быть противник» требовала только «ошибка есть», а действие, которому никто не отвечает, тоже кончается ошибкой по сроку — тест оставался зелёным без обеих преград, просто ожидая три секунды. Исправлено: каждый случай называет причину отказа, и мутация теперь краснеет мгновенно. Это ровно тот класс, который мы ловим всю дорогу: тест, который нельзя провалить, ничего не проверяет.
- 2026-09-11 · EPIC-003 · developer#3 (Opus): T-219 — заглушка боя и контекст роя готовы, 30 новых тестов, покрытие 82,9 %. Восемнадцать подмен, убито пятнадцать. Одна выжила осмысленно: снятие проверки «вход без региона» ничего не меняет, потому что в пустом регионе живого существа всё равно не находится — это защита экспортированного обработчика, а не поведение на шине. По ходу проверки исполнитель УБРАЛ недостижимую ветку: счётчик выданных трофеев не убивался ни одной подменой, потому что существо становится терминальным в проекции в момент публикации смертельного удара и целью больше не предлагается; вместо счётчика в коде названы два механизма, которые действительно держат запрет двойного трофея. Убрать код, который ничего не держит, лучше, чем оставить его как «на всякий случай».
- 2026-09-11 · EPIC-003 · Три решения оркестратора по T-219, все внесены сразу:
  1. **Директива подавления линтера заменена именованным правилом.** Заглушка боя обязана говорить типами C-03, иначе она и настоящие правила перестают удовлетворять одному интерфейсу — тот же довод, которым обосновано исключение для двойника механики. Но директива подавления живёт вне поля зрения конфигурации и копируется следующим автором, который не знает, зачем она там. Введено правило `shared-testkit-swarm` с единственным разрешением; директива из кода убрана. Проверено зондом: файл в другом общем пакете, тянущийся в контекст, линтер по-прежнему отвергает прежним текстом.
  2. **Уровень агента при создании встречи.** Задача требует уровня задачи, таблица владения §4.6 отдаёт создание встречи уровню домена с причиной появления, а уровню задачи — последующие изменения. Прав документ: заглушка в этот момент действует как GM региона. Правка уходит в итерацию T-219, иначе при включении проверки владения в EPIC-002 сломается интеграция.
  3. **Путь трофея на трупе получил константу.** Он был написан строкой независимо в ДВУХ заглушках — ровно так опечатка становится молчаливым расхождением между ними. Константа добавлена рядом с тремя остальными путями трупа, обе заглушки переведены на неё, тесты зелёные.
- 2026-09-11 · EPIC-003 · Расхождения задачи с принятым кодом, зафиксированные исполнителем T-219: сборка предложения через механику невозможна, функция возвращает «не реализовано» — заглушка строит операции сама, место замены названо (T-053/T-054); выбор цели пока без канала ошибки, при T-053 сигнатура поедет; имя блупринта взято по контракту с префиксом заглушки, а не по тексту задачи; четыре броска — это максимум, а не всегда (при промахе бросок один, при смерти ответа нет), оба края закреплены тестами; перечень состояний участника в модели данных старше кода, следует код.
- 2026-09-11 · ПЕРЕХОД НА ПЛАГИН dev-team 0.6.0 по указанию владельца (работали по 0.3.0; в кеше лежат 0.4.0, 0.5.0, 0.6.0, каталог 0.3.0 помечен как осиротевший). Что изменилось по существу: результат работы роли существует ТОЛЬКО как файл, и оркестратор обязан проверять наличие артефактов после каждого возврата агента, а не верить отчёту; у каждой задачи своя карточка `tasks/T-NNN.md`, а `tasks.md` становится индексом; у архитектурных ролей обязательны диаграммы в `architecture/diagrams/`; появились профили моделей в конфигурации, приёмка эпика отдельным файлом, журнал вопросов требований, сводки перед гейтами и учёт времени.
- 2026-09-11 · Что уже совпадало с 0.6.0 без правок: маршрутизация моделей (тяжёлое размышление на Fable, инженерия на Opus, рутина на Sonnet) — ровно профиль `balanced`; правило спуска на ступень ниже при исчерпании лимита — в 0.6.0 записано как «если Fable недоступен, брать Opus и сообщить пользователю один раз», что я и сделал сегодня; проверка артефактов после агентов — я вёл её вручную весь день (несколько раз именно она ловила недоделанное, например пять файлов T-397 вне индекса).
- 2026-09-11 · Миграция, выполненная оркестратором: в конфигурацию добавлен профиль моделей `balanced`; создано 30 карточек задач по обоим эпикам. Карточки заведены задним числом и честно об этом говорят: описание и определение готовности не дублируются, а ссылаются на индекс — иначе появился бы второй источник истины, ровно та болезнь, которую мы весь день лечили. **Учёт времени не выдуман:** он посчитан по меткам `activity` в живом состоянии, где у оркестратора есть начало и конец каждого вызова агента. Итог: 19 задач с учтённым временем, суммарно 62 часа 40 минут работы агентов. Самая дорогая — T-014 (ворота волны 1): 12 часов на четыре итерации разработки и четыре ревью.
- 2026-09-11 · Пробелы против 0.6.0, оставшиеся незакрытыми и требующие настоящей работы, а не скрипта: каталога диаграмм нет вовсе (обязательны карта контекста, карта контейнеров, карта доменов, компоненты блоков, схема данных, последовательности ключевых сценариев, потоки данных с доверенными границами, развёртывание) — это работа системного архитектора и архитекторов блоков; нет файла приёмки эпика, хотя приёмка волны 0 проведена и лежит в ревью; нет журнала вопросов требований и сводок перед гейтами. Диаграммы ставлю в работу сейчас, остальное — вместе с ближайшим вызовом соответствующей роли.
- 2026-09-11 · Дашборд перезапущен и обновлён до 0.6.0. Причина перезапуска оказалась настоящей: сервер работал из копии плагина 0.3.0, помеченной как осиротевшая. Процесс опознан по порту и сверен по командной строке ПЕРЕД остановкой — опознание по имени после сегодняшнего происшествия не используется.
- 2026-09-11 · Страница дашборда тоже обновлена: шаблон 0.6.0 весит 75 КБ против 46 КБ у 0.3.0 и делает каждую задачу ссылкой на её карточку. Но путь он выводит из идентификатора эпика (`epics/EPIC-001/tasks/…`), а наши папки называются с суффиксом (`EPIC-001-foundation`), поэтому ссылки вели бы в никуда. Проставил путь явно полем `card` у всех 30 задач; проверено по HTTP: и страница, и карточка отдаются кодом 200.
- 2026-09-11 · **МОЯ ОШИБКА, устранена:** запустил `init --force`, чтобы обновить страницу, и он перезаписал `state.js` ПУСТЫМ — команда создаёт состояние заново, а `--force` снимает защиту от перезаписи. Живое состояние со всей историей волны 0 и 1 было потеряно на диске. Восстановлено из индекса git (`git checkout -- state.js`), потому что артефакты я индексирую после каждого шага; потеряны оказались только правки последних десяти минут, которые я применил заново по памяти о собственных действиях. Вывод на будущее: для обновления страницы существует `init --force`, но перед ним состояние надо сохранить отдельно, а лучше обновлять страницу копированием шаблона, не трогая `state.js`.
- 2026-09-11 · code-reviewer#2 (Opus): ревью боевого пути — ОБЕ задачи ВЕРНУТЬ. T-400: Critical 0, Major 3. T-219: Critical 2, Major 1. Главное открытие ревью: **в дереве нет ни одного теста, который сводит харнесс с заглушкой встречи**. Харнесс проверяется против собственной подставной встречи, которая никогда не промахивается и никогда не заканчивает бой; тесты заглушки публикуют действия прямо в шину; сквозные тесты идут без боя. Ревьюер собрал такой зонд сам и прогнал 60 раз: 35 успешных, 25 зависаний, падают все три боевых шага. Это ровно тот класс, ради которого волна 0 строила контрактный набор: две половины, зелёные врозь и рвущиеся вместе.
- 2026-09-11 · Critical-1: **моё решение «одно действие — ровно одно атомарное предложение» в коде НЕ выполнено.** Когда никто не попал, заглушка не публикует ничего, и сущность встречи после создания не обновляется вовсе — а именно ею решение и обосновано. Хуже: обратное поведение ЗАКРЕПЛЕНО тестом, то есть тест охраняет дефект. Прицельный зонд «оба мимо» даёт ноль предложений при ожидаемом одном. Чем наполнить пакет — есть: таблица владения отдаёт уровню задачи сущность встречи по любому пути с причинами боя и бегства.
- 2026-09-11 · Critical-2: действие, которое заглушка не отрабатывает (встреча закрыта, целей нет, вход без региона, чужой мир), остаётся без любого ответа и неотличимо от «никто не слушает». Требование готовности «ноль действий без ответа» не выполняется.
- 2026-09-11 · **РЕШЕНИЕ ОРКЕСТРАТОРА по Critical-2** (открытый вопрос ревью, вариантов было два — заглушка публикует явный отказ, либо харнесс узнаёт состояние встречи): **чинить на стороне харнесса, контракт не трогать.** Довод: четыре случая из перечня — это не события среды, а ошибки сценария. Атака после конца встречи, атака без целей, вход без региона, действие в чужом мире — так писать сценарий нельзя, и правильный ответ на это немедленная внятная ошибка, а не ожидание. Харнесс уже слушает шину, и `encounter.started`/`encounter.ended` дают ему состояние встречи без единой правки контракта. Вариант «заглушка публикует отказ» отвергнут: он потребовал бы нового типа события ради заглушки, которую мы через два эпика снимаем, — ровно то, от чего отказались в T-018.
- 2026-09-11 · **РЕШЕНИЕ по второму открытому вопросу** (сценарий фиксированной длины против того, сколько ходов переживёт существо): сценарий делается адаптивным — бить, пока встреча не кончится, затем продолжать. Скрипт фиксированной длины обещает то, чего механика не гарантирует; это уже отмечено автором T-400 как допущение о балансе. Требование «тридцать ходов» относится к длине прогона, а не к числу ударов по одному волку.
- 2026-09-11 · Замечание Major-3 к T-400 стоит отдельно: свойство «ждать факт по КАЖДОЙ названной сущности» не держится ничем — мутация «возвращаться по первому факту» зелена при одиночном прогоне и краснеет только при двадцати пяти повторах. То есть страж свойства сам является гонкой. Мутации автора били по соседним точкам, по названной им самим — не ставились. Это тот же урок, что и с якорем волны 0: доказательство свойства обязано быть детерминированным, иначе оно не доказательство.
- 2026-09-11 · EPIC-003 · developer#3 (Opus): T-219 итерация 2 — оба блокирующих закрыты. Critical-1: каждое принятое действие теперь кладёт в пакет набор для сущности встречи, и таблица владения это разрешает. При бое движется номер раунда, при сдвиге участия едут участники, при конце боя — состояние, исход и ссылка на событие закрытия. Участники едут только когда участие реально изменилось, иначе хранитель состояния опубликовал бы факт с пустым списком изменений. При промахе меняется ровно один путь — номер раунда, — и пакет всё равно ровно один. Прицельный зонд ревьюера теперь даёт единицу вместо нуля.
- 2026-09-11 · EPIC-003 · Тонкость, которую исполнитель заметил сам: событие конца встречи теперь СТРОИТСЯ до пакета и публикуется после, чтобы пакет мог сослаться на его идентификатор. Иначе закрытие боя поехало бы вторым пакетом, то есть вторым ответом на одно действие — прямое нарушение того самого решения, ради которого итерация и делалась.
- 2026-09-11 · EPIC-003 · Major-1 закрыт по таблице владения: открытие встречи идёт уровнем домена с причиной появления, всё дальнейшее — уровнем задачи. Проверка сделана не списком в тесте, а сверкой каждого предложения с настоящими правилами владения из реестра: десять предложений, ноль отклонений. Тест дополнительно требует, чтобы прогон произвёл каждый из пяти видов предложения — иначе он был бы зелёным над меньшим, чем обещает. Это правильная форма такой проверки.
- 2026-09-11 · EPIC-003 · Двенадцать мутаций, убиты все. Одна сначала ВЫЖИЛА: «участники едут всегда, изменилось участие или нет» — свойство держалось только комментарием. Исполнитель не стал убирать защиту, а усилил тест утверждением «у пакета промаха ровно одна операция». Это верный ход: слабое место найдено мутацией и закрыто утверждением, а не удалением проверки.
- 2026-09-11 · EPIC-003 · Открытые вопросы итерации 2 T-219: (1) контракт C-05 молчит о действии, которое встреча не принимает; по решению оркестратора отказ живёт на стороне харнесса, но вопрос вернётся, когда заглушку сменит настоящий агент встречи — записать в контракт заранее или нет, решает тимлид. (2) Перечень состояний участника в модели данных расходится с кодом, и теперь это расхождение попало в публикуемые данные — свести. (3) Тест-часовой на пару задач затрагивает оба каталога, и исполнитель честно говорит, что в одиночку завести его не может, не нарушив карту владения.
- 2026-09-11 · EPIC-001 · developer#1 (Opus): T-400 итерация 2 — в дереве появился стенд, которого не было: харнесс, заглушка состояния и НАСТОЯЩАЯ заглушка встречи на одной шине, шестнадцать боёв за прогон. Замер на 45 боях в четырёх сочетаниях версий — образец того, как надо отвечать на вопрос «чья вина»: старый харнесс со старой заглушкой 23 из 45, новый харнесс со старой заглушкой 31 (остаются ровно её дефекты), старый харнесс с новой заглушкой 33 (остаются ровно его), новый с новым — 45 из 45.
- 2026-09-11 · EPIC-001 · Причина, по которой мутация «возвращаться по первому факту» выживала, оказалась не в слабом утверждении, а в негодном стенде: оба факта атомарного пакета публикует один и тот же двойник состояния одним куском, и первый факт отстаёт от второго на микросекунды. Против настоящего хранителя состояния это свойство проверить нельзя в принципе. Исполнитель убрал состояние со стенда и поставил подставного ответчика, который публикует ровно те факты, которые просит случай: оба, только персонаж, только существо, ни одного. Мутация краснеет 3 прогона из 3 вместо 0 из 10. Урок общий: если свойство не проверяется, иногда виноват не тест, а стенд.
- 2026-09-11 · **РЕШЕНИЕ ОРКЕСТРАТОРА по остаточной гонке (открытый вопрос T-400 №1).** Симптом: заглушка отвечает на удар по своему представлению о мире, которое пополняет фактами по отдельной подписке; если удар обгоняет факт, ожидаемая версия оказывается на единицу меньше и хранитель состояния отвергает весь пакет. Замер: 2 отказа на 270 боёв, стенд краснеет примерно раз в двенадцать прогонов. Вариант «уменьшить число боёв на стенде» отвергнут сразу — это прятать дефект. Вариант «дождаться, пока догонит журнал» ТОЖЕ отвергнут, и по существу: контракт прямо говорит, что порядок между топиками не гарантирован, а дизайн уже опирается на это (событие закрытия раунда сделано самодостаточным именно для того, чтобы агент не ждал события из чужого топика). Значит «дождаться» невыразимо и противоречит принципу. **Принято: повтор при конфликте версий.** Оптимистическая блокировка для того и существует: получив отказ по конфликту, агент обновляет представление по пришедшему факту и предлагает заново, ограниченное число раз. Настоящему агенту встречи придётся делать ровно это, поэтому заглушка обязана показать правильную форму, а не обойти проблему.
- 2026-09-11 · Следствие для контракта: правило «агент, получивший отказ по конфликту версий, обновляет представление и предлагает заново» стоит записать в C-05 рядом с описанием агента встречи — сейчас об этом молчат и контракт, и дизайн, а без него каждый следующий исполнитель будет заново открывать ту же гонку. Заявка architect#1.
- 2026-09-11 · system-architect#1 (Opus): создан каталог диаграмм — 18 файлов, 17 диаграмм и указатель. Обязательный набор плагина 0.6.0 закрыт с запасом: четыре сценария вместо трёх и три жизненных цикла вместо двух. Работа велась ОТ ДЕРЕВА, а не от текста, и это оказалось решающим: из семи контекстов платформы написан один, остальные зарегистрированы пустыми заглушками, а играбельный срез собран из двойников. Поэтому во всех диаграммах три вида узлов — существующий, заглушка, будущее, — и каждый файл несёт разделы «что здесь будущее» и «расхождения с деревом». Все 17 блоков проверены машинно на парность и соответствие объявленному типу.
- 2026-09-11 · Рисование нашло 24 расхождения документов с кодом. Самое важное: **`shared/agent/levels.go` не существует**, хотя контракт объявляет его единственной истиной таблицы владения и требует блокирующего теста равенства с копией — фактической истиной служит копия, ровно то, что процедура запрещает. Далее: `components/*.md` описывают ответственность всех семи контекстов как реализованную; нет каталогов блупринтов, законов, конфигурации и схем агентов, на которые ссылаются пять контрактов; `shared/agent` остался прежним, ни один целевой файл не создан; имя переменной допущенных клиентов шлюза расходится между контрактом, манифестом и композицией, причём значения по умолчанию разные.
- 2026-09-11 · **Заглушка рассказчика уже́ контракта, и сквозной тест этого не ловит.** Контракт перечисляет шесть поводов для нарратива, код знает три. Следствие: соло-удар сегодня не порождает нарратива вообще, а сквозной тест зелёный, потому что ожидания выводятся из той же таблицы, что и реализация. Это прямая работа T-220, и находка подтверждает, что задачу надо делать, а не считать сделанной волной 0.
- 2026-09-11 · ПРОВЕРЕНО ОРКЕСТРАТОРОМ, утверждение подтвердилось: `MV_MODE` и `MV_BUS` объявлены в манифесте, передаются композицией и не читаются никем — обращений к ним в коде нет. Настоящий выбор идёт флагами, а композиция передаёт только состав контекстов. Оператор, поставивший режим воспроизведения, получит рабочий режим и не узнает. Словари значений расходятся: манифест объявляет один вид шины, флаг принимает два других имени. Тот же разбор относится к адресам шлюза и памяти: объявлены, не читаются, все три процесса берут адрес из переменной ядра. Заведена задача T-408; чинить решено так же, как адрес модели в T-404 — манифест единственный источник, флаг перекрывает.
- 2026-09-11 · Заведена задача T-409 на сведение документов с деревом, туда же внесены пять решений оркестратора и архитектора: правило повтора при конфликте версий — в контракт; молчание встречи на непринятое действие — записать явно; состав зависимостей рантайма — править контракт под код; проверка перехода статуса в себя же — править код под контракт, потому что «ход засчитан, движения не было» уже принятое правило и повтор действия не должен превращаться в ошибку игроку; имя подкоманды отчёта привести к зарезервированному. Счётчик задач 407 → 409.
- 2026-09-11 · Правило, предложенное архитектором и принимаемое: владелец эпика, вливающий настоящий контекст, тем же изменением снимает пометку «будущее» со своей диаграммы. Иначе каталог диаграмм повторит судьбу обзора архитектуры, который к волне 1 оказался старше кода.
- 2026-09-11 · EPIC-003 · developer#3 (Opus): T-219 итерация 3 — повтор при конфликте версий реализован ровно по решению. Замер на 960 боях: гонок семь, и после правки все семь пакетов доходят до состояния (было ноль из семи). Повтору хватало одной попытки во всех случаях. Число гонок правка не меняет — меняет исход. Восемь мутаций, убиты все; покрытие 83,7 → 85,7 %.
- 2026-09-11 · EPIC-003 · Решение по ходу, о котором исполнитель рассказал сам и которое стоит запомнить: первая версия отката возвращала состояние, бывшее ДО пакета, и ломала ровно то, ради чего правка делалась — факт, пришедший в то же окно (а конфликт именно про него), откатывался вместе с пакетом, и пересчёт считал бы от устаревшего здоровья. Поэтому мир заглушки разделён надвое: что говорят факты и что говорят факты плюс пакет в полёте. Откат возвращает к первому.
- 2026-09-11 · EPIC-003 · Заглушка повторяет ТОЛЬКО конфликт версий. Остальные четыре причины отказа не повторяются: это дефекты, а не гонки, и дефект, исчезающий на третьей попытке, хуже дефекта, который видно сразу. Повтор идёт под тем же идентификатором предложения — это та же попытка ответить на то же действие: кости брошены один раз, решение боя опубликовано один раз, и правило «одно действие — один пакет» сохраняется.
- 2026-09-11 · EPIC-001 · **Стенд остался красным по другой причине, и она на стороне харнесса**: он считает ЛЮБОЙ отказ концом ожидания и возвращает ошибку раньше, чем повтор успевает случиться, а повторное предложение с тем же идентификатором принимает за второй пакет. Исполнитель T-219 честно не полез в чужой каталог и передал находку с готовым критерием приёмки: стенд обязан дать ноль красных из шестидесяти прогонов. Запущена итерация 3 T-400 ровно с этим критерием. Это тот случай, когда правка одной половины пары обнажает дефект другой — то самое, ради чего стенд и заводился.
- 2026-09-11 · EPIC-001 · Параллельно запущена T-408: режим и вид шины к одному источнику истины. В задание отдельным пунктом внесено требование проверить ВЕСЬ манифест окружения на переменные, объявленные и не читаемые никем, и назвать по каждой, будущее это или мусор. Чинить все не требуется — требуется назвать: сегодняшний день показал, что такая переменная это не мелочь, а ловушка, в которую оператор попадёт молча.
- 2026-09-11 · EPIC-001 · developer#1 (Opus): T-400 итерация 3 — пара стала зелёной. Стенд: ноль красных из шестидесяти против семи до правки, и все семь падений до этого были одной природы, то есть диагноз коллеги подтвердился буквально. Отдельный зонд на тех же шестидесяти прогонах: гонок десять, пережитых повтором десять, «издатель сдался» ноль. Частота гонок не изменилась — изменился исход.
- 2026-09-11 · EPIC-001 · Разделительная линия проведена не по «отказ или не отказ», а по «гонка или дефект». Конфликт версий продлевает ожидание, четыре остальные причины кончают его немедленно, пакет без сущностей кончает немедленно. Сообщение на сроке теперь различает «издатель сдался после повторов» и «никто не ответил» — иначе следующий искал бы не там. Прямо проверено, что ожидание не стало бесконечным: срок заводится один раз до цикла, ветка гонки его не касается, и это закреплено тремя случаями теста плюс отдельным тестом против настоящего состояния, где встреча всегда на версию позади и никогда не предлагает заново.
- 2026-09-11 · EPIC-001 · Исполнитель честно назвал НЕЗАКРЫТУЮ мутацию: если снять со стенда требование «пакет вернулся фактом», стенд остаётся зелёным, потому что единственный издатель на нём повторяет всегда, и случая «конфликт без возврата» там не бывает. Решение оркестратора: **оставить страховкой, второго боевого двойника не заводить.** Довод: свойство уже держится на уровне модульного теста, где подставной издатель публикует ровно то, что просит случай, включая «сдался после повторов»; строить ради красного второй двойник боя — ровно то, от чего отказались в T-018, и он пережил бы задачу, ради которой создан.
- 2026-09-11 · EPIC-001 · devops-engineer (Opus): T-408 закрыта. Режим и вид шины теперь читаются процессом и решают на самом деле: значение манифеста становится умолчанием флага, а флаг перекрывает его только когда передан явно. Композиция флагов не передаёт, значит в контейнерах решает файл настроек. Процесс печатает при старте не только значение, но и ИСТОЧНИК: «режим воспроизведения, источник — переменная» либо «источник — флаг». Это то, чего не хватало: раньше оператор не мог узнать, подействовала его настройка или нет.
- 2026-09-11 · EPIC-001 · Словарь сведён к одному и сделан общим структурно: проверка строится из перечня самого манифеста, своего списка значений у разбора флагов больше нет, а тест держит равенство словаря с режимами рантайма. Выбрано имя протокола, а не продукта, и довод не про вкус: брокеры и так задаются переменной с именем протокола, клиент кафковый, а переезд на другую реализацию того же интерфейса не должен требовать переименования значения, описывающего то, что не изменилось. Прежнее имя ОТВЕРГАЕТСЯ, а не принимается синонимом: принять синоним значит оставить второй словарь с предупреждением, которое в логе контейнера никто не читает.
- 2026-09-11 · EPIC-001 · Адрес процесса объявлен ОДИН. Разбор исполнителя: рантайм создаёт один слушатель на процесс и отдаёт его всем контекстам набора, поэтому два имени из трёх были мертвы по построению. Вторая причина конкретнее — переменные выглядели настраиваемыми и не были ими: опубликованный порт, проверка здоровья и адрес ядра для шлюза были литералами, поэтому смена «адреса прослушивания» в настройках не переносила сервис, а ломала стенд. Убрана мнимая возможность, а не настоящая.
- 2026-09-11 · EPIC-001 · Проверен ВЕСЬ манифест на переменные, объявленные и не читаемые никем: 26 имён, все — будущее, мусора нет. Важное различие, которое исполнитель сформулировал точно: ловушка возникает не там, где переменная пока не читается, а там, где РЯДОМ с ней живёт флаг, литерал или второй файл, делающий выбор вместо неё. Таких в манифесте после задачи не осталось. Ближайшее к делу из списка — переменная среды исполнения: правило «отладочный уровень журнала не бывает в промышленной среде» сейчас не держится ничем.
- 2026-09-11 · EPIC-001 · Правка `.env` владельца выполнена оркестратором (исполнитель намеренно не трогал личный файл): вид шины приведён к новому имени, две отменённые переменные адресов удалены. Без этого следующий запуск не поднял бы платформу — процессы отказались бы стартовать с текстом, называющим замену. Проверено после правки: сверка манифеста с окружением 66 переменных и код 0, конфигурация композиции 0, линтер композиции 0, проверка здоровья поднятого стека 0, полная сборка проверок 0.
- 2026-09-11 · EPIC-001 · Открытые вопросы T-408 в T-409: имя переменной адреса неудачно — рядом живёт переменная с почти тем же именем, означающая именно ядро, и правильное имя было бы про прослушивание, но переименование задевает пять файлов и диаграммы; механизм вывода из обращения умеет отменять ИМЯ, но не ЗНАЧЕНИЕ, поэтому объясняющая фраза про старое имя шины живёт в отказе процесса, а не в проверке манифеста; режим воспроизведения из окружения включается, а журнал воспроизведения задаётся только флагом, которого композиция не передаёт — новую переменную исполнитель намеренно не завёл, потому что читать её сегодня некому, а это ровно тот класс, который задача и чинит.
