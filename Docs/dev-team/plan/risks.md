# Риски MVP-1

Версия 1.0 · 2026-09-09 · project-manager · статус: к G3. Обновляется на каждом планировании, интеграции и ретроспективе.
Сведено из: `plan/epics.md` v0.3 §7, `plan/decomposition-review.md` §4, `epics/EPIC-001…005/tasks.md` (разделы «Риски»), `testing/strategy.md` §10 и §4, `architecture/consolidation.md` §10–§13, `architecture/threat-model.md` (Critical/Major), `architecture/infrastructure.md` v0.3 §11/§13, `journal.md`.

Шкала: вероятность и влияние — **Н / С / В**. Реакция: **избежать / снизить / принять / передать**. Статус: `открыт / снижен / реализовался / закрыт`.
Владелец — роль, которая ведёт реакцию; решения продуктового объёма принимает пользователь.

## 1. Процесс и ресурс

| ID | Риск | Вер. | Влияние | Реакция | Триггер | План реакции | Владелец | Статус |
|---|---|---|---|---|---|---|---|---|
| **R-01** | Последовательная работа одной командой удлиняет MVP-1 с 9–11 до **26–35 недель** | В (реализовался как факт решения) | В | принять + снизить | решение пользователя 2026-09-09 (`maxTeams: 1`) | план пересобран (`roadmap.md`); срок явно показан пользователю на G3; компенсация — отрезание Should (005-memory, T-248…T-250, T-254, T-320) даёт −3–5 недель; вехи вместо дат | project-manager | открыт |
| **R-02** | **Лимит сессии агента**: агент прерывается (уже дважды — A3 шаги 2–3, отчёты потеряны), работа между сохранением файлов и отчётом теряется | В | С | снизить | прерывание агента на отчёте | задача ≤ M и один пакет-владелец; агент пишет файлы **до** отчёта; `dev-log.md` заполняется в той же задаче; **коммит по подтверждению на каждую подволну**, не на веху, в длинных вехах M2/M4/M5; при потере — повтор только последней задачи | project-manager + оркестратор | открыт |
| **R-03** | `maxAgentsPerRole: 1` не даёт запустить второго `developer`; второй слот годен только для другой роли → фактическая пропускная способность ниже плановой | В | С | снизить | планирование каждой подволны | во второй слот — `devops`/`tester`/`tech-writer`/`security`/`architect`/`code-reviewer`; e2e, golden и документация вынесены во второй слот (`roadmap.md` §4.1 п. 3); **вопрос пользователю: разрешить `maxAgentsPerRole: 2`** (−25–30 % срока) | project-manager | открыт |
| **R-04** | Один человек — ревьюер последней инстанции, оператор стенда, источник всех продуктовых решений и единственный игрок | В | В | снизить | накопление очереди на ревью/стенд | ревью в два слоя (`code-reviewer#N` на задачу, человек — сводка на веху и выборочно `shared/*`, `schemas/`, `links`, `filter`, `guardian`); стендовые прогоны пакетами по вехам; вопросы пользователю копятся до гейта, кроме блокеров | tech-lead#1 | открыт |
| **R-05** | Артефакты расходятся с реальностью: `tasks.md` трёх эпиков описывают 3 команды, `epics.md` §4 — лимиты 3/3/6, `counters.task` сводился вручную | С | Н | снизить | чтение планов исполнителем | `roadmap.md` — единственный источник порядка; таблицы волн в `tasks.md` читаются как порядок внутри эпика (уже помечено tech-lead#2, §10 п. 13); `counters.task` сводит tech-lead#1 один раз после G3; PM сверяет `state.md`/`journal.md` на каждой вехе | project-manager | открыт |
| **R-06** | Первая играбельная версия слишком поздно: без I1-α человек впервые играет на 21–27-й неделе | С | В | избежать | план без I1-α | I1-α сохранена как веха M3 (≈ 10–13-я неделя); мостовой блок T-214/215/219/220/255 перенесён в конец волны 0; чистая надбавка ≈ 1 M-экв | project-manager | снижен |
| **R-07** | Объём вырос с 95–100 до **156 задач** (EPIC-003 — 56 вместо ~35) | В (реализовался) | В | снизить | нарезка тимлидов A4 | меры tech-lead#2 §8: e2e/golden — во второй слот, Should I2 отрезаемы; правило «задача ≤ M» не смягчается (иначе растёт риск R-02); ревизия объёма — на G3 | tech-lead#1 | открыт |
| **R-08** | Дефект интеграции чинится «быстрой правкой» в интеграционной ветке, карта владения размывается | С | С | избежать | интеграция I1/I2 | дефект = задача `T-NNN` в эпик-владелец (`epics.md` §5 п. 6); конфликт в `shared/*`/`schemas/` — только через system-architect (`contract-change`) | tech-lead#1 | открыт |

## 2. Технологии и внешние зависимости

| ID | Риск | Вер. | Влияние | Реакция | Триггер | План реакции | Владелец | Статус |
|---|---|---|---|---|---|---|---|---|
| **R-10** | **llama.cpp #20345**: при включённом thinking грамматика `json_schema` не применяется; обновление билда ломает httptest-фикстуры и живой прогон | С | С | снизить | обновление `llama-server`; падение `valid_first_try` | `chat_template_kwargs.enable_thinking=false` на всех запросах со схемой (ADR-005 доп. 2); пин `LLAMACPP_BUILD` в `build/versions.env` (нужен вывод `llama-server --version` от владельца); `strip_think` в парсере как страховка; расхождение → задача на обновление фикстур, не «подправить парсер» | architect#2 + devops | открыт |
| **R-11** | **VRAM/качество варианта E** (`Qwen3.8-27B UD-Q3_K_XL`, 13,1 ГБ + KV 2–4 ГБ на RTX 4090 24 ГБ) не подтверждены замером: NFR-002 (p95 ≤ 5 с), NFR-090 (русский), NFR-022 (валидный JSON) | С | В | снизить | результат F-8 (T-013, T-260) | критерий и порядок зафиксированы ADR-005 доп. 3 (пороги и длина нарратива; E → Qwen3.6-35B-A3B → C → A; редакционно, T-440); модель — параметр блупринта (правка YAML, не Go); I1a пишется против `fake`/`recorded`; замер не блокирует код; при C/A стартует T-254 (+1 M-экв) и меняются пороги | architect#1 | открыт |
| **R-12** | **MinIO upstream архивирован** (ADR-021): образы не публикуются, сборка из исходников может сломаться на новом Go | С | С | снизить | провал `make minio-image` | сборка из форка владельца (`MINIO_REPO`) + тарбол исходников в `backups/` как вторая страховка; фолбэк сборки `golang:1.24.x`; интерфейс `objstore` не зависит от сервера; триггеры замены (spike SeaweedFS) — EPIC-012 | devops-engineer | открыт |
| **R-13** | Профиль `legacy` (narrative-orchestrator + semantic-memory + chroma) не поднимается → критерий **S5** не проверяется сравнением нарративов | С | С | снизить | решение Д-2 в конце M0 (T-008), проверка T-264 | запасной критерий S5: «флаг удалён, `gm_path=agent` в 100 % `llm.output`, `legacy_gm_path_share = 0`» без сравнения (INT-14 без ST-07); решение фиксируется в `journal.md` до старта M5 | devops + tech-lead#2 | открыт |
| **R-14** | Записи LLM (`testdata/recordings/*.jsonl`) устаревают при правке промптов → массовое падение e2e | С | С | снизить | изменение `prompt_hash` | ключ записи `(correlation_id, agent.id, phase, attempt)` не зависит от текста промпта; расхождение `prompt_hash` — предупреждение, не ошибка; перезапись — отдельная задача с ревью диффа (ADR-010) | tech-lead#2 | открыт |
| **R-15** | `membus` расходится с Redpanda → «в CI зелено, на стенде нет» | С | В | избежать | старт M1 | contract-тест шины (T-014) — **ворота**: без зелёного прогона на обеих реализациях следующая веха не стартует; §5.4 `strategy.md` — один CT-набор на заглушку и реализацию в каждой волне | tech-lead#1 + qa-engineer#1 | открыт |
| **R-16** | Изменение конверта `meta` или контрактов после волны 0 → каскад правок | Н | В | избежать | запрос `contract-change` | конверт фиксируется в T-005 до M0+; `shared/{eventbus,contracts,entity}` и `schemas/events/_common.json` — только через system-architect; при одной команде каскад дешевле, но история ветки эпика ломается | system-architect | открыт |
| **R-17** | Стенд = машина разработки: прогоны LLM конкурируют с работой агентов, GPU-замер вручную (U-12) | С | С | принять | нехватка времени владельца | стендовые задачи собраны в пакеты по вехам (`roadmap.md` §6); слот разработчика не занимают; self-hosted runner не заводим | devops-engineer | открыт |

## 3. Требования, объём, качество

| ID | Риск | Вер. | Влияние | Реакция | Триггер | План реакции | Владелец | Статус |
|---|---|---|---|---|---|---|---|---|
| **R-20** | **Нет тестеров для S2**: критерий требует 30 раундов группы из 3 живых игроков, у проекта — один человек | В | В | снизить | подготовка T-393 | вопрос пользователю на G3: два тестера с Telegram или зачёт S2 на `sim`-клиентах; запасной путь — генератор `sim`-клиентов в `testkit/gateway.Harness` (пробел G-1, ≤ S, EPIC-004 I2) + `POST /rounds/close` (T-354); при выборе `sim` критерий S2 переформулирует PO | product-owner + PM | открыт |
| **R-21** | **T-320 заблокирована (З-3)**: критерий US-008 «бот показывает уведомление об облачном провайдере» не покрыт — `config.cloud_enabled` живёт в шине, бот шину не читает | С | С | снизить | G3 | решение system-architect: добавить совместимое поле в C-08 (`GET /v1/worlds` или `/v1/players/{id}`) либо перенести критерий в EPIC-013 с явной отметкой недопокрытия | system-architect + PO | открыт |
| **R-22** | Замечания З-1, З-2, З-4, З-5 (EPIC-004) не решены до старта M2 → переделка миграций SQLite во втором инкременте | С | В | избежать | старт M2 | решить до подволны 1.2 EPIC-004; в последовательном плане запас есть (EPIC-002 I1 идёт раньше) — вопросы вынесены в очередь BA/SA сразу после G3 | business-analyst + system-analyst | открыт |
| **R-23** | `enum validation_status` (11 значений `data-model` vs 7 ADR-017) не решён к T-215, который в новом порядке идёт **раньше** | С | С | снизить | старт M0+ | решение сведения 3 (system-architect); запасной ход: слить `llm.output` без сужения `enum`, добавить отдельным PR; эскалация tech-lead#2 | system-architect | открыт |
| **R-24** | Отрезание 005-memory (Should) → US-005/US-036 без памяти, US-015 без `/v1/trace` | С | Н | принять | G3/G4 | Must-часть держится на `journalContext` (T-234) и `mvctl trace` (T-133, Must в 005-ops); все Must-кейсы построены с `MV_MEMORY_ENABLED=false`; CT-09, IT-10, E2E-17, INT-08 помечены P3 и отрезаются вместе | product-owner | открыт |
| **R-25** | **Пробелы тестирования без задач-владельцев**: G-1 (sim-клиенты, NFR-005/049), G-2 (NFR-016 отказ зависимости ≤ 30 с), G-4 (негативы облака), G-5 (сверка токенов ≤ 5 %), G-6 (тома и восстановление `links.db`), G-7 (рестарт между Phase 1 и Phase 2) | В | С | снизить | приёмка I1/I2 | завести задачи на G3: G-7 → обязательный INT-06 + пункт в DoD T-238; G-1 → расширение `Harness` (EPIC-004 I2); G-4 → ST-16 (стенд, EPIC-003); G-2 → IT-кейс EPIC-001; G-5 → в рамках B7; G-6 → чек-лист devops в T-019/runbook | qa-engineer#1 | открыт |
| **R-26** | Пороги NFR «после замера» не закреплены к G4 (S4 — критерий выхода) | С | С | снизить | M11 (T-140) | `strategy.md` §6.3: до фиксации — наблюдение, не дефект; перенос порога фиксируется в `journal.md` решением tech-lead#1; вход — `baseline.md` (T-013, T-265) и прогоны S1/S2 | business-analyst + architect#1 | открыт |
| **R-27** | Заглушка расходится с реализацией (`FakeState` ↔ `internal/state`, `FakeEncounter` ↔ роль `encounter`, `Harness` v0 ↔ v1) | С | С | снизить | замена заглушки | §5.4 `strategy.md`: один CT-набор на пару, дифф должен быть пуст; неактуальные заглушки удаляются (`MV_SWARM_FAKE` — T-256, обязательное условие тега `mvp-1/i1`) | qa-engineer#1 | открыт |
| **R-28** | Инициализация мира: расхождение «фикстуры vs `npc_table` блупринта» | Н | С | избежать | M1/M5 | зафиксировано `epics.md` §2: фикстуры — источник, `npc_table` — только респаун; тест согласованности INT-13 в EPIC-003 I1b | system-architect | снижен |
| **R-29** | Качество нарратива (NFR-023/024 — расхождение с механикой и каноном) — метрика без порога и без исполнителя | С | Н | принять | после I1 | чек-лист 100 ходов (ST-12) ведёт qa-engineer#2 + человек; порога в MVP-1 нет, наблюдение | qa-engineer#2 | открыт |

## 4. Безопасность, приватность, право

| ID | Риск | Вер. | Влияние | Реакция | Триггер | План реакции | Владелец | Статус |
|---|---|---|---|---|---|---|---|---|
| **R-30** | **Секреты в истории git** (T-33, Critical as-is): `.mcp.env` с токенами в коммите `6e328eb`; реальный ключ `sk-…` в `shared/oracle/README.md` | В (реализовался) | В | снизить | — | токены отзывает пользователь (подтвердить в `journal.md`); T-001: плейсхолдер, `git rm --cached`, `gitleaks` + `pre-commit` **первым шагом**, `gitleaks git --redact` = 0; job `security` в CI; `git filter-repo` — только по явной команде пользователя, до этого токены считаются скомпрометированными | security-engineer | снижен |
| **R-31** | **Telegram и 18+**: целевой рейтинг 18+ при отсутствии реального фильтра в MVP-1; доступ третьих лиц к боту | С | В | снизить | подключение тестеров | allowlist `MV_TELEGRAM_ALLOWED_USER_IDS` (SEC-06/07, U-7), только личные чаты; фильтр категории (a) fail-closed + абсолютные запреты в системном промпте; уведомление «контент создан ИИ» и 18+ в `/start` (T-318); полный фильтр 18+ и аутентификация — EPIC-013; бот не публикуется | security-engineer + PO | открыт |
| **R-32** | ПДн: внешние Telegram ID или тексты игроков попадают в шину, логи, `testdata` | С | В | избежать | privacy-scan | псевдонимизация на входе gateway, связка только в `links.db`; `privacy-scan` в job `security` и на живом стенде (T-139, T-392); редакция токена и внешних ID в логах (`shared/logging`); `/forget` — физическое удаление (`secure_delete`, checkpoint+vacuum), учения на стенде | security-engineer | открыт |
| **R-33** | Инъекция в тексте игрока пробивает промпт (SEC-17) | С | С | снизить | e2e `injections-10` | экранирование `<`/`>` и лимит 500 символов в `prompt/escape.go` (T-218); страж поверх ответа (T-217); e2e 10 инъекций (T-243) | architect#2 | открыт |
| **R-34** | Цепочка поставок: уязвимость в зависимостях, подмена Action | Н | С | снизить | CI | `govulncheck` блокирующий, `go mod verify`, `tidy -diff`, Dependabot, пин Actions по SHA, CODEOWNERS на `blueprints/`, `laws/`, `config/`, `.github/`; branch protection (действие владельца) | devops + security | открыт |
| **R-35** | Облачный провайдер включается непреднамеренно → внешние затраты и утечка контента (нарушение S8) | Н | В | избежать | конфигурация | гейт по host `MV_LLM_URL` (`IsLocalEndpoint`), запуск только при `MV_LLM_CLOUD_ENABLED=true`; событие `config.cloud_enabled` без ключей; негативные кейсы — пробел G-4, задача на G3 | architect#2 | открыт |

## 5. Снятые и переданные риски

| ID | Риск | Почему снят |
|---|---|---|
| R-40 | «Ранний merge `shared/testkit/swarm` вне порядка 002 → 004 → 003» | при одной команде порядок задач = порядок поставки; ранние merge как практика не нужны (`roadmap.md` §4.2). На G3 подтвердить снятие |
| R-41 | «Правило „I2 после интеграции I1“ простаивает две команды из трёх» | параллельных команд нет |
| R-42 | «Перебор слотов 7 → 6, приоритет критическому пути TEAM-2» | один слот разработчика |
| R-43 | «`FakeEncounter` не готов к I1-α» | мостовой блок ставит T-219/T-220 до EPIC-002; запасной вариант `encounterStub` не нужен |
| R-44 | «`WithEncounterStub` прижилась в `shared/testkit`» | снята сведением 2 (C-05 v1.2): `FakeEncounter` — единственная заглушка боя |
| R-45 | OQ-A-20 (MinIO: сборка / SeaweedFS / форк) | решено на G2 — сборка из исходников на MVP-1; остаточный риск ведётся как R-12 |

История изменений: v1.0 — 2026-09-09 — создано (A4, шаг 4). Следующее обновление — на G3 по решениям пользователя и на приёмке вехи M0.
