# Карта владения

Версия 0.6 · 2026-09-13 · system-architect#1 (утверждено на G2 в v0.2; ведёт tech-lead#1). **Изменения v0.6 (T-444, `contracts.md` v0.11, ADR-001 доп. 2026-09-13):** `cmd/mvctl/**` разделён — каркас и реестр у EPIC-001, подкоманды и файлы `commands_<владелец>.go` у эпиков-владельцев; регистрация контекстов — файлы `cmd/multiverse/contexts_<владелец>.go`, порядок остаётся в `contexts.go` (EPIC-001); `shared/contracts/registry.go` — файлы `registry_<владелец>.go`; писатель записей LLM — `internal/llm/providers/recorded.Writer` (не testkit); `FakeGateway` — `internal/gateway/gatewaytest` (не testkit); узкие исключения `shared/testkit/swarm` → `internal/swarm/template$` и `shared/testkit/gateway` → `internal/gateway/{client,api}$`; новый §3 — правила на период трёх веток эпиков; владельцы `test/e2e/**` и пар `testdata/fixtures/events/**`; `cmd/mvctl/internal/world/**` — EPIC-002 (T-058). **Итерация 2 T-444 (ревью #1):** §3 п. 4 — мягкий режим с названными исключениями (правки `cmd/*` до T-446 в ветке задачи эпика, объявление переменной вместе с чтением, точечные дополнения общего кода с ревью system-architect); правило имён сценариев `test/e2e` (префикс владельца) и привязка сценариев к задачам; задача раскладки — T-446. **Изменения v0.5 (T-416, `contracts.md` v0.7):** таблица владения — один файл `shared/contracts/ownership.go`, строку меняет владелец её семантики (ADR-025, подтверждён); прежняя процедура «истина `levels.go` → копия тем же PR» отменена; `membus` — вторая реализация C-01, переезжает в `shared/eventbus/membus`, до переезда `cmd/multiverse/bus_memory.go` — единственный импорт `shared/testkit` в бинарнике по узкому временному исключению линтера (C-01 v1.4, ADR-001 доп. 2026-09-11); адреса сервисов сети compose — явный набор из шести переменных (`contracts.md` §16 п. 5). Записи v0.2–v0.4 ниже — история: упоминания копии и `levels.go` как истины в них отменены v0.5. **Изменения v0.4 (сведение 3, `consolidation.md` §14):** копия таблицы владения `shared/contracts/ownership.go` и процедура правки «истина `levels.go` → копия тем же PR» (`contracts.md` §16 п. 6); `Spec.Publishers` в реестре — владелец типа перечисляет издателей (§16 п. 7); имена env `MV_TELEGRAM_ALLOWED_USER_IDS` (единое), `MV_KAFKA_BROKERS` для CLI на хосте, `MV_SWARM_REPLAY_WAIT`, `MV_TELEGRAM_CLOUD_FLAG_TTL`. Правило по умолчанию: путь меняет только команда-владелец; общий код — только через запрос на изменение контракта (раздел 2). Изменения v0.2: `shared/clock`, `shared/runtime`, `build/versions.env`, `build/minio.Dockerfile`, `.gitattributes`, `.golangci.yml`; префикс `snapshots-{world}/gateway/`; совладение схемы `analytics.replay.completed`; `services/_archive/` вместо удаления (OQ-A-17); `shared/agent/levels.go` как источник `contracts.OwnershipRules`; таблица запросов заполнена и закрыта (`architecture/consolidation.md`). **Изменения v0.3 (сведение A4-2, `consolidation.md` §10–§13)**: `shared/testkit/swarm` — `FakeEncounter` + `FakeNarrator` у EPIC-003 (v0 нарратора — F-10, без боя), хук `cmd/multiverse/fake_contexts.go` (`MV_SWARM_FAKE`); совладение раздела `admin` в `api/gateway.openapi.yaml`; схема `analytics.consistency.violated.v1.json` создаётся в F-4b для EPIC-005; `mvctl trace` — 005-ops; импорт `internal/memory → internal/llm` (Provider/Registry); `scripts/llm-server.ps1`, `ops/metrics/bench-matrix.json`; env `MV_LLM_URL`/`MV_LLM_API_KEY`; запросы 16–24 в §2. **Правка T-428 (2026-09-11)**: строки `shared/eventbus/membus`, `shared/testkit/**`, `fake_contexts.go` приведены к T-418 — редакционная правка текста, без изменения решений.

## 1. Пути и ресурсы

| Путь / ресурс | Владелец (эпик) | Правило |
|---|---|---|
| `go.mod`, `go.sum`, `cmd/multiverse/main.go`, **`cmd/multiverse/{serve,contexts}.go`, `cmd/mvctl/main.go`, `cmd/mvctl/internal/{cli,contracts,env,storage,privacy}/**` (v0.6, T-444)**, `build/` (`Dockerfile`, `minio.Dockerfile`, `versions.env`, `redpanda-init.sh`), `docker-compose.yml`, `Makefile`, `.github/workflows/**`, `.github/dependabot.yml`, `CODEOWNERS`, `.gitignore`, `.gitattributes`, `.golangci.yml`, `.gitleaks.toml`, `.env.example`, `.mcp.env.example` | EPIC-001 (после волны 0 — tech-lead#1 + devops-engineer) | изменения после волны 0 — через tech-lead#1. **v0.6, итерация 2 T-444:** `cmd/multiverse/{serve,contexts}.go` и `cmd/mvctl/main.go` до T-446 правятся в ветке задачи эпика с обязательным просмотром tech-lead#1 (приёмка — tech-lead эпика задачи) (T-055, T-060 — `serve.go`; T-058 — `cmd/mvctl/main.go`; регистрации в `contexts.go`); после T-446 регистрация и подкоманды — только в файлах владельцев; строка новой переменной в `.env.example` — вместе с её объявлением (строка `shared/env` ниже, §3 п. 4); добавление зависимости в `go.mod` — любой командой, но с пометкой в отчёте; версии образов — только в `build/versions.env` |
| `shared/eventbus/**`, `shared/jsonpath/**`, `shared/contracts/**`, `schemas/events/_common.json` | EPIC-001 → system-architect | **общий код**: только через запрос на изменение контракта (C-01); политики топиков в реестре — system-architect. **`shared/contracts/ownership.go` (`OwnershipRules`) — единственная истина таблицы владения (ADR-025, подтверждён T-416)**: создан EPIC-001 по `data-model.md` §4 (со строкой gateway); строку меняет владелец её семантики — уровни роя EPIC-003, строку `gateway` EPIC-004, строки `author`/`system` EPIC-002 — PR с меткой `contract-change`, ревью system-architect + tech-lead#1, в описании PR — затронутые блупринты (`contracts.md` §16 п. 6); копии и теста равенства нет. **`shared/eventbus/membus`** (с T-418 — переехал из `shared/testkit/membus`, C-01 v1.4) — вторая реализация C-01, правки тем же порядком, что остальной `shared/eventbus`. `Spec.Publishers` для своего типа заполняет владелец типа (§16 п. 7). **v0.6 (T-444):** `registry.go` собирает `Default()` из файлов `registry_<владелец>.go` (раскладка — T-446); строки своих типов владелец добавляет в ветке задачи эпика с ревью system-architect и обязательным просмотром tech-lead#1 (§3 п. 6); строку своей семантики в `ownership.go` — там же, с ревью system-architect и tech-lead#1 (п. 6 `contracts.md` §16); сборку `Default()`, `sources.go` и изменение API пакета — только задачей EPIC-001 (§3 п. 4) |
| `shared/clock/**`, `shared/runtime/**` | EPIC-001 → system-architect | **общий код** (часть C-01 v1.1): интерфейсы `Clock/Timers`, `Deps/Context`, HTTP-сервер процесса; правки — запрос |
| `shared/objstore/**`, `shared/env/**`, `shared/logging/**` | EPIC-001 → tech-lead#1 (`objstore` — интерфейс по ADR-021: правки через system-architect) | правки `env`/`logging` — запрос к tech-lead#1 (не контракт, но общий код); расширение интерфейса `objstore` — контракт. **v0.6, итерация 2 T-444:** новая переменная объявляется в `shared/env/vars.go` тем изменением, которое вводит её чтение, в ветке задачи эпика вместе со строкой `.env.example`, с обязательным просмотром tech-lead#1 (приёмка — tech-lead эпика задачи) (`contracts.md` C-14 v0.4, §16 п. 8); изменение формы манифеста (API `shared/env`) — задачей EPIC-001 |
| `schemas/events/<type>.v<n>.json` | владелец типа по `contracts.md` §0 (EPIC-002/003/004/005) | добавлять схемы своих типов можно; менять чужие — запрос |
| `schemas/events/analytics.replay.completed.v1.json` | **совладение**: файл — EPIC-002 (`mode=recovery`); поля `mode=test`, `events_hash_match` добавляет EPIC-005 через PR в файл с ревью EPIC-002 | несовместимые правки — запрос |
| `shared/testkit/**` | EPIC-001 (`Dedup`-псевдоним, `Versions()`, фикстуры; `membus` — с T-418 в `shared/eventbus/membus`, C-01 v1.4, см. строку `shared/eventbus/**` выше); фейки контрактов добавляет **поставщик** контракта (`FakeState`/`FixedMechanics` — EPIC-002, **`FakeEncounter`**/`FakeNarrator`/`template/ru.go` — EPIC-003, `Harness` — EPIC-004; **v0.6, T-444:** `RecordingWriter` → `internal/llm/providers/recorded.Writer`, `FakeGateway` → `internal/gateway/gatewaytest`, `template/ru.go` в T-228 → `internal/swarm/template`); v0 заглушек создаёт F-10 (EPIC-001) по спецификации поставщика — `FakeNarrator` v0 **только нарратив** (без `WithEncounterStub`, решение d) | каждый фейк — в подпакете владельца: `shared/testkit/state`, **`…/mechanics`** (двойник механики — свой подпакет, а не часть `…/state`: `contracts.md` §17 говорит иначе и правится), `…/swarm`, `…/gateway`; фейки могут публиковать типы подменяемого контракта только в e2e/`MV_SWARM_FAKE=true` (`contracts.md` §0, исключение); `shared/testkit/swarm` без зависимости от `testify` (импортируется `cmd/multiverse` до I1); **ранний merge** `shared/testkit/swarm` (C4) в `integration/mvp-1` сразу после приёмки tech-lead#2 — до I1-α. **v0.6 (T-444; ADR-001 доп. 2026-09-13 п. 4–5):** из `internal/*` двойники видят только названное узкими правилами `.golangci.yml` — `…/mechanics` и `…/swarm` → `internal/mechanics$`, `…/swarm` → `internal/swarm/template$`, `…/gateway` → `internal/gateway/client$`, `internal/gateway/api$`; новое исключение — только через system-architect |
| **`internal/gateway/gatewaytest/**`** (v0.6, T-444) | EPIC-004 | двойник `FakeGateway` поверх настоящего шлюза (C-08 v1.4); импорт из production-кода запрещён линтером, из тестов — разрешён; e2e с ним живёт вне `internal/<контекст>` |
| **`internal/llm/providers/recorded/**`** (`Writer` записей, v0.6, T-444) | EPIC-003 | формат `*.jsonl` и его читатель — один пакет (C-07 v1.3); фильтр SEC-23 — в писателе; `mvctl record` (EPIC-003) его вызывает |
| `cmd/multiverse/fake_contexts.go` (хук `MV_SWARM_FAKE`) | EPIC-001 (F-2/F-10; регистрирует `testkit/swarm.FakeContext` под именем `swarm`) | хук T-255 (принят tech-lead#1 2026-09-11; импортирует ровно `shared/testkit/swarm$` — узкое правило depguard `cmd-multiverse-fake-contexts`); с T-418 (переезд `membus`) — единственный импорт `shared/testkit` в production-бинарнике (`depguard`-исключение по файлу; сам запрет записан правилом `no-testkit-in-production` в `.golangci.yml` 2026-09-10, до того он только подразумевался). *(Между T-255 и T-418 импортов `shared/testkit` в бинарнике было два: этот хук и `cmd/multiverse/bus_memory.go` → ровно пакет `shared/testkit/membus$`, временное исключение до переезда `membus` в `shared/eventbus/membus`, C-01 v1.4, ADR-001 доп. 2026-09-11.)* Хук **удаляется при слиянии EPIC-003 I1** (критерий тега `mvp-1/i1`, задача EPIC-003 T-256) |
| `shared/entity/**`, `internal/state/**`, `internal/mechanics/**`, `internal/replay/**`, `rules/**`, **`cmd/multiverse/contexts_state.go`, `cmd/mvctl/commands_state.go`, `cmd/mvctl/internal/world/**` (v0.6, T-444)** | EPIC-002 | `rules/dark-forest.yaml` — числа меняет EPIC-002 по DR-04 после прогонов; блупринты ссылаются на файл; `internal/replay` импортирует только `cmd/multiverse` (ADR-001 дополнение п. 2) |
| `shared/agent/**` (включая `levels.go` — реестр уровней для валидатора блупринтов; таблицы владения не держит, ADR-025), `internal/swarm/**`, `internal/llm/**`, `internal/laws/**`, `blueprints/**`, `laws/**`, `schemas/agent/**`, `config/absolute-limits.yaml`, **`cmd/multiverse/contexts_swarm.go` (контексты `llm`, `laws`, `swarm` — один общий стек), `cmd/mvctl/commands_swarm.go`, `cmd/mvctl/internal/{blueprint,laws,record}/**` (v0.6, T-444)** | EPIC-003 | формат блупринта (C-11) и `laws` (C-12) — контракты; строки уровней роя в `shared/contracts/ownership.go` меняет EPIC-003 PR с меткой `contract-change` (влияет на State через C-02; `contracts.md` §16 п. 6, ADR-025); `levels.go` держит только `AllowedEventTypes` и получает вид «типы сущностей уровня» от вызывающего, собранный из `contracts.OwnershipRules`; новые блупринты регионов после MVP-1 — авторы (оператор) без запроса |
| `internal/gateway/**`, `cmd/telegram-bot/**`, `api/gateway.openapi.yaml`, `internal/gateway/migrations/{links,gateway}/*.sql`, **`cmd/multiverse/contexts_gateway.go` (v0.6, T-444)** | EPIC-004; **раздел `admin` в `gateway.openapi.yaml` — совладение**: файл EPIC-004, текст маршрутов `/v1/admin/agents*`, `/v1/admin/llm/usage` — EPIC-003 через PR с ревью EPIC-004 (C-06 v0.3) | миграции SQLite — `goose` из embed FS, без `Down` (ADR-019 п. 2), нумерация `0001_*.sql`… только EPIC-004; коды ошибок и лимиты C-08 — совместимые дополнения без запроса, несовместимые — запрос |
| `internal/memory/**`, **`cmd/multiverse/contexts_memory.go`, `cmd/mvctl/commands_ops.go`, `cmd/mvctl/internal/{golden,llmusage,memory,report,trace}/**`** (v0.6, T-444: прежняя строка отдавала EPIC-005 весь `cmd/mvctl/**`; каркас и реестр — у EPIC-001, подкоманды EPIC-002/003 — у них), `api/memory.openapi.yaml`, `ops/metrics/**` (кроме `bench-matrix.json`), `testdata/recordings/**`, `testdata/analytics/**` | EPIC-005 (005-ops: `cmd/mvctl/internal/{report,llmusage,trace}` — **`mvctl trace <correlation_id>` по журналу, Must**; 005-memory: `internal/memory`, `/v1/trace` Should) | `mvctl contracts check`, `mvctl blueprint validate`, `mvctl storage init` — каркас в EPIC-001, наполнение EPIC-005/EPIC-003 (валидатор и команда `blueprint` — EPIC-003: так говорят реестр `cmd/mvctl/main.go` и задача T-204; **v0.6, T-444:** прежняя запись «команда CLI — EPIC-005» заменена); записи — только из сессий `actor_kind=ci` (ADR-010 дополнение); `internal/memory` импортирует из `internal/llm` только `Provider`/`Request`/`Response`/`providers.Registry` (ADR-001 доп. п. 7, `depguard`) |
| `schemas/events/analytics.consistency.violated.v1.json` | EPIC-005 (владелец); **создаётся в EPIC-001 F-4b, блок «в»** по спецификации EPIC-005 `design.md` §4.1 и передаётся владельцу | как `replay.completed` для EPIC-002 |
| `scripts/llm-server.ps1`, `Makefile` цели `llm-up/llm-down/llm-health`, `ops/metrics/bench-matrix.json`, `scripts/llm-bench.{ps1,sh}` | EPIC-001 → devops-engineer (скрипт запуска, замер); конфигурации/пороги `bench-matrix.json` — system-architect/architect#1 (F-8) | нативный llama-server вне compose (ADR-005 доп. 2 п. 7); параметры семплинга в скрипте не фиксируются — задаёт шлюз на запрос |
| `testdata/fixtures/**` (мир, регион, NPC, игроки `player-A/B/C`) | EPIC-001 (создаёт) → общие | добавлять можно всем; менять существующие — уведомить tech-lead#1. **v0.6 (T-444):** `testdata/fixtures/events/**` — пары фикстур `<тип>.v<n>.{valid,invalid}.json` добавляет и меняет **владелец типа** (`contracts.md` §0) вместе со своей схемой; чужие пары — запрос владельцу; правка `shared/contracts/blockv_test.go` — задачей EPIC-001 |
| **`test/e2e/**`** (v0.6, T-444) | каркас и общие помощники (`doc.go`, `empty_world_test.go`, `lifecycle_test.go`, `signal_*_test.go`, `stubs_v0_test.go` — файлы без префикса) — EPIC-001; сценарий принадлежит **задаче, которая его вводит**, и живёт в файле с префиксом владельца. *(Итерация 2 T-444, ревью #1 Ma-3; прежняя редакция отдавала `solo-30` одному EPIC-003 и расходилась с планами трёх эпиков.)* Привязка: `solo-30` — три варианта: EPIC-002 T-061/T-062 на State и двойниках (`state_solo30_test.go`), EPIC-004 T-313 через HTTP шлюза (`gateway_solo30_test.go`), EPIC-003 T-242 на настоящем рое (`swarm_solo30_test.go`); `death`, `flee-fail` — EPIC-004 T-313 (`gateway_death_test.go`, `gateway_fleefail_test.go`) и EPIC-003 T-242 (`swarm_death_test.go`, `swarm_fleefail_test.go`); S3 `recovery` — EPIC-002 T-062 (`state_recovery_test.go`), EPIC-004 T-314 (`gateway_recovery_test.go`, вместе с `gateway_forget_test.go`, `gateway_privacy_test.go`) и EPIC-003 T-243 (`swarm_recovery_test.go`); `group-3x30` — EPIC-002 T-070 (`state_group3x30_test.go`) EPIC-004 T-357 (`gateway_group3x30_test.go`, `gateway_forget_group_test.go`) и EPIC-003 T-251 на настоящем рое (`swarm_group3x30_test.go`; там же `swarm_injections_memory_test.go` и `swarm_s6_repeat_test.go`); `background-6h`, `degraded`, `injections-10` — EPIC-003 T-243 (`swarm_…`), инъекции через память — дополнение T-251; e2e бота — EPIC-004 T-315 (`gateway_bot_test.go`) | **правило имён в общем пакете:** файл `<владелец>_<сценарий>_test.go`, владелец ∈ `state` (EPIC-002), `swarm` (EPIC-003), `gateway` (EPIC-004), `ops` (EPIC-005); тестовые функции `Test<Владелец><Сценарий>…` (`TestStateSolo30`, `TestGatewaySolo30`); неэкспортируемые функции, типы и переменные уровня пакета в файле владельца — с тем же префиксом (`stateWorld`, `gatewayClient`); общие помощники без префикса — только в файлах EPIC-001, меняются задачей EPIC-001, а владелец сценария зовёт их, но не переопределяет. Файлы сценариев — `package e2e_test`, как общие файлы EPIC-001 (в пакете `e2e` только `doc.go`). **`TestMain` и общая инициализация (итерация 3; ревью #2, Mi-3):** `TestMain` в пакете один, префикса у него быть не может, поэтому он живёт только в файле EPIC-001 `main_test.go`. Эпик свою инициализацию уровня пакета в `TestMain` не пишет. Однократная подготовка — ленивым помощником с префиксом владельца на `sync.Once` (`stateFixtures(t)`). Подготовка до и после всего пакета — регистратором из `main_test.go`: `func init() { registerPackageSetup("state", stateSetup) }`, где `stateSetup func() (teardown func(), err error)`; `TestMain` вызывает зарегистрированные подготовки в порядке имени владельца, а завершения — в обратном. Файл `main_test.go` с регистратором заводит задача EPIC-001 по первому запросу эпика; до неё хватает ленивых помощников. *Почему префикс, а не подпакеты `test/e2e/<эпик>`:* общие помощники живут в `_test.go` пакета `e2e_test` и из подпакета не импортируются — пришлось бы переносить их в не-тестовый файл, то есть менять код EPIC-001 до старта волн; префикс ничего не переносит, а случайное совпадение имён ломает сборку громко. Нейтральное место: правила depguard контекстов на `test/**` не действуют, поэтому e2e с `internal/gateway/gatewaytest` живёт здесь, а не в `internal/<контекст>` |
| `services/<frozen>/**` (world-generator, universe-genesis-oracle, ontological-archivist, cultivation-module, plan-manager, city-governor, entity-actor, evolution-watcher) | заморожены (EPIC-006…010) | не менять до старта соответствующего эпика; `FROZEN.md` — EPIC-001 F-3 |
| `services/narrative-orchestrator/**`, `services/semantic-memory/**` (as-is, профиль `legacy`) | EPIC-003 (только на время профиля `legacy`); Neo4j-часть semantic-memory переносится в `internal/memory` EPIC-005 | не развивать; в EPIC-003 I2 (S5) → `services/_archive/` |
| `services/game-service/**`, `services/entity-manager/**`, `services/rule-engine/**` | источники для переписывания (EPIC-004, EPIC-002, EPIC-002) | переносить код, а не править на месте; после переноса → `services/_archive/` владельцем нового пакета |
| **`services/_archive/**`** (ban-of-world, reality-monitor, `shared/{schema,redis,config,minio,oracle,rules,intent,tinyml,spatial}`, `fake_deps`, `test_minio.go`, `shared/agent/tools/*`, лишние Dockerfile; позже — legacy-сервисы) | EPIC-001 F-1/F-3 создаёт; далее — никто (только чтение) | решение пользователя OQ-A-17: **ничего не удалять**; каждый каталог — `ARCHIVED.md` (причина, коммит, эпик возврата); вне `go.mod`/compose/Makefile/линтера; возврат кода в сборку — через архитектора |
| `Docs/dev-team/architecture/**` | system-architect (overview, ADR-001…010, ADR-021, contracts, consolidation); `architect#N` — `components/*.md`, ADR-011…020; security-engineer — `threat-model.md`; `infrastructure.md` — совместно: devops-engineer — эксплуатация (все разделы, кроме §6.4), architect#1 — §6.4, методика замера LLM (T-399, T-435) *(запись приведена к факту, T-440)* | ADR системного уровня — только system-architect; правки `infrastructure.md` по решениям сведения (порт 8090, профиль `legacy` + Chroma, `prompts-*` 30 дней, gitleaks allowlist, `versions.env`) — devops-engineer |
| `Docs/dev-team/plan/**` | tech-lead#1 (epics после G2, teams, decomposition-review, ownership), project-manager (roadmap, risks) | — |
| `Docs/archive/**`, `CLAUDE.md`, `AGENTS.md`, `README.md` | tech-writer (EPIC-001 F-9) | по факту кода |
| Топики Redpanda и их конфигурация (`redpanda-init`: retention, `segment.ms`, `max.message.bytes`) | EPIC-001 → devops-engineer | новый топик — только через архитектора (ADR-007) |
| Бакеты MinIO (`entities-*`, `snapshots-*`, `prompts-*`, `ops-artifacts`) | EPIC-002 (`entities`, `snapshots/state`), EPIC-003 (`snapshots/swarm`, `prompts`), **EPIC-004 (`snapshots/gateway`)**, EPIC-005 (`ops-artifacts`) | имена бакетов и префиксов — в `shared/objstore/buckets.go` (EPIC-001); versioning/ILM включает `EnsureBucket` (ADR-004 дополнение п. 2) |
| Коллекции Qdrant, схема Neo4j | EPIC-005 | без плагина APOC (ADR-004 дополнение п. 5) |
| `links.db`, `gateway.db` | EPIC-004 | доступ только из `internal/gateway`; внешний ID — только `links.db` (включая `character_requests`); тест NFR-041 (EPIC-005) читает оба файла и WAL только для проверки отсутствия утечек |
| Переменные окружения `MV_*` | владелец процесса/контекста объявляет через `shared/env.Declare`; `.env.example` — EPIC-001/devops; LLM: `MV_LLM_PROVIDER=openai_compat|ollama|anthropic|recorded|fake`, `MV_LLM_URL`, `MV_LLM_API_KEY`, `MV_OLLAMA_URL` (только `ollama`), `MV_SWARM_FAKE` (хук `cmd/multiverse`, до I1) — EPIC-003/EPIC-001; **v0.4**: allowlist бота — единое имя `MV_TELEGRAM_ALLOWED_USER_IDS` (EPIC-004); CLI на хосте (`mvctl laws bump|world init|record`) читает `MV_KAFKA_BROKERS=127.0.0.1:19092` (EPIC-001, `infrastructure.md` §1 п. 8; прежние скобки этой строки называли «неверным» то же самое имя — след глобальной замены, исправлено в v0.5); **v0.5**: умолчание compose, называющее сервис своей сети, законно только для шести адресов (`MV_KAFKA_BROKERS`, `MV_MINIO_ENDPOINT`, `MV_CORE_URL`, `MV_QDRANT_ADDR`, `MV_NEO4J_URI`, `MV_TELEGRAM_GATEWAY_URL`), а `OLLAMA_*` получают умолчания в манифесте (`contracts.md` §16 п. 5, T-413); `MV_SWARM_REPLAY_WAIT` (EPIC-003), `MV_TELEGRAM_CLOUD_FLAG_TTL` (EPIC-004) | без префикса `MV_` — только сторонние (`OLLAMA_*`, `MINIO_ROOT_*`, `NEO4J_AUTH`, `COMPOSE_PROFILES`) |

## 2. Запросы на изменение контракта

Формат: контракт C-NN, суть изменения, совместимость, потребители. Процедура — `architecture/contracts.md` §16. Полная таблица с решениями по каждому запросу и замечанию — `architecture/consolidation.md`.

| # | От | Что | Затрагивает | Статус |
|---|---|---|---|---|
| 1 | architect#1 (EPIC-002) | C-01: `Journal{ReadRange, Tail, End}`, `PositionFromContext`, `eventbus.Dedup` | EPIC-001 (реализация), 002/003/004 (потребители) | принято → C-01 v1.1 |
| 2 | architect#1 | ADR-001 п. 3: `state → mechanics`, `cmd/multiverse → internal/replay`; пакеты `shared/clock`, `shared/runtime` | EPIC-001, EPIC-002 | принято → ADR-001 дополнение п. 2–3 |
| 3 | architect#1 | C-03: совместимые дополнения Go-API | EPIC-003 | принято → C-03 v1.1 |
| 4 | architect#1 | C-02: семантика `rest`, `atomic=false`, `applied_at`, `duplicate_entity`, `append` | EPIC-003, EPIC-004 | принято → C-02 v1.1 |
| 5 | architect#1 | C-14: `latest.json` указатель, `component` enum | EPIC-003, EPIC-004, system-analyst | принято → C-14 v1.1 |
| 6 | architect#1 | схема `analytics.replay.completed` — совладение | EPIC-005 | принято → §1 этой карты |
| 7 | architect#3 (EPIC-004) | C-08: коды ошибок, `202 pending`, `creating` | бот, харнесс | принято → C-08 v1.1 |
| 8 | architect#3 | C-05: `encounter.started.round{}` | EPIC-003 (издатель) | принято → C-05 v1.1, C-11 v1.1 |
| 9 | architect#3 | C-14/ownership: `snapshots-{world}/gateway/` | EPIC-001, EPIC-005 | принято → §1 |
| 10 | architect#3 | C-04: `say` не открывает раунд; BR-13 лидерство при смерти | BA (FR-025, BR-13) | принято как уточнение → C-04; требование — BA |
| 11 | architect#3 | ADR-006 п. 5: только личные чаты в MVP-1 | бот | принято → ADR-006 дополнение п. 2 |
| 12 | architect#3 | C-01: признак «конец журнала» (`Bus.Lag()`) | EPIC-001 | принято с изменением → `Journal.End()` вместо `Lag()` |
| 13 | architect#2 (EPIC-003) | C-11: glob в `trigger.event_name`; схемы: `llm.output.parse{}`, `narrative_event_id`, `lod_allowed` обязателен, `content_hash` | EPIC-004, EPIC-005 | принято → C-05/C-06/C-07/C-11 v1.1 |
| 14 | security-engineer | 5 Major + 12 Minor/Info (`threat-model.md` §8) | ADR-004/005/006/009/010, C-01/C-08/C-09/C-11 | принято (16), передано BA (1: текст FR-009) |
| 15 | devops-engineer | 14 замечаний (`infrastructure.md` §11) | ADR-001/004/005/007/009/010, ADR-021, C-01/C-06, overview §13/§19 | принято (10), принято с изменением (4: Chroma — в профиле `legacy`; порт `core` 8090; логи бота — по объёму; golden `merge=binary` без `-diff`) |
| 16 | пользователь (U-8, DevOps) | C-15/ADR-005: провайдер по умолчанию `openai_compat` (llama-server), Ollama — второй, облако — тот же адаптер | EPIC-003 (B3a/B3b), EPIC-005 (`Embed`), devops (`infrastructure.md` §6), F-8 | принято → ADR-005 доп. 2; C-15 v1.1; OQ-A-18 вариант E; OQ-A-21 |
| 17 | architect#1 (EPIC-005) | ADR-001 п. 3: импорт `internal/memory → internal/llm` (`Provider`/`Registry`) | EPIC-001 (`.golangci.yml`), EPIC-005 | принято → ADR-001 доп. п. 7 |
| 18 | architect#1 (EPIC-005) | схема `analytics.consistency.violated.v1.json` в F-4b блок «в» | EPIC-001 F-4b | принято → C-10 v0.3; `epics.md` §6 |
| 19 | architect#1 (EPIC-005) | `mvctl trace` — 005-ops (Must), `/v1/trace` — 005-memory (Should) | EPIC-005, `epics.md` §1 | принято → C-10 v0.3; `epics.md` §1/§2 |
| 20 | architect#1 / architect#2 | одна заглушка Phase 1 для I1-α: `WithEncounterStub` (F-10) vs `FakeEncounter` (EPIC-003 C4) | EPIC-001 F-10, EPIC-002 e2e, EPIC-003 C4, EPIC-004 I1-α | принято с изменением → `FakeEncounter` у EPIC-003, ранний merge; F-10 — только `FakeNarrator` v0 (C-05 v0.3, §0 исключение, §17) |
| 21 | architect#2 (EPIC-003) | флаг `MV_SWARM_FAKE=true` вместо отдельного контекста | EPIC-001 (`cmd/multiverse`) | принято с изменением → флаг читает `cmd/multiverse` (ADR-001 доп. п. 8) |
| 22 | architect#2 (EPIC-003) | совладение раздела `admin` `api/gateway.openapi.yaml` | EPIC-004 | принято → C-06 v0.3; §1 |
| 23 | devops / architect#3 | миграции — `goose` без `Down` (единообразие ADR-004/019/infrastructure) | EPIC-004 | подтверждено → ADR-004 строка исправлена; ADR-019 п. 2 — единственный источник |
| 24 | architect#2 → BA / SA | US-014 «две модели» → «по `baseline.md`»; UC-028/029 vs UC-036 | BA; SA (проверено — v0.2 актуальна) | передано BA; SA — действий нет |
| 25 | tech-lead#1/#2/#3 (сверка планов EPIC-002/003/004), ревью T-214/T-215, T-441, T-443 | ревизия контрактов 4: границы импортов 1a–1f, `config.cloud_enabled`, форма хешей, условная обязательность записей LLM, `gm.created` от шлюза, таймауты HTTP процесса, регистрация по файлам владельцев, мелкие правки, правила трёх веток | EPIC-001 (T-445, T-446), EPIC-002, EPIC-003, EPIC-004 | принято → `contracts.md` v0.11, ADR-001 доп. 2026-09-13, §3 этой карты (T-444) |
| 26 | code-reviewer#3 (ревью #1 T-444, Ma-2, Ma-3) | переходный порядок общего кода на период трёх веток (мягкий режим, решение оркестратора); владение сценариями `test/e2e` и правило имён в общем пакете | EPIC-001, EPIC-002 (T-055, T-058, T-060, T-061, T-062, T-070), EPIC-003 (T-242, T-243, T-251), EPIC-004 (T-303, T-305, T-309, T-313, T-314, T-315, T-357) | принято → §1 (строки 9, 10, 12, `test/e2e/**`), §3 п. 4; `contracts.md` §16 п. 8 (итерация 2 T-444) |

## 3. Период трёх веток эпиков (v0.6, T-444; `contracts.md` §16 п. 8)

Действует, пока EPIC-002, EPIC-003 и EPIC-004 идут параллельными ветками `epic/*` от develop. Предложение tech-lead#1, принятое с дополнениями system-architect#1 и решением оркестратора 2026-09-13. Контрольные слияния веток эпиков в develop и синхронизация develop → epic идут по постоянному разрешению пользователя от 2026-09-13.

| # | Что | Где правится | Как попадает к остальным | Почему |
|---|---|---|---|---|
| 1 | Общие документы: `architecture/**` (включая `components/**`, `diagrams/**`, ADR), `plan/**`, `analysis/**`, `requirements/**` | в одной ветке задачи от эпика EPIC-001 или develop; коммит в develop после ревью | синхронизация develop → epic (merge, не rebase) — перед стартом подволны и после каждой версии `contracts.md` | документ, изменённый в двух ветках, конфликтует на каждом слиянии, а решение одной ветки не видно двум другим |
| 2 | `epics/EPIC-00N/**` (design, tasks, dev-log, review, карточки) | только в ветке своего эпика | при контрольном слиянии эпика в develop | это рабочие документы команды; чужая команда их не правит |
| 3 | `journal.md`, `state.js`, `dashboard.html`, `CLAUDE.md`, `README.md` | только на develop (оркестратор; `CLAUDE.md` и `README.md` — tech-writer) | синхронизация develop → epic | горячие файлы: любая правка в ветке эпика — гарантированный конфликт |
| 4 | Общий код (мягкий режим, итерация 2 T-444): (а) `shared/{eventbus,contracts,clock,runtime}`, интерфейс `shared/objstore`, `schemas/events/_common.json`, `.golangci.yml`, `shared/contracts/{registry,ownership}.go`, `cmd/mvctl/internal/cli` (`shared/contracts/sources.go` — только задачей EPIC-001, итерация 3); (б) `cmd/multiverse/{main,serve,contexts}.go`, `cmd/mvctl/main.go`; (в) `shared/env/vars.go`, `.env.example` | (а) изменение API или поведения, которое видят другие эпики, — задачей EPIC-001 через system-architect (`contract-change`); точечное дополнение, нужное одной задаче эпика (строка своей семантики в `ownership.go` — ещё и с ревью tech-lead#1, строки своих типов в реестре, правило depguard своего пакета), — в ветке этой задачи с ревью system-architect и меткой `contract-change`; раскладка `registry.go` и сборка `Default()` — только T-446. Задачу принимает tech-lead её эпика; правки общих файлов (б), (в) и `registry.go` обязательно просматривает tech-lead#1 (`contracts.md` §16 п. 8, итерация 3). (б) до T-446 — в ветке задачи эпика с обязательным просмотром tech-lead#1 (приёмка — tech-lead эпика задачи) (T-055, T-060 — `serve.go`; T-058 — `cmd/mvctl/main.go`; T-303 и другие — регистрация в `contexts.go`); после T-446 регистрация и подкоманды — только в `contexts_<владелец>.go`, `commands_<владелец>.go`, `serve.go` — в ветке задачи с обязательным просмотром tech-lead#1 (приёмка — tech-lead эпика задачи); `main.go` и порядок в `contexts.go` — только EPIC-001. (в) переменная объявляется тем изменением, которое вводит её чтение, в ветке задачи вместе со строкой `.env.example`, с обязательным просмотром tech-lead#1 (приёмка — tech-lead эпика задачи) (C-14 v0.4); изменение формы манифеста — задачей EPIC-001 | (а) изменение API — контрольным слиянием EPIC-001 в develop и синхронизацией; точечные дополнения — контрольным слиянием своего эпика или ранней поставкой в develop, если оно нужно другому эпику; (б), (в) — контрольным слиянием эпика | общий код, который видят другие эпики, меняет только EPIC-001: правка API в ветке одного эпика ломает сборку двух других после слияния. Точечные дополнения и переходные правки `cmd` идут в ветке задачи, потому что строгий режим отменил бы C-14 и переходный порядок `contracts.md` §0 и потребовал бы перепланировать закоммиченные задачи EPIC-002 и EPIC-004 (решение оркестратора, ревью #1 T-444 Ma-2). Строки `ownership.go` меняют поведение State: EPIC-002 узнаёт о них из отчёта PR (`contracts.md` §16 п. 6) |
| 5 | `go.mod`, `go.sum` | новая зависимость — в ветке эпика с пометкой в отчёте | при слиянии конфликт решается `go mod tidy` | запрет на зависимости остановил бы эпики, а конфликт `go.sum` механический |
| 6 | Свои схемы `schemas/events/<тип>.v<n>.json` и строки реестра своих типов | в ветке эпика-владельца типа, в файл `registry_<владелец>.go` (до раскладки — в блок своего эпика в `registry.go`), ревью system-architect и обязательный просмотр tech-lead#1 | тип, который нужен потребителю другого эпика до интеграции, — ранней поставкой в develop (в планах эпиков это ещё названо слиянием в `integration/mvp-1`) | потребитель не может ждать интеграции, а схема без реестра не публикуется |
| 7 | Свои файлы в общих каталогах: `cmd/multiverse/contexts_<владелец>.go`, `cmd/mvctl/commands_<владелец>.go`, `cmd/mvctl/internal/<своя команда>` | в ветке эпика-владельца | при контрольном слиянии | порядок и реестр остаются у EPIC-001, владелец меняет одну строку в своём файле |
| 8 | Двойники `shared/testkit/<поставщик>`, `internal/<контекст>/<контекст>test` | поставщик контракта в своей ветке | ранней поставкой в develop после приёмки; смена сигнатуры — `contract-change` с ревью тимлида потребителя | потребитель компилируется против двойника, и молчаливая смена сигнатуры ломает чужую ветку |
| 9 | Хук `cmd/multiverse/fake_contexts.go` | EPIC-001 до T-256; T-256 удаляет его PR через tech-lead#1 | как п. 4 | без изменений (ADR-001 доп. п. 8) |
