# ADR-029: Ответ нарратива ссылается ярлыками вызова `eK`/`bK`, а не id; выдуманный ярлык отбрасывает страж поэлементно

Статус: **принято** — system-architect#1 в T-457 подтвердил поправки к ADR-016 (п. 4 ниже) и ADR-017 (п. 5 ниже), решил запрос п. 6 как C-07 v1.5 (`ref`) и принял для п. 8 хеш таблицы ярлыков (`labels_hash`); текст приведён в T-459 · Дата: 2026-09-13 · Автор: architect#2 (TEAM-2, EPIC-003; задача T-439, итерация 2 по ревью #1; T-459)
Уровень: реализация блока EPIC-003 — сборщик контекста, парсер, страж, роли нарратива. Схемы событий меняются только по C-07 v1.5 (принят, T-457): ветка `unknown_entity` с `ref` (п. 6, схема — T-213) и `llm.output.labels_hash` (п. 8, схема — T-211). Временной формы нет. Стек и границы блоков не затрагиваются.
Связи: ADR-005 доп. 3 и «Уточнение исполнения» п. 4; ADR-016 п. 1, 2, 4; ADR-017 п. 1–3, 5 и доп. 1; ADR-028 УИ п. 1; C-07 v1.3 и v1.5, C-15 v1.4; `ops/metrics/baseline.md` §5; NFR-002, NFR-021, NFR-061, NFR-080; US-017, US-018; КД `components/swarm-llm-laws.md` §3, §9.2, §10.2, §11.2, §11.4, §13.4, §13.4.1, §13.4.2; design EPIC-003 §9, риск 8. Задачи: T-203, T-209, T-211, T-212, T-213, T-217, T-218, T-233, T-235, T-237, T-247, T-248, T-260. Диаграмма: `diagrams/seq-swarm-llm-laws-narrative-labels.md`.

## Контекст

Потолок ответа нарратива считается по формуле ADR-005 УИ п. 4: `text.maxLength` = (N − каркас − служебные поля) × символов на токен × запас. Служебные поля там — шесть элементов (`mentions` ≤ 4, `background_refs` ≤ 2) по ≈ 6 токенов, «идентификатор в кавычках с запятой». На базовой конфигурации E потолок N = 160 токенов.

Шесть токенов верны только для коротких ссылок. В платформе ссылки длинные:
- id события — UUID (`shared/eventbus/sources.go` — v4, `cause_id.go` — v5);
- id встречи — `encounter-<id события>` (ADR-028 УИ п. 1), у NPC респауна — производный id.

Такой id занимает ≈ 20–25 токенов. Цена id:
- **в ответе** — при `maxItems` 4/2 массивы займут ≈ 140 из 160 токенов, на текст почти ничего не останется; даже два `background_refs` — ≈ 50 токенов;
- **в промпте** — 20 событий `<absence>` с id добавляют ≈ 500 токенов «прочего». Бюджет замера посчитан на промптах ~420 токенов, и каждое удлинение промпта уменьшает N (ADR-005 УИ п. 4, T-434).

Формулировка T-439 в индексе EPIC-001 говорит об элементах-id. Решение ниже от неё отступает, меняет правило 3 ADR-017 для нарратива, добавляет проверку в парсер ADR-016 и меняет смысл полей C-07. Поэтому оно оформлено ADR (ревью #1 T-439, Ma-1).

## Рассмотренные варианты

| Вопрос | Вариант | Плюсы | Минусы |
|---|---|---|---|
| Чем модель ссылается на сущность и фоновое событие | id как есть | таблица не нужна; `response_raw` читается без восстановления | ≈ 20–25 токенов на элемент: формула ADR-005 не держится; промпт длиннее на сотни токенов |
| | объекты `{"entity":{"id","type"}}`, как в фикстуре `llm.output.v1.*` | та же форма, что у событий шины | ≈ 15–18 токенов на элемент даже с короткими id, `maxLength` на E ≈ 110 символов; тип от модели не доверенный — страж всё равно берёт его из `View` |
| | **ярлык вызова `eK`/`bK`** | ≈ 6 токенов на элемент, формула держится; промпт короче; id модель не пишет вовсе | таблица ярлыков на вызов; replay и аудит должны её восстанавливать (п. 7–8) |
| Схема ответа | сужение на вызов: `items.enum` выданных ярлыков, `maxItems: 0` без отсутствия (итерация 1 T-439) | под грамматикой `json_schema` модель не выдумает ярлык | схема собирается на вызов: компиляция и кэш вопреки ADR-016 п. 1. Выдуманный ярлык на провайдере без грамматики — `schema_invalid` на весь ответ и повтор (≈ 5 с), а не отброс элемента. Под грамматикой модель, «задумавшая» чужой ярлык, вынуждена выбрать выданный: ошибка ссылки становится невидимой. `enum: []` при пустой таблице спорен |
| | сужение только в грамматике провайдера, парсер — по статической схеме | на `openai_compat` ярлык не выдумывается; парсер компилирует схему при старте | у одного вызова две схемы; невидимая ошибка ссылки — та же |
| | **статический `pattern` в файле схемы; таблицу проверяет страж** | одна схема на роль, компиляция при старте (ADR-016 п. 1 не меняется); поведение одинаково на любом провайдере; выдуманный ярлык — отброс элемента, текст остаётся (ADR-017, правило 3) | модель может выдумать ярлык в пределах `e1…e99` — это видно как отброс; для события отброса нужна ветка C-07 без id — `ref`, C-07 v1.5 (п. 6) |
| Replay: пересобранный контекст дал другие ярлыки | не сверять | просто | `eK`/`bK` молча разрешаются в другие id, ссылка приписана не тому |
| | расхождение `prompt_hash` — ошибка записи | расхождение видно | любая правка текста промпта ломает все записи `testdata/recordings`, а перезапись — только на стенде; design EPIC-003 §9 риск 8 прямо задаёт «расхождение — предупреждение» |
| | ярлык из хеша id (`e7k2`) | дрейф контекста ярлык не переназначает | 3–5 токенов на ярлык вместо 1–2, коллизии, случайные строки модель копирует хуже |
| | расхождение `prompt_hash` — предупреждение; ярлыки ответа при расхождении не разрешаются (итерация 2) | ни ложной атрибуции, ни поломки записей от правки текста | правка промпта вне таблицы — L в `## phase2`, `<facts>` при `NopMemory`, текст `<format>` — гасит ссылки ответа до перезаписи |
| | **хеш таблицы ярлыков в записи — `labels_hash` (C-07 v1.5; ревью #2 T-439 Mi-11, T-457)** | расхождение `prompt_hash` — только предупреждение; ссылки теряются, лишь когда разошлась сама таблица; атрибуция точная | опциональное поле в `llm.output`; запись до v1.5 сверяется по `prompt_hash` |

## Решение

1. **Где.** Ярлыки — только в вызовах `Kind=narrative` (`player-gm`, `group-narrator`). Вызовы тика несут id, как прежде.

2. **Выдача ярлыков — сборщик контекста (T-235).**
   - Сущности, выведенные в `<state>`, получают `e1…eN` в порядке id (байтовое сравнение строк). События `<absence>` — `b1…bM` в порядке `at`, при равенстве — по id. Одинаковый контекст даёт одинаковые ярлыки и одинаковый `prompt_hash`.
   - Пределы: N ≤ 99 — предел `pattern`. По NFR-080 в scope не больше 50 сущностей, поэтому предел недостижим; если всё же больше — сущности сверх 99 выводятся без ярлыка (сослаться на них нельзя) и пишется лог `warn`. M ≤ 20 — лимит `Absence` (КД §11.2).
   - Края: N = 0 или M = 0 — таблица пуста, схема та же. Сборщик отдаёт таблицу всегда: пустая карта — не `nil` (`nil` означает «таблицу не передали», п. 5). Ярлык в ответе при пустой таблице — выдуманный, его отбрасывает страж (п. 5).
   - Форма в промпте — ярлык в квадратных скобках после имени: `Альфа-волк [e3]`, `[b2] Буря повалила мост`. Id в промпт нарратива не попадает. В `<events>` сущность названа именем и, если она есть в `<state>`, ярлыком.
   - Ярлыки живут только в `user`. `system`, включая `<format>`, от набора ярлыков не зависит: префикс остаётся общим, и кэш промпта llama-server (`cached_tokens`) сохраняется (ревью #1, Mi-4).

3. **Схема — `schemas/agent/narrative.json` (T-203).** Элементы массивов — строки с `pattern` `^e[1-9][0-9]?$` и `^b[1-9][0-9]?$`, `maxItems` — по таблице КД §13.4.1. Схема статична: на вызов не сужается, компилируется при старте, та же схема уходит провайдеру в `response_format`. Кэш скомпилированных схем не нужен. Грамматика llama-server строит правило строки из `pattern` вида `^…$`; если провайдер `pattern` не применит, ответ с id вместо ярлыка поймает парсер по той же схеме (`schema_invalid`, повтор) — поэтому доля `valid_first_try` нарратива проверяется на стенде (T-260). `<format>` — статичный текст (T-218): «на сущности и события ссылайся только ярлыками из промпта; в `text` ярлыков не пиши, называй по имени; сослаться не на что — пустой массив».

4. **Ярлык в `text` — ошибка формата (поправка к ADR-016 п. 1 и 4).**
   - После валидации схемой парсер для `Kind=narrative` проверяет `text` выражением `(^|[^0-9A-Za-z_])[eb][1-9][0-9]?([^0-9A-Za-z_]|$)`.
   - Совпадение — `validation_status=invalid`, `llm.output.rejected reason=schema_invalid` без `element`, повтор с подсказкой в `user`, затем шаблон.
   - Текст не правится: ADR-016 не меняет содержимое строк, а вырезанный ярлык оставил бы фразу без подлежащего.
   - `CheckLanguage` ярлыки не исключает: до неё ответ с ярлыком в `text` не доходит (ревью #1, Mi-5).

5. **Страж (поправка к ADR-017 п. 1 и правилу 3 для нарратива).**
   - `guardian.Input.Mentions map[string]string` (ярлык → id сущности) и `AbsenceEventIDs map[string]string` (ярлык → id события; прежде `map[string]bool`, множество id) заполняет роль из таблицы сборщика.
   - Правило 3 для нарратива — по элементу:
     - ярлык есть в таблице и сущность видима — элемент остаётся;
     - ярлык есть в таблице, но сущность не видима по КД §10.3 (дефект сборщика) — элемент отброшен, `unknown_entity`, `entity` — id, тип и имя из `View`;
     - **ярлыка в таблице нет (выдуман)** — элемент отброшен, текст остаётся, статус `partially_rejected`. Так правило 3 и работало с выдуманным id: поэлементный отброс сохранён.
   - `background_refs[i]` из таблицы видимы по построению: в `<absence>` попадают только события индекса своего мира и региона.
   - Таблицу не передали — ошибка программы, а не ответ модели. Условие проверяется **по каждому массиву**: непустой `mentions` при `Input.Mentions == nil` или непустой `background_refs` при `AbsenceEventIDs == nil` (пустая таблица — не `nil`, п. 2). Весь ответ `invalid`, `reason=other` без `element`, лог `error` (ревью #1, N-3; условие по массиву — ревью #2 N-6, подтверждено T-457).
   - Статусы стража, порядок эффектов и повторы не меняются.

6. **Событие отброса выдуманной ссылки — `unknown_entity` с `element` и `ref` (C-07 v1.5, принят в T-457).**
   - C-07 v1.3 требовал для `unknown_entity` `element` и одно из двух: `entity` (`EntityRef` с обязательным `type`) или `background_ref{event{id}}`. У выдуманного ярлыка нет ни id, ни типа.
   - Пробел у двух массивов разный *(уточнено при приёмке T-439, ревью #2 Mi-9)*:
     - **`mentions` и тик — пробел прежний, не от ярлыков.** `EntityRef` требует `type`, а у выдуманной сущности типа нет. Схемы тика дают только `entity?: {id}` и `affects?: [{id}]` (КД §13.4), и выдуманный id тика — `npc-999` из US-018 — сегодня тоже нельзя опубликовать, не придумав тип.
     - **`background_refs` — пробел создан ярлыками.** C-07 v1.3 ввёл `background_ref{event{id}}` как раз для выдуманной фоновой ссылки, и `EventRef` требует только `id`: с id такая ссылка публиковалась как `unknown_entity` с `background_ref`. У ярлыка `b7` id нет, и без новой ветки ей оставался бы только `other`.
   - Решение C-07 v1.5 (запрос этого пункта, вариант A T-457): третья ветка условия `unknown_entity` — опциональное поле `ref`, строка 1…128 символов в том виде, в каком её написала модель (id в тике, ярлык в нарративе). В событии ровно одно из `entity` | `background_ref` | `ref`; `ref` допустим только при `reason=unknown_entity`. Расширение совместимое. Предел 128 — общий с id схем тика (T-203), `ref` не усекается. Запрос шёл под номером «v1.4», но этот номер C-07 занят денежным лимитом (T-449), и изменение пошло как v1.5.
   - **Отображение (T-213).** Шлюз сразу публикует отброс выдуманной ссылки как `unknown_entity` с `element{index, type}` и `ref`; `llm.output.reasons[]` содержит `unknown_entity`. Временная форма итерации 2 (`reason=other` с `element`) отменена в T-457 до старта T-213 и в код не идёт. Поведение стража (элемент, а не весь ответ) от формы события не зависит.
   - **Влияние на NFR-021 и US-018 (запись для business-analyst — T-457 п. 1.6).** Критерий NFR-021 «только зарегистрированные `entity_id`» для нарратива усиливается: id в события ставит роль по таблице, модель id не пишет. Метрика `guardian_rejections_per_100` по `unknown_entity` считает все три ветки, включая `ref`: выдуманные ярлыки нарратива и выдуманные id тика попадают в неё сразу, временной формы под `other` нет. В US-018, критерий 2, для нарратива «ссылка на `npc-999`» читается как «ярлык вне таблицы вызова, например `e7`»; «отсутствующая ссылка» в событии — `ref`; для тика `npc-999` — тоже `ref`. ADR-017 доп. 1 п. 4 («`reason=unknown_entity` с отсутствующей ссылкой») выполняется буквально *(уточнено при приёмке T-439, ревью #2 Mi-9; приведено к T-457 в T-459)*.

7. **Смысл полей C-07 без смены схем (пометку в `contracts.md` вносит system-architect#1).**
   - `llm.output.response_raw` нарратива содержит ярлыки, а не id; `llm.output.rejected.element.index` — индекс в массиве ответа.
   - `llm.output.rejected.entity` — сущность из `View` (существует, но агенту не видима), тип — из `View`, а не «названный моделью». Несуществующая ссылка — `ref` (п. 6).
   - `narrative.output.background_refs` — id событий. Ярлыки разрешает роль (T-233, T-247), повторы одного ярлыка схлопываются с сохранением порядка. Во внешних событиях ярлыков нет.
   - Таблица ярлыков в `llm.output` не пишется — только её хеш `labels_hash` (C-07 v1.5, п. 8). Id, которые вызов изменил в мире или показал игроку, уже есть во внешних событиях, а таблица в каждой записи — объём ради редкого аудита. Аудит (US-018, «отлаживать промпты») читает ярлыки так:
     - при `MV_LLM_STORE_PROMPTS=true` сохранённый промпт (`prompts-{world}/…`) содержит `Имя [eK]` — таблица читается из него;
     - при выключенном флаге таблица восстанавливается правилом п. 2 из контекста на момент причины (replay журнала до `meta.causation_id`), совпадение подтверждает `labels_hash` записи (у записи без поля, сделанной до C-07 v1.5, — `prompt_hash`);
     - всё, что вызов изменил в мире или показал игроку, уже несёт id: `narrative.output.background_refs`, `llm.output.rejected.entity`.

8. **Replay и восстановление по записи (ревью #1, Mi-6; сверка по `labels_hash` — ревью #2 Mi-11, C-07 v1.5).**
   - В `mode=replay` и при нарративе из записи после рестарта (КД §8.4) шлюз пересобирает промпт и таблицу ярлыков и сверяет их с записью.
   - `prompt_hash` не совпал — лог `warn` `llm_replay_prompt_drift` с `agent`, `phase`, `attempt`. У `Kind=tick` и у нарратива с пустыми массивами других следствий нет (design EPIC-003 §9, риск 8, без изменений).
   - Ярлыки нарратива с непустыми `mentions`/`background_refs` разрешаются по `llm.output.labels_hash` записи — хешу таблицы ярлык → id (кодирование — C-07 v1.5):
     - хеш пересобранной таблицы совпал — ярлыки разрешаются, даже если `prompt_hash` расходится;
     - не совпал — лог `warn` `llm_replay_labels_drift` с `agent`, `phase`, `attempt` — у любой записи с полем, в том числе с пустыми массивами (C-07 v1.5); непустые массивы считаются пустыми, `text` используется. Расхождение таблицы не приписывает ссылку другому id молча; цена — потерянные ссылки до перезаписи;
     - у записи без `labels_hash` (сделанной до v1.5) — прежнее правило: `prompt_hash` расходится — массивы пусты, `text` используется.
   - Следствие: смена L или текста `## phase2` (T-260), текст `<format>` и `<facts source="memory">`, которых нет в replay с `NopMemory` (I2, T-248), таблицу не меняют — ссылки записей не теряются. С `NopMemory` ссылки теряются, только если различается сама таблица (`<absence>`). Но `AbsenceSummary` памяти заменяет список журнала (КД §11.3), поэтому у записи, снятой с живой памятью, таблица обычно различается, и e2e на таких записях `background_refs` не сверяют (T-248).
   - Таблица с ключом не формы `eK` (в `mentions`) или `bK` (в `background_refs`) — ошибка программы: `llm.LabelsHash` отвечает ошибкой, шлюз отвергает такой вызов на входе, до бюджета и провайдера (T-211, T-213). Сборщик (п. 2) такой таблицы не выдаёт.
   - Новых `llm.output` и `llm.output.rejected` в replay нет (C-07).

9. **Длина — связанные решения (КД §13.4.1).**
   - Служебные поля — 6 токенов на ярлык в кавычках с запятой, 66 токенов вместе с каркасом при `maxItems` 4/2.
   - N округляется вниз до десятков, в том числе для Qwen3.6: 210 и 400 вместо «≈ 220» и «≈ 410» ADR-005 УИ п. 4 (ревью #1, Mi-1). 220 токенов при 107,2 ток/с — это 5003 мс, выше порога.
   - `text.maxLength` = (N − 66) × min(0,9 × c; c_min), округление вниз до кратного 5. c — ожидаемые символы на токен (до калибровки 2,5), c_min — нижняя граница плотности (до калибровки 2,0). E: 185 символов, а не 210 (ревью #1, Mi-2). До калибровки запас дешевле ошибки: 25 символов текста против обреза `max_tokens`, повтора и шаблона именно на самых длинных ответах.
   - Тест T-203 проверяет согласованность N, `maxLength`, `maxItems`, c и c_min, записанных рядом с числами, и то, что `maxLength` из медианы c не выходит за c_min. Запас держит отдельная константа c_min, а не тот же c: поэтому тест падает именно на той ошибке, которую нашло ревью, — `maxLength` 210 итерации 1 даёт 66 + ⌈210 / 2,0⌉ = 171 > 160. Верно ли само значение c_min, CI не видит: реальную плотность модели проверяет только стенд (T-260 в сессии T-438).

## Последствия

- **Позитивные.**
  - Формула ADR-005 держится: на E текст — 185 символов, а не ≈ 110 и не «ничего».
  - Промпт нарратива короче на сотни токенов — меньше «прочего» в NFR-002.
  - Модель не пишет id: ссылка в событии всегда взята из таблицы, которую построил код.
  - Одна статическая схема на роль; ADR-016 п. 1 (компиляция при старте) не меняется.
  - Выдуманная ссылка стоит отброса элемента, а не повтора на 5 с.
- **Негативные.**
  - Таблица ярлыков — ещё одно состояние вызова: сборщик, страж, роль и replay должны видеть одну и ту же.
  - Аудит `response_raw` без сохранённых промптов требует пересборки контекста.
  - Ярлык в `text` стоит повтора на весь ответ.
  - Расхождение самой таблицы ярлыков при replay (`llm_replay_labels_drift`) теряет ссылки в ответах с ярлыками до перезаписи. Правка текста промпта вне `<state>`/`<absence>` ссылок не теряет (C-07 v1.5).
- **Что сделать** (строки DoD — `epics/EPIC-003-swarm-llm-laws/tasks.md` и отчёт tech-lead#2 по T-439):
  - T-203 — статическая схема с `pattern`, числа E, c и c_min рядом с числами, тест инварианта, длина в `## phase2`; фикстуры `llm.output.v1.*` с ярлыками; `maxLength: 128` у id в `schemas/agent/*` — предел общий с `rejected.ref` (T-457, Mi-5);
  - T-209 — проверка ярлыка в `text` (п. 4), компиляция при старте без сужения;
  - T-211 — `llm.LabelTable` и `llm.LabelsHash` (ключ вне формы — ошибка), схема `labels_hash`, golden-кодирование, запись поля из явного входа `Recorder`; поля `llm.Request.AgentID`/`Attempt` (C-15 v1.4); `called_at` (C-07 v1.6, T-456, после её слияния);
  - T-212 — `warn llm_replay_prompt_drift` в replay и `labels_hash` записи в `recorded.Record` (п. 8); вызов проверки п. 4 на шаге парсера;
  - T-213 — схема `llm.output.rejected` с `ref` и отображение выдуманной ссылки в `unknown_entity` + `element` + `ref` (п. 6), `reasons[]`; вход `Recorder` из `Call.Guard` и свойство-тест «`labels_hash` есть ⇔ `Kind=narrative`»; сверка ярлыков replay по `labels_hash` и правило для записи без поля (п. 8) — `Call.Guard` вводит эта задача *(распределение — ревью #1 T-459, Ma-1)*;
  - T-217 — `Input.Mentions`/`AbsenceEventIDs`, правило 3 по п. 5, ветка «таблицы нет» по каждому массиву; `Rejection.Ref` в `internal/llm/types.go`;
  - T-218 — статичный `<format>` с правилом ярлыков; `system` не зависит от набора ярлыков;
  - T-233, T-247 — разрешение ярлыков в id `narrative.output.background_refs`, схлопывание повторов;
  - T-235 — выдача ярлыков по п. 2, таблица в `guardian.Input`;
  - T-237 — нарратив из записи при догоне идёт через сверку п. 8;
  - T-248 — расхождение списка отсутствия с `NopMemory` покрыто п. 8;
  - T-260 — калибровка c и c_min, стендовые доли (КД §13.4.1, «Проверки»).
- **Документы других владельцев** (T-439 перечислил их в отчёте; состояние — на T-459):
  - `analysis/api-contracts.md` §3.4 (`mentions: [entity_id]`, `background_refs: [event_id]`) — элементы ответа нарратива — ярлыки вызова, id — во внешних событиях;
  - ADR-005 УИ п. 4 — «идентификатор в кавычках» → «ярлык вызова в кавычках (ADR-029)»; N Qwen3.6 по правилу округления — 210 и 400; `maxLength` по п. 9 (architect#1, и в `baseline.md` §5; строка «см. ADR-029» внесена T-457, перечень правок чисел — там же);
  - `contracts.md` C-07 — смысл полей (п. 7), `ref` (п. 6) и `labels_hash` (п. 8) внесены T-457 как C-07 v1.5;
  - ADR-016 и ADR-017 — пометки «см. ADR-029» у п. 1 и правила 3 внесены T-457: поправки подтверждены;
  - ADR-017 доп. 1 п. 4 (формулировка US-018 «`reason=unknown_entity` с отсутствующей ссылкой») — пометка о нарративе (`ref`, C-07 v1.5) внесена T-457 вместе с поправкой правила 3 *(приёмка T-439, ревью #2 Mi-9)*;
  - US-018 и метрика NFR-021 — запись для business-analyst (п. 6; текст — T-457 п. 1.6, передаёт оркестратор).

## Условия пересмотра

- Доля **попыток** нарратива с ярлыком в `text` (ответ отвергнут парсером, п. 4) выше 2 % на стенде (T-260) или в прогонах S1/S2. Принятого ответа с ярлыком в `text` не бывает по построению, поэтому мерится доля попыток *(уточнено при приёмке T-439, ревью #2 Mi-10)*. Тогда меняется форма ярлыка в промпте либо вырезание ярлыка из `text` становится допустимым.
- Выдуманных ярлыков (ярлык ответа вне таблицы промпта; в платформе — отброс стражем, п. 5–6) больше 5 на 100 вызовов нарратива на стенде (T-260) или в прогонах S1/S2. Тогда сужение `enum` в грамматике провайдера стоит своей цены.
- Провайдер без грамматики (`anthropic`, E-H) становится основным для нарратива: пересчитать обе доли выше на нём.
- `llm_replay_labels_drift` в CI чаще раза на запись: таблица ярлыков расходится между записью и replay не из-за правки данных, а из-за недетерминизма сборки контекста. Тогда пересматривается правило выдачи п. 2 (порядок, источник `<absence>`); хеш таблицы уже в записи (C-07 v1.5).
- Появляется удаление сущностей (после MVP-1): тип из `View` может пропасть, и таблица должна нести тип.
- NFR-080 поднимает предел сущностей в scope выше 99: расширяется `pattern`.
- Аудит без сохранённых промптов становится регулярной операцией: в `llm.output` пишется сама таблица ярлыков, а не только её хеш (смена C-07).

## История изменений

- 2026-09-13 · T-439, итерация 2 (architect#2) — первая редакция, статус «предложено».
- 2026-09-13 · T-459 (architect#2) — приведено к решениям T-457: статус «принято»; п. 5 — условие «таблицу не передали» по каждому массиву (ревью #2 T-439 N-6); п. 6 — C-07 v1.5 (`ref`), временная форма `other` отменена; п. 7–8 — `labels_hash` и сверка replay по нему (Mi-11); «Рассмотренные варианты», последствия, «Что сделать» (T-203, T-211, T-212, T-213, T-217), документы других владельцев и условия пересмотра приведены.
- 2026-09-13 · T-459, итерация 2 (architect#2, ревью #1) — п. 8: `llm_replay_labels_drift` при любых массивах (C-07 v1.5), ключ таблицы вне формы — ошибка программы, оговорка про `AbsenceSummary` и e2e на записях с памятью; «Что сделать» — распределение `labels_hash` между T-211 (функция и вход `Recorder`), T-212 (`prompt_hash`) и T-213 (`Call.Guard`, свойство-тест, сверка ярлыков); `Rejection.Ref` в T-217.
