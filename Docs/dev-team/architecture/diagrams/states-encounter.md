# Жизненный цикл встречи

**Вопрос читателя: какие состояния проходит встреча, кто её открывает и закрывает и по каким причинам она заканчивается?**

Дата: 2026-09-11 · system-architect#1
Иллюстрирует: [`../../analysis/data-model.md`](../../analysis/data-model.md) §3.7 и §9.2, [`../contracts.md`](../contracts.md) C-05 (жизненный цикл встречи), C-02 (владение и предложения), C-04 (каскад забвения).
Проверено по дереву: `shared/entity/types.go` (`EncounterStateActive`, `EncounterStateResolved`, `ResolutionNPCDead`, `ResolutionPlayersOut`, `ResolutionAbandoned`), `schemas/events/encounter.started.v1.json`, `schemas/events/encounter.ended.v1.json`, `shared/testkit/swarm/fake_encounter.go`, `shared/testkit/gateway/harness.go`.

```mermaid
stateDiagram-v2
    direction TB
    state "предложена" as proposed
    state "active — идёт схватка" as active
    state "resolved — терминальное" as resolved
    state "ожидание действия" as waiting
    state "разрешение действия" as resolving
    state "раунд открыт" as roundopen

    [*] --> proposed : entity.create.proposed тип encounter, cause spawn
    note right of proposed
        Создаёт GM региона — уровень домена.
        Всё дальнейшее ведёт агент встречи — уровень задачи.
        Двойник в дереве играет обе роли и подписывает
        событие соответствующим уровнем.
    end note

    proposed --> active : entity.created плюс encounter.started
    proposed --> [*] : entity.update.rejected

    state active {
        [*] --> waiting
        waiting --> resolving : действие игрока принято
        resolving --> waiting : броски, решение боя, атомарный пакет применён
        waiting --> roundopen : область группы, первое открывающее действие
        roundopen --> waiting : round.closed
    }

    active --> resolved : encounter.ended reason npc_dead
    active --> resolved : encounter.ended reason players_out
    active --> resolved : encounter.ended reason abandoned
    resolved --> [*]

    note left of resolved
        npc_dead — все противники упали.
        players_out — живых игроков в бою не осталось:
        смерть, удавшийся побег или переход в abandoned по забвению.
        abandoned — все вышли из региона или сессии,
        агент задачи завершён по времени жизни.
        Встреча не открывается заново: новая схватка — новая сущность.
    end note
```

## Правила, которые эта диаграмма закрепляет

1. **Открывает и закрывает встречу не один и тот же уровень.** Создание с причиной «появление» принадлежит уровню домена, всё последующее — уровню задачи. Таблица владения это различает, и проверка в дереве сделана не списком в тесте, а сверкой с самой таблицей.
2. **Событие конца строится до пакета изменений, а публикуется после факта этого пакета** (C-05 v1.4 п. 5, ADR-026). Строится до — чтобы пакет сослался на его идентификатор; публикуется после факта — чтобы не объявить конец, которого State не принял. Пакет выброшен — конца нет, встреча продолжается. То же для начала: `encounter.started` — после `entity.created` (п. 4).
3. **Ни одно принятое действие не остаётся без следа.** Даже обмен, в котором никто не попал, кладёт в пакет набор изменений сущности встречи: иначе действие неотличимо от «никто не слушал».
4. **Один противник — одна активная встреча.** Ограничение записано в модели данных (`data-model.md` §3.7) и нормативно для открывающего — GM региона (C-05 v1.4 п. 6); State его не проверяет. Область игрока или группы держит обратную сторону — у игрока одна встреча, — но не эту: двойник сегодня открывает две встречи против одного волка, когда в регион входят двое.
5. **Забвение встречу не закрывает напрямую.** Шлюз ничего не делает со встречей: агент видит факт перехода персонажа в `abandoned` и закрывает встречу сам с причиной «живых игроков нет».
6. **Встречу, чьё существо убито чужой рукой, закрывает агент встречи** — пакетом закрытия без трофея, затем `encounter.ended {reason: npc_dead}` без убийцы, с scope встречи в конверте (C-05 v1.4 п. 6). При соблюдении правила 4 в MVP-1 путь недостижим; он для будущих источников (Entity-Actor, пробой законов).

## Что здесь будущее

- **Раунд группы** (`round.opened` → `round.closed`) — координатора раундов нет: `internal/gateway` не написан. Во вложенном состоянии эта ветка нарисована, потому что она часть контракта C-04, но сегодня по ней не проходит ничего.
- **Настоящий агент встречи** — `internal/swarm` не написан; роль играет двойник `testkit/swarm.FakeEncounter`, и он работает только на шине в памяти или в `core` при включённом хуке, которого в дереве тоже нет.
- **Причина `abandoned`** — требует времени жизни агента и выхода из региона; ни того, ни другого в дереве нет. Двойник закрывает встречу только по двум первым причинам.

## Расхождения с деревом

**Сверка T-409 (2026-09-11, architect#1).** «Закрыто» — документ приведён к дереву.

| # | Статус | Что сделано |
|---|---|---|
| 1 | закрыто | `data-model.md` §3.7: у атрибута `resolution` сказано, что в `encounter.ended` то же значение — в поле `reason` |
| 2 | закрыто | `data-model.md` §9.2: петля заменена вложенными состояниями «ожидание — разрешение — раунд» с веткой повтора при конфликте; правило — C-05 v1.3 п. 1 |
| 3 | закрыто (T-416) | C-05 v1.4 п. 4, ADR-026: `encounter.started` публикуется после `entity.created`, а при отказе создания его нет вовсе. У читателя двух топиков состояние «объявлена, сущности ещё нет» осталось, но оно всегда временное: факт в журнале уже есть |
| 4 (новое, T-416) | код (задача «двойники по C-05 v1.4») | правила 2, 4 и 6 ниже — целевые: двойник публикует `encounter.started` и `encounter.ended` до факта и открывает вторую встречу против NPC, занятого в активной встрече; расхождения названы в C-05 v1.4 «Заглушка» |

Формулировки, записанные при рисовании (до сверки):

1. **Поле причины в событии называется не так, как атрибут сущности.** В схеме `encounter.ended` это `reason`, в сущности встречи — `resolution` (`shared/entity/types.go`). Значения совпадают, имена — нет. `data-model.md` §3.7 называет только `resolution`, и читатель, ищущий его в событии, не найдёт.
2. **`data-model.md` §9.2 рисует переход `active → active` по `round.opened`, `combat.decided`, `round.closed`** как петлю без внутренней структуры. Это скрывает то, что решает дефекты: внутри активной встречи есть ожидание действия и его разрешение, и именно в момент разрешения агент обязан обновить своё представление о мире, если получил отказ по конфликту версий. Правило пока живёт в журнале решений, а не в C-05.
3. **Состояния «предложена» в модели данных нет.** Между предложением создать встречу и фактом её создания сущности ещё не существует, а событие `encounter.started` уже может быть опубликовано — двойник публикует его сразу за предложением. Кто и что видит в этот промежуток, контракт не оговаривает.
