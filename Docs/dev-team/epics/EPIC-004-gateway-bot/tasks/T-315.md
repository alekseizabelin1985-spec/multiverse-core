# T-315: e2e бота на `FakeUpdateSource`/`FakeSender`/`FakeGateway`

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I1 / 1.9 |
| Размер | M |
| Исполнитель | developer#2 |
| Зависит от | T-308, T-312 |
| Ветка | `task/T-315-bot-e2e-fakes` (папка `.worktrees/T-315`, от `epic/EPIC-004-gateway-bot` 585de13; кончик эпика `6b67fec` влит в ветку — 7f0ff6b) |
| Статус | done |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-14 03:21 / 2026-09-14 |
| Метка | нет |

## Описание и DoD
Главный источник — раздел «### T-315» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- Сценарий от `/start` до `/forget confirm`; golden-тексты, порядок сообщений, ack, стабильный `action_key`.
- Ветки SEC-06, SEC-07, SEC-10 и ошибки `429`/`503`; три прогона подряд дают одинаковый результат.

## Выполнение (developer)

**developer#2 · 2026-09-14, 03:21–03:42.** Ветка `task/T-315-bot-e2e-fakes` от кончика `epic/EPIC-004-gateway-bot` (585de13), папка `.worktrees/T-315`. Не коммитил.

### Что сделано — по строкам DoD

| Строка DoD | Где | Как проверено |
|---|---|---|
| Тексты (golden), клавиатуры, порядок, ack каждой доставки, стабильный `action_key` | `cmd/telegram-bot/e2e/scenario_test.go`, `TestBotScenarioFromStartToForget` | Сценарий: `/start` → согласие кнопкой → неверное имя → имя, совпавшее с username (кнопка «Да») → выбор из двух миров → `/enter dark-forest-01` → `/attack` без цели → `/say` → то же обновление ещё раз → `/forget` → `/forget confirm`. Golden-запись: текст и клавиатура каждого ответа по порядку, доставки каждого шага (механика, нарратив, `world_event` с боевой клавиатурой). Каждая выданная доставка подтверждена ровно один раз, ни одна не выдана дважды. Бот отправил доставки в порядке выдачи шлюзом. Повтор `update_id` дал тот же `action_key` и один `player.said` на шине. |
| Группа молчит (SEC-07), посторонний — один отказ на серию (SEC-06) | `branches_test.go`, `TestBotIsSilentInAGroupAndRefusesAStrangerOnce` | Два сообщения из группы от игрока из allowlist остались без ответа. На три сообщения постороннего ушёл один ответ «Доступ по приглашению.». Шлюз получил один вызов — `resolve` игрока, который писал после них. Счётчики `Denied=3`, `NotPrivate=2`. |
| `parse_mode`: нарратив с `[x](http://…)` и `<a>` приходит буквально (SEC-10) | `branches_test.go`, `TestBotDeliversTheMarkupOfANarrativeAsText` | Нарратор публикует через обёртку шины, которая подменяет `text` в `narrative.output` на Markdown- и HTML-ссылки. Оба нарратива входа дошли символ в символ, с пометкой шаблона. |
| `429 rate_limited` и `503 bus_unavailable` → подсказка, команда не теряется | `branches_test.go`, `TestBotKeepsTheCommandOnRateLimitAndAnUnavailableBus` | `429` — настоящий лимит шлюза (`MV_GATEWAY_RATE_ACTIONS_PER_MIN=1`, `BURST=1`). Игрок получил «Слишком часто, подождите 60 с.»; то же обновление через минуту принято с тем же ключом. `503` подставляет прокси перед шлюзом. Один `503` клиент `flow` повторяет сам с тем же ключом, игрок получает «Принято.». Два `503` дают «Сервис недоступен, повторите позже.»; повтор того же обновления уходит с тем же ключом. На четыре обновления на шине ровно четыре `player.said`. |
| Пауза после раннего пустого ответа (приёмка T-307, дополнение 2) | `internal/deliver/loop.go`: `EarlyEmpty = Wait/2`, `Options.Clock`; `pace_test.go` | Пустой ответ быстрее `EarlyEmpty` ставит паузу, как неудачный long-poll: 1, 2, 4 … 30 с. Пустой ответ, дождавшийся `wait_ms` (или не меньше `EarlyEmpty`), паузы не даёт, и счёт сбоев сбрасывается. Unit на `clock.Timers` проверяет, что шлюз в остановке получает не больше одного запроса за паузу. Отдельный тест проверяет границы: `Wait`, `EarlyEmpty` и `EarlyEmpty − 1 мс`. |
| Повтор ack при `503` не дольше лизинга (приёмка T-307, ревью #1) | `loop.go`: `NewAckClient`, `AckHTTPTimeout` 6 с, `AckBackoff` (1 повтор, 200 мс), `MaxFlush` 12,2 с, `Options.Acks`; `serve.go` | Способ — срок на flush через отдельный клиент ack. Unit использует настоящий клиент и шлюз, который отвечает `503` через 5 с по ручным часам. Следующий long-poll начинается через ≈ 20,4 с после выдачи лизинга, это меньше 30 с. До него — 4 попытки ack, по две на flush. Контроль: с клиентом по умолчанию тот же тест даёт ≥ 30 с. `TestTheAckClientEndsWithinTheLease` проверяет, что 2 × `MaxFlush` < лизинга. Проводку в `main` проверяет `TestTheWiringHandsOutThePrivacyLoggerAndTheRightClients`. |
| Три прогона подряд — одинаковая запись | `TestBotScenarioFromStartToForget` | Сценарий трижды проходит с нуля в одном тесте, записи сравниваются. `-count=10` без флаков (см. «Прогоны»). |
| `flow.ValidName` → `api.ValidCharacterName` (приёмка T-312) | `flow/onboarding.go`; `flow/onboarding_test.go`; `internal/gateway/api/names_test.go` | Копия регулярки и `TestValidName` удалены. Бот обрезает пробелы по краям и проверяет имя функцией шлюза. Новый `TestTheNameIsCheckedByTheRuleOfTheGateway`: два пробела подряд, имя в NFD, `Вася!` и `В` получают `render.NameInvalid` без вызова шлюза; `"  Вася Пупкин \t"` уходит в `POST /v1/characters` как `Вася Пупкин`. В тест `api` перенесены отсутствовавшие там случаи: `Вася-2`, `Анна Мария`, `John Smith 3`, `Вася\u0007`, `Вася\t`, `[x](y)`, `Вася.`. |

### Изменённые файлы

- создан: `cmd/telegram-bot/e2e/harness_test.go`, `scenario_test.go`, `branches_test.go` (`//go:build e2e`, пакет `e2e_test` без production-файлов);
- создан: `cmd/telegram-bot/internal/deliver/pace_test.go`;
- изменён (production): `cmd/telegram-bot/internal/deliver/loop.go`, `cmd/telegram-bot/serve.go`, `cmd/telegram-bot/internal/flow/onboarding.go`;
- изменён (тесты): `cmd/telegram-bot/internal/deliver/fakes_test.go` (у фейка `outbox` появились `stopping` и `emptyTook`; nil-запись `pollErr` сдвигает часы на `wait`), `loop_test.go` (`TestASuccessfulPollResetsThePause` идёт на часах фейка), `flow/onboarding_test.go`, `cmd/telegram-bot/main_test.go`, `internal/gateway/api/names_test.go`;
- изменён (документ): `cmd/telegram-bot/README.md` — строка `e2e/` в таблице частей, строки таблицы клиентов про long-poll и ack;
- изменён (общий файл EPIC-001): `.golangci.yml` — правило `cmd-telegram-bot-e2e` и исключение его файлов из `cmd-telegram-bot` (см. «Отклонения», п. 1).

### Отклонения от дизайна и индекса

1. **Место e2e и правило линтера.** `ownership.md` (строка `test/e2e/**`) называет файл `test/e2e/gateway_bot_test.go`. Там тест не собрать: пакеты бота лежат в `cmd/telegram-bot/internal/*`, а Go не даёт импортировать их из `test/e2e`. Тест живёт в `cmd/telegram-bot/e2e/`. Правило depguard `cmd-telegram-bot` запрещает в тестах бота `internal/*` и `shared/eventbus`, а e2e нужны `internal/gateway/gatewaytest` и `membus`. Поэтому добавлено парное тестовое правило `cmd-telegram-bot-e2e` по образцу `internal-swarm-tests`, как предлагает комментарий самого `cmd-telegram-bot`. Файлы — `**/cmd/telegram-bot/e2e/**_test.go`; разрешены `client$`, `api$`, `gatewaytest$` и шина; остальной `internal/*` запрещён. Production-файлы бота и его прочие тесты остаются под `cmd-telegram-bot`. Это открытый вопрос 2 T-470 («до T-315») и правка файла EPIC-001 — нужно подтверждение system-architect (см. «Открытые вопросы»). Тест границы импортов (`go list -deps ./cmd/telegram-bot/...`) каталог без тега пропускает, проверено.
2. **«Одинаковая запись `FakeSender`».** В записи два потока, как в боте два отправителя: ответы обработчика (шлюз доступа и `flow`) и доставки цикла. Порядок между потоками — гонка по устройству бота: «Принято.» и доставки того же действия идут параллельно. Ответы сравниваются в точном порядке. Доставки одного шага сравниваются набором. Шлюз ставит их в outbox из четырёх топиков по мере прихода событий, C-01 топики между собой не упорядочивает, и порядок внутри шага от прогона к прогону разный. Это видно в исследовательском прогоне: у `/enter` механика шла то первой, то последней. То, что бот ничего не переставляет, проверено отдельно: отправленные сообщения совпадают с выданными шлюзом доставками по порядку.
3. **`503 bus_unavailable`** подставляет прокси перед шлюзом (`httputil.ReverseProxy`). У `FakeGateway` нет способа уронить публикацию на `membus`, а менять `gatewaytest` — не объём задачи. `429` — настоящий лимит шлюза.
4. **SEC-10** проверен на уровне текста: `FakeSender` не видит `parse_mode`. Отсутствие `parse_mode` в `sendMessage` проверяют тесты `sender` (T-310).
5. **Ack при `503`:** выбран «срок на flush» через клиент ack с коротким таймаутом и одним повтором, без дедлайна на таймерах. Дедлайн на `clock.Timers` отменял бы flush в тестах с мгновенными таймерами и потребовал бы переписать тесты цикла. Клиент ограничивает и зависший шлюз: 6 с на попытку.

### Прогоны

| Команда | Результат |
|---|---|
| `go build ./... && go vet ./...` | 0 |
| `go vet -tags e2e ./cmd/telegram-bot/... ./test/e2e/...` | 0 |
| `go test -short -count=1 ./...` | exit 0, все пакеты `ok` (`internal/updates` на этот раз прошёл обычным `go test`, обход T-462 не понадобился) |
| `go test -tags e2e -count=1 ./test/e2e/...` | `ok` (10,7 с) |
| `go test -tags e2e -count=1 ./cmd/telegram-bot/...` | 11 пакетов `ok`, `e2e` — 1,7 с |
| `go test -tags e2e -count=10 ./cmd/telegram-bot/e2e/` | `ok` (9,1 с) — после исправления гонки в ожидании теста группы (см. ниже) |
| `go test -short -count=10 ./cmd/telegram-bot/internal/deliver/ ./cmd/telegram-bot/internal/flow/` | `ok` |
| `golangci-lint run ./...` | 0 issues |
| `make test` | exit 0 (без `-race`: нет cgo; порог покрытия пройден) |

Первый прогон `-count=10` поймал гонку в самом тесте группы. Ожидание «ещё один ответ» считалось до того, как записался отказ постороннему. Тест теперь ждёт ответа именно игроку из allowlist; после этого — 10 из 10.

`-race` локально недоступен (нет cgo, как в `make test`); `make test-race` в CI каталог `cmd/telegram-bot/e2e` не гоняет (`RACE_PKGS`) — предложение ниже.

### Мутанты

Копия дерева — scratch `t315-mut`, без `-overlay`; удалена по точному пути.

| # | Мутант | Результат |
|---|---|---|
| 0 | контрольный, без изменений: `e2e`, `deliver`, `access` | зелёный |
| 1 | `loop.go`: `early := false` — нет паузы после раннего пустого ответа | красный: `TestAGatewayThatAnswersEmptyAtOnceIsPolledOncePerPause`, `TestAnEmptyAnswerThatTookTheWaitIsNotPaused` |
| 2 | `loop.go`: `AckBackoff` — 3 повтора до 1,6 с | красный: `TestAnAckTheBrokerRefusesDoesNotHoldTheNextPollPastTheLease`, `TestTheAckClientEndsWithinTheLease` |
| 3 | `access/gate.go`: групповой чат пропускается как личный | красный: `TestBotIsSilentInAGroupAndRefusesAStrangerOnce` (ответ в группу, два `resolve`, `NotPrivate=0`) |

### Не сделано / вне объёма

- Порядок механики и нарратива внутри хода — поведение шлюза, не бота (см. предложения).
- `CLAUDE.md`, `ownership.md`, `contracts.md` не правил: строки для них — в «Открытых вопросах».

### Предложения в бэклог

1. **Шлюз (outbox/consumer):** нарратив хода может прийти игроку раньше механики того же хода. В e2e у `/attack` текст «vasya достаёт Альфа-волк…» в одном прогоне ушёл раньше «Попадание! Урон 1…». Порядок постановки зависит от того, какой топик consumer обработал первым. Вариант: в лизинг не отдавать нарратив, пока механика того же `correlation_id` не подтверждена; либо закрепить в C-08, что порядок между видами доставок не гарантирован.
2. **CI:** добавить `./cmd/telegram-bot/e2e/...` в `RACE_PKGS` job `race` (Makefile, EPIC-001): в e2e три горутины бота и шлюз в одном процессе.
3. **C-08:** закрепить темп клиента после раннего пустого ответа (у бота порог — половина `wait_ms`, пауза как после сетевой ошибки), если system-architect сочтёт это частью контракта (строка DoD это допускала).

### Открытые вопросы (оркестратору)

1. **system-architect / EPIC-001:** подтвердить место e2e бота (`cmd/telegram-bot/e2e/`) и правило `cmd-telegram-bot-e2e` в `.golangci.yml`. Поправить строку `test/e2e/**` в `ownership.md` (там `gateway_bot_test.go`) и закрыть вопрос 2 T-470.
2. Строка для `CLAUDE.md` (вносит оркестратор): e2e бота — `go test -tags e2e ./cmd/telegram-bot/e2e/`.

### Риски и допущения

- Golden-тексты боя зависят от таблицы `FixedMechanics` и последовательности идентификаторов `FakeGateway`/`testkit.Deterministic`. Смена двойников EPIC-002/003 или числа идентификаторов, которые шлюз берёт до `player.attacked`, поменяет цифры урона — golden придётся обновить осознанно.
- Каждый шаг ждёт свои ответы, доставки и ack всех выданных доставок, и только потом подаёт следующее обновление: так идентификаторы прогонов совпадают. Лишняя доставка шага, пришедшая позже, попадёт в следующий шаг и уронит сравнение с golden — это громкий отказ, не тихий.
- Время ожидания в тестах — настенное, до 10 с на шаг; обычный шаг занимает миллисекунды.
- Бюджет ack (2 × 12,2 с < 30 с) не учитывает время отправок в Telegram между выдачей и ack. Отправка под `429` с большим `retry_after` может сама превысить лизинг — это прежнее поведение T-312.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#3, TEAM-3) | 2026-09-14 | 0 | 1 | 2 | 5 | вернуть |
| 2 (code-reviewer#3, TEAM-3) | 2026-09-14 | 0 | 0 | 0 | 1 | принять |

### Ревью #1

Полная запись — `../review.md`, раздел «T-315 · ревью #1».

Зелёные прогоны: `build`/`vet` (с `-tags e2e`), `-short` бота и `api`, e2e `-count=3` (в копии `-count=10`), `golangci-lint` — 0 issues. Мутанты: контрольный зелёный, двойная отправка и запрещённые импорты красные; ack-клиент без таймеров и `serve.go` без шлюза доступа в e2e зелёные.

Major:
- **Ma-1.** После слияния с эпиком (T-309) красный `TestTheLoopIsDegradedAfterASeriesOfFailedPolls`: мгновенный пустой ответ двойника теперь ранний и не сбрасывает `FailedPolls`. Не решено, делает ли ранний пустой ответ `/health` бота `degraded`. Влить эпик в ветку, поправить тест T-309, решение закрепить тестом и godoc.

Minor:
- **Mi-1.** e2e собирает бота вручную, а не через `serve.go`; лимиты клиента `flow` скопированы литералами. Поправить формулировки, общую сборку — в бэклог.
- **Mi-2.** Таймеры `NewAckClient` не закреплены ни тестом проводки, ни тестом лизинга.

Nit:
- **N-1.** Нет проверки тишины после последнего шага.
- **N-2.** Замер раннего ответа включает `flush`.
- **N-3.** Порог «половина `wait_ms`» не закреплён.
- **N-4.** NFD в тесте `flow` — невидимый символ.
- **N-5.** Старт двойников в порядке обхода `map`.

### Ревью #2

Полная запись — `../review.md`, раздел «T-315 · ревью #2». Вердикт — **принять**: Critical 0 · Major 0 · Minor 0 · Nit 1.

Зелёные прогоны:
- `build`/`vet` (с `-tags e2e`);
- `-short` бота и шлюза — 26 пакетов, тесты `/health` из T-309;
- e2e `-count=6 -cpu=1,2,4`, а также `-count=3` и `-count=4 -cpu=1` под фоновой нагрузкой;
- `golangci-lint --build-tags e2e` — 0 issues.

Мутанты:
- контрольный — зелёный;
- запоздавший лишний ответ после последнего шага — красный от окна тишины; без барьера и окна тот же мутант зелёный;
- `serve.go` отдаёт клиенту ack `nil` вместо таймеров — красный в тесте проводки.

- **Ma-1 закрыто.** У настоящего шлюза пустой ответ раньше `wait_ms` бывает только по `Stop` (и при `wait_ms=0`, которого бот не шлёт). Срок идёт по настенным таймерам, бот мерит на `clock.Real`, поэтому ложного `degraded` при штатной работе нет.
- **Mi-1, Mi-2, N-1…N-5 закрыты.** Зазор 20,4 с на `clock.Manual` точный и детерминированный.

Nit:
- **N-1 (ревью #2).** Методы `scriptedGateway` в `loop_test.go` разорваны `runScripted`; не блокирует.

Бэклог: тест `outbox.MaxWait == client.MaxPollWait` — порог бота завязан на срок шлюза.

## Итерация 2 (developer)

**developer#2 · 2026-09-14, до 09:37.** Ветка `task/T-315-bot-e2e-fakes`: итерация 1 (wip c9f84b1) плюс кончик эпика с T-309 (7f0ff6b). Не коммитил. Ответ на ревью #1 и решения оркестратора.

### Ответы по замечаниям

| # | Решение | Что сделано |
|---|---|---|
| Ma-1 | исправлено | Ранний пустой ответ растит `FailedPolls` (решение оркестратора), порог `/health degraded` — прежний `DegradedAfterFailedPolls = 7`. В тесте T-309 у двойника `scriptedGateway` появились ручные часы: `nil` — пустой ответ по истечении `wait` (часы сдвигаются на `wait`), `errEarlyEmpty` — пустой ответ сразу. Цикл `runScripted` работает на этих часах; `TestTheLoopIsDegradedAfterASeriesOfFailedPolls` снова зелёный. Новые тесты: `TestASeriesOfEarlyEmptyAnswersDegradesTheLoop` — шесть ранних пустых ответов ещё не дают degraded, седьмой даёт, штатный пустой ответ снимает; `TestAnEmptyAnswerAtTheEndOfTheWaitResetsTheFailedPolls` — смешанные серии (ошибки и ранние пустые) сбрасываются штатным пустым ответом, следующий сбой считается с единицы. Поведение закреплено в godoc `EarlyEmpty`, `DegradedAfterFailedPolls`, `Health.Degraded` и `Run`, а также в README бота: раздел `/health` (поля `deliveries_failed_polls`, `telegram_token_unauthorized`, условие `degraded`) и строка long-poll таблицы клиентов. |
| Mi-1 | исправлено | Godoc `bot` в `harness_test.go`: «собран по образцу `serve.go` из тех же компонентов, проводку `serve.go` проверяет `main_test.go`». Отличия от `serve.go` перечислены в godoc `startBot`: нет `getMe`/`OnReady`, один записывающий отправитель на шлюз доступа и `flow`, копия лимитов `flow`, часы, нет `/health`/`privacy`/сигналов. Лимиты клиента `flow` (`FlowGatewayTimeout`, `FlowGatewayBackoff`) не экспортированы — это поля `productionLimits` пакета `main`, импортировать их нельзя. Поэтому копия осталась, с комментарием, где оригинал; границы держит `TestTheProductionLimits`. Лимит команд в минуту теперь берётся из умолчания манифеста (`env.TelegramCommandsPerMin`), без литерала. Фраза «как в `serve.go`» в записи итерации 1 `dev-log.md` исправлена на «по образцу `serve.go`». Вынос сборки в `cmd/telegram-bot/internal/app` не делал — пункт бэклога ниже. |
| Mi-2 | исправлено | Тест проводки сверяет `ackGW.Timers` с `e.timers` процесса. `TestAnAckTheBrokerRefusesDoesNotHoldTheNextPollPastTheLease` сверяет точный зазор на `clock.Manual`: 2 × (2 попытки × 5 с + `AckBackoff.Pause(0)`) = 20,4 с, без допуска. `TestTheAckClientEndsWithinTheLease` проверяет, что клиент берёт переданные таймеры, а без них идёт на настенных часах, и что `MaxFlush` в точности равен сумме попыток и пауз. Мутант «`NewAckClient` не берёт таймеры» красный — см. «Мутанты». |
| N-1 | исправлено | В конце сценария шаг-барьер `/help`: ровно один ответ (уведомление с клавиатурой согласия — после `/forget` связки нет), ноль доставок. После него окно тишины 200 мс: новых ответов и доставок нет, все выданные доставки подтверждены. |
| N-2 | исправлено | `once` возвращает длительность самого long-poll; `began` берётся после `flush`. Тест `TestASlowAckBeforeTheLongPollDoesNotHideAnEarlyEmptyAnswer`: ack длиннее `EarlyEmpty` перед мгновенным пустым ответом не прячет паузу. |
| N-3 | исправлено | В `TestAnEmptyAnswerThatTookTheWaitIsNotPaused` — проверка `EarlyEmpty == Wait/2` и границы от `Wait`: `Wait`, `Wait − 1 с`, `Wait/2` — без паузы; `Wait/2 − 1 мс`, `1 с` — с паузой. |
| N-4 | исправлено | NFD в `flow/onboarding_test.go` записан escape-последовательностью `"\u0438\u0306\u0432\u0430"`, байтов `U+0306` в файле нет. |
| N-5 | исправлено | Двойники State, встречи и нарратора стартуют из среза в фиксированном порядке. |
| Место e2e и правило `cmd-telegram-bot-e2e` | не менял | Вопрос system-architect и tech-lead#1 (решение оркестратора). |

### Изменённые файлы (итерация 2)

- `cmd/telegram-bot/internal/deliver/loop.go` — замер long-poll после `flush`, godoc `EarlyEmpty`, `DegradedAfterFailedPolls`, `Health.Degraded`, `Run`;
- `cmd/telegram-bot/internal/deliver/loop_test.go` — `scriptedGateway` с часами и режимом раннего пустого ответа, `runScripted`, два новых теста;
- `cmd/telegram-bot/internal/deliver/pace_test.go` — границы от `Wait`, медленный ack перед опросом, точный зазор и таймеры клиента ack;
- `cmd/telegram-bot/main_test.go` — таймеры клиента ack в проводке;
- `cmd/telegram-bot/e2e/harness_test.go` — godoc `bot`/`startBot`, лимит команд из манифеста, фиксированный порядок старта двойников;
- `cmd/telegram-bot/e2e/scenario_test.go` — шаг-барьер `/help` и окно тишины;
- `cmd/telegram-bot/internal/flow/onboarding_test.go` — escape NFD;
- `cmd/telegram-bot/README.md` — раздел `/health`, строка long-poll;
- `Docs/.../dev-log.md` — формулировка в записи итерации 1 и запись итерации 2; карточка — этот раздел.

### Прогоны

| Команда | Результат |
|---|---|
| `go build ./... && go vet ./... && go vet -tags e2e ./cmd/telegram-bot/...` | 0 |
| `go test -short -count=1 ./cmd/telegram-bot/... ./internal/gateway/...` | 10 пакетов бота и 16 пакетов шлюза `ok` |
| `go test -tags e2e -count=5 ./cmd/telegram-bot/e2e/...` | `ok` (8,7 с) |
| `golangci-lint run --build-tags e2e ./...` | 0 issues |
| `make test` | exit 0 (без `-race`: нет cgo; порог покрытия пройден) |

### Мутанты

Копии дерева в scratch с префиксом `t315i2-`, без `-overlay`, контрольный первым; каждая копия удалена по точному пути.

| # | Мутант | Результат |
|---|---|---|
| 0 | контрольный, без изменений: `deliver`, пакет `cmd/telegram-bot` | зелёный |
| 1 | `NewAckClient` не берёт переданные таймеры | красный: `TestAnAckTheBrokerRefusesDoesNotHoldTheNextPollPastTheLease`, `TestTheAckClientEndsWithinTheLease`, `TestTheWiringHandsOutThePrivacyLoggerAndTheRightClients` |
| 2 | ранний пустой ответ не растит `FailedPolls` | красный: `TestASeriesOfEarlyEmptyAnswersDegradesTheLoop`, `TestAnEmptyAnswerAtTheEndOfTheWaitResetsTheFailedPolls` |
| 3 | замер long-poll начинается до `flush` | красный: `TestASlowAckBeforeTheLongPollDoesNotHideAnEarlyEmptyAnswer` |

### Предложения в бэклог (итерация 2)

1. **Общая сборка бота** — вынести `build` из `serve.go` в `cmd/telegram-bot/internal/app` с внедряемыми `updates.Source` и `sender.Sender`, чтобы e2e и `serve.go` делили один код сборки: отличия из godoc `startBot` уйдут, мутант «без `gate.Wrap`» станет виден в e2e (Mi-1, решение оркестратора).
2. Прежние предложения итерации 1 в силе: `RACE_PKGS` для e2e бота; порядок механики и нарратива хода в outbox или запись в C-08; темп клиента после раннего пустого ответа в C-08.

### Риски и допущения (итерация 2)

- Ранний пустой ответ и неудачный опрос считаются одним счётчиком. Если шлюз при остановке отвечает пустым дольше ~61 с, `/health` бота станет `degraded` без ошибки в логе (есть строка `deliveries answered empty before the wait`). Так решил оркестратор; README это описывает.
- Окно тишины в e2e — 200 мс настенного времени. Опоздание дольше окна им не ловится, но шаг-барьер ловит любое опоздание, которое пришло раньше ответа на `/help`.

## Просмотр system-architect

**system-architect#2 · 2026-09-14, Opus.** Вопросы оркестратора 1–3. Основание:
- «Решения architect#3» в карточке T-313, вопросы 4–6 (`.worktrees/T-313`, только чтение);
- ветка `task/T-315-bot-e2e-fakes`: `.golangci.yml`, `Makefile`, `internal/gateway/{consumer,outbox,handlers}`, `shared/testkit/gateway/http.go`.

Go-код, `.golangci.yml` и `Makefile` не менялись. Не коммитил.

### 1. Место e2e бота — подтверждено: `cmd/telegram-bot/e2e/`
- **Почему.** Из `test/e2e` пакеты `cmd/telegram-bot/internal/*` не импортируются. Вынос пакетов бота из `internal` ослабил бы границу ADR-001 (доп. 2026-09-13, граница 1e) ради теста. C-08 «Заглушка» требует от e2e с `FakeGateway` одного: жить вне `internal/<контекст>`, и это выполнено. Каталог уже принадлежит EPIC-004 (`cmd/telegram-bot/**`), новой строки владения не нужно. Вопрос 2 T-470 закрыт.
- **Правило `cmd-telegram-bot-e2e` проверено. Оно узкое:**
  - файлы — только `**/cmd/telegram-bot/e2e/**_test.go`, и только они исключены из `cmd-telegram-bot`. Не-тестовый файл, если он появится в `e2e/`, останется под `cmd-telegram-bot` (fail-closed);
  - `allow` — три пакета с `$` (`client`, `api`, `gatewaytest`), префиксных записей нет, поэтому затенения по порядку сортировки нет; `deny` — весь `internal`. Форма — как у `shared-testkit-gateway` и `internal-swarm-tests`;
  - для этих файлов снят только запрет `shared/eventbus`: он нужен ради `membus`. Production бота и прочие его тесты запрет сохраняют;
  - `no-testkit-in-production` не ослаблен. Его `files` правка не трогала, а для файлов e2e он снимается прежним исключением `_test.go` по тексту сообщения, как для любого теста. `cmd-others` (`internal/replay`) и `internal-unlisted` не тронуты;
  - правило работает в `make lint` и CI: `run.build-tags` включает `e2e`. `golangci-lint run ./cmd/telegram-bot/...` на ветке — 0 issues; мутанты запрещённых импортов красные (ревью #1).
- **`ownership.md`** — правка внесена: в строке `test/e2e/**` текст п. 6 (б) карточки T-313. Шапку и версию файла не менял, чтобы не создавать конфликт с другими ветками.
- **`RACE_PKGS` — не в этой ветке и не в `RACE_PKGS`.** `go list ./cmd/telegram-bot/e2e/...` без тега даёт `matched no packages`: в каталоге только файлы с `//go:build e2e`. `go test -race $(RACE_PKGS)` с этим каталогом молча ничего бы не проверил. Правильное место — вторая команда цели `test-race` (с `-tags e2e`): `./test/e2e/... ./cmd/telegram-bot/e2e/...`, и в `echo`, и в `go test`. Это пункт для devops-engineer (EPIC-001): там же меняются строки `infrastructure.md` §3 про `make test-race` и задание `race`, которые называют ровно `./test/e2e/...`. Проверить правку можно только в CI-задании `race`: локально нет cgo. До этой правки e2e бота уже идёт без детектора в CI-задании `e2e` и в `make test-e2e` (`./...` с тегом). Приёмку T-315 это не блокирует.
- **`CLAUDE.md`** — строку вносит оркестратор: e2e бота — `go test -tags e2e ./cmd/telegram-bot/e2e/`.

### 2. C-08, порядок доставок — принято, C-08 v1.5a
- **Проверено по коду.** `consumer.Dispatcher` подписан на четыре топика независимо. `consumer/deliveries.go` ставит доставки в транзакции события. `outbox.Store.Lease` выдаёт очередь игрока по `seq`, по одной в лизинге. Порядок внутри топика сохраняется (C-01), между топиками — нет.
- **Внесено сверх рекомендации.** Явно сказано, что доставки из событий одного топика идут в порядке топика. На это опирается C-05 п. 8 (порядок `narrative_output`); без оговорки уточнение читалось бы как отмена п. 8. Добавлены «Почему не удерживать» и «Когда пересмотреть»: T-391/T-392, удержание в `Lease` по предикату Phase 1.
- **T-315.** Сравнение доставок шага набором (отклонение 2) соответствует контракту. Пунктов к коду нет.

### 3. C-08, темп клиента — принято, C-08 v1.5a
- **Проверено по коду.** `outbox.Service.Serve` отвечает пустым списком раньше `wait` в двух случаях: по `Stop` (`runtime.ShuttingDown`) или когда клиент сам просит `wait_ms=0`. Сброс доставок без маршрута ожидание не прерывает. Значит, «ранний пустой ответ — остановка шлюза» верно.
- **Внесено.**
  - Обязанность клиента: не повторять опрос сразу, а сделать паузу с нарастанием или прекратить опрос. Второй вариант нужен тестовым клиентам.
  - Пустой ответ по истечении `wait_ms` паузы не требует.
  - Порог и длины пауз — реализация клиента.
  - `Retry-After` — бэклог.
  - Счётчик `FailedPolls` и `/health degraded` бота — реализация бота, в контракт не вносятся.
- **Найдено расхождение.** `shared/testkit/gateway.HTTPHarness.await` после пустого ответа повторяет опрос сразу. В C-08 это помечено «Код расходится», исправление — бэклог EPIC-004. Сценарии T-314 рестартуют шлюз в «осевшей» точке, поэтому сейчас дефект не проявляется.
- **T-315.** Бот (`deliver.EarlyEmpty = Wait/2`, паузы 1 … 30 с) контракту соответствует. Пунктов к коду нет.

### Версия `contracts.md` и конфликт номера
- Шапка — «Версия 0.19», запись v0.19 добавлена в строку версий. Номер — следующий свободный после v0.17 (T-472) и v0.18 (T-059); конфликт номеров разрешает оркестратор.
- В разделе C-08 изменены строка «Тип», «Гарантии» и «История изменений», добавлен подраздел «Дополнения v1.5a».
- Сводку «## Изменения v0.19» не добавлял: по условию меняются только C-08 и строка версий. Если сводка нужна, её содержание — подраздел v1.5a.
- Раздел C-08 на develop, в `epic/EPIC-004-gateway-bot`, в T-472 и в T-059 одинаков (сверено по хешу раздела). Конфликт ожидается только в строке 3 — с записью v0.17 из T-472.

### Изменённые файлы (system-architect#2)
- `Docs/dev-team/architecture/contracts.md` — строка 3 и раздел C-08;
- `Docs/dev-team/plan/ownership.md` — одна фраза в строке `test/e2e/**`;
- карточка — этот раздел; `dev-log.md` — запись в конце.

### Предложения в бэклог
1. **devops-engineer, EPIC-001:** e2e бота во второй команде `test-race` (`./test/e2e/... ./cmd/telegram-bot/e2e/...`) и строки `infrastructure.md` §3; проверка — CI-задание `race`.
2. **EPIC-004:** `HTTPHarness.await` после пустого ответа раньше ожидания делает паузу или завершает ожидание ошибкой (C-08 v1.5a); unit на шлюзе в остановке.
3. **EPIC-004, не сейчас:** `Retry-After` на раннем пустом ответе long-poll — совместимая альтернатива эвристике порога.
4. **EPIC-004, по условию:** удержание нарратива до механики хода в `Lease` — только если инверсия мешает игроку на T-391/T-392.

## Отметка владельца EPIC-001 (по назначению оркестратора, tech-lead#3)

**tech-lead#3 · 2026-09-14, Opus.** Отметку tech-lead#1 по `.golangci.yml` ставлю по назначению оркестратора. Границу правила уже проверил system-architect#2 (раздел выше). Здесь — взгляд владельца файла: стиль и согласованность с остальными правилами. Правка файла в ветке (дифф от `6b67fec`) — правило depguard `cmd-telegram-bot-e2e` и одна строка исключения в `cmd-telegram-bot`, других изменений нет.

**Решение: одобрено.**
- **Форма.** Пара «правило кода / правило тестов», как `internal-swarm-tests`: отдельный ключ, `list-mode: lax`, в `files` — только `_test.go` одного каталога. В `allow` три пакета с `$` и ни одной префиксной записи, поэтому затенения по порядку нет. В `deny` — весь `multiverse-core.io/internal`, у записи есть `desc` со ссылкой на ADR-001 (граница 1e) и номер задачи.
- **Комментарий** над правилом написан в стиле файла: по-английски, объясняет «почему» (пакеты бота лежат в его `internal`, из `test/e2e` не импортируются) и что остаётся под `cmd-telegram-bot`.
- **Исключение** в `cmd-telegram-bot` узкое: `!**/cmd/telegram-bot/e2e/**_test.go`. Не-тестовый файл, если он появится в `e2e/`, остаётся под правилом кода. `internal-unlisted`, `no-testkit-in-production` и `cmd-others` не тронуты.
- **Действует в `make lint` и CI.** В `run.build-tags` уже есть `e2e`. `golangci-lint run --build-tags e2e ./...` на ветке — `0 issues.`
- **Nit, не блокирует (§8 З-32).** Комментарий над `cmd-telegram-bot` всё ещё говорит о будущем: «a test that needs internal/gateway/gatewaytest or membus gets a rule of its own, like internal-swarm-tests». При следующей правке файла назвать там `cmd-telegram-bot-e2e`.

## Приёмка (tech-lead)

**Решение:** принято, статус `done`. **Дата:** 2026-09-14. **Кто:** tech-lead#3 (TEAM-3), Opus.
**Итог:** e2e бота на `updates.Fake`, записывающих отправителях и `FakeGateway` проходит сценарий от `/start` до `/forget confirm` и ветки SEC-06/07/10, `429`/`503`; пауза после раннего пустого ответа и ack при `503` в пределах лизинга закрыты unit-тестами на ручных часах — DoD закрыт построчно, замечания ревью #1 закрыты, ревью #2 — «принять».

### Прогоны приёмки
Рабочая папка `.worktrees/T-315`, go1.26.8 windows/amd64, golangci-lint 2.13.2, без `-race` (нет cgo). Дерево — wip-коммит итерации 1 (c9f84b1), слияние с кончиком эпика (7f0ff6b) и незакоммиченные правки итерации 2 и system-architect#2.
- `go build ./... && go vet ./... && go vet -tags e2e ./cmd/telegram-bot/...` — 0.
- `go test -short -count=1 ./cmd/telegram-bot/... ./internal/gateway/...` — 10 пакетов бота и 16 пакетов шлюза `ok`. Пакет `updates` прошёл обычным `go test`, обход T-462 не понадобился.
- `go test -tags e2e -count=3 -v ./cmd/telegram-bot/e2e/...` — `ok` (4,9 с). Четыре теста зелёные во всех трёх прогонах; внутри `TestBotScenarioFromStartToForget` сценарий ещё трижды сравнивает записи.
- `golangci-lint run --build-tags e2e ./...` — `0 issues.`
- `make test` — exit 0: 58 пакетов `ok`, coverage-gate зелёный.
- `git diff --name-only 6b67fec` и `git status`. Изменены пути EPIC-004 (`cmd/telegram-bot/**`, `internal/gateway/api/names_test.go`, документы эпика), `.golangci.yml` (EPIC-001, отметка выше), а также `contracts.md` и `ownership.md` — правки system-architect#2 по назначению оркестратора. `shared/*`, `go.mod`/`go.sum`, схемы, `shared/env/vars.go` и `.env.example` не менялись.

Мутанты при приёмке не запускал. Опираюсь на мутанты исполнителя (итерации 1 и 2) и ревью #1/#2: контрольные зелёные, остальные красные. Их тексты сверены с кодом ниже.

### Сверка DoD (индекс, раздел «T-315»)
| Пункт | Чем подтверждено | Итог |
|---|---|---|
| Golden-тексты, клавиатуры, порядок, ack каждой доставки, стабильный `action_key` при повторе `update_id` | `scenario_test.go`: `script`, `runScenario`, `checkActionKeys`. Ответы — в точном порядке. Отправленные доставки совпадают с выданными шлюзом по порядку. Каждая выданная подтверждена ровно один раз, ни одна не выдана дважды. Повтор обновления `/say` — два запроса с одним ключом и один `player.said` | выполнено; доставки внутри шага сравниваются набором — ниже, «Отступления» |
| SEC-07: группа — нет ответа и вызова шлюза; SEC-06: посторонний — один отказ на серию, шлюз не вызван | `TestBotIsSilentInAGroupAndRefusesAStrangerOnce`: `Denied=3`, `NotPrivate=2`, один `resolve` — у игрока, писавшего после них; мутант «группа как личный чат» красный | выполнено |
| SEC-10: нарратив с `[x](http://…)` и `<a>` приходит буквально | `TestBotDeliversTheMarkupOfANarrativeAsText` — текст символ в символ с пометкой шаблона | выполнено на уровне текста; отсутствие `parse_mode` в `sendMessage` — тесты `sender` (T-310) |
| `429 rate_limited` и `503 bus_unavailable` → подсказка, команда не теряется, тот же `action_key` | `TestBotKeepsTheCommandOnRateLimitAndAnUnavailableBus`. `429` — настоящий лимит шлюза, повтор через минуту принят с тем же ключом. Один `503` — клиент `flow` повторяет сам. Два `503` — `render.Unavailable`, повтор обновления уходит с тем же ключом. На четыре обновления ровно четыре `player.said` (проверено по коду) | выполнено; `503` — от прокси перед шлюзом |
| Приёмка T-307, доп. 2: пауза после пустого ответа раньше `wait_ms`, порог — константа, unit на `clock.Timers` | `deliver.EarlyEmpty = Wait/2`; `pace_test.go`: `TestAGatewayThatAnswersEmptyAtOnceIsPolledOncePerPause`, `TestAnEmptyAnswerThatTookTheWaitIsNotPaused` (границы `Wait`, `Wait/2`, `Wait/2 − 1 мс`), `TestASlowAckBeforeTheLongPollDoesNotHideAnEarlyEmptyAnswer`. Замер long-poll идёт после `flush` (дифф `loop.go` сверен) | выполнено; соответствует C-08 v1.5a (порог и паузы — выбор клиента) |
| Приёмка T-307, ревью #1: повтор ack при `503` не держит цикл дольше лизинга 30 с | `NewAckClient` (6 с, 1 повтор 200 мс), `MaxFlush` 12,2 с. `TestAnAckTheBrokerRefusesDoesNotHoldTheNextPollPastTheLease` — точный зазор 20,4 с на `clock.Manual`; `TestTheAckClientEndsWithinTheLease`; проводка — `TestTheWiringHandsOutThePrivacyLoggerAndTheRightClients` (таймеры клиента ack = таймеры процесса) | выполнено |
| 3 прогона подряд — одинаковая запись `FakeSender` | `scenarioRuns = 3`, записи сравниваются `reflect.DeepEqual`; прогон приёмки `-count=3` зелёный; ревью #2 — `-count=6 -cpu=1,2,4` и под нагрузкой | выполнено |
| Приёмка T-312: `flow.ValidName` → `api.ValidCharacterName`, копия регулярки и `TestValidName` удалены, случаи перенесены, обрезка пробелов по краям | `flow/onboarding.go:174`; `TestTheNameIsCheckedByTheRuleOfTheGateway` (два пробела, NFD через `\u`, `Вася!`, `В` → `NameInvalid` без вызова шлюза; `"  Вася Пупкин \t"` уходит как `Вася Пупкин`); `internal/gateway/api/names_test.go` — перенесённые случаи | выполнено |
| Строки T-310…T-312, на которые опирается e2e | SEC-07 — молчание (приёмка T-310); `access.Gate` до шлюза и отдельный best effort отправитель — в `serve.go` (T-312), в e2e один записывающий отправитель на шлюз доступа и `flow` (перечислено в godoc `startBot`); разные сообщения механики и нарратива (T-312) — видно в golden `/attack`; ack пакетом и отсутствие перестановок (T-312) — сверка `sent == given` | выполнено |
| DoD-common | тесты, `vet` (с `-tags e2e`), линтер, `make test` зелёные. Время — `shared/clock`: ожидания e2e на `clock.RealTimers`, `forbidigo` чист. Пути — выше. Новых переменных и зависимостей нет: лимит команд — из умолчания манифеста `env.TelegramCommandsPerMin`. `dev-log.md` — две записи developer#2 и запись system-architect#2. В новых строках лога (`deliveries answered empty before the wait`) внешних ID нет | выполнено |

### Замечания ревью #1 (0/1/2/5) — проверка по коду
| # | Проверка при приёмке | Итог |
|---|---|---|
| Ma-1 | `loop.go`: `early` учитывается как сбой — растит `FailedPolls` и паузу. Godoc `EarlyEmpty`, `DegradedAfterFailedPolls`, `Health.Degraded`, `Run` и README бота (раздел `/health`) говорят одно. Тест T-309 `TestTheLoopIsDegradedAfterASeriesOfFailedPolls` снова зелёный | закрыто |
| Mi-1 | Godoc `bot`/`startBot` перечисляет отличия от `serve.go`. Копия `FlowGatewayTimeout`/`FlowGatewayBackoff` совпадает с `productionLimits` (`serve.go:63-64`). Лимит команд берётся из манифеста. Общая сборка — §8 З-26 | закрыто, остаток — бэклог |
| Mi-2 | `main_test.go`: `ackGW.Timers != e.timers` → ошибка; зазор 20,4 с без допуска | закрыто |
| N-1 | Шаг-барьер `/help` и окно тишины 200 мс; мутант «запоздалый ответ» красный, без барьера — зелёный (ревью #2) | закрыто |
| N-2…N-5 | Замер после `flush`, `EarlyEmpty == Wait/2`, NFD через `\u`, двойники стартуют из среза | закрыто |
| N-1 ревью #2 | порядок методов `scriptedGateway` в `loop_test.go` | не блокирует, §8 З-32 |

### Отступления, принятые при приёмке
1. **Место e2e — `cmd/telegram-bot/e2e/`.** Индекс и `ownership.md` называли `test/e2e/gateway_bot_test.go`. Из `test/e2e` пакеты `cmd/telegram-bot/internal/*` не импортируются. Подтвердил system-architect#2, `ownership.md` поправлен в ветке, правило линтера отмечено выше.
2. **Доставки одного шага сравниваются набором.** C-08 v1.5a: доставки из разных топиков между собой не упорядочены. То, что бот ничего не переставляет, проверено сверкой отправленного с выданным.
3. **`503 bus_unavailable` подставляет прокси перед шлюзом:** у `FakeGateway` нет способа уронить публикацию на `membus`. `429` — настоящий лимит шлюза.
4. **Ранний пустой ответ считается неудачным опросом для `/health`** (решение оркестратора по Ma-1). Остановка шлюза дольше ~61 с даёт `degraded` без ошибки в логе, но со строкой `deliveries answered empty before the wait`. Это описано в README бота.
5. **Ack при `503` — срок на flush через отдельный клиент ack**, без дедлайна на таймерах. Бюджет 2 × 12,2 с < 30 с не учитывает время отправок в Telegram между выдачей и ack. Это прежнее поведение T-312, риск записан исполнителем.

### Слияние
- Кончик `epic/EPIC-004-gateway-bot` — `6b67fec` (T-309), он уже влит в ветку задачи (7f0ff6b). После него эпик не менялся: `git log --oneline -3 epic/EPIC-004-gateway-bot` → `6b67fec`, `3c155ff`, `ff0d7c6`. Пробный `merge-file` не нужен: изменения ветки относительно эпика — только правки этой задачи.
- В `tasks.md` ветки версия 0.1.13 идёт после 0.1.12 эпика. §8 — З-26…З-32. **З-25 не занят в индексе:** его зарезервировала отметка tech-lead#3 по T-472 (EPIC-002, `readmodel.opFor`), строка придёт своим путём.
- `contracts.md`: в ветке C-08 v1.5a с шапкой «Версия 0.19». Номер — по порядку слияний.
- Имя ветки в индексе было `task/T-315-e2e-bot`, приведено к фактическому `task/T-315-bot-e2e-fakes`.

### Передать
- **Оркестратору, `CLAUDE.md`** (при контрольном слиянии в `develop`, раздел «Команды сборки/линта/теста» → «Тесты»): `go test -tags e2e ./cmd/telegram-bot/e2e/`  # e2e бота: бот на `updates.Fake` и записывающих отправителях против `FakeGateway` in-process, `membus`.
- **devops-engineer через tech-lead#1 (EPIC-001):** e2e бота во второй команде `test-race` — §8 З-31. В T-315 не делается.
- **tech-writer / devops (З-24):** раздел `/health` README бота обновлён в T-315. Для runbook §6 остаётся строка про `deliveries_failed_polls` и ранний пустой ответ.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка (developer#2) | 14.09 03:12 | 14.09 03:46 | 34m |
| Ревью #1 (code-reviewer#3, с простоем сессии) | 14.09 03:46 | 14.09 09:37 | 351m |
| Итерация 2, ревью #2, просмотр system-architect#2 | 14.09 09:37 | 14.09 10:04 | 27m |
| Приёмка (tech-lead#3) | 14.09 10:04 | 14.09 10:11 | 7m |
| **Всего** | | | **419m** |
