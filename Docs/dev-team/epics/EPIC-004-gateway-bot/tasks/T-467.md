# T-467: Стабилизация флака `shared/testkit/gateway` `TestTheSkirmishStopsSwingingWhenTheFightEnds` под нагрузкой полного `./...`

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I1 / вне подволн |
| Размер | S |
| Исполнитель | по назначению оркестратора (developer TEAM-3) |
| Зависит от | — |
| Ветка | `task/T-467-skirmish-flake` (папка `.worktrees/T-467`, от `epic/EPIC-004-gateway-bot`) |
| Статус | todo |
| Итераций ревью | 0 |
| Начата / завершена | — / — |
| Метка | параллельно любой задаче, которая не правит `shared/testkit/gateway/**` |

## Описание и DoD
Главный источник — раздел «### T-467» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- Под нагрузкой полного `go test ./...` (`make test`) тест падает с «1 proposals were refused»: в `system_events` есть отказ предложения сценария. В одиночном прогоне тест зелёный. Падения — при фиксации T-303 и при приёмке T-208 (журнал 2026-09-13).
- Найти причину: какое предложение, код отказа, порядок событий под нагрузкой. Устранить её в харнессе или тесте без ожиданий в настенном времени, повторов и `t.Skip`.
- Проверка: `go test -count=30 -cpu=1,4 -run TestTheSkirmish ./shared/testkit/gateway/` под параллельной нагрузкой и три прогона `make test` подряд.

## Выполнение (developer)

## Ревью (code-reviewer)

## Приёмка (tech-lead)

## Учёт времени (оркестратор)
