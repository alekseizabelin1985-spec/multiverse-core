# T-319: Перенос `services/game-service` → `services/_archive/game-service/`

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I1 / после T-392 |
| Размер | S |
| Исполнитель | developer#2 |
| Зависит от | T-392 |
| Ветка | `task/T-319-archive-game-service` (папка `.worktrees/T-319`, от `epic/EPIC-004-gateway-bot`) |
| Статус | todo |
| Итераций ревью | 0 |
| Начата / завершена | — / — |
| Метка | правка compose/Makefile — согласование tech-lead#1 и devops |

## Описание и DoD
Главный источник — раздел «### T-319» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- `git mv`, `ARCHIVED.md`, строка в `services/_archive/README.md`; код не удаляется.
- `go.work` нет, а у `game-service` свой `go.mod`, поэтому корневой `go.mod` не меняется; после переноса `go build ./...` и `make ci` зелёные.

## Выполнение (developer)

## Ревью (code-reviewer)

## Приёмка (tech-lead)

## Учёт времени (оркестратор)
