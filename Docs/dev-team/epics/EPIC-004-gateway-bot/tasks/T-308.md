# T-308: `shared/testkit/gateway` — `FakeGateway` и HTTP-обвязка рядом с `Harness` v0

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I1 / 1.8 |
| Размер | M |
| Исполнитель | developer#1 |
| Зависит от | T-303, T-304, T-305, T-306, T-307 |
| Ветка | `task/T-308-fake-gateway-harness` (папка `.worktrees/T-308`, от `epic/EPIC-004-gateway-bot`) *(приёмка: заменено «`task/T-308-testkit-gateway`» — по фактической ветке)* |
| Статус | done |
| Итераций ревью | 2 (итерация 3 — без ревью, приёмка tech-lead#3 по решению оркестратора) |
| Начата / завершена | 2026-09-14 / 2026-09-14 |
| Метка | поставка в `develop` контрольным слиянием; место `FakeGateway` и исключение depguard — решение system-architect; в задаче принята правка кода шлюза вне объёма (`actions`, `turns`: запись хода до публикации, `actions.Turns.Withdrawn`/`Acked`) — решение оркестратора |

## Описание и DoD
Главный источник — раздел «### T-308» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- API `Harness` v0 (`NewHarness`, `CreatePlayer`, `Enter`, `Attack`, `Flee`…) не меняется: им пользуются EPIC-002/003 и стенд T-400.
- `FakeGateway` (in-process HTTP поверх настоящего `internal/gateway` на `membus`) и HTTP-обвязка (`RegisterAndEnter`, `Act`, `AwaitDelivery`, `CloseRound`-заглушка) на `internal/gateway/client`.
- Правило depguard `shared` запрещает `shared/testkit` импортировать `internal/*`; как это решить, определяет system-architect.

## Выполнение (developer)

developer#1 (TEAM-3), 2026-09-14. Ветка `task/T-308-fake-gateway-harness` (в заголовке карточки — `task/T-308-testkit-gateway`; фактическое имя дал оркестратор), папка `.worktrees/T-308`, от `epic/EPIC-004-gateway-bot` `599e883`. Не коммитил.

**Раскладка — по решению system-architect, уже принятому в ADR-001 доп. 2026-09-13 п. 5 и C-08 v1.4:** `FakeGateway` — `internal/gateway/gatewaytest`; HTTP-обвязка — `shared/testkit/gateway`, видит ровно `internal/gateway/client$` и `api$` (правило `shared-testkit-gateway` уже есть в `.golangci.yml`, T-445). `.golangci.yml` не менялся, `//nolint` нет.

**Что сделано**
- `internal/gateway/gatewaytest/gatewaytest.go` — `Start(Config) (*FakeGateway, error)`: настоящий `internal/gateway` за процессным HTTP-сервером `shared/runtime.HTTP` на `127.0.0.1:0` (маршруты → `Start` контекста → сервер, как `serve`). Умолчания: своя `membus` по всем топикам реестра, `clock.Manual` от `gatewaytest.Epoch`, id `eventbus.SequenceIDs("gw")` (`player-gw-N`), режим `live`, временный каталог данных, переменные — умолчания манифеста + `Config.Vars` (окружение процесса не читается, `MV_GATEWAY_DATA_DIR` — всегда `Dir`). `Close` идемпотентен: сервер (висящий long-poll отвечает сразу, C-01 v1.8) → контекст → шина и каталог, только если созданы им самим. Неудачный `Start` освобождает всё созданное. Методы и поля: `URL`, `Dir`, `Bus()`, `Clock()`, `Health()`, `Client(id)`. Импортов `shared/testkit` нет: пакет — не тестовый файл и подпадает под `no-testkit-in-production`.
- `shared/testkit/gateway/http.go` — `HTTPHarness` рядом с `Harness` v0: `NewHTTPHarness(*client.Client, fixtures)`; `NewCIClient(url)` (`ci-harness`, `X-Actor-Kind: ci`, без повторов); `RegisterAndEnter(ctx, fixture, region) (Entered{PlayerID, CorrelationID}, error)` — resolve → consent → `POST /v1/characters` → `enter` → ожидание `alive` в регионе по `GET /v1/players/{id}`; `Act(ctx, player, req) (correlation_id, error)` — пустой `action_key` заполняется `ci-N`; `AwaitDelivery(ctx, kind, timeout)` и `AwaitDeliveryOf(ctx, corr, kind, timeout)` — long-poll от последнего `cursor`, ack каждой выданной доставки, остальные виды ждут во «входящих», таймаут по настенным часам → `ErrNoDelivery`, запрос не просит шлюз ждать дольше остатка; `CloseRound` — голый `404` немонтированного маршрута I1 → `ErrRoundsNotMounted` (`501 not_implemented`), любой другой ответ, включая `409 no_open_round`, — как есть; `WithTimeout`, `WithClock` (`shown_at` согласия), `PlayerID`, `Client`, `WorldID`. Внешний ID фикстуры — её id (`player-A`), платформа — `telegram` (вариант (а) T-307).
- API `Harness` v0 не тронут: в `harness.go` изменён только абзац комментария пакета («HTTP не делает `Harness`; по HTTP — `HTTPHarness`»).
- Документы: `shared/testkit/gateway/README.md` (новый: два харнесса, `FakeGateway`, платформа `ci-harness`, **запрет запуска против шлюза с ботом**, лимит темпа на `clock.Manual`); `internal/gateway/README.md` (T-308 в «Готово», пакет `gatewaytest/`, потребитель `client`); `Docs/ops/runbook.md` §6 «Другие правила эксплуатации бота» — пункт о запрете (правка требует согласования с devops, см. открытые вопросы).

**Как закрыты пункты DoD**
- API v0 не изменён: экспортируемые имена в diff не тронуты; потребители EPIC-002/003 в этом дереве (`test/e2e/stubs_v0_test.go`, `shared/testkit/swarm/*`, `cmd/multiverse/fake_contexts_test.go`) собраны и зелёные без правок.
- Граница импортов — по ADR-001 доп. п. 5; `golangci-lint` — 0; мутанты 13–16 ниже.
- `FakeGateway` за ≤ 1 с без внешних зависимостей, детерминированные id и часы — `TestAFakeGatewayStartsAndStopsWithinASecond`, `TestTwoRunsGiveTheSameIdentifiers`.
- Unit обвязки: `RegisterAndEnter` → `alive` в регионе (`TestRegisterAndEnterBringsAFixtureCharacterAliveIntoTheRegion` на `FakeGateway` + `FakeState` + `FakeNarrator`); `Act` → `correlation_id` (там же и `TestActAnswersTheCorrelationOfTheAction`); `AwaitDelivery` не залипает (`TestAwaitDeliveryReturnsWhenNothingArrives`, `TestAwaitDeliveryTimesOutWithAClearError`).
- Тот же клиент, что у бота (NFR-092), — compile-time: `var _ func(*client.Client, []*entity.Entity) (*gateway.HTTPHarness, error) = gateway.NewHTTPHarness` и `var _ func() *client.Client = (*gateway.HTTPHarness)(nil).Client` в `http_test.go`.
- `shared/testkit/gateway` и `gatewaytest` не импортируются из production — мутанты 14–16.
- Платформа `ci-harness` окончательно не решена (решение оркестратора: доставки условны до system-architect). Поведение описано по коду T-307; `ClientPlatforms`, OpenAPI `pollDeliveries` и `TestTheHarnessTakesTheDeliveriesOfTelegram` не менялись. Сквозной тест фиксирует вариант (а) явно: маршрут `telegram` и внешний ID фикстуры видны харнессу.
- Запрет общей очереди — README обвязки и runbook §6 (README шлюза — с T-307).
- Сквозная проверка long-poll: `TestAwaitDeliveryTakesATurnThroughTheLongPollAndCompletesIt` — `enter` → механика (`entity.updated cause=move`) и нарратив (`entry` от `FakeNarrator`) через настоящий `GET …/deliveries` клиента `ci-harness`, `POST …/ack`, ровно один `analytics.turn.completed` по корреляции входа, после ack очередь пуста.

**Как мир попадает в проекцию `FakeGateway`.** Объектного хранилища у двойника нет (`Context.objects` задаётся только из `export_test.go` пакета `gateway`), снапшот State считается отсутствующим, проекция строится из журнала. Тест публикует `entity.created` мира, региона и NPC от `testkit/state` и сеет те же сущности в `FakeState` (`newWorld` в `gatewaytest_test.go`). Production-код для этого не менялся.

**Тесты и прогоны** (Windows, cgo нет → без `-race`; CI гоняет `-race` на Linux)
- `go build ./... && go vet ./...` — ок.
- `go test -short -count=1 ./...` — ок (пакет `updates` бота прошёл без «Access is denied»).
- `go test -short -count=10 ./internal/gateway/gatewaytest/` — ок.
- `go test -tags e2e -count=1 ./test/e2e/...` — ок (15,0 с).
- `golangci-lint run ./...` — 0 issues.
- `make test` — exit 0; флака `TestTheSkirmishStopsSwingingWhenTheFightEnds` не было, повтор не понадобился.

**Мутанты** (копия `scratchpad\t308-mutants`, без `-overlay`, контрольный первым; копия, скрипт и лог удалены по точным путям; каталог данных, оставленный мутантом 9 в `%TMP%`, удалён по точному пути):

| # | Мутант | Результат |
|---|---|---|
| 0 | контрольный, без изменений | зелёный |
| 1 | `AwaitDelivery` не подтверждает доставки | красный: `…AcknowledgesAndKeepsTheRest`, `…ReportsTheGateway`, сквозной `…TakesATurnThroughTheLongPoll…` |
| 2 | нет проверки дедлайна ожидания | красный: `…TimesOutWithAClearError` (сначала висел до таймаута `go test`; тест ограничен контекстом 5 с, повтор — красный за 5 с) |
| 3 | `wait_ms` без учёта остатка | красный: `…TimesOutWithAClearError` |
| 4 | любой `404` → `501` | красный: `TestCloseRoundIsAStubUntilTheRouteIsMounted` |
| 5 | `RegisterAndEnter` не ждёт региона | красный: `TestRegisterAndEnterStopsAtItsTimeout` |
| 6 | пустой `action_key` не заполняется | красный: `TestActAnswersTheCorrelationOfTheAction` |
| 7 | `unknown` в ответе ack игнорируется | красный: `TestAwaitDeliveryReportsTheGateway` |
| 8 | `cursor` не запоминается | красный: `…AcknowledgesAndKeepsTheRest` |
| 9 | `Close` не удаляет свой каталог | красный: `…StartsAndStopsWithinASecond`, `TestAFailedStartLeavesNothing` |
| 10 | префикс id не `gw` | красный: `TestRegisterAndEnterBringsAFixtureCharacterAliveIntoTheRegion` |
| 11 | `Close` закрывает шину теста | красный: `TestAFakeGatewayLeavesWhatTheTestPassed` |
| 12 | `shown_at` по настенным часам вместо `WithClock` | красный: `TestRegisterAndEnterStopsAtItsTimeout` |
| 13 | `http.go` импортирует `internal/gateway/outbox` | lint красный: `shared-testkit-gateway` |
| 14 | `gatewaytest.go` импортирует `shared/testkit/gateway/sqlitedir` | lint красный: `no-testkit-in-production` |
| 15 | бот импортирует `gatewaytest` | lint красный: `cmd-telegram-bot` |
| 16 | бот импортирует `shared/testkit/gateway` | lint красный: `no-testkit-in-production` |

**Изменённые файлы**
- создан: `internal/gateway/gatewaytest/gatewaytest.go`, `internal/gateway/gatewaytest/gatewaytest_test.go`, `shared/testkit/gateway/http.go`, `shared/testkit/gateway/http_test.go`, `shared/testkit/gateway/README.md`;
- изменён: `shared/testkit/gateway/harness.go` (только комментарий пакета), `internal/gateway/README.md`, `Docs/ops/runbook.md` (§6).

**Отклонения от описания задачи**
- «Фикстуры `player-A/B/C` платформы `ci`» (design §7) — фикстуры регистрируются на платформе `telegram`: HTTP API MVP-1 другой платформы не принимает (`handlers.decodeAccount`), решения по варианту (б) нет. Внешний ID = id фикстуры, как в design §7.
- `AwaitDelivery(kind, timeout)` принимает первым аргументом `ctx`; добавлен `AwaitDeliveryOf` с фильтром по корреляции — во «входящих» могут лежать доставки прежних ходов.
- `CloseRound` в I1 — не заглушка без HTTP, а перевод голого `404` немонтированного маршрута в `501 not_implemented`; с T-354 ответ маршрута проходит без изменений.
- `--id-source=sequence` — генератор `eventbus.SequenceIDs` в `Deps.IDs` двойника; глобальный источник id `eventbus` двойник не трогает (тест ставит его сам, `testkit.Deterministic`).

**Уведомление TEAM-1/TEAM-2 (для `dev-log`/`journal`).** `Harness` v0 (`shared/testkit/gateway`: `NewHarness`, `CreatePlayer`, `Enter`, `Attack`, `Flee`…) не заменён и не изменён — переходить не нужно. Рядом появились `HTTPHarness` (тот же пакет, по HTTP через `internal/gateway/client`) и `gatewaytest.FakeGateway` (`internal/gateway/gatewaytest`). Тестам контекстов `gatewaytest` недоступен по правилам depguard; e2e с настоящим шлюзом пишется вне `internal/<контекст>`, по образцу `internal/gateway/gatewaytest/gatewaytest_test.go`.

**Открытые вопросы**
1. Платформа `ci-harness` (вопрос 1 карточки T-307, варианты (а)/(б)/(в)) — к system-architect. До решения — вариант (а) без изменений в коде, запрет в README и runbook.
2. Пункт runbook §6 внесён разработчиком; по DoD правка согласуется с devops — нужен просмотр devops-engineer.
3. Мир в проекции `FakeGateway` без объектного хранилища: сейчас тест публикует `entity.created` сам. Если e2e T-313 понадобится загрузка из снапшота `FakeState.Snapshot`, нужен способ передать `objstore.Client` контексту шлюза извне пакета (например, опция `gateway.New`) — это изменение production-API шлюза, решают architect#3/tech-lead#3.

**Предложения в бэклог**
- Лимит темпа `live` на `clock.Manual` (серия 5) остановит e2e длиннее пяти действий кодом `429`: e2e T-313 либо двигает часы, либо поднимает `MV_GATEWAY_RATE_ACTIONS_BURST` через `Config.Vars` — решить в T-313.
- Карточка называет ветку `task/T-308-testkit-gateway`, фактическая — `task/T-308-fake-gateway-harness`; поправить при приёмке.

**Риски и допущения**
- ~~Сквозные тесты зависят от того, что `FakeNarrator` прочтёт факт персонажа раньше `player.entered_region`.~~ *(Исправлено в итерации 2 по Mi-1 ревью #1: причина названа неверно — `FakeNarrator.entry` от факта персонажа не зависит.)* Настоящая гонка была в шлюзе: `actions.Service.publish` публиковал действие раньше записи хода (`Turns.Accepted`), и нарратив, обработанный consumer'ом до записи, не находил хода — ход не завершался до таймаута. Исправлено в итерации 2 (вне объёма T-308, по решению оркестратора).
- `-race` локально не прогонялся (нет cgo); общие поля фейка в `http_test.go` защищены мьютексом, проверка — в CI.

### Итерация 2 (developer#1, 2026-09-14)

Ответ на ревью #1 (code-reviewer#3: принять, 0/0/4/4) и решение оркестратора по Mi-1. Не коммитил.

**Mi-1 — исправлено; правка вне объёма T-308, по решению оркестратора (код T-305/T-306).**
- Сверка с КД: §5.5 задаёт порядок «валидация → `Turn(received)` → `Bus.Publish` → при ошибке `503`, `Turn` не сохраняется → при успехе `Turn(accepted)` + ключ + `202`». Код T-305 записывал ход только после всей партии. Теперь порядок как в КД; C-08 не меняется, ответы клиенту те же.
- `internal/gateway/actions/service.go` (`publish`): ход записывается (`Turns.Accepted`) прямо перед публикацией события `player.*`. Для legacy `gm.created` уходит раньше. После успешной партии `Turns.Acked` ставит `acked_at` на момент ответа `202`, чтобы задержка брокера осталась в таймингах хода (NFR-003).
- Действие не подтверждено шиной → `Turns.Withdrawn` забирает ход обратно (строка удаляется, `turns_count` сессии −1). Клиент получает прежний `503 bus_unavailable`, ключ не пишется, партия удерживается, ход не висит до таймаута. Ход, у которого уже есть механика или нарратив, не забирается: событие всё-таки дошло до брокера.
- Сбой публикации предложения после подтверждённого действия ход **оставляет**: действие ушло, нарратив придёт. Раньше хода в этом случае не было вовсе.
- Ход не удалось записать до публикации → `500 internal`, ничего не публикуется, партия удерживается, повтор ключа публикует то же действие. Раньше сбой записи после публикации только логировался, и ход терялся.
- `internal/gateway/actions/turns.go`: в интерфейс `Turns` добавлены `Withdrawn(ctx, ref, eventID)` и `Acked(ctx, eventID, at)`; обновлён godoc. `internal/gateway/turns/tracker.go`: реализация обоих методов, godoc `Accepted`.
- Двойники `Turns` в тестах обновлены: `actions/helpers_test.go` (`memoryTurns`, `turns` с `withdrawn`, `acked`, `failAccepted`, `recorded`) и `characters/helpers_test.go` (`noTurns`).
- Тесты:
  - `actions`: `TestTheTurnIsRecordedBeforeTheActionIsPublished` (agent и legacy: ход стоит к моменту публикации `player.*`, `Acked` — после партии); `TestATurnThatCannotBeRecordedPublishesNothing`; `TestAFailedPublicationKeepsNothing` переписан (сбой действия — ход забран, сбой предложения — ход остаётся).
  - `turns` (новый `order_test.go`, настоящие сервис, трекер и `gateway.db`): `TestTheMechanicsAndTheNarrativeRightAfterThePublicationCompleteTheTurn` — механика и нарратив обрабатываются в хуке сразу после публикации `player.entered_region`, до публикации предложения, и ход завершается ack'ом; `TestAnActionTheBusDidNotAcknowledgeTakesItsTurnBack`; `TestATurnThatHeardFromItsActionIsNotTakenBack`; `TestTheAcknowledgementOfATurnIsTheAnswerOfItsAction`.
- В КД статус хода до публикации — `received`. Код пишет сразу `accepted`: шаги фактов, нарратива и таймаута читают `accepted`, а не подтверждённое действие забирается `Withdrawn` (открытый вопрос 4).
- Неверно названная причина риска в «Рисках и допущениях» итерации 1 исправлена.

**Mi-2 — исправлено.** `TestCloseAnswersAWaitingLongPollAndStopsTheServer`: long-poll 15 с внутри сервера (признак — `409 poll_in_progress` пробы того же клиента; если проба опередила опрос, опрос перезапускается), `Close` → пустой список без ошибки ≤ 2 с, затем `/health` не отвечает.

**Mi-3 — исправлено.** Утверждение «в prod `ci-harness` не допущен» заменено действием оператора: задать в `.env` `MV_GATEWAY_CLIENT_IDS` без `ci-harness` (например, `telegram-bot,mvctl`). Умолчание сверено с `shared/env/vars.go:100` (`telegram-bot,ci-harness,mvctl`), compose передаёт переменную без значения. Правки: `Docs/ops/runbook.md` §6 (там же — убрать `ci-harness` из `MV_GATEWAY_ACTOR_KIND_CLIENTS` и `MV_CORE_ADMIN_CLIENTS`, как в комментарии `.env.example`), `shared/testkit/gateway/README.md`, `internal/gateway/README.md` (бывшие строки 204–205).

**Mi-4 — исправлено.** «Один `HTTPHarness` на шлюз» — в godoc `HTTPHarness` и в README. Срок `AwaitDelivery` считается до захвата блокировки. Тест `TestAwaitDeliveryCountsItsTimeoutFromTheCall`.

**N-1 — исправлено частично.**
- `ErrRoundsNotMounted` — сентинел `errors.New`.
- `CloseRound` возвращает значение неэкспортируемого типа `roundsNotMounted`: `errors.Is` совпадает с сентинелом, `errors.As` каждому вызывающему отдаёт свою копию `*client.APIError` `501 not_implemented`, текст сохраняет ответ сервера.
- Отличить голый `404` немонтированного маршрута от `404` неверного базового URL нельзя: mux шлюза отвечает на неизвестный путь как любой Go-сервер. Перевод оставлен и записан в godoc и README, снимается в T-354.
- Тест: изменение прочитанной копии не влияет на следующий вызов, в тексте есть `404`.

**N-2 — исправлено.** При `unknown` в ответе ack подтверждённые доставки того же опроса кладутся во «входящие» до возврата ошибки. При ошибке самого ack доставки остаются в лизинге шлюза — это записано в godoc и README. Тест дополнен.

**N-3 — исправлено.** `waitFor` в `gatewaytest_test.go` ждёт 5 с настенного времени, а не 500 пауз.

**N-4 — исправлено.** `newBus` в `gatewaytest` — переменная; `export_test.go` (`WatchBuses`) даёт тесту увидеть шину, созданную двойником. `TestAFailedStartLeavesNothing` проверяет, что после неудачного старта каталог удалён, а публикация в шину двойника возвращает `eventbus.ErrClosed`. Отказ после открытия БД не проверяется: `Config.Bus` — конкретный `*membus.Bus`, и сломать догон журнала снаружи нечем.

**Изменённые файлы итерации 2**
- вне объёма T-308 (Mi-1): `internal/gateway/actions/service.go`, `internal/gateway/actions/turns.go`, `internal/gateway/turns/tracker.go`, `internal/gateway/actions/service_test.go`, `internal/gateway/actions/helpers_test.go`, `internal/gateway/characters/helpers_test.go`, создан `internal/gateway/turns/order_test.go`;
- T-308: `internal/gateway/gatewaytest/gatewaytest.go`, `internal/gateway/gatewaytest/gatewaytest_test.go`, создан `internal/gateway/gatewaytest/export_test.go`, `shared/testkit/gateway/http.go`, `shared/testkit/gateway/http_test.go`, `shared/testkit/gateway/README.md`, `internal/gateway/README.md`, `Docs/ops/runbook.md`.

**Прогоны** (Windows, без `-race` — нет cgo)
- `go build ./... && go vet ./...` — ок.
- `go test -short -count=1 ./...` — ок (пакет `updates` бота без «Access is denied»).
- `go test -short -count=15` сквозных тестов (`TestRegisterAndEnterBringsAFixtureCharacterAliveIntoTheRegion`, `TestAwaitDeliveryTakesATurnThroughTheLongPollAndCompletesIt`, `TestCloseAnswersAWaitingLongPollAndStopsTheServer`, `TestTheMechanicsAndTheNarrativeRightAfterThePublicationCompleteTheTurn`) — ок.
- `go test -tags e2e ./test/e2e/...` — ок.
- `golangci-lint run ./...` — 0 issues.
- `make test` — exit 0; покрытие: `actions` 94,5 %, `turns` 83,1 %, `gatewaytest` 92,2 %. Флака `TestTheSkirmishStopsSwingingWhenTheFightEnds` не было.

**Мутанты итерации 2** (копия `scratchpad\t308i2-copy`, без `-overlay`, контрольный первым; копия удалена по точному пути, каталогов `gatewaytest-*` в `%TMP%` не осталось):

| # | Мутант | Результат |
|---|---|---|
| 0 | контрольный | зелёный |
| 1 | старый порядок: ход пишется после всей партии | красный: `TestTheTurnIsRecordedBeforeTheActionIsPublished`, `TestTheMechanicsAndTheNarrativeRightAfterThePublicationCompleteTheTurn` |
| 2 | проверка 2 ревьюера: запись хода через 30 мс после партии | красный: `TestAwaitDeliveryTakesATurnThroughTheLongPollAndCompletesIt` |
| 3 | нет `Withdrawn` при сбое действия | красный: `TestAFailedPublicationKeepsNothing/player.entered_region`, `TestAnActionTheBusDidNotAcknowledgeTakesItsTurnBack` |
| 4 | `Withdrawn` и при сбое после действия | красный: `TestAFailedPublicationKeepsNothing/entity.update.proposed` |
| 5 | `Withdrawn` удаляет и озвученный ход | красный: `TestATurnThatHeardFromItsActionIsNotTakenBack` |
| 6 | `Withdrawn` не уменьшает `turns_count` | красный: `TestAnActionTheBusDidNotAcknowledgeTakesItsTurnBack` |
| 7 | нет `Acked` | красный: `TestTheTurnIsRecordedBeforeTheActionIsPublished`, `TestAFailedPublicationKeepsNothing` |
| 8 | сбой записи хода не останавливает публикацию | красный: `TestATurnThatCannotBeRecordedPublishesNothing` |
| 9 | `Close` без `srv.Stop` (Mi-2) | красный: `TestCloseAnswersAWaitingLongPollAndStopsTheServer` |
| 10 | неудачный старт не закрывает свою шину (N-4) | красный: `TestAFailedStartLeavesNothing` |
| 11 | срок `AwaitDelivery` после блокировки (Mi-4) | красный: `TestAwaitDeliveryCountsItsTimeoutFromTheCall` |
| 12 | при `unknown` теряются доставки опроса (N-2) | красный: `TestAwaitDeliveryReportsTheGateway` |
| 13 | одна общая `*client.APIError` на всех (N-1) | красный: `TestCloseRoundIsAStubUntilTheRouteIsMounted` |
| 14 | ответ сервера пропал из текста ошибки (N-1) | красный: `TestCloseRoundIsAStubUntilTheRouteIsMounted` |

**Открытые вопросы (итерация 2)**
4. КД §5.5 и §4.2 называют ход до публикации `received`; код пишет сразу `accepted` и забирает ход при сбое. Принять как реализацию КД или ввести `received` во все шаги трекера — решает architect#3 (правка КД §5.5: ход остаётся и при сбое публикации события после подтверждённого действия).
5. Остаток гонки: если событие действия дошло до брокера без подтверждения, а нарратив обработан после `Withdrawn`, ход снова не найдётся, как до исправления. Окно — только при сбое шины; повтор ключа запишет ход, но нарратив уже прошёл. Нужна ли защита в `OnNarrative`/`OnMechanics` (ошибка → повтор обработчика шиной) — решает architect#3/tech-lead#3.
6. Пункты runbook §6 (запрет и действие оператора) по-прежнему требуют согласования с devops-engineer.

**Предложения в бэклог (итерация 2)**
- T-354: снять перевод голого `404` в `501` в `HTTPHarness.CloseRound` (N-1).
- T-313 / I2: несколько клиентов `ci-harness` для `group-3x30` — отдельные `client_id` или один харнесс на всех (Mi-4).

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#3, TEAM-3) | 2026-09-14 | 0 | 0 | 4 | 4 | принять |
| 2 (code-reviewer#3, TEAM-3) | 2026-09-14 | 0 | 0 | 1 | 3 | принять |
| 3 | — | — | — | — | — | ревью не проводилось: итерация меняет только тесты, Mi-1 ревью #2 закрыт мутантами; принимает tech-lead#3 (решение оркестратора) |

### Ревью #1

Полная запись — `../review.md`, раздел «T-308 · ревью #1».

В порядке:
- границы импорта и API `Harness` v0;
- окружение, часы, шина и каталог, которые двойник создал сам;
- «входящие» `AwaitDeliveryOf`, `ErrNoDelivery`, перевод в `501` только голого `404`.

Зелёные прогоны: `build`/`vet`, `-count=3`, 45 прогонов сквозных тестов, `golangci-lint`.

Minor:
- **Mi-1.** Гонка сквозного теста не в `FakeNarrator`, а в шлюзе. Действие публикуется до `Turns.Accepted`, и нарратив, обработанный раньше записи хода, теряет шаг: ход не завершается. Зонд с задержкой записи 30 мс красный. Со стороны теста это не устранить: нужен дефект шлюза до T-313, а риск в карточке надо исправить.
- **Mi-2.** Тест не проверяет, что `Close` останавливает сервер и отвечает висящему long-poll. Зонд без `srv.Stop` зелёный на всём наборе.
- **Mi-3.** Runbook §6 и README обвязки подают «в prod `ci-harness` не допущен» как факт, хотя по умолчанию он допущен. Нужна инструкция оператору.
- **Mi-4.** Не описано, что на шлюз допустим один `HTTPHarness`. Срок `AwaitDelivery` отсчитывается после захвата блокировки.

Nit:
- **N-1.** Голый `404` при неверном URL тоже станет `501`; `ErrRoundsNotMounted` изменяем.
- **N-2.** При `unknown` в ack теряются доставки того же опроса.
- **N-3.** `waitFor` ограничен числом итераций, а не сроком.
- **N-4.** Неудачный старт с собственной шиной не проверен.

### Ревью #2

Полная запись — `../review.md`, раздел «T-308 · ревью #2» (ссылка перенесена при приёмке).

Проверены исправления итерации 2 и регрессия от них, в том числе код шлюза вне объёма (Mi-1 ревью #1). Откат хода атомарен, `turns_count` не уходит в минус, повтор ключа после `503` идемпотентен. Риск слияния по `actions.Turns` — низкий.

Minor:
- **Mi-1.** Контексты без отмены у `Withdrawn` и `Acked` не закреплены тестами: мутанты `Acked(ctx)` и `Withdrawn(ctx)` зелёные, две прежние проверки описывают старый порядок. *(Закрыт итерацией 3, мутанты 1–2 красные.)*

Nit:
- **N-1.** Порог `TestAwaitDeliveryCountsItsTimeoutFromTheCall` зависит от абсолютного времени. *(Закрыт итерацией 3.)*
- **N-2.** Сбой `Turns.Accepted` на истёкшем бюджете отвечает `500 internal`. *(Оставлено `500` по КД §5.5 в редакции architect#3; `503 store_unavailable` — §8 З-18 индекса.)*
- **N-3.** `acked_at` хода, завершённого до ответа `202`. *(§8 З-17 индекса, architect#3.)*

## Приёмка (tech-lead)

**Решение:** принято, статус `done`. **Дата:** 2026-09-14. **Кто:** tech-lead#3 (TEAM-3), Opus.
**Итог:** `gatewaytest.FakeGateway` и `HTTPHarness` сделаны по DoD без изменения API `Harness` v0, гонка записи хода в шлюзе исправлена по КД §5.5 в редакции architect#3, замечания обоих ревью закрыты или вынесены в бэклог.
**Условие, которое слияние не держит:** отметка devops-engineer#1 по `Docs/ops/runbook.md` §6. Её запрашивает оркестратор после приёмки (обоснование — «Runbook §6 и devops»). «Учёт времени» заполняет оркестратор.

### Прогоны приёмки
Рабочая папка `.worktrees/T-308` (база `599e883`, изменения не закоммичены), go1.26.8 windows/amd64, без `-race` (нет cgo).
- `go build ./... && go vet ./...` — 0.
- `go test -short -count=1 ./...` — все пакеты `ok`, кроме `cmd/telegram-bot/internal/updates`: `fork/exec …\test.test.exe: Access is denied` (блокировка Windows, не тест). Обход: `go test -c -o <scratchpad>/t308tl_upd.exe` и запуск с `-test.short` — `PASS`; exe удалён по точному пути.
- `go test -short -count=5 ./internal/gateway/gatewaytest/ ./internal/gateway/turns/ ./internal/gateway/actions/ ./shared/testkit/gateway/` — `ok`.
- `go test -count=1 -tags e2e ./test/e2e/...` — `ok` (10,9 с).
- `golangci-lint run ./...` — `0 issues.`
- `make test` — exit 0 с первого раза, 57 пакетов `ok` (`updates` в нём прошёл), пороги покрытия пройдены; `actions` 94,5 %, `turns` 83,1 %, `gatewaytest` 92,2 %. Флака `TestTheSkirmishStopsSwingingWhenTheFightEnds` не было.

### Сверка DoD (индекс, раздел «T-308»)
| Пункт | Чем подтверждено | Итог |
|---|---|---|
| API `Harness` v0 не изменён | `git diff 599e883 -- shared/testkit/gateway/`: в `harness.go` — только абзац комментария пакета, `scenario.go` не менялся, экспортируемые имена v0 не тронуты. Потребители v0 (`test/e2e`, `shared/testkit/swarm`, `cmd/multiverse/fake_contexts_test.go`) собраны и зелёные без правок | выполнено |
| Граница импортов по решению system-architect, lint без `//nolint` | ADR-001 доп. 2026-09-13 п. 5, C-08 v1.4: `FakeGateway` — `internal/gateway/gatewaytest`, обвязка — `shared/testkit/gateway` на `internal/gateway/client$` и `api$` (правило `shared-testkit-gateway`, T-445). `.golangci.yml` не менялся, `//nolint` в новых файлах нет, lint — 0; мутанты 13–16 итерации 1 | выполнено |
| `FakeGateway` за ≤ 1 с, без внешних зависимостей, детерминированные id и часы | `TestAFakeGatewayStartsAndStopsWithinASecond`, `TestTwoRunsGiveTheSameIdentifiers`; своя `membus`, `clock.Manual`, `eventbus.SequenceIDs("gw")`, `-short` | выполнено; id из последовательности вместо ULID — по `--id-source=sequence` описания |
| Unit обвязки: `RegisterAndEnter` → `alive` в регионе; `Act` → `correlation_id`; `AwaitDelivery` не залипает | `TestRegisterAndEnterBringsAFixtureCharacterAliveIntoTheRegion`, `TestRegisterAndEnterStopsAtItsTimeout`, `TestActAnswersTheCorrelationOfTheAction`, `TestAwaitDeliveryReturnsWhenNothingArrives`, `TestAwaitDeliveryTimesOutWithAClearError` (`ErrNoDelivery`), `TestAwaitDeliveryCountsItsTimeoutFromTheCall`; мутанты 1–3, 5, 6 итерации 1 | выполнено |
| Тот же `internal/gateway/client`, что у бота (NFR-092), compile-time | `var _ … = gateway.NewHTTPHarness` и `(*gateway.HTTPHarness)(nil).Client` в `http_test.go` | выполнено |
| `shared/testkit/gateway` не импортируется из production | `no-testkit-in-production` (с T-445 запрещает и `gatewaytest`); мутанты 14–16 — lint красный | выполнено |
| Уведомление TEAM-1/TEAM-2 | текст — подраздел «Передать»; замены v0 нет, переходить не нужно | выполнено |
| Платформа `ci-harness` — по решению system-architect | решения нет. Вариант (а) без изменений кода: `ClientPlatforms`, OpenAPI `pollDeliveries`, `TestTheHarnessTakesTheDeliveriesOfTelegram` не менялись; сквозной тест фиксирует маршрут `telegram` и внешний ID фикстуры | выполнено условно, до решения system-architect (вопрос 1 карточки T-307 открыт) |
| Запрет запуска харнесса против шлюза с ботом: README обвязки и runbook §6 | `shared/testkit/gateway/README.md` (врезка «Запрет» и действие оператора), `Docs/ops/runbook.md` §6 (запрет; `MV_GATEWAY_CLIENT_IDS` без `ci-harness`, то же для `MV_GATEWAY_ACTOR_KIND_CLIENTS` и `MV_CORE_ADMIN_CLIENTS`); README шлюза — с T-307, уточнён по Mi-3 | выполнено; отметка devops — условие ниже |
| Сквозная проверка long-poll харнесса | `TestAwaitDeliveryTakesATurnThroughTheLongPollAndCompletesIt`: механика и нарратив через `GET …/deliveries` клиента `ci-harness`, `POST …/ack`, ровно один `analytics.turn.completed`, после ack очередь пуста | выполнено |
| DoD-common | тесты и lint зелёные; время — `shared/clock` (ожидания обвязки по `clock.Real`, как long-poll по C-01 v1.8); dev-log — три записи developer#1 и запись architect#3; внешних ID нет (id фикстур — не ПДн, design §7); переменных, зависимостей и миграций нет | выполнено |

**Карта владения.** Вне `internal/gateway` и `shared/testkit/gateway` задача меняет:
- `Docs/ops/runbook.md` §6 — по DoD, отметка devops;
- `Docs/dev-team/architecture/components/gateway-and-bot.md` (шапка, §4.2, §5.5, §9) — правка architect#3, владельца КД.

Чужих путей сверх этого нет.

### Правка кода шлюза вне объёма T-308 (решение оркестратора)
Принята в этой задаче. Файлы: `internal/gateway/actions/service.go`, `actions/turns.go` (интерфейс `actions.Turns` — `Withdrawn`, `Acked`), `turns/tracker.go`. Тесты: `actions/{helpers,pending,service}_test.go`, `characters/helpers_test.go`, `turns/tracker_test.go`, новый `turns/order_test.go`. Сверено по коду:
- `Turns.Accepted` → `Publish player.*` → `Withdrawn` при неподтверждении → `Acked` после всей партии — порядок КД §5.5 в редакции architect#3;
- `Withdrawn` — один `DELETE … status = 'accepted' AND mechanics_at IS NULL`, `turns_count − 1` только при удалённой строке, на `context.WithoutCancel`; `Acked` — на контексте без отмены;
- сбой записи хода — `500 internal`: ничего не опубликовано, ключа нет, партия удержана (`TestATurnThatCannotBeRecordedPublishesNothing`, три случая).

Мутанты итераций 2 и 3 (порядок, откат, счётчик сессии, контексты `Acked`/`Withdrawn`, `500 → 503`) красные.

**Итерация 3 принята без ревью #3** (решение оркестратора). Diff итерации меняет только тесты, `service.go` — в форме итерации 2 (классификации кода ответа по причине сбоя в нём нет).
- Mi-1 ревью #2 закрыт мутантами 1–2 итерации 3.
- N-1 закрыт: проверка относительная.
- N-2 — `500 internal` по КД §5.5 (решение оркестратора); вопрос `503 store_unavailable` — к system-architect после замера T-392 (§8 З-18 индекса, строка DoD T-392).
- N-3 — к architect#3 (§8 З-17).

### Отступления, принятые при приёмке
- фикстуры регистрируются на платформе `telegram`, а не `ci` (вариант (а) до решения system-architect);
- `AwaitDelivery(ctx, kind, timeout)` принимает `ctx`; добавлен `AwaitDeliveryOf` с фильтром по корреляции;
- `CloseRound` в I1 переводит голый `404` в `501 not_implemented`; перевод снимает T-354 (строка DoD);
- `--id-source=sequence` — генератор в `Deps.IDs` двойника, глобальный источник id не трогается;
- мир в проекции `FakeGateway` — из журнала (`entity.created`), без объектного хранилища (решение ниже);
- ход пишется сразу `accepted`, статус `received` код не пишет (КД §4.2, §5.5);
- «один `HTTPHarness` на шлюз» — ограничение в godoc и README; несколько клиентов — строка DoD T-357.

### Runbook §6 и devops
Пункт §6 внёс исполнитель. **Условие:** отметку devops-engineer#1 запрашивает оркестратор после приёмки. Если devops попросит правки, они идут отдельной правкой в ветке эпика до контрольного слияния в `develop`.

**Слияние отметка не держит** — ни слияние задачи в ветку эпика, ни контрольное слияние в `develop`:
1. Пункт не меняет процедуры, compose и Makefile. Это запрет и сужение допуска клиента через `.env` оператора.
2. Действие оператора уже требуется принятым планом: DoD T-392 («`ci-harness` отсутствует во всех трёх списках» prod-`.env`, T-411) и комментарий над списками в `.env.example`. Умолчания сверило ревью #2 (`shared/env/vars.go:100, 102, 148`, compose без значений).
3. T-308 — «поставка в develop», её ждут TEAM-1/TEAM-2; удерживать её ради отметки к тексту-запрету несоразмерно.
4. Прецедент — Mi-11 T-317: §6 проверили ревью, живая сверка — на стенде. Живая сверка этого пункта — строка DoD T-390.

### Слияние с T-309 и кончиком эпика
Пробное слияние — копия `scratchpad/t308tl-merge`: дерево T-308 с неотслеживаемыми файлами плюс изменения `.worktrees/T-309` (итерация 2, только чтение) от `599e883`, общие файлы — трёхсторонний `git merge-file`. Копия удалена по точному пути.
- **Код не пересекается.** Общие файлы — только документы: КД шлюза, `dev-log.md`, `internal/gateway/README.md`.
- **Двойники `actions.Turns`.** В `.worktrees/T-309` новых реализаций нет. Есть только `turns.Tracker`, `memoryTurns`/`turns` (`actions/helpers_test.go`) и `noTurns` (`characters/helpers_test.go`). T-309 их не меняет, T-308 обновила все три. Integration-тест T-316 (кончик эпика `9651c19`) пишет ходы SQL-вставкой и `Turns` не реализует.
- **КД `gateway-and-bot.md`.** `git merge-file` на текущих копиях — 0 конфликтов в обоих порядках аргументов, база приведена к CRLF, как рабочие копии. Оговорка для ручных проверок: база из `git show` в LF против копий в CRLF даёт ложные конфликты (8 участков); `git merge` нормализует концы строк сам.
- **`internal/gateway/README.md`** — один конфликт в таблицах «Статус реализации»: в «Готово» оставить обе строки (T-308 `gatewaytest/`, затем T-309 `snapshot/`…), в «Ещё не реализовано» — только строку групп и раундов (T-352…T-356).
- **`dev-log.md`** — хвостовые записи обеих сторон, сводит драйвер `appendtail` (проверить, что он есть в `.git/merge-drivers`).
- **`tasks.md`.** T-309 индекс не меняет. С кончиком эпика (v0.1.10: T-316, T-473) — стандартный рецепт: строка версии в шапке — взять 0.1.11; в §9 — сначала 0.1.10, затем 0.1.11. Остальные правки 0.1.11 с правками 0.1.10 не пересекаются.
- **Слитое дерево** (плюс `redpanda_integration_test.go` кончика эпика): `go build ./... && go vet ./...` — 0; `go vet -tags integration ./internal/gateway/...` и `go vet -tags e2e ./test/e2e/...` — 0; `go test -short -count=1 ./internal/gateway/... ./shared/testkit/gateway/...` — 18 пакетов `ok`.
- **Кто разрешает.** Конфликты документные, разрешает оркестратор по рецепту выше. Если T-309 до слияния добавит новый двойник `actions.Turns`, дописать `Withdrawn`/`Acked` — developer#1 (автор обеих задач).
- После слияния T-309 контекст шлюза пишет снапшоты, только если есть объектное хранилище (`MV_MINIO_*`) и мир (`MV_WORLD_ID`). `FakeGateway` окружение процесса не читает, поэтому снапшотов у него нет; тесты `gatewaytest` в слитом дереве зелёные.

### Загрузка мира из снапшота в `FakeGateway` (вопрос 3 исполнителя)
**Решение tech-lead#3:** production-API шлюза в T-308 не меняется. Сценарии T-313 строят мир из журнала — как `newWorld` в `gatewaytest_test.go`: `entity.created` мира, региона и NPC плюс те же сущности в `FakeState`. Если сценарию T-313 нужна загрузка проекции из снапшота State, а `recovery` T-314 — снапшот шлюза T-309, объектное хранилище передаётся явно:
- поле `gatewaytest.Config.Objects objstore.Client`;
- экспортируемая опция конструктора контекста `internal/gateway` (например, `gateway.New(src, gateway.WithObjectStore(c))`) вместо `SetObjectStore` из `export_test.go`.

Сигнатуру согласует architect#3 (КД §6) до кода. `shared/runtime.Deps` не расширяется. Строка — в DoD T-313.

### Передать
**Уведомление TEAM-1/TEAM-2** (для `dev-log`/`journal.md` при контрольном слиянии в `develop`):
> `shared/testkit/gateway.Harness` v0 (`NewHarness`, `CreatePlayer`, `Enter`, `Attack`, `Flee`…) не заменён и не изменён — переходить не нужно. Рядом появились `HTTPHarness` (тот же пакет, по HTTP через `internal/gateway/client`) и `internal/gateway/gatewaytest.FakeGateway` — настоящий шлюз in-process на `membus`. Тестам контекстов `gatewaytest` недоступен по правилам depguard; e2e с настоящим шлюзом пишется вне `internal/<контекст>`, по образцу `internal/gateway/gatewaytest/gatewaytest_test.go`.

**Оркестратору:**
- запросить отметку devops-engineer#1 по runbook §6 (условие выше);
- при контрольном слиянии в `develop` — строка в `CLAUDE.md`, раздел «Тестовые заглушки»: `testkit/gateway.HTTPHarness` и `internal/gateway/gatewaytest.FakeGateway` (только тесты, `no-testkit-in-production`);
- предупредить исполнителя T-309: у `actions.Turns` появились `Withdrawn` и `Acked`;
- system-architect: вопрос 1 карточки T-307 (платформа `ci-harness`) по-прежнему открыт; `503 store_unavailable` — после замера T-392 (§8 З-18);
- architect#3: `acked_at` хода, завершённого до `202` (§8 З-17); диаграмма КД §7.2 после слияния T-309 (§8 З-19).

**Строки в индексе `tasks.md` v0.1.11:**
- DoD T-313 — обход лимита темпа; мир и объектное хранилище `FakeGateway`;
- DoD T-350 — `acted` при неподтверждённом действии;
- DoD T-354 — снять перевод `404 → 501`;
- DoD T-357 — несколько клиентов `ci-harness` на шлюз;
- DoD T-390 — стенд с ботом без `ci-harness` в `MV_GATEWAY_CLIENT_IDS`;
- DoD T-392 — учёт `500 internal` пути действий для З-18;
- §8: З-16 — мягкий откат хода, З-17 — `acked_at`, З-18 — `503 store_unavailable`, З-19 — диаграмма §7.2.

### Остаточные риски
- `-race` не запускался (нет cgo), CI гоняет его на Linux.
- Окно при сбое шины (КД §5.5, «Известное поведение») искажает аналитику C-10 на kafka-адаптере, на `membus` не воспроизводится. Если проявится — З-16.
- До решения system-architect `telegram-bot` и `ci-harness` делят очередь `telegram`; защита — только `.env` оператора.
- В `%TMP%` лежат 17 каталогов `gateway-sqlite-*` от прогонов других сессий (самый новый — 02:22, до этой приёмки). Они не мои и не удалялись.

## Итерация 3 (developer)

developer#1 (TEAM-3), 2026-09-14. Ответ на ревью #2 (code-reviewer#3: принять, 0/0/1/3). Код шлюза по-прежнему вне объёма T-308, по решению оркестратора. КД не трогал. Не коммитил.

**Mi-1 — исправлено.** Мутанты `Acked(ctx)` и `Withdrawn(ctx)` вместо контекста без отмены теперь краснеют.
- `actions/helpers_test.go`: двойник `turns` записывает `ctx.Err()` у `Acked` (`ackedOn`) и `Withdrawn` (`withdrawnOn`). Поле `acceptedOn` удалено: `Accepted` идёт до публикации, и проверка отмены на нём ничего не проверяла. `failAccepted` стал функцией от контекста.
- `actions/pending_test.go`:
  - `TestABudgetThatRunsOutAfterTheLastPublicationKeepsTheKey` проверяет `ackedOn`, а не прежний `acceptedOn[0]`, комментарий — по новому порядку;
  - новый `TestABudgetThatRunsOutOnTheActionTakesTheTurnBack`: публикация `player.*` упирается в бюджет → `503`, ход забран на контексте без отмены.
- `turns/tracker_test.go`: `TestTheTurnIsRecordedWhenTheBudgetRunsOutAfterTheLastPublication` переименован в `TestTheAcknowledgementIsRecordedWhenTheBudgetRunsOutAfterTheLastPublication`, комментарий переписан. Тест проверяет, что `acked_at` строки равен `acked_at` ответа, то есть моменту после истёкшего бюджета.
- `turns/order_test.go`: новый `TestABudgetThatRunsOutOnTheActionTakesItsTurnBack` на настоящем трекере. Бюджет кончается на `player.looked` → `503`, строк хода нет, `turns_count = 0`.

**N-2 — код ответа оставлен `500 internal`, отклонение от поручения.**
- Поручение разрешало `503 bus_unavailable` или код из C-08/api-contracts §1.6 для истёкшего бюджета, а если подходящего кода нет — оставить `500` и записать вопрос.
- В §1.6 кода для истёкшего бюджета нет: `503 bus_unavailable`/`state_unavailable` означают «зависимость недоступна».
- Исправленный architect#3 КД §5.5 прямо предписывает «Ход не записался → `500 internal`» и в абзаце «Почему `500 internal`, а не `503`» разбирает этот случай, включая исчерпанный бюджет решения. Там же указано, что `503` отправил бы эталонный клиент повторять запрос в ту же занятую `gateway.db`.
- Классификацию по причине я сначала реализовал, но откатил: она противоречила бы КД, который мне поручено не трогать. Вопрос записан ниже.
- Тест на оба пути: `TestATurnThatCannotBeRecordedPublishesNothing` — таблица «сбой хранилища», «хранилище вернуло `context.DeadlineExceeded`» и «бюджет решения истёк внутри записи». Во всех трёх случаях `500 internal`, ничего не опубликовано, ключа нет, партия удержана, повтор публикует действие один раз. Мутанты «истёкший бюджет → `503`» и «любой сбой → `503`» краснеют.

**N-1 — исправлено.** `TestAwaitDeliveryCountsItsTimeoutFromTheCall` сравнивает момент возврата второго вызова с моментом возврата первого (не больше 200 мс), а не с абсолютным порогом от старта горутин. Мутант «срок после блокировки» даёт разрыв не меньше 300 мс и краснеет.

**N-3 — только бэклог, код не менялся** (см. ниже).

**Изменённые файлы итерации 3** (все — тесты):
- вне объёма T-308: `internal/gateway/actions/helpers_test.go`, `internal/gateway/actions/pending_test.go`, `internal/gateway/actions/service_test.go`, `internal/gateway/turns/tracker_test.go`, `internal/gateway/turns/order_test.go`;
- T-308: `shared/testkit/gateway/http_test.go`.
- `internal/gateway/actions/service.go` после отката N-2 совпадает с итерацией 2.

**Прогоны** (Windows, без `-race` — нет cgo)
- `go build ./... && go vet ./...` — ок.
- `go test -short -count=3 ./internal/gateway/... ./shared/testkit/gateway/...` — ок, 17 пакетов.
- `golangci-lint run ./...` — 0 issues.
- `make test` — exit 0; покрытие `actions` 94,5 %, `turns` 83,1 %, `gatewaytest` 92,2 %; флака `TestTheSkirmishStopsSwingingWhenTheFightEnds` не было.
- Первый полный прогон я остановил на середине, когда откатывал N-2. Каталог `gateway-sqlite-171423342`, который он оставил в `%TMP%`, удалён по точному пути.

**Мутанты итерации 3** (копия `scratchpad\t308i3-copy`, без `-overlay`, контрольный первым; копия удалена по точному пути):

| # | Мутант | Результат |
|---|---|---|
| 0 | контрольный | зелёный (`actions`, `turns`, `gatewaytest`) |
| 1 | `Turns.Acked(ctx, …)` вместо `out` | красный: `TestABudgetThatRunsOutAfterTheLastPublicationKeepsTheKey`, `TestTheAcknowledgementIsRecordedWhenTheBudgetRunsOutAfterTheLastPublication` |
| 2 | `Turns.Withdrawn(ctx, …)` вместо `context.WithoutCancel(ctx)` | красный: `TestABudgetThatRunsOutOnTheActionTakesTheTurnBack`, `TestABudgetThatRunsOutOnTheActionTakesItsTurnBack` |
| 3 | истёкший бюджет при записи хода → `503` | красный: `TestATurnThatCannotBeRecordedPublishesNothing` (два подтеста о бюджете) |
| 4 | любой сбой записи хода → `503` | красный: `TestATurnThatCannotBeRecordedPublishesNothing` (все три подтеста) |
| 5 | срок `AwaitDelivery` после блокировки | красный: `TestAwaitDeliveryCountsItsTimeoutFromTheCall` |

**Открытые вопросы (итерация 3)**
7. N-2 ревью #2 и поручение оркестратора (`503` при истёкшем бюджете) расходятся с КД §5.5 в редакции architect#3 (`500 internal`, `503` отклонён). Оставлено по КД. Если нужен `503` — сначала правка КД §5.5 architect#3; правка кода — строка в `unrecordedCode`-подобной функции, тест уже есть. Та же классификация касается сбоя `Begin` (КД §5.5 п. 3).

**Предложения в бэклог (итерация 3)**
- **N-3 ревью #2.** `Acked` не трогает ход, завершённый раньше ответа `202`, например шаблонный нарратив, подтверждённый до публикации предложения. У такого хода `analytics.turn.completed` уходит с `acked_at` — моментом записи хода до публикации, и `ack_latency_p95_ms` занижается на задержку брокера. Варианты: зафиксировать это в КД §7.6 и `metrics.md` или писать `acked_at = NULL` в `Accepted` и считать его в `Payload`. Решает architect#3.

## Решения architect#3 (итерация 2)

architect#3 (TEAM-3), 2026-09-14. Основание: открытые вопросы 4 и 5 «Итерации 2 (developer)», Mi-1 ревью #1 (`../review.md`), незакоммиченный код в `.worktrees/T-308`. КД шлюза исправлен в этой папке: шапка, §4.2, §5.5, §9. Контракты C-01, C-08, C-10 не меняются. Код не менялся.

**Вопрос 4 (у оркестратора — 1): `received` или `accepted` до публикации — вариант (а), реализация принята.**
- Ход пишет `Turns.Accepted` сразу со статусом `accepted`, непосредственно перед `player.*`. Статус `received` не вводится. КД §5.5 переписан по коду итерации 2. В §4.2 `received` остаётся в `CHECK`, но код его не пишет; там же описаны `received_at` и `acked_at`.
- *Почему не (б).* Механика и нарратив приходят только после записи события в брокер, поэтому до подтверждения ход во всех шагах читался бы как `accepted`. Отличие нужно только пути сбоя, а его закрывает `Withdrawn`. Вариант (б) — это правка `OnMechanics`, `OnNarrative`, `Sweep`/`expired`, `open` и восстановления при старте без наблюдаемой разницы. Восстановление при старте (КД §7.6) параллельно трогает T-309. C-10 статуса `received` не несёт, а `received_at` пишется и так.
- Приняты и записаны в §5.5:
  - сбой публикации предложения после подтверждённого `player.*` ход оставляет;
  - `Withdrawn` не забирает ход, у которого уже есть механика или нарратив;
  - `Acked` переносит `acked_at` на момент `202`. Если `Acked` не записался, остаётся момент записи хода: `ack_latency` занижается на время публикации, сбой виден в логе (`Error`).
- Диаграмма §7.2 по-прежнему показывает шаги `Turn(received)` и `Turn(accepted)`. §5.5 объясняет, как их читать. Саму диаграмму не правил: правки ограничены §4.2, §5.5 и §9 из-за T-309. См. бэклог.

**Вопрос 5 (у оркестратора — 2): остаток гонки при сбое шины — окно принимаем. Защиту через ошибку обработчика не вводим. Описание — КД §5.5, «Известное поведение».**
- *Условия окна.* Работает только kafka-адаптер: на `membus` `Publish` с ошибкой событие не пишет, поэтому e2e и `FakeGateway` детерминированы. Брокер записал `player.*`, но подтверждение не пришло в бюджет решения. Механика или нарратив обработаны между `Withdrawn` и записью хода повтором ключа (паузы `client.DefaultBackoff` — 200, 400 и 800 мс), либо клиент исчерпал повторы.
- *Последствие — только аналитика.* Доставки игроку ставятся независимо от строки хода: в `consumer.OnCombat` и `OnNarrative` `UPDATE turns` без строк ошибки не даёт, `outbox.Enqueue` выполняется.
  - Клиент повторил ключ. Ход записан заново, но факты уже обработаны (дедуп по `event.id`). Через 60 с — `turn.completed status=timeout` и `turns_failed + 1`, хотя текст доставлен.
  - Клиент не повторил. Хода нет, `turns_count` на 1 меньше, `turn.completed` нет.
- *Почему не «ошибка `OnMechanics`/`OnNarrative`, если хода нет, а корреляция свежая».*
  1. Ошибка откатывает транзакцию consumer'а вместе с `outbox.Enqueue`. После трёх повторов (100, 500 и 2000 мс, C-01) событие уходит в `dead_letters`: игрок теряет текст механики или нарратива ради счётчика аналитики.
  2. Пока идут повторы (до ~2,6 с), следующие события топика ждут у всех игроков.
  3. Свежесть корреляции из события не видна. Те же типы фактов приходят по корреляциям без хода: ответ NPC на `round.closed`, `player.defended`, создание персонажа. Понадобился бы новый набор забранных `event_id` в памяти. Но и он не спасает, если клиент ключ не повторил.
- *Если окно проявится* на Redpanda (T-316) или на стенде (T-392), есть вариант «надгробие» без DLQ (бэклог):
  - `Withdrawn` ставит `received` вместо удаления и уменьшает `turns_count` на 1;
  - `OnMechanics` и `OnNarrative` принимают `received`, переводят ход в свой шаг и увеличивают `turns_count` на 1;
  - повтор ключа (`Accepted`, `ON CONFLICT … DO UPDATE … WHERE status = 'received'`) переводит ход в `accepted`;
  - `Sweep` удаляет просроченный `received` без `turn.completed`.

  Вариант закрывает оба последствия, `CHECK` не меняется.

**Дополнительный вопрос: `500 internal` при сбое записи хода — соответствует КД и C-08.**
- В `api-contracts.md` §1.6 и C-08 нет кода «собственное хранилище шлюза недоступно». `500 internal` — общий код (КД §5.1 п. 2, C-08 v1.5). Тем же кодом путь `Submit` уже отвечает на сбой `Keys.Lookup` и на сбой `Begin` не по шине; оба ответа приняты в T-305 и T-306.
- Гарантии C-08 соблюдены: ничего не опубликовано, ключ не записан, партия удержана. Повтор с тем же ключом публикует тот же `event.id` один раз (`TestATurnThatCannotBeRecordedPublishesNothing`).
- `503 bus_unavailable` был бы неверен: шина исправна. К тому же эталонный клиент повторяет `503` трижды, и каждая попытка шла бы в ту же занятую `gateway.db` (одно соединение, `busy_timeout` 5 с).
- *Оговорка.* Причины обычно преходящие: `SQLITE_BUSY` после 5 с или исчерпанный бюджет решения. А C-08 v1.5 называет `500` дефектом, который клиент не повторяет. Для игрока это ответ «ошибка, повторите»: новая команда идёт с новым ключом, удержанная партия истекает по TTL, номер хода пропускается (так допускает godoc `Begin`). Правка контракта сейчас не нужна.
- **К system-architect, низкий приоритет, после замера T-392.** Если на стенде `500` от занятой `gateway.db` окажется заметным — совместимый код `503 store_unavailable` для преходящих сбоев собственных БД шлюза (`contracts.md` §16 п. 1). Рекомендация — до замера не вводить.

**§9 КД — раунды, для T-350.** `rounds.Accept` вызывается в той же точке, что `Turns.Accepted`. Если шина не подтвердила `player.*`, отметка `acted` не снимается — в отличие от хода. Причина и цена — в КД §9. Предложение tech-lead#3 — строка DoD T-350: unit «действие раунда не подтверждено шиной: `acted` остаётся, повтор ключа не дублирует `acted`, второе действие того же игрока получает `409 already_acted`».

**Итерация 3 разработчику не нужна.** Код итерации 2 соответствует исправленному КД; пунктов к коду нет.

**Проверка слияния КД с T-309.** Сравнивались три версии: база — `599e883` (общая точка T-308 и T-309), наша — `.worktrees/T-308`, их — `.worktrees/T-309`, обе с незакоммиченными правками. `git merge-file` в CRLF и в LF — 0 конфликтов; при обратном порядке аргументов результат побайтно тот же. В результате есть обе строки шапки, правки T-309 в §7.6, §11.2 и §11.4 и правки T-308 в §4.2, §5.5 и §9. Строка шапки T-308 стоит перед первым разделителем `---`, отдельно от строки T-309, чтобы вставки не соприкасались. Файлы проверки лежали в scratchpad (`t308a-*`) и удалены по точным путям.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка (developer#1) | 14.09 01:18 | 14.09 01:55 | 37m |
| Ревью #1 (code-reviewer#3) | 14.09 01:55 | 14.09 02:10 | 15m |
| Итерация 2 (developer#1) | 14.09 02:10 | 14.09 02:35 | 25m |
| Ревью #2 (code-reviewer#3) и решения architect#3 | 14.09 02:35 | 14.09 02:48 | 13m |
| Итерация 3 (developer#1) | 14.09 02:48 | 14.09 02:56 | 8m |
| Приёмка (tech-lead#3) | 14.09 02:56 | 14.09 03:05 | 9m |
| **Всего** | | | **107m** |
