# T-390: Подготовка стенда — токен бота, allowlist, том данных

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I1 / стенд, до 1.9 |
| Размер | S |
| Исполнитель | пользователь + tester#3 |
| Зависит от | — |
| Ветка | нет (стенд, код не меняется) |
| Статус | todo |
| Итераций ревью | 0 |
| Начата / завершена | — / — |
| Метка | нет |

## Описание и DoD
Главный источник — раздел «### T-390» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- Токен — в `.env` оператора; сервису `telegram-bot` он передаётся только через `environment:` (D-10). `make secrets-scan` чист.
- `MV_TELEGRAM_ALLOWED_USER_IDS` заполняет владелец; порты публикуются только на `127.0.0.1`; бот не публикуется в каталогах.

## Выполнение (developer)

## Ревью (code-reviewer)

## Приёмка (tech-lead)

## Учёт времени (оркестратор)
