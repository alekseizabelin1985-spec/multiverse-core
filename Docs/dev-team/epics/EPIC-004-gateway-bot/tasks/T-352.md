# T-352: `groups` — create/join/leave, лидер, `group.entered_region`, `GET /v1/groups/{id}`, предусловия группы

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I2 / 2.1 |
| Размер | M |
| Исполнитель | developer#2 |
| Зависит от | T-305, T-306; EPIC-002 I2 (atomic-группы) |
| Ветка | `task/T-352-groups` (папка `.worktrees/T-352`, от `epic/EPIC-004-gateway-bot`) |
| Статус | todo |
| Итераций ревью | 0 |
| Начата / завершена | — / — |
| Метка | где живёт `cause: group_move` — решение system-architect |

## Описание и DoD
Главный источник — раздел «### T-352» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- Группа, лидерство по BR-13, `leader_id: null`, `409 no_leader`, `202 pending`.
- В `group.entered_region.v1.json` поля `cause` нет, а `group_move` стоит в enum `cause` у других `group.*`; нужен ответ system-architect.

## Выполнение (developer)

## Ревью (code-reviewer)

## Приёмка (tech-lead)

## Учёт времени (оркестратор)
