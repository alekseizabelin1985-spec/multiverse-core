# T-304: `readmodel` (проекция, bootstrap, waiters) и `consumer` (`entity.*`, `encounter.*`)

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I1 / 1.4 |
| Размер | M |
| Исполнитель | developer#1 |
| Зависит от | T-302, T-303 |
| Ветка | `task/T-304-readmodel-consumer` (папка `.worktrees/T-304`, от `epic/EPIC-004-gateway-bot`) |
| Статус | todo |
| Итераций ревью | 0 |
| Начата / завершена | — / — |
| Метка | нет |

## Описание и DoD
Главный источник — раздел «### T-304» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- Проекция State из `testdata/fixtures/snapshots/state/latest.json`, `AwaitFact`, `/health.projection`.
- Consumer четырёх топиков. Событие помечается обработанным только после эффекта: `processed_events` пишется в одной транзакции с эффектом и курсором (C-01 v1.4 `Has`/`Add`, ADR-027). Панику ловит `Delivery` (v1.5); `Close` обработчик не отменяет (v1.7).
- Конец встречи — первое из «факт `resolved`» и «`encounter.ended`» (C-04 v1.3). Срок ожидания ответа — не конец встречи (C-05 п. 1д).

## Выполнение (developer)

## Ревью (code-reviewer)

## Приёмка (tech-lead)

## Учёт времени (оркестратор)
