# T-354: `POST /v1/scopes/{scope_id}/rounds/close` (только `ci`) и `CloseRound` HTTP-обвязки

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I2 / 2.3 |
| Размер | S |
| Исполнитель | developer#1 |
| Зависит от | T-350, T-308 |
| Ветка | `task/T-354-rounds-close` (папка `.worktrees/T-354`, от `epic/EPIC-004-gateway-bot`) |
| Статус | todo |
| Итераций ревью | 0 |
| Начата / завершена | — / — |
| Метка | нет |

## Описание и DoD
Главный источник — раздел «### T-354» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- Служебный маршрут для `actor_kind=ci`; `409 no_open_round`.
- `CloseRound` заменяет заглушку HTTP-обвязки T-308; API `Harness` v0 не меняется.

## Выполнение (developer)

## Ревью (code-reviewer)

## Приёмка (tech-lead)

## Учёт времени (оркестратор)
