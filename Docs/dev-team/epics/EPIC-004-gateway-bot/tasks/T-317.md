# T-317: Документация блока — README gateway и бота, runbook «ротация токена», сверка `.env.example`

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I1 / 1.7 |
| Размер | S |
| Исполнитель | developer#2 (в паре с tech-writer) |
| Зависит от | T-303, T-310 |
| Ветка | `task/T-317-gateway-bot-docs` (папка `.worktrees/T-317`, от `epic/EPIC-004-gateway-bot`) |
| Статус | done |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-13 / 2026-09-13 |
| Метка | правка `Docs/ops/runbook.md` — согласование tech-writer и devops |

## Описание и DoD
Главный источник — раздел «### T-317» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- `internal/gateway/README.md` и `cmd/telegram-bot/README.md`.
- Ротация токена — раздел §6 `Docs/ops/runbook.md`, а не отдельный файл. Токен боту передаётся через `environment:` (D-10), не через `env_file`.

## Выполнение (developer)

*tech-writer#1, 2026-09-13. В паре с developer#2, который в это время занят T-311; вопросы к нему — в отчёте, не в этой карточке.*

Источники читал по дереву (не по КД целиком): статусы задач в
[`../tasks.md`](../tasks.md) (T-301…T-311, T-318, T-310 «Приёмка»), карточка
[`T-318.md`](T-318.md) (раздел «Условия правдивости У-1…У-9»), карточка
`Docs/dev-team/epics/EPIC-001-foundation/tasks/T-463.md` (срок хранения
архивов и копий — статус `todo`), код `internal/gateway/**`,
`cmd/telegram-bot/internal/**`, `shared/env/vars.go`, `.env.example`,
`docker-compose.bot.yml`.

**Сделано:**
- `internal/gateway/README.md` — назначение, пакеты, таблица «что реализовано
  / что нет» по факту дерева (T-301…T-305 есть, T-306…T-309 и
  `shared/testkit/gateway` — нет), как поднять локально на `--bus=memory`
  (требует `--contexts=all`, проверено тестами `cmd/multiverse`) и на kafka
  (`--contexts=gateway`), режимы `live`/`replay` по факту `context.go`
  (в `replay` выключены лимитер и фоновая уборка; полное дочитывание журнала
  — ещё T-309, не описано как готовое), переменные `MV_GATEWAY_*` и смежные,
  состав `/health`, четыре реально смонтированных маршрута
  (`resolveLink`/`consentLink`/`forgetLink`/`postAction` — по
  `api/router.go`), порядок middleware по `api/middleware.go`, публикация
  порта только на `127.0.0.1` (compose-lint правило 2, SEC-13).
- `cmd/telegram-bot/README.md` — явно зафиксировано отсутствие `main.go`
  (T-312 не выполнена): бинарник сейчас не собирается. Таблица готовых
  (T-310: `config`/`access`/`updates`/`sender`/`commands`/`privacy`) и
  отсутствующих (`flow`/`render` — T-311, `deliver`/`main` — T-312) пакетов.
  Получение токена у `@BotFather`, `/setjoingroups` → Disable, allow-list
  (fail-closed, SEC-06), профиль compose `bot` (обе формы запуска),
  переменные `MV_TELEGRAM_*` (с пометкой, что `ACTION_KEY_SALT` и
  `COMMANDS_PER_MIN` не переданы в `docker-compose.bot.yml` — T-463 часть
  «в»), таблица кодов выхода `updates.ExitCode` (0/1/3) и разбор `409
  Conflict`, раздел «Данные игроков» с правилами У-1, У-2, У-4, У-5 из
  карточки T-318, ограничения (приватные чаты, потеря обновления при
  `SIGTERM`, лимиты отказов, публикация порта на `127.0.0.1`).
- `Docs/ops/runbook.md` §6 переписан: статус (T-312 не выполнена — раздел
  описывает целевую процедуру), процедура ротации токена по шагам с
  чек-листом, правила оператора У-1/У-2/У-4/У-5 (ссылки на README и на
  T-318), правила из приёмки T-310 (`/setjoingroups`, потеря обновления при
  `SIGTERM`, лимиты отказов, `bot_denied_total`). Остальные разделы runbook
  (1–5, 7, 8) не менял.
- Сверка `.env.example` ↔ `shared/env/vars.go` для `MV_GATEWAY_*` и
  `MV_TELEGRAM_*`: расхождений нет. `go run ./cmd/mvctl env check` →
  `env check: 74 variables declared, compared with .env.example`, код
  возврата 0.

**Расхождения и открытые условия (не мои, зафиксированы ссылкой, не
исправлял):**
- T-463 (EPIC-001, devops) ещё `todo`: ротация архивов `make backup` и копий
  `links.db`/`gateway.db` старше 30 дней не реализована; часть «в» той же
  задачи (передача `MV_TELEGRAM_ACTION_KEY_SALT`/`MV_TELEGRAM_COMMANDS_PER_MIN`
  в `docker-compose.bot.yml`) тоже не сделана. Runbook и README ссылаются на
  T-463 как на условие, а не как на выполненный факт — иначе текст
  уведомления `/start` (T-318) был бы описан как правдивый преждевременно.
- T-131/T-132 (EPIC-005) не выполнены: `mvctl report` по сессиям живых
  игроков в README и runbook помечен «не запускать до них».

**Не делал:** правку кода, `.env.example`, `contracts.md`, `tasks.md`
эпика, `.claude/*`, `.mcp.json`, `.qwen/*`, `Docs/user-stories/`, `CLAUDE.md`
(запрос — в отчёте). Docker и стенд LLM `127.0.0.1:8888` не трогал, `.env` и
«— копия.env» не открывал. Не коммитил.

## Итерация 2 (tech-writer)

*tech-writer#1, 2026-09-13. Правки по `../review.md`, раздел «T-317 · ревью
#1» (code-reviewer#3). Каждый факт перепроверен по коду дерева (не только
переписан со слов ревью).*

**Ma-1 (`internal/gateway/README.md`, раздел `/health`).** Заменил описание
`links_store`/`gateway_store` на «сейчас всегда `ok`, пока контекст запущен;
настоящая проверка — T-309» со ссылкой на `context.go:460-461` и component
§11.4. Добавил уточнение: ключ `bus` появляется в ответе только со значением
`fail`, `bus: ok` не бывает (подтверждено чтением `Health()`).

**Ma-2 (`cmd/telegram-bot/README.md`, `Docs/ops/runbook.md` §6).** Оба места
переписаны: проверка webhook — **новым** токеном, ввод через `read -rs`
(Git Bash) / `Read-Host -AsSecureString` (PowerShell), конфигурация `curl`
через `-K -` (stdin), токен не попадает в URL, argv или историю оболочки.
Шаг «проверить старым токеном» убран (после `/revoke` он всё равно отвечает
`401`). Добавлено предупреждение не выкладывать вывод `docker compose config`
и `docker inspect telegram-bot`.

**Mi-1…Mi-11, N-1…N-8.** Внесены по тексту замечаний, с проверкой по коду:
- Mi-1 — описание `/forget` исправлено (сжатие в том же вызове, не
  «планируется»; `Retry-After: 5`; порядок сброса отметки) по
  `links/forget.go`, `handlers/links.go:27-31`, `store/retention.go`.
- Mi-2 — `shared/testkit/gateway` убран из таблицы «отсутствует» (`Harness`
  v0 есть); строка `client/` уточнена по факту потребителей.
- Mi-3 — перед командами «Как поднять локально» добавлен явный
  `export MV_GATEWAY_DATA_DIR=...`.
- Mi-4 — ограничение бота по `internal/*` заменено на точное правило
  depguard `cmd-telegram-bot` (`.golangci.yml:385-396`), тест `go list -deps`
  оставлен второй линией.
- Mi-5 — вводная бота приведена к формулировке КД §2 (персистентного
  хранилища нет; диалог и кэш — в памяти бота с TTL, T-311).
- Mi-6 — рядом с примером `PROFILES=memory,bot` добавлено условие У-2 и
  альтернатива `PROFILES=bot` для внешних игроков.
- Mi-7 — перед прямыми командами `docker compose -f ...` добавлен
  `export COMPOSE_ENV_FILES=.env,build/versions.env` со ссылкой на врезку
  runbook.
- Mi-8 — проверка присутствия сервисов переведена на `docker ps --filter
  label=com.docker.compose.project=...` (не зависит от набора `-f`), список
  дополнен `memory` и `narrative-orchestrator`; чек-лист ротации — на
  `make health PROFILES=...,bot` вместо несуществующей на этом дереве
  команды `/telegram-bot health --url ...`.
- Mi-9 — дублирующий хвост §6 (предупреждение `PROFILES=` и повторная
  «Проверка: …») убран; предупреждение `PROFILES=` перенесено в шаг 3
  ротации, с одной ссылкой на раздел 2.
- Mi-10 — шаг 2 и чек-лист ротации дополнены обновлением записи `.env` в
  менеджере паролей и резервных копий `.env` вне репозитория.
- Mi-11 — см. «Сухой прогон runbook §6» и «Согласование с devops» ниже.
- N-1 — проверка «токена нет» в логе бота переведена на `grep -cE
  'bot[0-9]+:[A-Za-z0-9_-]{20,}'` по всему логу контейнера.
- N-2 — шаг 1 ротации нейтрализован («выпустить новый токен; старый
  перестаёт действовать»); формулировка помечена как требующая сверки с
  интерфейсом @BotFather при живом прогоне.
- N-3 — путь `api/gateway.openapi.yaml` уточнён как «в корне репозитория»;
  добавлены задачи I2 (T-352/T-354/T-356).
- N-4 — добавлено исключение Windows (`store/open.go:213-215`) и исправлен
  порядок PRAGMA (`incremental_vacuum` + `wal_checkpoint(TRUNCATE)`,
  `store/compact.go:24-34`).
- N-5 — все пять формулировок исправлены (обращение «ты» убрано, «трекер
  задачи» заменено на явную ссылку на T-309, фраза про логирование
  `request_id`/`code` переформулирована без двусмысленности, ссылка на
  несуществующий «отчёт задачи» заменена на карточку T-463, цитата
  англоязычного комментария compose снята с кавычек).
- N-6 — добавлены кликабельные ссылки на карточку T-318 (оба README и
  runbook) и на КД `gateway-and-bot.md` (runbook).
- N-7 — поле «Ветка» карточки приведено к фактическому имени
  (`task/T-317-gateway-bot-docs`); строка `tasks.md` — вне моих прав правки,
  адресовано оркестратору (см. «Открытые вопросы» отчёта).
- N-8 — во вводную «Данные игроков» (README бота) и в раздел «Правила
  оператора» (runbook) добавлена фраза У-1 про хранение записей в бэкапах
  игры ещё до 30 дней сверх указанных сроков.

### Сухой прогон runbook §6 (Mi-11, часть 1)

Прогон — чтение шагов процедуры ротации против кода и `Makefile`, без
выполнения (нет прод-токена, сеть не трогал):

- Шаг 1 (BotFather `/revoke`/«API Token → Revoke») — действие вне репозитория
  (интерфейс Telegram); по коду не проверяется. Открыто: сверить точную
  формулировку пункта меню с текущим интерфейсом @BotFather при первом живом
  прогоне (T-390 или T-391).
- Шаг 2 (правка `.env`) — пройден: чисто текстовое изменение, не требует
  бинарника; согласуется с `docker-compose.bot.yml` (токен только через
  `environment:`, D-10) и с разделом 4 (`.env` восстанавливается из
  менеджера паролей).
- Шаг 3 (перезапуск `telegram-bot`) — логика `PROFILES=`/`ACTIVE_PROFILES`
  в `Makefile` и выбор compose-файлов проверены чтением, но сам перезапуск
  контейнера не пройден: `cmd/telegram-bot` не собирается (нет `main.go`,
  T-312), `docker-compose.bot.yml` не найдёт `/telegram-bot`. Перенос —
  T-390 «Подготовка стенда», где стенд поднимается с уже собранным ботом.
- Шаг 4 (проверка лога на токен) — шаблон `grep -cE
  'bot[0-9]+:[A-Za-z0-9_-]{20,}'` сверен с `Redact`/`Redactor`
  (`internal/privacy`); само выполнение требует запущенного контейнера —
  перенос туда же, T-390.
- Шаг 5 (проверка webhook) — не зависит от бинарника бота, только от
  валидного токена и сети; не выполнял по ограничению задачи (сетевых
  запросов не делать). Команда и приём (`curl -K -` из stdin) проверены по
  документации `curl` (`-K`/`--config` читает конфигурацию, включая `url =`,
  из файла или stdin при аргументе `-`).
- Чек-лист — пункты «токен в `.env`», «резервные копии `.env`» и «webhook»
  проверяемы без бинарника; пункт `make health` — только после T-312/T-390.

### Согласование с devops (Mi-11, часть 2)

Не выполнено в этой итерации: правка `Docs/ops/runbook.md` (поле «Метка»
карточки) требует согласования tech-writer и devops. Оставляю открытым
пунктом оркестратору/tech-lead#3 — маршрут тот же, что предложил ревьюер
(«через tech-lead#3»); сам с devops не связывался (вне моих полномочий и
вне поручения этой итерации).

**Решение (tech-lead#3 при приёмке, 2026-09-13, по решению оркестратора).**
Отдельное согласование с devops не требуется: runbook §6 проверили два ревью
code-reviewer#3 с поиском утечек (токен в URL, argv, истории оболочки, выводе
`docker compose config`/`docker inspect`), а живую сверку §6 на стенде с
собранным ботом делает T-390 или T-391 — строка DoD внесена в
[`../tasks.md`](../tasks.md), §T-390. Поле «Метка» закрыто этим решением.

### Передать (рекомендации ревьюера — не мои правки)

*Отметка tech-lead#3 при приёмке (2026-09-13):* строка DoD T-312 внесена в
`../tasks.md` (§T-312) в расширенном виде — с учётом строки приёмки T-311 про
README бота и runbook §6, которая уже есть в кончике ветки эпика d1d6331.
Строка DoD T-311 **не внесена**: T-311 принята и слита (d1d6331), а таблицу
статуса README бота (`flow`/`render` готовы) привела к факту сама приёмка
T-317. Строки для `CLAUDE.md` остаются здесь: их вносит оркестратор при
ближайшем контрольном слиянии ветки эпика в `develop`.

Из раздела «Вопросы tech-writer — рекомендация» ревью #1, пункт 2: строки
для `CLAUDE.md` (карта каталогов и «Статус кода») и предложения по DoD
T-311/T-312 — не вносил намеренно (код и `CLAUDE.md` вне зоны правок этой
задачи; `CLAUDE.md` живёт в `develop`, `internal/gateway` — пока в ветке
эпика). Формулировки — дословно из ревью:

- DoD T-312 (добавить строку): «`cmd/telegram-bot/README.md` (статус,
  `/health`, коды выхода из `main`) и `Docs/ops/runbook.md` §6 (снять
  пометку „выполнимо после T-312“, сухой прогон ротации) обновлены».
- DoD T-311 (добавить строку): «таблица статуса `cmd/telegram-bot/README.md`
  — `flow`/`render` готовы».
- `CLAUDE.md`, карта каталогов (при контрольном слиянии I1-α или I1
  EPIC-004 в `develop`, вместе с исправлением устаревшего «`gateway` —
  заглушка» в «Статус кода»):
  - `cmd/telegram-bot/  # Telegram-бот игрока (EPIC-004); main.go — T-312, до неё бинарник не собирается (cmd/telegram-bot/README.md)`
  - `internal/gateway/  # контекст gateway (C-08): links, actions, readmodel, consumer; статус — internal/gateway/README.md`

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#3, TEAM-3) | 2026-09-13 | 0 | 2 | 11 | 8 | вернуть |
| 2 (code-reviewer#3, TEAM-3) | 2026-09-13 | 0 | 0 | 2 | 2 | принять |

Записи — `../review.md`, разделы «T-317 · ревью #1» и «T-317 · ревью #2».

## Приёмка (tech-lead)

**Решение: принято** · 2026-09-13 · tech-lead#3 (TEAM-3, Opus).
Итог: README шлюза и бота и runbook §6 соответствуют DoD и коду кончика ветки эпика d1d6331; четыре новых замечания ревью #2 (Mi-12, Mi-13, N-9, N-10) и открытые N-7 и Mi-11 (часть 2) закрыты при приёмке правкой документов.

### Сверка DoD (`tasks.md` §T-317)

| Пункт DoD | Вывод приёмки |
|---|---|
| Оба README — актуальные списки переменных, совпадают с манифестом `shared/env` (NFR-074) | выполнен: таблицы `MV_GATEWAY_*`/`MV_TELEGRAM_*` сверены ревью #1 и #2 с `shared/env/vars.go`; `go run ./cmd/mvctl env check` в `.worktrees/T-317` — `74 variables declared, compared with .env.example`, код 0. В кончике d1d6331 `shared/env` и `.env.example` не менялись |
| Runbook §6: сухой прогон, пункт «токен нигде не логируется», SEC-08, D-10 | выполнен: пункт и ссылки — шаг 5 и шаг 2; сухой прогон — «Итерация 2», шаги, требующие бинарника, перенесены на T-390 (строка DoD T-390 внесена при приёмке) |
| Условия правдивости У-1, У-2, У-4, У-5 (приёмка T-318) | выполнен: README бота «Данные игроков», runbook «Правила оператора», ссылка на карточку T-318 |
| Документы согласованы с tech-writer; `CLAUDE.md`/`AGENTS.md` — не в задаче | выполнен: документы писал tech-writer#1; строки для `CLAUDE.md` — подраздел «Передать» |
| Публикация портов только на `127.0.0.1`, проверка `compose-lint` (SEC-13) | выполнен: «Ограничения» обоих README |
| `/setjoingroups` → Disable, потеря обновления при `SIGTERM`, лимиты отказов, `bot_denied_total` (приёмка T-310) | выполнен: README бота («Как получить токен», «Allow-list», «Ограничения»), runbook «Другие правила эксплуатации бота» |
| Согласование правки runbook с devops (поле «Метка») | закрыт решением оркестратора — см. «Согласование с devops (Mi-11, часть 2)» выше |

### Закрыто при приёмке (правки tech-lead#3)

| # | Что сделано |
|---|---|
| Mi-12 | В runbook §6 (шаг 3) и в README бота («Коды выхода и `409 Conflict`») — готовые команды для Git Bash и PowerShell, включая `deleteWebhook`. PowerShell: `$s = Read-Host -AsSecureString` → `$t = [System.Net.NetworkCredential]::new('', $s).Password` → `"url = …bot$t/getWebhookInfo…" \| curl.exe -sS -K -` → `Remove-Variable s, t`; явно `curl.exe` и почему (псевдоним `Invoke-WebRequest` в Windows PowerShell 5.1). Команды не запускались |
| Mi-13 | Runbook §6: проверка и снятие webhook — шаг 3, **до** перезапуска (шаг 4); к шагу 4 добавлено, что делать при выходе с кодом 3; проверка лога — шаг 5; чек-лист в порядке шагов. README бота ссылается на шаг 3. Нумерация шагов в «Сухой прогон runbook §6» выше — по редакции итерации 2, до перестановки (там шаг 3 — перезапуск, 4 — лог, 5 — webhook) |
| N-9 | Шаблон в runbook и README — `grep -cE '[0-9]{5,}:[A-Za-z0-9_-]{30,}'`. Проверен в scratch на синтетических строках (не на реальных файлах): совпали строки с `bot<цифры>:<35 символов>` в URL, с голым `<цифры>:<35 символов>` и с `<цифры>:<секрет>` из `_`/`-`; не совпали `bot<redacted>`, отметка времени с адресом `127.0.0.1:8089`, короткое `12345:short`. Старый шаблон `bot[0-9]+:…` на тех же строках находил только первую. Добавлено: ненулевой счёт — ротация, строки не выкладывать |
| N-10 | Runbook §6: `cmd/telegram-bot/internal/updates.ErrUnauthorized`, `…/updates.ErrConflict`, `…/privacy`, `…/render/notice.go` — полными путями. README шлюза: «у нового мира ещё нет снапшота» |
| N-7 | `tasks.md` §T-317: «Ветка» — `task/T-317-gateway-bot-docs` (прежнее имя оставлено пометкой «заменено») |
| Кончик d1d6331 | README бота: `flow` и `render` перенесены в «Готово» (T-311; `DialogTTL` 15 мин и `PlayerTTL` 1 ч, `sender.ReplyPolicy` — по коду `git show epic/EPIC-004-gateway-bot:…`), в «Ещё не реализовано» — только `deliver` и `main.go` (T-312); ссылка на `notice.go` без «появится в T-311». Врезка «Статус» runbook §6 — так же |

### Проверки

- `go run ./cmd/mvctl env check` (корень `.worktrees/T-317`) — 74 переменные, код 0.
- Относительные Markdown-ссылки — скрипт Python в scratch по обоим README, runbook, этой карточке и `tasks.md`: 19 ссылок, битых 0; скрипт удалён по точному пути.
- Слияние с кончиком ветки эпика d1d6331 — трёхсторонний `git merge-file` (LF) в копии scratch `t317acc-merge`, для `review.md` и `dev-log.md` — локальный драйвер `appendtail`; копия удалена по точному пути. Без конфликтов: оба README (в кончике их нет), runbook, эта карточка, `review.md`, `dev-log.md`. **Два конфликта в `tasks.md`**, оба механические: (1) строка «Версия» шапки — оставить `0.1.6 … приёмка T-317`; (2) история версий §9 — оставить обе строки, сначала `0.1.5` (T-311), затем `0.1.6` (T-317). Строки DoD T-312 обеих приёмок сливаются без конфликта. Разрешает оркестратор при слиянии; разработчик не нужен.
- Код, `.env.example`, `CLAUDE.md`, `contracts.md` не менялись; Docker, сеть, стенд LLM, `.env` не трогались; не коммитилось.

### Не входит в T-317 (передано)

- Строки DoD T-312 и T-390 — внесены в `tasks.md`. Строки `CLAUDE.md` — подраздел «Передать», вносит оркестратор.
- Живая сверка поведения Telegram (webhook после `/revoke`, `409` на `getUpdates`, путь меню @BotFather) — T-390 или T-391.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Выполнение | 13.09 21:44 | 13.09 22:05 | 21m |
| Ревью #1 | 13.09 22:05 | 13.09 22:16 | 11m |
| Итерация 2 | 13.09 22:16 | 13.09 22:28 | 12m |
| Ревью #2 | 13.09 22:28 | 13.09 22:40 | 12m |
| Приёмка | 13.09 22:40 | 13.09 22:49 | 9m |
| Слияние | 13.09 22:49 | 13.09 22:49 | 0m |
| **Всего** | | | **65m** |
