# T-305: `actions` — валидация, идемпотентность, лимит, `InputFilter`, публикация `player.*`

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I1 / 1.5 |
| Размер | M |
| Исполнитель | developer#1 |
| Зависит от | T-303, T-304 |
| Ветка | `task/T-305-actions` (папка `.worktrees/T-305`, от `epic/EPIC-004-gateway-bot`) |
| Статус | todo |
| Итераций ревью | 0 |
| Начата / завершена | — / — |
| Метка | `gm.created` при `MV_GM_PATH=legacy` — решение system-architect |

## Описание и DoD
Главный источник — раздел «### T-305» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- Таблица предусловий §1.4, идемпотентность `action_key`, лимит 30/мин на `shared/clock`, `InputFilter` noop, `POST /v1/players/{id}/actions`.
- `enter`/`leave` предлагают только `position`, без `scope` (C-04 v1.2).
- `gm.created` — легаси-тип с издателем `legacy`; публикует ли его шлюз, решает system-architect. Остальная задача от этого решения не зависит.

## Выполнение (developer)

## Ревью (code-reviewer)

## Приёмка (tech-lead)

## Учёт времени (оркестратор)
