# T-356: Прокси `/v1/admin/*` к `MV_CORE_URL`, `actor_kind` `ci/sim`, раздел `admin` в OpenAPI

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I2 / 2.4 (или конец I1) |
| Размер | M |
| Исполнитель | developer#2 |
| Зависит от | T-303 |
| Ветка | `task/T-356-admin-proxy` (папка `.worktrees/T-356`, от `epic/EPIC-004-gateway-bot`) |
| Статус | todo |
| Итераций ревью | 0 |
| Начата / завершена | — / — |
| Метка | раздел `admin` OpenAPI — совладение с EPIC-003 |

## Описание и DoD
Главный источник — раздел «### T-356» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- `httputil.ReverseProxy` к `core` (`env.CoreURL`); `GET /v1/admin/sessions` без внешних ID.
- Допуск по `MV_GATEWAY_ACTOR_KIND_CLIENTS`; в prod-`.env` `ci-harness` нет ни в одном из трёх списков клиентов.

## Выполнение (developer)

## Ревью (code-reviewer)

## Приёмка (tech-lead)

## Учёт времени (оркестратор)
