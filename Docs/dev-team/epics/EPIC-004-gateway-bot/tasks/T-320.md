# T-320: Уведомление об облачном провайдере в боте (US-008)

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-004 / TEAM-3 |
| Инкремент / подволна | I1 / 1.11 |
| Размер | S |
| Исполнитель | developer#2 |
| Зависит от | T-307, T-311; EPIC-003 T-212/T-215 |
| Ветка | `task/T-320-bot-cloud-notice` (папка `.worktrees/T-320`, от `epic/EPIC-004-gateway-bot`) |
| Статус | todo |
| Итераций ревью | 0 |
| Начата / завершена | — / — |
| Метка | нет |

## Описание и DoD
Главный источник — раздел «### T-320» в [`../tasks.md`](../tasks.md): описание, DoD задачи и DoD-common (§0). Карточка DoD не дублирует; если они расходятся, прав индекс. Кратко:
- Проекция `config.cloud_enabled` в gateway отдаётся только через `GET /v1/worlds → worlds[].llm.cloud_enabled`.
- Бот кэширует флаг (`MV_TELEGRAM_CLOUD_FLAG_TTL`, переменная объявляется вместе с кодом) и показывает уведомление один раз за сессию диалога.

## Выполнение (developer)

## Ревью (code-reviewer)

## Приёмка (tech-lead)

## Учёт времени (оркестратор)
