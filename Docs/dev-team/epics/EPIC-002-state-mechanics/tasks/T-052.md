# T-052: I1-2 · Схемы EPIC-002 — ревизия и примеры

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-002 / TEAM-1 |
| Волна | 1 · подволна 1.2 |
| Размер | S |
| Исполнитель | TEAM-1 / developer#2 |
| Статус | done (принята TEAM-1/tech-lead#3 после ревью #1 и итерации 2) |
| Ветка / папка | `task/T-052-schemas-fixtures` от `epic/EPIC-002-state-mechanics` (8768980), `.worktrees/T-052` |
| Итераций ревью | 1 |
| Начата / завершена | 2026-09-13 12:32 / 2026-09-13 13:12 |

> Карточка заведена tech-lead#1 при сверке плана EPIC-002 (2026-09-13). Описание
> и DoD живут в индексе `../tasks.md` и не дублируются здесь, чтобы не завести
> второй источник истины. Ход работы — `../dev-log.md`, замечания — `../review.md`,
> решения — `journal.md`.

## Описание и DoD
См. раздел T-052 в `../tasks.md`.

## Выполнение (developer)
developer#2 (TEAM-1), 2026-09-13. Не закоммичено (по указанию оркестратора).

**Состав.** По `registry.go` у EPIC-002 (`OwnerState`) восемь типов со схемой: `entity.create.proposed`, `entity.update.proposed`, `entity.created`, `entity.updated`, `entity.update.rejected`, `dice.rolled`, `snapshot.created`, `analytics.replay.completed`. Типа `entity.delete.proposed` в реестре и в `schemas/events/` нет — удаление сущности в MVP-1 не предлагается (терминальные статусы вместо удаления), фикстур для него нет.

**Ревизия схем (что уже было на месте, правок не потребовало).**

| Пункт DoD | Где | Итог |
|---|---|---|
| `ops[].op ∈ set\|inc\|append\|remove` | `entity.update.proposed` | есть |
| `expected_version` опционален | `entity.update.proposed` (`required: [entity, ops]`) | есть |
| `reason` — семь значений C-02 v1.4, с `duplicate_entity` и `law_violation`, без `invariant` | `entity.update.rejected` | есть; закреплено тестом |
| `changed[]` для `append` — путь элемента, `old` отсутствует | `entity.updated` (`required: [path, new]`) | есть |
| `cause` с `forget`, `resolve`, `init`, `author` (объединение §2.3.4 и §4.6) | три схемы `entity.*` | есть; сторож — `shared/contracts/ownership_test.go` |
| `component ∈ state\|swarm\|gateway` (C-14 v1.2) | `snapshot.created` | есть; закреплено тестом |
| `mode ∈ recovery\|test`; `events_hash_match` не удалён и не переименован | `analytics.replay.completed` | есть; закреплено тестом. `events_hash_match` и значение `mode=test` (поля EPIC-005) не менялись; `state_hash_before` получил шаблон хеша — уведомление tech-lead#1 и EPIC-005 в `dev-log.md` (итерация 2) |
| `proposal_id` у `entity.create.proposed` (ОВ-21) | — | оставлен **опциональным**: §2.3.4 и C-02 его не называют, описание схемы уже фиксирует «при наличии State возвращает его в `duplicate_entity`»; сделать обязательным — несовместимое сужение, которое без решения владельца контракта не вносится. Окончательно решает tech-lead#1 / system-architect (открытый вопрос в отчёте) |

**Изменено в схемах — шаблон хеша** (решение system-architect#1, ревизия контрактов 4, п. 3): три поля хеша состояния были строками без формы (`snapshot.state_hash` — `minLength: 1`, `replay.state_hash_before` — любая строка, включая пустую, `replay.state_hash_after` — `minLength: 1`). Всем трём задан `pattern: ^sha256:[0-9a-f]{64}$` с описанием — та же форма, что выдаёт `entity.StateHash` (ADR-011 п. 2) и что лежит в `testdata/fixtures/snapshots/state/*.json`. Проверено, что производители уже пишут эту форму: `testkit/state` (`announceRecovery`, указатель снапшота) — `entity.StateHash`; их тесты зелёные без правок.

**Сверка с `api-contracts.md`.**
- §2.3.4 `entity.update.proposed` — пример перенесён в валидную фикстуру дословно по форме (с конкретными `proposal_id` и `event_id` вместо `<uuid>`/`…`, `cause: combat` вместо перечня); валиден. Перечень `cause` в примере §2.3.4 уже, чем enum схемы (нет `leave|flee|death|resolve|init|author`) — это расширение по ОВ-20, пример не противоречит.
- §2.3.4 `entity.created`, `entity.updated`, `entity.update.rejected`, `entity.create.proposed` — поля совпадают; валидные фикстуры построены по ним.
- §2.3.5 `dice.rolled` — `roll{index, formula, seed, result, natural}`, `purpose` (семь значений), `roller{entity}`, `seed` — десятичная строка; совпадает.
- §2.3.12 `snapshot.created` — поля и `component` совпадают; валидная фикстура — метаданные `testdata/fixtures/snapshots/state/latest.json`.
- §2.3.14 / C-14 уточнение v0.4 `analytics.replay.completed` — совпадает.

**Правила, которые проверяет State, а не схема** (записано и в `testdata/fixtures/events/README.md`, и в комментарии `test/fixtures/state_schemas_test.go`):
- **C-02 v1.3** «одна сущность — один набор изменений»: пакет, дважды называющий одну сущность, схема принимает (JSON Schema не требует уникальности по вложенному полю); отвергает State целиком `invalid_op` (T-056).
- **C-02 v1.4** «путь ↔ причина»: пары путь/причина схема проверяет по отдельности; норму поверх `OwnershipRules` проверяет State (T-056).
- **C-02 v1.4** «статус в то же значение»: для схемы это обычный `set`; факт с пустым `changed[]` вместо отказа решает State (T-056); ответ `entity.StatusTransitionAllowed(x, x)`, на который правило опирается, правит T-050.

**Фикстуры** (`testdata/fixtures/events/`, 16 файлов; у каждой невалидной ровно одно нарушение — проверено выводом ошибок валидатора в копии дерева):

| Тип | Невалидная: единственное нарушение | Ошибка валидатора |
|---|---|---|
| `entity.create.proposed` | нет `attributes` | `missing property 'attributes'` |
| `entity.update.proposed` | нет `proposal_id` | `missing property 'proposal_id'` |
| `entity.created` | `version: 0` | `/version: value must be 1` |
| `entity.updated` | нет `changed` у хода без изменений (`cause: combat`; итерация 2) | `missing property 'changed'` |
| `entity.update.rejected` | `reason: "invariant"` | `/reason: value must be one of …` |
| `dice.rolled` | `roll.seed` числом | `/roll/seed: got number, want string` |
| `snapshot.created` | `state_hash` без префикса `sha256:` | `/snapshot/state_hash: does not match pattern` |
| `analytics.replay.completed` | `mode: "replay"` | `/mode: value must be one of 'recovery', 'test'` |

`test/fixtures/events_test.go` не менялся: пары подхватываются по имени, валидные доходят до обработчика, невалидные паркуются в `dead_letters` и проходят через `Lenient()`.

**Тесты.** Новый `test/fixtures/state_schemas_test.go` (по образцу проверок словарей T-215 в `events_test.go`: одна невалидная фикстура не держит целый словарь):
- `TestStateRejectionReasonsAreTheSevenOfC02` — enum `reason` равен семи значениям C-02 v1.4;
- `TestReplayModesAndSnapshotComponents` — `mode` и `component`;
- `TestReplayKeepsTheFieldsOfModeTest` — поля совладения с EPIC-005 на месте;
- `TestStateHashesCarryTheOneForm` — шаблон у трёх полей и совпадение шаблона с выводом `entity.StateHash`.

**Вне карты владения (по указанию оркестратора «поправить тесты»).** `shared/contracts/validate_test.go` (EPIC-001): четыре литерала хеша `"abcdef"`/`"h"` заменены на `sha256:0046…c29e` — в `TestPayloadExamples` (примеры `snapshot.created`, `analytics.replay.completed`, иначе красный) и в двух случаях `TestPayloadRejects` (`unknown snapshot component`, `replay without incomplete_record`), чтобы каждый случай снова отвергался ровно по одной причине. Код и реестр `shared/contracts` не менялись.

**Мутации** (копия дерева в scratch, без `-overlay`; прогон `go test -short -count=1 ./test/fixtures/ ./shared/contracts/`; копия и скрипт удалены по точным путям). Копия без мутаций — зелёная.

| # | Мутант | Итог | Что краснеет |
|---|---|---|---|
| M0 | контрольный: лишнее поле в `entity.created.v1.valid.json` | красный | `TestValidFixturesReachAHandler/entity.created.v1.valid.json` |
| M1 | `attributes` не обязателен | красный | `TestInvalidFixturesAreParkedOnRead/entity.create.proposed.v1.invalid.json` |
| M2 | `proposal_id` не обязателен | красный | `…/entity.update.proposed.v1.invalid.json` |
| M3 | `version: const 1` → `minimum 0` | красный | `…/entity.created.v1.invalid.json`, `TestPayloadRejects/created_with_version_two` |
| M4 | `changed` не обязателен | красный | `…/entity.updated.v1.invalid.json` |
| M5 | `invariant` в enum `reason` | красный | `…/entity.update.rejected.v1.invalid.json`, `TestStateRejectionReasonsAreTheSevenOfC02` |
| M6 | у `seed` снят `type: string` (остался `pattern`) | красный | `…/dice.rolled.v1.invalid.json` |
| M7 | снят шаблон `snapshot.state_hash` | красный | `…/snapshot.created.v1.invalid.json`, `TestStateHashesCarryTheOneForm` |
| M8 | `replay` в enum `mode` | красный | `…/analytics.replay.completed.v1.invalid.json`, `TestReplayModesAndSnapshotComponents` |
| M9 | снят шаблон `state_hash_after` | красный | `TestStateHashesCarryTheOneForm` |
| M10 | снят шаблон `state_hash_before` | красный | `TestStateHashesCarryTheOneForm` |
| M11 | шаблон допускает заглавные hex | красный | `TestStateHashesCarryTheOneForm` |
| M12 | удалён `events_hash_match` | красный | `TestReplayKeepsTheFieldsOfModeTest` |
| M13 | из `reason` убран `duplicate_entity` | красный | `TestStateRejectionReasonsAreTheSevenOfC02` |
| M14 | из `component` убран `gateway` | красный | `TestReplayModesAndSnapshotComponents` |

Первый прогон M0 был загрязнён резервной копией `.orig` в каталоге фикстур (загрузчик отвергает посторонний файл); M0 перезапущен с резервной копией вне дерева — результат в таблице. M1–M14 держали резервную копию в `schemas/events/`, которую никто не читает, — проверено: в каждом краснеет только ожидаемый тест.

**Прогоны** (в `.worktrees/T-052`):
- `go run ./cmd/mvctl contracts check` — `65 types, 8 topics, 58 schema files checked`;
- `go test -short -count=1 ./shared/contracts/... ./test/fixtures/...` — ok, ok;
- `golangci-lint run ./test/... ./shared/contracts/...` — `0 issues`;
- `go build ./...`, `go vet ./test/... ./shared/contracts/...`, `gofmt -l test shared` — чисто;
- `go run ./cmd/mvctl privacy scan testdata/` — `no external identifiers (145 files read)`;
- `make test` — exit 0, 27 пакетов ok, покрытие `internal/mechanics` 94,9 % (без `-race`: нет cgo);
- `git diff --check` — чисто.
Docker, интеграционные прогоны и стенд `:8888` не трогались, `.env` не открывался.

**Отклонения от дизайна.** Нет по схемам. Сверх описания: файл `test/fixtures/state_schemas_test.go` (DoD называет словарь `reason` — одна фикстура его не держит) и правка тестовых литералов `shared/contracts/validate_test.go` (следствие шаблона хеша).

**Предложения в бэклог.**
- `metrics.md` §4.2 (стр. 275) и FR-087 перечисляют в `replay{}` поле `snapshot_at`, которого нет ни в C-14, ни в схеме — сверить (BA / system-analyst).
- Форма `changed[]` для `op: remove` в C-02 не описана: схема требует `new`, `entity.Change` пишет `new: null` — записать в C-02 (`new: null`, `old` — удалённый элемент) до T-055.
- Описание `snapshot.created.v1.json` ссылается на C-14 v1.1 — обновить ссылку при следующей правке схемы.
- `_common.json` — вынести шаблон хеша в `$defs` (`StateHash`) после слияния T-445, который правит `_common.json`.

### Итерация 2 (по ревью #1)

developer#2 (TEAM-1), 2026-09-13. Не закоммичено. Ответ по каждому пункту `../review.md`, «T-052 · ревью #1»:

| # | Замечание | Ответ | Что сделано |
|---|---|---|---|
| Mi-1 | валидная `analytics.replay.completed`: `events_replayed: 42` вместе с `identical: true` и равными хешами противоречит §4.4 | исправлено | Выбран вариант (а) — штатная остановка: `events_replayed: 0`, `state_hash_before == state_hash_after` (реальный хеш снапшота seq 0 `sha256:0046…c29e`), `identical: true`. Вариант (б) потребовал бы выдуманного `state_hash_after`, а (а) проверяем по `testdata/fixtures/snapshots/state/latest.json`. Невалидная пара приведена к тем же значениям и отличается от валидной только `mode: "replay"` (`diff` — одна строка) |
| Mi-2 | README и комментарий теста приписывают EPIC-005 поля `identical`, `state_hash_before` | исправлено | README и комментарий `TestReplayKeepsTheFieldsOfModeTest`: EPIC-005 добавляет только `events_hash_match` и значение `mode=test` (`ownership.md` §1, C-14); `identical` и `state_hash_before` — поля владельца файла, State публикует их в `mode=recovery` (§4.4), харнесс читает в обоих режимах. Текст ошибки теста — без ссылки на EPIC-005. В строке ревизии выше «правок полей EPIC-005 нет» заменено; уведомление tech-lead#1 и EPIC-005 о шаблоне на `state_hash_before` — в `dev-log.md` |
| Nit 3 | ссылки на задачи State разнятся (T-055/T-056, T-050/T-056) | исправлено | Одна форма по `tasks.md`: все три нормы C-02 v1.3/v1.4 проверяет State в **T-056**; ответ `entity.StatusTransitionAllowed(x, x)` правит **T-050** — отдельной фразой. Приведены README, комментарий `state_schemas_test.go`, раздел «Правила, которые проверяет State» этой карточки |
| Nit 4 | `schemaNode` повторяет обход `schemaEnum` | не трогаю (решение оркестратора) | в бэклог — строкой ниже |
| Nit 5 | README: сценарий невалидной `entity.updated` «повторный `/forget`» противоречит `dead_entity` для `abandoned` | исправлено | Сценарий — `set status alive` у живого персонажа в бою, `cause: combat` (C-02 v1.4, T-056 DoD). Фикстура: `cause: combat`, `version: 24`; строка README переписана; в README к норме «статус в то же значение» добавлено, что у терминальной сущности тот же `set` — `dead_entity` |

Строка `entity.updated` в таблице фикстур выше приведена к новому сценарию.

**Вопросы исполнителя (переданы оркестратором system-architect и tech-lead#1, в этой итерации не трогаются):**
- `proposal_id` у `entity.create.proposed` — опционален или обязателен (ОВ-21; ревьюер: без него отказ и дедуп невыразимы, `testkit/state` подставляет `event.id`).
- Форма `changed[]` для `append`/`remove` на проводе и правило переигрывания §4.4 — будет отдельной задачей до T-055.

**Проверки итерации 2** (в `.worktrees/T-052`):
- у каждой из восьми невалидных фикстур ровно одна ошибка валидатора — диагностический тест в копии дерева в scratch (без `-overlay`, копия удалена по точному пути): `missing property 'attributes'` / `'proposal_id'` / `'changed'`, `/version: value must be 1`, `/reason: value must be one of …`, `/roll/seed: got number, want string`, `/snapshot/state_hash: does not match pattern`, `/mode: value must be one of 'recovery', 'test'`;
- `go run ./cmd/mvctl contracts check` — `65 types, 8 topics, 58 schema files checked`;
- `go test -short -count=1 ./shared/contracts/... ./test/fixtures/...` — ok, ok;
- `golangci-lint run ./test/... ./shared/contracts/...` — `0 issues`;
- `go run ./cmd/mvctl privacy scan testdata/` — `no external identifiers in testdata/ (145 files read)`;
- `gofmt -l test`, `git diff --check` — чисто.

**Бэклог итерации 2.**
- `test/fixtures`: выразить `schemaEnum` (`events_test.go`) через `schemaNode` (`state_schemas_test.go`) — один обход на пакет, после слияния T-052 (Nit 4 ревью #1).
- C-02 v1.4: пример «второй `/forget`» в обосновании «статус в то же значение» противоречит `dead_entity` для терминальной сущности — уточнить пример (system-architect; предложение ревьюера).

## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Critical / Major / Minor / Nit | Запись |
|---|---|---|---|---|---|
| 1 | 2026-09-13 | code-reviewer#1 (TEAM-3, помощь TEAM-1) | **принять** | 0 / 0 / 2 / 3 | `../review.md`, «T-052 · ревью #1»: Minor 1 — валидная `analytics.replay.completed` сочетает `events_replayed: 42` с `identical: true` вопреки §4.4; Minor 2 — README и комментарий теста приписывают EPIC-005 поля `identical`/`state_hash_before` вопреки `ownership.md`. Все 8 невалидных фикстур — ровно одно нарушение (проверено ревьюером); мутанты M0/R1/R2 красные |

## Приёмка (tech-lead)
**Решение: принято.** 2026-09-13, tech-lead#3 (TEAM-1). **Итог:** схемы восьми типов EPIC-002 сверены с C-02 v1.4 и C-14 v1.2, у трёх полей хеша шаблон формы `entity.StateHash`, 16 фикстур valid/invalid на месте. Итерация 2 закрыла оба Minor и Nit 3, 5 без новых нарушений. Повторное ревью не требуется.

Приёмка — в `.worktrees/T-052` на незакоммиченном дереве (HEAD `8768980`).

**DoD по пунктам.**

| # | Пункт | Как проверено | Итог |
|---|---|---|---|
| 1 | Фикстуры для всех типов EPIC-002 | 16 файлов `testdata/fixtures/events/{entity.create.proposed,entity.update.proposed,entity.created,entity.updated,entity.update.rejected,dice.rolled,snapshot.created,analytics.replay.completed}.v1.{valid,invalid}.json` (`git status`). Итог README: 35 типов, 70 файлов. Зелёные `TestEventFixturesComeInPairs`, `…BelongToRegisteredTypes`, `TestValidFixturesReachAHandler`, `TestInvalidFixturesAreParkedOnRead`, `TestInvalidFixturesPassWhenValidationOnReadIsOff`; `events_test.go` не менялся | выполнено |
| 2 | У каждой невалидной одно нарушение, названное в README | Зонд tech-lead: копия дерева в scratch, без `-overlay`, удалена по точному пути. Проверены все восемь, в том числе обе, изменённые в итерации 2 (`entity.updated`, `analytics.replay.completed`). `contracts.Validate` выдаёт одну причину: `missing property 'attributes'` / `'proposal_id'` / `'changed'`, `/version: value must be 1`, `/reason: value must be one of …`, `/roll/seed: got number, want string`, `/snapshot/state_hash: … does not match pattern`, `/mode: value must be one of 'recovery', 'test'`. После починки названного нарушения каждый документ валиден (`<nil>`); это и есть контроль, что зонд не пустой. Все восемь валидных — `<nil>`. Строки таблицы README совпадают | выполнено |
| 3 | Сверка с примерами `api-contracts.md` §2.3.4/§2.3.5/§2.3.12/§2.3.14 | Пример `entity.update.proposed` §2.3.4 перенесён в валидную фикстуру (пакет из двух сущностей, `append` в `inventory` с `source`). `entity.updated` для `append`: путь `inventory[0]`, без `old`. `dice.rolled`: `seed` строкой, `purpose` из семи значений. Поля `snapshot.created` — C-14; `component` по C-14, а не §2.3.12. Владение `analytics.replay.completed` по §2.3.14 | выполнено |
| 4 | Запись «C-02 v1.3/v1.4 проверяет State» | README, раздел «Чего схема не проверяет и проверить не может»; комментарий `state_schemas_test.go`; карточка. Все три нормы — T-056, `StatusTransitionAllowed(x, x)` — T-050 | выполнено |
| 5 | Шаблон хеша на трёх полях = `entity.StateHash` | `^sha256:[0-9a-f]{64}$` у `snapshot.state_hash`, `replay.state_hash_before`, `replay.state_hash_after`. Это соответствует `shared/entity/hash.go` (`"sha256:" + hex.EncodeToString`); держит `TestStateHashesCarryTheOneForm` (шаблон в схемах и вывод `StateHash`) | выполнено |
| 6 | `reason` — семь значений C-02 v1.4 без `invariant` (FR-034); `mode`, `component`; `events_hash_match` и `mode=test` не удалены и не переименованы | `TestStateRejectionReasonsAreTheSevenOfC02`, `TestReplayModesAndSnapshotComponents`, `TestReplayKeepsTheFieldsOfModeTest` | выполнено |
| 7 | `make contracts` (часть `contracts check`), `go test ./test/fixtures/...`, линтер, `privacy scan` | прогоны ниже | выполнено |
| 8 | `dev-log.md` заполнен | две записи: основная и итерация 2 | выполнено |

**Итерация 2 (повторного ревью не было — проверено при приёмке).**

| Замечание | Проверка | Итог |
|---|---|---|
| Mi-1: пара `analytics.replay.completed` и КД | Валидная фикстура: `mode: recovery`, `events_replayed: 0`, `state_hash_before == state_hash_after`, `identical: true`, `snapshot_id: state:dark-forest-world:000000`. Это штатная остановка по правилу `identical` в `state-and-mechanics.md` (§4.8 «Протокол восстановления»). Хеш сверен: зонд tech-lead пересчитал `entity.StateHash` по `entities` объекта `testdata/fixtures/snapshots/state/20260101T000000Z-000000.json` (6 сущностей) и получил `sha256:0046b8a7…c29e`. Это совпадает с обоими полями фикстуры и со `snapshot.state_hash` в `latest.json` (§4.4). `id` снапшота тоже совпадает. Невалидная отличается от валидной одной строкой `mode` | закрыто |
| Mi-2: владение полями | README и комментарий `TestReplayKeepsTheFieldsOfModeTest` описывают владение так же, как `ownership.md` §1 (строка 14), C-14 и §2.3.14. За EPIC-005 — `events_hash_match` и значение `mode=test`. `identical` и `state_hash_before` — поля владельца файла (EPIC-002), их публикует State в `mode=recovery`. Текст ошибки теста без EPIC-005 | закрыто |
| Уведомление EPIC-005 | В `dev-log.md` (итерация 2) записано, что `state_hash_before`/`state_hash_after` получили шаблон, а пустая строка теперь невалидна: без снапшота поле опускается. Адресаты — tech-lead#1 и EPIC-005 | записано; доставку адресатам делает оркестратор (см. «Для tech-lead#1») |
| Nit 3: ссылки на задачи | Одна форма в README, комментарии теста и карточке: нормы C-02 v1.3/v1.4 — T-056, `StatusTransitionAllowed(x, x)` — T-050. Совпадает с `tasks.md` (T-050 и T-056) | закрыто |
| Nit 5: сценарий `entity.updated` | Сценарий: `set status alive` у живого персонажа, `cause: combat`, `version: 24`. Для терминальной сущности в README отдельно указан `dead_entity` | закрыто |
| Nit 4 | не правилось по решению оркестратора, вынесено в бэклог | принято |

Неблокирующая неточность (Nit, найдена при приёмке). Комментарий `test/fixtures/state_schemas_test.go:69` ссылается на `state-and-mechanics.md §4.4`, как и строки Mi-1/Mi-2 в карточке и `dev-log.md` (вслед за ревью #1). Но публикация `analytics.replay.completed` и правило `identical` описаны в §4.8 «Протокол восстановления State». §4.4 — это формат `latest.json`. На поведение не влияет; исправить вместе с задачей по Nit 4, которая правит тот же файл (бэклог ниже).

**Для tech-lead#1 (зона EPIC-001).** Вне карты владения T-052 правит `shared/contracts/validate_test.go`. Правка минимальна: заменены только четыре литерала хеша (`"abcdef"`/`"h"` → `sha256:0046…c29e`). Два из них — в `TestPayloadExamples`: без правки тест красный из-за нового шаблона. Два — в `TestPayloadRejects` (`unknown snapshot component`, `replay without incomplete_record`), чтобы каждый случай снова отвергался по своей единственной причине. В первом из них поле `state_hash` перенесено в конец объекта ради длины строки. Код и реестр `shared/contracts` не менялись. Правка следует из решения system-architect#1 (ревизия 4, п. 3) и указания оркестратора. Её нужно учесть при слиянии эпика в `develop`. Там же — уведомление о шаблоне на `state_hash_before` (совладение `analytics.replay.completed` с EPIC-005, `dev-log.md`, итерация 2).

**Прогоны tech-lead** (в `.worktrees/T-052`):
- `go run ./cmd/mvctl contracts check` — `65 types, 8 topics, 58 schema files checked`, exit 0;
- `go test -short -count=1 ./shared/contracts/... ./test/fixtures/... ./shared/testkit/state/... ./shared/entity/...` — ok ×4, exit 0;
- `golangci-lint run ./test/... ./shared/contracts/...` — `0 issues.`;
- `go run ./cmd/mvctl privacy scan testdata/` — `no external identifiers in testdata/ (145 files read)`;
- `go vet ./test/... ./shared/contracts/...`, `gofmt -l test shared/contracts`, `git diff --check` — чисто.

Docker, `make test-integration`, стенд `:8888` и `.env` не трогались.

**Бэклог.**
- Вопросы `proposal_id` у `entity.create.proposed` (ОВ-21) и форма `changed[]` для `append`/`remove` вместе с правилом переигрывания при восстановлении вынесены в **T-448** (EPIC-002, `contract-change`, до T-055, решение — system-architect; `journal.md` 2026-09-13). В T-052 не решаются.
- Остальное остаётся строками бэклога этой карточки и `../review.md` (оркестратор переносит в `plan/backlog.md`):
  1. `metrics.md` §4.2 и FR-087: поле `replay.snapshot_at`, которого нет ни в C-14, ни в схеме — сверить (BA / system-analyst).
  2. Описание `snapshot.created.v1.json` ссылается на C-14 v1.1 — обновить при следующей правке схемы.
  3. `_common.json`: вынести шаблон хеша в `$defs.StateHash` после слияния T-445.
  4. `test/fixtures`: выразить `schemaEnum` через `schemaNode` (Nit 4). Той же правкой исправить ссылку §4.4 → §4.8 в комментарии `state_schemas_test.go:69`. XS, после слияния T-052.
  5. C-02 v1.4: пример «второй `/forget`» в обосновании «статус в то же значение» противоречит `dead_entity` для терминальной сущности — уточнить (system-architect).

**Риск слияния.** Текстовый конфликт `testdata/fixtures/events/README.md` с T-445 (EPIC-001) в двух местах: абзац после «(T-217)» и раздел «Числа». Смыслового противоречия нет, разрешается склейкой абзацев. Если при слиянии в `develop` будет конфликт, его разрешает developer#2 (TEAM-1, автор T-052), а tech-lead#1 проверяет.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 12:32 | 13.09 12:48 | 15m |
| Ревью | 13.09 12:48 | 13.09 13:01 | 12m |
| Разработка 2 | 13.09 13:01 | 13.09 13:04 | 3m |
| Приёмка | 13.09 13:04 | 13.09 13:12 | 8m |
| **Всего** | | | **39m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
