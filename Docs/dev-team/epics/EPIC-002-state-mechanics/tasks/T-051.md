# T-051: `mechanics` — RNG и формулы: сверка и добор тестов

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-002 / TEAM-1 |
| Волна | 1, подволна 1.1 (I1-3) |
| Размер | S (в индексе — M; основное сделано в EPIC-001 T-015, по сверке tech-lead#1 осталась статистика) |
| Исполнитель | developer#1 (Opus) |
| Статус | done (принята TEAM-1/tech-lead#2 после ревью #1 (Minor и Nit закрыты при приёмке)) |
| Итераций ревью | 1 |
| Начата / завершена | 2026-09-13 12:09 / 2026-09-13 12:28 |
| Метка | нет: меняются только тесты `internal/mechanics`, Go-API и контракт C-03 не меняются |

## Описание и DoD
Главный источник — раздел «### T-051» в `../tasks.md`. Основания: `../design.md` §4.1 I1-3 и §9 (unit `mechanics`), `state-and-mechanics.md` §5.3/§5.5, ADR-003 п. 5, ADR-012, NFR-060, C-03.

Сверка tech-lead#1 перед стартом: `Seed`, `NewRNG`, `Roll`, `RollCheck`, `CheckExpr.Eval`, векторы `Seed`, золотые броски, «1000 причин × 4 броска не совпадают», таблицы формул сделаны в T-015; вычислитель формул готов; расхождений с `contracts.md` v0.10 нет; покрытие 94,9 %.

Добрано в этой задаче:
1. Воспроизводимость: 1000 причин × 4 броска, два прохода в разном порядке — совпадение 100 %.
2. χ² по d20: n ≥ 2000, df = 19, порог 43,82 (p = 0,001); тест детерминирован на фиксированных причинах.
3. Корреляция Пирсона между бросками индексов 0 и 1 на 1000 причин: |r| < 0,1.
4. Векторы — inline в тестах (разрешено оркестратором вместо `testdata`).

## Выполнение (developer)
developer#1 (Opus), TEAM-1, ветка `task/T-051-rng-statistics` (от `1c2ee7e`), папка `.worktrees/T-051`. Не закоммичено: коммит делает оркестратор.

### Итог по пунктам DoD

| # | Пункт | Что сделано | Чем проверено | Мутанты |
|---|---|---|---|---|
| 1 | 1000 × 4, два прохода, 100 % | `TestRollReproducibleThousandCausesTwoOrders`. Ход соло-боя по индексам 0..3 (`d20` hit, `2d6+1` damage, `d20` npc_hit, `d4` npc_damage). Проход 1 — причина за причиной, индексы по возрастанию. Проход 2 — на втором наборе правил, индекс за индексом от 3 к 0, причины от последней к первой. Сравнивается весь `Roll` (seed, результат, natural, формула, purpose); требуется ровно 4000 совпадений из 4000 | тест зелёный | S1 — **красный** |
| 2 | χ² по d20 | `TestRollD20ChiSquare`. 2500 причин × 4 индекса `d20` через `Rules.Roll`. χ² против равномерного отдельно по каждому индексу (n = 2500) и по всем вместе (n = 10000); порог 43,82 (df = 19, p = 0,001). Фактические значения: 22,45 / 15,14 / 29,28 / 20,10, общий 13,69 — выводятся `t.Logf` | тест зелёный | D1, D2, D3 — **красные** |
| 3 | Пирсон 0–1, \|r\| < 0,1 | `TestRollIndicesUncorrelated`. 1000 причин × 4 `d20`. Пара 0–1 из DoD и остальные пять пар хода с тем же порогом; NaN (нет дисперсии) — провал. Фактически: 0–1 = +0,0349, остальные от −0,0127 до +0,0182 | тест зелёный | C1, C2 — **красные** |
| 4 | Формулы помощников | `TestStatisticsHelpers`: χ² ровной гистограммы = 0, всё на одной грани = 60 (посчитано вручную); r прямой = 1, обратной = −1, ручной пример = 0,8, константа = NaN. Чтобы зелёный статистический тест не был сломанной формулой | тест зелёный | — |
| 5 | Векторы `Seed` | Уже есть inline: `TestSeedVectors`, `TestGoldenRolls` (T-015). Не менялись | — | — |

### Мутанты
Копия рабочей папки в scratch (`mktemp -d`, `git ls-files -co --exclude-standard` без `services/` и `Docs/`), не `-overlay`. Базовый прогон копии зелёный. Мутации вносил скрипт точной заменой (`assert count == 1`), каждая показана `diff`; после каждой файл восстановлен побайтно (`assert`), в конце `rng.go`, `formula.go`, `rng_stats_test.go` копии сверены `cmp` с рабочей папкой — равны. Копия удалена по сохранённому точному пути.

| # | Мутант | Новые тесты | Прочие красные тесты пакета |
|---|---|---|---|
| M0 | контрольный: `func broken( {` в конце `rng.go` | **красный**: `rng.go:106:14: syntax error` | сборка |
| D1 | `formula.go`: `die := 1 + min(max(rng.IntN(d.Sides+2)-1, 0), d.Sides-1)` — грани 1 и 20 вдвое чаще, среднее 10,5, все грани выпадают | **красный** `TestRollD20ChiSquare`: χ² 203,23 / 192,67 / 210,24 / 270,61, общий 810,68 | `TestRollNdMPlusK`, `TestGoldenRolls`. `TestRollDistribution` (грани и среднее) — **зелёный**: этот сдвиг ловит только χ² |
| D2 | `die := 1 + min(rng.IntN(d.Sides+2), d.Sides-1)` — сдвиг вверх, грань 20 втрое чаще | **красный** χ²: 351,31…447,68, общий 1556,26 | `TestRollNdMPlusK`, `TestGoldenRolls`; `TestRollDistribution` зелёный |
| D3 | после броска `if die == 1 { die = 2 }` | **красный** χ²: 252,51…338,69, общий 1045,75 | `TestRollDistribution`, `TestGoldenRolls` |
| C1 | `rng.go` `Roll`: `Seed(causeEventID, rollIndex&^1)` — индекс 1 бросает на seed индекса 0 (3 — на seed 2) | **красный** `TestRollIndicesUncorrelated`: r(0,1) = r(2,3) = +1,0000. χ² зелёный (общий 43,26) | `TestRollIndicesDiffer`, `TestGoldenRolls` |
| C2 | индекс 1 повторяет индекс 0 примерно у каждой пятой причины (`Seed(cause, 0)%5 == 0`) | **красный**: r(0,1) = +0,2316 | `TestGoldenRolls` (одна строка); `TestRollIndicesDiffer` — **зелёный**: частичную зависимость ловит только корреляция |
| S1 | `NewRNG` несёт состояние: `rand.NewPCG(seed, rngOpened%2)` со счётчиком открытых генераторов | **красный** `TestRollReproducibleThousandCausesTwoOrders`: 2172 из 4000 совпали | `TestRollIsReproducible`, `TestRollNdMPlusK`, `TestRollCheck`, `TestGoldenRolls`, `TestRestore` |

### Изменённые файлы
- `internal/mechanics/rng_stats_test.go` (новый): три теста DoD, `TestStatisticsHelpers`, помощники `chiSquareUniform`, `pearson`, `statCause`, таблица хода `turnRolls`.
- `Docs/dev-team/epics/EPIC-002-state-mechanics/tasks/T-051.md` (эта карточка), `dev-log.md` (новый файл, запись T-051). `tasks.md` эпика не правился: индекс ведёт tech-lead#1.

### Прогоны (go1.26.8 windows/amd64, golangci-lint 2.13.2)
- `go build ./... && go vet ./...` — 0.
- `gofmt -l internal/mechanics` — пусто.
- `go test -short -count=1 ./internal/mechanics/...` — ok; `go test -short -count=1 ./...` — 27 пакетов ok, 0 FAIL.
- `golangci-lint run ./internal/mechanics/...` — 0 issues.
- `make test` — exit 0; `coverage-gate`: `internal/mechanics` 94,9 % (481 из 507), порог 60 %. Покрытие не изменилось: добавлены только тесты. `-race` недоступен (нет cgo).
- Время новых тестов — сотые доли секунды.

### Отклонения от задачи
- Код не менялся: RNG и вычислитель формул уже удовлетворяют DoD, задача — только добор тестов (по сверке tech-lead#1).
- DoD индекса требует векторы `Seed` «в `testdata`». Они inline в `TestSeedVectors`/`TestGoldenRolls` (T-015); оркестратор разрешил inline, перенос не делался.
- Сверх минимума: χ² по каждому индексу и общий, корреляция по всем шести парам хода, а не только 0–1; проверка формул помощников. Порог тот же.

### Предложения в бэклог
1. Строка DoD индекса T-051 про `testdata` расходится с фактом (inline). Поправить формулировку при обновлении индекса (tech-lead#1).
2. Размер T-051 в индексе — M, фактически S. Для истории оценок.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 | 2026-09-13 | 0 | 0 | 1 | 1 | принять |

Ссылка: `../review.md`, раздел «T-051 · ревью #1» (code-reviewer#1, TEAM-1). Minor — комментарий теста корреляции обещает независимость, Пирсон ловит только линейную зависимость (мутант ревьюера P11 проходит пакет); Nit — оговорка о ложном красном при смене выборки причин.

## Приёмка (tech-lead)

tech-lead#2 (TEAM-1), 2026-09-13, модель Opus. **Решение: принята.**

**Итог.** Статистическая часть NFR-060 для RNG `mechanics` закреплена тестами: 4000 из 4000 бросков воспроизводятся в двух порядках, χ² d20 и Пирсон по индексам под порогами. Код пакета не менялся, Go-API и C-03 не затронуты.

Основание: ревью #1 code-reviewer#1 («принять»; Critical 0, Major 0, Minor 1, Nit 1; `../review.md`, «T-051 · ревью #1»). После ревью разработчик больше не дорабатывал задачу: по решению оркестратора Minor и Nit внесены при приёмке (ниже).

**DoD** (уточнён сверкой tech-lead#1, раздел «Описание и DoD»; индекс — раздел T-051 в `../tasks.md`). Коммитов в ветке нет (HEAD `1c2ee7e` = родитель). `git diff HEAD` по отслеживаемым файлам пуст: `rng.go` и `formula.go` не менялись. Новые файлы: `internal/mechanics/rng_stats_test.go`, эта карточка, `../dev-log.md`, `../review.md`.

| # | Пункт | Чем подтверждён |
|---|---|---|
| 1 | 1000 причин × 4 броска, два прохода, 100 % | Прочитан `TestRollReproducibleThousandCausesTwoOrders`. Проход 1: причины 0→999, индексы 0→3. Проход 2: на отдельном `Rules`, индексы 3→0, причины 999→0. Сравнивается весь `Roll`, условие `total == 4000 && matched == total`. Зелёный; мутант S1 исполнителя (состояние в `NewRNG`) его краснит |
| 2 | χ² d20: n ≥ 2000, df = 19, порог 43,82 | `TestRollD20ChiSquare`: 20 корзин, ожидание n/20. χ²₀.₉₉₉(19) = 43,82 — верно. n = 2500 на индекс, 10000 в сумме. Прогон приёмки: 22,45 / 15,14 / 29,28 / 20,10, в сумме 13,69 — совпадает с карточкой и ревью |
| 3 | Пирсон индексов 0–1: \|r\| < 0,1 | `TestRollIndicesUncorrelated`: пара 0–1 и остальные пять пар хода, NaN — провал. Прогон приёмки: r(0,1) = +0,0349, прочие от −0,0127 до +0,0182. `pearson` и `chiSquareUniform` закреплены ручными примерами (`TestStatisticsHelpers`) |
| 4 | Векторы inline вместо `testdata` | `TestSeedVectors`, `TestGoldenRolls` (T-015) на месте, зелёные. Замена разрешена оркестратором |
| — | Вычислитель формул — таблица позитив/негатив (DoD индекса) | Закрыт в T-015 по сверке tech-lead#1: `TestParseCheck`/`TestParseCheckRejects`, `TestParseDice`/`TestParseDiceRejects`, `TestHitTable`, `TestDamageTable`, `TestFleeTable`, `TestRollCheck`/`TestRollCheckRejects` зелёные |
| — | Тесты зелёные, линтер чист | см. «Прогоны приёмки» |
| — | dev-log заполнен | Запись T-051 в `../dev-log.md` есть и совпадает с карточкой |
| — | Карта владения | Только `internal/mechanics/**` (EPIC-002) и артефакты эпика |

Вывод о «статистической независимости» из DoD индекса сужен до того, что проверено на деле: нет линейной зависимости (Пирсон) и маргиналы равномерны (χ²). Уточнённый DoD tech-lead#1 требует именно Пирсона, поэтому это не возврат. Нелинейная проверка — в бэклог (п. 1).

**Замечания ревью — внесены при приёмке** (`internal/mechanics/rng_stats_test.go`, меняются только комментарии и текст одной ошибки, логика тестов прежняя):
- **Minor-1** (комментарии обещали независимость индексов):
  - шапка файла, `:9-13`: вместо «do not follow each other» — «show no linear dependence (Pearson)». Добавлено: Пирсон не доказывает независимость, фиксированная перестановка граней проходит;
  - комментарий `TestRollIndicesUncorrelated`, `:129-137`: вместо «says nothing about» — «shows no linear dependence on», «away from independence» → «away from zero correlation». Добавлен абзац: это не тест независимости, мутант P11 ревьюера (r(0,1) ≈ −0,08) проходит;
  - сверх замечания, чтобы текст ошибки не расходился с комментарием: в сообщении `t.Errorf` `:163` «one roll follows the other» → «… linearly».
- **Nit-1** (оговорка о ложном красном):
  - шапка файла, `:17-23`: пороги — граница поломки только на текущей выборке причин. При смене префикса или числа причин в `statCause` исправный генератор краснит с вероятностью ≈ 1–1,5 % (5 проверок χ² при p = 0,001 и 6 проверок \|r\| при p ≈ 0,0015). Сначала пробовать соседнюю выборку, потом подозревать генератор;
  - комментарий `chiSquareD20Critical`, `:85-89`: дописано, что новая выборка может попасть в эту «одну тысячную» на исправном генераторе, со ссылкой на шапку.

Замечания в `../review.md` оставлены со статусом «открыто»: запись ревьюера не правлю, закрытие зафиксировано здесь.

**Мутанты приёмки** не ставились: логика тестов после правок прежняя (комментарии и текст сообщения). Достаточно мутантов исполнителя (M0, D1–D3, C1, C2, S1 — красные) и ревьюера (M0 красный, P11 — граница метода). Значения χ² и r в прогоне приёмки побайтно совпали с прогоном до правок.

**Прогоны приёмки** (go1.26.8 windows/amd64, golangci-lint 2.13.2, рабочая папка, после правок):
- `go vet ./internal/mechanics/...` — 0;
- `go test -short -count=1 ./internal/mechanics/...` — ok; `-v` по четырём новым тестам — PASS, 0,00–0,02 s;
- `go test -short -count=1 -cover ./internal/mechanics/` — 94,9 %;
- `gofmt -l internal/mechanics` — пусто;
- `golangci-lint run ./internal/mechanics/...` — 0 issues;
- `-race` недоступен (нет cgo). Строки LF в `.go` и карточке сохранены.

**Бэклог.** `../tasks.md` не правился: индекс сейчас ведёт tech-lead#1 в ветке эпика. Отдельные задачи заводит оркестратор.
1. Проверка индексов без ограничения линейной связью, `internal/mechanics`, размер XS: χ² по `(r1 − r0) mod 20` для пары 0–1 (df = 19, порог 43,82, n ≥ 2000). Ловит P11 и любую зависимость вида «r1 = f(r0)» со сдвигом или перестановкой граней. Альтернатива — таблица сопряжённости d20×d20 (df = 361), дороже по выборке.
2. Индекс `../tasks.md`, раздел T-051 (tech-lead#1): в DoD «фиксированные `Seed`-векторы в `testdata`» → «inline в тестах»; размер M → S. Там же сузить «статистически независимы» до «нет линейной корреляции, χ² на равномерность», как в уточнённом DoD.

**Контейнеры и стенд.** Docker и интеграционные тесты не запускались, стенд LLM `:8888` не трогался, `.env` не открывался. Не коммитил.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 12:09 | 13.09 12:18 | 9m |
| Ревью | 13.09 12:18 | 13.09 12:24 | 6m |
| Приёмка | 13.09 12:24 | 13.09 12:28 | 3m |
| **Всего** | | | **19m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
