# Ревью кода EPIC-003 «Рой GM, LLM, законы»

Формат записи: задача, номер итерации, дата, экземпляр ревьюера; границы ревью; вердикт;
проверенные пункты DoD; замечания с серьёзностью (Critical / Major / Minor / Nit) в виде
«файл:строка — суть — предлагаемая правка». Записи добавляются, старые не удаляются.
Язык — русский; код, конфиги и команды — английские.

---

## T-219 · ревью #1 · 2026-09-11 · code-reviewer#2

### Границы ревью

`shared/testkit/swarm/{fake_encounter.go,fake_context.go,fake_encounter_test.go,fake_context_test.go,README.md}`
— изменения из индекса. Задача ревьюировалась **вместе с T-400** (`shared/testkit/gateway`): по
указанию оркестратора это один боевой путь, и порознь он не проверяется. Замечания по харнессу —
в разделе «T-400 · ревью #1» файла `epics/EPIC-001-foundation/review.md`.

Читалось как данность и не переоткрывалось: решения оркестратора от 2026-09-11 —
**«одно действие — ровно одно атомарное предложение» (вариант «б»)**, «директива подавления
линтера заменена именованным правилом», «уровень агента при создании встречи: прав документ,
правка ожидается в итерации», «путь трофея на трупе получил константу». Контракты C-02 v1.3,
C-03 v1.2, C-04 v1.2, C-05 v1.2, `state-and-mechanics.md` v0.3, `internal/mechanics/**`,
`shared/testkit/{state,mechanics}/**` — чтением.

**Метод.** В дереве нет ни одного теста, который сводит заглушку с харнессом: тесты пакета
публикуют действия игрока прямо в шину, `test/e2e` гоняет сценарии **без боя**. Поэтому собран
временный зонд (`membus` + `state.FakeState` + `gateway.Harness` + `swarm.FakeEncounter` на одной
шине), прогнан 60 раз; отдельно смоделирован прицельно случай «все промахнулись» — подобран
идентификатор события, у которого и удар, и ответ падают на промах по таблице `FixedMechanics`.
Границы линтера проверены четырьмя зондами. Все зонды и мутации сняты; дерево сверено
(`git diff` по `shared/`, `internal/`, `test/`, `cmd/`, `.golangci.yml` — пусто).

Заявленные автором 15 убитых подмен из 18 не перепроверялись поштучно — вместо этого сделаны
свои, прицельно в стык между задачами.

### Вердикт

**ВЕРНУТЬ** — Critical 2, Major 1, Minor 4, Nit 1.

Заглушка сделана хорошо и подробно: пакет на сущность, дедуп переизданного действия, проекция
мира по фактам State, три конца встречи, трофей один раз, всё публикуемое проходит валидацию схем.
Замечания не об этом.

Возвращаю за то, что **решение оркестратора «одно действие — ровно одно атомарное предложение»
в коде не выполнено** — и не выполнено дважды: при промахе обоих и при действии, которое заглушка
решила не отрабатывать. Харнесс T-400 построен ровно на этом решении, поэтому боевой путь рвётся:
25 прогонов из 60 падают по таймауту. Более того, обратное поведение **закреплено тестом**
(`TestAMissChangesNothing`), то есть починка потребует не только правки кода, но и снятия стража,
который сегодня охраняет дефект.

Третье — подтверждаю то, о чём оркестратор уже вынес решение: уровень агента при создании встречи
сломает интеграцию при включении проверки владения. Проверено таблицей: это ровно одно
предложение из всех, что публикует заглушка; всё остальное в §4.6 укладывается.

### Замечания по серьёзности

#### Critical

**Cr-1. `fake_encounter.go:770-780` (`propose`) — при обмене, в котором никто не попал, заглушка
не публикует НИЧЕГО, и харнесс упирается в таймаут. Это прямое расхождение с решением
оркестратора от 2026-09-11 (вариант «б»).** Код:

```go
sets := pending.sets()
if len(sets) == 0 {
    e.log.Debug("nothing changed", "event_id", cause.ID, "type", cause.Type)
    return nil
}
```

Решение оркестратора: «каждое разрешённое встречей действие порождает ровно одно атомарное
предложение… Промах не меняет здоровья, но меняет УЧАСТИЕ — а без записи участия раунд невозможно
закрыть ни по „все походили“, ни по порогу пропущенных ходов… Промах расходует ход, и это доменная
правда, а не удобство теста». В коде участие не записывается: сущность встречи после создания не
обновляется вовсе (собственное отклонение 3 в `dev-log.md`), а именно она — тот механизм, которым
решение обосновано.

Доказательство запуском. Прицельно, подобранный идентификатор `probe-53`
(`Verdict(id,0)=miss`, `Verdict(id,2)=miss`):

```
double-miss cause event id: probe-53 (attack=miss, counter=miss)
entity.update.proposed published for the double-miss attack: 0 (decision: exactly 1)
```

На полном боевом пути, 60 прогонов `solo-skirmish`:

```
runs=60 ok=35
timeout: step 4 (attack player-A)  x8
timeout: step 5 (attack player-A)  x9
timeout: step 6 (flee  player-A)   x8
```

Текст отказа харнесса: `nobody resolved the attack of player-A on wolf-alpha within 5s: an
encounter answers every action it takes with one proposal of change, and none arrived`.
Тот же ноль-пакетов у бегства: провал побега без свободной атаки (или со свободной атакой мимо)
изменений не даёт.

Как исправить. Пакет без операций State действительно отвергает, поэтому «публиковать пустое» не
годится — и оркестратор это учёл. Правильный пакет у заглушки есть чем наполнить: §4.6 даёт
уровню `task` право менять сущность встречи по любому пути с `cause=combat|flee`. Достаточно на
каждое разрешённое действие класть в `pending` набор для сущности встречи — `round_seq` и
`participants[].state`/`acted` — и публиковать его вместе с ранениями, когда они есть, и одного —
когда их нет. Это заодно закрывает отклонение 3: сущность встречи перестанет быть замороженной
на момент создания.

**Одновременно снять `fake_encounter_test.go:313` `TestAMissChangesNothing`** — он утверждает
`0 proposals for an exchange that changed nothing`, то есть охраняет ровно то поведение, которое
решением отменено. На его место — тест «промах тоже даёт ровно один пакет, и в нём ход участника».

**Cr-2. `fake_encounter.go:339-369, 963-975, 984-1008` — действие, которое заглушка решила не
отрабатывать, остаётся БЕЗ ЛЮБОГО ответа, и отличить это от «никто не слушает» нельзя.**
Веток четыре, и все выходят с `nil` молча: встреча уже закрыта (`fightOf` → «action outside an
encounter»), целей не осталось (`targetOf` → пусто), вход без региона, действие чужого мира.

Для боевого пути главная — первая. Встреча закрывается, как только волк или игрок упал, а скрипт
`solo-skirmish` (T-400) бьёт дважды и потом бежит. Детерминированно, волк на 1 hp:

```
encounter.ended reason=npc_dead
attack after encounter.ended -> nobody resolved ... within 2s (waited 2s)
flee   after encounter.ended -> nobody resolved ... within 2s (waited 2.001s)
```

DoD задачи требует «0 действий без ответа». Сегодня это не выполняется: действие без ответа —
штатный исход трёх ветвей из четырёх.

Как исправить. Тут не нужен пакет изменения — нужен **явный отказ вместо тишины**. C-05 уже даёт
заглушке право закрыть встречу событием; симметрично ей нужен способ сказать «твоё действие не
принято и не будет». Наименьшая правка, не трогающая контракт: на `player.attacked`/
`player.flee_attempted` вне живой встречи публиковать `combat.decided` невозможно (нет встречи),
поэтому договорённость должна лечь в C-05 отдельным пунктом. До неё — сторона харнесса (Ma-1
ревью T-400): подписка на `encounter.ended` и мгновенный отказ. Прошу оркестратора решить, где
проводить границу; **любое из двух решений закрывает Cr-2, но пока не принято ни одно, сквозной
сценарий не исполним.**

#### Major

**Ma-1. `fake_encounter.go:57, 928-940` — встреча создаётся от имени агента уровня `task`, а §4.6
такого права не даёт: создание встречи принадлежит уровню `domain` с `cause=spawn`.**
Подтверждаю решение оркестратора и уточняю радиус поражения: это **единственное** предложение
заглушки, которое таблица владения отвергнет. Зонд прогоняет бой целиком (вход, четыре удара,
смерть волка с трофеем, бегство) и сверяет каждое предложение с `contracts.OwnershipRules()`:

```
DENY create encounter cause=spawn level=task
     -> no rule of §4.6 lets proposer=task change a encounter with cause=spawn (create=true)
OK   update player   cause=combat level=task paths=[hp]
OK   update npc      cause=combat level=task paths=[died_at hp killed_by loot_claimed_by status]
OK   update player   cause=combat level=task paths=[inventory]
OK   update player   cause=flee   level=task paths=[position]
```

Расхождений два, не одно: у уровня `task` для сущности `encounter` нет ни `Create`, ни причины
`spawn` (разрешены `combat, flee, group, death, resolve`). У уровня `domain` для `encounter` есть
и то и другое.

Как исправить: `encounterAgent` должен отдавать уровень по роли момента. При создании встречи —
`domain` (заглушка в этот момент действует как GM региона), при последующих изменениях — `task`.
Идентификатор агента `fake-encounter:solo:<player>` при этом менять не надо: §4.6 смотрит на
`meta.agent.level`, а не на имя. Заодно `AgentLevelTask` перестанет быть единственной константой
уровня в файле — рядом нужна `AgentLevelDomain`.

#### Minor

**Mi-1. `fake_encounter.go:14-25 — комментарий у импорта `internal/mechanics` устарел и просит
того, что уже сделано.** Он говорит: «the rule of .golangci.yml belongs to EPIC-001, and turning
this line into a named depguard rule is a request to tech-lead#1». Правило `shared-testkit-swarm`
уже введено (`.golangci.yml:89-97`), директивы подавления в коде нет. Комментарий, просящий о
сделанном, следующий автор прочтёт как «правила ещё нет» и заведёт `//nolint` — ровно то, от чего
уходили. Проверено зондами, что правило работает:

```
shared/testkit/zzprobe/probe.go:4 — import internal/mechanics is not allowed from list 'shared':
    shared/* must not import internal/* ... (ADR-001 p. 3)               ← граница цела
shared/testkit/swarm/... — import internal/<другой контекст> is not allowed
    from list 'shared-testkit-swarm': the combat double may see internal/mechanics
    and no other context                                                 ← двойник боя не дотянется
shared/testkit/mechanics/... — то же от 'shared-testkit-mechanics'        ← симметрия сохранена
```

Исключение для `_test.go` в `.golangci.yml:251-254` ограничено текстом
`multiverse-core.io/shared/testkit`, то есть на тестовые файлы этих пакетов правило тоже
распространяется. Граница цела полностью. Как исправить: заменить последние три строки
комментария ссылкой на действующее правило.

**Mi-2. `fake_encounter.go:66-71` — висячий doc-комментарий об `entity.AttrLootClaimedBy`, который
утверждает неправду.** Он не прикреплён ни к какому объявлению (после него сразу идёт блок
`const CauseSpawn…`) и говорит: «It is written out because shared/entity has no constant for it».
Константа есть — `shared/entity/types.go:128` — и код её использует (`fake_encounter.go:745`).
Как исправить: удалить комментарий; открытый вопрос T-017 «где жить константе» закрыт решением 3
оркестратора.

**Mi-3. `fake_context_test.go:83-112` — два негативных теста требуют только «ошибка есть».**
`TestTheContextRefusesToStartTwiceAndWithoutABus` и `TestTheContextFailsOnRulesItCannotRead`
проверяют `err == nil` и не смотрят на текст. Это тот самый класс, который автор T-400 нашёл у
себя и исправил: причин отказа у `Start` четыре (нет шины, уже запущен, не читаются правила, не
читаются фикстуры), и тест, довольный любой из них, останется зелёным, если сломается не та.
Как исправить: `strings.Contains(err.Error(), "no bus in deps")`, `"already started"`,
и для правил — имя пути или слово `rules`.

**Mi-4. `fake_context_test.go:23` — `TestTheContextRunsAWholeFight` боя не ведёт.** Он публикует
единственный вход в регион, ждёт открытия встречи и нарратива и на этом заканчивается: ни одного
`player.attacked`, ни одного `combat.decided`. Имя обещает сквозной бой, и ревьюер, читающий
список тестов, засчитает покрытие, которого нет. Как исправить: либо переименовать в
`TestTheContextOpensAFightAndTellsAboutIt`, либо довести до удара — второе лучше, потому что
именно в `FakeContext` собраны обе половины заглушки.

#### Nit

**N-1. `fake_encounter.go:1113-1118` — детерминированный порядок наборов в пакете объявлен
комментарием («The order is the order the entities were first touched, so that the same scenario
produces the same package on every run»), но ни одним тестом не закреплён.** Мутация «обходить
`c.order` в обратном порядке» оставляет зелёными и `shared/testkit/swarm`, и
`shared/testkit/gateway`, и сквозные тесты. Для сравнения записанных прогонов (NFR-061) порядок
имеет значение. Как исправить: один тест, сравнивающий порядок `changes[]` с порядком, в котором
бойцы получили урон.

### Что проверено экспериментом

| Что | Как | Итог |
|---|---|---|
| «Все промахнулись» | подобран `probe-53`: удар и ответ оба мимо | **0 пакетов**, решение требует 1 (Cr-1) |
| Боевой путь целиком | зонд на одной шине, 60 прогонов `solo-skirmish` | **35 ok / 25 таймаутов** (Cr-1 + Cr-2) |
| Действие после конца встречи | волк на 1 hp | таймаут у `Attack` и `Flee` (Cr-2) |
| Игрок гибнет в том же обмене, что и волк | чтение кода + прогон | **невозможно по построению**: при `out.TargetDead` ответ NPC не разрешается вовсе. Край закреплён тестом автора |
| Игрок гибнет, скрипт идёт дальше | игрок 1 hp, волк 40 hp | `rest` и `leave` отвергнуты с `dead_entity` |
| Владение по §4.6 | сверка каждого предложения с `contracts.OwnershipRules()` | 4 вида предложений в норме, **создание встречи отвергается** (Ma-1) |
| Мутация «два пакета вместо одного» | `propose` публикует пакет на сущность | **убита тремя тестами** пакета — свойство держится, когда менять есть что |
| Мутация «обратный порядок наборов» | `changes.sets()` | **выжила** во всех наборах (N-1) |
| Мутация «предложение отклонено» | `Observe` логирует и не действует | поведение как задумано; заглушка не восстанавливается — согласен |
| Дедуп переизданного действия | тест автора + чтение `Act` | под `e.mu`, до разбора — верно |
| Граница линтера | 4 зонда (другой общий пакет; двойник боя → чужой контекст; двойник механики → чужой контекст; исключение `_test.go`) | **граница цела**, тексты прежние, правило `shared-testkit-swarm` работает |
| Сборка, `vet`, короткие тесты | `go build ./...`, `go vet`, `go test -short -count=1 ./...` | зелено |
| Сквозные тесты | `go test -tags e2e -count=1 ./test/...` | зелено (боя в них нет) |
| Линтер | `golangci-lint run ./...` | 0 issues |
| Контракты | `go run ./cmd/mvctl contracts check` | 65 типов, 8 топиков, 58 схем — ок |
| Детектор гонок | `go test -race` | **не запускался**: нет gcc, cgo недоступен (T-401) |

### DoD задачи — что закрыто и что нет

- [ ] **e2e `solo-30` без роя, 30 ходов, 0 действий без ответа** — не выполнено и сегодня
      невыполнимо (Cr-1, Cr-2). Сквозного набора с харнессом в дереве нет вовсе.
- [x] Смерть NPC, смерть игрока, провал `flee` покрыты тестами; трофей выдаётся один раз.
- [x] Все публикации проходят валидацию схем; `dead_letters` пуст, `contracts.Validate` на каждом
      событии полного боя.
- [x] `FakeContext` реализует `runtime.Context`, стартует без `internal/**`; пакет не зависит от
      `testify` и тестовых пакетов — закреплено обходом импортов, а не обещанием.
- [x] README помечает заглушку как временную с условием снятия (T-242, T-256).
- [ ] Ранний merge — вне ревью, зависит от вердикта.

### Предложения в бэклог

1. Тест-«часовой» на пару задач: один сквозной набор, где харнесс T-400 и заглушка T-219 стоят на
   одной шине. Сегодня их нет ни в одном пакете, и обе задачи были зелёными врозь.
2. Записать в C-05 и вторую половину решения «одно действие — один пакет»: что происходит с
   действием, которое встреча НЕ принимает. Сегодня контракт молчит, и обе стороны молчат по-своему.
3. Сущность встречи как живая запись раунда (`round_seq`, `participants[].state`) — после Cr-1 она
   перестанет быть замороженной, и это стоит зафиксировать отдельным пунктом для агента встречи C4.

---

## T-219 · ревью #2 (итерации 2 и 3) · 2026-09-11 · code-reviewer#2

### Границы ревью

`shared/testkit/swarm/{fake_encounter.go,fake_context.go,fake_encounter_test.go,fake_context_test.go,README.md}`
в коммите `3c628cb`. Ревьюировалось **вместе с T-400** (раздел «T-400 · ревью #2» в
`epics/EPIC-001-foundation/review.md`). Повторное ревью: исправления по ревью #1, повтор при
конфликте версий (итерация 3) и регрессия от них.

Как данность, не переоткрывалось: решения оркестратора от 2026-09-11 — отказ на стороне харнесса,
молчание заглушки на непринятое действие, «одно действие — один пакет», повтор при конфликте,
только для конфликта, до трёх попыток, под тем же идентификатором.

**Метод.** Детерминированный зонд в пакете (S1…S5, `Act`/`Observe` руками, без подписок), стенд
T-400 (120 прогонов), зонды G3 и G5 в `gateway`. Все зонды удалены, дерево сверено
(`git diff HEAD -- shared internal test cmd schemas .golangci.yml` — пусто).

### Вердикт

**ВЕРНУТЬ** — Critical 0, Major 1, Minor 2, Nit 1.

Всё, за что возвращал в ревью #1, закрыто и подтверждено запуском. Боевой путь с харнессом сходится:
0 красных на 130 прогонах. Возвращаю за одно — за новый повтор при конфликте. Он ломается на
**дублирующей доставке отказа**, то есть на той семантике at-least-once (C-01), от которой заглушка
уже защищает действия (`Act` → `e.acted.Seen`, `fake_encounter.go:509`), но не защищает отказы.
Правка маленькая.

### Статус замечаний ревью #1

| Замечание | Статус | Доказательство |
|---|---|---|
| Cr-1 (промах — ноль пакетов) | **закрыто** | зонд S1: «оба мимо» (`sw-24`) → **1** пакет, один набор `encounter-sw-1 [{set round_seq 2}]`; провал бегства без попавшей свободной атаки (`sw-6`) → **1** пакет, тот же набор. `TestAMissChangesNothing` снят, на его месте `TestAMissStillCostsTheRound` |
| Cr-2 (действие без ответа) | **закрыто решением** | отказ живёт у харнесса (T-400); молчание заглушки закреплено `TestAnActionAfterTheFightIsOverIsAnsweredWithSilence`; на стенде 0 таймаутов |
| Ma-1 (создание встречи уровнем `task`) | **закрыто** | `TestEveryProposalPassesTheOwnershipTable -v`: 18 предложений двух прогонов — 18 OK, 0 DENY; создание — `domain`/`spawn`, остальное — `task` |
| Mi-1 (устаревший комментарий импорта) | **закрыто** | ссылается на правило `shared-testkit-swarm`, запрещает `//nolint` |
| Mi-2 (висячий комментарий) | **закрыто** | удалён |
| Mi-3 (негативные тесты без текста) | **закрыто** | `assertRefusal`: `no bus in deps`, `already started`, `load rules`, `seed the world` |
| Mi-4 (`TestTheContextRunsAWholeFight` без боя) | **закрыто** | публикует удар, ждёт `combat.decided` |
| N-1 (порядок наборов без теста) | **закрыто** | `TestThePackageKeepsTheOrderTheFightersWereTouchedIn` |

### Повтор — что держится

- Не второй ответ: кости и `combat.decided` не повторяются. Это тест автора; в G5 на 160 боях решений
  на корреляцию не больше двух.
- Тот же `proposal_id`: G5 — ровно один на действие.
- Три попытки и «gave up» в журнале: S5 — 3 предложения, строка есть.
- Только конфликт: четыре остальные причины не повторяются (тест автора).
- Гонка стенда закрыта: 1 гонка на 160 боёв, пакет применён; 120 прогонов стенда без красных, а
  `standRefusals` требует факт за каждым прощённым конфликтом.

### Замечания по серьёзности

#### Major

**Ma-1 (новое). `fake_encounter.go:1262-1302` (`refused`), `:1286-1296`, `:1308-1315` (`awaiting`) —
дублирующая доставка отказа `version_conflict` запускает повтор пакета, который уже применён, и
представление заглушки расходится с State.** Причины две:
- `enc.pending` снимается только в ветке «сдался»: факт успешного повтора его не гасит;
- сам отказ не дедуплицируется.

Доказательство — зонд S2, детерминированно:

```
after the refusal:            offers=2; offer2 wolf hp=9 pinned v1
[State применил offer2: факт wolf hp 10→9, v2, proposal prop-sw-14]
after the DUPLICATE refusal:  offers=3 (want 2)
offer3 proposal_id=prop-sw-14 (same=true) wolf hp=8 pinned v2   ← урон снят второй раз
next action's package:        wolf pinned v3, wolf hp->3         (State: v2, hp=9)
```

`FakeState` отбрасывает третье предложение молча, как уже применённое (`testkit/state/apply.go:188-191`:
ни факта, ни отказа). Но заглушка уже сложила его в своё представление (`apply`), и оно разошлось с
State. Следующее действие гарантированно проигрывает гонку и тратит попытку. Если фантомный урон
доводит NPC до нуля, `targetOf` вернёт пусто: следующий удар останется без ответа (харнесс — «none
arrived» по сроку), а встреча не закроется.

Дубль подряд, до факта (S2b): один отказ, доставленный дважды, тратит **две** попытки из трёх
(`offers=3`).

Утверждение записи итерации 3 (§9: «дублирующая доставка отказа даст лишнее предложение, но не лишнее
применение») верно для State и неверно для самой заглушки.

Достижимость: на `membus` дубль возможен только после ошибки обработчика; на Kafka — при ребалансе
или перезапуске. `FakeContext` монтируется в процесс ядра поверх настоящей шины (T-255).

Как исправить:
1. В `Observe` при факте с `proposal_id`, равным `pending.proposalID()`, гасить `enc.pending`: пакет
   решён, отвечать на отказ больше не о чем.
2. Дедуплицировать `entity.update.rejected` по `ev.ID` тем же `eventbus.Dedup`, что и действия.

Достаточно п. 1; п. 2 дёшев и закрывает S2b. Тест: отказ → факт повтора → тот же отказ ещё раз ⇒
предложений по-прежнему 2; отказ дважды подряд ⇒ 2.

#### Minor

**Mi-1 (новое). `fake_encounter.go:868-918` (`pack`), `:998-1008` (`wound`), `:792-809` (`finish`) —
повтор пересчитывает смерть, но не пересчитывает конец боя.** Смерть выводится из пересчитанного
здоровья, а исход встречи и `encounter.ended` решены один раз при первой попытке. Зонд S3 (удар не
смертельный, между попытками кто-то оставил волку 1 hp):

```
offer1: wolf hp 10->9, round_seq=2
offer2 wolf-alpha ops=[hp 0, status dead, died_at, killed_by player-A, loot_claimed_by player-A]
offer2 player-A   ops=[append inventory (волчья шкура)]
offer2 encounter  ops=[round_seq 2, participants …]            ← бой не закрыт
encounter.ended published=0, encounter still active in the stub=true
next attack: 0 events published
```

Обратный случай S4 (смертельный удар, волк между попытками восстановлен): `offer2` оставляет волка
на 9 hp, а сущность встречи — `resolved`/`npc_dead`, и `encounter.ended reason=npc_dead` уже
опубликован.

Достижимо, только если NPC пишет кто-то второй между попытками. На соло-стенде этого нет. В фазе 1
есть — двое в одном регионе: `livingNPCOf` даёт обоим одного волка. `TestARetryDoesNotStrikeAFighterWhoHasFallen`
проверяет лишь «не бьют» и «раунд едет» и тем самым зелен над встречей, открытой над трупом.

Как исправить (минимально): если пересчитанный пакет меняет, кто жив, относительно решённого исхода,
пакет не предлагать — это «сдался» с явной строкой «the world moved past the decision». Правило
стоит записать в C-05 вместе с правилом повтора (T-409), потому что настоящему агенту встречи
придётся делать то же.

**Mi-2 (новое). `fake_encounter.go:1286-1296` — «сдался» без отката.** Неприменённая третья попытка
остаётся в представлении заглушки. Зонд S5:

```
gave up logged=true offers=3
next action pins wolf v2 (State: v1, nothing applied), round_seq=3, wolf hp->4 (State hp=10)
```

Следующее действие гарантированно проигрывает гонку, а раунд 2 не будет записан никогда. Как
исправить: `e.rollback(ans)` перед `enc.pending = nil`; тест — к `TestTheStubGivesUpOnAPackageItKeepsLosingWith`
добавить следующий удар и сверить `expected_version`.

#### Nit

**N-1 (новое). `fake_encounter.go:1057-1061` — doc-комментарий `propose` ссылается на `recordTurn`,
такой функции нет: её роль у `turnOps`.** То же имя стоит в таблице мутаций записи итерации 2 в
`dev-log.md`.

### Что проверено экспериментом

| Что | Как | Итог |
|---|---|---|
| «Оба мимо», провал бегства без попадания | S1 | ровно 1 пакет, 1 набор, 1 операция |
| Дублирующий отказ после факта повтора | S2 | **третье предложение, двойной урон, расхождение с State** (Ma-1) |
| Дублирующий отказ подряд | S2b | две попытки на один конфликт (Ma-1) |
| Пересчёт делает удар смертельным / обратно | S3, S4 | смерть без конца боя / конец боя без смерти (Mi-1) |
| После «сдался» | S5 | представление держит неприменённую попытку (Mi-2) |
| Сдавшаяся заглушка против харнесса | G3 (EPIC-001) | 3 отказа, 3 предложения, харнесс кончает сроком с верным текстом |
| Одно действие — один ответ | G5 (EPIC-001), 160 боёв | 0 нарушений |
| Владение §4.6 | `TestEveryProposalPassesTheOwnershipTable -v` | 18 OK, 0 DENY |
| Сборка, `vet`, короткие тесты | `go build`, `go vet`, `go test -short -count=1 ./...` | зелено |
| Сквозные тесты | `go test -tags e2e -count=1 ./test/...` | `ok` |
| Линтер | `golangci-lint run ./...` | 0 issues |
| Контракты | `go run ./cmd/mvctl contracts check` | 65 типов, 8 топиков, 58 схем |
| Покрытие | `go test -cover` | `shared/testkit/swarm` **85,7 %** |
| Детектор гонок | `-race` | **не запускался**: нет cgo (T-401) |

### DoD задачи — что закрыто и что нет

- [~] **e2e `solo-30` без роя, 0 действий без ответа.** 0 действий без ответа — выполнено на стенде
      T-400 (130 прогонов, 2080 боёв). В `test/e2e` боя по-прежнему нет, есть только упоминание в
      комментарии `stubs_v0_test.go:13`. Засчитывать ли стенд за `solo-30` — решение tech-lead на приёмке.
- [x] Смерть NPC, смерть игрока, провал `flee`, трофей один раз — покрыто.
- [x] Все публикации проходят валидацию схем.
- [x] `FakeContext` — `runtime.Context`, без `internal/**` кроме `internal/mechanics` по правилу линтера.
- [x] README с условием снятия.

### Предложения в бэклог

1. C-05 (T-409): к правилу повтора добавить два пункта — повтор гаснет с первым фактом под своим
   `proposal_id` (Ma-1), и повтор, меняющий состав живых, не предлагается (Mi-1).
2. Перечень состояний участника `data-model.md` §3.7 против `shared/entity` — открытый вопрос автора,
   теперь он в публикуемых данных.

## T-219 · ревью #3 (итерация 4) · 2026-09-11 · code-reviewer#2

### Границы ревью

Проиндексированные правки итерации 4: `shared/testkit/swarm/{fake_encounter.go,fake_encounter_test.go,README.md}`
(хеш `fake_encounter.go` — `a911330f`, совпадает с заявленным в `dev-log.md`). Повторное ревью: только
исправления по ревью #2 и регрессия от них. Эталон поведения — C-05 v1.3, п. 1, 1а–1в
(`architecture/contracts.md` §5).

Как данность, не переоткрывалось: решение оркестратора по Mi-1 «не предлагать» (`journal.md`) и зачёт стенда
пары за `solo-30`.

**Метод.** Все зонды и мутанты лежали только во временном каталоге и подключались через `go test -overlay`:
рабочее дерево не менялось ни на одном шаге. После работы временные файлы удалены. Сверка: `git status --short -- shared internal cmd test schemas` показывает
только три проиндексированных файла автора, хеш `fake_encounter.go` прежний.

### Вердикт

**ПРИНЯТЬ** — Critical 0, Major 0, Minor 0, Nit 1.

Ma-1 закрыт, и обе правки нужны: каждая мутация красит свой случай. Mi-1 и Mi-2 закрыты в рамках, которые
C-05 п. 1в прямо разрешает двойнику. Решение «не предлагать» не превращается в тихое «никто не ответил».
Действие, от которого мир ушёл, заканчивается громко с обеих сторон: у заглушки — `Error`, у харнесса — срок
с текстом «not silence on the bus». Его цена — не дефект заглушки, а вопрос к контракту, он ниже.

### Статус замечаний ревью #2

| Замечание | Статус | Доказательство |
|---|---|---|
| Ma-1 (дубль отказа повторяет применённый пакет) | **закрыто** | зонд S2 повторён точно: `offer2 wolf hp=9 pinned v1 proposal=prop-sw-14` → факт повтора → тот же отказ ⇒ `offers=2` (было 3); следующее действие: `wolf pinned v2, decided from hp=9 (State: v2, hp=9)` — представление совпадает с State |
| Ma-1, правка 1 (`settle`) порознь | **нужна** | мутант M1 (без `settle`): S2 зелёный, S2c (отказ того же предложения под новым `event.id` после факта) — **красный**, `3 offers`; тест автора — красный, «the retry State applied was offered again under prop-sw-14» |
| Ma-1, правка 2 (`refusals.Seen`) порознь | **нужна** | мутант M2 (без дедупликации): S2 зелёный, S2b (один отказ дважды до факта) — **красный**, `3 offers`; тест автора — красный, «one lost race delivered twice is one retry, not two» |
| Mi-1 (повтор меняет, кто жив) | **закрыто** (вариант 2 п. 1в) | мутант M4 (без сравнения `kills`) красит подтесты «would now kill» и «would no longer kill»; M5 (труп снова «выбрасывается») — подтест «fell to somebody else». Последствия — ниже |
| Mi-2 («сдался» без отката) | **закрыто** | зонд S5: `gave up logged=true`; следующий удар пинит волка к v1 (как первое предложение) и решается от hp=10 (State 10). Мутант M6 (откат только до лимита) — красный по трём сущностям |
| N-1 (`recordTurn`) | **закрыто** | в `fake_encounter.go` имени нет, doc-комментарии ссылаются на `turnOps` |

### «Не предлагать» — последствия, проверенные запуском

Зонд заглушки (прямые `Act`/`Observe`), три случая после «the world moved past the decision»:

| Случай | Встреча у заглушки | Следующая атака | Бегство | Повторный вход |
|---|---|---|---|---|
| FE: волк упал от чужой руки (удар не смертельный) | открыта, `encounter.ended` 0 | молчание (0 событий) | отвечено: `dice.rolled`, `combat.decided`, пакет; в этом прогоне не удалось, встреча открыта | молчание (игрок уже во встрече) |
| S3: тот же урон теперь убивает | открыта | решена от hp=1, пинит волка к v2 (как State), закрывает бой; `combat.decided round.seq=2`, в State `round_seq` так и остался 1 | — | — |
| S4: смертельный удар больше не убивает | закрыта, `encounter.ended npc_dead` 1 | молчание | молчание | открывает **новую** встречу против живого волка |

Живой зонд харнесса: `Harness` + `FakeState.Apply` + `FakeEncounter` на одной шине, 16 боёв. Подписчик вместо
`FakeState.Start` вставляет чужой смертельный пакет перед пакетом заглушки. В 10 из 16 боёв сработал «мир ушёл
дальше». В каждом из них:
- удар ждёт полный срок (2 с) и кончается текстом «lost the version race and never came back as a fact: this is
  a publisher that ran out of retries and gave up, not silence on the bus (version conflicts: 1…)»;
- следующая атака сразу получает `ErrFightOver` («wolf-alpha is dead»), это C-05 п. 3 на стороне харнесса;
- бегство отвечено за 1 мс.

В 2 из 10 бегство не удалось, и встреча осталась `active` в State над трупом. То же видно и в бою без
повтора (p10): это прежний класс «волк убит в другом месте» (`TestAWolfKilledElsewhereIsNotAttackedAgain`),
итерация 4 его не создала и не расширила.

**Оценка цены.** Для заглушки первой фазы приемлемо и соответствует C-05 п. 1в («двойнику достаточно
второго варианта»). Тихого «никто не ответил» нет: сторона харнесса говорит «not silence», сторона заглушки пишет
`Error` с предложением, сущностью и действием. Неточен лишь диагноз харнесса: он говорит «кончились
повторы», а на деле мир ушёл дальше. Это код T-400, в бэклог. Пункт контракта нужен, но не для заглушки,
а для настоящего агента — см. «Открытые вопросы».

### Соответствие C-05 v1.3, п. 1 и 1а–1в

- п. 1: повторяется только `version_conflict` под тем же `proposal_id`, не более трёх попыток, кости и
  `combat.decided` не повторяются — тесты автора зелёные; «сдавшись, откатывает» — S5 и M6.
- 1а (для издателя): отказ относится к пакету по `proposal_id` — `awaiting(proposal)`; чужой пакет
  (`p-elsewhere`) заглушку не трогает, это тест автора.
- 1б: гашение первым фактом и дедупликация по `event.id` — обе есть, обе необходимы (M1/M2).
- 1в: вариант 2 — не предлагать, запись «мир ушёл дальше решения» — есть (`propose`, лог проверен
  тестом и зондом).

### Замечания по серьёзности

#### Nit

**N-1 (новое). `fake_encounter.go:698-701` (`attacked` → `enc.dealt`, `lastDamager`) — участие не
откатывается вместе с пакетом, от которого мир ушёл (и при «сдался»).** Зонд S3: удар, выброшенный на
повторе, остаётся в `damage_dealt`. Следующий пакет пишет `participants: [{damage_dealt: 4 …}]` = 1
(не дошёл до State) + 3 (этот удар). Это согласуется с решением автора не откатывать счётчик раунда («решение
опубликовано»), и в фазе 1 `damage_dealt` ни на что не влияет. Править не требую. Достаточно одной фразы в
`dev-log` или в doc-комментарии `answer`, что участие, как и раунд, остаётся за решением. Для настоящего агента —
в бэклог вместе с п. 1в.

### Что проверено экспериментом

| Что | Как | Итог |
|---|---|---|
| Зонд ревью #2 точно | S2 через `-overlay` | 2 предложения; следующее действие v2/hp 9 = State |
| Правки Ma-1 порознь | мутанты M1, M2 + S2b, S2c | каждая мутация красит только свой случай и тест автора |
| Мутации Mi-1, Mi-2 | M4, M5, M6 против тестов автора | все красные, с текстом автора |
| Последствия «не предлагать» | зонд FE/S3/S4 (заглушка) + зонд харнесса, 16 боёв | громко с обеих сторон; встреча над трупом — прежний класс; S4 — см. открытый вопрос |
| Стенд пары | `go test -count=60 -run TestTheHarnessAndTheEncounterOfTheSwarmOnOneBus ./shared/testkit/gateway/` | 60 из 60 `PASS`, 0 `FAIL` (960 боёв) |
| Сборка, `vet` | `go build ./...`, `go vet ./...` | зелено |
| Короткие тесты | `go test -short -count=1 ./...` | 27 пакетов `ok` |
| Сквозные | `go test -tags e2e -count=1 ./test/...` | `ok` |
| Линтер | `golangci-lint run ./...` | 0 issues |
| Контракты | `go run ./cmd/mvctl contracts check` | 65 типов, 8 топиков, 58 схем |
| Формат | `gofmt -l shared/testkit/swarm/` | пусто |
| Покрытие | `go test -cover ./shared/testkit/swarm/` | **86,0 %** (заявлено 86,0 %) |
| Детектор гонок | `-race` | **не запускался**: нет cgo (T-401) |

### Открытые вопросы

1. **C-05 п. 1в противоречит сам себе в случае смертельного удара (S4).** Вариант 2 разрешён двойнику, но
   при нём `encounter.ended npc_dead` с убийцей уже опубликован над живым волком, а сущность встречи в State
   навсегда остаётся `active`. Повторный вход открывает вторую встречу. Это ровно «исход, несогласованный со
   здоровьем», который тот же пункт называет недопустимым. Для заглушки это разрешённая цена. Для настоящего
   агента (T-220) решение архитектора — одно из двух: публиковать `encounter.ended` только после факта
   закрывающего пакета, либо явно записать в C-05, что при варианте 2 опубликованный конец боя остаётся и
   чем потребитель его гасит.
2. **Что видит ждущий потребитель при варианте 2.** Отказа нет, есть только срок. Для gateway EPIC-004 это
   ожидание до таймаута раунда при уже доставленном `combat.decided`. Стоит одной строкой записать в C-05 рядом с п. 1в.

### Предложения в бэклог

1. C-05 / T-220: два вопроса выше.
2. T-400 (EPIC-001, харнесс): текст срока при одном конфликте без повтора называет причиной «кончились
   повторы». Точнее — «издатель не ответил на конфликт: исчерпал повторы или мир ушёл дальше решения; смотрите
   лог издателя».
3. Настоящий агент встречи: встреча, чей NPC убит чужой рукой, не закрывается никем (прежний класс, виден и без
   повтора) — это координатор C-04 / T-220.

## T-220 · ревью #1 · 2026-09-11 · code-reviewer#2

### Границы ревью

Проиндексированные правки T-220 в `shared/testkit/swarm/`: `fake_narrator.go` (из `narrator.go`, `git diff --cached -M`),
`fake_narrator_test.go` (из `narrator_test.go`), `fake_narrator_stand_test.go`, `fake_context.go`, `fake_context_test.go`,
`template/ru.go`, `template/ru_test.go`, `README.md`. Эталон — C-05 v1.3 (блок «Заглушка», v1.2 `player.looked → entry`,
«Гарантии»), C-04 (`round.closed`), C-02 v1.2 (`abandoned`), схема `narrative.output.v1.json`, DoD T-220 в `tasks.md`.
Параллельная T-410 (`cmd/multiverse`, `shared/runtime`, `shared/contracts/ownership.go`, `.golangci.yml`) не ревьюилась.
Решение оркестратора о потолке T-018 (для T-220 не действует) и передача вопросов (а)–(г) архитектору не пересматривались.

**Метод.** Зонды и мутанты лежали только в каталоге из `mktemp -d` и подключались через `go test -overlay`. Рабочее
дерево не менялось. После работы каталог удалён целиком по точному пути. Сверка: `fake_narrator.go` и `template/ru.go`
в рабочем дереве побайтно равны индексу (SHA-1 `731297c8…` и `065524a3…`).

### Вердикт

**ПРИНЯТЬ** — Blocker 0, Major 0, Minor 2, Nit 2.

Шесть поводов на пять видов реализованы и держатся тестами. Правило «последнее `combat.decided` обмена» проверено на
всех последовательностях, которые реально публикует `FakeEncounter`, включая повтор после `version_conflict` и «сдался».
Ни хода без текста, ни двойного текста там нет. Оба замечания Minor локальные и дешёвые. Автор назвал оба риска сам,
но не их полную цену: у Mi-1 потеря не оставляет следа вообще.

### 1. Целостность после инцидента с мутациями

- `fake_encounter.go`: SHA-1 содержимого `a911330f694363893b593ff886e7ed1ae6452133` (`sha1sum`, рабочее дерево = индекс),
  совпадает с заявленным и с ревью #3 T-219. Git-хеш блоба — `e0af71c7`: заявленное число — это `sha1sum`, не
  `git hash-object`.
- Все 18 правил таблицы мутаций `dev-log.md` §7 сверены с кодом `fake_narrator.go` построчно, каждое на месте в исходной
  форме: M1 `:582`; M2/M3 `:585`; M4 `diedIn` `:356` (только `StatusDead`); M5 `:312` (`TypePlayer`); M6 `recipientOf`
  `:839`; M7 `:725`; M8 `fake_context.go` `errors.Join(encounter.Err(), narrator.Err())`; M9 `soloPlayer` `:605`; M10
  `:379`; M11 `:315`; M12 `collect` `:549-553`; M13 таблица `narrated` `:125`; M14 в `narrated` нет `player.attacked`/
  `flee_attempted`; M15 `Err` `:285-289`; M16 — таблицы без `<>*_` и обратной кавычки; **M17 — `serves` в `Observe`
  `:299`, восстановлено**; M18 `basedOn` из всех решений `:525-528`.
- Следов мутантов нет: поиск `mutant|mutation|if false|true \|\||false &&|// M[0-9]|TODO|FIXME` по `fake_narrator.go`,
  `template/ru.go`, `fake_context.go` пуст; `gofmt -l` пуст.

### 2. Правило «последнее `combat.decided` обмена» против настоящего издателя

Сверено с `FakeEncounter.attacked`/`fled`/`report`. Удар без смерти всегда даёт ответ `npc_attack`
(`fake_encounter.go:709`). Бегство даёт `free_attack`, только если `!escaped && out.FreeAttack && npcID != ""`
(`:775`). При этом `targetOf` и `livingCount` считают одних и тех же живых NPC (`:1499-1524`), значит `npcID == ""`
⇔ `living_enemies = 0`. Правило нарратора (`:579-590`) совпадает с этим условие в условие, и по
`rules/dark-forest.yaml` `flee.on_fail: free_attack` ветка «`free_attack` не выставлен при живых» сейчас недостижима.

Зонд P3: настоящий `FakeEncounter`, решения скормлены нарратору в порядке топиков.

| Последовательность | Действий с решениями | Ходов | `based_on` |
|---|---|---|---|
| промах, затем промах | 2 | 2 | `[sw-8 sw-11]`, `[sw-15 sw-18]` |
| конфликт версий, повтор принят (предложений 3) | 2 | 2 | по два решения на ход; повтор решений не публикует — второго текста нет |
| три конфликта, заглушка сдалась (предложений 4) | 2 | 2 | то же |
| бегство при волке, убитом чужой рукой | 1 | 1 | `[sw-8]` — одно решение, ход закрыт сразу |

Смерть персонажа от ответа NPC, «волк падает» и «бегство поймано, затем удалось» — тест автора
`TestEveryTurnOfTheFightIsToldOnce`, зелёный.

**Когда ход остаётся незакрытым.** С настоящим издателем это возможно только при сбое его публикации между двумя
решениями. Тогда `FakeEncounter` сам глотает повтор действия (`acted.Seen` до обработки), и второе решение не
появится никогда. Нарратор выбрасывает ход с `Warn` при следующем действии этого персонажа. Это дефект издателя,
а не нарратора. Склеить ход со следующим было бы хуже: текст рассказал бы одно действие ударами другого. Для
заглушки такое поведение допустимо, при условии Nit N-2 ниже.

Цена, которую нарратор не создаёт, но наследует: при «сдался» и «мир ушёл дальше» (T-219, итерация 4) текст хода уже
ушёл, а State удара не принял. Нарратор рассказывает по `combat.decided`, как требует C-05. Это тот же вопрос п. 1в,
он уже у архитектора.

### 3. Дедупликация до публикации — см. Mi-1

### 4. SEC-10 для подставляемых значений — см. N-1

### 5. `death` раньше `turn`

Зонд P2: стенд нарратора (`solo-skirmish`, те же 16 префиксов), порядок `narrative.output` в топике.

| Режим | Боёв | Персонаж погиб | `death` раньше своего `turn` |
|---|---|---|---|
| стенд как есть | 16 | 5 | 0 |
| hp персонажа = 1 | 16 | 15 | 0 |
| hp = 1, подписка нарратора на `game_events` медленнее на 20 мс | 8 | 8 | **8** |

На стенде в обычных условиях инверсии не видно. Достаточно чуть медленнее читать `game_events`, и она воспроизводится
в каждом бою: порядок между топиками не гарантирован (C-01). Для I1-α это допустимо. Факты не расходятся, неудобен
только порядок чтения. Решение уже передано архитектору как (г). Замечанием к T-220 это не считаю. Попутно: ветка
`death` стенда реально исполняется (5 из 16 боёв).

### 6. Тексты (потолок T-018 не проверялся — решение оркестратора)

Двадцать два текста: entry 6, round 4, turn 8 (попал 2, промах 2, убил 2, ушёл 1, пойман 1), death 2, world_event 2.
Буквальных дублей нет. `TestEverySubstitutionIsMade` требует ровно 22 различных вывода. Тексты внутри одной таблицы
различаются формулировкой и набором подстановок, а не только словом, и выбираются по `id` события. Это и есть их
причина. Дублирования без причины нет.

### Замечания по серьёзности

#### Minor

**Mi-1. `fake_narrator.go:315`, `:379` (`told.Seen` до публикации) + `:726-728` (`publish`) — неудачная публикация
нарратива не оставляет следа вообще: нет нарратива, нет `dead_letters`, нет лога, нет `Err()`.** Автор назвал потерю,
но не то, что она полностью беззвучна и что этим путём в обход DLQ уходит и дефект. Доказательство — зонд P1: шина,
которая отказывает публикации `narrative.output`, и `player.looked` на неё.

| Отказ публикации | Попыток публикации | Нарративов | `dead_letters` | `Err()` |
|---|---|---|---|---|
| один раз (сбой брокера) | 1 | 0 | 0 | nil |
| всегда (как при нарушении схемы) | 1 | 0 | 0 | nil |
| контроль: обработчик без окна, всегда ошибка | — | — | **1** | — |

Механизм такой. `Narrate` возвращает ошибку, `Delivery.Deliver` повторяет обработчик (`eventbus/delivery.go:97`).
Повтор натыкается на `told.Seen` и отвечает `nil`, и событие считается обработанным. `Delivery` пишет в лог только при
парковке (`:114`), а нарратор ошибку публикации не логирует. Ссылка на `FakeEncounter` здесь не работает. У встречи
до сбоя уже вышли кости и решения, и повтор их задвоил бы, поэтому запоминание до обработки там — меньшее зло
(ревью T-219). У нарратора побочный эффект один — сама публикация, и запоминание после успеха ничего не задваивает.
Подписка на топик обрабатывает события по одному, гонки нет.

*Как исправить:* запоминать `id` только после успешной `bus.Publish`. Без правки `eventbus.Dedup` это делается так:
на неудаче убрать `id` из локального набора «к повтору» и пропускать `told.Seen`, если `id` в этом наборе. Для хода
буфер обмена при ошибке не удалять. Минимум, если оставить как есть, — `n.log.Error` на неудаче публикации, чтобы
потеря была видна. Тест: шина с однократным отказом `Publish` → после повтора ровно один нарратив, а при постоянном
отказе событие в `dead_letters`. Для настоящего GM EPIC-003 это уже Major — в бэклог C4/T-229.

**Mi-2. `fake_narrator.go:627-630` + `template/ru.go:131-133` — текст «пойман» утверждает удар вдогонку, которого не
было.** Исход `Caught` ставится на любое неудачное бегство. Единственный его текст — «{foe} отрезает путь и бьёт
вдогонку: {taken}». Когда `free_attack` не последовал (`living_enemies = 0`), игрок читает, как мёртвый волк бьёт.
Зонд P3, бегство при волке, убитом чужой рукой, дал текст: «Вася пытается уйти, но Альфа-волк отрезает путь и бьёт
вдогонку: 0. Сил — из —.» Комментарий самого `template/ru.go:110-114` запрещает ровно это: «a text that said "the wolf
bites" over it would be telling the player something that did not happen». Путь достижим, пусть и редок: волк убит
чужой рукой, открытый вопрос (б). *Как исправить:* в `turnText` различать «пойман, удар последовал» и «не ушёл, удара
нет» (например, отдельный исход или вариант текста без удара). Тест: `flight(false, true, 0)` → в тексте нет «бьёт».

#### Nit

**N-1. `dev-log.md` §9 (риск SEC-10), `T-220.md` — неверная ссылка «проверка имён — на стороне gateway (C-08)».** В
C-08 правила об имени нет. Правило есть в `analysis/api-contracts.md` (`name_invalid`: 2–32 символа, буквы, цифры,
пробел, дефис — `*`, `_`, `<` не проходят) и в `components/gateway-and-bot.md` §848/§875. Сам SEC-10 закрывает бот,
который отправляет текст без `parse_mode` (ADR-006 доп. п. 4, ADR-018 п. 4, threat-model SEC-10 — EPIC-004). Имена
регионов и NPC приходят из авторских фикстур, gateway их не проверяет, от них защищает только plain text бота. Вывод
автора верен, основание — нет. Поправить ссылку, код не трогать.

**N-2. `fake_narrator.go:550` — выброшенный незакрытый ход пишется в лог на уровне `Warn`.** Комментарий `collect`
называет это «defect of the publisher». `FakeEncounter` пишет свои дефекты на `Error` («gave up», «world moved past»).
Для единообразия и для поиска по логам стенда — `Error`.

### DoD T-220

| Пункт | Статус | Доказательство |
|---|---|---|
| Все шесть поводов покрыты тестами; `recipients[]` — только игроки scope | закрыт | таблица `dev-log` §1; M6; стенд — все адресаты `player-A` |
| Текст без разметки (SEC-10), тест на `<`, `*`, `_` | закрыт | `TestNoTextNeedsAParseMode` (ещё `>` и обратная кавычка); мутант MR16b красит |
| `narrative_event_id = id` | закрыт | `:725`, M7 |
| Нет логики боя, негатив на `player.attacked` | закрыт | `TestTheNarratorHasNoFightInIt`, M14 |
| Ранний merge в `integration/mvp-1` | **открыт** | вне ревью: коммит и слияние — tech-lead/оркестратор |

### Что проверено экспериментом

| Что | Как | Итог |
|---|---|---|
| Сборка, `vet` | `go build ./...` (T-410 дерево не ломает), `go vet ./shared/testkit/...` | зелено |
| Короткие тесты | `go test -short -count=1 ./shared/testkit/...` | 8 пакетов `ok` |
| Сквозные | `go test -tags e2e -count=1 ./test/...` | `test/e2e`, `test/fixtures` — `ok` |
| Стенд пары | `-count=20 -run TestTheHarnessAndTheEncounterOfTheSwarmOnOneBus ./shared/testkit/gateway/` | **320 из 320** подтестов `PASS`, 0 `FAIL` |
| Стенд нарратора | `-count=20 -run TestTheNarratorTellsTheFightsOfTheStand ./shared/testkit/swarm/` | **320 из 320** `PASS`, 0 `FAIL` |
| Покрытие | `go test -cover` | `swarm` **88,5 %**, `template` **95,8 %** (= заявленному) |
| Линтер | `golangci-lint run ./shared/testkit/...` | 0 issues |
| Контракты | `go run ./cmd/mvctl contracts check` | 65 типов, 8 топиков, 58 схем |
| Формат | `gofmt -l shared/testkit/swarm/` | пусто |
| Мутант MR7: `playersOf` без отсева повторов | `-overlay` | красный: `TestARoundClosedBecomesOneTextForEveryoneInIt` — «recipients are [player-A player-B player-C player-A]» |
| Мутант MR9: ответ NPC закрывает ход только при попадании | `-overlay` | красный: «a miss is answered too» — «after decision 2 of 2: 0 turns told, want 1»; стенд — сроки |
| Мутант MR16b: обратная кавычка в тексте `death` | `-overlay` | красный: `TestNoTextNeedsAParseMode` — «text 11 carries markup» |
| Мутант MR17 (= M1 автора) против **стенда** | `-overlay`, `-count=3` | 51 из 51 `FAIL` — «9 turn narratives, the run is worth 5»: стенд ловит двойной ход сам, не только модульный тест |
| Зонд P1 — отказ публикации | `-overlay` | Mi-1 |
| Зонд P2 — порядок `death`/`turn` | `-overlay`, 40 боёв | §5 |
| Зонд P3 — последовательности настоящего `FakeEncounter` | `-overlay` | §2, Mi-2 |
| Детектор гонок | `-race` | **не запускался**: нет cgo (T-401) |

### Открытые вопросы

Новых к архитектору нет, (а)–(г) уже переданы оркестратором. К (г) добавить данные зонда P2: инверсия 8 из 8 при
задержке нарратора на 20 мс, на обычном стенде 0 из 21 смерти.

### Предложения в бэклог

1. Настоящий GM EPIC-003 (C4, T-229): запоминание события после успешной публикации нарратива (Mi-1 — там Major).
2. `FakeEncounter` (T-219): неудачная публикация тоже не оставляет следа — ошибка не логируется, а повтор глотает
   `acted.Seen`. Хотя бы `log.Error` на пути ошибки `publish`/`report`. Это не T-220.
3. Пункт (в) — явный признак конца обмена в `combat.decided` — снимет с `closesExchange` зависимость от формы обмена.

## T-220 · ревью #2 (итерация 2) · 2026-09-11 · code-reviewer#2

### Границы ревью

Проверены только исправления итерации 2 и регрессия от них. Файлы: `fake_narrator.go` (окно `window`, порядок
`has` → публикация → `add` в `Narrate`/`Observe`, буфер хода в `turn`/`collect`, `log.Error` в `publish`),
`fake_narrator_failure_test.go`, `window_test.go`, `template/ru.go` (исход `Stuck`), dev-log §9 (N-1). Эталон — мои
Mi-1/Mi-2/N-1/N-2 ревью #1, `eventbus.Dedup`/`Delivery`, подписки `membus` и `kafka`, настоящий `FakeEncounter.fled`.
Каталоги T-410 не открывались. Сборку дерева незаконченная T-410 не ломает: `go build ./...` зелёный.

**Метод.** Зонды и мутанты лежали только в каталоге из `mktemp -d` (`/tmp/tmp.X2OcYzXSnt`) и подключались через
`go test -overlay`. После работы каталог удалён целиком по точному пути. Сверка после работы: SHA-1 (`sha1sum`)
`fake_encounter.go` `a911330f…` (= заявленному), `fake_narrator.go` `7c8de95b…`, `template/ru.go` `4ba0efed…`.
Рабочее дерево побайтно равно индексу (`git diff` пуст), неотслеживаемых файлов в `shared/testkit/` нет. Поиск
следов мутантов пуст, `gofmt -l` пуст.

### Вердикт

**ПРИНЯТЬ** — Critical 0, Major 0, Minor 1, Nit 1.

Все четыре пункта ревью #1 закрыты, и каждое заявление автора подтвердил мой собственный прогон. Регрессии в
правиле «последнее решение обмена» нет. Новое замечание Mi-3 касается не кода, а обещания в комментарии и в
dev-log: «запоминание после публикации не может ничего задвоить». Это неверно, и неверно было и моё собственное
утверждение в ревью #1, Mi-1 («запоминание после успеха ничего не задваивает»). Поведение выбрано правильно:
вместо беззвучной потери — возможный дубль. Неверна только формулировка гарантии, а её унаследует T-229.

### Статус замечаний ревью #1

| Пункт | Статус | Доказательство запуском |
|---|---|---|
| Mi-1 — потеря без следа | **закрыт** | зонд P1 (ниже) повторён точно: один отказ → 2 попытки, 1 нарратив, `dead_letters` 0, `ERROR` в логе; постоянный отказ → 4 попытки = `attempts` записи DLQ, 1 запись с исходным `player.looked`, нарративов 0, 4 строки «narrative not published»; мутанты K1, K2, K7, K8 красные |
| Mi-2 — «пойман» без удара | **закрыт** | зонд P7 на настоящем `FakeEncounter`; мутанты K5, K6 красные |
| N-1 — ссылка SEC-10 на C-08 | **закрыт** | dev-log §9: `api-contracts.md` (`name_invalid`) и `gateway-and-bot.md`, «в C-08 этого правила нет», SEC-10 закрыт ботом без `parse_mode`. Устаревшая строка 27 карточки («`eventbus.Dedup`») — см. Mi-3 |
| N-2 — уровень лога выброшенного хода | **закрыт** | `fake_narrator.go:586` — `n.log.Error`; зонд P6 видит строку в логе |

### 1. Порядок «запомнить после публикации» — где он даёт дубль

Зонд P1 и три режима, в которых дубль мог бы появиться.

| Сценарий | Как воспроизведён | Нарративов | Вывод |
|---|---|---|---|
| P1: один отказ `Publish`, через подписку | обёртка над `membus`, `player.looked` в топик | 1 (попыток 2) | как заявлено |
| P1: всегда отказ | то же | 0; DLQ 1, `attempts` 4 = попыток публикации 4 | как заявлено |
| P1: повторная доставка после успеха | `membus.Chaos{Duplicate: true}`: осмотр, удар, ответ волка — каждый в топике дважды | различных `id`: 2 (entry 1, turn 1), `based_on` = `[удар, ответ]` | дубля нет |
| P1: ход, отказ один раз на ответе волка, через подписку | — | 1, `based_on` полный | как заявлено |
| **P4: публикация записалась, но шина вернула ошибку** | `Publish` пишет запись, затем отвечает ошибкой (потерянный ack) | **2**, `id` `pe-2` и `pe-3`, у обоих `causation_id` `pe-1` | **дубль**, см. Mi-3 |
| **P5: две конкурентные доставки одного события** | два `Narrate` одного осмотра, публикация ждёт обоих (барьер) | **2**; то же для закрывающего решения хода — **2 хода** | дубль, но с шинами репозитория недостижим, см. ниже |

**Чем окно защищено от гонки `has` → publish → `add`.** Не мьютексом окна, а тем, что обработчик одного топика не
вызывается конкурентно. `membus` держит курсор группы через всю `Deliver` (`membus.go:287-309`). `kafka` читает одним
циклом `FetchMessage` → `deliver` → `CommitMessages` (`kafka.go:187-205`). `Delivery` повторяет обработчик
последовательно (`delivery.go:97-113`). Второй `Start` отвергается (`fake_narrator.go:253-256`), а `FakeContext`
создаёт один нарратор (`fake_context.go:149`). `id` принадлежит одному топику, а `Narrate` (три топика) и
`Observe` (`system_events`) работают с разными `id`. Значит, P5 достижим только прямым конкурентным вызовом
экспортированного обработчика. В итерации 1 атомарный `Dedup.Seen` это покрывал, итерация 2 — нет. Предусловие
записано в комментарии `window` (`:819-820`), но не у экспортированных `Narrate`/`Observe`.

**P4 реален для настоящего брокера.** Ошибка `WriteMessages` по таймауту после того, как брокер принял запись, и
есть потерянный ack. Шлюз гасит дубли по `id` события (`narrative_event_id` = `id`, C-05 v1.1), а у повторной
публикации `id` новый: `Derive` всегда берёт `nextID()` (`types.go:182`), опции задать `id` нет. Игрок увидит текст
дважды. Для заглушки на `membus` путь почти недостижим: запись атомарна, разве что в режиме `Chaos.Duplicate`. Но это
цена at-least-once, и её надо назвать, а не отрицать.

### 2. Буфер хода при постоянном отказе

Зонд P6 через подписку. Действие A (удар и ответ волка): публикация хода всегда отказывает, ответ волка уходит в DLQ.
Затем отказы выключены, и действие B (промах и ответ волка) рассказано. Итог: DLQ 1 (ответ волка A); нарративов 1,
`based_on` = ровно `[удар B, ответ B]`. Решений A в нём нет, склейки нет. В логе `ERROR` «dropped untold». Текст B:
«Удар уходит в пустоту. Альфа-волк отвечает — 1; Вася держится: 7 из 10.»

Внутренний зонд, длинный бой: 11 000 действий подряд, текст каждого хода отказан (4 вызова, как у `Delivery`).
`exchanges` = 1 запись с 2 решениями, окно = 10 000 (ёмкость). Памяти не течёт: буфер — не больше одного хода на
персонажа, и он заменяется при следующем действии. Остаётся только запись персонажа, который больше не
действует (сбежал или погиб). Это одна запись на персонажа, для заглушки допустимо.

Попутно: для этого случая сообщение лога неточно — см. N-3.

### 3. Своё окно рядом с `eventbus.Dedup`

| | `Dedup` | `window` |
|---|---|---|
| Ёмкость | 10 000 (`DefaultDedupCapacity`), `≤0` → по умолчанию | та же константа; `newWindow(0)` паникует (`index out of range`), но единственный вызов — с константой |
| Вытеснение | LRU: повторное попадание освежает | FIFO по времени ответа: `has` не освежает |
| Снапшот C-14 | `IDs`/`Restore` | нет |

Зонд: после `a, b, c, (дубль a), d` у `Dedup` остаются `[c a d]`, у окна — `b c d`. Для заглушки разница несущественна:
повтор приходит сразу, в пределах `Backoff` и 10 000 событий. Нарратор не stateful-контекст C-14, снапшот ему не нужен.
**Вывод:** для T-220 это нормальная локальная реализация, отдельный вопрос ради неё не нужен. Вопрос о
двухшаговом API `Dedup` оркестратор уже отправил архитектору (journal). По существу он нужен T-229: настоящему GM
понадобятся и два шага, и снапшот, и тогда второй механизм без `IDs`/`Restore` станет дефектом. К вопросу стоит
приложить P4: двухшаговый `Dedup` дубль при потерянном ack не лечит. Лечит его детерминированный `id` нарратива от
причины (например, `id` причины + вид), а это тоже изменение `shared/eventbus` (`Derive`) — в ту же пачку.

### 4. `Stuck` против настоящего `FakeEncounter`

Зонд P7: настоящий `FakeEncounter` (`fightStubWith`), волк убит чужой рукой через `enc.Observe(died(wolfID))`.
Решения скормлены нарратору в порядке топиков.

| Случай | Что опубликовал `FakeEncounter` | Текст хода |
|---|---|---|
| волк мёртв, бегство не удалось | `flee(free_attack, living_enemies=0)`, ответа нет | «Вася пытается уйти, но тропа петляет и выводит обратно, туда же. Уйти не вышло; сил — из —.» — ни удара, ни волка |
| волк жив, бегство не удалось | `flee(free_attack, living=1)` + `free_attack` | «…но Альфа-волк отрезает путь и бьёт вдогонку: 0. Сил 10 из 10.» — удар есть (промах удара вдогонку — число 0, по правилу таблицы) |
| волк мёртв, бегство удалось | `flee(living=0)` | «Вася вырывается из схватки и уходит в туман. Альфа-волк остаётся позади.» — ложного действия нет |

Настоящий издатель при `living_enemies = 0` ставит `free_attack`, так что ветку `Stuck` здесь держит именно условие
`LivingEnemies == 0` в `closesExchange`, а тест `flight(false, true, 0)` моделирует ровно эту форму.

### 5. Регрессия итерации 1

Тесты правила «последнее решение обмена» (`TestEveryTurnOfTheFightIsToldOnce`,
`TestATurnIsToldAfterItsLastDecision`, `TestATurnNeverClosedIsDroppedUntold`) зелёные, оба стенда — 320/320. Мои
мутанты ревью #1 повторены на новом коде, все красные:

| Мутант | Что снято | Кто покраснел |
|---|---|---|
| MR7 | `playersOf` без отсева повторов (`:725`) | `TestARoundClosedBecomesOneTextForEveryoneInIt` — «recipients are [player-A player-B player-C player-A]» |
| MR9 | ответ NPC закрывает ход только при попадании (`:625`) | стенд нарратора — «timed out waiting for the narratives of the fight» (fight-00, 02, 04…) |
| MR16b | обратная кавычка в тексте `death` (`ru.go:147`) | `TestNoTextNeedsAParseMode` — «text 10 carries markup» |

Свои мутанты итерации 2 (независимо от I1–I7 автора):

| Мутант | Что снято | Кто покраснел |
|---|---|---|
| K1 | `Narrate` запоминает и на ошибке | `…FailedOnceIsToldAfterTheRetry`, `…AlwaysFailsEndsInDeadLetters`, `…TurnWhosePublicationFailed…` |
| K2 | `Narrate` не запоминает никогда | `TestAnEventDeliveredTwiceIsToldOnce` — «3 narratives…», `…TurnWhosePublicationFailed…` — «2 narratives» |
| K3 | повторное последнее решение собирается дважды (`:594`) | «based on [sw-2 sw-3 sw-3]» |
| K4 | ход убирается из буфера до публикации (`:600`) | «based on [sw-3], want the whole exchange» |
| K5 | ответ не переводит `Stuck` в `Caught` (`:687`) | `…ClaimsNoBlow/caught_and_struck`, `…TurnIsToldAfterItsLastDecision/a_caught_flight…` |
| K6 | неудачное бегство сразу `Caught` (`:674`) | `…ClaimsNoBlow/nobody_left_standing`, `/the_rules_call_for_no_strike` |
| K7 | `Observe` не запоминает смерть (`:329`) | «2 deaths told for one death refused once…» |
| K8 | отказ публикации пишется на `Debug` (`:778`) | «the failed publication left no error in the log» |

### Замечания по серьёзности

#### Minor

**Mi-3. `fake_narrator.go:179-184`, `:815-820`; `dev-log.md` «T-220 · итерация 2» («Дублей не бывает»); карточка
`T-220.md:27` — задокументирована гарантия «запоминание после публикации ничего не задваивает», которой нет.**
Доказательства — зонды P4 и P5 выше. Потерянный ack: 2 нарратива с разными `id` на одну причину, и шлюз по `id` их не
склеит. Конкурентная доставка одного события: 2 нарратива, 2 хода. Поведение правильное: для заглушки дубль лучше
беззвучной потери, и P5 с шинами репозитория недостижим. Но комментарий `told` отрицает дубль, а комментарий `window`
выдаёт предусловие («подписка отдаёт топик по одному событию») за свойство кода. T-229 возьмёт этот образец. То же
неверное утверждение было и в моём ревью #1, Mi-1 («запоминание после успеха ничего не задваивает»). Настоящим
исправляю его.
*Как исправить* (код не менять):
- в комментарии `told` вместо «cannot write anything twice» написать, что поздняя запись меняет потерю на
  возможный дубль: публикация, чья ошибка пришла после записи, будет рассказана повторно под новым `id`;
- у `Narrate`/`Observe` добавить предусловие: не вызывать конкурентно для одного события, так их вызывает шина;
- поправить строку dev-log «Дублей не бывает» и строку 27 карточки (`eventbus.Dedup` → окно `window`).

Тест не нужен. P4 для T-229 — в бэклог (ниже).

#### Nit

**N-3. `fake_narrator.go:586-587` и комментарий `collect` `:571-576` — ход, публикация которого окончательно
отказала, выбрасывается с сообщением «a turn was never closed» и объяснением «defect of the publisher».** Зонд P6: ход A
был закрыт, его ответ волка уже в `dead_letters`, а лог говорит, что он не закрывался, и винит издателя решений.
При разборе стенда это уводит не туда. *Как исправить:* различать по `closesExchange` последнего решения буфера.
Если ход закрыт, сообщение «a told turn whose publication failed is dropped (its last decision is in dead_letters)»;
иначе прежнее.

### Что проверено экспериментом

| Что | Как | Итог |
|---|---|---|
| Сборка, `vet` | `go build ./...`, `go vet ./shared/testkit/...` | зелено (T-410 дерево не ломает) |
| Короткие тесты | `go test -short -count=1 ./shared/testkit/...` | 8 пакетов `ok` |
| Сквозные | `go test -tags e2e -count=1 ./test/...` | `test/e2e`, `test/fixtures` — `ok` |
| Стенд пары | `-count=20 -run TestTheHarnessAndTheEncounterOfTheSwarmOnOneBus ./shared/testkit/gateway/` | **320 из 320** подтестов `PASS`, 0 `FAIL` |
| Стенд нарратора | `-count=20 -run TestTheNarratorTellsTheFightsOfTheStand ./shared/testkit/swarm/` | **320 из 320** `PASS`, 0 `FAIL` |
| Покрытие | `go test -cover` | `swarm` **89,0 %**, `template` **95,8 %** (= заявленному) |
| Линтер | `golangci-lint run ./shared/testkit/...` | 0 issues |
| Контракты | `go run ./cmd/mvctl contracts check` | 65 типов, 8 топиков, 58 схем |
| SHA-1 `fake_encounter.go` | `sha1sum` | `a911330f694363893b593ff886e7ed1ae6452133` = заявленному |
| Зонд P1 (4 режима) | `-overlay` | §1 — как заявлено |
| Зонд P4 — потерянный ack | `-overlay` | дубль, Mi-3 |
| Зонд P5 — конкурентная доставка | `-overlay` | дубль при прямом вызове; с шинами репозитория недостижим |
| Зонд P6 + внутренний «длинный бой» | `-overlay` | склейки нет, буфер ≤ 1 хода на персонажа, окно ≤ 10 000; N-3 |
| Окно против `Dedup` | внутренний зонд | FIFO против LRU, `newWindow(0)` паникует — вызов только с константой |
| Зонд P7 — `Stuck` на настоящем `FakeEncounter` | `-overlay` | §4 |
| Мутанты MR7, MR9, MR16b, K1–K8 | `-overlay` | 11 из 11 красные |
| Детектор гонок | `-race` | **не запускался**: нет cgo (T-401) |

### Открытые вопросы

Новых к архитектору нет. К уже отправленному вопросу о двухшаговом API `Dedup` приложить данные P4: дубль при
потерянном ack двухшаговый `Dedup` не снимает. Снимает его детерминированный `id` нарратива от причины, а это тоже
`Derive` в `shared/eventbus`.

### Предложения в бэклог

1. T-229 (настоящий GM): детерминированный `id` `narrative.output` от причины, чтобы повтор после потерянного ack
   гасился дедупликацией шлюза по `id`. Требует опции в `Derive` — через архитектора.
2. T-229: окно «ответил ли я» со снапшотом C-14 (`IDs`/`Restore`) — либо двухшаговый `Dedup`, либо эквивалент. Своё окно
   без снапшота там уже дефект.

## T-255 · ревью #1 · 2026-09-11 · code-reviewer#1

### Границы ревью

Файлы задачи в индексе: `cmd/multiverse/{contexts.go,fake_contexts.go,fake_contexts_test.go,main_test.go}`,
комментарий в `bus_memory.go`, `shared/env/vars.go`, `.env.example`, `.golangci.yml`. Рабочее дерево по коду равно
индексу. SHA-1: `fake_contexts.go` `f7850df6…`, `fake_contexts_test.go` `f5b17fe0…`, `contexts.go` `dc243702…`.

Эталоны:
- DoD индекса T-255 с уточнением оркестратора: без флага — прежняя заглушка, а не «контекста нет»;
- ADR-001 доп. п. 8;
- `contracts.md` v0.7: §0 (исключение для фейков), C-01 `Deps`, C-05 «Заглушка»;
- `ownership.md` §1;
- dev-log T-255;
- `TestStubContextsAreRegistered` из HEAD.

Файлы T-410 (`serve.go`, `serve_test.go`) читались только как путь исполнения.

**Метод.** Мутанты Go и зонды — через `go test -overlay` из каталога `mktemp -d`. Мутанты depguard — в экспорте
индекса (`git checkout-index`). Бинарник «до задачи» собран из дерева через `-overlay`: `contexts.go` из HEAD,
`fake_contexts.go` удалён. Живые процессы запускались только с `--bus=memory` на свободных портах loopback. Каждый
сверен по пути exe, командной строке и владельцу порта, затем остановлен. После работы из каталога не осталось ни
одного процесса. Каталог удалён целиком по точному пути. `git diff` по `cmd`/`shared`/`test` пуст, неотслеживаемых
файлов нет.

### Вердикт

**ПРИНЯТЬ** — Critical 0, Major 0, Minor 2, Nit 4.

Каждое заявление автора подтверждено моим прогоном. Без флага процесс ведёт себя байт-в-байт как до задачи. Выбор
делается при построении контекстов, а не в `init`. Нечитаемое значение даёт громкий отказ с именем переменной. Узкое
правило depguard держит. Оба Minor касаются того, что видит оператор или разработчик вне теста: какой отказ он
получит и зависят ли тесты от его оболочки. Логику хука они не затрагивают.

### 1. Выбор по флагу и порядок старта

- **Без флага — как до задачи.** Бинарники «до» и «после» запущены с `--contexts=all --bus=memory` без
  `MV_SWARM_FAKE`. Тела `/health` совпали побайтно (`diff` пуст), строки старта stdout тоже. Порядок в обоих:
  `state,laws,mechanics,llm,swarm,gateway,memory`. С `MV_SWARM_FAKE=false` то же самое.
- **С `true`.** `/health` 200 `ok`, `swarm` — `{stub: testkit/swarm.FakeContext, phase1: rules, removed_by: T-256,
  encounters: 0}`. `multiverse health --url` вернул код 0. В stderr — `WARN no fixtures…` и `phase 1 stub running`.
- **Замена `TestStubContextsAreRegistered`.** В HEAD тест проверял «каждое имя списка зарегистрировано» и «в списке
  7 имён». `TestPlatformContextsAreRegisteredOnceInTheStartOrder` сравнивает `runtime.Names()` и `platformContexts`
  с литералом порядка старта. Проверка стала строже: ловит и сдвиг порядка (G6 автора). В
  `TestStubIsHealthyAndDoesNothing` добавлены `clearVar` и проверка типа `stub` у всех семи. Ослаблений нет.
- **Момент чтения.** Объявления `shared/env` — переменные пакета, они готовы до `init` пакета `main`. Фабрику
  вызывает `runtime.New` в `serve` после `parseServe`. `.env` процесс сам не читает. `TestTheFlagChoosesTheSwarm`
  задаёт переменную после инициализации пакета и различает все четыре случая.

### 2. `refused`

- Живой запуск с `MV_SWARM_FAKE=maybe`: процесс сразу завершился с кодом 1. Сообщение:
  `multiverse: start context swarm: context swarm: MV_SWARM_FAKE="maybe": expected true or false`. Имя и значение в
  нём есть, порт не занят, процесс не висит. Двойной префикс — N-1.
- **Тесты без срока.** Мой мутант O3 снимает проверку ошибки `StartAll` в `process.run`. Под ним
  `TestAMalformedFlagRefusesToStart` краснеет за 5,0 с с текстом «the process started with MV_SWARM_FAKE=maybe».
  Среди тестов T-255 без срока ничего нет: у `startProcess`/`stop` — 30 с, `health` — 30 с, `waitUntil` и
  `bootstrap` — 10 с, шаг харнесса — 2 с. **Вне T-255** под тем же мутантом висит
  `TestServeClosesTheBusLastWhenTheStartFails` (`serve_test.go:246`, `run(context.Background(), …)`): «panic: test
  timed out after 1m0s». Это тот же класс дефекта, который автор нашёл у себя, но в файле T-410. В моих двух ревью
  T-410 он пропущен. В бэклог, п. 1.

### 3. Тесты через `process.run`

- **Путь.** Контексты строит `runtime.New` из реестра пакета `main`, то есть через хук. Процесс запускается
  настоящим `process.run` на транспорте настоящего `openBus`. Снаружи на ту же шину ставятся только двойники того,
  чего в бинарнике ещё нет: `FakeState` и харнесс. Мимо кода `cmd/multiverse` идут только `parseServe`/`serve`/
  `withSignals`, их покрывают свои тесты. Мутант G1 автора (фейк не строится) краснит оба процессных теста.
- **Мой мутант O5**: хук передаёт `FixturesDir: "testdata/fixtures"`. Краснеет `TestTheProcessTellsTheDeath…`
  («0 death narratives…», исход `npc_dead`). Значит, решение «фикстур у процесса нет, мир — из `entity.created`»
  охраняется тестом, пусть и косвенно.
- **Сломанный порядок (зонд O1).** Обёртка транспорта задерживает первую доставку в группу
  `testkit-encounter-system_events` на 3 с: заглушка узнаёт волка позже входа персонажа. Итог — красный, не
  зависание. Каждый бой падает примерно за 3 с с сообщением «step 4 (attack player-A): … nobody resolved the attack
  … within 2s». Причину («заглушка не знала волка при входе») сообщение не называет — N-3. В обычном прогоне гонка не
  проявилась: `-count=10` зелёный, `-shuffle=on -count=3` зелёный.
- **Вероятностный тест смерти.** По `rules/dark-forest.yaml` и таблице `FixedMechanics` (6 hit + 1 critical из 10)
  укус попадает с вероятностью 0,7. Урон `d4` не меньше 1, при 1 hp любое попадание убивает. Скрипт — не больше 8
  ударов (`maxBlows`), и на каждый волк с 1000 hp отвечает укусом. Потом бегство: успех 0,7 (уходит живым), провал
  0,3, и тогда удар вдогонку. Точная вероятность ложного падения — 0,3⁸ · (0,7 + 0,3 · 0,3) ≈ 5,2·10⁻⁵ (1 на ~19 000).
  Оценка автора 0,3⁸ ≈ 6,6·10⁻⁵ — верхняя граница того же порядка, верна. Сообщение при падении проверено зондом O2
  (персонаж бессмертен, 3 прогона). В двух прогонах выведены оба сообщения, в одном — только «0 death narratives,
  want the one of player-A». Пояснение «the wolf missed every bite» приклеено к проверке исхода и не выводится в самом
  вероятном ложном случае (8 промахов, затем удачное бегство, исход `players_out`) — N-2.
- **`t.Chdir` и `testkit.Deterministic`.** `t.Parallel` в пакете нет. Очистки выполняются в обратном порядке:
  сначала останавливается процесс, затем двойники, потом восстанавливаются окружение, каталог и источник
  идентификаторов. `-shuffle=on -count=3` зелёный, утечки нет.

### 4. depguard

Повторено в экспорте индекса, `golangci-lint run --enable-only depguard ./cmd/multiverse/...`:

| Мутант | Ожидание | Итог |
|---|---|---|
| база | 0 | 0 issues |
| L1: хук импортирует `testkit/state` | красный | `cmd-multiverse-fake-contexts` |
| L3: хук импортирует `testkit/swarmx` (пакет создан, чтобы не было ошибки типов) | красный | `cmd-multiverse-fake-contexts` |
| L5: хук импортирует корень `testkit` | красный | `cmd-multiverse-fake-contexts` |
| **M7 (мой):** новый `cmd/multiverse/zz_probe_test.go` импортирует `testkit/swarm` | зелёный | 0 issues |
| **M8 (мой):** второй не-тестовый `cmd/multiverse/zz_extra.go` импортирует `testkit/swarm` | красный | `no-testkit-in-production` |
| L6: L1 без нового правила | молчит | 0 issues |

M7 зелёный по замыслу. `.golangci.yml` (комментарий к `no-testkit-in-production`, раздел `exclusions`) открывает
двойники для `_test.go`, и `fake_contexts_test.go` законно импортирует `swarm`/`state`/`gateway`/`membus`. Дверь
«второй файл хука» закрыта (M8).

Транзитивно бинарник теперь включает `testkit/{swarm,swarm/template,state,mechanics}`. `testing`, testify и
testcontainers в `go list -deps ./cmd/multiverse` нет, условие ADR-001 доп. п. 8 выполнено. Размер вырос с 13,37 до
15,79 МБ. Без флага эти пакеты не исполняются.

### 5. Риск Docker

Живой запуск с `MV_SWARM_FAKE=true` из каталога без `rules/` — то же, что в образе, где `rules/` нет: код выхода 1
сразу. Сообщение: `multiverse: start context swarm: testkit/swarm: load rules: mechanics: read rules: open
rules/dark-forest.yaml: The system cannot find the path specified.` Отказ уже громкий: процесс не поднимается,
healthcheck compose покраснеет. Молча сломанного состояния нет. Чего не хватает: связи с флагом в сообщении и одной
фразы в `.env.example` — Mi-1. Compose флаг не передаёт: `env_file` в `docker-compose.yml` нет, переменные
перечисляются явно. Отдельный громкий отказ «флаг в контейнере» не нужен.

### Замечания по серьёзности

#### Minor

**Mi-1. `cmd/multiverse/fake_contexts.go:42-43`, `.env.example` (блок `MV_SWARM_FAKE`) — отказ фейка без `rules/`
не называет флаг, а `.env.example` молчит о зависимости от рабочего каталога.** Доказательство — живой запуск
выше и прогон e2e в Mi-2. Оператор, включивший флаг для контейнера, получит «open rules/dark-forest.yaml». Из этого
не видно, что `rules/` нужен из-за `MV_SWARM_FAKE=true` и что в образе его нет по построению.
*Как исправить:*
- в `newSwarm` обернуть `FakeContext` декоратором, чей `Start` дополняет ошибку: «MV_SWARM_FAKE=true: заглушка
  читает rules/dark-forest.yaml из рабочего каталога: …». Тип `refused` рядом, по размеру это то же самое;
- в комментарий `.env.example` добавить: «нужен `rules/` в рабочем каталоге процесса; в образе его нет — в
  контейнере с флагом процесс не стартует».

Тест — вариант `TestAMalformedFlagRefusesToStart` с `t.Chdir(t.TempDir())`: в отказе должно быть имя переменной.

**Mi-2. `test/e2e/empty_world_test.go:46-48` — e2e не изолирован от `MV_SWARM_FAKE` оболочки разработчика.**
Доказательство: `MV_SWARM_FAKE=true go test -tags e2e -run TestTheEmptyWorldAnswersTheHealthProbe ./test/e2e/`
даёт `FAIL` через 62,5 с. Дочерний процесс наследует окружение, запускается из `test/e2e` без `rules/` и умирает на
старте, а проба ждёт до конца бюджета. Правило проекта записано у `clearVar`: «переменная, выставленная
разработчиком в своей оболочке, не должна менять то, что доказывает тест». Файл вне диффа, но зависимость внесла
новая переменная. `go test -short ./cmd/...` с тем же флагом зелёный: тесты пакета себя изолируют.
*Как исправить:* в тесте рядом с `t.Setenv(env.CoreAddr.Name(), addr)` добавить
`t.Setenv(env.SwarmFake.Name(), "false")`.

#### Nit

**N-1. `fake_contexts.go:59-61` — двойной префикс.** Живое сообщение: «start context swarm: context swarm:
MV_SWARM_FAKE="maybe"…». Имя контекста уже добавляет `StartAll`. *Как исправить:* `refused.Start` возвращает `r.err`
как есть. Тест продолжит находить `swarm` в тексте `StartAll`.

**N-2. `fake_contexts_test.go:264-266, 279-285` — пояснение про невезение печатается не там.** Зонд O2: в самом
вероятном ложном случае выводится только «0 death narratives…», без слова о вероятности. Комментарий «the wolf misses
eight times in a row» не учитывает бегство и удар вдогонку. *Как исправить:* перенести пояснение в сообщение
проверки числа смертей: «волк промахнулся 8 раз, и бегство удалось; вероятность ≈ 5·10⁻⁵». В комментарии дать точную
формулу.

**N-3. `fake_contexts_test.go:324-331, 354-357` — поздний `entity.created` виден как таймаут атаки.** Зонд O1:
красный за 3 с, но с текстом про атаку. Барьера «заглушка знает волка до входа» нет, и автор сам назвал это слабым
местом. *Как исправить* (любое):
- перед `h.Run` дождаться, пока `fake.Encounter()` знает `wolf-alpha` (нужен геттер в `testkit/swarm`, владелец —
  EPIC-003);
- либо при ошибке `h.Run` проверять, открывалась ли встреча, и сообщать «заглушка не узнала волка до входа».

**N-4. `cmd/multiverse/contexts.go:9-13` — шапка противоречит хуку.** Шапка говорит: «swarm, llm, laws — EPIC-003»
заменяют заглушку своим `runtime.Register`. А `fake_contexts.go:27-30` запрещает `internal/swarm` регистрироваться,
пока жив хук: иначе паника, мутант G5. Первым шапку прочтёт автор T-229. *Как исправить:* в шапке оговорить, что
`swarm` подключается через `newSwarm` до T-256.

### DoD T-255

| Пункт | Статус |
|---|---|
| `true` → фейк; не задан → прежняя заглушка (по уточнению оркестратора); тест на обе ветки | выполнено: `TestTheFlagChoosesTheSwarm`, `…HealthOfTheProcessNamesTheSwarmItRuns`, живые запуски |
| Импорт `shared/testkit` только в этом файле; `golangci-lint` зелёный с исключением | выполнено (`bus_memory.go` — второе исключение T-410, ADR-001 доп. 2026-09-11 п. 2); 0 issues; L1/L3/L5/M8 красные |
| PR через tech-lead#1; запрос на depguard и `.env.example` | процесс, решает tech-lead#1 |
| I1-α: бой и нарратив в собранном бинарнике, не только в тестах | **не выполнимо до EPIC-002**: в бинарнике `state` — заглушка. Доказано тестом через `process.run`; бинарник показывает только `/health` фейка. Отклонение обосновано в dev-log §4 — открытый вопрос |

### Что проверено экспериментом

| Что | Как | Итог |
|---|---|---|
| Сборка, `vet` | `go build ./... && go vet ./...` | зелено |
| Короткие тесты | `go test -short -count=1 ./...` | код 0, 27 пакетов `ok` |
| e2e | `go test -tags e2e -count=1 ./test/...` | `test/e2e`, `test/fixtures` — `ok` |
| Повтор пакета | `go test -count=10 ./cmd/multiverse/` | `ok` |
| Порядок тестов | `go test -shuffle=on -count=3 ./cmd/multiverse/` | `ok` |
| Процессные тесты по отдельности | `-v -run TestTheProcess…` | 16 боёв: `entry` 32, `turn` 51, `world_event` 16, `death` 2; исходы `npc_dead` 14, `players_out` 2; тест смерти `PASS` |
| Линтер | `golangci-lint run ./...` | 0 issues |
| Контракты, окружение | `mvctl contracts check`, `mvctl env check` | 65 типов / 8 топиков / 58 схем; 67 переменных; код 0 |
| gitleaks, индекс | `gitleaks dir --config=.gitleaks.toml` по экспорту индекса | no leaks found |
| gitleaks, диапазон ветки | `gitleaks git --log-opts=main..HEAD` | 1 находка `generic-api-key` в закоммиченном `swarm-llm-laws.md:494` (коммит `744fb10`), не T-255 — открытый вопрос |
| Бинарник «до» против «после», без флага | живые процессы, `diff` | `/health` и stdout идентичны |
| `false` / `true` / `maybe` / `true` без `rules/` | живые процессы | заглушка / фейк `ok`, `health` 0 / выход 1 с именем переменной / выход 1 без имени флага (Mi-1) |
| Флаг из оболочки | `MV_SWARM_FAKE=true go test -short ./cmd/...` и e2e | `cmd/...` зелёные; e2e `FAIL` за 62,5 с (Mi-2) |
| depguard L1, L3, L5, M7, M8, L6 | экспорт индекса | как в таблице §4 |
| Зонд O1 (поздние факты State) | `-overlay` | красный за ~3 с, не зависание; N-3 |
| Зонд O2 (бессмертный персонаж) | `-overlay`, 3 прогона | сообщения — N-2 |
| Мутант O3 (`StartAll` без проверки) | `-overlay`, `-timeout 60s` | тест T-255 — красный за 5 с; `serve_test.go` T-410 — висит (бэклог п. 1) |
| Мутант O5 (хук передаёт `FixturesDir`) | `-overlay` | красный: `TestTheProcessTellsTheDeath…` |
| Зависимости бинарника | `go list -deps ./cmd/multiverse` | `testkit/{membus,mechanics,state,swarm,swarm/template}`; `testing`/testify/testcontainers нет |
| Детектор гонок | `-race` | **не запускался**: нет cgo (T-401) |

### Открытые вопросы

1. **tech-lead#1 / оркестратору:** DoD п. 4 («бой в собранном `cmd/multiverse`, не только в тестах») до EPIC-002
   закрыть нельзя. Принять ли его закрытым тестом через `process.run` с переносом живого боя в EPIC-002 или держать
   открытым? Решение — не ревьюера.
2. **Оркестратору:** первая половина `make secrets-scan` (`gitleaks git` по `main..HEAD`) находит
   `generic-api-key` в закоммиченном `Docs/dev-team/architecture/components/swarm-llm-laws.md:494` (коммит
   `744fb10`, похоже на ложное срабатывание по тексту `num_predict=max_tokens`). Если `.gitleaksignore` его не
   гасит, цель красная независимо от T-255. Отпечаток: `744fb100…:Docs/dev-team/architecture/components/swarm-llm-laws.md:generic-api-key:494`.

### Предложения в бэклог

1. T-410 / EPIC-001: `TestServeClosesTheBusLastWhenTheStartFails` (`serve_test.go:246`) — дать `run` контекст со
   сроком, как в `TestAMalformedFlagRefusesToStart`. Сейчас регрессия «процесс стартовал» вешает пакет до таймаута
   `go test`.
2. `test/e2e`: проба `/health` должна замечать, что дочерний процесс уже вышел, а не ждать 60 с (виден в Mi-2).
3. Предложение автора поддерживаю: подтест e2e бинарника с `MV_SWARM_FAKE=true`, только `/health`. Каталог — корень
   дерева (`cmd.Dir`), иначе упрётся в Mi-1.
4. Находка автора — невоспроизводимые идентификаторы внутри процесса (одна последовательность `eventbus`, `Deps.IDs`
   не используется в `NewRoot`/`Derive`). Через архитектора, в одну пачку с детерминированным `id` из T-229 (ревью
   T-220 #2, бэклог п. 1).

## T-255 · ревью #2 · 2026-09-11 · code-reviewer#1

### Границы ревью

Итерация 2 по ревью #1. В индексе: `cmd/multiverse/{fake_contexts.go,contexts.go,fake_contexts_test.go,serve_test.go}`,
`.env.example`, `test/e2e/empty_world_test.go`. Рабочее дерево по `cmd`/`shared`/`test`/`.env.example` совпадает
с индексом, неотслеживаемых файлов нет. SHA-1 из индекса:
- `fake_contexts.go` `dc88f18b`;
- `fake_contexts_test.go` `9fbcb033`;
- `contexts.go` `ce6b1f73`;
- `serve_test.go` `a7b38277`;
- `empty_world_test.go` `7c498fbb`;
- `.env.example` `eb222601`.

Эталоны: ревью #1, dev-log «T-255 · итерация 2», карточка, DoD индекса. Тимлид выровнял DoD по уточнению
оркестратора: три ветви флага; бой — тестом через `process.run`. Проверены только исправления и регрессия от них.
В `serve_test.go` (файл T-410) — только правка `witness` и срок у `…LastWhenTheStartFails`.

**Метод.** Мутанты и зонды — через `go test -overlay` из одного каталога `mktemp -d`, `-timeout 60s`. Бинарник
«до задачи» собран так же, как в ревью #1: `contexts.go` из HEAD, `fake_contexts.go` удалён через overlay. Живые
процессы запускались только с `--bus=memory` на портах 18731–18735 loopback. Каждый сверен по PID владельца
порта, пути exe и командной строке, затем остановлен. После работы из каталога не осталось ни одного процесса.
Каталог удалён целиком по точному пути. Стек владельца и порт 8888 не трогались.

### Вердикт

**ПРИНЯТЬ** — Critical 0, Major 0, Minor 1, Nit 1.

Все шесть замечаний ревью #1 и добавка O3 закрыты. Каждое заявление автора повторено моим прогоном. Правка
`witness` тесты T-410 не ослабила: все пять мутантов R1, R2, R3, M1, M2 по-прежнему красные.

Новое Minor не про код: `flagged` ведёт себя верно, это доказал зонд. Оно про то, что ветку `default` не держит ни
один тест.

### Статус замечаний ревью #1

| # | Статус | Доказательство |
|---|---|---|
| Mi-1 | закрыто | Живой запуск `MV_SWARM_FAKE=true` из пустого каталога: выход 1 сразу. Сообщение: `multiverse: start context swarm: MV_SWARM_FAKE=true: the stub reads rules/ from the working directory of the process, and the image of the platform has none: testkit/swarm: load rules: … open rules/dark-forest.yaml: …`. `TestAFakeWithoutRulesNamesTheFlag` зелёный; под Mi1b автора и под O3 красный. Фраза в `.env.example` есть. Подсказка — только при `fs.ErrNotExist`: зонд, §1 |
| Mi-2 | закрыто | `MV_SWARM_FAKE=true go test -tags e2e -run TestTheEmptyWorldAnswersTheHealthProbe` — `PASS` за 2,94 с. В ревью #1 было `FAIL` через 62,5 с |
| N-1 | закрыто | Живой `maybe`: `multiverse: start context swarm: MV_SWARM_FAKE="maybe": expected true or false`. Строка `context swarm` встречается один раз. Тест считает вхождения |
| N-2 | закрыто | Мой зонд P-N2 — персонаж бессмертен, hp и `hp_max` по 1000. Итог: `0 death narratives and the encounter ended "" … about 5.2e-5 of runs; any other outcome is a defect`. Пояснение теперь печатается при любом не-смертельном исходе. Арифметика: 0,3⁸ = 6,561·10⁻⁵ ≈ 6,6·10⁻⁵ — верхняя граница; × (0,7 + 0,09) = 5,18·10⁻⁵ ≈ 5,2·10⁻⁵. Обе цифры в комментарии теста верны. «7·10⁻⁵» — округление автора в dev-log итерации 1, строка 1549. В ревью #1 было 6,6·10⁻⁵, расхождения нет |
| N-3 | закрыто сообщением | Мой зонд P-N3: мир приходит на шину без волка, харнесс волка знает. Итог — красный за 4 с: `step 4 (attack player-A): … within 2s` и `no encounter was opened: the likely cause is that the fake had not yet learnt wolf-alpha …`. Барьера нет. Причина обоснована: нужен геттер в `testkit/swarm`, а файл занят T-419/T-420. Для Nit этого достаточно |
| N-4 | закрыто | Шапка `contexts.go:15-21`: `swarm` — исключение до T-256, подключается через `newSwarm`; `internal/swarm` приходит строкой в `newSwarm`, а не своим `runtime.Register` |
| O3 (бэклог п. 1) | закрыто | Мутант O3 — снята проверка ошибки `StartAll`. Пакет красный за 18 с, без паники и таймаута. Красные: `AMalformedFlag…`, `AFakeWithoutRules…`, `…LastWhenTheStartFails/a_context_fails_to_start`, у каждого 5,00 с, сообщение `run error = <nil>, want one naming "second"`. В ревью #1 под тем же мутантом пакет висел до таймаута `go test` |

### 1. Обёртка `flagged`

- **Прозрачность.** `flagged{*swarm.FakeContext}` встраивает указатель: `Name`, `DependsOn`, `Health` и `Stop`
  берутся у фейка. Мой зонд сравнил их у обёртки и у фейка через `reflect.DeepEqual` — совпали: `swarm`, `[]`, одно
  и то же `degraded {state: stopped …}`. Необязательный интерфейс `runtime.Routes` — единственный, который проверяет
  `StartAll` (`lifecycle.go:17`). У фейка его нет, у обёртки тоже. Живой `/health` с `true` из корня отдаёт тот же
  набор деталей, что в ревью #1: `stub`, `phase1`, `removed_by: T-256`, `encounters: 0`.
- **Ветки ошибок** — мой зонд, вызов `flagged.Start` напрямую:

  | Причина | Сообщение | Подсказка о `rules/` | `errors.Is(ErrNotExist)` |
  |---|---|---|---|
  | нет шины в `Deps` | `MV_SWARM_FAKE=true: testkit/swarm: no bus in deps` | нет | нет |
  | битый YAML правил | `MV_SWARM_FAKE=true: testkit/swarm: load rules: …bad.yaml: mechanics: invalid rules: …` | нет | нет |
  | файла правил нет | `MV_SWARM_FAKE=true: the stub reads rules/ … has none: … open …nope.yaml: …` | да | да |
  | повторный `Start` | `MV_SWARM_FAKE=true: testkit/swarm: context already started` | нет | нет |

  Ни одна ошибка не проглатывается, цепочка `%w` сохраняется. Подсказка появляется только при ненайденном файле.
  Повторный `Stop` возвращает nil.
- **Чего не держат тесты** — Mi-3 ниже.

### 2. Правка `witness` в `serve_test.go` (T-410)

`witness.Stop` без старта (`deps.Contracts == nil`) пишет в хронологию `stop <name> (never started)`. Раньше в этом
месте была паника на `nil`-реестре. На нормальном пути ничего не меняется: `StartAll` останавливает только
стартовавшие (`lifecycle.go:22`). Мутанты T-410 из ревью EPIC-001 и dev-log T-410 на текущем дереве:

| Мутант | Итог |
|---|---|
| R1: снят `release()` в `run` | красный: `ServeReleasesTheSignalsBeforeStopping` |
| R2: без `defer`, `closeBus` сразу после `release()` | красный: `…AfterEveryContextStopped`, оба подтеста `…LastWhenTheStartFails`, `ReleasesTheSignals…` |
| R3: без `defer`, `closeBus` только перед `return serveErr` | красный: оба подтеста `…LastWhenTheStartFails` |
| M1: `defer` оставлен, лишний `closeBus` до `StopAll` | красный: `…AfterEveryContextStopped`, `ReleasesTheSignals…` |
| M2: шина не закрывается вовсе | красный: `…AfterEveryContextStopped`, оба подтеста `…LastWhenTheStartFails`, `ReleasesTheSignals…` |
| **W1 (мой):** `StartAll` останавливает и контекст с упавшим `Start` (`contexts[:i+1]`) | красный, читаемо: `lifetime = ["start first" "stop second (never started)" "stop first" "bus closed"]` |

W1 — ровно тот путь, где раньше была бы паника пакета. Сейчас он краснеет одним понятным тестом.

### 3. Тесты, которые запускают процесс без срока

Грепнул `run(`, `exec.Command` и `process{` в `cmd/multiverse` и `test/e2e`. Вызовы:
- `runUntilStarted` — ожидание 30 с;
- `…LastWhenTheStartFails` — 5 с (новое);
- `ReleasesTheSignals…` — контекст отменён заранее;
- `AMalformedFlag…`, `AFakeWithoutRules…` — 5 с;
- `startProcess` — `stop` и `health` по 30 с;
- `main_test.go` — `run(version)` и `run(nil)`, это разбор CLI, процесс не запускается;
- e2e `empty_world_test.go` — проба 60 с и `CommandContext`, отменяемый в `Cleanup`;
- `stubs_v0_test.go` процесс не запускает.

Вызовов без срока нет.

### Новые замечания

#### Minor

**Mi-3. `cmd/multiverse/fake_contexts.go:59-68` (`flagged.Start`, ветка `default`) — поведение при ошибке, отличной
от ненайденного файла, не закреплено тестом.** Код верен, это показал зонд §1. Но все три мутанта ветки выживают:
пакет зелёный полностью, без `-short`.

| Мутант | Итог |
|---|---|
| Mi1c2: подсказка о `rules/` к любой ошибке | пакет зелёный |
| Mi1d: другие ошибки без префикса `MV_SWARM_FAKE=true: ` | пакет зелёный |
| Mi1e: другие ошибки проглатываются, `return nil` | пакет зелёный |

Mi1e — ровно то «маскирование ошибок `Start`», которое проверял этот раунд. При такой регрессии процесс
поднимается, а `swarm` в нём не запущен и молчит. `/health` покажет его `degraded`, но ни один тест не покраснеет.
Заявление автора «подсказку к любой ошибке не клею» верно по коду, но тестом не охраняется.

*Как исправить:* в `fake_contexts_test.go` добавить таблицу из двух строк рядом с `TestAFakeWithoutRulesNamesTheFlag`.
Каждая строка вызывает `flagged{swarm.NewFakeContext(cfg)}.Start`:
- `Deps{Log: discard}` без шины;
- `cfg.RulesPath` указывает на файл с битым YAML во `t.TempDir()`, `Deps{Bus: <openBus(memory)>, Log: discard}`.

Для каждой строки требовать:
- ошибку не nil;
- префикс `MV_SWARM_FAKE=true: `;
- отсутствие `working directory`;
- исходный текст (`no bus in deps` или `load rules`).

Мой зонд делает именно это, около 30 строк. По построению его проверки ловят все три мутанта: Mi1e вернёт nil,
Mi1d потеряет префикс, Mi1c2 добавит подсказку. На самих мутантах зонд не прогонялся.

#### Nit

**N-5. `.env.example` (блок `MV_SWARM_FAKE`) — «отказ начинается с MV_SWARM_FAKE=true» неточно.** Живой запуск из
пустого каталога даёт строку `multiverse: start context swarm: MV_SWARM_FAKE=true: …`. Флаг стоит сразу после
имени контекста, но не в начале строки. Оператор, который ищет строку, начинающуюся с имени флага, её не найдёт.
*Как исправить:* «в отказе сразу после имени контекста стоит MV_SWARM_FAKE=true».

### DoD T-255 (индекс, выровнен)

| Пункт | Статус |
|---|---|
| `true` → фейк; не задан или `false` → прежняя заглушка, порядок старта прежний; нечитаемое → отказ с именем; тест на каждую ветвь | выполнено. Живые запуски: `/health` и строка старта «до» и «после» совпали побайтно (`diff` пуст), `false` — то же; `true` из корня — 200, `multiverse health` — 0; `maybe` — выход 1 с именем и значением. Тесты `TestTheFlagChoosesTheSwarm`, `TestAMalformedFlagRefusesToStart` |
| Импорт `shared/testkit` только в файле хука; `golangci-lint` зелёный | выполнено: 0 issues. depguard в итерации не менялся, мутанты L1/L3/L5/M8 — ревью #1 |
| PR через tech-lead#1 | процесс, решает tech-lead#1 |
| Бой и нарратив — тестом через `process.run`; бинарник — только `/health` фейка | выполнено. `-v`: 16 боёв `PASS`, нарративы `entry` 32, `turn` 53, `world_event` 16, `death` 3; исходы `npc_dead` 13, `players_out` 3. `TellsTheDeath…` — `PASS`. Открытый вопрос №1 ревью #1 закрыт выравниванием DoD |

### Что проверено экспериментом

| Что | Как | Итог |
|---|---|---|
| Сборка, `vet` | `go build ./... && go vet ./...` | зелено |
| Короткие тесты | `go test -short -count=1 ./...` | код 0, 27 пакетов `ok` |
| e2e | `go test -tags e2e -count=1 ./test/...` | `test/e2e` и `test/fixtures` — `ok` |
| e2e с флагом в окружении | `MV_SWARM_FAKE=true go test -tags e2e -v -run TestTheEmptyWorld… ./test/e2e/` | `PASS` за 2,94 с (Mi-2) |
| Повтор пакета | `go test -count=10 ./cmd/multiverse/` | `ok`; процессные тесты реально исполнялись (`-v`, 16 боёв `PASS`) |
| Линтер | `golangci-lint run ./...` | 0 issues |
| Окружение | `mvctl env check` | 67 переменных, код 0 |
| gitleaks, индекс | `gitleaks git --staged --redact .` | no leaks found |
| Живые процессы | «до»/«после» без флага, `false`, `true` из корня, `true` из пустого каталога, `maybe` | идентичны / идентичны / фейк `ok`, `health` 0 / выход 1, флаг назван / выход 1, `context swarm` один раз |
| Зонд `flagged` | `-overlay`, новый `_test.go` | прозрачен; ветки ошибок — таблица §1 |
| O3 | `-overlay` | красный за 18 с, без паники |
| R1, R2, R3, M1, M2, W1 | `-overlay` | все красные (§2) |
| Mi1c2, Mi1d, Mi1e | `-overlay` | все живы — Mi-3 |
| Зонды P-N2, P-N3 | `-overlay` | сообщения — таблица статусов |
| Детектор гонок | `-race` | **не запускался**: нет cgo (T-401) |

### Открытые вопросы

Нет. Вопрос №1 ревью #1 закрыт выравниванием DoD. Вопрос №2 (gitleaks, `744fb10`) разобран оркестратором.

### Предложения в бэклог

1. Остаётся из ревью #1, п. 2: проба `test/e2e` должна замечать, что дочерний процесс уже вышел, а не ждать 60 с.
   После Mi-2 это не всплывает у разработчика с флагом, но любой отказ процесса на старте всё ещё стоит минуту.
2. Остаются п. 3–4 ревью #1: подтест e2e бинарника с флагом из корня дерева; идентификаторы через архитектора.

## T-419 · ревью #1 (пункты 1–6) · 2026-09-11 · code-reviewer#2

### Границы ревью

Индекс по `shared/testkit/swarm/` (`fake_encounter.go`, `fake_narrator.go`, `README.md`, тесты, новый
`fake_encounter_lifecycle_test.go`), `shared/testkit/gateway/{harness.go,end_by_fact_test.go}`,
`schemas/events/combat.decided.v1.json`. Эталон — C-05 v1.4 (п. 1г, 1д, 3–7, блок «Заглушка»), C-04 v1.3,
C-02 (отказы), ADR-026, `data-model.md` §3.7, запись developer#3 в `dev-log.md`. Пункт 7 задачи (окно нарратора
на `Dedup`) не ревьюился. По решению оркестратора не считаются замечаниями: причина пакета Б1 (`resolve`) и id
событий жизненного цикла через `WithCauseID` — это итерация 2. Каталоги T-417 не открывались.

**Метод и целостность.** Блобы индекса выгружены в каталог из `mktemp -d` и подключались через
`go test -overlay`: `fake_encounter.go` `2ac07829`, `fake_narrator.go` `bf8d47bc`, `harness.go` `4066da8b`,
схема `e51f80ef`, `fake_encounter_lifecycle_test.go` `bf8473f0`, `end_by_fact_test.go` `13ad2ced`. Зонды и
мутанты лежали там же. Причина такой постановки — в 16:24:45–16:25:31 неудачный коммит T-417 через pre-commit
временно вернул рабочей копии версии HEAD (сообщение оркестратора). Прогоны, попавшие в это окно (первая серия
мутантов и три чтения тестов), отброшены и повторены. Позже в основном дереве появились чужие незакоммиченные
правки `cmd/multiverse/{main.go,serve.go}` и неотслеживаемый `cmd/multiverse/dispatch_test.go`. Процессные тесты
и e2e повторены на индексных версиях этих файлов, чужой тест исключён оверлеем. К моменту записи хэши десяти
файлов задачи в рабочей копии равны индексу. Дерево мною не менялось, каталог удалён по точному пути.

### Вердикт

**ПРИНЯТЬ** — Blocker 0, Major 0, Minor 3, Nit 1.

Четыре из пяти расхождений блока «Заглушка» закрыты и держатся запуском; пятое — окно нарратора, пункт 7, вне
этой итерации. Шесть сценариев из задания не дали ни двойного решения, ни решения над оконченным боем. Одна
потеря действия найдена — на пути, недостижимом в MVP-1 (Mi-1). Три принятых отступления ничего не ломают, а
второе необходимо (§2). Проверка харнесса «никто не ответил» не ослаблена. Minor — в итерацию 2 вместе с
`resolve`/`WithCauseID`.

### 1. Четыре фазы встречи и отложенные действия

| Сценарий | Как проверено | Итог |
|---|---|---|
| действие во время создания | зонд P6: удар до `entity.created` — 0 решений; после — 1; повторная доставка — по-прежнему 1 | решается один раз |
| действие во время закрытия | тест автора и зонд P5 | молчание после факта |
| выброшенный закрывающий пакет, затем действие | `TestADroppedClosingPackageEndsNothing`; мутант MI (`drop` не будит отложенные) убит | решается по обычным правилам, в раунде 2 |
| Б1 во время закрытия бегством | зонд P3 | один конец `players_out`, закрытия Б1 нет |
| Б1 во время закрытия смертельным ударом, State пакет применил | зонд P3b | один конец `npc_dead` с `killer`, закрытия Б1 нет |
| Б1, пока свой пакет в полёте | мутант MH (снято `pending == nil`) убит `TestARetryThatWouldChangeWhoIsStandingIsNotOffered` | закрытие — только после решения своего пакета |
| два персонажа, один волк; оба входят до `entity.created` первого | зонд P4 | одно создание, у B боя нет |
| смерть персонажа от укуса, удар в полёте | зонд P5 | `players_out`, удар — молчание |
| **State отверг закрытие Б1, действие отложено** | **зонд P1** | **отложенное не решается никогда, более позднее решается — Mi-1** |

Риски, названные автором (очередь без предела; вечное ожидание, если State не ответит), подтверждаю чтением и в
замечания не выношу: у двойника очередь живёт один оборот State, предел — в бэклоге автора для T-230.

### 2. Три принятых отступления

- **Отказ не по гонке выбрасывает пакет с откатом.** Согласуется с ADR-026 п. 1 и 3 («нет факта — нет
  события»): закрывающий пакет, отвергнутый как дефект, иначе подвесил бы бой. Харнесс получает отказ сразу
  (C-05 п. 1: дефект виден сразу), `Error` в логе остаётся. Не ломает: `TestARefusalThatIsNotARaceIsNotRetried`
  зелёный, а мутант MI показывает, что без продолжения после `drop` бой встаёт.
- **Действия откладываются и на время создания.** Это не просто допустимо, а необходимо: мутант MC
  (откладывать только на закрытие) краснит оба стенда и оба процессных теста `cmd/multiverse`. Харнесс шлёт
  удар сразу за входом, и без отсрочки удар до `entity.created` теряется. Своего юнит-теста у отступления нет —
  Mi-3.
- **`ActiveEncounter`/`ActiveCount` считают бой от `entity.created` до факта закрытия.** Вне тестов
  `ActiveCount` читает только `fake_context.go:212` (поле `encounters` в health), `ActiveEncounter` не читает
  никто. Счёт совпадает с `state` сущности в State. Не ломает.

### 3. Откат участия (п. 1г)

Мутант MD (откат без `last_damager`) убит `TestADroppedAttemptTakesItsParticipationWithIt`. Зонд P2:
смертельный пакет выброшен после трёх конфликтов версий, затем второй смертельный удар применён. Итог: один
`encounter.ended`, `killer=player-A`, корреляция второго удара; инвентарь 0 → 1 (лут волка — один предмет),
`loot_claimed_by=player-A`. Трофей не задваивается и не уходит не тому: в соло у встречи один участник, а трофей
строится из ударов текущего пакета (`ans.blows`), а не из представления. Номер раунда не откатывается (тест
автора: раунд 2 после выброшенного раунда 1).

### 4. Харнесс шлюза (совладение)

Пять правок прочитаны. Прежние тесты харнесса не менялись, пакет зелёный, покрытие 92,6 %.
- **«Никто не ответил» не ослаблена.** Новый путь к `r.over` включается только фактом сущности `encounter`
  со `state → resolved` и только для боёв с этим id. Зонд P8: факт чужой встречи (`encounter-2`) во время
  ждущего удара — удар кончается сроком «nobody resolved», а не `ErrFightOver`. Зонд P7: факт своей встречи
  будит ждущий удар с `ErrFightOver`, до срока.
- **Порядок топиков.** Факт раньше `encounter.ended` — `TestTheFactOfTheEncounterEndsTheFight`. Конец раньше
  старта — `TestAnEndHeardBeforeTheStartKeepsTheFightOver`. `encounter.ended` раньше факта — прежний путь
  `closed`; повторный `end` по тому же id ничего не будит (`f.ended != ""`).
- Утверждение «конец записан раньше `record`» тестом не закреплено — N-1.

### 5. Схема `exchange` и нарратор

Поле опционально; внутри объекта `required: [index, last]` и `additionalProperties: false`; `schema_version` не
менялся. `mvctl contracts check` — 65 типов, 8 топиков, 58 схем. Нарратор при наличии поля смотрит только на
`exchange.last`: мутант MG («верить `last`, только если оно `true`, иначе угадывать») убит
`TestATurnClosesOnTheMarkOfItsExchange`. Без поля остаётся прежний вывод — старые тесты нарратора без поля
зелёные. Форма обменов на настоящем двойнике: тест автора закрепляет четыре формы — `(0,false)(1,true)` удар и
ответ, `(0,true)` смертельный удар, `(0,true)` побег, `(0,false)(1,true)` пойманный побег. Зонд P9: 16 обменов
долгого боя, индексы подряд с 0, `last` ровно на последнем.

### 6. Регрессия T-219, T-220, T-255

Всё поверх индекса. Стенд пары и стенд нарратора `-count=20` — ok. Процессные тесты `cmd/multiverse` `-count=3` на
индексных `main.go`/`serve.go` — PASS: 16 боёв, нарративы `entry:32 world_event:16 turn:48–50 death:1–2`, концы
`npc_dead:14–15 players_out:1–2`; `TestTheProcessTellsTheDeathOfACharacter` и
`TestTheOtherRefusalsOfTheFakeNameTheFlag` — PASS. Холостые тесты — Mi-2.

### Замечания по серьёзности

**Mi-1 (Minor) — после отказа State в закрытии Б1 отложенные действия не решаются никогда.**
`fake_encounter.go:1342-1344` (`advanceOne`) выбирает `closeOrphaned`, как только `pending == nil`,
`downed != nil` и живых NPC не осталось. `closeOrphaned` при `enc.orphaned == cause.ID` (`:1372-1376`) пишет
`Error` и возвращает `nil`, не разбирая `enc.deferred`, и каждый следующий `advanceOne` идёт тем же путём.
Зонд P1: бой открыт, чужой факт убил волка, двойник предложил закрытие Б1, в полёте — бегство; State отверг
закрытие (`invalid_op`); затем второе бегство. Отложенное бегство решено 0 раз, более позднее — 1 раз, бой
остаётся `active` над трупом. Это нарушает C-05 v1.4 п. 5 («пакет выброшен — действие решается по обычным
правилам») и порядок прихода действий. Путь требует двух дефектов (чужая рука и отказ закрытия) и в MVP-1
недостижим, поэтому Minor. Но двойник — образец для T-230.
*Как исправить:* перенести проверку `orphaned` в условие `advanceOne`
(`… && e.livingCount(enc) == 0 && enc.orphaned != enc.downed.ID`), чтобы после отказа закрытия отложенные
действия разбирались обычным путём; закрепить тестом по сценарию P1.

**Mi-2 (Minor) — четыре теста боя проходят при нуле боёв.** Мутант «встреча никогда не становится активной»
(`announce` ставит `active=false`) краснит набор в целом, но эти тесты остаются зелёными:
- `TestEveryTurnOfTheFightIsToldOnce` (`fake_narrator_test.go`) — `len(turns) != len(actions)` при 0 = 0.
  Исправление автора (`openFight`) нужно, но от того же класса не страхует;
- `TestOneEntityOneChangeSet` (`fake_encounter_test.go:200-214`) — `range` по пустому списку предложений;
- `TestAnActionAfterTheFightIsOverIsAnsweredWithSilence` (`:465-481`) — `swingUntilOver` (`:1580-1590`) сразу
  выходит, если бой не открывался, а предусловие проверяет только `!open`;
- `TestARedeliveredActionIsAnsweredOnce` (`:743-755`) — `before == after` на нуле событий.

С текущим кодом тесты работают по делу; дефект — в отсутствии нижней границы.
*Как исправить:* `len(actions) == 0` → `t.Fatal`; не меньше одного предложения; перед проверкой тишины —
предусловие «бой был» (`encounter.ended` есть, либо `ActiveEncounter` было `true` после `openFight`); у повторной
доставки — первая доставка дала события. Проще всего — чтобы `openFight` сам проваливал тест, если бой не открылся.

**Mi-3 (Minor) — фаза «создаётся» не закреплена юнит-тестами.** Мутант MB (`engaged` учитывает только идущую
встречу, но не создающуюся) переживает весь набор трёх пакетов; его ловит только мой зонд P4 (двое входят до
`entity.created` первого). `TestTwoCharactersDoNotShareOneWolf` проверяет правило 3 лишь после открытия первой
встречи. Отступление 2 (отсрочка на время создания) держат только стенды и процессные тесты (мутант MC), своего
юнит-теста у него нет.
*Как исправить:* перенести зонды P4 и P6 в `fake_encounter_lifecycle_test.go`.

**N-1 (Nit) — порядок «конец раньше `record`» в `Observe` харнесса не закреплён.** Мутант ME (сначала `record`,
потом `end`) выживает. Последствие мягкое: действие, успевшее проскочить, всё равно получает `ErrFightOver`,
потому что `end` будит ждущих (зонд P7). Либо тест, либо смягчить формулировку в комментарии `harness.go:423-425`
и в `dev-log.md` («уже видит бой оконченным» → «в худшем случае получит `ErrFightOver` с приходом конца»).

### Что проверено экспериментом

| Проверка | Итог |
|---|---|
| `go build ./... && go vet ./...` | зелёные |
| `go test -short -count=1 ./...` поверх индекса | все ok |
| `go test -tags e2e -count=1 ./test/...` | `test/e2e` ok, `test/fixtures` ok |
| стенды пары и нарратора `-count=20` | ok |
| процессные тесты `cmd/multiverse` `-count=3` | PASS |
| покрытие | swarm 89,4 % (совпадает с заявленным), gateway 92,6 % |
| `golangci-lint run ./...` | 0 issues |
| `go run ./cmd/mvctl contracts check` | ok: 65 типов, 8 топиков, 58 схем |
| зонды P1, P2, P3, P3b, P4, P5, P6, P7, P8, P9 | P1 красный (Mi-1), остальные зелёные |

| # | Мутант | Итог |
|---|---|---|
| MA | встреча не становится активной (детектор холостых тестов) | набор красный, четыре теста зелёные — Mi-2 |
| MB | `engaged` без фазы создания | выжил весь набор, убит только зондом P4 — Mi-3 |
| MC | отсрочка только на закрытие | убит стендами и процессными тестами, юнит-тестами — нет (Mi-3) |
| MD | откат участия без `last_damager` | убит `TestADroppedAttemptTakesItsParticipationWithIt` |
| MH | закрытие Б1, пока свой пакет в полёте | убит `TestARetryThatWouldChangeWhoIsStandingIsNotOffered` |
| MI | `drop` не будит отложенные действия | убит `TestADroppedClosingPackageEndsNothing` |
| MG | нарратор верит только `last=true` | убит `TestATurnClosesOnTheMarkOfItsExchange` |
| ME | харнесс: `record` раньше `end` | выжил — N-1 |
| MF | харнесс: повторный `end` по уже оконченному бою | выжил; безвреден (лишняя строка лога), в замечания не выношу |

### Открытые вопросы

По коду нет. По процессу: основное дерево в ходе ревью дважды менялось чужими руками — окно pre-commit T-417 и
незакоммиченные `cmd/multiverse/{main.go,serve.go}` с неотслеживаемым `dispatch_test.go` (владелец мне
неизвестен). Выводы от этого не зависят, так как проверялся индекс через оверлей, но принимающему стоит сверить
хэши файлов задачи с перечисленными в «Методе».

### Предложения в бэклог

1. T-230: слот `pending` на встречу один, а вне фаз создания и закрытия действия не откладываются. Второе
   действие, пришедшее до факта первого пакета, перезаписывает `pending` (унаследовано от T-219, в HEAD так же).
   Отказ первого пакета тогда не найдёт, что откатывать, и п. 1г для него не сработает. Харнесс так не ходит
   (ждёт факта), шлюз EPIC-004 может. Запуском не проверялось — это наблюдение по чтению.
2. Поддерживаю предложения автора: предел очереди отложенных действий в T-230, `exchange` в `api-contracts.md`
   §2.3.6, валидная фикстура `combat.decided` с `exchange`.
3. После приёмки T-419 блок «Заглушка» C-05 v1.4 устареет в четырёх пунктах из пяти; правка документа — за
   системным архитектором.

## T-419 · ревью #2 (итерация 2: пункт 7, `WithCauseID`, `resolve`, замечания ревью #1) · 2026-09-11 · code-reviewer#2

### Границы ревью

Проверялся индекс по `shared/testkit/swarm/`:
- `fake_narrator.go` `bd83b95d`, `fake_encounter.go` `dc555f11`;
- тесты `fake_encounter_lifecycle_test.go` `3819291b`, `fake_encounter_test.go` `f3ab0b21`, `fake_narrator_test.go` `aebc8631`;
- `README.md` `f1629c44`, удалённый `window_test.go`;
- `shared/testkit/gateway/harness.go` `760c4c7d`.

Эталон — ADR-026, ADR-027 п. 2–3, C-05 v1.4 п. 4–5, `shared/eventbus/README.md` («Id из причины»), запись
«итерация 2» в `dev-log.md`, решения оркестратора по T-419 в `journal.md`. Причина `WithCauseID` у событий
жизненного цикла проверялась с учётом решения оркестратора: причина — событие, от которого строится пакет, а не
факт State.

Чтобы видеть только правки итерации, блобы ревью #1 сравнивались с текущими:
- `fake_encounter.go` `2ac07829 → dc555f11`: только заявленное — `WithCauseID` у двух событий, `CauseResolve`,
  условие Mi-1 в `advanceOne`, `Error` в `drop`, мёртвая ветка `closeOrphaned` удалена;
- `harness.go` `4066da8b → 760c4c7d`: только комментарий;
- `fake_narrator.go` `bf8d47bc → bd83b95d`: `window` → `eventbus.Dedup`.

**Метод и целостность.** Индекс выгружен дважды через `git checkout-index -a --prefix=…` в каталог из
`mktemp -d`. Первая копия — для прогонов, вторая — для зондов и мутантов. В пути временного каталога есть
кириллица, поэтому мутанты вносились не через `-overlay`, а правкой файла в копии. Перед каждым прогоном замена
проверялась `cmp` (иначе — «NOT APPLIED»), после прогона файл восстанавливался. Итоговые хэши восстановленных
файлов равны индексу. У каждого мутанта свой набор красных тестов, одинаковых `ok` нет. К моменту записи хэши
семи файлов задачи в рабочей копии равны индексу. В `cmd/multiverse` я только гонял тесты, файлы не открывал на
запись. Дерево мною не менялось, временный каталог удалён по точному пути.

### Вердикт

**ПРИНЯТЬ** — Critical 0, Major 0, Minor 1, Nit 0.

Итерация закрывает все четыре замечания ревью #1, и каждое держится своим мутантом. Пункт 7 держит все четыре
свойства:
- один отказ публикации — один нарратив после повтора;
- постоянный отказ — `dead_letters`;
- повтор после успеха гасится;
- путь смерти ведёт себя так же, как `Narrate`.

Беззвучная потеря из T-220 не вернулась. Id жизненного цикла соответствуют ADR-027 п. 2 и переживают рестарт,
в том числе на пути Б1. Сдвиг распределения исходов T-255 доказан как следствие генератора id, а не правил.

Новое замечание одно: у двойника появился путь паники `WithCauseID` на действии без `id` (Mi-A). Путь достижим
только при выключенной проверке конверта при чтении, поэтому это Minor.

### 1. Пункт 7 — окно нарратора на `Dedup`

Порядок `Has` → публикация → при ошибке возврат без `Add` → `Add` после успеха есть в обоих путях:
`fake_narrator.go:418-437` (`Narrate`) и `:340-347` (смерть в `Observe`). `Dedup.Has`/`Add` пустой id не
запоминают — как прежнее окно. Своё окно и `window_test.go` удалены. Отсутствие снапшота обосновано:
`FakeNarrator` не stateful-контекст C-14, это названо в комментарии поля и в README.

| Сценарий | Как проверено | Итог |
|---|---|---|
| один отказ публикации (P1 ревью T-220) | тест автора `APublicationThatFailedOnceIsToldAfterTheRetry` | один нарратив, 2 попытки, 0 `dead_letters` |
| постоянный отказ | тест автора `APublicationThatAlwaysFailsEndsInDeadLetters` | `dead_letters` с исходным событием, попыток публикации столько же, сколько попыток шины |
| смерть, постоянный отказ, через подписку | мой зонд RA | `dead_letters` с фактом смерти, 0 нарративов, окно не обрывает повторы |
| смерть, один отказ, через подписку, затем повторная доставка | мой зонд RB | ровно одна смерть, 0 `dead_letters` |
| повторная доставка после успеха, через подписку | мой зонд RC и тест автора `AnEventDeliveredTwiceIsToldOnce` | один нарратив |

Мутанты на пункт 7 (все убиты):
- **M7a** — смерть по `Seen`, то есть запомнить до публикации. Это возврат беззвучной потери. Убит
  `ADeathWhosePublicationFailedIsToldOnTheRetry`, RA, RB;
- **M7b** — `Narrate` запоминает и при ошибке. Убит тремя тестами отказа автора;
- **M7c** — смерть без `Has`. Убит `AnEventDeliveredTwiceIsToldOnce`, `ADeathWhose…`, RB;
- **M7d** — `Narrate` без `Add`. Убит `ATurnWhosePublicationFailed…`, `AnEventDeliveredTwice…`, RC.

### 2. `WithCauseID` для жизненного цикла

- `encounter.started`: `Derive(вход, …, WithCauseID(enc.id))` (`:727-736`), `enc.id = "encounter-"+вход.ID`.
- `encounter.ended`: `Derive(причина пакета, …, WithCauseID(enc.id))` (`:1265-1267`). Причина — действие
  закрывающего удара или бегства, для Б1 — чужой факт.

Id строится до пакета и называется в `opened_by_event_id`/`closed_by_event_id`. Публикация — только после
факта (`announce`, `:1281-1304`). Всё это совпадает с решением оркестратора и ADR-026 п. 1.

**Части id по таблице ADR-027 п. 2.**
- Две встречи от одного входа невозможны по построению: `entered` открывает одну встречу, и её id выводится из
  id входа.
- Одна причина с разными встречами различается частью `enc.id`.
- Начало и конец одной встречи различаются типом (зонд RE).
- Конец одной встречи до и после рестарта:
  - путь удара — тест автора `TheLifecycleOfAFightKeepsItsIdentifiersAcrossARestart`;
  - путь Б1 — мой зонд RD: свежий двойник на тех же входе и чужом факте даёт тот же id `encounter.ended`, его же
    называет `closed_by_event_id`, и он равен `Derive(факт, encounter.ended, WithCauseID(id встречи))`.
- Мутанты на части id убиты:
  - **MID** — конец с лишней частью `round`: убит тестом автора и RD. Тест закрепляет именно части, а не только
    одинаковость;
  - **MIS** — начало с частью `region` вместо id встречи: убит тестом автора.

**Причина без id.** Паника `Derive` на причине без `id` достижима — Mi-A ниже.

### 3. Сдвиг распределения исходов T-255

`FixedMechanics` выбирает строку таблицы по `(id причины, индекс броска)` (`testkit/mechanics/fixed.go:85-89`).
Процессный тест ставит `testkit.Deterministic` — последовательные id на каждый бой. Правила и механика в индексе
не менялись. Эксперимент на копии индекса, `TestTheProcessRunsTheFightsOfIAlpha`, `-count=3`:

| Вариант `fake_encounter.go` | нарративы / концы |
|---|---|
| текущий (`dc555f11`) | `death:5 turn:64–68`, `npc_dead:11 players_out:5`; во втором прогоне серии `death:4`, `npc_dead:12` |
| **MG** — текущий, но генератор тратит по id там же, где итерация 1 (холостой `Derive` без опции у двух событий) | `death:1–2 turn:49–58`, `npc_dead:14–15 players_out:1–2` |
| блоб итерации 1 `2ac07829` | `death:1–3 turn:51–52`, `npc_dead:13–15 players_out:1–3` |

Одна строка — возврат расхода генератора — возвращает распределение итерации 1. Значит, сдвиг вызван только
тем, что `WithCauseID` не зовёт генератор (решение (2) оркестратора по T-417), и последовательность id действий
сместилась.

От удачи тест не зависит. Нижние границы — хотя бы один нарратив `entry`/`world_event`/`turn` и хотя бы один
`npc_dead` на 16 боёв — при 11–15 `npc_dead` во всех вариантах держатся с большим запасом. Смерть закреплена
отдельным тестом с детерминированным миром. Счёт `turn` плавает между прогонами (64–68) и в итерации 1 плавал
тоже: процесс не детерминирован по планировщику, и тест на точное число не опирается.

### Статус замечаний ревью #1

| # | Статус | Доказательство |
|---|---|---|
| Mi-1 | закрыто | Условие `&& enc.orphaned != enc.downed.ID` в `advanceOne` (`:1368-1369`), `Error` один раз в `drop` (`:1321-1329`). Тест автора `AFightWhoseClosureStateRefusedStillAnswersItsActions` — это мой сценарий P1: отложенное бегство решено ровно раз, закрытие предложено один раз. Мутант Mi1 (условие снято) убит им |
| Mi-2 | закрыто | `openFight` проваливает тест, если бой не открылся (`fake_encounter_test.go:1669-1676`). Мутант MA («встреча никогда не активна») красит 51 тест, в том числе все четыре из замечания: `EveryTurnOfTheFightIsToldOnce`, `OneEntityOneChangeSet`, `AnActionAfterTheFightIsOverIsAnsweredWithSilence`, `ARedeliveredActionIsAnsweredOnce` |
| Mi-3 | закрыто | Мутант MB (`engaged` без фазы создания) убит `TwoCharactersEnteringBeforeTheFirstFightExistsDoNotShareOneWolf`. Мутант MC (откладывать только на закрытие) убит `AnActionDuringTheCreationIsAnsweredOnceTheFightExists` и стендом нарратора |
| N-1 | закрыто формулировкой | Дифф `harness.go` — только комментарий. Довод «детерминированно порядок не отличить» принимаю: окно — между двумя захватами одного мьютекса в одном вызове `Observe` |

Причина Б1 — `resolve`: строка владения `task`/`encounter` её разрешает (`shared/contracts/ownership.go:98`),
`contracts check` зелёный, мутант r1 автора в диффе теста — литерал.

### Новые замечания

**Mi-A (Minor) — действие без `id` роняет двойник паникой `WithCauseID`; путь Б1 от неё защищён случайно.**
`FakeEncounter.Act` (`fake_encounter.go:599-622`) не проверяет `ev.ID` и доходит до
`Derive(ev, encounter.started, …, WithCauseID(enc.id))` (`:727-736`). Удар или бегство без `id` так же дойдут до
`endEvent` (`:1265-1267`) на закрывающем пакете. `WithCauseID` на причине без `id` паникует (`cause_id.go:48-50`).

Зонд RF: вход с `ID=""` → `panic: eventbus: WithCauseID on "encounter.started", an event without a cause`.

Путь Б1 от паники не падает (зонд RG: факт смерти волка с `ID=""` → `Observe` возвращает `nil`), но только
случайно. Условие Mi-1 сравнивает `enc.orphaned` (`""`) с `enc.downed.ID` (`""`), равенство выключает
закрытие. Мутант Mi1 превращает RG в ту же панику. Без мутанта бой молча не закрывается.

Достижимость узкая, поэтому Minor. `membus.Publish` конверт без `id` не пропускает (`Route` → `ValidateEnvelope`,
`registry.go:150`). При проверке при чтении по умолчанию такой конверт уходит в `dead_letters`. Остаётся
`MV_BUS_VALIDATE_ON_READ=false` и конверт от продюсера без проверки (Kafka, профиль `legacy`). Двойник стоит в
процессе под `MV_SWARM_FAKE=true`, и паника без `recover` роняет процесс со всеми контекстами.

Это регрессия итерации: в `2ac07829` `Derive` без опции не паниковал. Кроме того, это расхождение с правилом
`shared/eventbus/README.md:94-99`: «обработчик, который может работать без валидации при чтении, проверяет
`ev.ID != ""` до `Derive(…, WithCauseID(…))»`. Двойник — образец для T-230, и настоящий агент унаследует
пропуск.

*Как исправить:*
- в `Act` до `acted.Seen` — `if ev.ID == "" { Warn; return nil }`;
- в `downedBy` — явно пропускать факт без `id` с `Warn` вместо случайного `"" != ""`;
- закрепить тестом по зондам RF/RG.

### Что проверено экспериментом

| Проверка | Итог |
|---|---|
| `go build ./... && go vet ./...` (копия индекса) | зелёные |
| `go test -short -count=1 ./...` (копия индекса) | 27 пакетов ok, выход 0 |
| `go test -tags e2e -count=1 ./test/...` | `test/e2e` ok, `test/fixtures` ok |
| процессные тесты T-255 `-count=3` (`RunsTheFightsOfIAlpha`, `TellsTheDeathOfACharacter`, `OtherRefusalsOfTheFakeNameTheFlag`) | PASS; распределение — §3 |
| покрытие | swarm 89,6 % (совпадает с заявленным), gateway 92,6 % |
| `golangci-lint run ./...` | 0 issues |
| `go run ./cmd/mvctl contracts check` | ok: 65 типов, 8 топиков, 58 схем |
| мои зонды RA, RB, RC, RD, RE, RG | зелёные |
| мой зонд RF | красный — Mi-A |

| # | Мутант | Итог |
|---|---|---|
| M7a | смерть по `Seen` (запомнить до публикации) | убит |
| M7b | `Narrate` запоминает и при ошибке | убит |
| M7c | смерть без `Has` | убит |
| M7d | `Narrate` без `Add` | убит |
| MB | `engaged` без фазы создания | убит юнит-тестом (Mi-3) |
| MC | отсрочка только на закрытие | убит юнит-тестом и стендом (Mi-3) |
| MA | встреча никогда не активна | 51 тест красный, все четыре из Mi-2 |
| Mi1 | условие `orphaned` снято | убит тестом автора (и RG — паникой) |
| MID | конец с лишней частью id | убит |
| MIS | начало с частью `region` | убит |
| MG | возврат расхода генератора (эксперимент, не мутант-тест) | распределение итерации 1 — §3 |

Стенды `-count=20` гонялись после удаления копии, на рабочем дереве. Перед прогоном проверено, что
`git diff --quiet -- shared/` пуст, то есть дерево равно индексу. Результат:
- стенд пары `TestTheHarnessAndTheEncounterOfTheSwarmOnOneBus` — ok;
- стенд нарратора `TestTheNarratorTellsTheFightsOfTheStand` — ok;
- стенды харнесса `TestTheSkirmish*` — ok.

### Предложения в бэклог

1. T-229: применить `WithCauseID` к `narrative.output` (у двойника id нарративов по-прежнему из генератора —
   названо автором) и проверку `ev.ID != ""` до неё в каждом обработчике, выводящем id из причины.
2. T-230: взять проверку пустого `id` из исправления Mi-A в образец агента встречи.

---

<!-- review T-201 #1 -->
## T-201 · ревью #1 · 2026-09-13 · TEAM-2/code-reviewer#1

### Границы ревью

- **Ветка:** `task/T-201-agent-blueprint-v2`, HEAD `cd21a48` = merge-base с `epic/EPIC-003-swarm-llm-laws`.
  Эпик с тех пор ушёл вперёд на слияние T-206 (`b18ecd6`); `.golangci.yml` и `shared/agent` там не менялись.
- **Изменения не закоммичены.** В индексе 16 переименований `R100`, в рабочей копии — 8 изменённых файлов и
  21 новый файл. Файлов в индексе 974 и до переноса (`ls-tree HEAD`), и после (`ls-files`): ничего не удалено.
- **Входы:**
  - раздел T-201 `tasks.md` (ревизия 4) из ветки эпика, карточка `tasks/T-201.md`, запись `dev-log T-201`;
  - КД `swarm-llm-laws.md` §2, §3 (сигнатуры `shared/agent`), §11.4, §13.1–13.3;
  - `contracts.md` C-11, `api-contracts.md` §3.1–3.3, ADR-015, ADR-025;
  - решение 3 ревизии 4 (форма `sha256:<64 hex>`);
  - решение оркестратора об исключении `shared/agent/` (журнал develop, 2026-09-13): перевод на `shared/clock`
    в самой T-201, исключение снять, приёмка правки `.golangci.yml` — tech-lead#1;
  - `threat-model.md` B8/EP-07/T-26 (`content_hash` — контроль подмены блупринта).
- **Метод.** Прогоны — на рабочей папке, только чтение. Зонды и мутанты — в копии `go.mod`, `go.sum`,
  `.golangci.yml`, `shared/agent`, `shared/clock` в scratch, без `-overlay`, по полному (не короткому) пути.
  Копия до мутаций зелёная. Контрольный мутант шёл первым. После каждого мутанта файл восстанавливался из эталона.
  В конце `parser.go` и `lod.go` копии сверены `cmp` с рабочей папкой. Копия удалена по сохранённому точному пути.
  Docker, стенд LLM и `.env` не трогались.

### Вердикт

**ВЕРНУТЬ** — Critical 0, Major 1, Minor 4, Nit 3.

Пакет написан аккуратно. DoD по пунктам выполнен, заявления автора подтверждены прогонами. Отклонения обоснованы
и T-202/T-203 не ломают. Возврат — из-за одного дефекта `ContentHash` (Ma-1). Кодировку хэша автор закрепил
эталоном, её будут хранить `agent.spawned` и снапшот роя, поэтому исправлять форму нужно до первых потребителей.
Mi-2 чинится тем же изменением. Остальные Minor — локальные правки парсера и тестов.

### Прогоны

| Команда | Результат |
|---|---|
| `go build ./... && go vet ./...` | 0 |
| `go test -short -count=1 ./...` | все пакеты ok, FAIL нет |
| `golangci-lint run ./...` | `0 issues.` (с первого раза) |
| `make test` | 0; порог покрытия `internal/mechanics` 94.9 %, прочие `internal/*` ещё не существуют |

### 1. DoD (специфика T-201)

| Пункт | Итог |
|---|---|
| `ParseFile` разбирает `testdata/blueprints/valid/` (5 `.md`) и чистый YAML (`personal-gm.yaml`); секции — в `Prompts.*` | да (`TestParseFileParsesTheValidCorpus`, `TestParseMarkdownFillsEverySection`, `TestParsePureYAMLEqualsMarkdown`) |
| Неизвестный ключ → ошибка с полем и файлом; плоский `llm.model` → предупреждение, разбор не падает | да; вдобавок строка файла и путь поля (`llm.phase2.provider`, `npc_table[0].count`) |
| `ContentHash` стабилен на 100 разборах, меняется при изменении любого байта, форма `^sha256:[0-9a-f]{64}$` | форма и стабильность — да. «Любой байт» — да для **замены** байта. Для **удаления строки** нарушено: Ma-1 |
| `go build ./...` зелёный; вне `services/_archive/**` на удалённые символы никто не ссылается | да. Импортов `shared/agent` в `cmd/`, `internal/`, `shared/testkit` нет. `services/narrative-orchestrator` импортирует пакет (в т. ч. `tools.NewToolRegistry()`), но собирается из `LEGACY_SRC_REF` через `git archive` (`Makefile:307-317`), не из дерева |
| `golangci-lint run ./shared/agent/...` зелёный; названо, что осталось под исключением | да; исключение снято целиком, под ним ничего не осталось |
| Список переноса по дереву `1c2ee7e` | да, 14 файлов + `README.md`/`MIGRATION.md`. `filter.go` и `e2e_dark_forest_test.go` уже в архиве (T-003) |

**Отклонения автора:**
- **`Severity info`** нужен правилам 7а и 14 КД §13.2 (T-202). Принято.
- **`AgentBlueprint.ParseIssues` (`yaml:"-"`)** сохраняет сигнатуру `ParseFile` из КД §3 и в хэш не входит
  (`TestContentHashIgnoresParseMetadata`). T-202 выдаёт их вместе со своими `Issue`. Принято.
- **`TickLLM.Temperature`/`Thinking`.** DoD T-203 требует `thinking: false` на все фазы и `temperature: 0.7`,
  а `KnownFields(true)` иначе отверг бы их в `tick`. Без этого T-203 не собралась бы — принято.
  `api-contracts.md` §3.1 (`llm.tick`) стоит поправить у владельца (бэклог п. 4).
- **`Temperature *float64`, `Retries *int`.** Отличают «не задано» от явного 0 (в `api-contracts.md` у `retries`
  умолчание 2). Принято.
- **Плоский `llm.schema` — ошибка.** В v2 встроенной схеме места нет, молча выбросить нельзя. as-is блупринты
  новым парсером не читаются: T-203 пишет файлы заново. Принято.
- **Текст вне секций — ошибка.** Скелеты `api-contracts.md` §3.3 и КД §13.3 заголовка `# …` перед секциями не
  имеют, T-203 не задет. Принято.
- **`NewLODManager(cfg, Clock, Timers)`, `NewToolRegistry(Clock)`.** Это прямое следствие решения оркестратора.
  Потребителей в модуле нет, у legacy сборка из `LEGACY_SRC_REF`. КД §2 («без изменений») — к правке в документе
  (бэклог п. 4). Принято.

### 2. `ContentHash`

**Форма.** `"sha256:" + hex.EncodeToString` — всегда нижний регистр. Тест формы есть. Контрольный мутант `sha1:`
красный (4 теста).

**Детерминизм.**
- Ключи сортируются в `writeCanonical`. Порядок ключей и секций, стиль YAML, `.md`/`.yaml`, имя файла, BOM и
  CRLF на хэш не влияют — это проверено тестами автора.
- Зонд P1: восемь вариантов разделителя внутри `## system` (LF, CR, NEL, LS, PS, TAB, « \n», «\n »). Восемь
  разных хэшей. Нормализации переводов строк при круге `yaml.Marshal` → `yaml.Unmarshal` нет: `yaml.v3` берёт
  для таких строк кавычки с экранированием.
- Зонд P2: `value: 1` и `value: 1.0` в `ops[].value` дают один хэш, `temperature: 1` и `1.0` — тоже. Это одно
  JSON-число, для значения поля допустимо. `value: null` и отсутствие `value` — одно значение `nil`, тоже
  допустимо.

**Зависимость от `yaml.v3`.** Числа и строки после круга через YAML пишутся `encoding/json`, так что от
форматирования библиотеки зависят только имена тегов и выбор int/float. Это неизбежно и закреплено эталоном
`TestContentHashEncodingIsPinned`. Чего можно избежать — Ma-1 и Mi-2: хэш считается от `yaml.Marshal` всей
структуры, а не от значения.

**Мутант M1.** `%T:%v` → `%v` в ключе `map[any]any`: убит `TestContentHashOfEdgeValues`.

### 3. Парсер

- **Строгий режим.** `dec.KnownFields(true)` (`parser.go:326`). `UnmarshalYAML` у `ScopeTypes` декодирует только
  скаляр и последовательность. Зонд: лишний ключ рядом, `scope_binding.extra`, ловится с путём. Повторный ключ
  (`mapping key "name" already defined`) и второй документ — ошибки.
- **Сообщения.** Везде `файл:строка: поле: причина`. Строка — строка файла, смещение frontmatter учтено. Путь поля
  есть для unknown field и type error. Для синтаксических ошибок YAML поля нет — это нормально.
- **`---` в блочном скаляре.** Закрытием frontmatter считается первая строка, равная `---` без отступа
  (`TrimRight`, не `TrimSpace`).
  - Зонд P4: `description: |` с отступленной строкой `  ---` разбирается верно (`"a\n---\nb"`).
  - `---` в нулевой колонке внутри блочного скаляра по спецификации YAML — маркер документа, а не содержимое.
    Ограничение совпадает с самим YAML и практически ничего не отнимает.
  - `---` в теле секции остаётся текстом.
- **Безопасность разбора (граница B8: файлы автора, не ввод игрока).**
  - Зонд P7: «billion laughs» из 9 уровней → `document contains excessive aliasing`.
  - Умеренное раздувание (1 КБ × 10³) тоже отвергается.
  - Вложенность 20 000 → `exceeded max depth of 10000`.
  - Защиты `yaml.v3 v3.0.1` работают и во втором проходе `yaml.Unmarshal(…, &root)` в `yamlError`.
  - `fieldPath` в алиасы не спускается, рекурсия ограничена той же глубиной.
  - Размер файла не ограничен (`os.ReadFile`). Для файлов репозитория это не DoS-вектор — бэклог п. 3.

### 4. Перевод на `shared/clock`

- `time.Now`/`time.Since`/`time.NewTicker` в `lod.go` и `tools/registry.go` заменены на внедрённые
  `clock.Clock`/`clock.Timers`. Глобалей нет.
- Тесты на `clock.Manual`: `DecayLOD` на границе `DecayTime`, время смены в статистике, тик `Start` на ручных
  таймерах, латентность и окно rate-limit.
- `TestLODStartTicksOnTheInjectedTimers` детерминирован. Настенный таймер там только как дедлайн 5 с и опрос
  1 мс, результат от них не зависит.

**Гонка в `tools/registry.go:163-197` — в бэклог, не Major.**
- `checkRateLimit` меняет `limiter` под `RLock`, `updateStats`/`GetStats` вообще без блокировки.
- Дефект as-is, T-201 его не вносила: сменилась только строка источника времени.
- В модуле у пакета нет потребителей. Правило 9 валидатора (КД §13.2) в MVP-1 запрещает любые `tools`.
- DoD исправления не требует, а `-race` локально не запустить (нет cgo).
- Условие: исправить до первого потребителя в `internal/swarm` (бэклог п. 1).

### 5. Снятие исключения линтера

- Исключение `path: shared/agent/` (`errcheck`, `forbidigo`, `staticcheck`, `unused`) удалено. На его месте —
  комментарий в стиле соседних записей. Других упоминаний `shared/agent` в `.golangci.yml` нет.
- Мутант L1 в копии: функция с `time.Now()` в `shared/agent/lod.go`.
  - Контроль: копия без мутанта — `0 issues`.
  - С мутантом: `lod.go:316:49: use of time.Now forbidden … (forbidigo)`, код 1.
  - Правило действует на пакет.
- Правка `.golangci.yml` в ветке задачи разрешена решением оркестратора, но требует приёмки tech-lead#1
  (`ownership.md` §1). Это не замечание — отмечено в рисках.

### 6. Архив

- **Перенос.** Только `git mv`: 16 × `R100`, 0 вставок и 0 удалений в индексе, число файлов 974 → 974.
  `services/_archive/go.mod` без `require` держит архив вне `./...` корня.
- **Строки.** `services/_archive/README.md` (2 строки), `services/_archive/shared/agent/ARCHIVED.md` (раздел
  T-201: причина, последние рабочие коммиты, «что использовало»), `Docs/archive/README.md` (2 строки).
  Tech-writer назван к уведомлению.
- **`types.go`** сверен с `agent_types.go@HEAD`: значения `iota` и строки `String()` у трёх типов совпадают.
- **Устаревшие ссылки вне задачи:** `PULL_REQUEST.md:21-29` (исторический файл) и документы, перечисленные
  автором (бэклог п. 4).

### 7. Мутанты и зонды ревьюера

| # | Изменение | Результат |
|---|---|---|
| M0 | контрольный: `"sha256:"` → `"sha1:"` (`parser.go:553`) | убит (4 теста) |
| M1 | ключ `map[any]any` без типа: `%T:%v` → `%v` (`parser.go:579`) | убит `TestContentHashOfEdgeValues` |
| M2 | закрытие fence любым маркером: `case marker == fence:` → `default:` (`parser.go:201`) | **выжил** (Mi-4) |
| L1 | `time.Now()` в `shared/agent/lod.go` + `golangci-lint run ./shared/...` | красный (`forbidigo`), контроль зелёный |
| P1 | 8 вариантов разделителя строк в секции | 8 разных хэшей |
| P2 | `1`/`1.0`, `null`/отсутствие, `"1"`/`1`, `true`/`"true"` | одно значение → один хэш; разные значения → разные |
| P3 | `allowed_event_types: []` против отсутствия ключа | **один хэш** при разных значениях (Ma-1) |
| P4 | `---` в блочном скаляре и в теле | отступленный — верно; нулевая колонка — ошибка с файлом и строкой |
| P5 | незакрытый ` ``` ` в `## system`, за ним `## phase2` | **принят**: `phase2` пуст, текст задания — внутри `system` (Mi-1) |
| P6 | frontmatter из одного комментария; `~`; список; `<<` merge | `bp.md: EOF` без строки (N-1); `~` — пустой блупринт без ошибки (N-2); список — type error; merge работает |
| P7 | billion laughs, раздувание 10³, вложенность 20 000 | отвергнуты `yaml.v3` |

### Замечания

| # | Серьёзность | Файл:строка | Суть | Как исправить |
|---|---|---|---|---|
| Ma-1 | Major | `shared/agent/parser.go:530-554`; `shared/agent/parser_test.go:191` | **Хэш не различает то, что различает сам парсер.** `ContentHash` считается от `yaml.Marshal(bp)`, а `yaml.v3` пишет `nil` и пустой срез одинаково (`[]`). Зонд P3: `allowed_event_types: []` и блупринт без ключа дают один хэш, хотя значения разные (`[]string{}` против `nil`). Тест автора прямо требует это различие (`owned_entity_types: []` — «empty non-nil list»). По `api-contracts.md` §3.1 `allowed_event_types[]`/`owned_entity_types[]` обязательны на всех уровнях, а `[]` — законное значение у нарраторов. **Следствие:** у невалидного блупринта (ключа нет) и валидного (`[]`) один `content_hash`. `content_hash` уходит в `agent.spawned`/`agent.blueprint_reloaded` и снапшот роя, а в модели угроз (B8, T-26) служит контролем смены содержимого. Реестр, сравнивающий хэш при перезагрузке (КД §2 `registry.go`, I2), не увидит исправления файла. Кодировка закреплена эталоном — чем позже правка, тем больше сохранённых хэшей она сдвинет. | Строить каноническую форму от значения, а не от `yaml.Marshal`: обход `reflect` по тегам `yaml` (имя до запятой, `-` пропускать). **Правила:** `nil`-указатель, `nil`-срез/map и нулевой скаляр — поле не пишется; непустой или **пустой ненулевой** срез — `[...]`/`[]`; `any` — как сейчас через `writeCanonical`. **Тесты:** пара «ключа нет / `[]`» для `allowed_event_types` и `owned_entity_types` даёт разные хэши; свойство «равные значения ⇔ равные хэши» на корпусе `valid` (разбор → `reflect.DeepEqual` против сравнения хэшей на парах мутаций). Эталон `TestContentHashEncodingIsPinned` обновить осознанно. |
| Mi-1 | Minor | `shared/agent/parser.go:191-231` | **Незакрытый fence молча съедает следующие секции.** Зонд P5: ` ``` ` без пары в `## system` — всё до конца файла, включая `## phase2 …`, уходит в `Prompts.System`, `Phase2` пуст, ошибки нет. Для `personal-gm` это задание фазы 2 внутри системной роли. Правило 13 КД §13.2 требует только `## system`, так что валидатор этого не поймает. | После цикла: `fence != ""` → `ParseError{Line: <строка открытия>, Field: "## <секция>", Reason: "code fence is not closed"}`. Тест в `TestParseRejectsMalformedBlueprints`. |
| Mi-2 | Minor | `shared/agent/parser.go:541`; `shared/agent/blueprint.go:17-71` | **Хэш зависит от набора полей типа.** `yaml.Marshal` пишет нулевые значения всех полей, поэтому новое необязательное поле в `AgentBlueprint` сдвинет `content_hash` каждого существующего блупринта, даже не использующего поле. После обновления бинарника все хэши в снапшоте разойдутся с файлами. Эталонный тест сделает это осознанным, но не избежит. | То же исправление, что в Ma-1: нулевые и `nil`-значения в каноническую форму не пишутся. Тогда добавление поля меняет хэш только у блупринтов, которые его задают. В комментарий `ContentHash` — правило эволюции. |
| Mi-3 | Minor | `shared/agent/placeholders.go:55` | **Опечатку в плейсхолдере никто не заметит.** Регулярка берёт только `{[a-z]…}` без пробелов, так что `{Player.name}`, `{ player.name }`, `{player-name}`, `{region.Description}` вообще не считаются плейсхолдерами. `UnknownPlaceholders` их не вернёт, правило 13 (T-202) пропустит, а `internal/llm/prompt` не подставит: в LLM уйдёт литерал. Защита от JSON (`{"text": …}`) при этом нужна. | Второй, «подозрительный» шаблон: `\{\s*[A-Za-z][\w.-]*\s*\}`, который не совпал с точным. `SuspiciousPlaceholders(text)` (или флаг в `ExtractPlaceholders`) для T-202 — `warning`/`error` на выбор валидатора. Тест на четыре приведённые формы и на JSON-пример. |
| Mi-4 | Minor | `shared/agent/parser_test.go:379-387` | **Смешанные fence не проверены тестом.** Мутант M2 (любой маркер закрывает fence) выжил: тест есть только на `~~~ … ~~~`. Реализация верна (зонд P5: ` ``` ` / `~~~` / ` ``` ` — верно), но не защищена. | Случай ` ``` ` → `~~~` → `## tick` → ` ``` `: `## tick` остаётся текстом `system`. |
| N-1 | Nit | `shared/agent/parser.go:327-328` | Frontmatter из одного комментария (`---\n# note\n---`) даёт `bp.md: EOF` — без строки и причины: `Decode` вернул `io.EOF`, `yamlError` отдал его как есть. Файл назван, но суть — нет. | `errors.Is(err, io.EOF)` в первом `Decode` → та же ошибка, что для пустого YAML: `Line: lineOffset+1`, `Reason: "empty YAML: …"`. Тест. |
| N-2 | Nit | `shared/agent/parser.go:321-339` | `---\n~\n---` (frontmatter `null`) разбирается в пустой блупринт без ошибки, хотя пустой frontmatter отвергается. До валидатора дойдёт, но две «пустые» формы ведут себя по-разному. | Если текст не пуст, а `bp` после `Decode` нулевой, — та же ошибка «empty YAML». |
| N-3 | Nit | `shared/agent/parser.go:491`, `513` | Плоские `llm.max_tokens: 0` и `llm.model: ""` молча отбрасываются: миграции нет, предупреждения тоже. Значение совпадает с «не задано», но автор as-is файла не узнает, что ключ устарел. | Отмечать присутствие ключа (указатель или `yaml.Node`), а не значение, и выдавать то же предупреждение о миграции. |

### Открытые вопросы

Нет. Решение по Ma-1 техническое, в пределах T-201.

### Предложения в бэклог

1. **`shared/agent/tools/registry.go` (до первого потребителя в `internal/swarm`).** Гонка данных:
   `checkRateLimit` пишет под `RLock`, `updateStats`/`GetStats` без блокировки. Там же — ключи `lodDistribution`
   в `lod.go`, не совпадающие с `LODLevel.String()`, и `lastHighLOD`, который пишется при любой смене
   (найдено автором). Проверка — `go test -race` в CI (Linux, cgo).
2. **Повторная проверка Ma-1 и Mi-2 в T-202/T-2xx (`BlueprintRegistry`).** Решение «перезагружать ли» принимать по
   `content_hash` только после исправления Ma-1. Тест реестра: файл без ключа → `[]` вызывает перевалидацию.
3. **Потолок размера блупринта** в `ParseFile` (например, 1 МиБ через `io.LimitReader` + ошибка «too large»).
   Не DoS-вектор при файлах репозитория (B8), но дешёвая защита для `mvctl blueprint validate <dir>` на
   произвольном каталоге и для I2 hot reload.
4. **Документы (владельцы: architect#2, tech-lead#2, tech-writer).**
   - `api-contracts.md` §3.1 `llm.tick` без `temperature`/`thinking`; КД §2 «`lod.go` / `tools/registry.go` без
     изменений» (конструкторы сменились);
   - `diagrams/c4-component-swarm-llm-laws.md:45`, шапка КД §2, ADR-015 п. 6, `tasks.md` T-203 «удалён» →
     «перенесён в архив» (найдено автором);
   - `PULL_REQUEST.md:21-29` ссылается на перенесённые `md_parser.go`, `examples/`, `README.md`, `MIGRATION.md`.

---

<!-- review T-201 #2 -->
## T-201 · ревью #2 (итерация 2: замечания ревью #1) · 2026-09-13 · TEAM-2/code-reviewer#1

### Границы ревью

- **Что проверялось.** Исправления итерации 2 и регрессия от них: подраздел «Итерация 2» карточки, запись
  `dev-log T-201 iteration 2`.
- **Файлы итерации:** новые `shared/agent/content_hash.go` и `content_hash_internal_test.go`; изменённые
  `parser.go`, `blueprint.go`, `placeholders.go` и тесты к ним. `.golangci.yml` в итерации не менялся.
- **Состояние ветки.** Коммита по-прежнему нет, HEAD `cd21a48`, переименования в индексе — те же 16 × `R100`.
- **Метод.** Прогоны — на рабочей папке, только чтение. Мутанты и зонды — в копии `go.mod`, `go.sum`,
  `.golangci.yml`, `shared/agent`, `shared/clock` в scratch, без `-overlay`, по полному пути.
  - Копия до мутаций зелёная, контрольный мутант первым.
  - Файл после каждого мутанта восстанавливался из эталона.
  - `content_hash.go` и `parser.go` копии сверены `cmp` с рабочей папкой.
  - Копия удалена по сохранённому точному пути.

### Вердикт

**ВЕРНУТЬ** — Critical 0, Major 1, Minor 2, Nit 2.

Замечания ревью #1 закрыты по существу. Новая каноническая форма верно различает `nil` и `[]`, отсутствие ключа и
указатель на ноль и не зависит от набора полей типа. Но переход с `yaml.Marshal` на обход `reflect` принёс
регрессию (Ma-2): `yaml.v3` читает незакавыченную дату в `any` как `time.Time`, у которой все поля
неэкспортируемые, поэтому **любые даты дают один хэш**. Итерация 1 даты различала. Правка локальная, в той же
функции, что и Mi-5.

### Прогоны

| Команда | Результат |
|---|---|
| `go build ./... && go vet ./...` | 0 |
| `go test -short -count=1 ./...` | все пакеты ok |
| `golangci-lint run ./...` | `0 issues.` (с первого раза) |
| `make test` | 0; `shared/agent` 92.7 %, `shared/agent/tools` 68.1 %; порог `internal/mechanics` 94.9 % |

### 1. Статус замечаний ревью #1

| # | Статус | Проверка |
|---|---|---|
| Ma-1 | закрыто | `TestContentHashTellsAMissingKeyFromAnEmptyValue`: 9 пар «нет / `[]` / `{}` / `0`», у каждой проверено, что парсер их различает. Свойство «равные значения ⇔ равные хэши» проверяется на всех разобранных мутациях байт трёх файлов. Мутант M5 (указатель на ноль пропускается) убит двумя тестами. Зонд: `[]`/`{}`, `[a]`/`{a: ~}`, `"a<NUL>b"`/`"a\\u0000b"`, `"\t"`/`"\\t"` — разные хэши. Регрессия по датам — Ma-2 |
| Mi-1 | закрыто | `ParseError` на строке открытия с полем секции (`parser.go:234-242`); fence закрывается только своим маркером |
| Mi-2 | закрыто | `TestCanonicalFormIgnoresFieldsLeftAtZero`: тип с лишними нулевыми полями, `yaml:"-"` и неэкспортируемым полем даёт тот же канонический текст. Мутант M4 (без сортировки) убит 4 тестами |
| Mi-3 | закрыто для латиницы | `SuspiciousPlaceholders` ловит `{Player.Name}`, `{ player.name }`, `{player-name}`, `{region.Description}`. Кириллица — Mi-6 |
| Mi-4 | закрыто | M2 повторён: убит (`TestParseKeepsSectionHeadingsInsideMixedFences`, `TestParseRejectsMalformedBlueprints`) |
| N-1 | закрыто | `empty YAML: only comments` со строкой (`.md` и `.yaml`) |
| N-2 | закрыто | `~` и `{}` → `empty YAML: the document sets no field` со строкой |
| N-3 | закрыто | плоские ключи — указатели; предупреждение выдаётся по наличию ключа, нулевое значение отбрасывается без создания `phase2` (`parser.go:536-564`) |
| бэклог п. 3 | сделано | `MaxBlueprintSize = 1 << 20`, `io.LimitReader(…, Max+1)`, тесты на ровно 1 МиБ и на 1 МиБ + 1 байт |

### 2. Каноническая форма: различимость и детерминизм

**Проверено и верно:**
- **Ключи.** Поля структуры сортируются по имени тега, map — по ключу.
- **`nil` против пустого.** `nil`-срез/map/указатель и нулевой скаляр у поля не пишутся. Пустой ненулевой срез
  пишется как `[]`. `nil` внутри `any` — `null`, и он отличается от `[]`/`{}`.
- **Строки.** `strconv.Quote` инъективен: управляющие символы и `\uXXXX` отличаются от их экранированной записи
  (зонд).
- **Числа.** Число не совпадает со строкой: `NaN`/`±Inf` пишутся без кавычек. `uint64` вне `int64` — отдельной
  ветвью.
- **Независимость от `yaml.v3`.** Энкодер `yaml.v3` больше не участвует. От библиотеки остаётся только типизация
  декодера (int/float/`time.Time` в `any`) — отсюда и Ma-2.
- **Стабильность.** Порядок ключей, `.md`/`.yaml`, CRLF, BOM, имя файла — тесты автора зелёные.

**Зонды — где различимость или детерминизм теряются:**

| Пара значений в `ops[].value` (или поле) | `reflect.DeepEqual` | Один хэш | Вывод |
|---|---|---|---|
| `2001-01-01` / `2020-06-01` (без кавычек → `time.Time`) | нет | **да** | Ma-2 |
| `!!timestamp 2001-01-01` / `!!timestamp 2020-06-01` | нет | **да** | Ma-2 |
| `{1: a}` / `{"int:1": a}` | нет | **да** | Mi-5 |
| `{1: a, "int:1": b}`, 200 разборов одного файла | — | **2 разных хэша** | Mi-5, недетерминизм |
| `temperature: -0.0` / `temperature: 0` | да | нет | N-1 |
| `1` / `1.0` | нет (int/float64) | да | N-1 (сознательно) |
| `1000000` / `1000000.0` | нет | нет | N-1 (непоследовательно с `1`/`1.0`) |
| `.nan` / `.nan` | нет (`NaN != NaN`) | да | верно |
| `!!binary aGVsbG8=` / `hello` | да | да | верно |
| пустая `## canon` / без секции | да (`nil`) | да | верно |

### 3. `SuspiciousPlaceholders`

Ложных срабатываний на JSON нет: `{"text": …}`, `{ "text": … }`, `{text: string, mood: string}`,
`{A, B}`, `{a|b}`, `[A-Z]{2,5}`, `{0}`, `{name!r}`, `{#id}` не находятся. Go-шаблоны `{{.Name}}` и
`{{ .Player.HP }}` тоже не находятся.

Находятся:
- `{{Name}}` → `{Name}`, `{N}`, `{Note}` — это действительно уйдёт в модель литералом, находка по делу;
- `\frac{A}{B}` → `{A}`, `{B}` и `${HOME}` → `{HOME}` — ложные срабатывания, но редкие для промпта GM.

Итог: для правила 13 это годится при серьёзности `warning`. Выбор серьёзности — T-202 (бэклог п. 2).

**Кириллица — Minor (Mi-6), не бэклог.** `{игрок.имя}`, `{Игрок}`, `{ имя }` не находит ни точный, ни
нестрогий шаблон: в RE2 `\w` и `[A-Za-z]` только ASCII. Промпты пишутся по-русски (`locale: ru`, секции корпуса),
так что опечатка кириллицей — самый вероятный случай, который Mi-3 и должен был ловить. Правка в одну строку.

### 4. Мутанты ревьюера

| # | Мутант | Результат |
|---|---|---|
| M0 | контрольный: `"sha256:"` → `"sha1:"` (`content_hash.go:49`) | убит (5 тестов) |
| M2 | повтор: `case marker == fence:` → `default:` (`parser.go:210`) | убит 2 тестами |
| M3 | `nil`-срез пишется как `[]` (условие `IsNil` отключено, `content_hash.go:73`) | убит `TestContentHashOfEdgeValues` |
| M4 | без сортировки записей (`content_hash.go:172`) | убит 4 тестами |
| M5 | поле-указатель на нулевое значение пропускается (`content_hash.go:139`) | убит `TestCanonicalFormIgnoresFieldsLeftAtZero`, `TestContentHashTellsAMissingKeyFromAnEmptyValue` |

### Замечания

| # | Серьёзность | Файл:строка | Суть | Как исправить |
|---|---|---|---|---|
| Ma-2 | Major | `shared/agent/content_hash.go:93-94`, `123-145` | **Все даты хэшируются одинаково.** Незакавыченное `2001-01-01` (и `!!timestamp`) `yaml.v3` декодирует в `any` как `time.Time`. `writeStruct` пропускает неэкспортируемые поля, и любая дата пишется как `{}`. Зонд: `ops[].value` и `conditions[].value` с разными датами дают один хэш при `DeepEqual == false`. Это ровно то свойство, которое чинил Ma-1: смена даты в блупринте не меняет `content_hash`, реестр I2 её не увидит, а контроль T-26 (B8) обходится обычной правкой. Итерация 1 даты различала (через круг `yaml.Marshal`), это регрессия. Шире: любая структура с неэкспортируемыми полями, попавшая в `any`, схлопывается в `{}`. | В `writeCanonical` до общего `reflect.Struct`: `time.Time` → строка с меткой типа без кавычек, например `time:` + `t.UTC().Format(time.RFC3339Nano)` (с кавычками она совпала бы со строкой). Для прочих структур внутри `any`, у которых есть неэкспортируемые поля, — ошибка «no canonical form» (→ `""`, как у func/chan). Тесты: две даты → разные хэши; дата ≠ строка с тем же текстом; `TestContentHashChangesWithAnyByteOfTheContent` на файле корпуса с датой в `ops[].value` или `conditions[].value`. |
| Mi-5 | Minor | `shared/agent/content_hash.go:162-165`, `172` | **Ключ «тип:значение» совпадает со строковым ключом, хэш недетерминирован.** Нестроковый ключ пишется как `strconv.Quote("int:1")` — байт в байт как строковый ключ `"int:1"`. Зонд: `{1: a}` и `{"int:1": a}` дают один хэш. Файл `value: {1: a, "int:1": b}` на 200 разборах дал **2 разных хэша**: два элемента с равным ключом, `sort.Slice` нестабилен, порядок обхода map случаен. Вход надуманный, но нарушает пункт DoD «стабилен на повторных разборах». | Писать нестроковый ключ так, чтобы он не мог совпасть с `Quote` строки: без кавычек (`int:1`, `bool:true`) или с байтом-меткой вне JSON-строки. Сравнение в `writeEntries` оставить строгим — после правки ключи уникальны. Тест: `{1: a}` ≠ `{"int:1": a}`; хэш `{1: a, "int:1": b}` одинаков на 100 разборах. |
| Mi-6 | Minor | `shared/agent/placeholders.go:62` | `suspiciousRe` не видит кириллицу: `{игрок.имя}`, `{Игрок}`, `{ имя }` не находит ни одна функция, и в LLM они уйдут литералом. Для русскоязычных промптов это главный случай опечатки. | `\{\s*\p{L}[\p{L}\p{N}_.\-]*\s*\}`. Точная форма по-прежнему отсекается `isExactPlaceholder`. Тест на три приведённые формы и на JSON-пример. Возможный шум на русских фигурных скобках в прозе принимается уровнем `warning` в T-202. |
| N-1 | Nit | `shared/agent/content_hash.go:105-116` | Числа нормализуются непоследовательно. `1` и `1.0` дают один хэш (форма `'g'`), а `1000000` и `1000000.0` — разные (`1000000` против `1e+06`). `temperature: -0.0` и `0` — разные хэши при равных значениях. Вреда нет: лишнее «изменилось», не пропуск. | Выбрать одно правило: либо метка типа у всех чисел (совпадает с `DeepEqual`), либо целочисленный float в пределах `int64` писать как целое и `-0` как `0`. Поправить комментарий и тест `hash(1) == hash(1.0)` под выбранное. |
| N-2 | Nit | `shared/agent/parser.go:251-260` | Строка, начинающаяся с инлайн-кода ` ```json``` пример`, считается открытием fence, и теперь это громкая ошибка «not closed». По CommonMark такой строки-забора нет: у backtick-fence в info string не бывает обратных кавычек. | Для ` ``` ` не считать строку забором, если после маркера в ней есть ещё обратная кавычка. Тест. |

### Открытые вопросы

Нет.

### Предложения в бэклог

1. Прежние пункты ревью #1 п. 1, 2, 4 (гонка `tools/registry.go`, решение о перезагрузке по `content_hash` в I2,
   документы) остаются в силе; п. 3 выполнен в итерации 2.
2. **T-202, правило 13.** `SuspiciousPlaceholders` — `warning`, `UnknownPlaceholders` — `error`. Ложные
   срабатывания зонда (`\frac{A}{B}`, `${HOME}`) не должны блокировать активацию блупринта.

---

<!-- review T-201 #3 -->
## T-201 · ревью #3 · 2026-09-13 · code-reviewer#1 (TEAM-2)

### Границы ревью

- **Что проверялось.** Исправления итерации 3 по ревью #2 (Ma-2, Mi-5, Mi-6, N-1, N-2) и регрессия от них:
  подраздел «Итерация 3» карточки, запись `dev-log T-201 iteration 3`.
- **Файлы итерации:** `shared/agent/{content_hash,placeholders,parser}.go`, тесты
  `{content_hash,content_hash_internal,placeholders,parser}_test.go`, новый
  `testdata/blueprints/valid/domain-fair.md`.
- **Состояние ветки.** Коммита нет, HEAD `cd21a48`. `.golangci.yml` в итерации не менялся.
- **Метод.** Прогоны — на рабочей папке, только чтение. Зонды и мутанты — в копии `go.mod`, `go.sum`,
  `.golangci.yml`, `shared/agent`, `shared/clock` в scratch (`t201r3-copy`), без `-overlay`.
  - Копия до мутаций зелёная, контрольный мутант первым.
  - После каждого мутанта файл восстанавливался из эталона; в конце `content_hash.go` копии сверен `cmp` с рабочей
    папкой.
  - Копия удалена по сохранённому точному пути.

### Вердикт

**ПРИНЯТЬ** — Critical 0, Major 0, Minor 0, Nit 2.

Все замечания ревью #2 закрыты. Даты различаются и не зависят от записи в YAML и от пояса машины. Ключи map
однозначны, порядок детерминирован. Кириллица в плейсхолдерах находится. Числа нормализуются по одному правилу.
Закреплённый хэш не изменился. Отступление исполнителя (смещение сохраняется, в UTC не переводится) принимается,
обоснование в п. 2. Два Nit — пробелы в тестах для путей, которые из YAML недостижимы или сейчас верны; блокером
не являются.

### Прогоны

| Команда | Результат |
|---|---|
| `go build ./... && go vet ./...` | 0 |
| `go test -short -count=1 ./shared/agent/...` | `shared/agent` ok, `shared/agent/tools` ok |
| `golangci-lint run ./shared/agent/...` | `0 issues.` |

### 1. Статус замечаний ревью #2

| # | Статус | Проверка |
|---|---|---|
| Ma-2 | закрыто | `time.Time` → `time:<RFC3339Nano>` без кавычек (`content_hash.go:106-109`, `147-150`). Зонд: даты, наносекунда, `!!timestamp`, дата-ключ `{2001-01-01: a}` против строкового ключа `"time.Time:time:2001-01-01T00:00:00Z"` — разные хэши. Семь записей одной даты — один хэш (тест автора). Структура с неэкспортируемым полем ниже `any` → ошибка → `""` (`content_hash.go:162-166`). Мой мутант M18 (перевод в UTC) убит 2 тестами. Пустой хэш из парсера недостижим, см. п. 3 |
| Mi-5 | закрыто | Нестроковый ключ пишется без кавычек, с меткой типа (`content_hash.go:211-226`); вторичный порядок по значению (`239-245`). Зонд: `{1: a}`/`{"int:1": a}`, `{true: a}`/`{"bool:true": a}`, `{~: a}`/`{"null": a}`, `{1: a}`/`{1.0: a}` — разные хэши. `{~: a}`/`{null: a}` — один хэш (одно значение). Map `{1: a, "int:1": b, 1.0: c, .nan: d, .NaN: e, .NAN: f, 2001-01-01: g, "time.Time:time:2001-01-01T00:00:00Z": h, 0.0: i, -0.0: j}` на 300 разборах дала **1** хэш (в ревью #2 было 2 на 200). Пробел в тестах ключа `null` — N-3 |
| Mi-6 | закрыто | `\{\s*\p{L}[\p{L}\p{M}\p{N}_.\-]*\s*\}` (`placeholders.go:64`). Находятся `{игрок.имя}`, `{Ответ}`, `{имя_игрока}`, `{Ёж}`, `{й}` в обеих записях. Не находятся `{см. выше}`, `{ключ: значение}`, `{«имя»}`, `{имя—фамилия}`, JSON, Go-шаблоны, квантификаторы, `{player_hp}` (он у `UnknownPlaceholders`). Ложные срабатывания: `\text{Привет}`, `{日本}`, `{x²}` — тот же класс, что `\frac{A}{B}` в ревью #2; при `warning` в T-202 допустимо. `{ имя }` с NBSP не находится: `\s` в RE2 только ASCII (риск, не замечание) |
| N-1 | закрыто | `writeFloat` (`content_hash.go:123-139`): целое значение float в пределах `int64`/`uint64` пишется как целое, прочее — `'g'`. Преобразование `int64(f)` для целого `f` в диапазоне точное, поэтому разные float не склеиваются. Зонд: `9007199254740992` (int) = `9007199254740993.0` (float, округлён YAML до 2^53) — одно значение; `9007199254740993` (int) ≠ `9007199254740993.0`. `TestContentHashEncodingIsPinned` зелёный, константа та же `sha256:811cfa8e…1371`, что после итерации 2. Отношение к C-02 — п. 2 |
| N-2 | закрыто | `fenceMarker` (`parser.go:255-267`). Зонд: `` ```json``` пример `` вне блока — текст секции, `## tick` разбирается. Внутри ` ``` `-блока строка `` ```json``` x `` блок не закрывает. `` ``` ` `` не открывает блок. Забор из четырёх обратных кавычек остаётся забором. `` ~~~ `x` `` открывает `~~~`-блок (по CommonMark верно) |

### 2. Решения, вынесенные на ревью

**Смещение времени не переводится в UTC — принять.**
- **Контроль подмены T-26.** В `threat-model.md:171` контроль — `content_hash` в `agent.blueprint_reloaded`.
  Ему опасен пропуск (правка есть, хэш тот же), а не лишнее «изменилось». Замена `03:00+03:00` на `00:00Z` меняет
  текст, который уйдёт в JSON события (`time.Time.MarshalJSON` сохраняет смещение), и `reflect.DeepEqual`. Форма с
  UTC скрыла бы такую правку, форма автора её видит. Это безопаснее.
- **Детерминизм от пояса машины.** `yaml.v3` зовёт `time.Parse` без `.UTC()`. Location бывает `Local`, `FixedZone` или
  `UTC` в зависимости от пояса машины, но `Format` пишет только числовое смещение.
  - Зонд: 11 значений в поясах `UTC`, `Europe/Moscow`, `America/New_York`, `Asia/Kolkata`, `Europe/London` и в поясе
    машины (подмена `time.Local`, `time/tzdata`). Среди значений — летнее время Москвы 2001 (`+04:00` → `Local`,
    `+03:00` → `FixedZone`), `+00:00`, переходы DST, `1850-…+02:30`. Хэши во всех поясах одинаковые.
  - `+00:00` в любом поясе пишется как `Z`.
- **Остаток.** RFC 3339 не пишет секунды смещения. Значение в историческом поясе со смещением `+02:30:17` (LMT),
  собранное в Go вручную, может совпасть с другим моментом в `+02:30`. Из YAML это недостижимо: `time.Parse`
  ставит `Local` только при равенстве смещений до секунды, а в YAML смещение — часы и минуты. Замечанием не считаю.

**N-1 и C-02 (граница |x| ≤ 2^53−1 для State, T-448) — противоречия нет.**
- Каноническая форма — вход SHA-256, а не проводной формат. Числа из неё никто не читает, и граница C-02 к ней не
  применяется.
- Правило «по значению» совпадает с направлением `CanonicalJSON`/`StateHash` (`1` = `1.0`).
- За 2^53 хэш различает больше, чем JSON-декодер во `float64` (int `2^53+1` ≠ float `2^53`). Для контроля это
  безопасная сторона: лишнее «изменилось», не пропуск.
- Число за границей в `ops[].value` отвергнет State при применении. Раньше его может ловить валидатор T-202
  (бэклог п. 2).

### 3. Пустой `content_hash` у вызывающих

- `ContentHash` вызывается только в `parser.go:130`. `""` возможен лишь для значения, собранного в Go вручную.
- Зонд по 20 видам YAML-значений: `!!set`, `!!omap`, локальный тег `!foo`, `!!binary`, `0x10`, `0o17`, `+1`, `±.inf`,
  `uint64`, `2^64`, merge `<<`, `!!str`/`!!float`/`!!int`, дата-ключ, список дат, границы 2^53. Все дают
  `sha256:…`: `yaml.v3` в `any` не кладёт других структур, кроме `time.Time`.
- `agent.blueprint_reloaded.content_hash` — `minLength: 1` и `required`, пустое значение не пройдёт схему.
- `agent.spawned.content_hash` необязателен: при `omitempty` пустой хэш молча пропадёт из события. Это решение
  потребителя I2 (бэклог п. 1), не дефект T-201.

### 4. Мутанты ревьюера

| # | Мутант | Результат |
|---|---|---|
| M0 | контрольный: `"sha256:"` → `"sha1:"` (`content_hash.go:58`) | убит (5 тестов) |
| M18 | `t.Format` → `t.UTC().Format` (`content_hash.go:149`) | убит `TestCanonicalFormOfTimes`, `TestContentHashOfOneDateInOtherNotations` |
| M16 | ключ `nil` пишется как `strconv.Quote("null")` — совпадает со строковым ключом `"null"` (`content_hash.go:214`) | **выжил** → N-3 |
| M9 | поле структуры ниже `any` получает `untyped=false` (`content_hash.go:179`) | **выжил** → N-4 |
| M10 | значение map получает `untyped=false` (`content_hash.go:201`) | **выжил** → N-4 |
| M17 | `k.Type() == string` → `k.Kind() == reflect.String` (`content_hash.go:218`) | выжил; для YAML эквивалентен (ключи — только `string`), замечанием не считаю |

### Замечания

| # | Серьёзность | Файл:строка | Суть | Как исправить |
|---|---|---|---|---|
| N-3 | Nit | `shared/agent/content_hash_test.go:349-373` (код `content_hash.go:214` верен) | Нет теста, что ключ `~` отличается от строкового ключа `"null"`. Мутант M16 возвращает для ключа Mi-5 и выживает: `{~: a, "null": f}` в тесте детерминизма после мутанта остаётся детерминированным (равные ключи упорядочены по значению), а `map[any]any{nil: 1}` в `TestContentHashOfEdgeValues` проверяется только на форму. | В `TestContentHashOfMixedMapKeys` добавить пару `{~: a}` ≠ `{"null": a}`. |
| N-4 | Nit | `shared/agent/content_hash_internal_test.go:173-184` (код `content_hash.go:179`, `201` верен) | Флаг `untyped` у поля структуры и у значения типизированной map ниже `any` тестом не закреплён (мутанты M9, M10 выжили). Все случаи теста проходят через интерфейс, который сам ставит флаг. Из YAML такие формы недостижимы, но комментарий `ContentHash` обещает `""` для любой структуры со скрытыми полями ниже `any`. | Добавить случаи `OpTemplate{Value: struct{ Inner hidden }{}}` и `OpTemplate{Value: map[string]hidden{"k": {}}}`. |

### Открытые вопросы

Нет.

### Предложения в бэклог

1. **I2 (реестр блупринтов, `agent.spawned`).** Пустой `ContentHash` считать ошибкой, а не пропускать поле. Либо
   `ParseBytes` возвращает внутреннюю ошибку при `""`, если `ContentHash` станет вызываться не только парсером.
2. **T-202 (валидатор).** Предупреждать о числах за |x| ≤ 2^53−1 и о `time.Time` в `ops[].value` блупринта. State
   (C-02 v1.6, T-448) отвергнет такое значение только при применении, а в JSON дата уедет строкой.
3. Прежние пункты ревью #1/#2 (гонка `tools/registry.go`, ключи `lodDistribution`, документы, серьёзность правила 13)
   остаются в силе.
