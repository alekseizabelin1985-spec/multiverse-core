# T-203: A3 · Пять блупринтов MVP-1 и схемы `schemas/agent/`

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-2 |
| Подволна | C (`../tasks.md` §6.1) |
| Размер | M |
| Исполнитель | developer#1 |
| Статус | done |
| Ветка | `task/T-203-mvp1-blueprints` от `epic/EPIC-003-swarm-llm-laws` (база `7a8c100`), папка `.worktrees/T-203` |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-13 / 2026-09-13 18:55 (разработка); итерация 2 — завершена 2026-09-13 19:30; принята 2026-09-13 |

## Описание и DoD
Главный источник — раздел T-203 в `../tasks.md` (индекс эпика, ревизия 4 от 2026-09-13), общий DoD — там же, §1. Здесь не дублируется, чтобы не завести второй источник истины. Кратко:
- Зависит от: T-201, T-202, T-214 (схемы в дереве), **T-439**.
- Главное из ревизии 4: стартует после T-439; в фазе блупринта только `{model, temperature, max_tokens, thinking, schema_ref}`.

## Выполнение (developer)
developer#1, 2026-09-13. Подробно — запись `<!-- dev-log T-203 -->` в `../dev-log.md`. Коммита нет (запрет в промпте), новые файлы не в индексе.

**Что сделано.**
- `blueprints/` — пять блупринтов MVP-1 по КД §13.3 и скелетам `api-contracts.md` §3.3:
  - `global-dark-forest-world.md` — `global-gm`, таймер `idle: 60m`, `llm.tick` (`tick-global.json`, 512), `budget.background_calls_per_hour_world: 4`, `background_events` из скелета, `invariants` — `inv-01…inv-10` с `check`, равным id (как в `laws/dark-forest-world.v1.yaml` и `mechanics.InvariantIDs()`), секции `## system`, `## tick`;
  - `domain-dark-forest.md` — `version: "1.1"`, `intervals{idle: 30m, active: 60s}`, `phase1: rules`, `llm.tick` (`tick-region.json`, 512), `npc_table` (волк, только респаун), `respawn_ttl: 24h`, `encounter{detect_on: tick, perception: all, chance: "0.25", child_blueprint: encounter-wolf}` (`chance` — `encounter_chance` фикстуры региона), `background_events` из скелета, секции `## description` (текст фикстуры региона), `## canon` (стая и законы law-1, law-2), `## system`, `## tick`;
  - `encounter-wolf.md` — `scope_binding{type: "solo|group", pattern: "*"}`, `llm{phase1: rules, fallback: rules}`, `round{timeout: 60s, idle_after_missed: 2}`, `rules_ref`, `absolute_limits_ref` (правило 10 требует его у роли `encounter`);
  - `player-gm.md` и `group-narrator.md` — `llm.phase2{model: Qwen3.8-27B-UD-Q3_K_XL, temperature: 0.7, max_tokens: 160, thinking: false, schema_ref: schemas/agent/narrative.json}`, `retries: 2`, `fallback: template`, `allowed_event_types: [narrative.output]`, `owned_entity_types: []`, `absolute_limits_ref`; у группы `max_tokens` — своё число со своим комментарием; `## phase2` просит «1–2 предложения, до ~140 символов» ровно один раз.
  - Во всех фазах с моделью — модель E, `temperature: 0.7`, `thinking: false` и комментарий-указатель `# model: per ops/metrics/baseline.md §5 (OQ-A-18; базовая E, запасные по decision_order ADR-005 доп. 3: Qwen3.6-35B-A3B, C, A)`; у `max_tokens` нарраторов — указатель на `baseline.md` §5, КД §13.4.1, ADR-029. Полей провайдера и `top_p/top_k/min_p/presence_penalty` нет.
- `schemas/agent/` (JSON Schema 2020-12, у каждой `$schema` и `$id`):
  - `narrative.json` — форма КД §13.4: `text` 1…185, `mentions` ≤ 4 строк `^e[1-9][0-9]?$`, `background_refs` ≤ 2 строк `^b[1-9][0-9]?$`, `tone` enum, `additionalProperties: false`, `$comment` `narrative cap: config=E; c=2.5; c_min=2.0; overhead=66; ops/metrics/baseline.md §5, swarm-llm-laws.md §13.4.1, ADR-029`;
  - `tick-global.json` — `events` ≤ 2, `type` ∈ `world.weather_changed|world.time_advanced|world.event_occurred`, `summary` 1…300, `ops[]{path ∈ weather|time_of_day|day, value}`;
  - `tick-region.json` — `events` ≤ 3, `type` ∈ `region.event_occurred|npc.moved|npc.spawned`, `summary` 1…300, `entity?{id}`, `affects?[{id}]`, `ops[]{path, value}`; каждый id — `maxLength: 128`, в `description` — предел общий с `llm.output.rejected.ref` (C-07 v1.5);
  - `breach.json` — заглушка `{"$comment": "reserved E-B"}`.
- `testdata/fixtures/events/llm.output.v1.{valid,invalid}.json` — `response_raw` по `narrative.json` (`{"text":"Альфа-волк отступает в подлесок, зализывая рану.","mentions":["e1","e2"],"background_refs":[]}`, ярлыков в `text` нет), `params.max_tokens: 160`, `response_len: 143` (байты UTF-8 `response_raw`), `response_hash` — настоящий sha256 этого ответа. Остальные поля не менялись; `invalid` остаётся невалидной по `validation_status`.
- `blueprints/blueprints_test.go` (`package blueprints_test`) — тест данных задачи, 13 тестов (таблица DoD ниже).

**Решения по ходу.**
1. **Место теста — `blueprints/blueprints_test.go`.** Тесту нужны `shared/contracts` (типы, владение), `internal/mechanics` (ключи инвариантов), `internal/laws` (разбор законов) и `jsonschema/v6`. Из `shared/agent` импорт `internal/*` запрещён depguard и в тестах; `internal/swarm` ещё нет (T-222); `internal/laws` не видит `mechanics`. Каталог `blueprints/**` принадлежит EPIC-003 (`ownership.md` §1), правил depguard для него нет, парсер и `EnvFromProject` читают в нём только `.md/.yaml/.yml`. Образец — `test/fixtures` (Go-тест, который стережёт данные). Альтернатива — `test/blueprints/`; выбор за ревью.
2. **Окружение валидатора собрано как у вызывающего:** `agent.EnvFromProject("..", типы contracts.All(), вид из contracts.OwnershipRules, mechanics.InvariantIDs(), models)`. Модели — `{E}` в основном тесте (правило 7а живое: контрольный мутант M1 убит им) и `nil` в офлайн-тесте (ровно одна `info` на поле `llm` у блупринтов с моделью).
3. **`config/absolute-limits.yaml` в дереве нет** (файл T-216, статус `todo`). Правило 10 даёт `error` у `encounter-wolf`, `player-gm`, `group-narrator`. Тест допускает только эту находку и только пока файла нет (`pendingFiles`, `t.Logf` с номером задачи); остальные находки любой серьёзности — красные. Появится файл — исключение перестанет действовать само. Вопрос — в отчёте оркестратору.
4. **`invariants` глобального блупринта — `inv-01…inv-10`, без `inv-11`.** Окружение валидатора берёт ключи из `mechanics.InvariantIDs()` (КД §13.2), `laws_version_current` там нет — `inv-11` исполняет страж. Сверку «id/check как в файле законов» (КД §13.3) валидатор T-202 не делает, её делает тест.
5. **`enum` тика — полный набор уровня (TL2-7).** Формулировка «валидатор проверяет `allowed ⊆ enum`» к региону буквально не подходит: `encounter.started` есть в `allowed_event_types`, но его публикует правило обнаружения, а не модель. Тест проверяет, что `enum` равен белому списку роли без `entity.*.proposed` и `encounter.started`, и что `enum ⊆ allowed_event_types` блупринта и реестра: сужение в рантайме ничего не выбрасывает.
6. **`allowed_event_types` — как в скелетах §3.3:** без `entity.update.proposed` у global/domain и без `dice.rolled` у встречи. Валидатор сверяет владение по белому списку роли, а не блупринта; как их трактует `Emitter` (T-223) — риск в отчёте.
7. **`max_tokens` тиков — 512 из скелетов.** Потолок тиков в T-439 не считался (бэклог ниже).
8. **`response_len` — байты UTF-8** (`len()` в Go). Единица в C-07 не названа; прежнее 214 не совпадало ни с байтами (156), ни с символами (116) старого ответа.
9. **Описания в JSON-схемах — по-английски**, как в `schemas/events` (код и комментарии — на английском); смысл строки DoD T-459 передан по-английски.

**Изменённые файлы.**
- Созданы: `blueprints/{global-dark-forest-world,domain-dark-forest,encounter-wolf,player-gm,group-narrator}.md`, `blueprints/blueprints_test.go`, `schemas/agent/{narrative,tick-global,tick-region,breach}.json`.
- Изменены: `testdata/fixtures/events/llm.output.v1.valid.json`, `testdata/fixtures/events/llm.output.v1.invalid.json`, эта карточка, `../dev-log.md`.
- Не менялись: `shared/agent/**`, `../tasks.md`, КД (строка 521 цела), `contracts.md`, `.gitleaksignore`. Старый `shared/agent/examples/domain-dark-forest.md` уже в `services/_archive` (T-201), `git mv` не понадобился.

**Тесты и проверки.**
- `go build ./... && go vet ./...` — зелёные.
- `go test -short -count=1 ./...` — все пакеты ok.
- `golangci-lint run ./...` — 0 issues.
- `go run ./cmd/mvctl contracts check` — 65 типов, 8 топиков, 58 схем; `go test ./shared/contracts/... ./test/fixtures/...` — ok (фикстуры `llm.output` проходят схему C-07).
- `make test` (GNU make из WinGet) — exit 0, порог покрытия `internal/*` зелёный.
- `gitleaks dir` по `blueprints`, `schemas/agent` и двум фикстурам — no leaks found.
- Мутанты — копия дерева (`go.mod`, `go.sum`, `blueprints`, `schemas`, `shared`, `internal`, `laws`, `rules`, `testdata`, `config`) в scratch `t203-mutants`, без `-overlay`; копия до мутаций и после возврата — зелёная. 8 из 8 убиты:
  - M1 (контрольный, первым): модель `player-gm` → `qwen3:30b-a3b` — `TestBlueprintsValidate/player-gm` (7а), `TestPhasesAreConfigurationE/player-gm`;
  - M2 (мутант DoD): `text.maxLength: 210` — `TestNarrativeCap/invariants/{player-gm,group-narrator}` (66 + 105 = 171 > 160), `TestNarrativeCap/row_E`, `TestNarrativeSchemaInstances/text_over_maxLength`;
  - M3: `maxLength` id в `tick-region.json` 128 → 129 — `TestTickRegionSchemaInstances/entity_id_of_129`;
  - M4: `до ~150 символов` у `player-gm` — `TestNarrativeCap/invariants/player-gm` (150 > 148), `row_E`;
  - M5: из `invariants` глобального блупринта убран `inv-10` (валидатор этого не видит) — `TestGlobalInvariantsFollowTheLaws`;
  - M6: `npc.moved` в `enum` `tick-global.json` — `TestTickEnumsFollowTheWhiteLists/global-dark-forest-world`, `TestTickGlobalSchemaInstances/type_of_another_level`;
  - M7: `temperature: 0.8` в тике региона — `TestPhasesAreConfigurationE/domain-dark-forest`;
  - M8: `response_len` фикстуры 143 → 156 — `TestLLMOutputFixturesAnswerByNarrativeSchema/valid`.

  Копия и скрипт удалены по точным путям.

**DoD (специфика).**

| Пункт | Статус | Тест / проверка |
|---|---|---|
| `agent.Validate` на пяти файлах — 0 `error` | да, кроме `absolute_limits_ref` у трёх файлов до T-216 (решение 3) | `TestBlueprintsValidate` (0 находок любой серьёзности, модели `{E}`), `TestBlueprintsValidateOffline`, `TestDirectoryHoldsTheBlueprintsOfMVP1` |
| Модель E, `thinking: false`, поля провайдера нет | да | `TestPhasesAreConfigurationE` (ключи фазы, модель, `thinking`, указатель на `baseline.md`), `TestSamplingAndProviderAreNotBlueprintFields/provider` |
| Схемы компилируются `jsonschema/v6` (2020-12); `enum` тика — полный набор MVP-1 | да | `TestAgentSchemasCompile`, `TestTickEnumsFollowTheWhiteLists` |
| `domain-dark-forest.md` заменяет пример `shared/agent/examples` | да | `TestDirectoryHoldsTheBlueprintsOfMVP1` (примера в пакете нет) |
| Плейсхолдеры — только из словаря | да | `TestBlueprintsValidate` (правило 13: ни `error`, ни `warning`) |
| Фаза — `{model, temperature, max_tokens, thinking, schema_ref}`, `temperature: 0.7`, разбор с `KnownFields(true)` | да | `TestPhasesAreConfigurationE`, `TestSamplingAndProviderAreNotBlueprintFields` (`top_p`, `top_k`, `min_p`, `presence_penalty` — ошибка разбора) |
| Потолок E: 160/160, `maxLength` 185, `maxItems` 4/2, `pattern` ярлыков, `$comment`, L = 140, три инварианта | да | `TestNarrativeCap` (строка E, `overhead` = 30 + 6 × Σ`maxItems`, инварианты по c и c_min из `$comment`), `TestNarrativeSchemaInstances` |
| `temperature: 0.7` во всех фазах с LLM | да | `TestPhasesAreConfigurationE` |
| Фикстуры `llm.output.v1.*` по `narrative.json`, `max_tokens: 160`, `response_len` | да | `TestLLMOutputFixturesAnswerByNarrativeSchema`; `go test ./shared/contracts/... ./test/fixtures/...`, `mvctl contracts check` |
| `maxLength: 128` у id схем тика; id из 129 символов невалиден | да по схеме; ответ парсера `schema_invalid` — T-209 | `TestTickRegionSchemaInstances` |

**Предложения в бэклог.**
- Потолок `max_tokens` тиков (512) и `maxItems` у `ops[]` не рассчитаны: три события региона по 300 символов при c_min = 2,0 — около 450 токенов одного текста. Расчёт по образцу КД §13.4.1 — architect#2 (T-260).
- Тест согласованности `npc_table[].stats_ref` ↔ `rules/dark-forest.yaml` и фикстуры (api-contracts §3, I1b) удобно положить в `blueprints_test.go`.
- Фикстура региона пишет `blueprint_ref: "domain-dark-forest@1.0"`, блупринт по КД — `version: "1.1"` (фикстура — EPIC-001, уведомить tech-lead#1).
- `build/Dockerfile` при копировании `blueprints/` (T-447) возьмёт и `blueprints_test.go`: исключить `**/*_test.go` в `.dockerignore` или копировать только `*.md`.

## Итерация 2 (developer)
developer#1, 2026-09-13, по ревью #1 code-reviewer#3 (`../review.md`, «T-203 · ревью #1») и решениям оркестратора 1–7. Коммита нет, новые файлы не в индексе. Запись в `../dev-log.md` — `<!-- dev-log T-203 iteration 2 -->`.

**Ответы на замечания.**

| # | Серьёзность | Ответ | Что сделано |
|---|---|---|---|
| Ma-1 | Major | исправлено, вариант (а) (решение оркестратора 1) | `allowed_event_types` дополнены: `encounter-wolf` — `+ dice.rolled, entity.update.proposed`; `domain-dark-forest` — `+ entity.create.proposed, entity.update.proposed`; `global-dark-forest-world` — `+ entity.update.proposed`. Над каждым списком — комментарий: все публикации роли идут через `Emitter` и сверяются с этим списком (КД §5.4). Новый тест `TestBlueprintsListWhatTheirRolesPublish` (см. ниже). Решение 6 итерации 1 («как в скелетах §3.3») этим снято |
| Mi-1 | Minor | не менялось (решение оркестратора 2) | Проверка `enum ⊆ allowed_event_types` и «`enum` = белый список роли без `entity.*.proposed` и `encounter.started`» остаётся в `TestTickEnumsFollowTheWhiteLists`, после Ma-1 зелёная без правок. Новая формулировка TL2-7 (КД §13.4/§20) и нужна ли эта проверка в валидаторе (поле `ValidationEnv`, T-222/T-204) — за architect#2 |
| Mi-2 | Minor | не менялось (решение оркестратора 3) | Плейсхолдеры `{state}`, `{events}`, `{canon}`, `{absence}`, `{region.description}` в `## phase2`/`## tick` и секции сборщика КД §11.4 (`<canon>`, `<state>`, `<events>`, `<absence>`) могут дублировать данные в промпте; `{player.name}`, `{region.*}`, `{canon}` в `## system` делают кэшируемую часть зависимой от игрока и региона. Вопрос к architect#2 до T-218. Если данные уйдут в секции сборщика — правка текста секций данными; тест T-203 её переживёт (L и фраза остаются) |
| Mi-3 | Minor | не менялось (решение оркестратора 4), риск записан ниже | `max_tokens: 512` тиков и `maxItems` у `affects[]`/`ops[]` — T-260 и architect#2 |
| N-1 | Nit | исправлено в пределах теста | В комментарии к `pendingFiles` сказано, что задача с последним ожидаемым файлом удаляет карту вместе с `pendingReference` (T-216). Строку DoD T-216 в `../tasks.md` исполнитель не правит — предложение tech-lead#2 (ниже) |
| N-2 | Nit | исправлено | В `TestTickGlobalSchemaInstances` и `TestTickRegionSchemaInstances` — случаи «лишнее поле события», «лишнее поле `ops[]`», «лишнее поле ответа»; в региональной таблице ещё «`affects[]` с `type`». Мутант M5 теперь убит |
| N-3 | Nit | исправлено | `TestSamplingAndProviderAreNotBlueprintFields` проверяет не любую `*agent.ParseError`, а ровно `Field` = путь ключа (`llm.phase2.top_p` … `provider`) и `Reason` = `unknown field`. Подтесты названы путями полей. Зонд Z2 теперь краснеет |

**Сверка добавленных типов (Ma-1).**

| Блупринт | Тип | Источник в дизайне | Белый список `levels.go` | Владение `contracts.OwnershipRules` |
|---|---|---|---|---|
| `encounter-wolf` | `dice.rolled` | КД §15.1 (`GE dice.rolled ×4`), C-05 п. 1 и «Почему так» п. 5 (решения обмена до пакета) | `encounter`: есть | — |
| `encounter-wolf` | `entity.update.proposed` | КД §15.1 (`SE entity.update.proposed {atomic, cause: combat}`), C-05 п. 1–2 (пакет обмена, след в сущности встречи) | `encounter`: есть | `task`: `player`, `npc`, `encounter` = `owned_entity_types` |
| `domain-dark-forest` | `entity.create.proposed` | ADR-028 п. 1 (подъём агента встречи, затем `entity.create.proposed`), C-05 п. 4 (`encounter.started` после `entity.created`) | `region-gm`: есть | `domain`: `region`, `npc`, `encounter` = `owned_entity_types` |
| `domain-dark-forest` | `entity.update.proposed` | КД §15.2 (`SE entity.update.proposed {npc.position, cause: tick}`) | `region-gm`: есть | то же |
| `global-dark-forest-world` | `entity.update.proposed` | `api-contracts.md` §2.4 (`global` → … `+ entity.update.proposed(world)`) | `global-gm`: есть | `global`: `world` = `owned_entity_types` |

Правило 8 валидатора зелёное: все типы есть в реестре и в белом списке роли. `entity.create.proposed` у встречи не добавлен: ни КД, ни `api-contracts.md` §2.4, ни `levels.go` его встрече не дают. `enum` схем тика не менялся: `entity.*.proposed` и `encounter.started` модель тика не предлагает.

**Новый тест `TestBlueprintsListWhatTheirRolesPublish`.** Статическая таблица `publishedByDesign` (`blueprints_test.go:509`) выписана из документов, а не из `levels.go`, у каждой строки — ссылка на КД §15.1/§15.2, §13.4, ADR-028, C-05, `api-contracts.md` §2.4. Для каждого из пяти блупринтов тест проверяет:
- таблица покрывает ровно пять блупринтов MVP-1;
- каждый тип таблицы есть в белом списке роли — значит, таблица не разошлась с `levels.go`;
- каждый тип таблицы есть в `allowed_event_types` блупринта — это ловит повторение Ma-1;
- непустые `owned_entity_types` требуют в `allowed_event_types` хотя бы одного `entity.*.proposed`. Это ловит внутреннее противоречие файла, о котором сказано в ревью.

Всего 14 тестов в `blueprints_test.go`.

**Изменённые файлы итерации 2.**
- `blueprints/encounter-wolf.md`, `blueprints/domain-dark-forest.md`, `blueprints/global-dark-forest-world.md` — `allowed_event_types` и комментарий над ним.
- `blueprints/blueprints_test.go` — `publishedByDesign`, `TestBlueprintsListWhatTheirRolesPublish`, случаи N-2, проверка N-3, комментарий N-1, импорт `maps`.
- Эта карточка, `../dev-log.md`.
- Не менялись: `schemas/agent/**`, фикстуры `llm.output`, `player-gm.md`, `group-narrator.md`, `shared/agent/**`, КД, `contracts.md`, `../tasks.md`, `.gitleaksignore`.

**Прогоны.**
- `go build ./... && go vet ./...` — зелёные.
- `go test -short -count=1 ./...` — exit 0, все пакеты `ok`.
- `golangci-lint run ./...` — 0 issues.
- `go run ./cmd/mvctl contracts check` — 65 типов, 8 топиков, 58 файлов схем.
- `make test` (GNU make из WinGet) — exit 0: race detector отключён локально (нет cgo), порог покрытия `internal/mechanics` 94,9 %, `internal/llm` 99,7 %.
- `go test -count=1 -v ./blueprints/` — 14 PASS; три `t.Logf` об `absolute_limits_ref` (T-216), других находок нет.
- `gitleaks dir blueprints` — no leaks found.

**Мутанты.** Копия дерева (`go.mod`, `go.sum`, `blueprints`, `schemas`, `shared`, `internal`, `laws`, `rules`, `testdata`, `config`) в scratch `t203i2-mutants`, без `-overlay`. Скрипт проверял, что якорь замены встречается ровно один раз, и после каждого мутанта возвращал исходные байты. Копия зелёная до мутаций и после возврата. Убиты 8 из 8:

| # | Мутация | Итог |
|---|---|---|
| M0 (контрольный, первым) | `player-gm` `max_tokens: 160 → 150` | убит: `TestNarrativeCap/{row_E, invariants/player-gm}`, `TestLLMOutputFixturesAnswerByNarrativeSchema/{valid,invalid}` |
| M5 (из ревью) | `tick-global.json` — снят `additionalProperties: false` у элемента `events[]` | убит: `TestTickGlobalSchemaInstances/extra_field_of_an_event` |
| M5r | то же в `tick-region.json` | убит: `TestTickRegionSchemaInstances/extra_field_of_an_event` |
| Z2 (зонд из ревью) | вставка ключей фазы в тесте с отступом 3 вместо 4 (ошибка синтаксиса YAML) | убит: `TestSamplingAndProviderAreNotBlueprintFields/llm.phase2.{top_p,top_k,min_p,presence_penalty}` |
| M9 (решение оркестратора) | `encounter-wolf` без `dice.rolled` | убит: `TestBlueprintsListWhatTheirRolesPublish/encounter-wolf` |
| M10 | `domain-dark-forest` без `entity.create.proposed` | убит: `TestBlueprintsListWhatTheirRolesPublish/domain-dark-forest` |
| M11 | `global-dark-forest-world` без `entity.update.proposed` | убит: `TestBlueprintsListWhatTheirRolesPublish/global-dark-forest-world` |
| M12 | `encounter-wolf` без `entity.update.proposed` | убит: `TestBlueprintsListWhatTheirRolesPublish/encounter-wolf` (оба условия: таблица и `owned_entity_types` без предложений) |

M9–M12 валидатор T-202 не видит: в итерации 1 такие файлы были зелёными. Копия и скрипт удалены по точным путям.

**Риски и допущения итерации 2.**
- *Mi-3, риск.* `max_tokens: 512` у тиков не рассчитан, а у `affects[]`/`ops[]` нет `maxItems`. Грамматика `json_schema` не останавливает массив, поэтому упор в 512 даёт обрез, затем `schema_invalid`, повтор, откат на правила и расход бюджета фона (4 вызова в час на мир). На E 512 токенов — около 12,5 с генерации при `MV_LLM_SLOTS=1`. Если уступка планировщика не прерывает идущую генерацию, нарратив игрока, пришедший во время тика, ждёт её конца (NFR-002). Расчёт — T-260 и architect#2.
- *Комментарий валидатора устарел.* Комментарий к правилу 8 в `shared/agent/validator.go` (`checkWhiteLists`) говорит, что встреча меняет сущности через механику и сама `entity.update.proposed` не перечисляет. После Ma-1 (а) блупринт встречи его перечисляет. Код и поведение правила 8 верны, расходится только текст. `shared/agent/**` — не файлы этой задачи (T-202), поэтому пункт ушёл в бэклог.
- *Отступление от скелетов.* `allowed_event_types` трёх блупринтов теперь шире скелетов `api-contracts.md` §3.3, на которые ссылается КД §13.3 («без изменений»). КД и `api-contracts.md` не менялись: это решает architect#2.
- *Таблица теста — ручная копия документов.* Если КД назначит роли новую публикацию, таблицу нужно дополнить руками. Тест проверяет, что таблица не шире белого списка роли, но не проверяет, что она полна относительно КД.

**Открытые вопросы (architect#2 через оркестратора).**
1. Ma-1 (а): отразить в КД §13.3 и `api-contracts.md` §3.3, что `allowed_event_types` включает предложения сущностей роли (`entity.*.proposed`) и `dice.rolled` встречи. Заодно — DoD T-223: `Emitter` сверяет тип с `allowed_event_types` блупринта.
2. Mi-1: формулировка TL2-7 и проверка `enum` против блупринта в валидаторе.
3. Mi-2: данные в `## phase2`/`## tick` плейсхолдерами или секциями сборщика; плейсхолдеры игрока и региона в кэшируемом `## system`.
4. Mi-3: потолок `max_tokens` тиков и `maxItems` (T-260).

**Предложения в бэклог (итерация 2).**
- tech-lead#2, DoD T-216: «тест T-203 проходит без исключения; `pendingFiles` пуст или удалён вместе с `pendingReference`» (N-1).
- T-222 (владелец `shared/agent`): поправить комментарий правила 8 в `validator.go` про встречу и `entity.update.proposed`. После T-222 перевести исключение `pendingReference` на код находки (`Issue.Code`), если T-216 к тому времени не придёт.
- Пункты итерации 1 остаются в силе: `.dockerignore`/T-447, `stats_ref` против `rules/dark-forest.yaml`, `blueprint_ref` фикстуры региона `@1.0 → 1.1`.

## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Critical / Major / Minor / Nit | Запись |
|---|---|---|---|---|---|
| 1 | 2026-09-13 | code-reviewer#3 (TEAM-2) | вернуть | 0 / 1 / 3 / 3 | `../review.md`, «T-203 · ревью #1»: Ma-1 — `allowed_event_types` global/domain/encounter без `entity.*.proposed` и `dice.rolled` (правка данных или решение architect#2 по `Emitter` T-223); место теста `blueprints/` подтверждено; исключение правила 10 узкое и снимается само |
| 2 | 2026-09-13 | code-reviewer#3 (TEAM-2) | принять | 0 / 0 / 1 / 1 | `../review.md`, «T-203 · ревью #2»: Ma-1 закрыт (5 добавленных типов сверены с КД §15.1/§15.2, ADR-028 п. 1, C-05, `api-contracts.md` §2.4, `levels.go`, `OwnershipRules`; лишних прав нет); N-1…N-3 закрыты, M5/M5r/Z2 краснеют; Mi-4 — `owned_entity_types` не стережётся тестом (R2a–R2d выжили), N-4 — сверка таблицы односторонняя (R4 выжил), исправление проверено зондом P1; Mi-1…Mi-3 — у architect#2 |

## Приёмка (tech-lead)
tech-lead#2 (TEAM-2), 2026-09-13, модель Opus. **Решение: принята.**

**Итог.** Пять блупринтов MVP-1, схемы `schemas/agent/` и фикстуры `llm.output.v1.*` соответствуют DoD и КД §13.3–§13.4.1; тест `blueprints/blueprints_test.go` при приёмке стал сверять типы публикаций и владение равенством.

Основание — ревью #2 code-reviewer#3: «принять», Critical 0, Major 0, Minor 1, Nit 1 (`../review.md`, «T-203 · ревью #2»). Итераций ревью — 2. Mi-4 и N-4 закрыты при приёмке правкой теста по зонду P1 ревьюера, повторного ревью нет. Сверка — по рабочему дереву `.worktrees/T-203` (база `7a8c100`, коммитов нет): `git diff 7a8c100` и неотслеживаемые `blueprints/`, `schemas/agent/`. `dev-log.md` заполнен (записи `<!-- dev-log T-203 -->` и `<!-- dev-log T-203 iteration 2 -->`).

**DoD** (раздел T-203 в `../tasks.md`; пункты отмечены `[x]`).

| Пункт | Чем подтверждён |
|---|---|
| `agent.Validate` — 0 `error` | `TestBlueprintsValidate` (модели `{E}`, ни одной находки) и `…Offline`. Исключение одно — `error` правила 10 `absolute_limits_ref` у `encounter-wolf`, `player-gm`, `group-narrator`, пока нет `config/absolute-limits.yaml` (T-216). Строка DoD T-216 снимает исключение |
| Модель E, `thinking: false`, без полей провайдера и семплинга | `TestPhasesAreConfigurationE`, `TestSamplingAndProviderAreNotBlueprintFields` (путь поля и `unknown field`) |
| Схемы 2020-12; `enum` тика — полный набор уровня | `TestAgentSchemasCompile`, `TestTickEnumsFollowTheWhiteLists`, таблицы экземпляров схем |
| Потолок E 160/160/185/L 140/4/2, три инварианта, `$comment` | `TestNarrativeCap`, `TestNarrativeSchemaInstances` |
| Фикстуры `llm.output.v1.*` | `TestLLMOutputFixturesAnswerByNarrativeSchema`, `go test ./test/fixtures/...`, `mvctl contracts check` |
| `maxLength: 128` у id тика | `TestTickRegionSchemaInstances`; ответ парсера `schema_invalid` — T-209 |
| Общий DoD: тесты, линтер, dev-log, критерии истории | прогоны ниже; `dev-log.md` |

**Закрыто при приёмке.**

| # | Что сделано | Где |
|---|---|---|
| N-4 | Три набора — `allowed_event_types` блупринта, строка `publishedByDesign`, белый список роли `agent.AllowedEventTypes` — сравниваются равенством отсортированных списков, а не вложением. Намеренное сужение в будущем блупринте записывается в таблицу явно (комментарий теста) | `blueprints/blueprints_test.go`, `TestBlueprintsListWhatTheirRolesPublish` |
| Mi-4 | Если в строке `publishedByDesign` есть `entity.*.proposed`, `owned_entity_types` блупринта равен `ownedFromContracts(level)` без повторов (`contracts.OwnershipRules`: `task` → `encounter, npc, player`, `domain` → `encounter, npc, region`, `global` → `world`); пустая строка владения у предлагающей роли — `Fatalf`. Без предложений `owned_entity_types` должен быть пуст | там же |

**Мутанты** (копия дерева в scratch `t203acc-mut`: `go.mod`, `go.sum`, `blueprints`, `schemas`, `shared`, `internal`, `laws`, `rules`, `testdata`, `config`; без `-overlay`; скрипт проверял, что якорь встречается ровно один раз, и возвращал байты; копия зелёная до и после). Убиты 9 из 9:

| # | Мутация | Итог |
|---|---|---|
| M0 (контрольный, первым) | `player-gm` `max_tokens: 160 → 150` | убит: `TestNarrativeCap/{row_E, invariants/player-gm}`, `TestLLMOutputFixturesAnswerByNarrativeSchema/{valid,invalid}` |
| R2a | `encounter-wolf` без `owned_entity_types` | убит: `TestBlueprintsListWhatTheirRolesPublish/encounter-wolf` |
| R2b | `domain-dark-forest` без `owned_entity_types` | убит: `…/domain-dark-forest` |
| R2c | `global-dark-forest-world` без `owned_entity_types` | убит: `…/global-dark-forest-world` |
| R2d | `encounter-wolf` `owned_entity_types: [encounter]` | убит: `…/encounter-wolf` |
| R2e | `domain-dark-forest` `+ player` в `owned_entity_types` | убит: `…/domain-dark-forest` и правило 8 (`TestBlueprintsValidate`, `…Offline`) |
| R4 | `encounter-wolf` `+ entity.create.proposed` в блупринте и в белом списке `encounter` (`levels.go`) | убит: `…/encounter-wolf` |
| R4b | `+ entity.create.proposed` только в белом списке `encounter` | убит: `…/encounter-wolf` |
| R5 | `dice.rolled` снят и из `encounter-wolf`, и из строки таблицы | убит: `…/encounter-wolf` (белый список шире таблицы) |

Копия и скрипт удалены по точным путям.

**Прогоны при приёмке** (рабочая папка задачи).
- `go build ./... && go vet ./...` — зелёные; `gofmt -l blueprints` — пусто.
- `go test -short -count=1 ./blueprints/... ./shared/agent/... ./test/fixtures/...` — все `ok`; `go test -count=1 -v ./blueprints/` — 14 PASS.
- `golangci-lint run ./blueprints/...` — 0 issues.
- `go run ./cmd/mvctl contracts check` — 65 типов, 8 топиков, 58 файлов схем.
- `make test` — exit 0, `FAIL` нет; порог покрытия: `internal/mechanics` 94,9 %, `internal/llm` 99,7 %.
- `gitleaks dir blueprints --redact -c .gitleaks.toml` — no leaks found.

**Слияние с кончиком эпика `b9a169c`** (T-451, changelog 0.3.8, синхронизация develop с contracts v0.14). Трёхсторонняя проверка `git merge-file` в scratch `t203acc-merge` (база `7a8c100`, рабочая копия приведена к LF, как в объектах git); копия удалена по точному пути.
- `tasks.md` — 2 конфликта: **шапка** (0.3.9 против 0.3.8) — оставить 0.3.9; **changelog** (обе стороны добавили строку после 0.3.7) — оставить обе, сначала 0.3.8 (T-451), затем 0.3.9 (T-203). **Хвост** (§13, раздел T-451 перед §14) и строки DoD T-208/T-254 кончика слились без конфликта. Драйвера `appendtail` у `tasks.md` нет — разрешает оркестратор руками.
- `dev-log.md`, `review.md` — у `git merge-file` конфликт хвоста (обе стороны дописали записи). Драйвер `appendtail` (`.git/merge-drivers/append_tail.py`) на тех же входах — rc 0, маркеров нет.
- `tasks/T-203.md` — без конфликта; `blueprints/`, `schemas/agent/`, `testdata/fixtures/` на кончике не менялись.

**Правки индекса `../tasks.md` (v0.3.9)** — скриптом Python: точная замена, каждый фрагмент ровно один раз, CRLF и NUL до и после.
- T-203 — `done`, пункты DoD `[x]`, строка приёмки; ветка в разделе исправлена на `task/T-203-mvp1-blueprints`.
- N-4 приёмки T-456: T-211 (свойство-тест — только T-212; пометки «уточняется T-456» и «действует после её слияния» сняты), T-212 (условие «не раньше `timestamp`» — только на `clock.Manual`), T-250 (двухвариантная строка удалена, окно по `called_at` с `ErrBudgetKey`).
- Строки DoD: T-216 (тест T-203 без исключения, `pendingFiles` и `pendingReference` удалены; в «Файлы» добавлен `blueprints/blueprints_test.go`), T-222 (комментарий правила 8; при отсутствии T-216 — исключение по `Issue.Code`), T-223 (`Emitter` по `allowed_event_types`/`owned_entity_types` блупринта, тест роли по `publishedByDesign` на `blueprints/`), T-236 (У-3 ревью безопасности T-318, дословно, и удаление только по точным ключам), T-447 (раздел заведён в «Хвосте I1b»: `blueprints/*_test.go` не в образе), стенд T-260 (потолок `max_tokens` тиков, `maxItems` у `affects[]`/`ops[]`, тест).

### Вопросы architect#2
1. **КД §13.3 и `api-contracts.md` §3.3.** `allowed_event_types` трёх блупринтов шире скелетов: `encounter-wolf` — `+ dice.rolled, entity.update.proposed`; `domain-dark-forest` — `+ entity.create.proposed, entity.update.proposed`; `global-dark-forest-world` — `+ entity.update.proposed` (ревью #1 Ma-1, вариант (а)). КД §13.3 ссылается на скелеты «без изменений». Нужно отразить в обоих документах, что список включает все публикации роли, в том числе `entity.*.proposed` и `dice.rolled` встречи, потому что `Emitter` сверяет с ним каждую публикацию (КД §5.4). Если решение — `Emitter` сверяет владение по таблице владения, а не по блупринту, Mi-4 снижается до Nit, а строку DoD T-223 нужно поправить.
2. **TL2-7** (КД §20 `:1116`, `tasks.md` §10 п. 7). Формулировка «валидатор проверяет `allowed ⊆ enum` файла» к региону буквально не подходит: `encounter.started` и `entity.*.proposed` есть в `allowed_event_types`, но модель тика их не предлагает. Сейчас тест T-203 проверяет «`enum` = белый список роли без `entity.*.proposed` и `encounter.started`» и `enum ⊆ allowed_event_types`. Нужны новая формулировка и решение, нужна ли проверка в валидаторе (поле `ValidationEnv`, T-222/T-204).
3. **Плейсхолдеры против секций сборщика КД §11.4.** `{state}`, `{events}`, `{canon}`, `{absence}`, `{region.description}` в `## phase2`/`## tick` могут дублировать секции `<canon>`, `<state>`, `<events>`, `<absence>` сборщика; `{player.name}`, `{region.*}`, `{canon}` в `## system` делают кэшируемую часть промпта зависимой от игрока и региона. Решение нужно до T-218 и T-235; правка текста секций блупринтов тест T-203 переживёт (L и фраза остаются).
4. **Единица `response_len` (к system-architect#1, через оркестратора).** C-07 единицу не называет. T-203 записала байты UTF-8 `response_raw` (`len()` в Go, 143); прежнее 214 не совпадало ни с байтами, ни с символами. Нужна норма в C-07 (байты или символы) и строка DoD T-211, если выбраны символы.

**Бэклог, не закрытый приёмкой.**
- Фикстура региона пишет `blueprint_ref: "domain-dark-forest@1.0"`, блупринт — `version: "1.1"`. Фикстура — EPIC-001: уведомить tech-lead#1 через оркестратора.
- Тест `npc_table[].stats_ref` ↔ `rules/dark-forest.yaml` и фикстуры — уже в DoD T-232 (бэклог T-202 п. 2); удобное место — `blueprints/blueprints_test.go`.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 18:40 | 13.09 18:59 | 19m |
| Ревью #1 | 13.09 18:59 | 13.09 19:18 | 19m |
| Итерация 2 | 13.09 19:18 | 13.09 19:26 | 8m |
| Ревью #2 | 13.09 19:26 | 13.09 19:38 | 12m |
| Приёмка | 13.09 19:38 | 13.09 19:50 | 12m |
| Слияние | 13.09 19:50 | 13.09 19:50 | 0m |
| **Всего** | | | **70m** |
