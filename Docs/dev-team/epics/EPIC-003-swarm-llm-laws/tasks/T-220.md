# T-220: C4b · `testkit/swarm.FakeNarrator` и шаблоны `template/ru.go` — **ранний merge `shared/testkit/swarm`, вход в 

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-2 (выполнена в период одной команды) |
| Волна | 1 |
| Исполнитель | developer#3 |
| Статус | done (принята tech-lead#2; итерация 3 сверена по диффу) |

> Карточка заведена задним числом при переходе на плагин 0.6.0. Описание и DoD
> живут в индексе `../tasks.md` и не дублируются здесь, чтобы не завести второй
> источник истины. Ход работы — `../dev-log.md`, замечания — `../review.md`,
> решения — `journal.md`.

## Описание и DoD
См. раздел T-220 в `../tasks.md`.

## Выполнение (developer)

developer#3, 2026-09-11. Подробности, решения по ходу и таблица мутаций — `../dev-log.md`, раздел «T-220».

**Что сделано.** `FakeNarrator` отвечает на все шесть поводов блока «Заглушка» C-05: `player.entered_region`
и `player.looked` → `entry` (C-05 v1.2), `encounter.started` → `world_event`, последний `combat.decided`
обмена в scope `solo` → `turn`, `entity.updated` игрока со `status → dead` → `death`, `round.closed` →
`round`. Каждый нарратив несёт `generated_by: template`, `fallback_reason: unavailable`,
`filter{applied: false, status: pass, filter_version: none}`, `laws_version: v1`, `narrative_event_id = id`.
Адресат — только игрок. Повтор доставки после успешной публикации не даёт второго нарратива (окно `window`,
запоминание после публикации — итерация 2); цена — возможный дубль под новым `id` при потерянном ack (итерация 3). Появился неблокирующий
`FakeNarrator.Err()`, и `FakeContext.Health()` спрашивает обе заглушки. Шаблоны `template/ru.go` — по таблице
на вид и на исход хода, без `<`, `>`, `*`, `_`, обратной кавычки.

**«Последний `combat.decided` обмена»** выводится из полей события (`action`, `outcome.target_dead`,
`outcome.success`, `free_attack`, `outcome.living_enemies`) по правилу обмена C-05, без времени;
решения собираются по `meta.causation_id`. Явного признака в событии нет — открытый вопрос.

**Изменённые файлы**
- переименован и переписан: `shared/testkit/swarm/narrator.go` → `fake_narrator.go`;
- переименован и переписан: `shared/testkit/swarm/narrator_test.go` → `fake_narrator_test.go`;
- создан: `shared/testkit/swarm/fake_narrator_stand_test.go`;
- изменены: `shared/testkit/swarm/fake_context.go`, `fake_context_test.go`, `template/ru.go`,
  `template/ru_test.go`, `README.md`;
- `fake_encounter.go` не менялся (SHA-1 `a911330f`).

**Тесты**

| Команда | Результат |
|---|---|
| `go build ./... && go vet ./...` | зелёные |
| `go test -short -count=1 ./...` | 27 пакетов `ok` |
| `go test -tags e2e -count=1 ./test/...` | `ok` |
| покрытие `shared/testkit/swarm` / `template` | 88,5 % (было 86,0 %) / 95,8 % (было 93,8 %) |
| `golangci-lint run ./...` | 0 issues |
| `go run ./cmd/mvctl contracts check` | 65 типов, 8 топиков, 58 схем |
| стенд пары `TestTheHarnessAndTheEncounterOfTheSwarmOnOneBus`, `-count=20` | 320 из 320 PASS |
| стенд нарратора `TestTheNarratorTellsTheFightsOfTheStand`, `-count=20` | 320 из 320 PASS |
| мутации ключевых правил | 18 из 18 красят свой тест |
| `-race` | не запускался: нет cgo (T-401) |

**Отклонения от дизайна и задачи**
1. Потолок T-018 «≤ 10 шаблонов» больше не выполняется: в таблицах 22 текста (entry 6, round 4,
   turn 8 на пять исходов, death 2, world_event 2). Потолок ставился для v0 из трёх видов;
   тест потолка заменён тестом достижимости каждого текста. Вынесено в открытые вопросы.
2. Плейсхолдер `{hp_max}` переименован в `{hpmax}`: иначе тест SEC-10 по таблице «как она написана»
   ловил бы подчёркивание в самом плейсхолдере.
3. Блок `filter` — `{applied: false, status: pass, filter_version: none}`, а не только
   `{applied: false}` из текста задачи: схема требует все три поля.
4. Нарратор не подключён к стенду пары в `shared/testkit/gateway` (чужой каталог). Вместо этого в
   своём пакете появился тот же стенд плюс нарратор — `fake_narrator_stand_test.go`.

**Предложения в бэклог:** явный признак конца обмена в `combat.decided`; обновить C-05 «Заглушка»
(«фактическое покрытие v0 — три повода») и расхождение № 3 в
`diagrams/c4-component-swarm-llm-laws.md` и `diagrams/seq-combat-solo.md` — это документы архитектора;
e2e `solo-30` с нарративом хода — T-242.

### Итерация 2 — замечания ревью #1 (2026-09-11)

Все четыре пункта закрыты. Подробности и мутации — `../dev-log.md`, «T-220 · итерация 2».

| Пункт | Что сделано | Тест | Мутация |
|---|---|---|---|
| Mi-1 | событие запоминается только после успешной публикации — своё окно `window`, потому что `Dedup.Seen` спрашивает и запоминает одним шагом; на отказе `log.Error`, шина повторяет обработчик, при постоянном отказе событие уходит в `dead_letters`; решения хода лежат до успешной публикации, и последнее решение при повторе второй раз не собирается | `TestAPublicationThatFailedOnceIsToldAfterTheRetry` (зонд P1, один отказ: один нарратив, 2 попытки, DLQ пуст), `TestAPublicationThatAlwaysFailsEndsInDeadLetters` (всегда: запись в DLQ с исходным событием, попыток публикации = попыток шины), `TestATurnWhosePublicationFailedIsToldFromAllItsDecisions`, `TestADeathWhosePublicationFailedIsToldOnTheRetry`; повторная доставка после успеха — `TestAnEventDeliveredTwiceIsToldOnce` | I1–I5 красные |
| Mi-2 | новый исход `template.Stuck`: неудачное бегство без удара вдогонку рассказывается текстом без удара и без врага | `TestAFlightNothingStruckAtClaimsNoBlow` (`flight(false, true, 0)`, `flight(false, false, 1)` и пойманное с ударом), `TestAFlightNothingStruckAtNamesNoBlow`; SEC-10 нового текста — `TestNoTextNeedsAParseMode` | I6, I7 красные |
| N-1 | ссылка в `dev-log.md` §9 исправлена: имя персонажа — `api-contracts.md` (`name_invalid`) и `gateway-and-bot.md`; SEC-10 закрыт ботом без `parse_mode` (ADR-006 доп. п. 4); имена регионов и NPC из фикстур шлюз не проверяет | — | — |
| N-2 | выброшенный незакрытый ход — `log.Error` | — | — |

Шаблонов теперь 23 (исход `Stuck` +1). Проверки: 27 пакетов `ok`, e2e `ok`, покрытие `swarm` **89,0 %**
(было 88,5 %), `template` 95,8 %, линтер 0, `contracts check` чисто, стенды пары и нарратора — 320 из 320
каждый. Мутанты — только через `go test -overlay` из каталога `mktemp -d`, файлы дерева не
подменялись (SHA-1 до и после совпали).

### Итерация 3 — замечания ревью #2 (2026-09-11)

Только текст плюс одно сообщение лога, поведение не менялось. Подробности — `../dev-log.md`, «T-220 · итерация 3».

| Пункт | Что сделано |
|---|---|
| Mi-3 | обещание «запоминание после публикации ничего не задваивает» снято. Цена названа: поздняя запись меняет беззвучную потерю на возможный дубль под новым `id` — при потерянном ack (P4) и при конкурентной доставке одного события (P5). У `Narrate`/`Observe` записано предусловие: не вызывать конкурентно для одного события, сегодня его держат `membus`, `kafka`, `Delivery`. Правки: комментарии `told`, `Narrate`, `Observe`, `window` в `fake_narrator.go`, строка 27 этой карточки, уточнение в dev-log |
| N-3 | сброс закрытого хода, текст которого не опубликован, логируется отдельным сообщением: «text of a turn was never published, its last decision is in dead_letters». Для незакрытого хода сообщение прежнее |

## Ревью (code-reviewer)

| Итерация | Дата | Ревьюер | Вердикт | Critical | Major | Minor | Nit |
|---|---|---|---|---|---|---|---|
| #1 | 2026-09-11 | code-reviewer#2 | **принять** | 0 | 0 | 2 | 2 |
| #2 | 2026-09-11 | code-reviewer#2 | **принять** | 0 | 0 | 1 | 1 |

Полный разбор — `../review.md`, раздел «T-220 · ревью #1»: Mi-1 (дедупликация до публикации — потеря без следа, в
обход `dead_letters`), Mi-2 (текст «пойман» утверждает удар вдогонку, которого не было), N-1 (ссылка SEC-10 на C-08),
N-2 (уровень лога выброшенного хода).

Ревью #2 (итерация 2) — `../review.md`, раздел «T-220 · ревью #2»: Mi-1, Mi-2, N-1, N-2 закрыты, подтверждено
зондами и мутантами (MR7, MR9, MR16b, K1–K8 красные). Новые: Mi-3 — комментарий и dev-log обещают «дублей не бывает»,
а потерянный ack и конкурентная доставка дают дубль (зонды P4, P5; поведение верное, неверна гарантия); N-3 — ход с
окончательно отказанной публикацией выбрасывается с сообщением «never closed».

## Приёмка (tech-lead)

**Решение:** **принята** · 2026-09-11 · tech-lead#2 (владелец EPIC-003). Статус `done` ставит оркестратор после
заполнения «Учёта времени». Ранний merge `shared/testkit/swarm` выполняется после приёмки; в ветке
`epic/EPIC-001-foundation` он не выполняется (журнал, приёмка T-219).

**Итог:** заглушка нарратора первой фазы отвечает на все шесть поводов C-05 v1.3 и выполняет специфический и общий
DoD. Итерация 3 без ревью #3 сверена по диффу: поведение не менялось.

**Сверка итерации 3 (ревью #3 не проводилось, решение оркестратора).** Версия итерации 2 извлечена из висячего blob
индекса `1d2ed1b7`: `sha1sum` `7c8de95b` — тот же, что в ревью #2. Сравнена с индексом `016f9546`: `sha1sum`
`5f4e0b11`, рабочее дерево равно индексу. Дифф `fake_narrator.go` +35/−11. Изменены только комментарии `told`,
`Observe`, `Narrate`, `collect`, `window` и выбор одного из двух сообщений `Error` в `collect` по
`closesExchange(последнее решение)`. Это чистая функция; `ex.decisions` в буфере всегда непуст. Других изменений
логики нет. Прежних версий тестов итерации 2 среди висячих blob нет, значит тесты не переписывались.
`template/ru.go` (`4ba0efed`) и `fake_encounter.go` (`a911330f`) те же, что на ревью #2.

**Прогоны tech-lead#2:**

| Команда | Итог |
|---|---|
| `go build ./... && go vet ./...` | зелёные |
| `go test -short -count=1 ./shared/testkit/...` | 8 пакетов `ok` |
| `go test -tags e2e -count=1 ./test/...` | `test/e2e`, `test/fixtures` — `ok` |
| стенд пары и стенд нарратора, `-count=10` | 160 из 160 подтестов `PASS` у каждого, `FAIL` 0 |
| покрытие `swarm` / `template` | 88,9 % / 95,8 %; непокрыты только строки 603–606 — новая ветка N-3 |
| `golangci-lint run ./shared/testkit/...`, `gofmt -l` | 0 issues, пусто |
| `go run ./cmd/mvctl contracts check` | 65 типов, 8 топиков, 58 схем |
| `-race` | не запускался, нет cgo (T-401) |

**DoD.** Шесть поводов покрыты тестами, адресаты — только игроки scope (`recipientOf`, мутант M6). SEC-10 держит
`TestNoTextNeedsAParseMode`. `narrative_event_id = id` проверяет `TestEveryOccasionOfTheContractIsTold`.
Негатив «нет боя» — `TestTheNarratorHasNoFightInIt`. `contracts.Validate` вызывают и сквозной тест, и стенд.
Переменных окружения нет (`TestTheStubNamesNoEnvironmentVariable`). Пункт `goleak` не применим: в модуле его нет,
подписки останавливаются через `Wait`/`Stop` в каждом тесте.

**Замечания приёмки, не блокируют:**
1. Непокрытая ветка сообщения N-3 (−0,1 п.) принята как есть. Она выбирает только текст лога, условие
   `closesExchange` покрыто в других местах. Тест на формулировку лога ничего не удержит.
2. README пакета (раздел о повторе доставки) не называет цену позднего запоминания — дубль под новым `id` при
   потерянном ack. Утверждение README формально верно, но неполно. Исправить в проходе по документам или в T-229.
3. Цитата сообщения N-3 в карточке и dev-log короче, чем в коде: в коде есть хвост «; the turn is dropped».

**Подпакет целиком.** Этой приёмкой tech-lead#2 подписывает `shared/testkit/swarm` для раннего merge по C-05:
T-219 принят tech-lead#1, `fake_encounter.go` с его приёмки не менялся (`a911330f`), стенд пары зелёный.
Отдельная переподпись T-219 не нужна.

**Вопросы (а)–(г) у архитектора** заглушку первой фазы не блокируют. (в) обязателен до T-229/T-230, (г) — до
доставки EPIC-004.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 11.09 12:04 | 11.09 12:49 | 45m |
| Ревью | 11.09 12:49 | 11.09 13:06 | 16m |
| Разработка 2 | 11.09 13:06 | 11.09 13:20 | 13m |
| Ревью 2 | 11.09 13:20 | 11.09 13:35 | 14m |
| Разработка 3 | 11.09 13:35 | 11.09 13:41 | 6m |
| Приёмка | 11.09 13:41 | 11.09 13:51 | 9m |
| **Всего** | | | **1h 47m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет. Метки до 2026-09-11 11:56
перепривязаны к настоящему времени коммитов (журнал, 2026-09-11) — это оценка.
