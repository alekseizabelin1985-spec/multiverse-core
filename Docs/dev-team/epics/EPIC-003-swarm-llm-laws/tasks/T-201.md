# T-201: A1 · `shared/agent` v2: типы блупринта, парсер, плейсхолдеры, чистка пакета

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-2 |
| Подволна | A (`../tasks.md` §6.1) |
| Размер | M |
| Исполнитель | developer#1 |
| Статус | done (принята tech-lead#2 2026-09-13) |
| Ветка | `task/T-201-agent-blueprint-v2` от `epic/EPIC-003-swarm-llm-laws` (база `cd21a48`) |
| Итераций ревью | 3 (ревью #1 и #2 — вернуть; ревью #3 — принять) |
| Начата / завершена | 2026-09-13 12:58 / 2026-09-13 13:30 (разработка), итерации 2 и 3 — 2026-09-13 |

## Описание и DoD
Главный источник — раздел T-201 в `../tasks.md` (индекс эпика, ревизия 4 от 2026-09-13), общий DoD — там же, §1. Здесь не дублируется, чтобы не завести второй источник истины. Кратко:
- Зависит от: F-2, F-4b (EPIC-001, done); решение tech-lead#1 об исключении `shared/agent/` в `.golangci.yml`.
- Главное из ревизии 4: список удаления — по дереву; `ContentHash` вида `sha256:<64 hex>`.

## Выполнение (developer)
developer#1, 2026-09-13 12:58–13:30. Подробно — запись `<!-- dev-log T-201 -->` в `../dev-log.md`. Коммит не делался (запрет в промпте), перенос проиндексирован `git mv`, новые файлы не в индексе.

**Что сделано.**
- `shared/agent/types.go` — `AgentLevel`, `LODLevel`, `AgentLifecycleState` из `agent_types.go` без изменений.
- `shared/agent/blueprint.go` — `AgentBlueprint` v2 (КД §13.1) со всеми вложенными типами; ключи as-is приняты как `Legacy*` и переносятся парсером.
- `shared/agent/parser.go` — `ParseFile/ParseBytes`: frontmatter + секции `## system|phase1|phase2|tick|canon|description`, чистый YAML, `KnownFields(true)`, `ParseError{File, Line, Field, Reason}`, миграция `type`/`phase1_prompt`/`phase2_prompt`/плоских `llm.model|temperature|max_tokens` с `Issue{Severity: warning}` в `ParseIssues`; `ContentHash` = `sha256:<64 hex>` от канонического JSON (не зависит от порядка ключей, формата, CRLF, имени файла).
- `shared/agent/placeholders.go` — словарь КД §11.4 (17 имён) и функции `PlaceholderNames/IsKnownPlaceholder/ExtractPlaceholders/UnknownPlaceholders`.
- `shared/agent/testdata/blueprints/valid/` — пять `.md` и `personal-gm.yaml` (чистый YAML).
- `lod.go`, `tools/registry.go` — время через `shared/clock` (`NewLODManager(cfg, Clock, Timers)`, `NewToolRegistry(Clock)`); исключение `shared/agent/` в `.golangci.yml` **снято целиком** — под исключением не осталось ничего.
- Перенос `git mv` в `services/_archive/shared/agent/`: `agent_types.go`, `md_parser.go`, `blueprint_loader.go`, `blueprint_validator.go`, `blueprint_validator_test.go`, `helpers.go`, `worker_pool.go`, `state_manager.go`, `router.go`, `lifecycle.go`, `pipeline.go`, `interfaces.go` (целиком — устаревших частей в нём 100 %), `agent_test.go`, `examples/domain-dark-forest.md`; `README.md`, `MIGRATION.md` → `Docs/archive/shared/agent/`. Строки: `services/_archive/README.md`, `services/_archive/shared/agent/ARCHIVED.md`, `Docs/archive/README.md` (уведомить tech-writer).

**Изменённые файлы.** Созданы: `shared/agent/{types,blueprint,parser,placeholders}.go`, `shared/agent/{parser,content_hash,placeholders,types,lod}_test.go`, `shared/agent/tools/registry_test.go`, `shared/agent/testdata/blueprints/valid/{global-world,domain-region,encounter,personal-gm,group-narrator}.md`, `personal-gm.yaml`. Изменены: `shared/agent/lod.go`, `shared/agent/tools/registry.go`, `.golangci.yml` (только блок исключения `shared/agent/`), `services/_archive/README.md`, `services/_archive/shared/agent/ARCHIVED.md`, `Docs/archive/README.md`, `../dev-log.md`, эта карточка. Перенесены — 16 файлов (выше).

**Тесты и проверки.**
- `go build ./... && go vet ./...` — зелёные.
- `go test -short -count=1 ./...` — все пакеты ok; `shared/agent` 92.2 %, `shared/agent/tools` 68.1 %.
- `golangci-lint run ./...` — 0 issues; `golangci-lint run ./shared/agent/...` — 0 issues без исключения.
- `make test` — ok (без race: нет cgo, ОВ-5), порог покрытия `internal/*` зелёный.
- Мутанты в копии дерева в scratch (без `-overlay`): контрольный первым — убит; 27 из 27 убиты; копия удалена по точному пути.
- `gitleaks dir` по новым файлам — no leaks.
- Потребители: в `cmd/`, `internal/`, `shared/testkit` импортов `shared/agent` нет; as-is `services/narrative-orchestrator` собирается из `LEGACY_SRC_REF=e6103e4`, не из дерева.

**DoD (специфика).** ParseFile по корпусу и чистому YAML, секции в `Prompts.*` — да. Неизвестный ключ → ошибка с полем и файлом; плоский `llm.model` → предупреждение, разбор не падает — да. `ContentHash` стабилен на 100 разборах, меняется при замене любого байта (проверено перебором всех байт трёх файлов), форма `^sha256:[0-9a-f]{64}$` — да. `go build ./...` зелёный, вне `services/_archive/**` на удалённые символы никто не ссылается — да. `golangci-lint run ./shared/agent/...` зелёный; под исключением не осталось ничего (исключение снято).

**Отклонения от дизайна.** `Severity` включает `info` (нужен T-202); предупреждения парсера — в `AgentBlueprint.ParseIssues`, сигнатура `ParseFile` КД §3 сохранена; `TickLLM` принимает `temperature`/`thinking` (T-203); `Temperature *float64`, `Retries *int`; плоский `llm.schema` — ошибка (переносить некуда); текст вне секций — ошибка; сигнатуры `NewLODManager`/`NewToolRegistry` изменились из-за перевода на `shared/clock`.

**Предложения в бэклог.** (1) `lod.go`: ключи `lodDistribution` не совпадают с `LODLevel.String()`, `lastHighLOD` пишется при любой смене. (2) `tools/registry.go`: гонка данных — `checkRateLimit` пишет под `RLock`, `updateStats`/`GetStats` без блокировки. (3) Документы, описывающие as-is `shared/agent`: `diagrams/c4-component-swarm-llm-laws.md:45`, шапка §2 `components/swarm-llm-laws.md`, ADR-015 п. 6, `tasks.md` T-203 («удалён» → «перенесён в архив»).

### Итерация 2 (по ревью #1: 1 Major, 4 Minor, 3 Nit)
developer#1, 2026-09-13. Подробно — запись `<!-- dev-log T-201 iteration 2 -->` в `../dev-log.md`. Коммита нет, новые файлы не в индексе.

| # | Ответ | Что сделано |
|---|---|---|
| Ma-1 | исправлено | `ContentHash` вынесен в `shared/agent/content_hash.go` и строится обходом `reflect` по тегам `yaml`: ключи отсортированы; нулевые поля (nil-указатель, nil-срез/map, `""`, `0`, `false`, нулевая структура) не пишутся; пустой ненулевой срез — `[]`; указатель — значением (`temperature: 0` ≠ нет ключа); `any` — по динамическому типу, строковые ключи map строкой, прочие с типом. `yaml.Marshal` и `recover` убраны; значение без канонической формы (func, chan, complex) — `""`. Эталон обновлён осознанно: было `sha256:fa8d91b1…9457`, стало `sha256:811cfa8e…1371`; хэши ещё нигде не хранились. Канонический текст эталона закреплён отдельным тестом. Тесты: «ключа нет / `[]`» для `allowed_event_types`, `owned_entity_types`, `immediate_broadcast`, `tools`, пары `{}`/нет для `encounter`, `parent`, `phase2`, `retries: 0`, `temperature: 0`; свойство «равные значения ⇔ равные хэши» на всех разобранных мутациях байт трёх файлов корпуса (значение — JSON, различающий nil и `[]`). |
| Mi-2 | исправлено тем же | Комментарий `ContentHash` описывает правило эволюции; внутренний тест: структура с дополнительными полями в нулевом значении даёт тот же канонический текст, а поле, выставленное в указатель на ноль, — другой. |
| Mi-1 | исправлено | Незакрытый fence после цикла — `ParseError{Line: строка открытия, Field: "## <секция>", Reason: "code fence ``` is not closed"}`; fence закрывается только своим маркером (случай ` ``` ` без пары и `~~~`, закрываемый ` ``` `). |
| Mi-3 | исправлено | `SuspiciousPlaceholders(text) []string`: второй, нестрогий шаблон `\{\s*[A-Za-z][\w.\-]*\s*\}`; всё, что он нашёл, а точный — нет, возвращается с фигурными скобками (`{Player.name}`, `{ player.name }`, `{player-name}`, `{region.Description}`). Точная форма — даже неизвестная — не «подозрительная»: её отдаёт `UnknownPlaceholders`. JSON-пример не совпадает (после `{` идёт кавычка). Серьёзность выбирает валидатор (правило 13, T-202). Ограничение: кириллица в скобках (`{игрок}`) шаблоном не ловится. |
| Mi-4 | исправлено | Тест смешанных fence в обе стороны (` ``` `→`~~~`→`## tick`→` ``` ` и наоборот); мутант M2 ревьюера убит. |
| N-1 | исправлено | Frontmatter/файл из одних комментариев — `empty YAML: only comments` с номером строки. |
| N-2 | исправлено | `~` и `{}` — `empty YAML: the document sets no field of a blueprint` с номером строки. |
| N-3 | исправлено | Плоские `llm.model`, `llm.max_tokens`, `phase1_prompt`/`phase2_prompt` — указатели: предупреждение по наличию ключа; нулевое значение отбрасывается («obsolete»), `phase2` при этом не создаётся, хэш равен блупринту без ключа. |
| Бэклог п. 3 | сделано сейчас | `MaxBlueprintSize = 1 << 20`: `ParseBytes` отвергает больший вход, `ParseFile` читает через `io.LimitReader(…, Max+1)`; тест на ровно 1 МиБ (разбирается) и на 1 МиБ + 1 байт (ошибка с файлом). |

**Изменённые файлы итерации.** Создан `shared/agent/content_hash.go`, `shared/agent/content_hash_internal_test.go`. Изменены `shared/agent/{parser,blueprint,placeholders}.go`, `shared/agent/{parser,content_hash,placeholders}_test.go`, `../dev-log.md`, эта карточка. `.golangci.yml` в итерации не трогался.

**Проверки итерации.**
- `go build ./... && go vet ./...` — зелёные.
- `go test -short -count=1 ./...` — все пакеты ok; `shared/agent` 92.7 %, `shared/agent/tools` 68.1 %.
- `golangci-lint run ./...` — 0 issues.
- `make test` — ok (без race: нет cgo), порог `internal/*` зелёный.
- Мутанты в копии `go.mod`, `go.sum`, `shared/agent`, `shared/clock` в scratch, без `-overlay`: копия до мутаций зелёная, контрольный (`sha256:` → `sha1:`) первым — убит; M2 ревьюера — убит; итог 34 из 34 убиты (на первом прогоне выжили 2 — дописан тест канонической формы untyped-значений, эквивалентная проверка якорей в `isExactPlaceholder` упрощена). Копия удалена по точному пути.

**Бэклог (по ревью #1).**
- `shared/agent/tools/registry.go`: гонка данных (`checkRateLimit` пишет под `RLock`, `updateStats`/`GetStats` без блокировки); `lod.go`: ключи `lodDistribution` не совпадают с `LODLevel.String()`, `lastHighLOD` пишется при любой смене. Исправить до первого потребителя в `internal/swarm`, проверка — `go test -race` в CI (Linux, cgo).
- Реестр блупринтов I2 (`BlueprintRegistry`, `agent.blueprint_reloaded`): решение о перезагрузке по `content_hash` — с тестом «файл без ключа → `[]` вызывает перевалидацию».
- Лимит размера блупринта — сделан в итерации 2 (`MaxBlueprintSize`); для hot reload I2 — тот же лимит.
- Документы (architect#2, tech-lead#2, tech-writer): `api-contracts.md` §3.1 (`llm.tick` без `temperature`/`thinking`); КД §2 («`lod.go`/`tools/registry.go` без изменений», шапка «ни одного целевого файла»); `diagrams/c4-component-swarm-llm-laws.md:45`; ADR-015 п. 6; `tasks.md` T-203 («удалён» → «перенесён в архив»); `PULL_REQUEST.md:21-29` (ссылки на перенесённые `md_parser.go`, `examples/`, `README.md`, `MIGRATION.md`).

### Итерация 3 (по ревью #2: 1 Major, 2 Minor, 2 Nit)
developer#1, 2026-09-13. Подробно — запись `<!-- dev-log T-201 iteration 3 -->` в `../dev-log.md`. Коммита нет, новые файлы не в индексе.

| # | Ответ | Что сделано |
|---|---|---|
| Ma-2 | исправлено | `time.Time` пишется без кавычек как `time:<RFC 3339 с наносекундами>`. Структура с неэкспортируемыми полями ниже `any` — ошибка → `""`. Одна дата в разной записи — один хэш. Тот же момент с другим смещением — разные хэши: смещение сохраняется, в UTC не переводится. Это отступление от «UTC» в промпте; обоснование в dev-log: смещение уходит в JSON события, свойство перебора байт сверяется по JSON. Корпус: `domain-fair.md` с датами в `conditions[].value` и `ops[].value` (включён в перебор байт). |
| Mi-5 | исправлено | Нестроковый ключ пишется без кавычек, с меткой типа: `int:1`, `float64:1`, `time.Time:time:…`. Записи сортируются по ключу, при равном ключе — по форме значения, поэтому порядок детерминирован и для двух ключей `NaN`. Тесты: `{1: a}` ≠ `{"int:1": a}` ≠ `{1.0: a}`; смешанные ключи и `{.nan: a, .NaN: b}` дают один хэш на 101 разборе. |
| Mi-6 | исправлено | `\{\s*\p{L}[\p{L}\p{M}\p{N}_.\-]*\s*\}`. `\p{M}` добавлен, чтобы находилась и разложенная `й`. Тест на `{игрок.имя}`, `{Игрок}`, `{ имя }`, `{player.имя}`; JSON, `{0}`, `[A-Z]{2,5}`, Go-шаблоны не находятся. |
| N-1 | исправлено | Правило: число пишется по значению. Float с целым значением в пределах `int64`/`uint64` пишется как целое: `1` = `1.0`, `1000000` = `1000000.0`, `-0.0` = `0`. Прочие — формой `'g'`, в ней всегда есть `.` или показатель. Эталонный хэш не изменился. |
| N-2 | исправлено | Строка ` ``` ` с обратной кавычкой после серии маркера — не забор: не открывает и не закрывает блок. Для `~~~` правило не действует. |

**Изменённые файлы итерации.** Изменены `shared/agent/{content_hash,placeholders,parser}.go`, `shared/agent/{content_hash,content_hash_internal,placeholders,parser}_test.go`, `../dev-log.md`, эта карточка. Создан `shared/agent/testdata/blueprints/valid/domain-fair.md`.

**Проверки итерации.**
- `go build ./... && go vet ./...` — зелёные.
- `go test -short -count=1 ./...` — все пакеты ok.
- `golangci-lint run ./...` — 0 issues.
- `make test` — ok, без race (нет cgo). `shared/agent` 93.3 % (было 92.7 %), `shared/agent/tools` 68.1 %, порог `internal/*` зелёный.
- Мутанты в копии дерева в scratch, без `-overlay`. Контрольный мутант первым — убит. 23 из 23 убиты: дата, скрытые поля, ключи, числа, кириллица, fence. Копия удалена по точному пути.

## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Critical | Major | Minor | Nit | Запись |
|---|---|---|---|---|---|---|---|---|
| 1 | 2026-09-13 | TEAM-2/code-reviewer#1 | **вернуть** | 0 | 1 | 4 | 3 | `../review.md`, раздел «T-201 · ревью #1» |
| 2 | 2026-09-13 | TEAM-2/code-reviewer#1 | **вернуть** | 0 | 1 | 2 | 2 | `../review.md`, раздел «T-201 · ревью #2» |
| 3 | 2026-09-13 | TEAM-2/code-reviewer#1 | **принять** | 0 | 0 | 0 | 2 | `../review.md`, раздел «T-201 · ревью #3» |

Итерация 3, кратко:
- **Закрыто:** Ma-2, Mi-5, Mi-6, N-1, N-2. `go build/vet`, `go test -short ./shared/agent/...`, `golangci-lint ./shared/agent/...` (0 issues) — зелёные; закреплённый хэш не изменился.
- **Отступление принято:** смещение времени сохраняется (не UTC). Для контроля T-26 лишнее «изменилось» безопаснее пропуска; зонд в 6 поясах машины — хэши одинаковые.
- **N-1 и C-02:** противоречия нет — каноническая форма не проводной формат; за 2^53 хэш различает больше, чем `float64`.
- **N-3.** Нет теста `{~: a}` ≠ `{"null": a}`: мутант M16 выжил (`content_hash_test.go:349-373`).
- **N-4.** Флаг `untyped` у поля структуры и значения типизированной map ниже `any` не закреплён: M9, M10 выжили (`content_hash_internal_test.go:173-184`).

Итерация 2, кратко:
- **Закрыто:** все замечания ревью #1 (Ma-1, Mi-1…Mi-4, N-1…N-3), лимит размера сделан. Мутанты ревьюера M0, M2, M3, M4, M5 убиты. `go build/vet`, `go test -short`, `golangci-lint` (0 issues), `make test` — зелёные.
- **Ma-2.** Незакавыченная дата в `ops[].value`/`conditions[].value` декодируется в `time.Time` и пишется как `{}`: любые даты дают один хэш (`content_hash.go:93-94`, `123-145`). Это регрессия итерации 2.
- **Mi-5.** Нестроковый ключ `int:1` совпадает со строковым `"int:1"`; `{1: a, "int:1": b}` на 200 разборах дал 2 разных хэша (`content_hash.go:162-172`).
- **Mi-6.** `SuspiciousPlaceholders` не видит кириллицу (`placeholders.go:62`).
- **N-1.** Нормализация чисел непоследовательна (`1`/`1.0` против `1e6`, `-0.0`).
- **N-2.** ` ```json``` ` в начале строки считается забором fence.

Итерация 1, кратко:
- **Ma-1.** `ContentHash` строится через `yaml.Marshal`, поэтому `allowed_event_types: []` и отсутствие ключа дают один хэш при разных значениях (`parser.go:530-554`).
- **Mi-1.** Незакрытый fence молча съедает следующие секции.
- **Mi-2.** Новое поле типа сдвигает хэши всех блупринтов.
- **Mi-3.** Плейсхолдеры с опечаткой (`{Player.name}`, `{ x }`) невидимы для правила 13.
- **Mi-4.** Смешанные fence не покрыты тестом: мутант M2 выжил.
- **Подтверждено:** DoD, отклонения автора, перенос `git mv` (16 × R100, 974 → 974 файла), `forbidigo` на пакете (мутант L1 красный). Прогоны `go build/vet`, `go test -short`, `golangci-lint` (0 issues), `make test` — зелёные.

## Приёмка (tech-lead)
**Решение: принято** · 2026-09-13 15:40 · TEAM-2/tech-lead#2 · итераций ревью 3.

**Итог:** `shared/agent` v2 соответствует DoD ревизии 4: типы, парсер, `ContentHash` вида `sha256:<64 hex>`, плейсхолдеры; as-is рантайм перенесён в архив; исключение `shared/agent/` снято. Оба Nit ревью #3 закрыты тестами при приёмке.

**DoD (специфика)**

| Пункт | Проверка | Итог |
|---|---|---|
| `ParseFile` разбирает корпус `valid/` и чистый YAML, секции в `Prompts.*` | `TestParseFileParsesTheValidCorpus`, `TestParseMarkdownFillsEverySection`, `TestParsePureYAMLEqualsMarkdown` | да |
| Неизвестный ключ — ошибка с полем и файлом; плоский `llm.model` — предупреждение, разбор не падает | `TestParseUnknownKeyNamesTheFieldAndTheFile`, `TestParseMigratesTheFlatLLMKeysWithWarnings` | да |
| `ContentHash` стабилен на 100 разборах, меняется при замене любого байта, форма `^sha256:[0-9a-f]{64}$` | `TestContentHashIsStableOverRepeatedParses`, `TestContentHashChangesWithAnyByteOfTheContent`, `TestContentHashEncodingIsPinned`. Шаблон схем `agent.spawned`/`agent.blueprint_reloaded` на кончике эпика (T-445) тот же | да |
| `go build ./...` зелёный; вне `services/_archive/**` на удалённые символы никто не ссылается | в дереве задачи и на кончике `epic/EPIC-003-swarm-llm-laws` нет импортов `multiverse-core.io/shared/agent` вне самого пакета (`git grep`). Единственный импорт на кончике — перенесённый `blueprint_validator_test.go` | да |
| `golangci-lint run ./shared/agent/...` зелёный; названо, что осталось под исключением | исключение снято целиком, под ним ничего нет. Основание — решение оркестратора ОВ-7 (журнал 2026-09-09) и вариант (а) ревизии 4 | да, нужна отметка tech-lead#1 (ниже) |
| Общий DoD §1 | тесты и линтер — прогоны ниже. `forbidigo` на пакете работает. `shared/agent` импортирует только `shared/clock` и `yaml.v3`. dev-log: три записи `<!-- dev-log T-201 … -->`. Секретов нет (`gitleaks dir` исполнителя) | да |

**Прогоны приёмки** (рабочая папка `.worktrees/T-201`, Git Bash, 2026-09-13)
- `go build ./... && go vet ./...` — 0.
- `go test -short -count=1 ./...` — все пакеты ok.
- `golangci-lint run ./...` — `0 issues.`
- `go run ./cmd/mvctl contracts check` — 65 типов, 8 топиков, 58 схем, 0.
- `make test` — ok, без `-race` (нет cgo, ОВ-5; гонку проверяет CI на Linux). `shared/agent` 93.3 %, `shared/agent/tools` 68.1 %, порог `internal/*` зелёный.

**Nit ревью #3 — закрыты тестами при приёмке** (правка только в тестах `shared/agent`, код не менялся; `content_hash.go` сверен `cmp` с эталоном)
- **N-3.** `TestContentHashOfMixedMapKeys` проверяет, что `{~: a}` действительно разобран в ключ `nil` и что его хэш отличается от `{"null": a}`.
- **N-4.** В `TestCanonicalFormRefusesHiddenFieldsInUntypedValues` три новых случая:
  - поле структуры ниже `any`;
  - значение типизированной `map[string]hidden` ниже `any`;
  - ключ `map[any]int` в поле структуры.

  Поле в случаях ненулевое: нулевое поле не пишется вовсе.
- **Мутанты** — в копии `go.mod`, `go.sum`, `shared/agent`, `shared/clock` в scratch, без `-overlay`, контрольный первым:
  - M0 `sha256:` → `sha1:` — убит, 5 тестов;
  - M16 ключ `nil` в кавычках — убит `TestContentHashOfMixedMapKeys`;
  - M9 поле структуры с `untyped=false` — убит `TestCanonicalFormRefusesHiddenFieldsInUntypedValues`;
  - M10 значение map с `untyped=false` — убит тем же тестом.

  Копия удалена по точному пути.

**Совместимость с кончиком эпика** (`git diff --stat cd21a48 epic/EPIC-003-swarm-llm-laws` без слияния; пробы `git merge-file` в scratch)
- `shared/agent/**`, `services/_archive/**`, `Docs/archive/**`, эта карточка на кончике не менялись — конфликтов нет. Перенос `git mv` (16 × R100) сливается чисто.
- `.golangci.yml` — трёхстороннее слияние без конфликта. Эпик (T-445) переписал `depguard`, задача убрала только блок исключения `shared/agent/`. `golangci-lint config verify` на слитом файле — 0. `golangci-lint run ./shared/agent/...` со слитым файлом — `0 issues`. Мутант `time.Now` в `lod.go` со слитым файлом — `forbidigo: 1`.
- `dev-log.md`, `review.md` эпика — обе стороны дописывают свои разделы после одной строки. Обычный `merge-file` даёт конфликт, но у файлов `merge=appendtail`: драйвер держит разделы каждой стороны подряд. Если драйвер в `.git/merge-drivers` не подключён, конфликт разрешать «обе стороны, сначала эпик, затем T-201», ничего не удалять.
- `tasks.md` эпика — эпик правил шапку, changelog (стр. 29), T-214/T-215 (стр. 246+) и дописал §13 в конец. Правки приёмки стоят в разделах T-201, T-202, T-203, T-225, их нет в изменениях эпика. Ожидается чистое слияние. При конфликте разрешает tech-lead#2: берётся текст эпика плюс строки с пометкой «(приёмка T-201)».
- Порядок: сначала слить ветку эпика в ветку задачи (`sync`) или сразу задачу в эпик. Конфликт кода не ожидается, разработчик не нужен.

**Нужна отметка tech-lead#1** (`.golangci.yml` — файл EPIC-001, шапка ревизии 4). Правка — снятие исключения, этого ждало решение ОВ-7. Что проверить:
1. `git diff cd21a48 -- .golangci.yml` — один блок: удалён `- path: shared/agent/` с `errcheck`/`forbidigo`/`staticcheck`/`unused`, вместо него комментарий. Остальные правила не тронуты.
2. На слиянии с кончиком эпика (после T-445) `golangci-lint run ./...` — `0 issues`; слитый файл без конфликта.
3. Под `shared/agent/` не осталось as-is кода, которому было нужно исключение. Архив лежит под `services/`, его исключает `exclusions.paths`.
4. `forbidigo` на пакете действует: мутант `time.Now` красный (L1 ревью #1, повторено при приёмке).
5. Попутно по владению: строки в `services/_archive/README.md` и новый раздел `services/_archive/shared/agent/ARCHIVED.md` (строка `services/_archive/**`: «EPIC-001 создаёт, далее только чтение»), строки в `Docs/archive/README.md` (tech-writer). Их требует DoD T-201 (перенос вместо удаления, OQ-A-17). Нужно подтверждение tech-lead#1 и уведомление tech-writer.

**Бэклог, принятый при приёмке** (разнесён в `../tasks.md`: раздел T-201 и DoD T-202, T-225; ни один пункт не блокирует)
- Гонка `tools/registry.go` и ключи `lodDistribution`/`lastHighLOD` в `lod.go` — до первого потребителя в `internal/swarm`.
- Пустой `ContentHash` — ошибка, а не пропуск поля `agent.spawned.content_hash` (T-225).
- Предупреждения валидатора о числах за |x| ≤ 2^53−1 и о `time.Time` в `ops[].value`/`conditions[].value`; серьёзность правила 13 для `SuspiciousPlaceholders` (T-202).
- Устаревшие документы об as-is `shared/agent` (architect#2, tech-writer). Формулировка T-203 в `../tasks.md` исправлена при приёмке.

**Не заполнено tech-lead:** «Учёт времени» — ведёт оркестратор.

## Отметка владельца EPIC-001 (tech-lead#1)
**Решение: согласовано** · 2026-09-13 · TEAM-1/tech-lead#1 (владелец `.golangci.yml`, `services/_archive/**`). Слияние T-201 с моей стороны не блокируется.

**Итог:** правка `.golangci.yml` — ровно снятие исключения `shared/agent/` по ОВ-7. Со слиянием с кончиком эпика она совместима, `forbidigo` на пакете действует. Перенос в архив сделан `git mv` без потерь, строки в индексах архива только дописаны.

**1. `.golangci.yml`** (рабочая папка `.worktrees/T-201`, копия слитого дерева в scratch `t201own-*`, golangci-lint 2.13.2)

| Проверка | Итог |
|---|---|
| `git diff cd21a48 -- .golangci.yml` | один блок `.golangci.yml:299-303`: вместо `- path: shared/agent/` с `errcheck`/`forbidigo`/`staticcheck`/`unused` стоит комментарий. Остальное не тронуто |
| Основание | ОВ-7 (журнал 2026-09-09): снятие временных исключений as-is пакетов — критерий DoD задач EPIC-003 для `shared/agent`. Прецедент — `shared/eventbus` в T-005 |
| Трёхстороннее `git merge-file` (ours = рабочая копия, base = `cd21a48`, theirs = `3141755`, кончик `epic/EPIC-003-swarm-llm-laws` с T-445) | rc=0, маркеров конфликта нет. От файла эпика слитый отличается только этим блоком (стр. 424-428 слитого файла) |
| Копия слитого дерева: `git archive 3141755`, поверх — `shared/agent` из рабочей папки (на кончике `shared/agent` равен базе), архивные файлы и слитый `.golangci.yml` | `golangci-lint config verify` — 0; `go build ./...` — 0; `golangci-lint run ./shared/agent/...` — `0 issues`; `golangci-lint run ./...` — `0 issues` |
| Осталось ли что-то под исключением | нет. В `linters.exclusions.rules` нет правила по `shared/agent`. Общее `shared/[a-z]+/examples/` пакет не задевает: `examples/` перенесён в архив. Архив исключён `exclusions.paths` (`services/_archive`, `services/`) |
| Контрольный мутант M-own-1: `lod.go:124` `lm.clock.Now()` → `time.Now()` | со слитым файлом — `forbidigo: 1`, rc=1 (красный). Тот же мутант с `.golangci.yml` кончика эпика, где исключение ещё есть, — `0 issues`: краснеет именно из-за снятия исключения |
| Мутант M-own-2: `tools/registry.go:187` `r.clock.Now().Sub(start)` → `time.Since(start)` | `forbidigo: 1`, rc=1 (красный) |

Файлы копии возвращены к исходным (`cmp` с рабочей папкой). Копия удалена по точному сохранённому пути.

**2. Владение архивом — решение: допустимо в составе T-201, отдельная правка владельца не нужна**
- «Только чтение» в `plan/ownership.md` §1 (строка `services/_archive/**`) защищает содержимое перенесённого кода и уже записанные строки. Дописывать индекс при новом переносе этот запрет не мешает. Сама карта предполагает переносы из других эпиков: «после переноса → `services/_archive/` владельцем нового пакета» (строки `services/game-service/**` и прочих, `narrative-orchestrator` в EPIC-003 I2), и у каждого каталога должен быть `ARCHIVED.md`.
- Прецедент (`git log --follow services/_archive/README.md`): `5a20bb8` (T-002) создал индекс, `b7df900` (T-003) дописал строки `shared/agent/{filter.go,e2e_dark_forest_test.go}` и создал этот же `ARCHIVED.md`, `69d1aed` (волна 0) дописал строки. Каждый перенос сопровождался дописыванием индекса в том же коммите.
- Проверено: в `services/_archive/README.md` дописаны 2 строки (стр. 73-74), в `ARCHIVED.md` — раздел со стр. 23, удалённых строк нет (`git diff`: +2 и +22). Столбцы таблицы соблюдены. «Последний рабочий коммит» сверен `git log -1 cd21a48 -- shared/agent/<файл>` по всем 14 файлам: `b7df900` у 10, `e6103e4` у `blueprint_validator.go`/`lifecycle.go`/`interfaces.go`, `81aaf98` у `examples/domain-dark-forest.md` — совпадает. Раздел T-003 в `ARCHIVED.md` (стр. 1-21) не правился — так и должно быть.
- `Docs/archive/README.md:33-34` — две строки по правилам каталога (перенос `git mv`, строка в таблице). Каталог ведёт tech-writer (ownership §1, `Docs/archive/**`). **Уведомить tech-writer** (оркестратор): строки T-201 в `Docs/archive/README.md`; сводка архива в `README.md:169` («`shared/agent/tools/*` … и `filter.go`») теперь неполна, её закрывает «и др.». По правилу `services/_archive/README.md:93-94` строку лучше дополнить.

**3. Перенос `git mv`** (`git diff --cached -M --name-status`)
- 16 × `R100`: 14 файлов → `services/_archive/shared/agent/`, `README.md`/`MIGRATION.md` → `Docs/archive/shared/agent/`; содержимое не менялось (0 insertions/0 deletions), значит `git log --follow` после коммита пройдёт по истории.
- Удалений нет: `--diff-filter=D` пуст и в индексе, и в рабочей копии; отслеживаемых файлов 974 = 974 (`ls-tree HEAD` и `ls-files`).
- Все 16 файлов `shared/agent` из `HEAD`, кроме `lod.go` и `tools/registry.go`, перенесены. Эти два остались в пакете и изменены.

**Не блокирует** (правка владельца отдельной задачей в бэклоге EPIC-001, если до коммита T-201 не поправят сами):
- `services/_archive/README.md:37-40` — абзац «Коммит архивации для всех строк, кроме двух файлов `shared/agent`, один: T-002» после стр. 73-74 неточен: у двух новых строк перенос из T-201. Прецедент T-003 поправил этот абзац в том же коммите. Предлагаемая формулировка: «…кроме строк `shared/agent`: `filter.go` и `e2e_dark_forest_test.go` — T-003 (база `5a20bb8`), as-is рантайм и `examples/` — T-201 (EPIC-003, база `cd21a48`); см. `services/_archive/shared/agent/ARCHIVED.md`».
- `plan/ownership.md` §1, строка `services/_archive/**`: уточнить «только чтение» так: «содержимое и существующие строки — только чтение; строки индекса и `ARCHIVED.md` при новом переносе дописывает эпик-владелец переноса». Правка карты — tech-lead#1, v0.6.
- В рабочей копии `ARCHIVED.md` смешанные окончания строк (`w/mixed`; `text=auto` приведёт к LF в индексе). На коммит не влияет.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 12:52 | 13.09 13:30 | 38m |
| Ревью #1 | 13.09 13:30 | 13.09 13:45 | 15m |
| Итерация 2 | 13.09 13:45 | 13.09 14:08 | 23m |
| Ревью #2 | 13.09 14:08 | 13.09 14:22 | 14m |
| Итерация 3 | 13.09 14:22 | 13.09 14:39 | 17m |
| Ревью #3 | 13.09 14:39 | 13.09 14:52 | 13m |
| Приёмка (прервана лимитом API) | 13.09 14:52 | 13.09 15:22 | 30m |
| Приёмка | 13.09 15:26 | 13.09 15:39 | 13m |
| Отметка владельца EPIC-001 | 13.09 15:39 | 13.09 15:51 | 12m |
| **Всего** | | | **175m** |
