# T-214: Схемы событий части 1: рой, тики, мир, регион, NPC, законы

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-2 (выполнена в период одной команды) |
| Волна | 1 (подволна 1.1, до перезапуска) |
| Исполнитель | developer#3 |
| Размер | M |
| Статус | done (принята tech-lead#2 2026-09-13 после ревью #1 — принять: 0 Critical, 0 Major, 3 Minor, 3 Nit; открытые Minor/Nit — DoD T-230 и `tasks.md` §13) |
| Итераций ревью | 1 |
| Начата / завершена | 2026-09-10 22:49 / — |

> Карточка заведена задним числом при переходе на плагин 0.6.0. Разделы
> «Описание» и «DoD» живут в индексе `tasks.md` — дублировать их сюда значило бы
> завести второй источник истины. Подробности выполнения — в `dev-log.md`,
> замечания и вердикты — в `review.md`, решения — в `journal.md`.

## Описание и DoD
См. раздел T-214 в `../tasks.md`.

## Выполнение (developer)
См. запись T-214 в `../dev-log.md`.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (TEAM-2/code-reviewer#1, по дереву `acd653c`) | 2026-09-13 | 0 | 0 | 3 | 3 | принять |

Краткий итог по журналу 2026-09-13 (строку внёс tech-lead#2 в ревизии 4). Реестр 27 типов верен. 27 невалидных фикстур падают ровно на описанной причине. `mvctl contracts check`, `go test`, `golangci-lint` зелёные. Изменения контрактов v0.8–v0.10 схем не устаревают. Полный раздел ревью вносит ревьюер в `../review.md` в ветке эпика.

Подробности — разделы T-214 в `../review.md`.

## Приёмка (tech-lead)
**Решение: принята.** 2026-09-13, tech-lead#2. Итог: 22 типа части 1 зарегистрированы в нужных топиках, у каждого пара фикстур, невалидная паркуется при чтении; оставшиеся замечания ревью #1 — Minor/Nit без влияния на издателей, разнесены в DoD T-230 и `tasks.md` §13.

### Сверка DoD
| # | Пункт DoD (`tasks.md`, раздел T-214) | Результат | Чем проверено |
|---|---|---|---|
| 1 | `mvctl contracts check` зелёный; тип → топик по §0; `world.law_breach.*` — исключение «без издателя» | выполнен | прогон ниже; сверка топиков и издателей — ревью #1, раздел «Сверка реестра»; `registry.go` после `3c628cb` менял только T-445 (`gm.created`, `f353b38`), типов T-214 это не касается |
| 2 | Пара фикстур на тип; `MV_BUS_VALIDATE_ON_READ` отклоняет невалидную | выполнен | `TestEventFixturesComeInPairs`, `TestInvalidFixturesAreParkedOnRead`, `TestInvalidFixturesPassWhenValidationOnReadIsOff` (`test/fixtures/events_test.go`) зелёные |
| 3 | Слито в `integration/mvp-1` до T-219/T-220 и I1-α | выполнен (ревизия 4) | в `develop` с `1c2ee7e`; T-219/T-220 приняты на этих схемах |
| 4 | Общий DoD: тесты, линтер, dev-log | выполнен | прогоны ниже; запись T-214 в `dev-log.md` (строка 8) |

### Замечания ревью #1 (T-214 и общие)
| Замечание | Статус | Чем |
|---|---|---|
| Mi-3 (а) дубли словарей в `test/fixtures/events_test.go:347–391` | открыт, не блокирует | `tasks.md` §13 п. 2 |
| Mi-3 (б) след шаблона `provider` в контракте | закрыт | T-444: C-06 v1.2 (`contracts.md:518`, история `:526`) |
| Mi-4 валидная фикстура `combat.decided` без `exchange` | открыт, не блокирует | строка DoD T-230 (фикстура по-прежнему без `exchange`, `testdata/fixtures/events/combat.decided.v1.valid.json`) |
| Mi-5: `scope` в payload встречи (§2.3.7) | закрыт | T-444: `api-contracts.md:422`, v0.2.2 (`:14`) |
| Mi-5: Mi-7 T-009, копии формы `{event{id,type?}}` | закрыт решением | T-445: `$defs.EventRef` (`_common.json:55`), встроенные копии оставлены сознательно (`:57`) |
| Mi-5: словарь `tick.aborted.reason` — свободная строка (`tick.aborted.v1.json:16`) | открыт, не блокирует | `tasks.md` §13 п. 3 (T-227) |
| N-2 уровень агента конверта фикстур (`events_test.go:274–281`) | открыт | `tasks.md` §13 п. 2 |
| N-3 `SourceLLM` в потребителях `llm.output*` (`registry.go:131–136`) | открыт | `tasks.md` §13 п. 1 (EPIC-001, `contract-change`) |
| N-4 `encounter.ended` без `killer` | открыт | `tasks.md` §13 п. 4 |
| Открытый вопрос 2 ревью: кодировка `content_hash` | закрыт | T-445: `^sha256:[0-9a-f]{64}$` в `agent.spawned.v1.json:20`, `agent.blueprint_reloaded.v1.json:13`; `TestHashesAreSHA256` (`llmrecord_test.go:245`) |

### Прогоны приёмки (дерево эпика `fd52e31`, go1.26.8 windows/amd64, golangci-lint 2.13.2)
- `go run ./cmd/mvctl contracts check` — `65 types, 8 topics, 58 schema files checked`, код 0.
- `go test -short -count=1 ./shared/contracts/... ./test/fixtures/...` — оба пакета `ok`.
- `golangci-lint run ./shared/contracts/... ./test/...` — `0 issues`.
- Docker, `make up/down`, интеграционные тесты не запускались; стенд `:8888` и `.env` не трогались; код, схемы и фикстуры не менялись; коммитов нет.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 10.09 21:05 | 10.09 22:49 | 1h 43m |
| **Всего** | | | **1h 43m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет. Метки до 2026-09-11 11:56
перепривязаны к настоящему времени коммитов (журнал, 2026-09-11) — это оценка.
