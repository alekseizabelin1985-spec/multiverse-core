# T-255: Хук `cmd/multiverse/fake_contexts.go` (`MV_SWARM_FAKE`) — **совладение с EPIC-001, вход в I1-α

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-1 |
| Волна | 1 |
| Исполнитель | — |
| Статус | done (принята tech-lead#1; итерация 3 сверена по диффу, мутанты Mi-3 красные) |

> Карточка заведена задним числом при переходе на плагин 0.6.0. Описание и DoD
> живут в индексе `../tasks.md` и не дублируются здесь, чтобы не завести второй
> источник истины. Ход работы — `../dev-log.md`, замечания — `../review.md`,
> решения — `journal.md`.

## Описание и DoD
См. раздел T-255 в `../tasks.md`.

## Выполнение (developer)
developer#1, 2026-09-11. Подробности, таблица мутаций и находки — `../dev-log.md`, раздел T-255.

**Что сделано.** `MV_SWARM_FAKE` объявлена в манифесте (`IsBool`, `false`) и добавлена в
`.env.example`. `swarm` по-прежнему регистрируется один раз и на прежнем месте порядка старта, но
фабрикой хука `newSwarm`. Хук читает флаг при построении контекстов процесса, а не в `init`. При
`true` строится `testkit/swarm.FakeContext`, иначе — прежняя заглушка. Нечитаемое значение
отказывает при старте с именем переменной. Новое правило depguard `cmd-multiverse-fake-contexts`
открывает файлу хука ровно `shared/testkit/swarm$`. `FixturesDir` хук не передаёт: у процесса нет
источника каталога фикстур. Мир заглушка узнаёт из `entity.created`, как после `mvctl world init`.

**Изменённые файлы:**
- `shared/env/vars.go`, `.env.example`, `.golangci.yml`;
- `cmd/multiverse/contexts.go`, `cmd/multiverse/fake_contexts.go` (новый),
  `cmd/multiverse/bus_memory.go` (комментарий);
- `cmd/multiverse/main_test.go`, `cmd/multiverse/fake_contexts_test.go` (новый).

**Тесты и проверки:**
- `go build ./... && go vet ./...` — зелёные;
- `go test -short -count=1 ./...` — код 0;
- `go test -tags e2e -count=1 ./test/...` — ok;
- стенды `shared/testkit/gateway` и `shared/testkit/swarm`, `-count=5` — ok;
- процессные тесты `cmd/multiverse`, `-count=20` — ok;
- `golangci-lint run ./...` — 0 issues;
- `mvctl contracts check` и `mvctl env check` (67 переменных) — код 0;
- pre-commit в изолированной копии — восемь хуков Passed. В основном дереве два прогона не
  засчитаны: дерево параллельно меняли другие роли;
- `gitleaks git --staged --redact .` — no leaks found;
- мутанты: Go — 9 из 9 красные, depguard — 5 из 5 красные, контрольный — без правила депгард молчит.

**Что доказано.** Бой до конца и нарративы `entry`/`world_event`/`turn`/`death` через
`process.run` доказаны тестом: хук с флагом, `--bus=memory`, `FakeState` и харнесс на той же шине.
Живой запуск собранного бинарника доказывает только, что `/health` здоров и `swarm` — `FakeContext`.
Боя в бинарнике нет, потому что `state` там заглушка.

**Отклонения от задания.**
- Отдельного `stubContexts` без `swarm` нет. Вместо него один список `platformContexts` в порядке
  старта и `factoryOf`: перенос регистрации в хук сдвинул бы `swarm` в конец порядка старта.
- Смерть проверяется отдельным тестом со своим миром. На фикстурах смерть — дело планировщика:
  идентификаторы процесса не воспроизводимы.

**Предложения в бэклог.** Подтест e2e бинарника с флагом (только `/health`). Источник
идентификаторов на процесс в `shared/eventbus` (через архитектора). Выровнять DoD индекса T-255
(«контекст отсутствует» → «прежняя заглушка»).

### Итерация 2 — замечания ревью #1 (developer#1, 2026-09-11)

Вердикт ревью #1 — «принять» (Minor 2, Nit 4). Оркестратор закрывает всё в этой задаче и
добавляет срок тестам `serve_test.go` (T-410). Подробности — `../dev-log.md`, «T-255 · итерация 2».

| Замечание | Статус | Как |
|---|---|---|
| Mi-1 | исправлено | фейк монтируется обёрткой `flagged`: отказ `Start` начинается с `MV_SWARM_FAKE=true: `; при ненайденном файле добавлено «the stub reads rules/ from the working directory of the process, and the image of the platform has none». Фраза в `.env.example`. Тест `TestAFakeWithoutRulesNamesTheFlag` |
| Mi-2 | исправлено | `t.Setenv(env.SwarmFake.Name(), "false")` в `test/e2e/empty_world_test.go`; с флагом в окружении тест проходит за 3 с (было 62,5 с, FAIL) |
| N-1 | исправлено | `refused.Start` возвращает ошибку как есть; тест проверяет, что контекст назван один раз |
| N-2 | исправлено | одна проверка «смерть и `players_out`», сообщение верно для всех ложных исходов; в комментарии 0,3⁸·(0,7+0,09) ≈ 5,2·10⁻⁵, верхняя граница 0,3⁸ ≈ 6,6·10⁻⁵ |
| N-3 | исправлено сообщением | барьер требует геттера в `testkit/swarm` (владелец EPIC-003, файл занят T-419/T-420); при ошибке `h.Run` без `encounter.started` тест называет вероятную причину: заглушка не узнала волка из `entity.created` до входа |
| N-4 | исправлено | шапка `contexts.go`: `swarm` до T-256 подключается через `newSwarm`, `internal/swarm` приходит строкой в `newSwarm`, а не своим `runtime.Register` |
| Добавка (O3) | исправлено | срок 5 с у `run` в `TestServeClosesTheBusLastWhenTheStartFails`; остальные вызовы `run` в `serve_test.go` уже ограничены (`runUntilStarted` — 30 с, `ReleasesTheSignals…` — отменённый контекст). Под O3 всплыла паника в `witness.Stop` (контекст, не стартовавший, останавливается без реестра) — двойник теперь пишет это в ленту |

**Изменённые файлы итерации:**
- `cmd/multiverse/fake_contexts.go`, `cmd/multiverse/contexts.go`;
- `cmd/multiverse/fake_contexts_test.go`, `cmd/multiverse/serve_test.go`;
- `.env.example`, `test/e2e/empty_world_test.go`.

**Проверки:**
- `go build ./... && go vet ./...` — зелёные;
- `go test -short -count=1 ./...` — код 0;
- `go test -tags e2e -count=1 ./test/...` — ok;
- `golangci-lint run ./...` — 0 issues;
- `mvctl env check` — 67 переменных;
- pre-commit (изолированная копия) — Passed;
- `gitleaks git --staged --redact .` — no leaks found.

**Мутанты (`-overlay`, `-timeout 60s`):**
- Mi-1a (без подсказки о `rules/`), Mi-1b (фейк без обёртки) — красный `TestAFakeWithoutRulesNamesTheFlag`;
- N-1 (двойной префикс) — красный `TestAMalformedFlagRefusesToStart`;
- O3 (снята проверка `StartAll`) — пакет красный за 16 с без паники: `…LastWhenTheStartFails/a_context_fails_to_start`, `AMalformedFlag…`, `AFakeWithoutRules…` по 5 с.

**Зонды:**
- N-2, бессмертный персонаж — выводится новое сообщение;
- N-3, мир без волка — выводится вероятная причина.

### Итерация 3 — замечания ревью #2 (developer#1, 2026-09-11)

Код хука не менялся: добавлены тест и одна фраза. Подробности — `../dev-log.md`,
«T-255 · итерация 3».

| Замечание | Статус | Как |
|---|---|---|
| Mi-3 | исправлено | `TestTheOtherRefusalsOfTheFakeNameTheFlag` держит ветку `default` в `flagged.Start` двумя случаями: нет шины в `Deps`, `RulesPath` на битый YAML. В каждом отказ не nil, начинается с `MV_SWARM_FAKE=true: `, без подсказки про `rules/`, `errors.Unwrap` — ошибка самого фейка. Три мутанта красные |
| N-5 | исправлено | `.env.example`: «в отказе сразу после имени контекста стоит MV_SWARM_FAKE=true» |

**Проверки:**
- build, vet — зелёные;
- `go test -short -count=1 ./cmd/multiverse/ ./test/...` — ok;
- `go test -count=5 ./cmd/multiverse/` — ok;
- `golangci-lint run ./cmd/...` — 0 issues;
- `mvctl env check` — 67 переменных;
- pre-commit (изолированная копия) — Passed;
- `gitleaks git --staged --redact .` — no leaks found.

**Мутанты** (`-overlay`, `-timeout 60s`, весь пакет): M1 (подсказка ко всем ошибкам), M2 (без
префикса), M3 (ошибка проглочена) — все красные, оба случая таблицы.

## Ревью (code-reviewer)

| Итерация | Дата | Ревьюер | Вердикт | Critical | Major | Minor | Nit |
|---|---|---|---|---|---|---|---|
| #1 | 2026-09-11 | code-reviewer#1 | **принять** | 0 | 0 | 2 | 4 |
| #2 | 2026-09-11 | code-reviewer#1 | **принять** | 0 | 0 | 1 | 1 |

Полный разбор — `../review.md`, раздел «T-255 · ревью #1». Все заявления автора подтверждены своими прогонами. Без
флага `/health` и строка старта байт-в-байт совпадают с бинарником «до задачи». Мутанты depguard L1/L3/L5 и свой M8
красные, M7 (`_test.go`) законно зелёный, контроль L6 молчит. Отказ `maybe` громкий, зависших тестов T-255 нет.
Замечания:
- Mi-1 — фейку нужен `rules/` в рабочем каталоге, а отказ без него не называет `MV_SWARM_FAKE`. Это случай контейнера.
- Mi-2 — e2e не изолирован от `MV_SWARM_FAKE` из оболочки: краснеет через 62 с.
- N-1 — двойной префикс `context swarm:` в отказе.
- N-2 — пояснение «волк промахнулся» печатается не в том случае. Точная вероятность ложного падения ≈ 5,2·10⁻⁵.
- N-3 — при позднем чтении фактов State тест краснеет за 3 с, но сообщение не называет причину.
- N-4 — шапка `contexts.go` противоречит хуку.

Открытый вопрос к tech-lead#1: DoD п. 4 (бой в собранном бинарнике) до EPIC-002 закрыт только тестом через `process.run`.

Ревью #2 — `../review.md`, раздел «T-255 · ревью #2». Все шесть замечаний ревью #1 и добавка O3 закрыты, каждое повторено
своим прогоном. Mi-1: живой отказ из пустого каталога называет `MV_SWARM_FAKE=true`. Mi-2: e2e с флагом — `PASS` за 2,94 с.
N-1: `context swarm` встречается один раз. Зонды P-N2 и P-N3 выводят новые сообщения. O3 — красный за 18 с, без паники.
Правка `witness` тесты T-410 не ослабила: R1, R2, R3, M1, M2 и свой W1 — красные. Вызовов процесса без срока в
`cmd/multiverse` и `test/e2e` нет. Без флага `/health` и строка старта совпадают с бинарником «до задачи» побайтно.
Замечания:
- Mi-3 — ветка `default` в `flagged.Start` не закреплена тестом: мутанты «подсказка к любой ошибке», «без префикса»,
  «ошибка проглочена» выживают. Код верен (зонд).
- N-5 — `.env.example`: отказ не «начинается» с флага, флаг стоит сразу после `start context swarm:`.

## Приёмка (tech-lead)

**Решение:** **принята** · 2026-09-11 · tech-lead#1 (владелец пути `cmd/multiverse`, совладение с EPIC-001). Статус `done`
ставит оркестратор, когда заполнит «Учёт времени».

**Итог:** хук выбирает `swarm` по `MV_SWARM_FAKE` на всех трёх ветвях, без флага процесс прежний. Бой и нарратив I1-α
доказаны тестом через `process.run`, отказы фейка под флагом закреплены тестом.

**Итерация 3 без ревью #3 (решение оркестратора) сверена по диффу.** Блобы `fake_contexts.go` (`dc88f18b`),
`contexts.go`, `serve_test.go`, `empty_world_test.go` совпадают с зафиксированными в ревью #2. Изменились только:
- `fake_contexts_test.go` (`9fbcb033 → 976e03a1`): +48 строк, импорт `os` и тест `TestTheOtherRefusalsOfTheFakeNameTheFlag`;
- `.env.example` (`eb222601 → c956c590`): одна фраза N-5.

Поведение кода не менялось.

**Мутанты Mi-3 — свои**, `go test -overlay` из каталога `mktemp -d`, `-timeout 60s`, весь пакет `cmd/multiverse`:

| Мутант | Итог |
|---|---|
| B0 (база) | `ok` |
| M1 — подсказка про `rules/` к любой ошибке | красный: оба случая, строка 189 |
| M2 — без префикса `MV_SWARM_FAKE=true: ` | красный: оба случая, строки 186 и 192 |
| M3 — `return nil` | красный: оба случая, строка 183 «was swallowed» |

Все три красные по проверкам теста, а не по сборке. Хэш хука до и после совпал, каталог удалён.

**DoD** (индекс, выровнен):
- три ветви флага с тестом на каждую — выполнено;
- импорт `shared/testkit/swarm` только в файле хука под узким правилом `cmd-multiverse-fake-contexts` — выполнено; второй
  импорт — `bus_memory.go` T-410, ADR-001 доп. 2026-09-11 п. 2; `testing`/testify/testcontainers в зависимостях бинарника нет;
- запрос на depguard и `.env.example` принят владельцем пути — эта приёмка;
- бой — тестом через `process.run`, живой бинарник — только `/health` фейка — выполнено.

**Свои прогоны:**
- `go build ./... && go vet ./...` — зелёные;
- `go test -short -count=1 ./...` — 27 пакетов `ok`;
- `-tags e2e ./test/...` — `ok`;
- `-count=5 ./cmd/multiverse/` — `ok`;
- `golangci-lint run ./...` — 0 issues;
- `mvctl contracts check` — 65/8/58, `env check` — 67;
- `gitleaks git --staged --redact .` — no leaks found.

**Живой запуск** собранного бинарника: `--contexts=all --bus=memory`, 127.0.0.1:18861/18862, рабочий каталог — корень дерева.
- Без флага — `/health` `ok`, у `swarm` деталей нет.
- С `true` — у `swarm` `stub: testkit/swarm.FakeContext`, `removed_by: T-256`.

`multiverse health` — 0 в обоих случаях. Процессы остановлены после сверки порта, пути exe и командной строки. `-race`
не запускался (T-401).

**Совладение:** правки в `.golangci.yml`, `.env.example`, `shared/env/vars.go`, `cmd/multiverse/{contexts,main_test,
bus_memory,serve_test}.go` приняты владельцем пути.

**Остаток** — не блокирует задачу, в проход по документам:
- `plan/ownership.md` §1, строка хука: «в индексе файла нет» устарело; «R13/EPIC-001» — это T-256.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 11.09 13:51 | 11.09 14:37 | 46m |
| Ревью | 11.09 14:37 | 11.09 14:55 | 18m |
| Разработка 2 | 11.09 14:55 | 11.09 15:06 | 11m |
| Ревью 2 | 11.09 15:06 | 11.09 15:19 | 12m |
| Разработка 3 | 11.09 15:19 | 11.09 15:22 | 3m |
| Приёмка | 11.09 15:22 | 11.09 15:29 | 6m |
| **Всего** | | | **1h 38m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет. Метки до 2026-09-11 11:56
перепривязаны к настоящему времени коммитов (журнал, 2026-09-11) — это оценка.
