# T-424: R10d · Сверка встреч с State после догона

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-1 |
| Волна | 1 · подволна 1.15 |
| Исполнитель | developer#2 |
| Ревьюер | — (назначает оркестратор) |
| Размер | M |
| Статус | todo |
| Зависит от | T-237 (фаза догона), T-236 (восстановление экземпляров), T-232 (построитель `encounter.started`), T-422, T-423 (построители `encounter.ended`), T-417 |
| Связи | US-011; NFR-013/014, NFR-061; C-01 v1.4–v1.5; C-05 v1.4–v1.5 п. 4–5; C-14 v1.2 (б); ADR-026 п. 10; ADR-027 и его «Уточнение исполнения» п. 3; КД §4.3; design §3.2 R10 |
| Создана / начата / завершена | 2026-09-11 17:28 / — / — |

> Вынесено из T-237 при нарезке T-230 (tech-lead#2, 2026-09-11; правки T-425 внесены тогда же).
> Текст совпадает с разделом T-424 в `../tasks.md`; при расхождении прав индекс (правило эпика).
> Общий DoD эпика — `../tasks.md` §1.

## Описание
Файл — `internal/swarm/reconcile.go`. Шаг выполняется сразу после фазы догона T-237: догон
закончился, публикаций в live ещё не было. В `swarm.go` добавляется одна строка вызова.
`Health()` не трогается — его в той же подволне правит T-239.

Шаг находит встречи, у которых факт State есть, а события жизненного цикла нет, и публикует это
событие с тем id, который назван в сущности (C-14 v1.2 (б), C-05 v1.4 п. 5, ADR-026 п. 10).

- **Кандидаты.** Полного чтения журнала с начала нет. Кандидаты берутся из двух источников:
  - сущности встреч, чей факт создания или закрытия лежит в прочитанном при догоне диапазоне
    журнала;
  - экземпляры, восстановленные из снапшота с событием, которое построено, но ещё не
    опубликовано.
- **Проверка «событие есть».** Id события — в сущности встречи: `opened_by_event_id` или
  `closed_by_event_id`. Событие есть, если этот id встречался в диапазоне догона или известен из
  снапшота.
- **Сборка события с тем же id.** Событие перестраивается от той же причины, что в живом режиме
  (ADR-027 «Уточнение исполнения» п. 3, T-425). Причину находят в журнале по цепочке: факт →
  предложение по `proposal_id` → его `causation_id`. Чья это причина:

  | Событие | Причина |
  |---|---|
  | `encounter.started` | событие, по которому GM открыл встречу: вход игрока или `tick.fired` |
  | `encounter.ended` по обмену | действие игрока (соло); для группы — `round.closed` (I2, T-246) |
  | `encounter.ended` по чужому факту | этот факт: NPC стал терминальным (Б1) или `abandoned` последнего игрока |

  Цепочка годится, потому что `causation_id` пакета — действие или `round.closed`, а не
  `combat.decided` (`api-contracts.md` §2.3.6, T-425). Событие строится тем же построителем, что в
  живом режиме:
  - `encounter.started` — построитель GM региона (T-232);
  - `encounter.ended` — построитель агента встречи (T-422, T-423).

  Опция — `WithCauseID(<id встречи>)`. Событие не публикуется, а в лог идёт `Error`, если причины
  нет в журнале или полученный id не равен id в сущности: другой id хуже пропуска.
- **Интерфейс.** Роли подключаются к шагу через необязательный интерфейс `Reconciler` в
  `internal/swarm`. Он проверяется утверждением типа, поэтому `Behaviour` (T-223) не меняется.
  Реализации — в новых файлах `roles/region_gm_reconcile.go` и `roles/encounter_reconcile.go`.

**Почему отдельная задача, а не T-237 или часть T-230.**
- `encounter.started` при восстановлении некому дописать внутри агента встречи: рой поднимает
  агента по этому самому событию (КД §4.1). Живой издатель — GM региона (T-232).
- Сверке нужны построители двух ролей и фаза догона одновременно.
- Внутри T-237 она лежала бы на критическом пути (T-228 → T-237 → T-242). Отдельно она стоит вне
  его.

## Definition of Done
- [ ] *(перенесено из T-237)* Восстановившись, рой сверяет встречи с State **после** фазы догона,
  и догон по-прежнему ничего не публикует. Два случая:
  - сущность встречи есть, а `encounter.started` с id из причины в журнале нет → публикуется
    `encounter.started`;
  - встреча `resolved` с `closed_by_event_id`, а события с этим id нет → публикуется
    `encounter.ended` с тем же id.

  Тесты: оба случая. Если событие успело уйти до падения, второго не публикуется: id в журнале
  есть. Если оно ушло дважды, потребители гасят его по id.
- [ ] Id события, опубликованного при сверке, равен id, названному в сущности встречи. Причина не
  найдена в журнале или id не совпал → публикаций 0, `Error`. Тест на обе ветки. *(уточнение
  нарезки: как выполнить «с тем же id», не заводя второго источника причины)*
- [ ] **(T-425, 2026-09-11; ADR-027 «Уточнение исполнения» п. 3)** Перестроение — от той же
  причины, что в живом режиме. Тесты, по одному на строку таблицы:
  - `encounter.started`, открытая по входу;
  - `encounter.started`, открытая по `tick.fired`;
  - `encounter.ended` по обмену;
  - `encounter.ended` по чужому факту.

  Строка `round.closed` проверяется в I2 (T-246).
- [ ] Тест T-237 «догон не публикует событий (счётчик публикаций = 0)» остаётся зелёным;
  публикации сверки идут после конца догона.
- [ ] Общий DoD эпика (§1): тесты и линтер зелёные, `goleak`, `dev-log.md` заполнен.

## Выполнение (developer)
Задача не начата.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|

Не проводилось.

## Приёмка (tech-lead)
Не принята.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| — | — | — | задача ещё не запускалась |
