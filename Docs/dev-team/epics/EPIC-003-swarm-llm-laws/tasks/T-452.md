# T-452: `FakeEncounter` — павшему персонажу только `hp` и `status`

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-2 |
| Волна | вне подволн §6.1, до T-062 EPIC-002 (из ревью T-053; `tasks.md` §12) |
| Размер | XS |
| Исполнитель | developer#2 (Opus) |
| Статус | done (принята tech-lead#2 после ревью #1 (Nit закрыты при приёмке)) |
| Ветка | `task/T-452-fake-encounter-fallen-character` от `epic/EPIC-003-swarm-llm-laws` (`b18ecd6`) |
| Итераций ревью | 1 |
| Начата / завершена | 2026-09-13 13:31 / 2026-09-13 14:04 |
| Метка | нет: контракт, схемы и таблица владения не меняются, двойник приводится к ним |

## Описание и DoD
Главный источник — раздел «### T-452» в `../tasks.md`. Основание — расхождение Р3 карточки T-053 (EPIC-002) и решение system-architect#1 от 2026-09-13 («Р3 — дефект двойника»). Кратко:
- Павшему персонажу — только `hp` и `status`; у NPC `died_at`/`killed_by` остаются.
- Тест: пакет со смертью персонажа целиком проходит `contracts.OwnershipRules()`.
- Устаревшие комментарии про T-053 — нейтрально: механика EPIC-002.
- Не делать: `ChangesFor` и форму `changed[]` для `remove` (после T-053/T-448).

## Выполнение (developer)
developer#2 (Opus), TEAM-2, папка `.worktrees/T-452`. Не закоммичено: по промпту коммиты запрещены, коммит делает оркестратор.

### Итог по пунктам DoD

| # | Пункт | Что сделано | Чем проверено | Мутант → результат |
|---|---|---|---|---|
| 1 | Павшему персонажу — только `hp`, `status` | В `wound` введён признак `npcFell := dead && target.Type == entity.TypeNPC`. `status=dead` пишется любому павшему, `died_at` и `killed_by` — только павшему NPC. Выход перед трофеем — по тому же `npcFell`, поведение трофея не изменилось | `TestTheCharacterDies`: `status=dead` есть, операций над `died_at` и `killed_by` у персонажа нет (`assertNoOp`); прежнее утверждение `killed_by = wolf` у персонажа снято — оно закрепляло дефект | M1, M2, M3, M5 — **красные** |
| 2 | У NPC — как было | Операции NPC прежние: `hp`, `status`, `died_at`, `killed_by`, `loot_claimed_by` | `TestTheWolfDies`: к `killed_by` и `loot_claimed_by` добавлена проверка, что `died_at` задан непустой строкой (раньше `died_at` NPC тестом не закреплялся); лог `TestEveryProposalPassesTheOwnershipTable`: `update npc … paths=[died_at hp killed_by loot_claimed_by status]` | M6, M7 — **красные** |
| 3 | Тест по таблице владения | `TestAFallenCharacterIsProposedOnlyWhatTheOwnershipTableAllows`, 2 подтеста: персонаж с 1 HP против волка с 40 HP падает в обмене (`cause=combat`) и от свободной атаки после неудачного побега (`cause=flee`). Каждое предложение `testkit/swarm` проверяется `ownershipAllows` — той же проверкой по `contracts.OwnershipRules()`, что у `TestEveryProposalPassesTheOwnershipTable`. Набор изменений павшего персонажа — ровно `[hp status]`; предложение со смертью ровно одно, и `cause` соответствует пути | тест зелёный; без правки (M1) красный на обоих подтестах | M1–M5 — **красные**; M4 (лишний, но разрешённый таблицей путь `position`) ловит только проверка точного набора |
| 4 | Комментарии про T-053 | `fake_encounter.go:132-133` — «*mechanics.Rules, the mechanics of EPIC-002, and testkit/mechanics.FixedMechanics»; `:156` — «FixedMechanics answers from a table, the mechanics of EPIC-002 (Resolve) by the rules». Других упоминаний T-053/`ChangesFor` в `shared/testkit/swarm`, `cmd/multiverse`, `test/e2e` нет | `grep` | — |
| 5 | Потребители | `shared/testkit/swarm`, `cmd/multiverse` (`fake_contexts_test.go`), `test/e2e` — зелёные. `died_at`/`killed_by` персонажа никто не читал: вне `shared/entity` и `testkit/state` (`corpsePaths`, общий для любого типа) упоминаний нет | прогоны ниже | — |

Функция проверки таблицы в `shared/contracts` не экспортируется (есть только `OwnershipRules()`), поэтому тест использует уже существующий помощник пакета `ownershipAllows`. Помощник сбора путей вынесен в `pathsOf` и переиспользован в `proposalsOf`.

### Мутанты
Копия рабочей папки в scratch (`git ls-files -co --exclude-standard` без `services/`, `Docs/`, `.claude/`, `.qwen/`; каталог с точным именем), не `-overlay`. Базовый прогон копии `go test -short -count=1 ./shared/testkit/swarm/` зелёный. Мутации вносил скрипт точной заменой (`assert count == 1`), каждая показана диффом; после каждой файл восстановлен и сверен побайтно. В конце `fake_encounter.go` копии сверен `cmp` с рабочей папкой — равен. Копия и скрипт удалены по сохранённым точным путям.

| # | Мутант (`fake_encounter.go`, `wound`) | Результат |
|---|---|---|
| M0 | контрольный: `func broken( {` перед `wound` | **красный**: `fake_encounter.go:1179:14: syntax error` |
| M1 | возврат дефекта: `if npcFell` → `if dead` у `died_at`/`killed_by` | **красный**: `TestTheCharacterDies`, оба подтеста нового теста |
| M2 | `killed_by` и персонажу | **красный**: `TestTheCharacterDies`, оба подтеста |
| M3 | `died_at` и персонажу | **красный**: `TestTheCharacterDies`, оба подтеста |
| M4 | павшему персонажу лишний разрешённый путь `position` | **красный**: оба подтеста нового теста (точный набор путей) |
| M5 | `status=dead` только NPC | **красный**: `TestTheCharacterDies`, оба подтеста |
| M6 | у NPC нет `died_at` | **красный**: `TestTheWolfDies` |
| M7 | у NPC нет `killed_by` | **красный**: `TestTheWolfDies` |

### Изменённые файлы
- `shared/testkit/swarm/fake_encounter.go`: `wound` — `died_at`/`killed_by` только павшему NPC; комментарий `wound`; комментарии `Mechanics` и `EncounterConfig.Mechanics` без T-053.
- `shared/testkit/swarm/fake_encounter_test.go`: `TestAFallenCharacterIsProposedOnlyWhatTheOwnershipTableAllows`; `TestTheCharacterDies` — отсутствие `died_at`/`killed_by`; `TestTheWolfDies` — `died_at` у NPC; помощники `assertNoOp`, `setsStatusDead`, `pathsOf`.
- `Docs/dev-team/epics/EPIC-003-swarm-llm-laws/tasks.md` (раздел T-452), `tasks/T-452.md` (эта карточка), `dev-log.md` (запись в конце).

### Прогоны (go1.26.8 windows/amd64, golangci-lint 2.13.2)
- `go build ./... && go vet ./...` — 0; `go vet -tags e2e ./test/e2e/...` — 0.
- `gofmt -l shared/testkit/swarm` — пусто.
- `go test -short -count=1 ./...` — 29 пакетов ok, 0 FAIL.
- `go test -tags e2e -count=1 ./test/e2e/...` — ok; `go test -short -count=1 ./cmd/multiverse/` — ok.
- `golangci-lint run ./...` — 0 issues.
- `make test` — exit 0 (без `-race`: нет cgo; coverage-gate: `internal/mechanics` 94,9 %, `internal/llm` 99,3 %).
- Docker, `make up/down`, интеграционные тесты не запускались; стенд `:8888` не трогался; `.env` не открывался.

### Отклонения от задачи
- Нет. Сверх формулировки `TestTheWolfDies` закрепляет `died_at` у NPC: иначе пункт «у NPC — как сейчас» держался бы только на `killed_by`.
- Раздел T-452 добавлен в самый конец `tasks.md`, после §11 «Бэклог двойников», как указано в промпте.

### Предложения в бэклог
1. `shared/contracts`: экспортировать проверку предложения по таблице (`Allows(level, entityType, cause string, paths []string, create bool) bool`) — сейчас её дублируют `ownershipAllows`/`covers`/`coversPath` в тестах двойника, а настоящий State (T-056) напишет третью копию. Владелец строк — system-architect (`contract-change`); не блокирует.
2. `FakeState` (`shared/testkit/state`) не проверяет таблицу владения — поэтому Р3 молчал. Проверка `level_violation` в двойнике State по `OwnershipRules()` поймала бы такие дефекты во всех потребителях сразу, а не только в тестах `FakeEncounter`. Решение за владельцем двойника State (EPIC-002); по T-017 `level_violation` сознательно вне матрицы v0.
3. Устаревшие упоминания «until EPIC-002 … T-053/T-054» остаются вне `shared/testkit/swarm`: `shared/testkit/mechanics/consumer_test.go:95`, `fixed.go:336`, `shared/testkit/state/state.go:119,176`, `apply_test.go:483-484`, `internal/mechanics/*`. Их файлы — владение EPIC-002, часть снимает сама T-053; в T-452 не трогал.

## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Critical / Major / Minor / Nit | Запись |
|---|---|---|---|---|---|
| 1 | 2026-09-13 | code-reviewer#2 (TEAM-2) | **принять** | 0 / 0 / 0 / 3 | `../review.md`, «T-452 · ревью #1»: правка `wound` точная, ветки NPC и трофея прежние; тест проверяет весь поток предложений по `OwnershipRules()` и точный набор `[hp status]`; мутанты ревьюера R0–R4 (контрольный первым) красные; build/vet, `-short`, e2e, golangci-lint зелёные. Nit: рваный абзац комментария `fake_encounter.go:132-134`, место раздела T-452 в `tasks.md` (после §11), дублирование цикла проверки таблицы в тестах |

## Приёмка (tech-lead)
**Решение: принята.** 2026-09-13, tech-lead#2. Итог: павшему персонажу `FakeEncounter` предлагает ровно `[hp status]`, у NPC всё прежнее. Весь поток предложений боя со смертью персонажа проходит `contracts.OwnershipRules()`, так что T-062 EPIC-002 на настоящем State не упрётся в `level_violation` из-за двойника.

### Сверка DoD
| # | Пункт DoD (`tasks.md` §12) | Результат | Чем проверено |
|---|---|---|---|
| 1 | Павшему персонажу — только `hp` и `status`, у NPC — как было | выполнен | Дифф `wound`: `npcFell := dead && target.Type == entity.TypeNPC`, `status` получает любой павший, `died_at`/`killed_by` и трофей — только NPC. Сверено с `shared/contracts/ownership.go:82-93`: строка `task`/`player` — `hp, status, position, inventory, encounter_id`, строка `task`/`npc` — `hp, status, died_at, killed_by, loot_claimed_by`. Сверено с `analysis/data-model.md` §3.3/§3.4: `died_at` и `killed_by` есть только у NPC |
| 2 | Тест по таблице владения, оба пути гибели | выполнен | `TestAFallenCharacterIsProposedOnlyWhatTheOwnershipTableAllows` (подтесты `bitten in an exchange` и `struck fleeing`) зелёный. `TestTheCharacterDies` (нет `died_at`/`killed_by`) и `TestTheWolfDies` (непустой `died_at`) тоже зелёные |
| 3 | Комментарии про T-053 нейтральны | выполнен | `fake_encounter.go:131-134`, `:155-156`. Абзац переложен по ширине при приёмке (N-1) |
| 4 | Не делать `ChangesFor`, `changed[]` для `remove`, правки `ownership.go`/схем | соблюдено | `git diff --stat`: код — только `shared/testkit/swarm/fake_encounter{,_test}.go` |
| 5 | Мутанты: контрольный первым, остальные красные | выполнен | M0–M7 автора и R0–R4 ревьюера. Мои A0–A3 — ниже |
| 6 | Прогоны и dev-log | выполнен | прогоны ниже. Запись `<!-- dev-log T-452 -->` в `dev-log.md` соответствует диффу |

### Nit ревью #1 — внесены при приёмке
- **N-1** (`shared/testkit/swarm/fake_encounter.go:131-134`). Абзац doc-комментария `Mechanics` переложен до обычной ширины, текст не менялся.
- **N-2** (`tasks.md`). Раздел T-452 вынесен из §11 «Бэклог двойников (без номера)» в новый «§12. Задачи из ревью других эпиков», в конце файла. T-452 добавлена в перечень номеров эпика (шапка «(ревизия 4)», строка «Номера — только из общего счётчика»). «Волна 1» заменена на «вне подволн §6.1, до T-062 EPIC-002», так же в шапке карточки. В `tasks.md` добавлена строка changelog 0.3.1, версия в шапке — 0.3.1.
- **N-3** (`fake_encounter_test.go`). Правка на несколько строк, поэтому внесена, а не ушла в бэклог. Помощник `checkOwnership(t, ev) []proposal` (`:1850`) проверяет каждое предложение события через `ownershipAllows`: `DENY` — `t.Errorf`, `OK` — `t.Logf`. Возвращает все предложения. Им пользуются оба теста: `TestEveryProposalPassesTheOwnershipTable` (`:421`, по возврату собирает `kinds`) и новый тест (`:679`). Текст `DENY` у обоих теперь одинаковый, с `create=`. Фильтр по источнику `testkit/swarm` остался у вызывающих.

### Мутанты приёмки (на N-3)
Копия дерева в scratch (`git ls-files -co --exclude-standard` без `services/`, `Docs/`, `.claude/`, `.qwen/`, `.env*`), без `-overlay`. `go list` показал каталог копии, базовый прогон копии зелёный. Каждая замена — с проверкой «ровно одно вхождение» и показом диффа. После каждого мутанта файлы восстановлены из рабочей папки и сверены `cmp`. Копия и список файлов удалены по точным путям.

| # | Мутант | Результат |
|---|---|---|
| A0 | контрольный: `func broken( {` перед `checkOwnership` | **красный**: `[setup failed]` |
| A1 | `npcFell := dead` и в новом тесте выключена проверка точного набора (`false && !slices.Equal`) | **красный**: оба подтеста по `DENY update player … paths=[died_at hp killed_by status]` из `checkOwnership` (`:679`); `TestTheCharacterDies` |
| A2 | NPC при ране получает `position` | **красный**: `TestEveryProposalPassesTheOwnershipTable` (`DENY update npc … [hp position]`, `:421`) и подтест `bitten in an exchange` |
| A3 | `checkOwnership` возвращает `nil` | **красный**: `TestEveryProposalPassesTheOwnershipTable` — все пять видов предложений «не произведены» |

### Прогоны приёмки (go1.26.8 windows/amd64, golangci-lint 2.13.2, после правок N-1 и N-3)
- `gofmt -l shared/testkit/swarm` — пусто. `go build ./... && go vet ./...` — 0. `go vet -tags e2e ./test/e2e/...` — 0.
- `go test -short -count=1 ./shared/testkit/... ./cmd/multiverse/...` — 8 пакетов ok.
- `go test -tags e2e -count=1 ./test/e2e/...`. Первый прогон — FAIL в `lifecycle_test.go:36` / `empty_world_test.go:171`: пробник `/health` запущенного процесса не дождался ответа за 15 s, connection refused. Второй и третий прогоны — ok (32,3 s и 14,7 s; `-v`: `TestTheEmptyWorldAnswersTheHealthProbe`, `TestTheProbeGivesUpOnAProcessThatExited`, `TestTheProcessStopsCleanlyOnASignal`, `TestStubsV0RunTheScenarioTogether` — PASS). Эти тесты проверяют старт бинарника и `FakeEncounter` не затрагивают. Первый прогон шёл со сборкой бинарника при параллельной нагрузке машины. Считаю это нестабильностью по времени старта, не дефектом T-452.
- `golangci-lint run ./shared/testkit/...` — 0 issues.
- Docker, `make up/down`, интеграционные тесты не запускались; стенд `:8888` и `.env` не трогались; коммитов нет.

### Бэклог (из автора, ревью и приёмки; номера выдаёт оркестратор)
1. **`contracts.Allows` — экспортная проверка предложения по таблице владения** (system-architect, `contract-change`, EPIC-001). Сейчас логику дублируют `ownershipAllows`/`covers`/`coversPath` в тестах двойника, а настоящий State (T-056) написал бы третью копию. Размер S, не блокирует.
2. **Проверка `level_violation` в `FakeState` по `OwnershipRules()`** (EPIC-002, до или вместе с T-056). Двойник State таблицу не проверяет, поэтому Р3 молчал. Проверка поймала бы такой дефект у любого потребителя. Решение — за владельцем двойника (по T-017 `level_violation` сознательно вне матрицы v0).
3. **`FakeEncounter`: сборку пакета вручную заменить вызовом `ChangesFor`** (EPIC-003) — после слияния T-053 в `develop` (из приёмки T-053). Вместе с этим — форма `changed[]` для `remove` после T-448. Это пункт «Не делать» T-452.
4. **Устаревшие упоминания «until EPIC-002 … T-053/T-054»** вне `shared/testkit/swarm`: `shared/testkit/mechanics/{consumer_test.go,fixed.go}`, `shared/testkit/state/{state.go,apply_test.go}`, `internal/mechanics/*` (EPIC-002, п. 3 автора). Часть снимет сама T-053. Передать tech-lead EPIC-002.
5. **Нестабильность e2e на старте процесса.** 15 s ожидания `/health` в `test/e2e` (`lifecycle_test.go:36`) не хватило один раз из трёх под нагрузкой. Если повторится — владельцу `test/e2e` поднять таймаут или повторять пробу. Пока — наблюдение.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 13:31 | 13.09 13:45 | 13m |
| Ревью | 13.09 13:45 | 13.09 13:53 | 8m |
| Приёмка | 13.09 13:53 | 13.09 14:04 | 10m |
| **Всего** | | | **33m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
