# T-215: Схемы событий части 2: LLM и нарратив

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-2 (выполнена в период одной команды) |
| Волна | 1 (подволна 1.2, до перезапуска) |
| Исполнитель | developer#3 |
| Размер | S |
| Статус | done (принята tech-lead#2 2026-09-13 после ревью #1 — вернуть: Ma-1 и Mi-1 закрыты T-445, итерация T-215 не понадобилась; Mi-2 — `tasks.md` §13) |
| Итераций ревью | 1 |
| Начата / завершена | 2026-09-10 22:49 / 2026-09-11 01:32 |

> Карточка заведена задним числом при переходе на плагин 0.6.0. Разделы
> «Описание» и «DoD» живут в индексе `tasks.md` — дублировать их сюда значило бы
> завести второй источник истины. Подробности выполнения — в `dev-log.md`,
> замечания и вердикты — в `review.md`, решения — в `journal.md`.

## Описание и DoD
См. раздел T-215 в `../tasks.md`.

## Выполнение (developer)
См. запись T-215 в `../dev-log.md`.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (TEAM-2/code-reviewer#1, по дереву `acd653c`) | 2026-09-13 | 0 | 1 | 3 | 2 | вернуть |

Краткий итог по журналу 2026-09-13 (строку внёс tech-lead#2 в ревизии 4). **Ma-1:** в `llm.output.v1.json` не выражена условная обязательность — `response_raw` при `valid|partially_rejected|invalid`, `filter` и `error{}` при `error`. Решение 4 system-architect#1 — условия в схеме; делает **T-445** (EPIC-001), итерация T-215 в ветке эпика не нужна. Minor и Nit (Mi-1 `budget_exceeded`/`unknown_entity` в `rejected`, Mi-2 шаблон `provider` без теста, Mi-3 дубли проверок словарей, Mi-4 фикстура `combat.decided` без `exchange`, Mi-5 остальное из ревью T-009) разбираются при приёмке: закрыты T-445 или уходят в бэклог. Полный раздел ревью вносит ревьюер в `../review.md` в ветке эпика.

Подробности — разделы T-215 в `../review.md`.

## Приёмка (tech-lead)
**Решение: принята.** 2026-09-13, tech-lead#2. Итог: Major ревью #1 (условная обязательность записи модели) закрыт схемой и таблицей тестов T-445, словари статусов и причин закреплены; открытым остаётся только Minor Mi-2 — тест на уже работающий шаблон `provider`, он ушёл в `tasks.md` §13.

Итерация T-215 не открывалась: решение 4 system-architect#1 передало условия схемы T-445 (журнал 2026-09-13), T-445 принята tech-lead#1 и пришла в эпик синхронизацией `fd52e31`.

### Сверка DoD
| # | Пункт DoD (`tasks.md`, раздел T-215) | Результат | Чем проверено |
|---|---|---|---|
| 1 | `reason` enum = `guardian/reasons.go` (до пакета — TODO-тест) | выполнен | `TestRejectionReasonsMatchTheGuardianPackage` (`test/fixtures/events_test.go:405`) пропускается до T-217; включение — DoD T-217; хрупкость (N-1) — новая строка DoD T-217 |
| 2 | `validation_status` — ровно 6 значений; отрицательный тест на `rejected_*`/`budget_exceeded`; `reasons[]` опционален | выполнен | `llm.output.v1.json:39–51`; `TestValidationStatusEnum`, `TestBlockVPayloadRejects` (`blockv_test.go:276–289`), `TestRejectionReasonEnum` |
| 3 | `config.cloud_enabled` — схема без изменений; семантика C-06 v1.1 передана T-212 | выполнен | шаблон `provider` узаконен C-06 v1.2 (`contracts.md:518`, «схема без изменений» — `:526`); семантика — в разделе T-212 |
| 4 | Слито в `integration/mvp-1` | выполнен (ревизия 4) | в `develop` с `1c2ee7e` |
| 5 | Схема соответствует C-07 (общий DoD) | выполнен | C-07 v1.3 (`contracts.md:559`) ↔ `llm.output.v1.json:110–176`, `llm.output.rejected.v1.json:59–89`; прогоны ниже |

### Замечания ревью #1 (T-215)
| Замечание | Статус | Чем |
|---|---|---|
| Ma-1 условная обязательность `response_raw`/`filter`/`error` | закрыт | T-445: шесть `if/then` (`llm.output.v1.json:110–176`) — строже варианта (а) ревью: `filter.status` по статусу, `error{}` только при `error`; таблица C-07 v1.3 построчно — `TestLLMOutputFieldsFollowTheStatus` (`llmrecord_test.go:106–174`). Условие «`filter` при `invalid` по стадии» — свойство-тест T-211 (DoD есть) |
| Mi-1 `budget_exceeded` и `unknown_entity` в `llm.output.rejected` | закрыт | T-445: `llm.output.rejected.v1.json:59–89` (`budget` без `llm_output` и наоборот; `unknown_entity` — `element` и ровно одно из `entity`/`background_ref`); `TestLLMOutputRejectedFieldsFollowTheReason` (`llmrecord_test.go:201–240`) |
| Mi-2 шаблон `provider` без теста | **открыт**, не блокирует | шаблон в схемах (`llm.output.v1.json:10`, `config.cloud_enabled.v1.json:12`) и в C-06 v1.2; проба приёмки через `contracts.Validate` (временная программа в scratch, удалена по точному пути): `openai_compat`, `ollama`, `anthropic`, `recorded`, `fake` приняты, `http://127.0.0.1:8888`, `openai_compat?key=x`, `OpenAI` отвергнуты. Теста нет: `git grep '"provider"'` по `*_test.go` и `testdata/` — только допустимые имена. Запись «Статус замечаний» в `review.md` («`pattern` у `provider` с тестами» — T-445) неточна: T-445 тестов шаблона не добавляла. → `tasks.md` §13 п. 1 |
| Mi-3 (а) дубли словарей; (б) след шаблона | (а) открыт; (б) закрыт | (а) `tasks.md` §13 п. 2; (б) T-444, C-06 v1.2 |
| Mi-5 (общий с T-214) | см. карточку T-214 | `scope` и `EventRef` закрыты, `tick.aborted.reason` — §13 п. 3 |
| N-1 тест-заглушка берёт любой литерал `reasons.go` | передан | строка DoD T-217 |
| N-5 «девять» причин деградации в dev-log T-215 §2 | не правится | запись dev-log — история исполнителя; в КД и схеме восемь |
| Открытый вопрос 1 ревью: имя третьего поля `config.cloud_enabled` | закрыт | T-444: C-06 v1.2 — `external_players_ack` (`contracts.md:519`); схема `config.cloud_enabled.v1.json:15` |
| Открытый вопрос 3 ревью: пометка ADR-017 доп. 1 в ADR-016 | закрыт | T-409: `ADR-016-llm-output-parser-language-filter.md:36` |
| Бэклог ревью п. 5: `llm.output.error.message` через редактор секретов | передан | строка DoD T-211 |

### Прогоны приёмки (дерево эпика `fd52e31`, go1.26.8 windows/amd64, golangci-lint 2.13.2)
- `go run ./cmd/mvctl contracts check` — `65 types, 8 topics, 58 schema files checked`, код 0.
- `go test -short -count=1 ./shared/contracts/... ./test/fixtures/...` — оба пакета `ok`.
- `golangci-lint run ./shared/contracts/... ./test/...` — `0 issues`.
- Docker, `make up/down`, интеграционные тесты не запускались; стенд `:8888` и `.env` не трогались; код, схемы и фикстуры не менялись; коммитов нет.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 10.09 22:49 | 10.09 23:49 | 59m |
| **Всего** | | | **59m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет. Метки до 2026-09-11 11:56
перепривязаны к настоящему времени коммитов (журнал, 2026-09-11) — это оценка.
