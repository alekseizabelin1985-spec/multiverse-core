# T-459: Документы EPIC-003 под C-07 v1.5 и решения приёмок T-210, T-202

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-2 |
| Подволна | вне подволн §6.1 (`../tasks.md` §14) |
| Размер | S |
| Исполнитель | architect#2 (Opus); подтверждает tech-lead#2 |
| Статус | done |
| Ветка | `task/T-459-docs-call-labels-c07` от `epic/EPIC-003-swarm-llm-laws` (`49d1c6e`), папка `.worktrees/T-459` |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-13 / 2026-09-13 |

## Описание и DoD
Главный источник — раздел T-459 в `../tasks.md` (§14). Здесь не дублируется. Кратко:
- Основание: T-457 (EPIC-001, system-architect#1) — «Строки DoD для владельцев задач», пункт architect#2 (ADR-029 принят, C-07 v1.5, C-15 v1.4, C-01 v1.9); приёмка T-210 — КД §9.2 шаг 1 (Mi-2); приёмка T-202 — вопросы по валидатору.
- Код, схемы и `contracts.md` не меняются.

## Выполнение (architect)
architect#2, 2026-09-13. Коммитов нет, в индекс ничего не добавлено. Стенд LLM и Docker не использовались.

**Что внесено**
- `architecture/adr/ADR-029-narrative-call-labels.md`:
  - статус «принято» (T-457), «Уровень» и «Связи» — C-07 v1.5, C-15 v1.4;
  - п. 5 — условие «таблицу не передали» по каждому массиву (N-6);
  - п. 6 — `unknown_entity` с `element` и `ref`, временная форма `other` отменена; запись для BA — по T-457 п. 1.6;
  - п. 7–8 — `labels_hash`, сверка replay по нему; у записи без поля — прежнее правило по `prompt_hash`;
  - «Рассмотренные варианты» — строка `labels_hash`; последствия (пункт про `other` удалён), «Что сделать» (T-203, T-211, T-212, T-213, T-217), документы других владельцев, условия пересмотра; раздел «История изменений».
- КД `architecture/components/swarm-llm-laws.md`, пометки «(изм. T-459)»:
  - §2 и §3 — `recorded.Provider`/`fake.Provider`, `Request.AgentID`/`Attempt`, `Issue.Code`, `EnvFromProject`, источник белых списков; только однострочные замены, строка `:521` на месте;
  - §9.1 — источники записей `shared/recording`, ключ из полей `llm.Request` без `WithCall`, `ErrNoCallKey`/`ErrMalformedRecord`;
  - §9.2 — `budget.Allow` перед каждой попыткой (шаг 3a), отказ на повторе — `rejected budget_exceeded` без `llm_output` и `ErrBudget`; синхронный `Observe`; `labels_hash` в записи и сверка replay; отброс с `ref`; абзац с обоснованием;
  - §10.2 — правило 3 для нарратива и тика с `ref`, условие по массиву подтверждено; форма `rejected`;
  - §13.2 — состав `ValidationEnv`, правила 5, 6, 7а, 8, 10, 11, 12, 14 по коду и приёмке T-202, три решения;
  - §13.4 — `maxLength: 128` у id схем тика; §13.4.1 «Проверки» — порог по `unknown_entity` с `ref`; §13.4.2 — страж, аудит и replay по `labels_hash`;
  - §19 (допущение 3), «Дополнение T-439» — C-07 v1.5; новое «Дополнение 2026-09-13 (architect#2, T-459)».
- `architecture/diagrams/seq-swarm-llm-laws-narrative-labels.md` — сообщения replay (`llm_replay_labels_drift`), записи (`labels_hash`) и отброса (`unknown_entity` + `element` + `ref`), пояснения под диаграммой.
- `architecture/adr/ADR-015-blueprint-format-v2-validator.md` (документ architect#2) — «Уточнение» к п. 3 (`ValidationEnv`, `EnvFromProject(root, eventTypes, ownedEntityTypes, invariants, models)`, `Issue.Code`) и к п. 4 (источник белых списков, `city-gm`).
- `epics/EPIC-003-swarm-llm-laws/design.md` v0.3c — §3.1 A2: белые списки по `api-contracts.md` §2.4, сигнатура `EnvFromProject`.
- `../tasks.md` 0.3.6 — строки DoD T-203, T-204, T-211, T-212, T-213 (пункт «Отброс выдуманной ссылки нарратива…» заменён), T-217, T-221, T-222, T-237, T-246, T-247, T-248, стендовые T-260…T-262; зависимости T-212 и T-221; §14 с разделом T-459.

**Решения по вопросам приёмки T-202** (КД §13.2)
1. **Серьёзность 7а в рантайме — стабильный код находки `Issue.Code`** (`model_missing`, `models_not_checked`). Валидатор выдаёт `error`, `LoadDir` понижает по коду. Поле окружения отвергнуто: рантайму всё равно нужно найти блупринт и фазу для `/health` и шаблона, и без кода он искал бы находку по тексту `Reason`. Делает T-222 (подволна D), T-204 пользуется.
2. **`city-gm` — пустой белый список и резервная роль** (`info: reserved role, spawn disabled`). Список `domain` дал бы роли города полномочия GM региона без решения и без типа scope `city`; поднятый таймер-агент без разрешённых событий тратил бы бюджет `B`.
3. **Владение — по уровню (Mi-2 — оставить).** Роль сужает строку уровня только выводом из белого списка событий. Пересмотр — когда у одного уровня появятся две роли с предложениями сущностей и разными правами; тогда запрос к C-02.

**Отклонения и находки**
1. **В ветке задачи нет `contracts.md` v0.13.** `49d1c6e` содержит `contracts.md` v0.12, а ADR-016 и ADR-017 — без пометок T-457: T-457 слита только в `epic/EPIC-001-foundation` (`14b29aa`), в `develop` её нет. Тексты C-07 v1.5, C-15 v1.4 и C-01 v1.9 взяты из `epic/EPIC-001-foundation`, `contracts.md` здесь не менялся. До слияния EPIC-001 в `develop` и синхронизации эпика документы EPIC-003 ссылаются на версии контракта, которых в ветке эпика ещё нет.
2. **Имя источника таблицы для `labels_hash`** (T-457 оставил его за architect#2). Отдельного поля в `Call` нет: таблица — `Call.Guard.Mentions` и `Call.Guard.AbsenceEventIDs`, вид вызова — `Call.Guard.Kind`, хеш — одна функция `llm.LabelsHash` (`internal/llm/labels.go`, T-211) для `Recorder` и сверки replay T-212.
3. **T-212: два порядка при отсутствии T-458 к старту.** Приёмка T-207 выносит адаптер в отдельную задачу, T-457 — «привязка последней, по частям не сливать». В DoD записаны оба с пометкой «решение tech-lead#2 до старта».
4. **Сверх перечня T-457**, ради согласованности: КД §2/§3 (`Request`, `Issue`), `Observe` в §9.2, правила 5/6/11/12 §13.2 по Mi-1 T-202, строки диаграммы `:28` и `:52`, DoD T-237, T-247, T-248, T-261, T-262, таблица вариантов ADR-029.
5. **`.gitleaksignore`.** Все правки КД выше строки `:521` — однострочные замены. Строка таблицы провайдеров `providers/ollama` с inline `gitleaks:allow` осталась на `:521` и побайтно равна `HEAD:521`. Построчный отпечаток `Docs/dev-team/architecture/components/swarm-llm-laws.md:generic-api-key:521` не сдвинулся, `.gitleaksignore` правка не нужна.

**Проверки**
- `git grep -n "C-07 v1.4" -- Docs/dev-team`: в `architecture/` и `tasks.md` EPIC-003 остались только денежный лимит (`contracts.md:3`, `tasks.md` — пункты T-210 и T-250) и история приёмки T-439 (`tasks.md`, раздел T-439). Остальное — история: `dev-log.md`, `review.md`, карточки, `journal.md`, `state.js`, `dashboard.html`. `git grep -n "до v1.4\|после v1.4\|по v1.4"` в ADR-029 и КД роя — пусто.
- CRLF во всех изменённых и новых файлах, NUL и BOM нет. Правка — скриптами Python в scratch (`t459-edit`) с проверкой «ровно одно вхождение», без `sed -i`.
- `gitleaks dir --redact` по копии изменённых и новых файлов с `.gitleaks.toml` и `.gitleaksignore` ветки — результат в записи `../dev-log.md`.
- `git status`: изменены только документы из списка выше; код, схемы, `contracts.md`, `.gitleaksignore` не тронуты.

**Итерация 2 (по ревью #1) · 2026-09-13 · architect#2.** Ревью #1 code-reviewer#3 — «вернуть», 0/1/3/5 (`../review.md`, «T-459 · ревью #1»). Решения оркестратора по Ma-1 и Mi-1 приняты вместо tech-lead#2. Код, схемы, `contracts.md` не менялись; коммитов нет.
- **Ma-1 — источник таблицы `labels_hash`.** `Call.Guard` вводит только T-213 (подволна G), поэтому:
  - T-211 делает `llm.LabelTable`, `llm.LabelsHash(t) (string, error)`, схему, golden-значения и запись поля из явного входа `Recorder` (`Labels *LabelTable`: `nil` — не нарратив). Ключ вне формы `eK`/`bK` — ошибка `ErrLabelTable` (бэклог ревью п. 1);
  - T-213 заполняет вход из `Call.Guard` и держит свойство-тест «поле есть ⇔ `Kind=narrative`». Вызов с таблицей вне формы отвергается на входе `Generate`, до бюджета и провайдера;
  - сверка ярлыков replay (`labels_hash` и правило для записи без поля) — в T-213. Вариант «T-212 зависит от T-213» отвергнут: T-213 уже зависит от T-212, получился бы цикл. T-212 оставлены `warn llm_replay_prompt_drift` и `labels_hash` записи в `recorded.Record`.
  - Изменены `tasks.md` (T-211, T-212, T-213), КД §9.2 `:536`, ADR-029 «Что сделать». Дубль теста по `prompt_hash` снят (N-2): тесты ярлыков replay только в T-213.
- **Mi-1.** Условие старта T-212 — T-458 в `develop` и синхронизация эпика; по частям задача не сливается (T-457). Вариант «адаптер отдельной задачей» зачёркнут, фраза о расхождении порядка удалена.
- **Mi-2.** Оговорка «e2e на записях с памятью `background_refs` не сверяют» возвращена в T-248 с причиной: `AbsenceSummary` памяти заменяет список журнала (КД §11.3). То же добавлено в КД §13.4.2 и ADR-029 п. 8.
- **Mi-3.** `Rejection.Ref` — в «Файлах» T-217 (`internal/llm/types.go`), названо в КД §3 (строка `Evaluate`) и в «Дополнении T-459».
- **N-1.** `llm_replay_labels_drift` пишется при любых массивах, в том числе пустых (C-07 v1.5): ADR-029 п. 8, КД §9.2 и §13.4.2, тест T-213.
- **N-3.** `recorded.Key`/`KeyOf` со своей кодировкой удаляются; ключ вызова — `recording.LLMOutputKey` (T-212).
- **N-4.** В T-222 названа строка `levels_test.go:20`. КД §13.2, решение 2: правила уровня `domain`, в том числе 10 и 11, к резервной роли применяются — блупринт города остаётся валидным блупринтом уровня.
- **N-5.** Правило 11: «`round.timeout`, если задан, — положительная длительность».
- **C-07 v1.6 (`called_at`, T-456; пометка «действует после её слияния»).** T-211 — схема и `Recorder`; T-212 — `Clock.Now()` перед `Generate` после `Allow`; T-250 — окно бюджета от `called_at`, без поля — от `timestamp`. В КД §9.2 — строка `calledAt` и поле записи. `contracts.md` не правился.
- **Проверки.** Строка `:521` КД побайтно равна `HEAD:521` (правка выше неё — однострочная замена в §3). CRLF, без NUL и BOM. `gitleaks dir --redact` по копии — в записи `../dev-log.md` итерации 2.

## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Critical / Major / Minor / Nit | Где |
|---|---|---|---|---|---|
| 1 | 2026-09-13 | code-reviewer#3 (TEAM-2) | вернуть | 0 / 1 / 3 / 5 | `../review.md`, «T-459 · ревью #1»: Ma-1 источник `labels_hash` — `Call.Guard`, которого к старту T-211 (E) и T-212 (F) нет (вводит T-213, G); Mi-1 безусловная зависимость T-212 от T-458 при открытом выборе порядка; Mi-2 T-248 без оговорки e2e при `AbsenceSummary` памяти; Mi-3 `Rejection.Ref` вне файлов T-217 и КД §3 |
| 2 | 2026-09-13 | code-reviewer#3 (TEAM-2) | принять | 0 / 0 / 2 / 3 | `../review.md`, «T-459 · ревью #2»: Ma-1, Mi-1…Mi-3, N-1…N-5 закрыты; `called_at` по C-07 v1.6 верен; Mi-4 «Файлы» T-213 без `types.go` (`Call.Guard`) и T-212 без `providers/recorded`, `budget.go`; Mi-5 нулевой `called_at` на входе `Recorder` не определён; N-6 для system-architect (номер задачи свойство-теста и ключ вне формы в C-07), N-7 §6.1 F без условия T-458, N-8 КД без `LabelTable` и окна по `called_at` |

## Приёмка (tech-lead)
**Решение: принята** · tech-lead#2 · 2026-09-13 · итераций ревью 2 (ревью #2 code-reviewer#3 — «принять», 0/0/2/3).

**Итог.** Документы EPIC-003 приведены к C-07 v1.5/v1.6 и решениям приёмок T-210, T-202 без циклов зависимостей между T-211, T-212, T-213; замечания ревью #2 закрыты при приёмке однострочными правками.

**Сверка DoD** (рабочее дерево `.worktrees/T-459`, база `49d1c6e`, коммитов нет):
- `git grep -n "C-07 v1.4"` по `architecture/`, `tasks.md`, `design.md` — только `contracts.md:3` (строка версии), денежный лимит T-210/T-250, история T-439 и строка DoD T-459; `до v1.4\|после v1.4\|по v1.4` в ADR-029 и КД роя — пусто;
- строка `:521` КД (`providers/ollama`, inline `gitleaks:allow`) побайтно равна `HEAD:521` после правок приёмки; число строк КД не менялось;
- решения по вопросам приёмки T-202 — КД §13.2 (решения 1–3) и строки DoD T-204, T-222;
- CRLF во всех изменённых и новых файлах, NUL и BOM нет; правки — скриптами Python с проверкой «ровно одно вхождение», без `sed -i`;
- `gitleaks dir --redact` по копии изменённых файлов с `.gitleaks.toml` и `.gitleaksignore` ветки — «no leaks found»;
- `git status`: код, схемы, `contracts.md`, `.gitleaksignore` не менялись. Тесты и линтер кода не прогонялись: задача только документная.

**Закрыто при приёмке** (пометки «приёмка T-459» в `../tasks.md`, «изм. T-459» в КД):
- **Mi-4** — «Файлы» T-213: `internal/llm/types.go` (`Call.Guard`); «Файлы» T-212: `internal/llm/providers/recorded/*.go`, `internal/llm/budget.go` с `budget_test.go`;
- **Mi-5** — T-211: нулевой `CalledAt` на входе `Recorder` — ошибка `Recorder`, запись не публикуется; T-212: свойство-тест «`called_at` есть, не нулевое, не раньше `timestamp` причины». Обе строки помечены «уточняется T-456»: окончательную норму C-07 даёт system-architect#1;
- **N-7** — §6.1 строка F: T-212 стартует после контрольного слияния EPIC-002 с T-458 в `develop` и синхронизации эпика;
- **N-8** — КД §2 (строка `record.go`: вход `Labels`, `CalledAt`, `labels.go`), §3 (строка `var Err…`: `ErrLabelTable`, `LabelTable`, `LabelsHash`), §9.3 (окно по `called_at`, без поля — по `timestamp`). §2 и §3 — замены строк один к одному выше `:521`;
- **бэклог ревью #2** — T-237: окно бюджета при догоне читается `recording.ReadJournal`; T-213: ветка `background_ref` в MVP-1 не издаётся; T-211: симметричный golden `e1` в `Absence`.

**Не закрыто здесь.** N-6 (номер задачи свойство-теста `labels_hash` в C-07 и правило ключа вне формы) — system-architect#1 через оркестратора.

**Совместимость с кончиком эпика** `95507f0` (слиты T-205 и синхронизация с `develop`, `contracts.md` v0.13). Трёхстороннее слияние `tasks.md` в копии: два конфликта — шапка и строка changelog (обе версии добавили строку после 0.3.5). У T-459 номер **0.3.7**, шапка 0.3.7; при слиянии оставить обе строки changelog — 0.3.6 (T-205), затем 0.3.7 (T-459). Остальные правки кончика (T-205, T-234, T-238) слились без конфликта. КД роя, ADR-029, ADR-015, диаграмма и `design.md` кончиком после `49d1c6e` не менялись. `dev-log.md` и `review.md` сводит драйвер `appendtail`. Упоминания «0.3.6» в «Выполнении» карточки и в `dev-log.md` — история исполнителя, не правились.


## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Выполнение | 13.09 17:34 | 13.09 17:58 | 24m |
| Ревью #1 | 13.09 17:58 | 13.09 18:15 | 17m |
| Итерация 2 | 13.09 18:15 | 13.09 18:20 | 5m |
| Ревью #2 | 13.09 18:20 | 13.09 18:29 | 9m |
| Приёмка | 13.09 18:29 | 13.09 18:36 | 7m |
| Слияние | 13.09 18:36 | 13.09 18:37 | 1m |
| **Всего** | | | **63m** |
