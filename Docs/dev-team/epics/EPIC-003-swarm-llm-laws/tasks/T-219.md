# T-219: FakeEncounter — бой первой фазы без роя

| Поле | Значение |
|---|---|
| Инициатива / эпик / команда | EPIC-003 / EPIC-003 / TEAM-1 |
| Волна | 1 |
| Исполнитель | developer#3 |
| Размер | см. индекс `tasks.md` |
| Статус | in-review (итерация 2 сдана, ждёт ревью #2) |
| Итераций ревью | 1 |
| Начата / завершена | 2026-09-11 19:20 / — |

> Карточка заведена задним числом при переходе на плагин 0.6.0. Разделы
> «Описание» и «DoD» живут в индексе `tasks.md` — дублировать их сюда значило бы
> завести второй источник истины. Подробности выполнения — в `dev-log.md`,
> замечания и вердикты — в `review.md`, решения — в `journal.md`.

## Описание и DoD
См. раздел T-219 в `../tasks.md`.

## Выполнение (developer)

Подробности — записи T-219 в `../dev-log.md` (итерация 1 от 2026-09-10, итерации 2 и 3 от
2026-09-11). Ниже — то, что должен знать любой, кто откроет этот код следующим.

### Итерация 3 (developer#3, 2026-09-11) — повтор при конфликте версий

По решению оркестратора об остаточной гонке (`journal.md`, 2026-09-11).

**Гонка.** Заглушка отвечает на удар по своему представлению о мире, а пополняет его
фактами `entity.updated` по отдельной подписке. Порядок между `player_events` и
`system_events` контракт не гарантирует (C-01), поэтому удар обгоняет факт: пакет уходит
с `expected_version` на единицу меньше, и State отвергает его целиком (`atomic`).

**Что сделано.** Получив `entity.update.rejected` с причиной `version_conflict` на своё
предложение, заглушка (`refused`):

1. возвращает свой взгляд на мир к тому, что говорят факты (`rollback`) — пакет отвергнут
   целиком, значит не сдвинул ничего;
2. **пересчитывает** пакет по актуальному миру (`pack`): тот же урон снимается с того
   здоровья, которое у цели есть сейчас, а цель, успевшая упасть, не бьётся вовсе;
3. предлагает пакет снова под **тем же** `proposal_id` — это та же попытка ответить на то
   же действие, а не второй ответ: кости брошены один раз, `combat.decided` опубликован
   один раз;
4. не более `maxProposalAttempts` = 3 попыток на действие; исчерпание — `log.Error`
   «gave up», а не тихое молчание и не цикл.

Повтору подлежит **только** конфликт версий. `unknown_entity`, `invalid_op`,
`dead_entity`, `duplicate_entity` — дефекты, а не гонки: они логируются ошибкой и
остаются видны.

**Почему не «дождаться факта».** Контракт прямо не упорядочивает топики, и дизайн уже
опирается на это (событие закрытия раунда самодостаточно ровно затем, чтобы агент не ждал
события чужого топика). Ожидание невыразимо и противоречит принципу; оптимистическая
блокировка для того и существует.

**Два вида мира.** Появилось поле `facts` — мир таким, каким его описывают факты State,
рядом с `world`, куда заглушка складывает и то, что ещё не подтверждено (`apply`).
Откат берёт версию и атрибуты из `facts`, поэтому факт, пришедший в то же окно, не
теряется — именно он и делает пересчёт пересчётом.

**Чем закрыто:** `TestAPackageRefusedForAVersionConflictIsOfferedAgain`,
`TestARetryDoesNotStrikeAFighterWhoHasFallen`, `TestARefusedPackageMovedNothingAtAll`,
`TestTheStubGivesUpOnAPackageItKeepsLosingWith`, `TestARefusalThatIsNotARaceIsNotRetried`
(четыре причины), `TestARefusalOfSomebodyElsesPackageIsNotTheBusinessOfTheStub`. Восемь
мутаций, убиты все.

**Что этой правкой не закрывается.** Стенд `shared/testkit/gateway` продолжает краснеть с
прежней частотой, и причина теперь другая: харнесс T-400 считает **любой** отказ концом
ожидания (`harness.go`, `reaction.refusal` → `done()`), поэтому падает на самом событии
`entity.update.rejected`, не дожидаясь повторного предложения. Чтобы гонка закрылась
сквозным путём, харнессу нужно знать то же, что теперь знает агент: отказ по конфликту
версий не окончателен, пока возможен повтор, а предложение с тем же `proposal_id` — та же
попытка, а не второй пакет. Это каталог developer#1 — заявка оркестратору.

### Итерация 2 (developer#3, 2026-09-11) — по ревью #1

| Замечание | Что сделано |
|---|---|
| **Cr-1** | Каждое действие, которое встреча принимает, кладёт в пакет набор для **сущности встречи**: `round_seq` пока бой идёт; `state`/`resolution`/`closed_by_event_id` когда он кончился; `participants` когда участие сдвинулось. Пакет поэтому никогда не пуст, и промах даёт ровно одно предложение. Тест `TestAMissChangesNothing`, охранявший дефект, **снят**; на его месте `TestAMissStillCostsTheRound` (ровно один пакет, ровно одна операция `round_seq = 2`) плюс `TestEveryActionTheEncounterTakesIsAnsweredWithOnePackage` и `TestTheEncounterEntityRecordsTheFightItHeld`. |
| **Ma-1** | `encounterAgent` принимает уровень: открытие встречи (`entity.create.proposed` + `encounter.started`) — `domain`, всё остальное — `task`. Идентификатор агента не менялся. Сверка каждого предложения с `contracts.OwnershipRules()` — тестом `TestEveryProposalPassesTheOwnershipTable`: шесть видов, отклонений нет. |
| **Cr-2** | Не у меня — см. ниже. |
| **Mi-1** | Комментарий у импорта `internal/mechanics` ссылается на действующее правило `shared-testkit-swarm` и запрещает добавлять сюда `//nolint`. |
| **Mi-2** | Висячий комментарий об `AttrLootClaimedBy` удалён — константа есть в `shared/entity`. |
| **Mi-3** | Негативные тесты контекста читают текст отказа; добавлен четвёртый (фикстуры). |
| **Mi-4** | `TestTheContextRunsAWholeFight` доведён до удара. |
| **N-1** | Порядок наборов закреплён `TestThePackageKeepsTheOrderTheFightersWereTouchedIn`. |

### Cr-2: молчание заглушки — решение, а не забытая ветка

**Не «чините» это публикацией отказа.** Есть четыре случая, в которых `FakeEncounter`
не публикует ничего и возвращает `nil`: действие после конца встречи (`fightOf`), целей
не осталось (`targetOf`), вход без региона (`entered`), действие чужого мира (`Act`).

Решением оркестратора от 2026-09-11 это **ошибки сценария, а не события мира**, и
немедленную внятную ошибку на них даёт харнесс `shared/testkit/gateway` (T-400), который
знает состояние встречи из `encounter.started`/`encounter.ended`. Вариант «заглушка
публикует отказ» отвергнут: он потребовал бы нового типа события ради заглушки, которую
снимают через два эпика (T-242, T-256) — ровно то, от чего отказались в T-018.

Возвращать здесь ошибку тоже нельзя: шина повторит доставку и положит действие в
`dead_letters` — это не отказ сценаристу, а мусор в очереди.

Проверено и закреплено: `TestAnActionAfterTheFightIsOverIsAnsweredWithSilence`,
`TestAWolfKilledElsewhereIsNotAttackedAgain`, `TestAnEntryWithoutARegionIsPassedOver`,
`TestAnActionOfAnotherWorldIsPassedOver`. Довод продублирован в doc-комментарии
`FakeEncounter`.

### Итерация 1 (developer#3, 2026-09-10)

`fake_encounter.go` (подписчик `player_events` и `system_events`: вход → создание встречи
и `encounter.started`; атака и бегство → броски, решения, атомарный пакет, три конца),
`fake_context.go` (`runtime.Context` под именем `swarm`), `README.md` пакета с условием
снятия заглушки.

## Ревью (code-reviewer)

| Итерация | Дата | Ревьюер | Вердикт | Critical | Major | Minor | Nit |
|---|---|---|---|---|---|---|---|
| #1 | 2026-09-11 | code-reviewer#2 | **вернуть** | 2 | 1 | 4 | 1 |

Ревьюировалось **вместе с T-400** (`shared/testkit/gateway`, эпик EPIC-001): по указанию
оркестратора это один боевой путь, и порознь он не проверяется.

Существенные замечания итерации #1:

- **Cr-1** `fake_encounter.go` (`propose`) — при обмене, в котором никто не попал, не публикуется
  НИЧЕГО. Прямое расхождение с решением оркестратора «одно действие — ровно одно атомарное
  предложение» (вариант «б»). Обратное поведение закреплено тестом `TestAMissChangesNothing`,
  который придётся снять вместе с правкой.
- **Cr-2** `fake_encounter.go` — действие, которое заглушка решила не отрабатывать (встреча
  закрыта, целей не осталось, вход без региона, чужой мир), остаётся без любого ответа; отличить
  это от «никто не слушает» нельзя. DoD «0 действий без ответа» не выполняется.
- **Ma-1** встреча создаётся от имени агента уровня `task`, а §4.6 отдаёт создание уровню `domain`
  с `cause=spawn`. Подтверждает решение оркестратора; радиус — ровно одно предложение из всех,
  остальные четыре вида в таблицу владения укладываются.

Итог боевого пути: 25 прогонов из 60 падают по таймауту на всех трёх боевых шагах.

Подробности — раздел «T-219 · ревью #1» в `../review.md`.

## Приёмка (tech-lead)
Не принята: задача не в состоянии done. Решения по задаче — в `journal.md` по дате.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| — | — | — | задача ещё не запускалась |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
