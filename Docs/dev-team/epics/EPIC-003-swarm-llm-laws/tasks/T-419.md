# T-419: Двойники по C-05 v1.4: встреча после факта, признак конца обмена, окно нарратора на Dedup

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-003 / TEAM-2 (выполнена в период одной команды) |
| Волна | 1 |
| Размер | M |
| Статус | done (принята tech-lead#2; итерация 3 сверена по диффу, мутанты убиты) |

## Описание и DoD
- **Причина (T-416, ADR-026, ADR-027)**: `FakeEncounter` публикует `encounter.started`/`encounter.ended` до факта State и берёт NPC второй активной встречи — это нарушает C-05 v1.4 п. 4–6 и `data-model.md` §3.7; нарратор держит своё окно вместо `Dedup.Has`/`Add`.
- **Что сделать**: начало встречи — после `entity.created`, конец — после факта закрывающего пакета; «один NPC — одна активная встреча»; закрытие по чужому факту без `killer`; откат участия при выброшенной попытке (п. 1г); поле `combat.decided.exchange{index,last}` (схема — владелец EPIC-003) и закрытие хода нарратором по `exchange.last`; окно нарратора на `Dedup` (после T-417). Харнесс в `shared/testkit/gateway` — совместно с его владельцем.
- **DoD**: пять расхождений двойника с v1.4 из блока «Заглушка» закрыты тестами; стенды пары и нарратора зелёные. Срок — до T-230: иначе исполнитель T-230 возьмёт неверный образец.

## Выполнение (developer)
**developer#3, итерация 1 — пункты 1–6 (2026-09-11).** Пункт 7 (окно нарратора на `Dedup.Has`/`Add`)
не делался: он идёт отдельной итерацией после приёмки T-417. Время начала и окончания в задании не
указано.

Что сделано (C-05 v1.4, ADR-026):
1. `encounter.started` — только после `entity.created` встречи. При отказе в создании события нет,
   в лог идёт `Error`, NPC свободен.
2. `encounter.ended` — только после факта закрывающего пакета. Пока создание или закрытие в полёте,
   действия персонажа откладываются. Выброшенный пакет конца не даёт.
3. Один NPC — одна активная встреча.
4. Б1: встречу, чей последний NPC убит чужой рукой, двойник закрывает сам — `cause=death`, без
   трофея, `encounter.ended {npc_dead}` без `killer`, под scope встречи.
5. Участие откатывается вместе с выброшенным пакетом, номер раунда — нет.
6. `combat.decided.exchange{index, last}`: поле в схеме v1 (опциональное), двойник заполняет его
   всегда, нарратор закрывает ход по `exchange.last`, если поле есть.

Харнесс (совладение): конец боя признаётся и по факту сущности встречи со `state=resolved`, а старт,
пришедший после конца своей встречи, бой не открывает. Поимённый список правок — в `dev-log.md`.

Файлы:
- изменены: `shared/testkit/swarm/fake_encounter.go`, `fake_narrator.go`, `README.md`,
  `fake_encounter_test.go`, `fake_narrator_test.go`, `fake_context_test.go`;
  `shared/testkit/gateway/harness.go`; `schemas/events/combat.decided.v1.json`;
- созданы: `shared/testkit/swarm/fake_encounter_lifecycle_test.go`,
  `shared/testkit/gateway/end_by_fact_test.go`.

Тесты:
- `go test -short -count=1 ./...` — все ok; `go test -tags e2e ./test/...` — ok;
- стенды пары и нарратора `-count=20` — ok;
- T-255 в `cmd/multiverse` — зелёные; зонд N-3 «мир без волка» — сообщение на месте;
- покрытие `shared/testkit/swarm` — 89,4 %;
- мутанты — 14 из 14 убиты: правила 1–6 и две правки харнесса, таблица в `dev-log.md`;
- `golangci-lint` — 0 issues; `mvctl contracts check` — ok.

Отклонения:
- отказ не по гонке тоже выбрасывает пакет (иначе закрывающий пакет подвешивал бы бой);
- откладываются действия и на время создания встречи;
- причина пакета Б1 — `death` (контракт её не называет);
- id событий жизненного цикла пока из `Derive`, а не `WithCauseID`: T-417 не принята.

Предложения в бэклог — в `dev-log.md`.

**developer#3, итерация 2 — пункт 7, решения оркестратора, ревью #1 (2026-09-11).** Время начала и окончания
в задании не указано.

Что сделано:
1. **Пункт 7.** Окно нарратора — `eventbus.Dedup`: `Has` до ответа, при ошибке публикации — возврат ошибки
   без `Add`, `Add` после успеха. Своё `window` и его тест (`window_test.go`) удалены. Предусловие «не
   вызывать конкурентно для одного события» осталось в комментариях. Снапшота у нарратора нет: заглушка не
   stateful-контекст C-14; это названо в комментарии поля и в README.
2. **`WithCauseID` для жизненного цикла.** Часть id — id встречи (ADR-027 п. 2). Причина — событие, от
   которого строится пакет:
   - `encounter.started` — вход;
   - `encounter.ended` — действие закрывающего пакета, для Б1 — чужой факт.

   Публикация по-прежнему после факта State, и `opened_by_event_id`/`closed_by_event_id` называют эти id.
   Причиной не может быть сам факт: сущность называет событие до того, как факт существует. Тест:
   второй двойник («после рестарта») на тех же событиях даёт те же id.
3. Пакет закрытия Б1 — `cause=resolve`; строка владения `task`/`encounter` его разрешает.
4. **Mi-1.** Отказ State в закрытии Б1 больше не подвешивает бой. Закрытие по тому же факту не
   предлагается снова, отложенные действия решаются, `Error` пишется один раз. Сценарий P1 закреплён
   тестом.
5. **Mi-2.** `openFight` проваливает тест, если бой не открылся; у четырёх тестов появились нижние границы.
   Мутант «встреча никогда не активна» красит все четыре.
6. **Mi-3.** Зонды P4 и P6 перенесены в `fake_encounter_lifecycle_test.go`.
7. **N-1.** Формулировка смягчена в комментарии `harness.go` и в `dev-log.md`, тест не добавлялся:
   надёжно отличить порядок тестом нельзя, окно гонки — между двумя захватами одного мьютекса.

Файлы итерации:
- изменены: `fake_narrator.go`, `fake_encounter.go`, `README.md`, `fake_encounter_test.go`,
  `fake_narrator_test.go`, `fake_encounter_lifecycle_test.go` (все в `shared/testkit/swarm/`);
  `shared/testkit/gateway/harness.go` (только комментарий);
- удалён: `shared/testkit/swarm/window_test.go`.

Тесты:
- `go test -short ./...` — ok; e2e — ok;
- стенды `-count=20` — ok;
- T-255 — PASS;
- покрытие: swarm 89,6 %, gateway 92,6 %;
- мутанты — 11 из 11 убиты, таблица в `dev-log.md`;
- `golangci-lint` — 0 issues; `contracts check` — ok.

**developer#3, итерация 3 — Mi-A ревью #2 (2026-09-11).** Время начала и окончания в задании не указано.

Что сделано:
- `fake_encounter.go:615` — `Act` не отвечает на действие без `id` и пишет `Error`, до `acted.Seen`. Раньше
  `WithCauseID` на таком действии паниковал и ронял процесс (зонд RF). `Error`, а не `Warn`: это дефект
  издателя, и тот, кто ждёт действие, увидит только истечение срока.
- `fake_encounter.go:1486` — `downedBy` не берёт факт без `id` причиной закрытия Б1 и пишет `Error`: бой не
  закрывается, но не молча (зонд RG).
- Другие вызовы `Derive` от чужого конверта без `WithCauseID` не паникуют: `publish` и нарратор берут id из
  генератора, повтор пакета строится от уже принятого действия.
- Тесты: `TestAnActionWithoutAnIDIsRefusedOutLoud`, `TestAFactWithoutAnIDDoesNotCloseTheFight`.

Мутанты — overlay с относительным ключом, контрольный с синтаксической ошибкой красный:
- снятая проверка в `Act` — убит паникой в RF;
- снятая проверка в `downedBy` — убит в RG.

Проверки: build и vet, `go test -short ./shared/testkit/... ./cmd/...`, стенды `-count=5`, T-255, линтер — зелёные.

## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Замечания |
|---|---|---|---|---|
| ревью #1 (пункты 1–6) | 2026-09-11 | code-reviewer#2 | принять | Blocker 0 · Major 0 · Minor 3 · Nit 1 — Mi-1 отказ в закрытии Б1 оставляет отложенные действия без решения; Mi-2 четыре теста боя проходят на нулях; Mi-3 фаза «создаётся» без юнит-тестов; N-1 порядок `end`/`record` в харнессе не закреплён. Подробно — `review.md`, раздел «T-419 · ревью #1» |
| ревью #2 (итерация 2: пункт 7, `WithCauseID`, `resolve`, Mi-1…N-1) | 2026-09-11 | code-reviewer#2 | принять | Critical 0 · Major 0 · Minor 1 · Nit 0 — Mi-1…Mi-3 и N-1 закрыты (мутанты MB, MC, MA, Mi1 убиты); пункт 7 — четыре мутанта убиты, зонды RA–RC зелёные; id жизненного цикла по ADR-027 п. 2, одинаковы после рестарта и на пути Б1; сдвиг исходов T-255 — только генератор id (мутант MG возвращает распределение итерации 1). Mi-A: действие без `id` роняет двойник паникой `WithCauseID` (достижимо только при `MV_BUS_VALIDATE_ON_READ=false`). Подробно — `review.md`, раздел «T-419 · ревью #2» |

## Приёмка (tech-lead)
**Решение: принята.** 2026-09-11, tech-lead#2. Итог: пять расхождений двойника с C-05 v1.4 закрыты тестами, двойник
соответствует C-05 v1.5 и годится образцом формы для T-230; ревью #3 не требуется — итерация 3 не меняет
поведения сверх двух проверок пустого `id`.

Как проверено (копия индекса из `git checkout-index -a`, мутанты — правкой файла во второй копии со сверкой `cmp`;
обе копии удалены по точному пути, рабочее дерево не менялось):
- **Итерация 3 по диффу.** Блобы ревью #2 → индекс: `fake_encounter.go` `dc555f11 → 3f5ec9c2` (+28/−3) — только
  проверка `ev.ID == ""` в `Act` (`:615`, после фильтра типов, до мира и `acted.Seen`) и в `downedBy` (`:1486`;
  условие развёрнуто в `continue` без смены поведения), плюс комментарии; `fake_encounter_lifecycle_test.go`
  `3819291b → 5fb4dedc` (+57) — два теста. Остальные файлы задачи равны хешам ревью #2 (`fake_narrator.go`
  `bd83b95d`, `harness.go` `760c4c7d`, тесты `f3ab0b21`/`aebc8631`, `README.md` `f1629c44`) и ревью #1 (схема
  `e51f80ef`, `end_by_fact_test.go` `13ad2ced`); `window_test.go` удалён и в индексе, и на диске. Других путей кода
  в индексе нет. Рабочая копия файлов задачи равна индексу.
- **Мутанты (сам):** m0 контрольный (синтаксическая ошибка) — сборка красная; mA (снята проверка в `Act`) — убит
  `TestAnActionWithoutAnIDIsRefusedOutLoud`, паника `WithCauseID on "encounter.started"`; mB (снята проверка в
  `downedBy`) — убит `TestAFactWithoutAnIDDoesNotCloseTheFight` по отсутствию `Error` в логе; mA2 (свой: проверка только
  для входа) — убит второй половиной RF, но ошибкой `FixedMechanics` «resolve without a cause event», а не паникой.
  Файл после каждого мутанта восстановлен, хеш равен индексу.
- **DoD и прогоны:** build и vet зелёные; `go test -short -count=1 ./...` — все пакеты ok; e2e — ok; стенд пары,
  стенды харнесса `TestTheSkirmish*` и стенд нарратора `-count=20` — ok (прогон `-v`: 16 боёв, без SKIP);
  T-255 в `cmd/multiverse` `-count=3` — PASS (`death:2–6 turn:57–64`, `npc_dead:10–14 players_out:2–6`, нижние
  границы держатся); покрытие `shared/testkit/swarm` 89,7 % (порог 88,9 %), `gateway` 92,6 %; `golangci-lint` —
  0 issues; `mvctl contracts check` — 65 типов, 8 топиков, 58 схем. Мутационные таблицы итераций 1–3 — в `dev-log.md`.
- **C-05 v1.5:** Б1 — `cause=resolve`, конец без `killer` под scope встречи от чужого факта; п. 1е — любой отказ,
  кроме `version_conflict` (значит, и `level_violation`), выбрасывает пакет с откатом и отпускает отложенные
  действия; отсрочка и на время создания; NPC занят с предложения создания; причины `WithCauseID` — вход и
  действие/чужой факт, не факт State и не `combat.decided`; действие или факт без `id` — пропуск с `Error`.

Не образец для T-230 и её частей (двойник намеренно проще, у каждого пункта есть задача): очередь отложенных
действий без предела (→ T-422, лимит 16 и громкий отказ); один слот `pending` на встречу (→ T-421); конец по
`abandoned` последнего игрока не сделан (→ T-423); сверки встреч после рестарта нет (→ T-424); id нарративов из
генератора (→ T-229). Вторая половина RF закрепляет не панику, а ошибку `FixedMechanics`: в T-230 тест на действие
без `id` проверяет «0 публикаций, `Error`, без паники» и для обычного, и для закрывающего удара.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 11.09 15:36 | 11.09 16:13 | 36m |
| Ревью | 11.09 16:13 | 11.09 16:40 | 27m |
| Разработка 2 | 11.09 16:40 | 11.09 16:58 | 18m |
| Ревью 2 | 11.09 16:58 | 11.09 17:13 | 14m |
| Разработка 3 | 11.09 17:13 | 11.09 17:20 | 6m |
| Приёмка | 11.09 17:56 | 11.09 18:02 | 6m |
| **Всего** | | | **1h 49m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
