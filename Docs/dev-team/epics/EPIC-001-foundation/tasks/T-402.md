# T-402: Стендовый замер LLM и заполнение baseline.md

| Поле | Значение |
|---|---|
| Инициатива / эпик / команда | EPIC-001 / EPIC-001 / TEAM-1 |
| Волна | 1 |
| Исполнитель | devops-engineer#1 (Opus); прогоны 3 и 4 E — оркестратор, той же командой |
| Размер | S |
| Статус | done (принята tech-lead#1 после ревью #2 и итерации 3) |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-11 23:24 / 2026-09-12 00:37 |

> Карточка заведена задним числом при переходе на плагин 0.6.0. Разделы
> «Описание» и «DoD» живут в индексе `tasks.md` — дублировать их сюда значило бы
> завести второй источник истины. Подробности выполнения — в `dev-log.md`,
> замечания и вердикты — в `review.md`, решения — в `journal.md`.

## Описание и DoD
См. раздел T-402 в `../tasks.md`.

## Выполнение (developer)
devops-engineer#1 (Opus), 2026-09-11. Запись — T-402 в `../dev-log.md`.

**Объём** (решение оркестратора, журнал 2026-09-11): конфигурация E, прогон 1
из 3, на параметрах сервера владельца. Сервер не запускался, не
перезапускался и не перенастраивался. Выполнены только HTTP-запросы к стенду.

**Что сделано.**
- Стенд опознан только чтением. Порт → имя образа: `:8888` — `python3.13.exe`
  (роутер Unsloth Studio), `:62450` — `llama-server.exe`. Командные строки
  процессов не читались. Из `/props` бэкенда: билд b10840-58670d128, файл
  `Qwen3.8-27B-UD-Q3_K_XL.gguf`, alias `Qwen3.8`, 4 слота по 221 696. Тип KV
  не известен. По `nvidia-smi` на GPU только имена процессов: вычислительный
  один, `llama-server`, плюс ~35 процессов графики рабочего стола.
- Прогон `scripts/llm-bench.sh --configs E` с `.env` владельца. Загружен в
  подоболочке, как в `load_dotenv` Makefile, не печатался. `--out-dir` →
  `ops/metrics` этой папки, `--num-ctx 221696 --kv-cache unknown`. 30 запросов,
  0 ошибок, 2 мин 34 с.
- Отклонение: имя модели E сопоставлено с id роутера `Qwen3.8` через копию
  матрицы во временном каталоге (`--matrix`). `bench-matrix.json` не менялся.
  Id `unsloth/Qwen3.8-27B-GGUF` — другой квант, не загружен; запрос к нему
  сменил бы модель, поэтому он не использовался.
- Диагностика вне методики, 10 запросов: роутер против прямого обращения к
  `llama-server`.

**Результат прогона 1** — `ops/metrics/bench-20260911-2327.csv`,
`ops/metrics/bench-E-20260911-2327.json`, сводка в `ops/metrics/baseline.md`
§2.1, §3:

| Фаза | p50, мс | p95, мс | tps | `valid_json` | `lang_pass` | Вердикт |
|---|---|---|---|---|---|---|
| phase2 | 2996 | 7602 | 48,1 | 1,0 | 1,0 | fail |
| phase2-group3 | 5021 | 9753 | 47,7 | 1,0 | 1,0 | fail\* |
| tick | 3213 | 9803 | 44,5 | 1,0 | 1,0 | n/a |

**Главный вывод** (исправлено в итерации 2, M-1). Что роутер Unsloth Studio
добавлял ~5,3–6,3 с примерно к каждому третьему запросу, — **гипотеза, не
установлено**. За: в проверке после прогона через роутер 2 из 5 запросов с
5,3–5,6 с, напрямую 0 из 5; на чистом пути в прогонах 2–3 хвоста нет.
Против: у Q36 на чистом пути «прочее» больше 2 с в 11 из 30, и короткая
проверка этого не воспроизвела. Подробно — `baseline.md` §2.1. B3 и NFR-090
выполнены в 30 из 30 ответов. Генерация 44–48 ток/с — ниже справочных 70.
Прогон 1 в U-2 не засчитан (решение оркестратора 2026-09-12).

**Не замерено, с причинами:** C и A — Ollama не запущен. E+ — нет `qwen3-8b`
и режима роутера проекта. Четыре ячейки матрицы 8192/16384 × f16/q8_0 и
прогоны 2–3 требуют перезапуска сервера или другого момента. B2: сервер был
тёплым.

**Предложения в бэклог** (не сделаны в T-402, решает оркестратор):
1. `llm-bench.sh`/`.ps1`: брать `llamacpp_build` из `GET /props` → `build_info`,
   когда `MV_LLM_BIN` не задан. Сейчас в CSV попадает пин `b10441(pin)`, а
   стенд работает на b10840 (близко к T-404 п. 2).
2. `llm-bench.*`: сохранять в `requests[]` ещё `timings.prompt_ms` — без него
   задержку роутера пришлось выяснять отдельной диагностикой.
3. `bench-matrix.json` (architect#1): модель E на этом стенде идёт под id
   `Qwen3.8`. Нужно либо поле-алиас в матрице, либо договорённость о
   `--matrix` для стендов с роутером.
4. `make bench`: цель не передаёт аргументы (`--out-dir`, `--matrix`,
   `--num-ctx`) и при `pwsh` в `PATH` выбирает `.ps1`. На этой машине `pwsh`
   находится в `WindowsApps`.
5. `llm-bench.sh`: прогревочный вызов собирает JSON строкой bash и не
   экранирует `\` в id модели. На `llama-server` без `--alias` id — путь
   Windows, прогрев получает HTTP 500, и `first_call_ms` недействителен.
   Тело прогрева нужно собирать через python-помощник, как тела замера.

**Продолжение, 2026-09-11 после 23:50: дополнительный замер вне матрицы.**
Ответы владельца: путь платформы — чистый llama.cpp; прогоны 2–3 E — «позже,
скажу когда», сервер не трогать; роутер в локальной сети — задуманное свойство
стенда. Владелец поднял на `:8888` чистый `llama-server` (b10878-4850c7727,
`Qwen3.6-35B-A3B-UD-Q3_K_XL.gguf`, 4 слота × 200 192, тип KV не известен) и
попросил посмотреть. Сервер не трогался. Конфигурация `Q36-A3B` — копия E в
копии матрицы (`--matrix`): модель = id из `/v1/models` (путь к файлу),
`primary: false`. 30 запросов, 0 ошибок, 1 мин 49 с:

| Фаза | p50, мс | p95, мс | tps | `valid_json` | `lang_pass` | Вердикт |
|---|---|---|---|---|---|---|
| phase2 | 2941 | 3710 | 115,3 | 1,0 | 1,0 | pass |
| phase2-group3 | 3665 | 4244 | 110,8 | 1,0 | 1,0 | pass\* |
| tick | 2098 | 3182 | 75,2 | 1,0 | 1,0 | n/a |

- VRAM всей карты 19 595–19 641 МБ — в пределах справочных 20 480.
- Скорость генерации выше справочных 70 ток/с во всех фазах.
- `first_call_ms` недействителен (п. 5 выше). Корректный тёплый вызов — 539 мс.
- «Прочее» время больше 2 с — у половины нарративных запросов, включая
  медиану phase2 (11 из 30). Проверка после прогона на тех же промптах
  этого не воспроизвела (0,13–0,24 с). Гипотеза прогрева не сходится с
  порядком запросов и с прогонами 2–3 E. Причина не установлена
  (исправлено в итерации 2, Mi-1).

Сравнение с прогоном 1 E справочное: другая модель и другой путь. Замер не
оценивает качество нарратива. Стать кандидатом U-2 модель может только через
матрицу (решает architect#1). В `baseline.md` добавлены: §1 (роутер снят, путь
платформы прямо в `llama-server`), §2.1 (прогоны 2–3 — по сигналу владельца),
§2.5, §4 пп. 7–9, §5. Файлы: `ops/metrics/bench-20260911-2353.csv`,
`ops/metrics/bench-Q36-A3B-20260911-2353.json`.

### Итерация 2 (devops-engineer#1, 2026-09-12) — по ревью #1

Прогон 2 E сделал исполнитель (2026-09-12 00:03). Прогон 3 E — оркестратор
(00:08, после перезапуска сервера владельцем в 00:07:41), той же командой.
Оба на чистом `llama-server` b10878, `Qwen3.8-27B-UD-Q3_K_XL.gguf`, 4 × 200 192.

| Прогон | Фаза | p50, мс | p95, мс | tps | `valid_json` | `lang_pass` | Вердикт |
|---|---|---|---|---|---|---|---|
| 2 | phase2 | 2422 | 2585 | 48,0 | 1,0 | 1,0 | pass |
| 2 | phase2-group3 | 3900 | 4382 | 47,6 | 1,0 | 1,0 | pass\* |
| 2 | tick | 3158 | 4968 | 44,5 | 1,0 | 1,0 | n/a |
| 3 | phase2 | 2562 | 2916 | 46,4 | 1,0 | 1,0 | pass |
| 3 | phase2-group3 | 4213 | 4772 | 45,6 | 1,0 | 1,0 | pass\* |
| 3 | tick | 3236 | 6195 | 42,0 | 1,0 | 1,0 | n/a |

Вердикт: 2 `pass` из 2 зачётных. Прогон 1 не засчитан — решение оркестратора:
путь платформы — чистый llama.cpp. U-2 ждёт прогона 4 по сигналу владельца.

Закрыто по ревью:
- **C-1.** В четырёх JSON (`bench-E-20260911-2327`, `bench-Q36-A3B-20260911-2353`,
  `bench-E-20260912-0003`, `bench-E-20260912-0008`) `meta.matrix` заменено на
  нейтральное описание копии матрицы. В каждом файле изменена ровно одна
  строка, проверено построчным сравнением до/после. Грэп по всем CSV/JSON
  замера на путь временного каталога, имя профиля, UUID сессии и `C:/Users`
  — пусто. Ручная правка отмечена в `baseline.md` §1 и §2.5.
- **M-1.** Вывод о роутере переписан гипотезой с доводами за и против:
  `baseline.md` §2.1, §3, §4 пп. 1 и 6, «Главный вывод» выше. Сырые числа обеих
  диагностик сохранены: `ops/metrics/diag-T402-20260911.csv`, 16 строк, без
  путей. Доводы:
  - за — 2 из 5 через роутер против 0 из 5 напрямую; прогоны 2–3 на чистом пути
    с той же скоростью генерации, «прочее» 0,44–0,82 с во всех 60 запросах;
  - против — у Q36 на чистом пути 11 из 30 с «прочим» больше 2 с, не
    воспроизведено после; прогоны 2–3 отличаются ещё билдом, `n_ctx` и
    перезапуском.
- **Mi-1.** §2.5: медленна половина нарративных запросов Q36, включая медиану
  (idx phase2 1, 2, 8, 9, 10; group3 4, 5, 6, 9, 10). Гипотеза прогрева не
  сходится: медленные запросы идут до конца фаз, а прогоны 2–3 E через
  1–3 минуты после старта сервера задержек не дали.
- **Mi-2.** Латиница — среднее и максимум по ответу: Q36 group3 0,006 и 0,034;
  E прогон 2 group3 0,003 и 0,027. Фраза о характере латиницы убрана: тексты
  ответов не сохраняются.
- **Mi-3.** Перечень программ рабочего стола и каталог бинарника убраны.
  Осталось: «рабочий стол владельца занимает часть VRAM (~35 графических
  процессов); вычислительный — только `llama-server`; антивирус есть в списке
  процессов GPU».
- **Mi-4.** §6 описывает, как реально легли файлы: `.env` — из корня основной
  копии, скрипт и `--out-dir` — из папки задачи. Добавлена таблица отличий
  копий матрицы для E (прогон 1, прогоны 2–3) и Q36.
- **N-1.** Устаревшие фразы убраны («вопрос вынесен владельцу», «стенд —
  b10840»). §1 перестроен по трём периодам стенда.
- **N-2.** Пять LF-строк записи T-402 в `dev-log.md` переведены в CRLF, новая
  запись — в CRLF.

Дефект прогрева `llm-bench.sh` и путь матрицы вне репозитория в `meta.matrix`
вынесены в задачу T-434 (решение оркестратора). В T-402 они только описаны
(`baseline.md` §4 п. 8, §1).

### Итерация 3 (devops-engineer#1, 2026-09-12) — прогон 4 и Nit ревью #2

Ревью #2: «принять», 3 Nit. Прогон 4 E сделал оркестратор (2026-09-12
00:20:02–00:21:43, после перезапуска сервера владельцем в 00:19:00), той же
командой, что прогоны 2–3. Стенд: чистый `llama-server` b10878,
`Qwen3.8-27B-UD-Q3_K_XL.gguf`, 4 × 200 192. Ручной тёплый вызов — 536 мс,
`first_call_ms` = 128 недействителен (прогрев скрипта — 500).

| Прогон | Фаза | p50, мс | p95, мс | tps | `valid_json` | `lang_pass` | Вердикт |
|---|---|---|---|---|---|---|---|
| 4 | phase2 | 2590 | 3533 | 42,3 | 1,0 | 1,0 | pass |
| 4 | phase2-group3 | 3980 | 4720 | 42,8 | 1,0 | 1,0 | pass\* |
| 4 | tick | 2773 | 4488 | 40,3 | 1,0 | 1,0 | n/a |

«Прочее» время — 486–854 мс, ни одного запроса выше 2 с. p95 `phase2` дал
самый длинный ответ фазы: 130 токенов, генерация 3,0 с.

**Итог замера E:** три зачётных прогона на чистом llama.cpp (2, 3, 4) — все
`pass` по NFR-002, p95 `phase2` 2585 / 2916 / 3533 мс. Оговорки (`baseline.md`
§3):
- 40–48 ток/с ниже справочных 70;
- запас исчезает при длинном нарративе: 320 токенов — это ~6,7–7,6 с;
- контекст вне ячеек матрицы;
- тип KV неизвестен;
- VRAM — вся карта;
- все прогоны — за ~20 минут одной ночи.

Решение U-2 не принято: §5 — «—», решение — T-435 (architect#1).

В `baseline.md`:
- шапка;
- §1 — период стенда после перезапуска 00:19:00, дата прогона 4, JSON прогона 4;
- §2.1 — таблицы, разбор латентности, прогрев;
- §2.5 — сравнение с прогоном 4;
- §3 — итог и оговорки;
- §4 пп. 1, 4, 7;
- §5 — отсылка к T-435;
- §6.

Nit ревью #2:
- **N-1.** §2.1: p95 — «генерация самого длинного или почти самого длинного
  ответа плюс не больше 0,8 с». Для `phase2` прогона 2 названо прямо: 92 токена
  при максимуме 97, накладные 0,68 с — самые большие в фазе.
- **N-2.** Оценка при потолке 320 токенов согласована: ~6,7–7,6 с в §2.5 и
  §3, по tps `phase2` 42,3–48,1 (с учётом прогона 4).
- **N-3.** Конец прогона 3 — 00:10:36 (по времени записи CSV). В §2.1
  определено: `unexplained_ms` в `diag-T402-20260911.csv` = латентность −
  `prompt_ms` − `predicted_ms`, а «прочее» в таблицах = латентность −
  `predicted_ms`.

Стенд не трогался, запросов к нему в итерации 3 не было.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#2, Opus) | 2026-09-12 | 1 | 1 | 4 | 2 | вернуть |
| 2 (code-reviewer#2, Opus) | 2026-09-12 | 0 | 0 | 0 | 3 | принять |

Подробности — разделы T-402 в `../review.md`.

## Приёмка (tech-lead)
**Решение: принята** · 2026-09-12 · tech-lead#1, второй экземпляр (Opus — ступень ниже плановой Fable). **Итог:** у конфигурации E три зачётных прогона на чистом llama.cpp (2, 3, 4), все `pass` по NFR-002, оговорки названы. Решение U-2 честно не принято и передано в T-435, дефекты скрипта — в T-434.

**Метод.** Всё проверялось чтением в `.worktrees/T-402`. Ничего не коммитилось и не индексировалось, файлы замера не менялись. К стенду `:8888` запросов не было, `.env` не открывался, временных каталогов не создавалось. Кода в задаче нет: `scripts/`, `Makefile` и `bench-matrix.json` совпадают с базой ветки (`git diff HEAD` пуст). Поэтому `go test` и `golangci-lint` к приёмке не относятся.

| Пункт (раздел T-402 `tasks.md` + общий DoD) | Статус | Чем подтверждено |
|---|---|---|
| Прогон `scripts/llm-bench.sh` на конфигурации владельца | выполнен | E — четыре прогона: 1 через роутер (не зачётный), 2–4 прямо в `llama-server` b10878, 3 и 4 — после перезапусков владельцем. Q36-A3B — один прогон, справочно. На каждый прогон CSV и JSON, в каждом 30 запросов и 0 ошибок |
| Таблица `baseline.md` заполнена | выполнен | §1 — стенд по трём периодам. §2.1 — 12 строк прогонов, 4 ячейки матрицы «не замерено», разбор латентности. §2.2–§2.4 — «не замерено» с причинами. §2.5 — Q36. §6 — фактическая команда |
| Сверка с порогами NFR | выполнен | §3: NFR-002 — `pass` ×3 (p95 `phase2` 2585 / 2916 / 3533 ≤ 5000); группа 3 — справочно; NFR-022/B3 — 90 из 90; NFR-090 — `pass`; NFR-076 — справочно, в пределах; throughput ниже справочного, так и названо |
| Мой пересчёт прогона 4 | сошлось | По `requests[]` файла `bench-E-20260912-0020.json`, правилом скрипта: `percentile` — индекс `int(n·f)` (`llm-bench.sh:419-431`), tps = Σ `completion_tokens` / Σ `predicted_ms`. tick 2773 / 4488 / 40,3; **phase2 2590 / 3533 / 42,3** — p95 даёт `df-10-dawn-silence`, 130 токенов, генерация 3047 мс; group3 3980 / 4720 / 42,8. Совпадает с `cells`, CSV и §2.1. «Прочее» 486–854 мс, 0 из 30 выше 2 с. Средний / максимальный `completion_tokens` — 85 / 155, 88 / 130, 138 / 176. Кэш 0,35 / 0,37 / 0,32. Тем же способом пересчитал прогоны 2 и 3 — тоже сошлось |
| В файлах замера нет путей профиля и ключей | выполнен | Мой грэп без учёта регистра по пяти CSV, пяти JSON и `diag-T402-20260911.csv`: `Users`, `AppData`, `Temp`, `scratch`, `CD86`, имя профиля, `C:/`, `C:\`, шаблон UUID, `api_key`, `bearer`, `authorization`, `token=`, `secret`, `password`, `sk-`, `hf_`, `.env`, `claude` — пусто. Все пять значений `meta.matrix` — нейтральное описание. Тот же грэп по `baseline.md` — пусто. Остался id модели `D:\Models\…\*.gguf` — ревью #1 признало его допустимым как id |
| §3 и §5 не выдают себя за решение U-2 | выполнен | Шапка, итог §3 и §5 говорят «решение U-2 здесь не принимается — T-435». «Базовая конфигурация: —». §2.5 — «справочный материал, не кандидат `decision_order`». `bench-matrix.json` не правился, `decision.chosen` = null |
| Все «не замерено» — с причинами | выполнен | 4 ячейки 8192/16384 × f16/q8_0 — перезапуск стенда запрещён. C и A — Ollama не запущен, моделей нет, смена модели запрещена. E+ — нет `qwen3-8b` и режима роутера. B2 — сервер тёплый, прогрев получил 500. Тип KV — его нет в `/props`, командные строки не читались. VRAM процесса — WDDM её не отдаёт |
| Отклонения от методики названы | выполнен | Копия матрицы с id стенда; пин билда в CSV; ручная правка `meta.matrix` (по строке на файл); недействительный `first_call_ms` и ручной тёплый вызов; ячейка вне матрицы; неизвестный KV; три прогона за ~20 минут одной ночи |
| dev-log | выполнен | запись `<!-- dev-log T-402 -->` в `dev-log.md`, итерации 1–3. Сам файл я не правил |
| Ревью | выполнен | #1 — вернуть (C-1, M-1, Mi-1…Mi-4, N-1, N-2), всё закрыто итерацией 2. #2 — принять; N-1…N-3 закрыты итерацией 3, сверил по тексту §2.1 и §3. Файлы прогона 4 ревьюер пересчитал |

**Решения оркестратора, учтённые при приёмке (не замечания):**
- путь платформы — чистый llama.cpp (решение владельца);
- прогон 1 не засчитан;
- прогоны 3 и 4 выполнил оркестратор той же командой;
- Q36 — справочно;
- решение U-2 и вопрос о Q36 в матрице — T-435;
- дефект прогрева и путь матрицы — T-434.

**Nit без возврата** (на числа и выводы не влияют, можно поправить при коммите):
- `baseline.md:297`: фраза «у каждой модели по одному прогону у Q36» не согласована. По смыслу — «у Q36 один прогон».
- `baseline.md:20`: «Заполняет: architect#1» осталось от шаблона. §1–§4 заполнил исполнитель, за architect#1 только §5.

**Бэклог карточки разнесён:**
- пп. 1 (`build_info` из `/props`), 2 (`timings.prompt_ms`) и 5 (прогрев) — в T-434, вместе с пп. 1–2 бэклога ревью #1;
- пп. 3 (алиас модели в матрице) и 4 (`make bench` с аргументами) — строкой в раздел T-402 файла `tasks.md`;
- в `tasks.md` заведены T-434 (devops-engineer) и T-435 (architect#1).

**Оркестратору.** Раздел «Учёт времени» пуст. Статус `done` поставлен по его указанию, таблицу нужно заполнить до коммита.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 11.09 23:24 | 11.09 23:36 | 12m |
| Разработка 2 | 11.09 23:53 | 11.09 23:59 | 6m |
| Ревью | 11.09 23:59 | 12.09 00:11 | 11m |
| Разработка 3 | 12.09 00:02 | 12.09 00:06 | 3m |
| Разработка 4 | 12.09 00:11 | 12.09 00:18 | 7m |
| Ревью 2 | 12.09 00:18 | 12.09 00:23 | 4m |
| Разработка 5 | 12.09 00:23 | 12.09 00:29 | 6m |
| Приёмка | 12.09 00:29 | 12.09 00:37 | 7m |
| **Всего** | | | **59m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
