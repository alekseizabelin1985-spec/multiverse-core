# T-431: Ревизия контрактов 3: решения EPIC-003, T-430, литерал OLLAMA_*, редакционные

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / системный архитектор |
| Волна | 1 |
| Размер | S |
| Статус | done (принята tech-lead#1 после итерации 4 (возврат приёмки TL-1, TL-2)) |

## Описание и DoD
- **Причина**: очередь вопросов к системному архитектору после T-425 — решения architect#2 по EPIC-003 (коммит `23c845d`), T-430, вопрос исполнителя T-429, замечания приёмки T-428.
- **Состав**:
  1. ADR-028 и design EPIC-003 §14 проверить на соответствие C-01 v1.5, C-05 v1.5, ADR-026, ADR-027 (с «Уточнениями исполнения»). §14.1 — агент встречи на `Has`/`Add` с id из причины, слот ответа и курсор, `Add` в трёх случаях, предел 4 неудачи, остановка без `Add`.
  2. Запрос architect#2: пример «агент встречи: повтор задвоил бы кости» в C-01 v1.4 и ADR-027 п. 3; прочтение C-05 п. 1 «повторно не публикуются».
  3. T-430: `%w` только для `ErrHandlerPanic` при сбое записи в `dead_letters`; `recover` в `runtime.StopAll`.
  4. Литерал `OLLAMA_*` без подстановки в композиции: пропускать, отвергать любой или отвергать отличный от умолчания.
  5. Редакционные замечания из T-428: `OwnershipRules` названа «статичной копией/таблицей»; заголовок «Заглушка для потребителей» в C-01.
- **DoD**:
  - по каждому пункту решение с доводами и место записи;
  - `contracts.md` — новая версия;
  - ADR — «Уточнение исполнения»;
  - отметка в design EPIC-003 §14;
  - решения в разделах T-429 и T-430 индекса EPIC-001.
- **Исполнитель**: system-architect (Opus — ступень ниже плановой).

## Выполнение (system-architect)
2026-09-11 · system-architect#1 (Opus, ступень ниже плановой). Итог:
- контракты — **v0.9**: C-01 → v1.6, C-05 → v1.6, §16 п. 5;
- ADR-027 — «Уточнение исполнения 2», ADR-028 — «Уточнение исполнения»;
- новых ADR нет, новых задач нет;
- код не менялся.

1. **ADR-028 и design EPIC-003 §14 — подтверждено с четырьмя уточнениями.** Записано в design §14.5, ADR-028 «Уточнение исполнения», C-01 v1.6.
   - (а) **Router не гасит повтор раньше агента.** КД §5.1 шаг 1 запоминает событие до доставки, а окно лежит в снапшоте. Поэтому повтор шины и повторная доставка после рестарта до агента не доходят, и §14.1 п. 4–6 не работают. Правило — в C-01 v1.6 («посредник доставки»). Правки КД §5.1, T-224, T-228 — architect#2 и tech-lead#2, срок — до T-230 и T-421.
   - (б) **Граница ответа — пакет.** Предел и откат действуют, только пока пакет не опубликован. `encounter.ended` после факта ничего не откатывает; если его публикация сорвалась, событие дописывает сверка C-14.
   - (в) **Невиновное действие** при незаконченном слоте встаёт в очередь отложенных, а не уходит в `dead_letters`.
   - (г) **Открытие встречи тоже собирается один раз.** Id встречи и `proposal_id` выводятся из причины, выбор NPC записан в «открытии в полёте» до `SpawnChild`. Строки — T-232, T-236.
2. **Пример у `Seen` заменён** (C-01 v1.6 «Дедуп», ADR-027 «Уточнение исполнения 2» п. 5). Прочтение C-05 п. 1 «повторно не публикуются» = «не решаются заново» подтверждено (C-05 v1.6 п. 1, ADR-027 УИ2 п. 7).
3. **T-430.**
   - `recover` в `StopAll` — да. У `StopAll` появляется параметр `*slog.Logger` (`contract-change`), ошибки отката `StartAll` объединяются через `errors.Join`.
   - `%w` для `ErrHandlerPanic` — нет: цепочка не меняется. Ветку `DLQ == nil` перевести на `%v` для причины.
   - Записано: C-01 v1.6 (рантайм; «Паника обработчика» п. 4), раздел T-430 индекса, DoD обновлён.
4. **Литерал `OLLAMA_*` — вариант (б), отвергать любой.** Записано: `contracts.md` §16 п. 5 (v0.9), раздел T-432 индекса (вынесено из T-429 решением оркестратора) — строка к DoD, фикстуры и фраза в `infrastructure.md` §3.1.1 п. 8.
5. **Редакционные правки.**
   - `diagrams/c4-component-foundation.md:23` и `epics/EPIC-002-state-mechanics/design.md:62`: `OwnershipRules` названа единственной истиной (ADR-025).
   - Заголовок в C-01 заменён на «Реализации для потребителей».
   - В разделе T-428 индекса отмечено «выполнено в T-431».

## Итерация 2 (system-architect, по ревью #1)
2026-09-11. Ревью #1 (code-reviewer#2): вердикт «принять», Minor 10, Nit 3. Закрыто всё. Код не менялся.
- **Mi-1.** Шапки: в `contracts.md` — «Версия 0.9», «ADR-022…ADR-028»; в design EPIC-003 — «Версия 0.3a».
- **Mi-2.** Пометка v1.3a возвращена за фразу про `runtime.StartAll`, до абзаца v1.6.
- **Mi-3.** Предусловие двух шагов и «своё окно без `IDs`/`Restore`» снова в основном абзаце «Дедуп».
- **Mi-4.** `shared/runtime/runtime_test.go:320` назван в T-430 и в сводке v0.9.
- **Mi-5.** Ошибки отката `StartAll` пишутся в той же строке через «; », а не через `errors.Join` (C-01 v1.6, T-430; тест: в тексте нет перевода строки).
- **Mi-6.** Форма ветки `DLQ == nil` записана прямо: без `%w` и без нового значения ошибки; Go-API не меняется.
- **Mi-7.** DoD T-430: поведение описывают doc-комментарии в `lifecycle.go`, README не заводится.
- **Mi-8.** §16 п. 5: правило 5 проверяет только `OLLAMA_HOST`.
- **Mi-9.** Design §14.5 п. 1 и C-01 «Посредник»:
  - шаги 3–7 перечислены с требованием идемпотентности;
  - `budget.Observe` отдан T-227;
  - ошибка одного получателя не отменяет доставку остальным, шине возвращается любая из ошибок.
- **Mi-10** (решение оркестратора):
  - строки DoD «(T-431, 2026-09-11)» внесены в индекс EPIC-003 — в T-224, T-227, T-228, T-232, T-236;
  - в «Что» у T-224 теперь шаги 1–4 и 6–8;
  - КД §5.1: исправлены шаги 1, 3, 5, добавлен шаг 8 и абзац «Почему `Add` после доставки».
- **T-432** заведена в индексе EPIC-001, строка DoD литерала перенесена из T-429. Ссылки в `contracts.md` (сводка v0.9, §16 п. 5) и в этой карточке ведут на T-432.
- **N-1** — карточка переведена в CRLF. **N-2** — ADR-025:29 ссылается на новый заголовок. **N-3** — «первую ошибку» снято.

## Итерация 3 (system-architect, по ревью #2)
2026-09-11. Ревью #2 (code-reviewer#2): вердикт «принять», Minor 1, Nit 3. Закрыто всё. Код не менялся.
- **R2-Mi-1.** Повтор `tick.fired` гасится, если `tick_seq` не больше последнего закрытого тика — выполненного или прерванного (`tick.aborted`).
  - Ошибка тика закрывает тик: `tick.aborted` публикуется с id из причины (`parts` — `tick_seq`).
  - Шине ошибка уходит, только если `tick.aborted` не опубликован. Тогда повтор его допубликовывает, а тик не выполняет.
  - Где записано: design §14.5 п. 1 (шаг 6), КД §5.1 шаг 6, строка «(T-431, 2026-09-11)» в DoD T-227 с тестом.
- **R2-N-1.** В КД, шаг 8, условие — «шаги 3–7».
- **R2-N-2.** Router возвращает шине ошибку первого по порядку `Recipients(ev)`. Одинаково в C-01 «Посредник доставки», design §14.5, КД шаг 5 и T-224.
- **R2-N-3.** В DoD T-236 добавлена строка: id `llm.output`, учтённые в окне бюджета, и последний закрытый `tick_seq` входят в снапшот.
- **Риск ревьюера — многострочная паника.** Переводы строк в тексте паники (`Start` и `Stop`) экранируются как `\n` и `\r`: строка ошибки `StartAll` всегда одна, поле лога `panic` — как есть. Записано в C-01 v1.6 (рантайм) и в T-430 (решение и тест).

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#2) | 2026-09-11 | 0 | 0 | 10 | 3 | принять |
| 2 (code-reviewer#2) | 2026-09-11 | 0 | 0 | 1 | 3 | принять |

## Итерация 4 (system-architect, по возврату приёмки tech-lead#1)
2026-09-11. Код не менялся.
- **TL-1 — счётчик ошибок роли.** Правило T-228 «3 ошибки → `agent.stopped reason=error`» считает события, а не доставки.
  - Счётчик растёт на ошибке или перехваченной панике роли при первой доставке события. Повтор того же `ev.ID` его не увеличивает.
  - Отказ шины при публикации и отмена в счётчик не входят.
  - Останавливают агента три разных события подряд, успешная доставка другого события сбрасывает счётчик.
  - Если роль остановлена во время доставки события, Router до `Add` или рестарта помнит это и на повторе возвращает ту же ошибку. Поэтому событие уходит в `dead_letters`, а не коммитится без получателя.
  - Цена названа: падение процесса между повторами. Тогда событие после рестарта коммитится без получателя, но след остаётся — `agent.stopped reason=error` и `Error` роли.
  - Записано: design §14.5 п. 1, T-228 («Что» и строка «(T-431, 2026-09-11)» с тремя тестами), T-421 тест 2 («агент жив: отказы шины счётчик не считает»).
- **TL-2 — id встречи при открытии по тику.** Id встречи и `proposal_id` выводятся из события открытия и scope: id предложения создания — `WithCauseID(<тип scope>, <id scope>)`, id встречи — `encounter-<id предложения>`.
  - Довод в ADR-028 УИ исправлен: «на одну причину и один scope — не больше одного подъёма».
  - Записано: ADR-028 УИ (довод к C-01 и п. 1), design §14.5 п. 4, T-232 (тест «один тик — два игрока — разные id, оба создания приняты»).
- **T-432.**
  - Ветка `task/T-432-ollama-literal`.
  - В DoD добавлены «`make ci` зелёный» и «dev-log заполнен».
  - Порядок: от эпика после слияния T-429.

## Приёмка (tech-lead)
2026-09-11 · tech-lead#1. **Решение: возврат** (итерация 4, system-architect). Два противоречия между строками «(T-431, 2026-09-11)» и прежними строками DoD EPIC-003. Правка документная, в пределах S. Остальное принято.

**Проверено и принято:**
- **Вопросы 1–5.** У каждого есть решение с доводами и местом записи: design EPIC-003 §14.5, ADR-027 УИ2 п. 5–7, ADR-028 УИ, C-01 v1.6, C-05 v1.6, §16 п. 5 (v0.9), разделы T-428/T-429/T-430/T-432 индекса.
- **T-430 готова к старту.** В задаче есть:
  - `StopAll(ctx, contexts, log *slog.Logger)` с меткой `contract-change`;
  - все четыре вызова сверены с кодом: `lifecycle.go:26`, `serve.go:340`, `serve.go:349`, `runtime_test.go:320`;
  - формат отката «; » с `%w` у ошибки старта (довод сверен с `serve.go:62`);
  - экранирование `\n`/`\r` в тексте паники `Start` и `Stop`, тест многострочной паники;
  - ветка `DLQ == nil` на `%v` — сверено с `delivery.go:172` — и случай сторожа;
  - doc-комментарии `lifecycle.go`, мутант на `recover`, прогоны;
  - исполнитель, ветка `task/T-430-panic-in-stop`, файлы.
- **T-432 готова по существу.** В DoD три фикстуры, `expect-text`, `make compose-lint`, фраза в `infrastructure.md`. Порядок относительно T-429 и исполнитель указаны. Неблокирующие пункты — ниже.
- **Остальные строки индекса EPIC-003 противоречий не дают:**
  - T-224 — окно Router, проекции;
  - T-227 — `budget.Observe`, закрытие тика: схема `tick.aborted` принимает `reason` как свободный текст, КД §7.2 уже публикует `tick.aborted` при ошибке тика;
  - T-236 — обе строки;
  - T-232 — запись «открытие в полёте» как таковая.

  Строки architect#2 (T-223, T-225, T-229, T-230, T-421, T-422) и tech-lead#2 из T-415 (T-223, T-229, T-421) новые строки только дополняют.
- **Гигиена.** Во всех 12 файлах число CR равно числу LF, в конце ровно один `\r\n`. Истории версий подняты: `contracts.md` 0.9, C-01 v1.6, C-05 v1.6, design 0.3a, ADR-027 УИ2, ADR-028 УИ.

**Возврат — что исправить:**
1. **TL-1. Счётчик «3 ошибки → `agent.stopped reason=error»` (T-228, «Что») против повторов, которые теперь доходят до агента.**
   - Строки T-431 в T-224 и T-228 возвращают ошибку роли шине, и шина вызывает обработчик 4 раза (ADR-007 п. 6, `delivery.go`: три повтора).
   - Если счётчик T-228 считает каждую неудачную доставку, агент встречи остановится на 3-й доставке одного и того же действия. Это раньше предела 4 из §14.1 п. 5.
   - Противоречие 1: T-421, тест 2 (architect#2) — «постоянный отказ: действие в `dead_letters`, ответ выброшен с откатом, следующее действие решается от фактов». Для этого агент должен пережить 4 неудачи.
   - Противоречие 2: DoD T-228 «паника роли не роняет процесс и не теряет событие». При детерминированной панике доставки 1–3 паникуют, агент остановлен, а 4-я доставка не находит получателя. `Route` возвращает `nil`, Router делает `Add`, событие закоммичено мимо `dead_letters`.
   - *Нужно:* записать, что считает этот счётчик. Например, повтор того же `ev.ID` счётчик не увеличивает, а отказ шины при публикации (T-223/T-421) и отмена в счётчик не входят. Записать в design §14.5 п. 1 и строкой «(T-431, …)» в T-228, с тестом: паника роли на каждой доставке одного события → событие в `dead_letters`, агент жив. Если архитектор выберет другое правило, привести к нему T-421, тест 2.
2. **TL-2. Id встречи `encounter-<id события>` при открытии по `tick.fired` (T-232, строка T-431; ADR-028 УИ п. 1; design §14.5 п. 4).**
   - У GM региона `encounter{detect_on: tick, perception: all}` (КД `swarm-llm-laws.md:733`, `api-contracts.md:522`, `:583`): один тик проверяет всех игроков региона и может открыть встречи двоим (с разными NPC).
   - Прежние строки T-232 это требуют. FR-124: встреча не позднее одного активного интервала после входа. T-425: причина — вход игрока или `tick.fired`.
   - Формула двойника верна только для входа, где на одну причину приходится один игрок. Для тика два предложения получат один id встречи: второе отвергнет State (`duplicate_entity`), и FR-124 для второго игрока нарушен. Совпадут и id `encounter.started` (`parts` — id встречи).
   - Та же неточность в ADR-028 УИ: «на одну причину открытия приходится не больше одного подъёма агента встречи». Для `agent.spawned` это безвредно (`parts` = `agent.id`), но довод неверен.
   - *Нужно:* id встречи и `proposal_id` выводятся из события открытия **и** игрока или scope, например `WithCauseID` / UUIDv5 с `parts` = scope. Строка T-232 получает тест: один `tick.fired` открывает встречи двум игрокам с разными NPC → разные id встречи и `proposal_id`, оба создания приняты. Поправить ADR-028 УИ п. 1 и довод к C-01, design §14.5 п. 4.

**Не блокирует, внести в той же итерации:**
- **N-TL-1.** У T-432 нет слага ветки. Предлагаю `task/T-432-ollama-literal`.
- **N-TL-2.** В DoD T-432 нет стандартных пунктов: «`make ci` зелёный», «dev-log заполнен».

Повторная приёмка — после итерации 4. Ревью по TL-1/TL-2 — на усмотрение оркестратора: правка точечная, достаточно сверки тимлидом.

### Повторная приёмка (итерация 4)
2026-09-11 · tech-lead#1. **Решение: принята.** Итог: контракты v0.9 и уточнения ADR-027/028 записаны; строки DoD EPIC-003 согласованы с прежними; T-430 и T-432 готовы к старту. Нового ревью не было — по решению оркестратора, итерацию 4 сверил тимлид.

**TL-1 закрыт.** Правило записано в design §14.5 п. 1 и в T-228 («Что» и строка с тремя тестами), T-421, тест 2, уточнён. Сверка с прежними строками:
- T-228 DoD «паника роли не теряет событие»: детерминированная паника → 4 доставки → `dead_letters`, агент жив — выполнено;
- T-421, тест 2 (architect#2): отказы шины счётчик не считают, агент переживает предел 4 — выполнено; тест 4 (отмена) с правилом согласован;
- T-422 (architect#2): остановка `reason=error` по отказу создания — доставка без ошибки, повтора нет, память Router не задействуется — противоречия нет;
- T-223 и T-229 (tech-lead#2, из T-415): отмена не ошибка роли — согласовано.

**Остаточная дыра TL-1 приемлема, строка в бэклоге и задача не нужны.**
- *Почему дыра редкая:* нужны три разных события с ошибкой роли подряд — сам по себе громкий дефект: два события уже в `dead_letters`, `Error` у роли. Вдобавок рестарт должен случиться внутри окна повторов шины — около 2,6 с (100 + 500 + 2000 мс).
- *Почему не тихая:* след остаётся — `agent.stopped reason=error`, `Error` роли, `dead_letters` предыдущих событий.
- *Почему задача не окупается:* закрыть дыру целиком нельзя. При падении снапшот старше памяти Router, так что сериализация памяти в снапшот закрывает только плановую остановку.

Одна поправка к формулировке цены — неблокирующая, для tech-lead#2 при старте T-228: память Router теряет и плановый рестарт между повторами, а не только падение процесса. Класс и оценка те же.

**TL-2 закрыт.** Id предложения создания — `WithCauseID(<тип scope>, <id scope>)` от события открытия, id встречи — `encounter-<id предложения>`. Записано в ADR-028 УИ (довод к C-01 и п. 1), design §14.5 п. 4, T-232 (тест «один тик — два игрока — разные id, оба создания приняты»). Сверка с прежними строками T-232:
- T-416: `encounter.started` с `parts` = id встречи — теперь различается по scope;
- T-416: «один NPC — одна встреча» — не затронуто;
- T-425: причина — вход или `tick.fired`, `opened_by_event_id` — согласовано;
- architect#2: подъём до предложения, `Spawner.Alive` — согласовано.

T-236 (запись «открытие в полёте» с id встречи и `proposal_id`) и T-422 (фаза «открывается») формулы не касаются. Части `(<тип scope>, <id scope>)` не совпадают с частями других производных от того же события: у `agent.spawned` это `agent.id`, у `encounter.started` — id встречи, у `tick.aborted` — `tick_seq`.

**T-432:** ветка `task/T-432-ollama-literal`, в DoD добавлены `make ci` и dev-log, порядок — после слияния T-429. Готова.

**Гигиена:**
- во всех 12 файлах число CR равно числу LF, в конце ровно один `\r\n`;
- в индекс EPIC-003 tech-lead#1 при приёмке внёс строку журнала 0.2.5 о правках T-431 (по прецеденту T-416): её не было, а строки DoD правились в пяти задачах.

**Неблокирующее — tech-lead#2 при старте T-228:**
- в строке T-228 сказано «если роль остановлена во время доставки», а в design §14.5 — «остановлена счётчиком». Читать по design: память Router нужна только на повторе, то есть после доставки с ошибкой;
- цена TL-1 — см. выше.

## Приёмка (оркестратор)
Не принята.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 11.09 21:40 | 11.09 21:57 | 16m |
| Ревью | 11.09 21:57 | 11.09 22:08 | 11m |
| Разработка 2 | 11.09 22:08 | 11.09 22:15 | 7m |
| Ревью 2 | 11.09 22:15 | 11.09 22:20 | 5m |
| Разработка 3 | 11.09 22:20 | 11.09 22:22 | 2m |
| Приёмка | 11.09 22:22 | 11.09 22:29 | 6m |
| Разработка 4 | 11.09 22:29 | 11.09 22:33 | 3m |
| Приёмка 2 | 11.09 22:33 | 11.09 22:36 | 3m |
| **Всего** | | | **56m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
