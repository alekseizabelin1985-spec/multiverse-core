# T-441: Delivery — не писать «parked», если запись в dead_letters не удалась

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / TEAM-1 |
| Волна | 1 (бэклог) |
| Размер | XS |
| Исполнитель | developer#1 (Opus — ступень ниже плановой Fable) |
| Статус | done (принята tech-lead#2 после ревью #1 и итерации 2 (Minor закрыты)) |
| Итераций ревью | 1 |
| Начата / завершена | 2026-09-13 10:31 / 2026-09-13 11:08 |
| Метка | нет: Go-API и контракт не меняются, меняется только строка лога (правка `shared/eventbus` — с ревью system-architect по карте владения) |

## Описание и DoD
Главный источник — раздел «### T-441» в `tasks.md`. Основание — ревью #1 T-436 (Mi-1) и строка бэклога в разделе T-436. Кратко:
- «parked» в логе — только после успешной записи в `dead_letters`.
- Запись не удалась — строка «not parked» с причиной. Уровень по исходу: закрытая шина — `Warn`, сбой на живой шине — `Error`.
- Возврат ошибок, цепочка ошибки (C-01 v1.6) и коммит не меняются. Go-API не меняется.
- Тест на лог обеих ветвей; мутант «безусловный `Warn`» краснеет.

## Выполнение (developer)
developer#1 (Opus), TEAM-1, ветка `task/T-441-no-false-parked-warn` (от эпика `adadca5`), папка `.worktrees/T-441`. Не закоммичено: коммит делает оркестратор.

### Итог по пунктам DoD

| # | Пункт | Что сделано | Чем проверено | Мутант → результат |
|---|---|---|---|---|
| 1 | «parked» только после записи | Строку успеха пишет `deadLetter`: текст приходит параметром `parked` и пишется только после `WriteDeadLetter` без ошибки. `Deliver` (повторы исчерпаны) передаёт «event parked in dead letters», `DeliverRaw` — «undecodable message parked in dead letters». Ветки валидации и паники передают пустую строку: они уже залогировали, почему событие идёт в `dead_letters` («event rejected on read», `Error` паники), и число их записей не меняется | `TestDeliverLogsParkedOnlyOnceTheDeadLetterIsWritten`, ветка «the write succeeds»: одна запись `WARN` с нужным текстом, в `error` — причина | M1w, M2, M5 — **красные** (таблица мутантов) |
| 2 | «not parked» при неудачной записи | Новый `logNotParked`: «event not parked in dead letters; it stays uncommitted and will be delivered again», поля события, `bus_closed`, `error` — возвращаемая ошибка (ошибка записи и `cause: …` текстом). Пишется и при сбое `WriteDeadLetter`, и в ветке без приёмника | тот же тест: 4 варианта неудачи × 2 пути. `TestDeliverLogsAPanicThatCouldNotBeParked`: `Error` паники, затем `Warn` «not parked» | M1 — **красный** |
| 3 | Уровень по исходу | `busClosed(err)` смотрит только на ошибку приёмника. `ErrClosed` (membus и `Kafka.writer` закрытой шины) или `io.ErrClosedPipe` (писатель kafka-go, взятый из `Kafka.writer` до `Close` и вызванный после него; `Kafka.write` оборачивает эту ошибку) → `Warn`, `bus_closed=true`. Иная ошибка и отсутствие приёмника → `Error`, `bus_closed=false` | тест: `ErrClosed` и `io.ErrClosedPipe` → `WARN`; `errSink` и нет приёмника → `ERROR` | M3, M4 — **красные** |
| 4 | Возврат и коммит не меняются | Возвращаемые ошибки прежние: те же `fmt.Errorf`, `%w` — только на ошибку записи. Лог на ветвление не влияет | прежние `TestDeliverReportsAFailingSink*`, сторож `panic_stop_test.go`; новый тест проверяет `nil` или ошибку приёмника в каждой ветке | — |
| 5 | Go-API не меняется | Изменена только неэкспортируемая `deadLetter` (+`pos`, `parked`); добавлены неэкспортируемые `busClosed` и `logNotParked` | `go build ./...`, `go vet ./...` | — |

### Мутанты
Копия рабочей папки в scratch (`mktemp -d`, `git ls-files -co --exclude-standard` без `services/` и `Docs/`), не `-overlay`. Базовый прогон копии зелёный. Мутации вносил скрипт точной заменой строк (`assert count == 1`), каждая показана `diff`. После каждой файл восстановлен и сверен `cmp` с эталоном; в конце `delivery.go` копии побайтно равен рабочей папке. Копия удалена по сохранённому точному пути.

| # | Мутант | Результат |
|---|---|---|
| M0 | контрольный: `func broken( {` в конце `delivery.go` | **красный**: `delivery.go:358:14: syntax error` |
| M1w | путь повторов: `d.log(ctx, "event parked in dead letters", …)` до записи, `parked=""` — безусловный `Warn`, как до задачи | **красный**: 4 подтеста «retries spent, …» — `log = WARN "event parked in dead letters" although the dead letter was not written` |
| M1 | M1w плюс `logNotParked` ничего не пишет — полный откат к прежнему логу | **красный**: 8 подтестов и `TestDeliverLogsAPanicThatCouldNotBeParked` |
| M2 | `DeliverRaw`: безусловный `Warn` до записи | **красный**: 4 подтеста «undecodable message, …» |
| M3 | `logNotParked` всегда `Warn` | **красный**: 4 подтеста (сбой на живой шине, нет приёмника) |
| M4 | `busClosed` не узнаёт `io.ErrClosedPipe` | **красный**: 2 подтеста «kafka writer closed under it» (в итерации 2 подтест переименован в «kafka writer closed before the write») |
| M5 | строка успеха перед записью, а не после | **красный**: подтесты неудачной записи на обоих путях |

Первая версия M1 (инверсия `d.Log == nil` → `!= nil`) не засчитана: при `Log == nil` она разыменовала nil в `logNotParked` и уронила чужой тест паникой. Переделана на условие, которое всегда выходит.

### Изменённые файлы
- `shared/eventbus/delivery.go`: в `Deliver` и `DeliverRaw` строка «parked» перенесена в `deadLetter`; у `deadLetter` параметры `pos` и `parked`, лог после записи, `logNotParked` при неудаче и без приёмника; новые `busClosed` и `logNotParked`.
- `shared/eventbus/delivery_test.go`: `TestDeliverLogsParkedOnlyOnceTheDeadLetterIsWritten` (2 пути × 5 приёмников), `TestDeliverLogsAPanicThatCouldNotBeParked`, помощник `messagesOf`.
- `Docs/dev-team/epics/EPIC-001-foundation/tasks.md` (раздел T-441), `tasks/T-441.md` (эта карточка), `dev-log.md` (запись в конце).

### Прогоны (go1.26.8 windows/amd64, golangci-lint 2.13.2)
- `go build ./... && go vet ./...`: 0; `go vet -tags integration ./shared/eventbus/... ./shared/testkit/contract/`: 0.
- `gofmt -l shared cmd test`: пусто.
- `go test -short -count=1 ./...`: 27 пакетов ok, 0 FAIL.
- `golangci-lint run ./...`: 0 issues; `golangci-lint run --build-tags integration ./shared/eventbus/... ./shared/testkit/contract/...`: 0 issues.
- Contract-набор на Redpanda не запускался. Возврат и коммит не менялись, а `Delivery` одна на обе шины и покрыта unit-тестами с приёмником, который отвечает как закрытые membus и kafka (`ErrClosed`, `io.ErrClosedPipe`). Docker не использовался.
- `-race` недоступен (нет cgo).

### Отклонения от задачи
- По коду отклонений нет. Сверх формулировки Mi-1 то же исправлено в `DeliverRaw` (там строка «parked» тоже шла до записи) и в ветке без приёмника (теперь и она пишет «not parked»). Иначе на соседних путях осталась бы та же ложная строка.
- Раздел T-441 в `tasks.md` стоит после T-439: раздела T-440 в этой ветке нет, он в `task/T-440-overview-order-ownership`. При слиянии обеих веток в эпик порядок разделов — T-440, затем T-441.

### Предложения в бэклог
1. Строка `logPanic` «event handler panicked; the event goes to dead letters» тоже пишется до записи и говорит о намерении. Её не менял: это след `service_panics` (NFR-012, C-01 v1.5 п. 3), текст и число записей закреплены тестами. После T-441 при неудачной записи за ней идёт строка «not parked», так что ложного итога в логе нет. Переформулировать ли её — вопрос к владельцу метрики, не блокирует.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#1) | 2026-09-13 | 0 | 0 | 2 | 1 | принять |

## Итерация 2 (developer, по ревью #1)
2026-09-13, developer#1 (Opus). Вердикт ревью #1 — «принять». По решению оркестратора Mi-1, Mi-2 и N-1 исправлены до приёмки. Гонку в `Kafka.Close` (п. 1 бэклога ревьюера) не трогал — это отдельная задача.

- **Mi-1 — исправлено** (`delivery_test.go`, таблица `sinks`). Ошибки приёмника теперь подаются в том виде, в каком их отдают шины:
  - «membus or kafka closed» — голый `ErrClosed`, как сегодня отдают `Kafka.writer` и `topic` у membus;
  - «a closed bus, wrapped» — `fmt.Errorf("eventbus: write to %s: %w", TopicDeadLetters, ErrClosed)`: обёртка приёмника не должна превращать штатную остановку в `Error`;
  - «kafka writer closed before the write» — `io.ErrClosedPipe`, обёрнутый так же, как в `Kafka.write` (`kafka.go:402-403`). Голый `io.ErrClosedPipe` убран: так его не отдаёт ни одна шина.
- **Mi-2 — исправлено.** `logNotParked` ставит `slog.Bool("handled", false)` на обоих уровнях, как `logPanic` (NFR-033): событие не учтено ни при `Warn`, ни при `Error`. Тест требует, чтобы поле было и равнялось `false`, в каждой ветке неудачной записи.
- **N-1 — исправлено.** Комментарий `busClosed` переписан. `io.ErrClosedPipe` получает писатель, взятый из `Kafka.writer` до `Close` и вызванный после него. Запись, уже вошедшая в `WriteMessages`, `Close` дожидается, и так она не падает. Там же сказано, что `Kafka.write` ошибку оборачивает, поэтому нужен `errors.Is`. Подтест переименован, п. 3 таблицы DoD выше поправлен.

### Мутанты итерации 2
Новая копия рабочей папки в scratch (`mktemp -d`, `git ls-files -co --exclude-standard` без `services/` и `Docs/`), не `-overlay`. Базовый прогон копии зелёный. Мутации вносил скрипт точной заменой (`assert count == 1`), каждая показана `diff`. После каждой файл восстановлен и сверен `cmp`; в конце `delivery.go` копии побайтно равен рабочей папке. Копия удалена по сохранённому точному пути.

| # | Мутант | Результат |
|---|---|---|
| M0 | контрольный: `func broken( {` перед `truncateRaw` | **красный**: `delivery.go:239:14: syntax error` |
| R1 | ревьюера: `errors.Is(err, io.ErrClosedPipe)` → `err == io.ErrClosedPipe` | **красный**: 2 подтеста «kafka writer closed before the write» — `log = ERROR …, want WARN …`, `bus_closed = "false", want true`. `golangci-lint run ./shared/eventbus/` на этом мутанте — 0 issues: ловит только тест |
| R2 | ревьюера: `errors.Is(err, ErrClosed)` → `err == ErrClosed` | **красный**: 2 подтеста «a closed bus, wrapped» — то же |
| H1 | строка `slog.Bool("handled", false)` удалена | **красный**: все подтесты неудачной записи — `handled = "" (present=false), want false` |
| H2 | `slog.Bool("handled", !closed)` | **красный**: подтесты `Error` — `handled = "true" (present=true), want false` |

Первый запуск H2 не состоялся: шаблон замены совпал и со строкой `logPanic`, скрипт остановился на `assert`, мутация не применялась. Шаблон уточнён отступом, мутант прогнан заново.

### Прогоны итерации 2 (go1.26.8 windows/amd64, golangci-lint 2.13.2)
- `go build ./... && go vet ./...` — 0; `go vet -tags integration ./shared/eventbus/... ./shared/testkit/contract/` — 0.
- `gofmt -l shared cmd test` — пусто.
- `go test -short -count=1 ./...` — 27 пакетов ok, 0 FAIL.
- `golangci-lint run ./shared/eventbus/...`, `golangci-lint run --build-tags integration ./shared/eventbus/... ./shared/testkit/contract/...` и `golangci-lint run ./...` — 0 issues.
- Redpanda и Docker не запускались. `-race` недоступен (нет cgo).

## Приёмка (tech-lead)

tech-lead#2 (TEAM-1), 2026-09-13, модель Opus. **Решение: принята.**

**Итог.** «Parked» в логе `Delivery` пишется только после успешной записи в `dead_letters`, на обоих путях (`Deliver`, `DeliverRaw`). При неудачной записи и без приёмника пишется «not parked» с `handled=false`, `bus_closed` и ошибкой. Уровень `Warn` на закрытой шине, `Error` на живой. Возврат, цепочка ошибки C-01 v1.6 и коммит не изменились.

Основание: ревью #1 («принять»; Minor 2, Nit 1). Итерация 2 закрыла Mi-1, Mi-2 и N-1 до приёмки. Ревью #2 не проводилось (решение оркестратора), поэтому исправления итерации 2 проверены здесь, в том числе своими мутантами.

**DoD** (раздел T-441 в `../tasks.md`). Код сравнивался с `adadca5` (HEAD ветки, `git diff`): коммитов в ветке нет. Изменены только `shared/eventbus/delivery.go`, `delivery_test.go` и документы задачи.

| # | Пункт | Чем подтверждён |
|---|---|---|
| 1 | Запись удалась → одна строка «parked» (`Deliver` и `DeliverRaw`) | Прочитан `deadLetter`: строка `parked` пишется только после `WriteDeadLetter` без ошибки, ранних выходов после записи нет. Подтесты «…, the write succeeds» требуют ровно одну запись `WARN` с нужным текстом, позицией и причиной |
| 2 | Запись не удалась (закрытая шина, закрытый писатель, сбой на живой шине, нет приёмника) → «parked» нет, есть «not parked» нужного уровня с причиной | Таблица `sinks`: 5 исходов неудачи × 2 пути. Проверяются отсутствие «parked», ровно одна запись, текст, уровень, `bus_closed`, `handled`, причина и ошибка записи в `error`, возврат (`errors.Is` на ошибку приёмника). Мои мутанты W1, W2, L1, N1 — красные (ниже) |
| 3 | Паника с неудачной записью → `Error` паники плюс «not parked» | `TestDeliverLogsAPanicThatCouldNotBeParked`: 2 записи, `ERROR` с `panic=boom`, затем `WARN` «event not parked…». Мутанты L1 и C1 его краснят |
| 4 | Мутант «безусловный `Warn` до записи» краснеет; контрольный первым; копия в scratch, не `-overlay` | У исполнителя M1w и M2 красные (итерация 1). Повторил сам как W1 и W2 — красные. Порядок и условия — в разделе «Мутанты приёмки» |
| 5 | `go build ./... && go vet ./...`, `go test -short -count=1 ./...`, `golangci-lint run ./...` | go1.26.8 windows/amd64, golangci-lint 2.13.2, рабочая папка. `go build ./... && go vet ./...` — 0. `go vet -tags integration ./shared/eventbus/... ./shared/testkit/contract/` — 0. `gofmt -l shared/eventbus` — пусто. `go test -short -count=1 ./shared/eventbus/...` — ok (`eventbus`, `membus`). `go test -short -count=1 ./...` — 27 пакетов ok, 0 FAIL. `golangci-lint run ./shared/eventbus/...`, `run ./...` и `--build-tags integration ./shared/eventbus/... ./shared/testkit/contract/...` — 0 issues. `-race` недоступен (нет cgo) |
| 6 | Возврат, цепочка C-01 v1.6, коммит, Go-API не менялись | Построчно по `git diff`: ветка `DLQ == nil` и ветка ошибки записи возвращают те же `fmt.Errorf` с тем же `%w`/`%v`. Причина в цепочку не входит. Лог на ветвление не влияет. Экспортируемые имена не менялись: `deadLetter` неэкспортируемая, `busClosed` и `logNotParked` новые и тоже неэкспортируемые. Сторож `panic_stop_test.go` и `TestDeliverReportsAFailingSink*` зелёные |
| 7 | dev-log заполнен | Записи T-441 и «Итерация 2» в `dev-log.md` есть. Строки CRLF, число CR равно числу LF в `dev-log.md`, `tasks.md` и карточке |

**Проверка итерации 2.**
- **Mi-1 — закрыт.** Ошибки в таблице `sinks` подаются так, как их отдают шины. `kafkaDeadLetters.WriteDeadLetter` → `Kafka.write` оборачивает ошибку писателя: `fmt.Errorf("eventbus: write to %s: %w", …)`, `kafka.go:402-403`. Тест подаёт `io.ErrClosedPipe` в той же обёртке. Голый `ErrClosed` соответствует `Kafka.writer` (`kafka.go:412`) и `membus` (`membus.go:476`, `deadLetters.WriteDeadLetter` его не оборачивает). Обёрнутый `ErrClosed` закрепляет `errors.Is`. Голого `io.ErrClosedPipe` в таблице больше нет. Мутанты R1 (`err == io.ErrClosedPipe`) и R2 (`err == ErrClosed`) повторены мной — оба красные.
- **Mi-2 — закрыт.** `logNotParked` ставит `slog.Bool("handled", false)` на обоих уровнях. Тест требует, чтобы поле было и равнялось `false`, во всех 10 подтестах неудачи. Мой мутант H1 (строка удалена) красный на всех 10. Счёт `service_panics` (`metrics.md:114`: `level=error` без `handled=true`) правильный: `Error` на живой шине засчитывается, `Warn` под `Close` — нет.
- **N-1 — закрыт.** Комментарий `busClosed`: `io.ErrClosedPipe` получает писатель, взятый из `Kafka.writer` до `Close` и вызванный после него. Запись, уже вошедшая в `WriteMessages`, `Close` дожидается. Там же объяснено, зачем `errors.Is`. Подтест переименован в «kafka writer closed before the write», п. 3 таблицы исполнителя поправлен.

**Мутанты приёмки.** Копия рабочей папки в scratch (`git ls-files -co --exclude-standard` без `services/` и `Docs/`, каталог с точным именем), не `-overlay`. Базовый прогон копии `go test -short -count=1 ./shared/eventbus/` зелёный. Мутации вносил скрипт точной заменой (`assert count == 1`), после каждой файл восстановлен побайтно. В конце `delivery.go` копии сверен `cmp` с рабочей папкой — равен. Копия и скрипт удалены по сохранённым точным путям.

| # | Мутант (`delivery.go`) | Результат |
|---|---|---|
| M0 | контрольный: `func broken( {` перед `truncateRaw` | **красный**: `delivery.go:239:14: syntax error` |
| R1 | `errors.Is(err, io.ErrClosedPipe)` → `err == io.ErrClosedPipe` | **красный**: 2 подтеста «…, kafka writer closed before the write» |
| R2 | `errors.Is(err, ErrClosed)` → `err == ErrClosed` | **красный**: 2 подтеста «…, a closed bus, wrapped» |
| H1 | удалён `slog.Bool("handled", false)` | **красный**: 10 подтестов неудачи |
| L1 | уровень перевёрнут (`if !closed` → `Warn`) | **красный**: 10 подтестов и `TestDeliverLogsAPanicThatCouldNotBeParked` |
| C1 | `busClosed(cause)` вместо `busClosed(err)` | **красный**: 6 подтестов закрытой шины и тест паники |
| W1 | DoD: `Deliver` пишет «event parked in dead letters» до записи, `parked=""` | **красный**: 5 подтестов «retries spent, …» неудачи |
| W2 | `DeliverRaw` пишет «undecodable message parked…» до записи | **красный**: 5 подтестов «undecodable message, …» неудачи |
| N1 | ветка без приёмника с `closed=true` | **красный**: 2 подтеста «…, no sink» |

**Совместимость текстов логов.**
- «event parked in dead letters» и «undecodable message parked in dead letters» не изменились. Грэп по `*.go` и `*.md` вне документов задачи и `services/`: порядок строки относительно записи не закрепляют ни тест, ни документ.
- `logPanic` не менялась: «event handler panicked; the event goes to dead letters», `Error`, `handled=false`. Её закрепляют C-01 v1.5 п. 3 (`contracts.md:210`), README `shared/eventbus` («Паника обработчика перехватывается») и `delivery_test.go:541`. Все тесты паники зелёные, число их записей прежнее (1). Запись 2 появляется только при неудачной записи.
- `shared/logging.LogDeadLetter` пишет тот же текст «event parked in dead letters» уровнем `Error` с `handled=true`. Это отдельный помощник, шина его не вызывает, и правка его не касается.
- README `shared/eventbus`: «Если запись не удалась, `Deliver` возвращает ошибку и офсет не коммитится» — по-прежнему верно.

**Замечания, не блокирующие.**
- Ветка валидации с неудачной записью отдельным кейсом не проверена (заметил и ревьюер). Код у неё общий с паникой, DoD её не требует. Не возвращаю.
- `Warn` «not parked» на закрытой шине несёт `handled=false`. На `service_panics` это не влияет: метрика считает только `level=error`.

**Бэклог** (записан строками в раздел T-441 `../tasks.md`; отдельные задачи заводит оркестратор):
1. Гонка в `Kafka.Close` (владелец `shared/eventbus`, EPIC-001). `closed=true` ставится под `mu` (`kafka.go:348`) раньше, чем отменяется `loopCtx`: после `stopReaders()` (`:364`) и в горутине `context.AfterFunc` (`:206`). Если запись в `dead_letters` попадает в этот промежуток, `Kafka.writer` возвращает `ErrClosed`, `stopped(loopCtx, …)` его не узнаёт, и `Subscribe` возвращает «write dead letter … bus is closed» вместо `nil` (C-01 «Остановка»). В логе при этом `Warn` с `bus_closed=true`. Правка — `stopped` принимает `errors.Is(err, ErrClosed)` или адаптер проверяет `k.closing.Err()`; unit-тест на `stopped` и мутант. T-441 это окно не создаёт и не расширяет.
2. README `shared/eventbus`: одной строкой назвать исходы, которые `Delivery` пишет в лог: parked / not parked, `bus_closed`, уровни. Сейчас это знает только код. Не срочно.
3. Строка `logPanic` «event handler panicked; the event goes to dead letters» пишется до записи и говорит о намерении (NFR-012). В T-441 её не трогали: текст закреплён C-01 v1.5 и тестами. После T-441 за ней при неудачной записи идёт «not parked», так что ложного итога в логе нет. Переформулировать ли её — вопрос к владельцу метрики `service_panics`, не блокирует.

**Контейнеры и стенд.** Docker и интеграционные тесты не запускались, стенд LLM `:8888` не трогался, `.env` не открывался. Не коммитил.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 10:31 | 13.09 10:44 | 12m |
| Ревью | 13.09 10:44 | 13.09 10:54 | 9m |
| Разработка 2 | 13.09 10:54 | 13.09 11:00 | 5m |
| Приёмка | 13.09 11:00 | 13.09 11:08 | 7m |
| **Всего** | | | **36m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
