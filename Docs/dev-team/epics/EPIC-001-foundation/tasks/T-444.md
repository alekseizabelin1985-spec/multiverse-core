# T-444: Ревизия контрактов 4 — документы

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / system-architect |
| Волна | 1 (до старта первых волн EPIC-002/003/004) |
| Размер | M |
| Исполнитель | system-architect#1 (Opus) |
| Статус | done (принята TEAM-1/tech-lead#2 после ревью #2 и итерации 3) |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-13 12:34 / 2026-09-13 13:47 |
| Метка | `contract-change` (документы; код — T-445 и T-446) |

## Описание и DoD
Главный источник — раздел «### T-444» в `../tasks.md`. Основание — отчёт system-architect#1 по вопросам сверки планов EPIC-002/003/004 и ревью T-214/T-215, T-441, T-443 (пункты 1–9, приняты оркестратором без изменений) и дополнение оркестратора по плану EPIC-002 (пункты 10–11). Кратко:
- `contracts.md` v0.11 со сводкой по пунктам 1–11, дополнение ADR-001 2026-09-13, `ownership.md` v0.6;
- документы владельцев приведены к решениям с пометкой «(изм. T-444)»;
- строки DoD для тимлидов — ниже, `tasks.md` эпиков не правится.

## Выполнение (system-architect)
system-architect#1 (Opus), ветка `task/T-444-contracts-revision-4`, папка `.worktrees/T-444`. Не закоммичено: коммит делает оркестратор.

### Решения по пунктам (где записаны)

| П. | Решение | Где |
|---|---|---|
| 1a | страж получает данные (`LawsVersion`, `CheckKeys`, `Invariants map[string]InvariantFunc`), а не типы `laws`/`mechanics`; адаптер — в рое | ADR-001 доп. п. 1; КД роя §3, §10.4 |
| 1b | `providers/recorded.Writer` вместо `testkit/swarm.RecordingWriter` | ADR-001 доп. п. 2; C-07 v1.3; §17; КД роя §2 |
| 1c | рой: `llm$`, `llm/prompt$`, `llm/guardian$` (`parser$` — по дизайну), `providers/fake$` только в тестах; память — `llm$`, `providers$`, `fake$` в тестах | ADR-001 доп. п. 3; C-15 «Заглушка»; КД роя §3 |
| 1d | шаблоны — `internal/swarm/template` (листовой), двойник видит ровно его | ADR-001 доп. п. 4; C-05 «Заглушка»; КД роя §2 |
| 1e | `FakeGateway` — `internal/gateway/gatewaytest`; `Harness` видит `client$`, `api$` | ADR-001 доп. п. 5; C-04, C-08 v1.4; §17; КД шлюза §1, §3, §15 |
| 1f | `laws.WorldVersions` | ADR-001 доп. п. 6; C-12; КД роя §3, §12.2 |
| 2 | `{enabled, provider, external_players_ack}`, `provider` всегда | C-06 v1.2; КД роя §9.1, §9.4, §20, сведение 3; `threat-model.md`; `api-contracts.md` §2.3.16 |
| 3 | хеши `sha256:<64 hex>` | C-07 v1.3, C-11; КД роя §9.2, §14 |
| 4 | условия по статусу/причине — в схеме, по стадии — свойство-тест шлюза; `background_ref` | C-07 v1.3; КД роя §9.2; `api-contracts.md` §2.3.10 |
| 5 | `gm.created`: издатели `legacy`, `core/gateway` | §0; C-04 v1.4 |
| 6 | `ReadHeaderTimeout 5s`, `IdleTimeout 120s`, `runtime.SetDeadlines`, `runtime.ShuttingDown` | C-01 v1.8; КД шлюза §3, §5.1 |
| 7 | регистрация контекстов и подкоманд, `registry.go` — по файлам владельцев; один стек `llm`/`laws`/`swarm` | §0; ADR-001 доп. п. 7; `ownership.md` §1 |
| 8 | `ErrClosed`; заголовок C-08; `group_move`; `scope` в конверте; `EventRef`; ADR-016 уже помечен, КД §9.2 приведён | C-01 v1.8, C-04 v1.4, C-08; `api-contracts.md` §2.3.2, §2.3.3, §2.3.7 |
| 9 | правила трёх веток | §16 п. 8; `ownership.md` §3 |
| 10 | связка «путь ↔ причина» → `level_violation`; bootstrap `source=mvctl`, `author`, `cause=init`; `laws bump` → `cause=author`; `StatusTransitionAllowed(x,x)` — T-050 | C-02 v1.5; КД State §4.6, §4.10; КД роя §12.2 |
| 11 | `serve.go` в C-01 v1.4; `test/e2e/**`; `testdata/fixtures/events/**`; регистрация `state` — T-062 | C-01; `ownership.md` §1 |

### Строки DoD для тимлидов (в `tasks.md` эпиков не внесены)

**EPIC-002 (tech-lead#1)**
- T-050: `entity.StatusTransitionAllowed(x, x) = true` для нетерминального `x`; строка теста `alive→alive` меняет ожидание, `abandoned→abandoned` остаётся `false` (C-02 v1.4 «Статус в то же значение», метка `contract-change`).
- T-056: пара «путь, причина», не разрешённая предлагающему нормой C-02 v1.4, отвергается `level_violation`. Форма пакета — `invalid_op`, условие на состояние мира — `law_violation` с `invariant_id`, терминальная сущность — `dead_entity`. Тест таблицей по одному кейсу на каждую ветку, в том числе `gateway`: `set status` с `cause=move` → `level_violation` (C-02 v1.5).
- T-058: `Bootstrap` публикует `entity.create.proposed` с `source=mvctl`, `cause=init`, `meta.actor_kind=system`; State выводит предлагающего `author`. Реестр не меняется. Тест: предложения bootstrap проходят `contracts.OwnershipRules` строкой `author`. Подкоманда `world` — в `cmd/mvctl/commands_state.go` и `cmd/mvctl/internal/world` после раскладки (до неё — PR в `cmd/mvctl/main.go` через tech-lead#1).
- T-060: сборка `EventClock`/`NullTimers` — в `cmd/multiverse/serve.go` (не `main.go`); таймеры шины реальные (C-01 v1.4); регистрация контекста `state` в этой задаче не делается.
- T-062: настоящий `state` регистрируется в `cmd/multiverse/contexts_state.go` (переменная-фабрика; после T-446). Тест: `--contexts=state` поднимает настоящий контекст, заглушки `state` в процессе нет. e2e S1/S3 — в `test/e2e` файлами `state_solo30_test.go`, `state_recovery_test.go` (итерация 2, правило имён `ownership.md` §1).

**EPIC-003 (tech-lead#2)**
- T-201: `ContentHash` возвращает `sha256:` + 64 hex в нижнем регистре; тест формата (`^sha256:[0-9a-f]{64}$`) и стабильности; значение проходит схемы `agent.spawned`, `agent.blueprint_reloaded` (после T-445).
- T-205: в `internal/laws` объявлен `WorldVersions{LawsVersion(worldID) (string, bool)}`; `nil` или `ok=false` — старшая `approved` из файлов (тест обеих веток); `mvctl laws show` печатает источник версии; `Bump` предлагает `world.laws_version` с `cause=author` (не `laws`); `internal/laws` не импортирует `internal/swarm` (depguard зелёный без новых разрешений).
- T-211: свойство-тесты: (а) `filter` при `invalid` есть тогда и только тогда, когда шаг фильтра наступил; (б) `reasons[]` равно множеству `reason` связанных `rejected`; (в) число `rejected` с `element` равно числу отброшенных элементов; (г) каждая сгенерированная запись проходит `contracts.Validate` со схемой T-445; `prompt_hash`, `response_hash` — `sha256:<hex>`.
- T-212: вместо «Payload — только булев флаг»: payload ровно `{enabled, provider, external_players_ack}`; `provider` из `MV_LLM_PROVIDER` — всегда; `external_players_ack` из `MV_LLM_CLOUD_ALLOW_EXTERNAL_PLAYERS`; негативный тест по подстроке — URL, ключ и их фрагменты, не имя провайдера (C-06 v1.2).
- T-213: повтор при устаревших законах — через `LawsSource func(world string) (string, error)`, который передаёт рой; зависимость от T-205 — только интеграционный тест в рое; `internal/llm` не импортирует `internal/laws` и `internal/mechanics`; `rejected`: `budget_exceeded` — с `budget`, без `llm_output`; `unknown_entity` — с `element` и `entity` или `background_ref`.
- T-217: `guardian.Input{…; LawsVersion string; CheckKeys []string; Invariants map[string]InvariantFunc; …}`, `InvariantView` с набором методов `mechanics.StateView`; тесты на рукописных `InvariantFunc` без YAML правил; зависимость «T-205 (типы laws)» снята; отброшенная ссылка `background_refs[i]` → `rejected{reason: unknown_entity, element, background_ref}`.
- T-218: `prompt_hash` = `sha256:` + hex от `SHA-256(system + "\n \n" + user)`; тест формата.
- T-221: писатель — `internal/llm/providers/recorded/writer.go` (не `shared/testkit/swarm/recording_writer.go`); фильтр SEC-23 в писателе (тест: `actor_kind=human` → ошибка писателя); запись → чтение `recorded` по ключу; `cmd/mvctl/internal/record` не импортирует `shared/testkit` (depguard `no-testkit-in-production` зелёный).
- T-228: шаблоны — `internal/swarm/template`, импортирует только stdlib (правило T-445); `shared/testkit/swarm` импортирует ровно этот пакет; тесты `FakeNarrator` зелёные; каталог `shared/testkit/swarm/template` переносится (`git mv`) тем же изменением — копия текстов одна.
- Сверх списка: T-226, T-233 — `providers/fake` только в `_test.go`; T-235 — адаптер `mechanics.Invariant → guardian.InvariantFunc` в `internal/swarm` с проверкой при компиляции `var _ mechanics.StateView = guardian.InvariantView(nil)`.

**EPIC-004 (tech-lead#3)**
- T-301: из `cause` схем `group.created/joined/left/disbanded` снят `group_move` (невалидная фикстура с ним); `scope` в payload `round.opened` не вводится; пары фикстур своих типов — владелец (`ownership.md` §1).
- T-305: при `MV_GM_PATH=legacy` — `gm.created` с `source=core/gateway` (реестр — T-445), `meta.gm_path=legacy`, payload той формы, что разбирает `narrative-orchestrator` (`service.go:65`) и публиковал `game-service` (`handlers.go:273`) — тест на эталонном JSON; при `agent` не публикуется.
- T-308: `FakeGateway` — `internal/gateway/gatewaytest`; `Harness` в `shared/testkit/gateway` импортирует из шлюза ровно `client` и `api`; `client` импортирует только `api`; мутанты: `testkit/gateway` → `internal/gateway/links` краснеет, production-файл → `gatewaytest` краснеет; e2e бота с `FakeGateway` (T-315) — в `test/e2e` или в тестах `cmd/telegram-bot`, не в `internal/<контекст>`.

**Предложения новых задач подключения контекстов**
1. EPIC-003 (до T-256): «Стек `core` в `cmd/multiverse/contexts_swarm.go»: один лениво создаваемый стек (шлюз LLM, сервис законов, рой); контексты `llm` (публикует `config.cloud_enabled`, монтирует `/v1/admin/llm/usage`, здоровье провайдера), `laws` (подписка `world.laws.changed`, `/health` `unknown_check`), `swarm` (`DependsOn: llm, laws`). DoD: при `--contexts=llm,laws,swarm` экземпляр шлюза один (`Usage` видит вызовы роя); `--contexts=llm` без роя стартует; хук `MV_SWARM_FAKE` продолжает работать до T-256. Зависит от T-446, T-212, T-213, T-205, T-237.
2. EPIC-004 (в T-303 или T-309 — выбор tech-lead#3): регистрация `gateway` в `cmd/multiverse/contexts_gateway.go`; в T-307 — дедлайны маршрутов через `runtime.SetDeadlines` и завершение long-poll по `runtime.ShuttingDown` (тест: `Stop` процесса с активным long-poll < 1 с).
3. EPIC-002: регистрация `state` — T-062 (выше).
4. EPIC-005: регистрация `memory` в `contexts_memory.go` — при появлении контекста памяти (tech-lead#1).

### Записки владельцам (правка по существу, в документы не внесена)
- project-manager, tech-lead#1: `plan/teams.md` §3.2–§3.3 и `plan/roadmap.md` называют `integration/mvp-1`, тег `mvp-1/wave-0` и ветки `epic/EPIC-002-state`, `epic/EPIC-003-swarm`, `epic/EPIC-004-gateway`. В дереве — develop, `epic/EPIC-002-state-mechanics`, `epic/EPIC-003-swarm-llm-laws`, `epic/EPIC-004-gateway-bot` и контрольные слияния. Пересобрать документы — зона project-manager.
- EPIC-002 (владелец строк `author`/`system` в `ownership.go`): у строки `system` в MVP-1 не осталось издателя — снять её или оставить за будущим механизмом.
- architect#1: `components/state-and-mechanics.md` §4.6 выводит предлагающего по `source` `gateway`, а реестр и `sources.go` называют источник `core/gateway`; сверить при ближайшей правке (T-056 проверяет по значению из `sources.go`).

### Проверки
- `grep -rna allow_external_players Docs/dev-team/architecture Docs/dev-team/analysis schemas/` — одно совпадение: запись истории `components/swarm-llm-laws.md` §20 «Дополнение после G2», строка п. 10, с пометкой о смене полей.
- CRLF: у каждого изменённого файла число CR равно числу строк.
- `gitleaks dir --redact -c .gitleaks.toml` по каждому изменённому файлу — 0 находок после переноса отпечатка известного ложного срабатывания таблицы провайдеров КД роя §9.1 в `.gitleaksignore` (514 → 521; строку сдвинули правки T-444 выше таблицы). `.gitleaksignore` — в коммит вместе с документами.

## Ревью (code-reviewer)

| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#3, TEAM-1) | 2026-09-13 | 0 | 3 | 5 | 2 | вернуть |
| 2 (code-reviewer#3, TEAM-1) | 2026-09-13 | 0 | 0 | 3 | 3 | принять |

Подробности — разделы «T-444 · ревью #1» и «T-444 · ревью #2» в `../review.md`.

## Итерация 2 (system-architect, по ревью #1)
2026-09-13, system-architect#1 (Opus). Вердикт ревью #1 — «вернуть» (0/3/5/2). Решения оркестратора: Ma-2 — мягкий режим; Ma-1 — двойник правит EPIC-002 в T-056; Ma-3 — привязка сценариев к задачам и правило имён; задача раскладки — T-446; бэклог ревьюера п. 1–2 — сейчас, п. 3 — в T-445. Не закоммичено.

### Замечания ревью #1

| # | Что сделано | Где |
|---|---|---|
| Ma-1 | В C-02 v1.5 добавлен абзац «Код расходится». Двойник отвечает `invalid_op` (`apply.go:353-354`, тест `apply_test.go:213`), норма — `level_violation`. Правит поставщик EPIC-002 в T-056, почему — там же. Сводка п. 10 («Совместимость») и история C-02 называют двойник. Строка DoD T-056 — ниже | `contracts.md` C-02 v1.5, сводка п. 10 |
| Ma-2 | Мягкий режим с названными исключениями: (а) общий код, который видят другие эпики, — задачей EPIC-001, а точечное дополнение для одной задачи — в её ветке с ревью system-architect; (б) `cmd/multiverse/{serve,contexts}.go`, `cmd/mvctl/main.go` до T-446 — в ветке задачи эпика с приёмкой tech-lead#1, после T-446 — в файлах владельцев; (в) переменная и её строка `.env.example` — тем изменением, которое вводит чтение. Выровнены §0 (переходный порядок), C-01 v1.4 (`serve.go`), C-14 (объявление переменной), `ownership.md` строки 9, 10, 12 и §3 п. 4; в §16 п. 8 добавлены `registry.go` и `cmd/mvctl/internal/cli`; обоснование «почему мягкий» — в §16 п. 8 и §3 п. 4 | `contracts.md` §0, C-01, C-14, §16 п. 8, сводка п. 9; `ownership.md` §1, §3 п. 4, §2 № 26 |
| Ma-3 | Сценарий принадлежит задаче, которая его вводит; правило имён — префикс владельца в имени файла, тестовой функции и неэкспортируемых именах уровня пакета; общие помощники без префикса — только в файлах EPIC-001. Выбран префикс, а не подпакеты: помощники живут в `_test.go` пакета `e2e` и из подпакета не импортируются | `ownership.md` §1 строка `test/e2e/**`, §2 № 26 |
| Mi-1 | Версии и история: C-05 v1.7, C-11 v1.2, C-12 v1.1 (раздел истории заведён), C-15 v1.2 | `contracts.md` |
| Mi-2 | §17: «единственное место» → «первое место» с пометкой (с T-219 два пакета, с T-445 три) | `contracts.md` §17 |
| Mi-3 | КД роя: таблица провайдеров, строка `providers/fake` — `providers/recorded.Writer`; §12.2 — `mvctl laws bump` (EPIC-003); попутно §18 (строка «фикстуры») — тоже `providers/recorded.Writer` | `components/swarm-llm-laws.md` |
| Mi-4 | `response_hash` обязателен всегда: при `error` — хеш пустой строки и `response_len: 0`, при `quarantined`/`filter_error` — хеш полученного, несохранённого ответа. `api-contracts.md` §2.3.10: `response_raw` — при `valid\|partially_rejected\|invalid` | `contracts.md` C-07 v1.3; `api-contracts.md` §2.3.10 |
| Mi-5 | Строки DoD T-352, T-353 — ниже | карточка |
| N-1 | «`stopped` в `kafka.go`, `busClosed` в `delivery.go`» | `contracts.md` C-01 |
| N-2 | в проверках — `grep -rna` | карточка, `tasks.md` DoD 1, dev-log |
| бэклог 1 | §0: «девять типов», добавлен `time.syncTime` | `contracts.md` §0 |
| бэклог 2 | Байт NUL в КД роя был на месте пробела в формуле `prompt_hash`: `SHA-256(system + "\n␀\n" + user)`. Заменён пробелом — так записано в DoD T-218 (`"\n \n"`). Git перестаёт считать файл двоичным: при `text=auto` индекс нормализует его в LF (`git hash-object --path` даёт другой хеш, чем без фильтров). Поэтому **в коммите файл изменится целиком** (1024/1015 строк в `git diff --numstat`); смысловую разницу показывает `git diff --ignore-cr-at-eol`. CRLF в рабочей копии сохранён. Скан диапазона ветки после такого коммита увидел бы строку таблицы провайдеров как добавленную, под отпечатком нового коммита. Поэтому строка получила inline `gitleaks:allow` в HTML-комментарии ячейки (`infrastructure.md` §4.5 п. 1 это допускает); комментарий в `.gitleaksignore` дописан | `components/swarm-llm-laws.md` §9.1, §11.4; `.gitleaksignore` |
| T-446 | ссылки на «задачу EPIC-001 «runtime и раскладка `cmd`»» заменены на T-446 | `contracts.md`, `ownership.md`, ADR-001 доп. п. 7, карточка, `tasks.md` |
| T-445 (ответ) | в ADR-001 доп. п. 5 уточнено: исключение для `_test.go` снимает только `no-testkit-in-production`, правила контекстов действуют в тестах | ADR-001 доп. п. 5 |

### Строки DoD итерации 2 (в `tasks.md` эпиков не внесены)

**EPIC-002 (tech-lead#1)**
- T-056 (дополнение): поставщик двойника меняет `shared/testkit/state/apply.go` (`statusRefusal`). `status=abandoned` не с `cause=forget` → `level_violation`, `invalid_op` остаётся только за переходом, которого нет в матрице. Ожидание `TestAbandonedNeedsTheCauseOfForget/abandoned with another cause` (`apply_test.go:213`) меняется тем же изменением, комментарий над `statusRefusal` приводится к C-02 v1.5. Потребители двойника (`FakeEncounter`, `Harness`) компилируются и проходят без правок.
- T-061: файл `test/e2e/state_solo30_test.go` (не `solo30_test.go`), функции `TestStateSolo30…`, неэкспортируемые имена уровня пакета — с префиксом `state`; общие помощники EPIC-001 не переопределяются.
- T-062: `test/e2e/state_solo30_test.go` и `state_recovery_test.go` (не `{solo30,recovery}_test.go`), имена — как в T-061.
- T-070: `test/e2e/state_group3x30_test.go` (не `group3x30_test.go`), `TestStateGroup3x30…`.

**EPIC-003 (tech-lead#2)**
- T-211 (дополнение): при `validation_status=error` — `response_hash` = `sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, `response_len=0`; при `quarantined`/`filter_error` — хеш полученного ответа; тест на каждую ветку.
- T-242: `test/e2e/swarm_solo30_test.go`, `swarm_death_test.go`, `swarm_fleefail_test.go`, функции `TestSwarm…`.
- T-243: `test/e2e/swarm_background6h_test.go`, `swarm_degraded_test.go`, `swarm_recovery_test.go`, `swarm_injections10_test.go`, функции `TestSwarm…`.

**EPIC-004 (tech-lead#3)**
- T-313: `test/e2e/gateway_solo30_test.go`, `gateway_death_test.go`, `gateway_fleefail_test.go`, функции `TestGateway…`.
- T-314: `test/e2e/gateway_forget_test.go`, `gateway_privacy_test.go`, `gateway_recovery_test.go`.
- T-315: e2e бота с `FakeGateway` — `test/e2e/gateway_bot_test.go` (или тесты `cmd/telegram-bot`), не в `internal/<контекст>`.
- T-352: вместо «одно `group.entered_region {cause: group_move, by}`» — одно `group.entered_region {group, target, position, by, members[]}` без поля `cause` (схема `group.entered_region.v1.json`, C-04 v1.4).
- T-353: вместо «`entity.updated cause=group_move` — всем участникам» — адресация участникам по `entity.updated` позиции группы с `cause=move` (строки `gateway` таблицы владения для группы и игрока, `shared/contracts/ownership.go`, разрешают `move`); значения `group_move` нет ни в схемах, ни в таблице.
- T-357: `test/e2e/gateway_group3x30_test.go`, `gateway_forget_group_test.go`, функции `TestGateway…`.

### Проверки итерации 2
- `grep -rna allow_external_players Docs/dev-team/architecture Docs/dev-team/analysis schemas/` — одно совпадение, запись истории КД роя §20 п. 10.
- `grep -rn "runtime и раскладка" Docs/dev-team/architecture Docs/dev-team/plan` — только вместе с номером T-446.
- CRLF: у каждого изменённого файла число CR равно числу строк (проверка в каждом скрипте правки до записи и после).
- В КД роя 0 байтов NUL.
- `gitleaks dir --redact -c .gitleaks.toml` по каждому изменённому файлу — 0 находок. Контроль в scratch: КД роя в текущем виде без `.gitleaksignore` — 0 находок (inline allow работает); КД из HEAD без `.gitleaksignore` — 1 находка (контроль жив). Строка таблицы провайдеров осталась 521-й: правки выше неё строк не добавили.

## Итерация 3 (system-architect, по ревью #2)
2026-09-13, system-architect#1 (Opus). Вердикт ревью #2 — «принять» (0/0/3/3), замечания закрываются до приёмки. Не закоммичено.

| # | Что сделано | Где |
|---|---|---|
| Mi-1 | Одна формулировка. Задачу принимает tech-lead её эпика. Правки общих файлов в ветке эпика (`cmd/multiverse/{serve,contexts}.go`, `cmd/mvctl/main.go`, `registry.go`, `shared/env/vars.go`, `.env.example`) обязательно просматривает tech-lead#1, и без его отметки задача не принимается. Строки реестра дополнительно ревьюит system-architect. Все прежние «с приёмкой tech-lead#1» заменены | `contracts.md` §0, C-01 v1.4, C-14, §16 п. 8; `ownership.md` строки 9, 10, 12, §3 п. 4, п. 6 |
| Mi-2 | В привязку `group-3x30` добавлен EPIC-003 T-251 (`swarm_group3x30_test.go`, `swarm_injections_memory_test.go`, `swarm_s6_repeat_test.go`); строка DoD — ниже | `ownership.md` строка `test/e2e/**`, §2 № 26 |
| Mi-3 | `TestMain` пакета — только в файле EPIC-001 `main_test.go`. Эпики пишут ленивые помощники с префиксом на `sync.Once`, а подготовку до и после пакета регистрируют через `registerPackageSetup(owner, setup)` в `init()`. `TestMain` вызывает подготовки в порядке имени владельца, завершения — в обратном. Файл заводит задача EPIC-001 по первому запросу эпика | `ownership.md` строка `test/e2e/**` |
| N-1 | «пакета `e2e_test`»; в правиле — «файлы сценариев — `package e2e_test`» | `ownership.md` строка `test/e2e/**` |
| N-2 | обратные кавычки вокруг `error` в перечне сняты | `api-contracts.md` §2.3.10 |
| N-3 | `shared/contracts/sources.go` — только задачей EPIC-001 (новое значение `source` видит проверка у всех эпиков); из группы точечных дополнений убран | `contracts.md` §16 п. 8; `ownership.md` §3 п. 4 |
| бэклог 1 | строка бэклога EPIC-001 про `.git-blame-ignore-revs` | `tasks.md`, раздел T-444 |
| бэклог 2, 3 | строки DoD T-218 и T-056 — ниже | карточка |

КД роя в этой итерации не менялся: строка таблицы провайдеров — 521-я, inline `gitleaks:allow` на месте.

### Строки DoD итерации 3 (в `tasks.md` эпиков не внесены)
- **EPIC-003, T-251:** файлы `test/e2e/swarm_group3x30_test.go`, `swarm_injections_memory_test.go`, `swarm_s6_repeat_test.go`, `package e2e_test`, функции `TestSwarm…`; своего `TestMain` нет.
- **EPIC-003, T-218:** разделитель `prompt_hash` — `"\n \n"` (перевод строки, пробел, перевод строки), как в DoD. Байт NUL в КД до T-444 признан артефактом. Разделитель, который не встречается в тексте, не нужен: `prompt_hash` — не ключ записи (ключ — `correlation_id, agent.id, phase, attempt`), а граница `system`/`user` однозначна по фиксированным тегам секций. Тест держит golden-значение `prompt_hash` для фиксированных `system` и `user`, чтобы разделитель не сменили молча.
- **EPIC-002, T-056 (дополнение):** в двойнике появляется экспортируемая константа `state.ReasonLevelViolation = "level_violation"`. Это расширение Go-API двойника, в отчёте задачи его называют для потребителей (`ownership.md` §3 п. 8).
- **EPIC-003, план (tech-lead#2):**
  - в T-204, T-205, T-221 к «ревью tech-lead#2» дописать «обязательный просмотр tech-lead#1 правки `cmd/mvctl/main.go`»;
  - в шапке плана (`tasks.md:8`) фразу «Чужие пути (`cmd/**` …) — не "PR через tech-lead#1"» заменить формулировкой `contracts.md` §16 п. 8.
- **Все эпики:** тимлиды, у которых в плане написано «с приёмкой tech-lead#1» для общих файлов, читают это как «с обязательным просмотром tech-lead#1». EPIC-002 (T-055, T-058, T-060) и EPIC-004 (T-303, T-305, T-309) по существу уже соответствуют.

### Проверки итерации 3
- В `contracts.md` и `ownership.md` не осталось фразы «с приёмкой tech-lead#1».
- CRLF и отсутствие NUL — проверены скриптом до записи и после; `sed -i` не использовался.
- `gitleaks dir --redact -c .gitleaks.toml` по изменённым файлам — см. dev-log.

## Приёмка (tech-lead)

tech-lead#2 (TEAM-1), 2026-09-13, модель Opus. **Решение: принята.**

**Итог.** Ревизия контрактов 4 записана в документах: `contracts.md` v0.11 со сводкой по пунктам 1–11, дополнение ADR-001 от 2026-09-13, `ownership.md` v0.6, документы владельцев с пометкой «(изм. T-444)». Код, схемы и линтер — T-445 и T-446.

Основание — ревью #2 code-reviewer#3: «принять», Critical 0, Major 0, Minor 3, Nit 3 (`review.md`, «T-444 · ревью #2»). Итерация 3 закрыла эти замечания. Повторного ревью не было, итерацию 3 проверил я (ниже). Сравнивал `git diff --ignore-cr-at-eol` рабочей копии с HEAD `0fff090`: коммитов в ветке нет. Изменены 12 файлов, новый файл — эта карточка.

### DoD (раздел T-444 в `../tasks.md`)

| # | Пункт | Как проверено | Итог |
|---|---|---|---|
| 1 | `allow_external_players` только в истории | `grep -rna allow_external_players Docs/dev-team/architecture Docs/dev-team/analysis schemas/` — одно совпадение, `components/swarm-llm-laws.md:992`: запись истории §20 п. 10 с пометкой о смене полей | да |
| 2 | Сводка v0.11 по пунктам 1–11 | `contracts.md:13-30`: строки 1a–1f, 2–11, у каждой заполнены «Кто прав», «Совместимость», «Источник» | да |
| 3 | Версии и история затронутых контрактов | Версия в заголовке и запись в истории есть у C-01 v1.8, C-02 v1.5, C-04 v1.4, C-05 v1.7, C-06 v1.2, C-07 v1.3, C-08 v1.4 (заголовок сведён с историей v1.1…v1.4), C-11 v1.2, C-12 v1.1 (раздел истории заведён), C-15 v1.2. У C-14 добавлена одна процедурная фраза без записи истории — замечание 3 | да, с замечанием |
| 4 | Правки владельцев помечены | Каждая добавленная строка КД шлюза, State, роя, `threat-model.md`, `api-contracts.md` несёт «(изм. T-444)» или продолжает помеченный блок кода. Единственная строка без пометки — КД роя `:624`, замена байта NUL пробелом (итерация 2, названа в карточке). Правки, которые меняли бы решение владельца, перечислены в «Записках владельцам» | да |
| 5 | CRLF | Python по 13 файлам: число CRLF равно числу LF, одиночных LF и NUL нет, все файлы оканчиваются CRLF. После правок приёмки проверено повторно | да |
| 6 | `gitleaks dir` — 0 | см. «gitleaks» ниже | да |
| — | Дополнение ADR-001 | «Дополнение 2026-09-13»: п. 1–7 (1a–1f и регистрация контекстов), условия пересмотра | да |
| — | `ownership.md` v0.6 | Шапка v0.6; строки `cmd/*`, `shared/contracts/**`, `test/e2e/**`, `testdata/fixtures/events/**`; §3 п. 1–9 | да |

### Итерация 3 (проверена при приёмке)
- **Mi-1, «кто принимает».**
  - Определение одно: `contracts.md` §16 п. 8, `:731`. Остальные места ссылаются на него короткой скобкой «с обязательным просмотром tech-lead#1 (приёмка — tech-lead эпика задачи)»: §0 `:198`, C-01 `:232`, C-14 `:663`, §16 п. 8 `:734`, `:737`, `:738`, `ownership.md` `:9`, `:10`, `:12`, §3 п. 4 `:82`, п. 6 `:84`. Перечни файлов в ссылках совпадают с определением.
  - Проверил grep «приёмк» и «tech-lead#1» по обоим файлам. Фразы «с приёмкой tech-lead#1» нет.
  - Остались три формулировки, и ни одна не противоречит определению:
    - «приёмка tech-lead#1» в `contracts.md:84` — история v0.7 о T-410, задаче EPIC-001;
    - «PR через tech-lead#1» в `ownership.md` §3 п. 9 — хук `fake_contexts.go`, файла нет в перечне §16 п. 8;
    - «ревью system-architect и tech-lead#1» у строк `ownership.go` — процедура ADR-025 (§16 п. 6).
- **Mi-2.** T-251 стоит в привязке `group-3x30` (`ownership.md:26`) с тремя файлами `swarm_…`. Строка DoD T-251 — в карточке.
- **Mi-3.** Правило `TestMain` выполнимо.
  - В `test/e2e` одни и те же шесть файлов, в них нет ни `TestMain`, ни `registerPackageSetup`. Проверено в HEAD, develop, трёх `epic/*` (`git grep`) и в рабочих копиях всех 14 папок `.worktrees/*`, включая незакоммиченные правки.
  - `init()` файлов пакета выполняется до `TestMain`, поэтому регистрация успевает. Порядок по имени владельца детерминирован.
- **Nit.**
  - N-1: `package e2e_test` — `ownership.md:26`.
  - N-2: в `api-contracts.md` §2.3.10 вложенных обратных кавычек нет.
  - N-3: `sources.go` правится только задачей EPIC-001 — `contracts.md:735`, `ownership.md:10`, §3 п. 4.
- **Бэклог ревью #2.**
  - П. 1 — строка в `tasks.md`.
  - П. 2 и 3 — строки DoD T-218 и T-056 в карточке.

### Согласованность с другими ветками
Проверено через `git show` по веткам; T-445 — по рабочей папке `.worktrees/T-445`.
- **T-445** (EPIC-001, не слита, статус review):
  - `llm.output.v1.json`: таблица условной обязательности C-07 v1.3 совпадает с `allOf` построчно, все шесть правил. `response_hash` и `response_len` обязательны.
  - Шаблон `^sha256:[0-9a-f]{64}$` стоит у `prompt_hash`, `response_hash` и `content_hash` (`agent.spawned`, `agent.blueprint_reloaded`).
  - `llm.output.rejected.v1.json`. При `budget_exceeded` `budget` обязателен, а `llm_output` запрещён; при других причинах наоборот. При `unknown_entity` обязателен `element` и ровно одно из `entity`/`background_ref`.
  - `$defs.EventRef` = `{event{id, type?}}`.
  - Реестр: у `gm.created` издатели `legacy` и `core/gateway`.
  - `config.cloud_enabled` требует только `enabled`. C-06 v1.2 говорит то же, а «`provider` всегда» — обязанность издателя (DoD T-212).
- **T-301** (EPIC-004, слита, `fdc7e05`):
  - `group_move` снят из `cause` четырёх схем `group.*`, у каждой есть невалидная фикстура с этим значением. C-04 v1.4 («схемы сужает владелец в T-301», «издателей нет») этому соответствует.
  - В develop и в этой ветке значение пока есть — до контрольного слияния EPIC-004.
- **T-050/T-052** (EPIC-002, слиты, `03c551d`):
  - `StatusTransitionAllowed(alive, alive)` = `true`; для `abandoned→abandoned` и прочих терминальных — `false` (`entity_test.go`). Совпадает с C-02 и строкой DoD T-050.
  - `StateHash` имеет форму `sha256:<hex>`. T-052 добавила шаблон в схемы `snapshot.created` и `analytics.replay.completed`, как C-07 v1.3 и поручает владельцу.
  - Строка `author` в `ownership.go` разрешает `init` и `author`, в `entity.create.proposed.cause` оба значения есть. Bootstrap с `cause=init` и `laws bump` с `cause=author` проходят.
  - Противоречий нет. Абзац C-02 «Код расходится» устареет после слияния EPIC-002 в develop — замечание 2.

### gitleaks
- Копии 13 файлов с относительными путями и `.gitleaks.toml` ветки — в двух каталогах scratch: с `.gitleaksignore` ветки и без него.
- `gitleaks dir . -c .gitleaks.toml --redact` — «no leaks found» в обоих.
- Контроль: КД роя из HEAD без `.gitleaksignore` — «leaks found: 1», значит, правило живо. В текущем КД находку гасит inline `gitleaks:allow`, он там один.
- После правок приёмки карточку и `tasks.md` просканировал ещё раз — 0.
- Скан истории по диапазону ветки делает оркестратор после коммита (`make secrets-scan`). Ожидаю 0. КД роя в коммите изменится целиком, строку 521 гасит inline-комментарий.

### Правки при приёмке
- Карточка, «Строки DoD итерации 3», строка T-218. Скрипт итерации 3 записал экранирование `"\n \n"` настоящими переводами строк: строка распалась на три, и разметка показывала `" "`. Склеил обратно в `"\n \n"`, как в КД роя `:624` и в DoD T-218 плана. Смысл не менялся.
- Шапка карточки: статус и дата завершения.
- `../tasks.md`, раздел T-444: статус, строка бэклога про `test/e2e/main_test.go`, строка приёмки.

### Замечания (не блокируют, переданы)
1. **КД шлюза §8.1** (`components/gateway-and-bot.md:738`): «сам игрок (при `group_move` — все участники)». Значения `group_move` нет ни в схемах, ни в `ownership.go`; как правильно — сказано в строке DoD T-353 выше. Правит владелец КД (architect#1 или system-architect) на develop.
2. **C-02 v1.4, «Статус в то же значение», абзац «Код расходится»** (`contracts.md:323`). После контрольного слияния EPIC-002 в develop код будет соответствовать (T-050). Абзац пометить закрытым при ближайшей правке `contracts.md`. Сделать это просит и карточка T-050 (EPIC-002). Исполнитель — system-architect.
3. **C-14** (`contracts.md:663`): добавлена процедурная фраза со ссылкой на §16 п. 8, но версии и записи в истории нет. Семантика снапшотов не менялась, поэтому не возвращаю. При ближайшей правке — строка истории (system-architect).
4. **`ownership.md:26`**: «EPIC-002 T-070 (`state_group3x30_test.go`) EPIC-004 T-357» — пропущена запятая.
5. **План EPIC-003, `tasks.md:38`**: до T-446 реестр правится только задачей EPIC-001. Это строже мягкого режима, но совместимо. Смягчать или нет, решает тимлид эпика.

### Передача tech-lead#2 TEAM-2: план EPIC-003
Номера строк — по `epic/EPIC-003-swarm-llm-laws` (`b18ecd6`). `tasks.md` эпика правит его тимлид в ветке эпика, T-444 его не трогает.
- **T-204** (`tasks.md:104`). Вместо «до T-446 — одна строка в `commands()` с ревью tech-lead#2» записать: «до T-446 — одна строка в `commands()` с ревью tech-lead#2 и обязательным просмотром tech-lead#1 правки `cmd/mvctl/main.go`; без его отметки задача не принимается (`contracts.md` §16 п. 8)». В DoD добавить тот же пункт.
- **T-205** (`:121`) и **T-221** (`:413`) идут «по правилу T-204». Ссылку оставить и добавить в DoD каждой: «до T-446 правка `cmd/mvctl/main.go` — с обязательным просмотром tech-lead#1».
- **Шапка** (`:8`). Фразу «Чужие пути (`cmd/**` …) — не "PR через tech-lead#1"» заменить формулировкой `contracts.md` §16 п. 8:
  - задачу принимает tech-lead#2;
  - правки `cmd/multiverse/{serve,contexts}.go`, `cmd/mvctl/main.go`, `shared/contracts/registry.go`, `shared/env/vars.go` и `.env.example` в ветке эпика обязательно просматривает tech-lead#1;
  - строки реестра и точечные дополнения общего кода дополнительно ревьюит system-architect;
  - `shared/contracts/sources.go` правится только задачей EPIC-001.
- Остальные строки DoD для EPIC-003 из этой карточки вносит тот же тимлид: T-201, T-205, T-211 (и дополнение), T-212, T-213, T-217, T-218, T-221, T-228, T-226/T-233/T-235, T-242, T-243, T-251.
  - Разделитель T-218 — `"\n \n"` с golden-значением `prompt_hash`.

### Слияние в ветку эпика
`epic/EPIC-001-foundation` ушла вперёд (`ab6cb1d`, слияние T-405).
- **`.gitleaksignore`** правят обе ветки, но в разных участках: T-444 — строки 11–20, T-405 — 23–27. Ожидаю автослияние. После слияния должны остаться отпечаток КД роя `:521` и оба отпечатка `infrastructure.md` (`:549` и коммитный `:520`).
- **`dev-log.md` и `review.md`** обе ветки дописывают в конец, так что вероятен конфликт «добавлено/добавлено».
  - Разрешение: сохранить оба блока в порядке времени, текст не править.
  - Разработчик не нужен, разрешает оркестратор.
- **`tasks.md`**: T-405 правит середину, T-444 — конец, пересечения нет.
- После слияния — `make secrets-scan`.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 12:34 | 13.09 12:39 | 5m |
| Ревью | 13.09 12:39 | 13.09 12:55 | 16m |
| Разработка 2 | 13.09 12:55 | 13.09 13:12 | 16m |
| Ревью 2 | 13.09 13:12 | 13.09 13:30 | 18m |
| Разработка 3 | 13.09 13:30 | 13.09 13:34 | 3m |
| Приёмка | 13.09 13:34 | 13.09 13:47 | 13m |
| **Всего** | | | **1h 12m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
