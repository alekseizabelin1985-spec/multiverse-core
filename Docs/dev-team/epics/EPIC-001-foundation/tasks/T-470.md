# T-470: Документы по просмотру T-056 и ревью T-458 — `contracts.md` v0.15 (C-01 v1.11, C-02 v1.8, C-03 v1.4, C-05 v1.8a, C-14 v1.3, C-15 v1.6), КД State, `data-model.md` §3, ADR-001, ADR-005

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / system-architect |
| Волна | 1 |
| Размер | M |
| Исполнитель | system-architect#1 (Opus) |
| Статус | done |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-13 / 2026-09-14 |
| Метка | `contract-change` (документы; код, схемы и таблица адресов — задачи эпиков, строки DoD ниже) |
| Ветка / папка | `task/T-470-contracts-state-grammar` от `epic/EPIC-001-foundation` (`55ec4c4`), `.worktrees/T-470` |

## Описание и DoD
Раздела в `../tasks.md` пока нет — его добавит tech-lead#1. Поручение оркестратора:
1. Внести в `architecture/contracts.md` C-01 v1.11 (решение А5 ревью T-458), C-02 v1.8, C-03 v1.4, C-14 v1.3 (тексты просмотра system-architect T-056), сужение пометки «Код расходится» в C-15 и ADR-005 до `llm-bench` и строк успеха и `health` `llm-server` (решение оркестратора по T-463, T-468); шапка v0.15, история, сводка.
2. Внести тексты в КД State (§4.1, §4.5 п. 1–3 и 7–9, §4.6, §4.10, §8, §10), `analysis/data-model.md` §3.3, ADR-001, ADR-005; `infrastructure.md` §3.1.1 — только заменой строк, эталоны §4.2 не трогать.
3. Грамматику C-02 v1.8 писать под итерацию 4 T-056: ключ-целое (`inventory.0`) запрещён, индекс до девяти цифр, полный список скаляров.
4. Решить вопросы в пределах полномочий: смерть персонажа (C-05), соль `MV_TELEGRAM_ACTION_KEY_SALT` в законно пустых секретах `infrastructure.md` §3.1.1, `@` в пути адреса по C-15 (вопрос T-451). Остальные вопросы — перечнем в отчёте.
5. Строки DoD для T-471, T-057, T-212, T-237, T-468 — в разделе «Передать».

**DoD:**
1. У изменённых контрактов — версия в заголовке и запись в истории; строка сводки v0.15.
2. Тексты совпадают с одобренными в просмотре T-056 и в п. 6 ревью #1 T-458 (с поправками ревью #2); отступления названы с доводом.
3. Правки помечены «(изм. T-470)»; тексты C-15 и ADR-005 о правиле «локальный адрес» совпадают построчно.
4. CRLF, без NUL и BOM; строка 521 `swarm-llm-laws.md`, `.env.example` и эталоны §4.2 не тронуты; `gitleaks dir --redact` по копии изменённых файлов — 0.

## Выполнение (system-architect)
system-architect#1 (Opus), 2026-09-13. Рабочая папка `.worktrees/T-470`. Не закоммичено: коммит делает оркестратор.

Прочитано (только чтение):
- `.worktrees/T-056`: карточка T-056 (итерации 1–3, просмотр system-architect с текстами правок, отметка tech-lead#1); `internal/state/proposal.go` рабочей копии (`canonicalPath`, `canonicalPathSegment`, `scalarAttributes`) — грамматика C-02 v1.8 п. 2 сверена с ними.
- `develop`: карточка T-458 (ревью system-architect #1, п. 4–7; ревью #2, п. 3; приёмка, N2-1; «Передать»).
- Кончик эпика (`55ec4c4`): `shared/recording/{recording,journal}.go`, `internal/replay/*.go` — экспорт для КД §6.1; `testdata/llm/local-endpoints.tsv`; `scripts/lib/llm-endpoint.sh` (`llm_endpoint_judge`), `scripts/llm-bench.sh`, `scripts/llm-server.sh`; `build/Dockerfile`.
- Карточки T-451 (`.worktrees/EPIC-003`), T-456, T-463, T-468; `.worktrees/T-464` — правило 3 `compose-lint.sh` и выбор формы соли; `journal.md` (решения оркестратора по T-056, T-463, очередь вопросов).

### Решения
| # | Вопрос | Решение | Где |
|---|---|---|---|
| 1 | Форма текстов «читать так» (C-02 v1.8 п. 3, п. 4, «Заглушка») | Абзацы «Связка», «Семантика (v1.1)» и «Заглушка» правлены на месте с пометкой «изм. T-470» и прежней редакцией; п. 3 и п. 4 v1.8 говорят «заменена», а не «читать так». Иначе в контракте жили бы две редакции одной нормы, и читатель абзаца «Связка» получал бы старую. Пометки «Код расходится» v1.5 и расхождение двойника v1.7 зачёркнуты с записью «исправлено T-056; в EPIC-002 — со слиянием T-056»: T-056 ещё не принята | C-02 |
| 2 | Грамматика под итерацию 4 T-056 | Ключ не пуст, без `.`, `[`, `]`, и не любая запись, которую принимает `strconv.Atoi` (`0`, `007`, `+1`, `-1`) — ровно проверка `canonicalPath`. Индекс `\[(0\|[1-9][0-9]{0,8})\]`, у ключа их может быть несколько. Скаляры — по `data-model.md` §3; `scope` не скаляр (объект `{id, type}`, фикстуры). Добавлен довод «девять цифр помещаются в `int` любой платформы Go». *Итерация 2:* «сверено» относилось к грамматике ключа и индекса, а не к списку скаляров — список расходился с §3 в четырёх именах (Ma-1 ревью #1), решение — в разделе «Итерация 2» | C-02 v1.8 п. 2; КД §3.2 |
| 3 | Порядок проверок отдыха и условие пересмотра `expected_version` | Внесены в v1.8 п. 3 и п. 4 из ответов 5 и 6 просмотра: без порядка реализация вправе ответить `law_violation` на rest во встрече с неверным путём, а тест матрицы ждёт `level_violation` | C-02 v1.8; КД §4.6 |
| 4 | C-01 v1.11 | По п. 6 ревью #1 T-458 с поправками ревью #2. Префикс `recording:` назван и у `Writer`: приёмка T-458 (N2-1) привела его ошибки к той же форме, и строка приёмки разрешила расширить фразу | C-01 |
| 5 | КД State §6.1, §6.2 | Внесены здесь, хотя их нет в перечне поручения: ревью T-458, п. 5, отдаёт их «той же задаче документа, что вносит v1.11». Экспорт сверен с кодом `shared/recording` и `internal/replay` на кончике эпика. Задача T-466 (C-01 v1.11 и КД §6.1, §6.2) покрыта этой задачей целиком | КД §6 |
| 6 | Смерть персонажа в C-05 | Нужна одна фраза. Правило «персонажу без `died_at`/`killed_by`» следует из таблицы владения, но `FakeEncounter` его нарушил (итерация 1 T-056), а C-05 п. 9 — место, которое читает автор настоящего агента встречи (T-230). C-05 v1.8a — текст, схемы без изменений | C-05 п. 9 |
| 7 | `@` в пути адреса (вопрос ревью #2 T-451) | **`invalid` для `@` в любом месте значения.** `http://pw:2024/x@10.0.0.5:8080` сейчас `local` (хост `pw`, порт 2024): голова пароля проходит гейт облака без флага и уходит в DNS, соединение и ошибки `net/http`. Адресу платформы `@` в пути не нужен; если нужен — `%40`. *Отвергнуто:* осознанный `local` — ложный «локальный» дороже лишнего отказа (принцип C-15); `cloud` — флаг опечатку не исправит и отправит запросы на хост из пароля; «`@` после первого `/` authority» — тот же ответ, но правило «где угодно» проще и совпадает с правилом печати v1.5 | C-15 v1.6, ADR-005 |
| 8 | Охват правила печати (зависимость T-468) | Весь вывод реализации, не только отказы. С решением 7 значение с `@` дальше отказа не проходит, поэтому маскировка строк успеха, `health`, `llm-bench` и полей отчёта не нужна: объём T-468 — отказ, строки таблицы, стенд паритета | C-15 v1.6; строки T-468 |
| 9 | Пометка «Код расходится» | Не снята. Сужена до `llm-bench` (три отказа, пропуск фазы, `url`/`probe_url`) и строк успеха и `health` `llm-server`: отказы судьи, облачная фраза правила 6 и второй сервер закрыты T-463. Дополнена расхождением классов с v1.6 (таблица, скрипты, Go) | C-15, ADR-005 |
| 10 | Соль `MV_TELEGRAM_ACTION_KEY_SALT` | Законно пуста: без соли ключ HMAC `action_key` выводится из токена (T-310); форма `${VAR:-}`, `:?` остановил бы стек, который вправе стартовать (T-464, ревью #1 Mi-1). Одна строка §3.1.1 п. 3 заменена; §4.2 не тронут | `infrastructure.md` §3.1.1 |
| 11 | ADR-001 и §17 | Последнее предложение «Условий пересмотра» зачёркнуто, критерий потребителей — новым абзацем. В §17 `contracts.md` — примечание о четвёртом исключении | ADR-001; `contracts.md` §17 |

### Изменённые файлы
- `Docs/dev-team/architecture/contracts.md` — v0.15: строка версии, сводка v0.15; C-01 v1.11 (заголовок, «Время корневых событий», «Запись сессии», «Чтение журнала», история); C-02 v1.8 (заголовок, «Семантика», «Связка», «Причина отказа», пометка v1.5, «Издатель bootstrap», «Мир в конверте», новый пункт «Решения по T-056», «Заглушка», история); C-03 v1.4 (заголовок, пункт, история); C-05 v1.8a (заголовок, п. 9, история); C-14 v1.3 (заголовок, «Уточнение v1.3», история); C-15 v1.6 (заголовок, `invalid`, пункт «`@` в значении», «Печать значения», история); §17 (строка C-02, примечание).
- `Docs/dev-team/architecture/components/state-and-mechanics.md` — §3.2, §4.1, §4.5 п. 1, 2, 3, 7, 8, 9, §4.6 (отдых, строки `author`/`system`), §4.10, §6.1, §6.2, §8, §10.
- `Docs/dev-team/analysis/data-model.md` — §3.3.
- `Docs/dev-team/architecture/adr/ADR-001-modular-monolith-topology.md` — «Условия пересмотра» дополнения 2026-09-13.
- `Docs/dev-team/architecture/adr/ADR-005-llm-gateway-providers-models.md` — доп. 2 п. 3: `invalid`, «`@` в значении», «Печать значения», «Совместимость».
- `Docs/dev-team/architecture/infrastructure.md` — §3.1.1 п. 3 (одна строка).
- `Docs/dev-team/epics/EPIC-001-foundation/{tasks/T-470.md (новый), dev-log.md}`.

Не менялись: код, схемы, `testdata/**`, `.env.example`, `infrastructure.md` §4.2, `swarm-llm-laws.md`, `plan/ownership.md`, `tasks.md` эпиков.

### Передать
В `tasks.md` эпиков не внесены — передаёт оркестратор. *Приёмка (tech-lead#1, 2026-09-14):* строки EPIC-001 (T-468, T-469) внесены в `../tasks.md` — раздел T-468 и раздел T-470; строки EPIC-002 (T-471, T-472, T-057, приёмка T-056) и EPIC-003 (T-212, T-237, XS-задача `@` в `IsLocalEndpoint`) остаются здесь — их вносят тимлиды этих эпиков, передаёт оркестратор.

**EPIC-002 (tech-lead#2).**
- **T-471** (после T-056 и T-470; высокий приоритет, до интеграции EPIC-003 I1).
  1. **Законы в процессе.** `cmd/multiverse/contexts_state.go` строит `state.New(state.Config{Invariants: …})` из `rules/dark-forest.yaml` (`mechanics.Load` → `Rules.Invariants()`). Если правила не загрузились, контекст `state` отказывает в старте, и отказ называет файл и откуда он читается; State без законов не стартует. Источник пути один: рабочая папка процесса, как у `FakeContext`, или переменная манифеста `shared/env`, объявленная тем же изменением со строкой `.env.example` (§16 п. 8, просмотр tech-lead#1). Образ платформы несёт `rules/` (`build/Dockerfile`, просмотр tech-lead#1). Тест процесса `--contexts=all --bus=memory`: агент `task` предлагает игроку `set hp = hp_max + 1` с `cause=combat` → `law_violation {invariant_id: inv-02}`. Мутант «`state.Config{}` без законов» краснеет.
  2. **Норма отдыха (C-02 v1.8 п. 3), если T-056 её не закрыла.** При `cause=rest`: путь, кроме `hp`, — `level_violation`; `hp < hp_max` — `level_violation`; `hp > hp_max` или нет целого `hp_max` — `law_violation {inv-02}`; `hp` не целое — `invalid_op`; непустой `encounter_id` у сущности до операций — `law_violation` без `invariant_id`. Порядок: норма пути (шаг 6) → встреча → вид `hp` → значение. Строки матрицы: rest с `position` → `level_violation`; rest `hp_max − 1` → `level_violation`; rest ниже нуля → `level_violation` (строка итерации 3 T-056 «rest below zero → inv-02» меняет ожидание); rest без `hp_max` → `inv-02`; rest во встрече, который пишет `position`, → `level_violation`. Мутанты «не выше вместо равно», «rest пишет `position`», «встреча раньше нормы пути» краснеют. Издатели не меняются: `BuildRestProposal` (EPIC-004) и `Harness.Rest` пишут один `set hp = hp_max`.
  3. **Строка `system` (C-02 v1.8 п. 6), если T-056 её не закрыла.** `shared/contracts/ownership.go`: строка `system` без типов, путей, причин и `Create`, как у `object` и `monitor` (строка EPIC-002, ревью system-architect и tech-lead#1, §16 п. 6); константа `ProposerSystem` остаётся. Тесты: (1) bootstrap (`source=mvctl`, `cause=init`, `meta.actor_kind=system`) создаёт мир как `author`; (2) `meta.agent{level: system}` → `level_violation`; (3) `source=core/state` без агента, `cause=init` → `level_violation`; (4) `meta.actor_kind=system` при `source=swarm` без агента → `level_violation`; (5) `TestOwnershipRulesCoverEveryProposer` проверяет, что строка `system` пуста. Мутант «строка `system` с правами `*`» краснеет на (5).
- **T-057** (C-14 v1.3).
  1. `shared/contracts/registry_state.go`: у `snapshot.created` — `worldRequired(…)` (`Policy.World: WorldRequired`); ревью system-architect, обязательный просмотр tech-lead#1 (§16 п. 8).
  2. `shared/contracts/registry_test.go` закрепляет политику мира у трёх типов: `entity.create.proposed`, `entity.update.proposed` (бэклог отметки tech-lead#1 T-056) и `snapshot.created`. Мутант «без `WorldRequired`» краснеет в самом `shared/contracts`.
  3. State строит `snapshot.created` через `NewRoot`/`Derive` с миром снапшота. Тест: `Publish` без мира → `ErrPolicyViolation`.
  4. Мир есть в фикстурах и примерах: `testdata/fixtures/events/snapshot.created.v1.valid.json`, пример `snapshot.created` в `shared/contracts/validate_test.go`, событие из указателя в `test/fixtures/fixtures_test.go` и `shared/testkit/state/snapshot_test.go`; `go run ./cmd/mvctl contracts check` — exit 0.
  5. Уведомление tech-lead#2 (EPIC-003) и tech-lead#3 (EPIC-004): издатели `swarm` и `gateway` из `Spec.Publishers` строят `snapshot.created` с миром в конверте. Политику `analytics.replay.completed` не менять.

**EPIC-003 (tech-lead#2).**
- **T-212** (C-01 v1.11). `llm_records` для `providers/recorded` и для любого справочного чтения в `internal/llm` читается только `recording.ReadJournal`, разбор — после чтения. Своего обработчика над `Deps.Journal.ReadRange`/`Tail` для записей LLM нет: в replay такой обработчик — доставка, он двигает часы процесса и ставит `Meta.Replay`. Тест: двойник `eventbus.Journal`, который отвечает ошибкой, если у контекста обработчика нет `recording.InReadJournal`, — источник replay без записи строится без ошибки. Пока не решён вопрос о чтении истории на валидирующем журнале (открытый вопрос 1 ниже), «ошибки нет» не считается признаком полной записи; промах `recorded` остаётся громким (`ErrIncompleteRecord`).
- **T-237** (C-01 v1.11). Строка DoD окна бюджета («`recording.ReadJournal(…)` … с обработчиком `llm.Budget.Handle`») заменяется: чтение — `rec, err := recording.ReadJournal(ctx, Deps.Journal, llm_records, from)`, ошибка чтения — ошибка догона; затем `llm.Budget.Handle` (или `Observe`) по каждому `rec.Events(recording.TypeLLMOutput)`; `ErrBudgetKey` разбирается здесь, после чтения, а не внутри `ReadRange`; в replay это чтение часы процесса не двигает. Тест — с тем же двойником журнала, требующим метку. Догон роя по доменным топикам (`agent.spawned/stopped`, `tick.fired`) остаётся доставкой через `CatchUp`/`ReadRange` со своим обработчиком: часы в replay двигаются, как в T-060.
- **Новая задача XS (номер — оркестратор; C-15 v1.6, Go).** `internal/llm`: `IsLocalEndpoint` и разбор конфигурации отвечают `invalid` на `@` в любом месте значения, раньше классификации хоста; текст ошибки не называет хост и значение (правило `aboutTheHost`). В `testdata/llm/local-endpoints.tsv` — три строки из строки T-468 ниже, побайтно и в том же месте: тогда слияние develop → EPIC-003 чистое при любом порядке. `endpoint_table_test.go` зелёный; мутант «`@` только в данных пользователя» краснеет. Бэклог ревью #2 T-451, п. 1 (`LogValue` при `@`), закрывается этой задачей: значение с `@` до `LogValue` не доходит, это закрепляет тест отказа конфигурации. Порядок: задача сливается в EPIC-003 раньше или в той же синхронизации, что приносит T-468, иначе табличный тест Go краснеет на синхронизации.

**EPIC-001 (tech-lead#1).**
- **T-468** (C-15 v1.6; ответ system-architect на зависимость карточки).
  1. Правило печати относится ко всему выводу. Значение с `@` в любом месте — `invalid`, поэтому до строк п. 1–4 описания T-468 оно не доходит, и их маскировка не нужна. П. 1–4 описания и DoD 1, 5 снимаются строкой в «Выполнении» со ссылкой на C-15 v1.6; объём задачи — пункты ниже.
  2. `testdata/llm/local-endpoints.tsv` — три строки сразу после строки `http://@127.0.0.1:8888` (разделитель — табуляция; побайтно те же строки ставит задача EPIC-003 для Go):
     ```text
     http://pw:2024/x@10.0.0.5:8080	invalid	an @ in the path: what stands in front of it may be the end of a password with a bare /
     http://127.0.0.1:8888/v1@x	invalid	an @ in the path of a loopback address with a port
     https://api.openai.com/v1/x@y	invalid	an @ in the path of a cloud address
     ```
  3. `scripts/lib/llm-endpoint.sh` и `scripts/lib/LlmEndpoint.psm1`: `@` в сыром значении (`$LLM_EP_RAW` / `$ep.Raw`) → класс `invalid` с отдельным видом (например, `at-sign`) до классификации хоста, в том числе у `cloud`-хоста. Фраза отказа называет переменную и схему с хвостом маски, как `U19`; хост и значение не называет. Тексты sh и ps1 совпадают (P01/P02). Ответ один для `llm_endpoint_classify` (правило 6 `compose-lint`) и `llm_endpoint_resolve` (`llm-server`, `llm-bench`).
  4. Стенд паритета: `T01`/`T02` принимают три новые строки (слова DoD 6 карточки «классы таблицы не меняются» больше не верны). Сценарии: `llm-server up`, `llm-server health` и `llm-bench` с `@` в пути `MV_LLM_URL` — отказ до пробы, `AbsentFold` — голова пароля и хост; по сценарию без `@` на группу — строки прежние. Мутант «`@` в пути принимается» краснеет в каждой половине; контрольный мутант первым.
  5. Если найдётся путь, где значение с `@` обходит `llm_endpoint_resolve` (например, адрес из матрицы `llm-bench` без судьи), он закрывается отказом, а не маской.
  6. DoD 7 карточки: после слияния T-468 и задачи EPIC-003 — строка для system-architect «снять пометку „Код расходится“ в C-15 v1.6 и ADR-005».
  7. `infrastructure.md` §3.1.2 и §6.3.1 — число строк таблицы, сценариев и мутантов (через system-architect).

### Открытые вопросы (не решались в этой задаче)
1. Чтение истории на валидирующем журнале: запись, не прошедшая схему, паркуется в `dead_letters` при каждом `ReadJournal` и выпадает из записи без ошибки. Допускаем дубли в `dead_letters` или чтение истории повторно не паркует (бэклог ревью #1 T-458, п. 3) — до T-212 и T-237.
2. Где живёт e2e бота на `FakeGateway`/`membus`: depguard `cmd-telegram-bot` запрещает их в тестах (N-1 ревью T-461) — до T-315.
3. Единица `response_len` в C-07 (байты UTF-8 или руны).
4. `MV_STATE_WORLDS` и `MV_WORLD_ID` в `infrastructure.md` §4.2 (О-2 отметки T-055; эталон под отпечатком gitleaks — нужна процедура правки).
5. Журнал удалений и повтор `/forget` после восстановления `links.db` (Mi-8 T-318).
6. Сентинел ошибки схемы при `Publish` (`ErrInvalidPayload` → `publish_rejected`) и подписка или партиция `system_events` на мир для процесса с несколькими мирами (приёмка T-055).
7. Признак оценки токенов (`Estimated`) в `llm.Response` и схеме `llm.output.tokens`; откуда шлюз берёт модели блупринтов для `Health` (T-208, T-212).
8. `no-testkit-in-production` на `**/shared/**` без `**/shared/testkit/**` и тело `403` `runtime.AdminOnly` в форме `Error` (бэклог T-458) — задачи EPIC-001, решение по форме уже есть, нужна постановка.

### Проверки
- CRLF, NUL, BOM — по каждому изменённому файлу скриптом: у всех число `\r\n` равно числу `\n`, NUL 0, BOM нет.
- Тексты C-15 и ADR-005 (`local`, `invalid`, `cloud`, порт, точка на конце, «`@` в значении», «Печать значения») совпадают построчно — сравнение скриптом.
- `infrastructure.md` — одна строка в диффе (§3.1.1 п. 3); §4.2, `.env.example` и `swarm-llm-laws.md` в диффе отсутствуют.
- `gitleaks dir --redact --config=.gitleaks.toml` по копии семи изменённых файлов и черновика записи `dev-log.md` в scratch `t470-gl` — no leaks found; копия удалена по точному пути.

### Итерация 2 (по ревью #1 code-reviewer#1: 0/1/2/4 и решениям оркестратора) · 2026-09-13
system-architect#1 (Opus). Правки — в той же рабочей папке, коммитов нет.

| # | Решение | Где |
|---|---|---|
| Ma-1 | Единственный источник перечня скаляров — таблицы `data-model.md` §3; контракт отдельного списка не держит, список кода сверяется с §3. Расхождения с `scalarAttributes` T-056 и решение по каждому: **`encounter_chance`** (есть в коде и фикстуре региона, не было в §3) — строка в §3.2 (`number`, обяз., `0 ≤ x ≤ 1`); **`loot_claimed_by`** (есть в коде, КД §4.5 п. 5 и §4.6, не было в §3.4) — строка в §3.4 (`ref Character`, инвариант 3); **`name`** (общий атрибут §3) и **`last_session_ended_at`** (§3.3) — скаляры §3, в список кода их добавляет приёмка T-056 (решение оркестратора, сообщение tech-lead EPIC-002 отправлено), поэтому пометки «Код расходится» у них нет. C-02 v1.8 п. 2 и КД §3.2 ссылаются на §3 одинаково: «только таблицы §3, включая общий `name`, без `scope`» | `data-model.md` §3.2, §3.4; C-02 v1.8 п. 2; КД §3.2 |
| Mi-1 | «**Код расходится до T-471**»: у C-02 v1.8 п. 3 (State T-056 проверяет отдых как `hp` в `[0, hp_max]` без нормы пути и без `level_violation` при `hp < hp_max`) и п. 6 (строка `system` в `ownership.go` с `*`, `init, author`, `Create`), ссылкой — у «Связки», «Издателя bootstrap», КД §4.1, §4.6 (абзац «Отдых» и строка `system`). У «Заглушки» C-02, §17 и КД §8 — оговорка слияния: в EPIC-002 со слиянием T-056, в develop, EPIC-001, EPIC-003, EPIC-004 до контрольного слияния EPIC-002 — двойник v0. КД §4.10 говорит о строке `author`, она с кодом совпадает — пометки не требует | C-02; §17; КД §4.1, §4.6, §8 |
| Mi-2 | «Контейнеры §3 (`canon[]`, `spawned_by`, `inventory[]`, `participants[]`) и атрибуты вне §3 скалярами не считаются: модель открыта» | C-02 v1.8 п. 2; КД §3.2 |
| N-1 | Названа T-472: C-02 v1.8 п. 2, сводка, история C-02, КД §3.2. C-05 v1.8a добавлен в заголовок карточки | `contracts.md`; КД; карточка |
| N-2 | «В таблице строк с `@` в пути пока нет (их добавляет T-468), а скрипты и Go отвечают по хосту» — одинаково в C-15 и ADR-005; строка сводки поправлена | C-15; ADR-005; сводка |
| N-3 | «…индекс длиннее может переполнить `int` 32-битной платформы, а за 19 цифрами — любой» | C-02 v1.8 п. 2 |
| N-4 | «у тех из них, что передаются в композицию, — `${VAR:-}` (`MV_ANTHROPIC_API_KEY` в композицию пока не передаётся — T-469)»; строка одна, эталоны §4.2 не сдвинуты | `infrastructure.md` §3.1.1 п. 3 |

**Передать (итерация 2).**
- **T-469 (EPIC-001, devops).** `MV_ANTHROPIC_API_KEY` передаётся сервису `core` формой `${MV_ANTHROPIC_API_KEY:-}` (законно пустой облачный секрет, `infrastructure.md` §3.1.1 п. 3), без `:?` и без ключа без значения; фикстура правила 3 `compose-lint` на литерал этой переменной. После передачи оговорка «пока не передаётся — T-469» в §3.1.1 п. 3 снимается через system-architect.
- **Приёмка T-056 (tech-lead EPIC-002).** В `scalarAttributes` — `name` и `last_session_ended_at` (решение оркестратора); строка матрицы `set name.x` → `invalid_op`. Комментарий теста `TestEveryScalarOfTheDataModelIsListed` называет `encounter_chance` (§3.2) и `loot_claimed_by` (§3.4) — теперь это верно.
- **T-471.** После нормы отдыха и пустой строки `system` — строка для system-architect: снять пометки «Код расходится до T-471» в C-02 v1.8 п. 3, п. 6, «Связке», «Причине отказа по связке» *(добавлено при приёмке, N-5 ревью #2)*, «Издателе bootstrap» и КД §4.1, §4.6.
- **T-472 (бэклог ревью #1, п. 1; изм. при приёмке — Mi-3 ревью #2).** Тест полноты скаляров строить из таблиц сущностей `data-model.md` §3 — общие атрибуты, §3.1–§3.4, §3.6, §3.7, без §3.5 Item (его поля — значения внутри `inventory[]`, а не атрибуты) — по колонке «Тип», а не из рукописного списка. `как у Character` раскрывается по §3.3, `int / формула` считается целым; служебные корни `ReservedPaths` и `scope` исключаются явно. *Прежде:* «из таблиц `data-model.md` §3».

**Проверки итерации 2.** CRLF у всех изменённых файлов, NUL и BOM нет; тексты C-15 и ADR-005 о правиле адреса совпадают построчно; дифф `infrastructure.md` — одна строка; `gitleaks dir --redact --config=.gitleaks.toml` по копии в scratch `t470i2-gl` — no leaks found, копия удалена по точному пути.

## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Critical | Major | Minor | Nit | Запись |
|---|---|---|---|---|---|---|---|---|
| 1 | 2026-09-13 | code-reviewer#1 (TEAM-1) | **вернуть** (Ma-1: скаляры C-02 v1.8 п. 2 по ссылке на `data-model.md` §3 расходятся с `scalarAttributes` T-056 — `name`, `last_session_ended_at` нет в коде, `encounter_chance`, `loot_claimed_by` нет в §3; DoD 3 не выполнен; Mi-1: строка `system` «пуста», норма rest и «Заглушка» без пометки о коде до T-471 и слияния T-056; Mi-2: `canon[]`, `spawned_by` названы «вне §3»; N-1…N-4) | 0 | 1 | 2 | 4 | `../review.md` § «T-470 · ревью #1» |
| 2 | 2026-09-13 | code-reviewer#1 (TEAM-1) | **принять** (Ma-1, Mi-1, Mi-2, N-1…N-4 закрыты: `data-model.md` §3 совпадает со `scalarAttributes` T-056, кроме `name` и `last_session_ended_at` — их вносит приёмка T-056; пометки «Код расходится до T-471» сверены с `restRefusal` и `ownership.go`; регрессии нет. В приёмку: Mi-3 — «только таблицы §3» захватывает поля Item §3.5 (`item_id`, `acquired_at`, `source`), сузить источник в C-02 v1.8 п. 2, КД §3.2 и строке T-472; N-5 — пометка у примера rest в «Причине отказа по связке») | 0 | 0 | 1 | 1 | `../review.md` § «T-470 · ревью #2» |

## Приёмка (tech-lead)
tech-lead#1 (TEAM-1, Opus), 2026-09-14. Смотрел незакоммиченную рабочую копию `.worktrees/T-470` (база `55ec4c4`) против кончика `epic/EPIC-001-foundation` `b085228` (в нём T-463, T-464). Основание: DoD этой карточки, итерации 1–2, ревью #1 и #2 (`../review.md`), поручение оркестратора на приёмку. Код, схемы, `testdata/**` — только чтение; `.worktrees/T-056` — только чтение.

**Решение: принято** · 2026-09-14. **Итог:** `contracts.md` v0.15 и связанные документы вносят тексты просмотра T-056 и ревью T-458, грамматику пути под итерацию 4 T-056 и правило «`@` в значении — `invalid`» (C-15 v1.6); код догоняет в T-471, T-472, T-057, T-212, T-237, T-468 и XS-задаче EPIC-003, а слияние ждёт `name` и `last_session_ended_at` в `scalarAttributes` T-056 (п. 3).

### 1. Сверка DoD
| Пункт DoD | Чем закрыт | Итог |
|---|---|---|
| 1. Версии, история, сводка v0.15 | Строка версии `contracts.md` — 0.15, раздел «Изменения v0.15»; в строках «Версия» заголовков C-01 — v1.11, C-02 — v1.8, C-03 — v1.4, C-05 — v1.8a, C-14 — v1.3, C-15 — v1.6 (проверено скриптом); записи в истории — ревью #1, #2 | да |
| 2. Тексты совпадают с одобренными, отступления с доводом | Ревью #1 и #2 сверили тексты с просмотром T-056 и п. 6 ревью T-458; отступления — таблица «Решения» (1–11) и «Итерация 2» | да |
| 3. Пометки «(изм. T-470)»; C-15 и ADR-005 совпадают построчно | Вхождений «изм. T-470»: `contracts.md` — 17, КД — 16, `data-model.md` — 3, ADR-005 — 3, `infrastructure.md` — 1, ADR-001 — «уточнение T-470». Семь пунктов правила адреса в C-15 и ADR-005 попарно равны (ревью #2, п. 4); приёмка ADR-005 и C-15 не правила | да |
| 4. CRLF, NUL, BOM; нетронутые файлы; gitleaks | п. 6 | да |

### 2. Правки при приёмке
Точечные замены строк скриптом (CRLF сохранён, каждая подстрока найдена ровно один раз):
- **Mi-3 ревью #2.** C-02 v1.8 п. 2 и КД State §3.2: источник скаляров — таблицы сущностей `data-model.md` §3 (общие атрибуты с `name`, §3.1–§3.4, §3.6, §3.7); поля Item §3.5 (`item_id`, `acquired_at`, `source`) — значения внутри `inventory[]`, а не атрибуты, в перечень не входят. Прежняя редакция записана в скобках. В строке T-472 раздела «Передать» (итерация 2) — те же разделы, раскрытие `как у Character` по §3.3, `int / формула` — целое, `ReservedPaths` и `scope` исключаются явно. Строка сводки v0.15 и история C-02 дополнены одной фразой о приёмке.
- **N-5 ревью #2.** `contracts.md`, «Причина отказа по связке» (v1.5): у примера отдыха «при `cause=rest` путь, кроме `hp`, или `hp` ниже `hp_max`» — «**Код расходится до T-471** — v1.8 п. 3». Место добавлено в строку T-471 (итерация 2, «Передать»): пометку снимает system-architect вместе с остальными.
- **«Передать».** Строки EPIC-001 внесены в `../tasks.md`: T-468 — пункт «Объём по C-15 v1.6» в разделе T-468; T-469 — в разделе T-470 (раздел T-469 есть только на кончике эпика). Строки EPIC-002 (T-471, T-472, T-057, приёмка T-056) и EPIC-003 (T-212, T-237, XS-задача) остаются в карточке; их вносят тимлиды этих эпиков, передаёт оркестратор. Абзац «Передать» дополнен этой отметкой.
- Метаданные: статус `done`, завершена 2026-09-14.

`data-model.md`, ADR-001, ADR-005 и `infrastructure.md` при приёмке не менялись.

### 3. Сверка со `scalarAttributes` T-056 — условие слияния
Проверено 2026-09-14 чтением `.worktrees/T-056/internal/state/proposal.go:255-269` и `shared/entity/attrs.go` (рабочая копия не закоммичена, `proposal.go` изменён 2026-09-13 23:18). В `scalarAttributes` 36 имён; **`name` и `last_session_ended_at` в списке нет**. Констант `AttrName` и `AttrLastSessionEndedAt` в `attrs.go` тоже нет. Приёмка T-056 (tech-lead#2) записана как «принято» и, судя по п. 6 её «Правок при приёмке», правила только комментарии: решение оркестратора о двух именах в неё не вошло.

**Условие:** T-470 сливается в `epic/EPIC-001-foundation` только после того, как `name` и `last_session_ended_at` появятся в `scalarAttributes` T-056 (со строкой матрицы `set name.x` → `invalid_op`). Если T-470 нужно слить раньше, перед слиянием system-architect ставит пометку «Код расходится» у C-02 v1.8 п. 2 и КД §3.2 для этих двух имён. Приёмка пометку не ставила. Условие записано в раздел T-470 индекса.

### 4. Индекс `../tasks.md` → v0.1.5
- Версия 0.1.5 (0.1.4 — приёмка T-464 на кончике эпика) и строка правок приёмки T-470.
- Раздела T-470 не было, он добавлен перед разделом T-468. Статус `done`, условие слияния, строка приёмки, передачи, бэклог.
- Раздел T-468: пункт «Объём по C-15 v1.6» (семь подпунктов) перед «Меткой».
- T-466: раздела в индексе нет. Отметка «закрыта оркестратором как покрытая T-470» есть в строке правок версии и в разделе T-470 («Покрывает»).

### 5. Слияние с кончиком `b085228`
Трёхсторонний `git merge-file` на LF-копиях в scratch `t470acc-merge`: база `55ec4c4`, «наши» — рабочая копия, «их» — `b085228`.

| Файл | Кончик менял | Итог |
|---|---|---|
| `contracts.md` | нет | чисто |
| `infrastructure.md` | нет | чисто |
| `ADR-005-…` | нет | чисто |
| `tasks.md` | да (T-464, T-469) | **2 конфликта**, оба в шапке |
| `review.md`, `dev-log.md` (справочно) | да | простой `merge-file` даёт по одному конфликту хвоста. Драйвер `appendtail` (`.git/info/attributes`, `merge.appendtail.driver`) на копиях — rc 0, конфликтов нет, записи T-470 и T-464 на месте |

Конфликты `tasks.md` и разрешение:
1. Строка 3: «Версия 0.1.5 · 2026-09-14» против «Версия 0.1.4 · 2026-09-13». Взять 0.1.5.
2. Строка правок после приёмки T-463: у нас — «приёмка T-470; версия 0.1.5», на кончике — «приёмка T-464; версия 0.1.4». Оставить обе строки, сначала 0.1.4.

После такого разрешения маркеров нет. Порядок разделов: T-463, T-470, T-468, T-464, T-469. Пункт T-468 и раздел T-470 сливаются без конфликтов. Кто разрешает: конфликт механический, в индексе tech-lead#1, текст разрешения дан выше. Разрешает оркестратор при слиянии, разработчик не нужен. Копия `t470acc-merge` удалена по точному пути.

### 6. Гигиена
- CRLF, NUL, BOM: у всех изменённых файлов и карточки число `\r\n` равно числу `\n`, NUL и BOM нет (скрипт).
- `infrastructure.md`: одна строка в диффе (`@@ -357 +357 @@`), 1397 строк, строка 520 под отпечатком `.gitleaksignore` (`infrastructure.md:generic-api-key:520`) побайтно равна `HEAD` и `b085228`.
- `.env.example`, `swarm-llm-laws.md`, `.gitleaksignore` в диффе отсутствуют; код и схемы не менялись.
- `gitleaks dir --redact --config=.gitleaks.toml --gitleaks-ignore-path=.gitleaksignore` по копии десяти файлов (девять изменённых и `T-470.md`) в scratch `t470acc-gl`: no leaks found. Копия удалена по точному пути.
- Тесты и линтер не запускались: задача документная, Go-файлы в диффе отсутствуют.

### 7. Замечания приёмки
Блокирующих нет. Условие п. 3 — не дефект T-470: расхождение возникло между решением оркестратора и записанной приёмкой T-056.

### Предложения в бэклог
1. Оркестратор, tech-lead#2: довнести `name` и `last_session_ended_at` в `scalarAttributes` T-056, добавить строку матрицы `set name.x` → `invalid_op` и отметить это в приёмке T-056. Иначе нужна пометка в C-02 v1.8 п. 2 (п. 3).
2. system-architect, после T-471: в КД §4.1 сократить зачёркнутый хвост T-444 до ссылки на C-02 v1.8 п. 6 (ревью #2, бэклог п. 2).
3. Оркестратор: открытые вопросы 1–8 карточки — в очередь system-architect; вопрос 1 — до T-212 и T-237.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Выполнение | 13.09 23:14 | 13.09 23:40 | 26m |
| Ревью #1 | 13.09 23:40 | 13.09 23:52 | 12m |
| Итерация 2 | 13.09 23:52 | 14.09 00:35 | 43m |
| Ревью #2 | 14.09 00:35 | 14.09 00:43 | 8m |
| Приёмка | 14.09 00:43 | 14.09 00:52 | 9m |
| Слияние | 14.09 00:59 | 14.09 01:02 | 3m |
| **Всего** | | | **101m** |
