# T-399: Привести infrastructure.md к состоянию после T-397

| Поле | Значение |
|---|---|
| Инициатива / эпик / команда | EPIC-001 / EPIC-001 / TEAM-1 |
| Волна | 1 |
| Исполнитель | architect#1 |
| Размер | см. индекс `tasks.md` |
| Статус | done (принята tech-lead#1 после ревью #1 и итерации 2 (Minor и Nit закрыты)) |
| Итераций ревью | 1 |
| Начата / завершена | 2026-09-13 10:31 / 2026-09-13 11:13 |

> Карточка заведена задним числом при переходе на плагин 0.6.0. Разделы
> «Описание» и «DoD» живут в индексе `tasks.md` — дублировать их сюда значило бы
> завести второй источник истины. Подробности выполнения — в `dev-log.md`,
> замечания и вердикты — в `review.md`, решения — в `journal.md`.

## Описание и DoD
См. раздел T-399 в `../tasks.md`.

## Выполнение (architect)
2026-09-13 · architect#1 (Opus; ступень ниже плановой Fable). Ветка `task/T-399-infrastructure-after-t397`. Правки только в `Docs/dev-team/architecture/infrastructure.md`, код и конфиги не менялись.

**Ограничения постановки:**
- §6 не трогать: параллельно его правят T-437 (§6.2, §6.2.1, §6.5) и T-440 (§6.4);
- историю версий не переписывать — вместо этого строка редакционной правки;
- сверять с деревом;
- правила линтера — по строке на каждое, со ссылками на скрипт и README фикстур.

**Сверено с деревом:**
- `docker-compose.yml`, `docker-compose.bot.yml`, `docker-compose.legacy.yml` — шапки, сервисы, `profiles:`;
- `Makefile:36–82` (`COMPOSE_ENV_FILES`, `ACTIVE_PROFILES`, `COMPOSE_FILES`, `COMPOSE`), цели `up`, `legacy-src`, `down`, `reset`, `logs`, `compose-lint`;
- `scripts/compose-lint.sh` — шапка правил 1–8, набор файлов по умолчанию, чтение `.env.example` для правила 7, реализация правил 1–8;
- `testdata/compose-lint/README.md`;
- `.env.example`, `build/versions.env`, `.github/ci.env`;
- `.github/workflows/go.yml` — задание `compose-lint` и чтение `versions.env`;
- `README.md` — врезка «Профили `bot` и `legacy`», `CLAUDE.md`;
- `review.md`, раздел «T-397 · ревью #1».

**Правки по разделам:**
1. **Шапка.** Строка «Редакционная правка T-399» перед «Изменения v0.3». Истории v0.2/v0.3 не тронуты.
2. **§0.** Строка «Топология» — три файла и кто их подключает. Строка «Конфигурация» — формат `.env.example` и `[required]`.
3. **§1.3.**
   - В таблице профилей появился столбец «Файл». У `neo4j` указаны два профиля, `memory` и `legacy`.
   - У `legacy` — сборка из `LEGACY_SRC_REF`. У `bot` — вне набора `.env.example` до EPIC-004.
   - Абзацы «Почему файлов три» (интерполяция до отбора по профилю, `MV_TELEGRAM_BOT_TOKEN` и `CHROMA_IMAGE`, правило 7 как защита основного файла) и «Как файлы подключаются» (только `make`; голый `docker compose --profile bot` не поддерживается — ссылка на README).
   - Состояние профилей на 2026-09-13. `COMPOSE_PROFILES` в `.env.example` — `memory`.
4. **§1.4 и §10 (F-6).** Ссылка на правило «нет сервиса `llama-server`» исправлена: §3.1.1 п. 5 → п. 6 (нумерация T-416).
5. **§2.2.**
   - `make up`, `down`, `reset`, `logs` идут через `$(COMPOSE)`.
   - Новая строка `make legacy-src`. У `make compose-lint` появился второй шаг `--fixtures`.
   - Абзац «Как Makefile собирает команду compose»: `COMPOSE_ENV_FILES`, разбор `COMPOSE_PROFILES` из `.env` (последнее вхождение, `export`, пробелы, комментарий, кавычки — ревью T-397 Mi-1), порядок `-f` с `docker-compose.override.yml` последним (ОВ-40), `PROFILE_ARGS`.
6. **§2.4.** Эталон `build/versions.env`: инлайн-комментарии перенесены строкой выше, `CHROMA_IMAGE=` пуст (D-3). Добавлено правило «инлайн-комментарии запрещены» с причиной (`include` в Makefile, `$GITHUB_ENV`) и оговорка, что полный состав и значения — в файле. Вне перечня задачи, но это тот же класс дефекта, что Mi-2.
7. **§3.1.**
   - Общий шаг CI: `grep -E '^[A-Z][A-Z0-9_]*='` вместо `cat`.
   - Строка задания `compose-lint`: `docker compose version` и `--fixtures`.
8. **§3.1.1.** Раздел переписан как перечень:
   - абзац «Что читается»: три файла по умолчанию; первый `-f` заменяет набор; две модели чтения (T-429);
   - восемь правил, по строке на каждое, со ссылками на `scripts/compose-lint.sh` и `testdata/compose-lint/README.md`;
   - в правиле 7 — запрет инлайн-комментария после пустого значения, обе половины (T-397, T-413) и блок комментария (T-404);
   - в правиле 8 сохранена фраза T-432 про `${OLLAMA_X:-d}` и `${OLLAMA_X-d}`;
   - абзац о фикстурах сокращён до контракта самопроверки.

   Развёрнутые ветви правил 3 и 8 и перечень условий отказа `--fixtures` из текста раздела убраны: они живут в шапке скрипта и README фикстур.
9. **§4.1 п. 5 (новый).** Формат `.env.example`:
   - комментарий к пустому значению — строкой выше, с причиной;
   - после непустого значения инлайн-комментарий допустим;
   - в `versions.env` и `ci.env` инлайн-комментарии запрещены;
   - `[required]` — в сплошном блоке комментария над переменной, единственный список обязательных;
   - сверка пометок в обе стороны правилом 7.
10. **§4.2.**
    - Эталон переписан в текущем формате. Добавлены `MV_CORE_ADMIN_CLIENTS`, `MV_SWARM_FAKE`, `#OLLAMA_ORIGINS` и три `#LEGACY_*`. `COMPOSE_PROFILES=memory`, пути `MV_LLM_*` пусты (кавычки для путей Windows).
    - Пометка о том, что пояснения сокращены и при расхождении прав файл.
    - Абзац проверки формата. Подраздел «Что изменилось в v0.3» — запись истории, не тронут.

**Проверка (только чтение):**
- `grep -nE '^[A-Za-z_][A-Za-z0-9_]*=[[:space:]]*#'` по блоку §4.2 и по `.env.example` — пусто; по блоку §2.4 `grep -nE '^[A-Z][A-Z0-9_]*=.*#'` — пусто.
- Набор ключей (включая закомментированные `#KEY=`) блока §4.2 и `.env.example` — `diff` пуст.
- Переменные с `[required]` в сплошном блоке комментария над ними (тем же правилом чтения, что у линтера) — одинаковые семь в блоке и в файле: `MINIO_ROOT_USER`, `MINIO_ROOT_PASSWORD`, `NEO4J_PASSWORD`, `MV_MINIO_ACCESS_KEY`, `MV_MINIO_SECRET_KEY`, `MV_LLM_URL`, `MV_NEO4J_PASSWORD`. Первый прогон этой проверки поймал `MV_TELEGRAM_BOT_TOKEN`: пояснение «без [required]» стояло в том же блоке комментария. Формулировка заменена, блок отделён пустой строкой.
- `git diff` T-437 и T-440 против `adadca5` по `infrastructure.md` — только §6 (`:805–841`, `:985`, `:999`). Мои правки лежат в шапке и §0–§4 и §10 — пересечений по строкам нет.
- `make compose-lint` и `docker compose config` не запускались: код и compose не менялись, проверять нечего.

**Замечено вне задачи, не правилось (бэклог):**
- §2.2 `make build` — «три бинарника», в `cmd/` сейчас два: `multiverse`, `mvctl`.
- §2.2 `make contracts` — `blueprint validate` в цели нет.
- §2.2 `make secrets-scan` — второй проход читает содержимое индекса, а не рабочую копию (ОВ-8).
- §2.2 `make up`/`llm-health` — `GET $MV_LLM_URL/health` без учёта перехода на `/v1/models` (T-404).
- §0/§2.3 — «один образ с тремя бинарниками».

Запись в `../dev-log.md` — «architect#1 · T-399». Не коммитил.

### Итерация 2 (architect, по ревью #1)
2026-09-13. Ревью #1 (code-reviewer#2): «принять», Minor 2, Nit 3. По решению оркестратора всё закрыто в T-399 до приёмки. Правки только в `infrastructure.md`, §6 побайтно равен базе `adadca5`.

- **Mi-1.** В пересказ правил вернулись условия, сверенные с кодом скрипта:
  - §3.1.1 п. 3 — литерал и `$${VAR}` запрещены у любого секрета, ключ без значения — только у обязательного (`MUST_BE_REQUIRED`, `compose-lint.sh:936-945`, `:963-967`); облачные `MV_LLM_API_KEY` и `MV_ANTHROPIC_API_KEY` законно пусты;
  - п. 8 — `${MV_X}`/`$MV_X` без модификатора запрещены только при непустом умолчании манифеста (`:1290-1310`); `:+`/`+`, умолчание из другой подстановки, необъявленное или выведенное имя запрещены всегда (`unknown`, `check`).
- **Mi-2.** §9 сверен с `Docs/ops/runbook.md` (разделы 2 и 6, `:130-142`, `:358-374`) только в местах про профили и файлы композиции:
  - вводный абзац §9 — для сервисов `bot`/`legacy` нужен ещё `-f` файла профиля;
  - §9.1 п. 1 — `COMPOSE_PROFILES` в примере `memory`, когда добавлять `bot` и `legacy`;
  - §9.2 — у `make up` и `make down` один и тот же `PROFILES=`, `restart` — только для сервисов основного файла;
  - §9.6 п. 2–3 — два пути, как в runbook: `make up PROFILES=<набор>,bot` или `docker compose -f docker-compose.yml -f docker-compose.bot.yml up -d telegram-bot`; логи — с тем же `-f`;
  - §9.8, столбец бота — «Старт / стоп» и «Логи» через `PROFILES=…,bot` либо с `-f`.

  Голого `docker compose up -d telegram-bot` в файле больше нет (грэп). Остальная сверка §9 записана строкой бэклога в разделе T-399 индекса.
- **N-1.** §2.4: запрет инлайн-комментариев — «шапка файла, T-004». Причина из шапки названа, довод про `$GITHUB_ENV` помечен как добавленный в T-399.
- **N-2.** §2.2: все цели с `$(COMPOSE)` перечислены по `Makefile` — `up`, `down`, `reset`, `logs`, `health`, `backup`, `restore`, `archive-legacy`, `deploy`, `rollback`. Добавлено правило «тот же `PROFILES=` у `up` и `down`», `PROFILES=` замещает набор.
- **N-3.** §0, `prod`: `memory,bot` — `bot`, когда появится бот (EPIC-004), до того `memory`.
- **Пути.** `docs/ops/runbook.md` → `Docs/ops/runbook.md` в заголовке §9, заголовке §9.8 и строке F-9 §10. Текст заголовков не менялся.
- **Шапка.** Строка «Редакционная правка T-399» дополнена итерацией 2.
- **Бэклог в индексе, раздел T-399:** остальная сверка §9 с runbook; `build/versions.env:10` (`cat` против `grep -E` в `go.yml`, devops-engineer); перенесены пункты бэклога итерации 1.
- **Проверка:** §6 рабочей копии и базы `adadca5` — `diff` пуст; грэп по файлу на `docker compose up -d telegram-bot` и строчный `docs/ops/runbook.md` — пусто. `make`, `compose-lint` и контейнеры не запускались: код не менялся.

## Ревью (code-reviewer)
| Итерация | Дата · ревьюер | Вердикт | Critical / Major / Minor / Nit | Суть |
|---|---|---|---|---|
| 1 | 2026-09-13 · code-reviewer#2 | принять | 0 / 0 / 2 / 3 | Утверждения §1.3, §2.2, §3.1.1 совпали с деревом (`make --eval`, `compose-lint` и `--fixtures` зелёные). Эталон §4.2 = `.env.example` по ключам, значениям и семи `[required]`. §6 не тронут. Mi-1: пересказ правил 3 и 8 потерял условие («обязательный секрет», «непустое умолчание»). Mi-2: §9.6/§9.8 — голый `docker compose up -d telegram-bot` против нового §1.3 |

Подробности — разделы T-399 в `../review.md`.

## Приёмка (tech-lead)
**Решение: принята** · tech-lead#1 (Opus) · 2026-09-13. **Итог:** `infrastructure.md` описывает топологию после T-397 так, как её собирают `Makefile` и три compose-файла, а эталоны §2.4 и §4.2 больше не воспроизводят запрещённый формат; замечания ревью #1 закрыты в итерации 2, это проверено по коду скрипта и по runbook.

Кода нет, тесты Go и `golangci-lint` не относятся к задаче. Проверка — сверка текста с деревом, мои прогоны `compose-lint` и сравнение с базой ветки. `make up/down`, `make test-integration`, `make llm-*` не запускались, стенд `:8888` и контейнеры не трогались. `.env` не открывался, в рабочей папке его нет. Не коммитил, `git add` не делал.

**DoD по пунктам** (раздел T-399 индекса, «Состав»)

| Пункт | Статус | Доказательство |
|---|---|---|
| Топология трёх compose-файлов и причина разделения | выполнен | §0 «Топология», §1.3: таблица со столбцом «Файл» совпала с `profiles:` трёх файлов — 11 сервисов в основном, `telegram-bot` в bot-файле, `chromadb`/`semantic-memory`/`narrative-orchestrator` в legacy-файле, `neo4j` — `["memory", "legacy"]`. Абзацы «Почему файлов три» и «Как файлы подключаются» сходятся с шапкой `Makefile:52-82`. §2.2: `COMPOSE_ENV_FILES`, разбор `.env` (`tail -n 1`, `export`, пробелы, комментарий, кавычки), порядок `-f` с override последним, `PROFILE_ARGS` — по `Makefile:43-82`; цели `up`, `legacy-src`, `down`, `reset`, `logs` — по `:302-338`; `compose-lint` с `--fixtures` — по `:260-267` |
| Правило 7 в списке правил линтера | выполнен | §3.1.1: восемь правил по строке, ссылки на `scripts/compose-lint.sh` и `testdata/compose-lint/README.md` разрешаются из `Docs/dev-team/architecture/`. Мой прогон в рабочей папке: `bash scripts/compose-lint.sh` — `ok — 15 services in 3 file(s), 8 rules (profiles resolved: bot, dev, gpu, legacy, memory)`, rc=0; `--fixtures` — `fixtures ok — 52 bad, 10 good`, rc=0. §3.1, задание `compose-lint`: `docker compose version` → `config -q` → скрипт → `--fixtures` → hadolint, как в `go.yml:414-452`; общий шаг `grep -E` — `go.yml:88`, `:162` и др. |
| Запрет инлайн-комментария после пустого значения и пометка обязательных в §4.1/§4.2 | выполнен | §4.1 п. 5 новый. Мой разбор блока §4.2 и `.env.example`: строки `KEY=`/`#KEY=` со значениями после снятия инлайн-комментария совпадают (70 строк, `diff` пуст); `KEY=<пробелы>#` нет ни там, ни там; `[required]` в сплошном блоке над переменной — одинаковые семь (`MINIO_ROOT_USER`, `MINIO_ROOT_PASSWORD`, `NEO4J_PASSWORD`, `MV_MINIO_ACCESS_KEY`, `MV_MINIO_SECRET_KEY`, `MV_LLM_URL`, `MV_NEO4J_PASSWORD`). §2.4: в блоке `versions.env` нет `KEY=…#`, в самих `build/versions.env` и `.github/ci.env` — тоже; упомянутые «позже добавлены» `LEGACY_*`, `ALPINE_IMAGE`, `MINIO_COMMIT` в файле есть |
| §6 не тронут, история не переписана (ограничение постановки) | выполнен | §6 (от `## 6.` до `## 7.`) рабочей копии побайтно равен §6 `adadca5`, 257 строк. «Изменения v0.3» в шапке и «Что изменилось в v0.3» в §4.2 равны базе; строка «Редакционная правка T-399» вставлена перед историей |
| Тесты, линтер, dev-log | выполнен | тестов у задачи нет (документ); `compose-lint` зелёный, см. выше; запись «architect#1 · T-399» в `dev-log.md` с итерацией 2 есть |

**Проверка итерации 2** (повторного ревью не было)
- **Mi-1 — закрыт.**
  - Правило 3 сверено с `compose-lint.sh:927-977`. Литерал и `$${VAR}` запрещены у любого ключа из `SECRET_KEY`. Ключ без значения запрещён только у `MUST_BE_REQUIRED`. У обязательных нужна подстановка с `:?`. `MV_LLM_API_KEY` и `MV_ANTHROPIC_API_KEY` в `MUST_BE_REQUIRED` нет. Текст не мягче и не строже скрипта.
  - Одно исключение правлено при приёмке, одной строкой: «только подстановка **своей** переменной» было строже скрипта. Код принимает подстановку любой переменной, и так написан действующий `docker-compose.yml:371` (`NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:?…}`), а legacy-файл пишет `MINIO_ACCESS_KEY: ${MV_MINIO_ACCESS_KEY:?…}`. Теперь в тексте: «только подстановка переменной, не обязательно одноимённой (`NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:?…}`)».
  - Правило 8 сверено с `:1215-1435` (`unknown`, `check`, проход по сырым моделям). Без модификатора нарушение только при непустом умолчании манифеста (`op == ""`, `default != ""`). `:+`/`+`, умолчание-подстановка (`ref.inner`), необъявленное или выведенное имя (`unknown`) — нарушения при любом умолчании. Для `OLLAMA_*` отвергаются `-` без двоеточия, литерал и ключ без значения. Совпадает.
- **Mi-2 — закрыт.**
  - Грэп по файлу: `docker compose up -d telegram-bot` без `-f` не найден.
  - Вводный абзац §9, §9.1 п. 1, §9.2, §9.6 п. 2–3 и столбец бота §9.8 совпадают с `Docs/ops/runbook.md` (разделы 2 и 6): два пути, `make up PROFILES=<набор>,bot` или `-f docker-compose.yml -f docker-compose.bot.yml`, одинаковый `PROFILES=` у `up` и `down`.
  - Совпадают и с тем, как `Makefile` собирает `$(COMPOSE)`: `make logs … PROFILES=…,bot` и `make down PROFILES=…,bot` действительно подключают bot-файл, потому что `logs` и `down` идут через `$(COMPOSE)`.
- **N-1 — закрыт.** §2.4: «шапка файла, T-004», причина шапки названа, довод про `$GITHUB_ENV` помечен как добавленный в T-399.
- **N-2 — закрыт.** §2.2: `up`, `down`, `reset`, `logs`, `health`, `backup`, `restore`, `archive-legacy`, `deploy`, `rollback`. По `grep '$(COMPOSE)' Makefile` перечень полон, голого `docker compose` в рецептах нет.
- **N-3 — закрыт.** §0, `prod`: «`memory,bot` — `bot`, когда появится бот, EPIC-004; до того `memory`».
- **Пути — закрыты.** Строчного `docs/ops/runbook.md` в файле нет. Заголовки §9 и §9.8 и строка F-9 §10 пишут `Docs/ops/runbook.md`. Это закрывает и пункт бэклога T-398 про `infrastructure.md:1044`, `:1106`.

**Слияние в `epic/EPIC-001-foundation`.** Ветка стоит на `adadca5`, эпик ушёл вперёд на T-440, T-437 и T-398. Слияние смоделировано `git merge-file` в scratch: база `adadca5`, эпик, рабочая копия с LF.
- `infrastructure.md` сливается без конфликта. Хунки эпика — `:805–840`, `:985`, `:999` по базе, хунки T-399 — `:6–520` и `:1044–1154`. §6 результата побайтно равен §6 эпика, то есть правки T-437 (§6.2, §6.2.1, §6.5) и T-440 (§6.4, `:985`) сохраняются.
- `tasks.md` и карточка T-399 сливаются без конфликта, проверено уже с правками этой приёмки.
- Ожидаемые конфликты — `dev-log.md` и `review.md`: обе стороны дописывают в конец файла. Разрешение механическое, сохранить обе записи: сначала записи эпика (T-437, T-440, T-398), затем T-399. Разрешает оркестратор при слиянии, разработчик не нужен.

**Бэклог** — в разделе T-399 индекса, пп. 1–3 исполнителя и ревью подтверждаю. Добавлен п. 4:
- §1.1 (`:72`, `:77`) по-прежнему даёт `prod` с `COMPOSE_PROFILES=memory,bot` без оговорки N-3 про EPIC-004;
- §8 п. 1 и п. 3 описывают деплой и откат прямым `docker compose up -d --wait`, а `make deploy`/`make rollback` идут через `$(COMPOSE)`. При `bot` в наборе прямая команда не подключит bot-файл. Для §9.4/§9.5 (`memory`, пины образов основного файла) прямые команды верны.

Всё это — при сверке §9 с runbook (п. 1), тем же исполнителем.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 10:31 | 13.09 10:45 | 13m |
| Ревью | 13.09 10:46 | 13.09 10:59 | 13m |
| Разработка 2 | 13.09 10:59 | 13.09 11:04 | 4m |
| Приёмка | 13.09 11:04 | 13.09 11:13 | 8m |
| **Всего** | | | **40m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
