# T-420: Проход по устаревшим именам в документах вне индексов задач

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / TEAM-1 |
| Волна | 1 |
| Размер | S |
| Статус | done (принята оркестратором: грэп и secrets-scan проверены) |

## Описание и DoD
- **Причина (проход tech-lead#1 по T-416)**: вне индексов задач остались действующие тексты со старыми именами — `session-report` (`requirements/{prd,nfr,user-stories}.md`, `testing/strategy.md`, `project/metrics.md`, `project/open-questions.md:713`, `epics/EPIC-003-swarm-llm-laws/design.md:21, :303`, ADR-008, ADR-011, ADR-017, `diagrams/c4-component-foundation.md:83`), `MV_GATEWAY_LISTEN`/`MV_GATEWAY_CLIENTS` (`components/gateway-and-bot.md:363, :370, :891`, `components/foundation.md:259`, `diagrams/c4-component-gateway-and-bot.md:102, :110`, `epics/EPIC-001-foundation/design.md:141`, `epics/EPIC-004-gateway-bot/design.md:82`), `WithEncounterStub` (`epics/EPIC-002-state-mechanics/design.md:15, :89, :105, :175`, `testing/strategy.md:90, :373, :381`), `levels.go` (`epics/EPIC-002-state-mechanics/design.md:90`, `epics/EPIC-003-swarm-llm-laws/design.md:19, :64, :254, :299`); `plan/ownership.md` §1 и ADR-001 доп. п. 8 — «единственный разрешённый импорт `shared/testkit`» (с T-410 их два, удаление хука — T-256); README `shared/testkit/swarm` не называет цену позднего запоминания (приёмка T-220). `plan/backlog.md` не знает T-417…T-420.
- **Правило**: действующий текст — заменить; запись прошлого (ADR как решение своего времени, ревью, журнал, сведения) — не переписывать, достаточно пометки «имя заменено: …, см. T-416». Замены: `session-report` → `mvctl report`; `MV_GATEWAY_LISTEN` → `MV_CORE_ADDR`; `MV_GATEWAY_CLIENTS` → `MV_GATEWAY_CLIENT_IDS` и `MV_GATEWAY_ACTOR_KIND_CLIENTS`; `WithEncounterStub` → `FakeEncounter` (T-219); `levels.go` как истина → `shared/contracts/ownership.go` (ADR-025).
- **`plan/backlog.md`**: добавить T-417…T-420; порядок — T-417 до T-229, T-419 до T-230, T-418 и T-413 после приёмки T-255.
- **DoD**: грэп по `Docs/` на пять имён — вне записей прошлого пусто; список «файл:строка — заменено / помечено» в отчёте.
- **Исполнитель**: tech-writer (Sonnet — по маршрутизации моделей).

## Выполнение (tech-writer)

Выполнено tech-writer#1 (Sonnet), 2026-09-11.

**`session-report` → `mvctl report`.** Заменено в действующем тексте: `testing/strategy.md` (10 мест), `project/metrics.md` (5 мест, `CLI` и `--weekly`/`--json`/`--audit`), `project/open-questions.md:713`, `epics/EPIC-003-swarm-llm-laws/design.md:21`, `diagrams/c4-component-foundation.md:83` (помечено «запись до сверки», закрыто таблицей выше по строке); `requirements/{prd,nfr,user-stories}.md` — только имя подкоманды (7 мест), смысл требований не тронут. ADR-008/ADR-011/ADR-017 — текст решения не переписан, пометка «имя заменено … см. T-416» добавлена в конец раздела, где имя встречается. `epics/EPIC-003-swarm-llm-laws/design.md:303` уже нёс корректную историческую формулировку («прежнее имя подкоманды `session-report`, T-409») — не трогал. `project/metrics.md:367` (`tools/session-report` как отдельный Go-инструмент воркспейса) — не тронул, см. «Оставлено».

**`MV_GATEWAY_LISTEN` → `MV_CORE_ADDR`.** Действующие места (`components/gateway-and-bot.md`, `components/foundation.md`, `diagrams/c4-component-gateway-and-bot.md:102`, `architecture/contracts.md:43`) уже были приведены раньше (T-408/T-409/T-416) и корректно называют переменную несуществующей — правок не потребовалось. Исправлено: `epics/EPIC-001-foundation/design.md:141` (в списке переменных волны 0 дублировал `MV_CORE_ADDR` под именем `MV_GATEWAY_LISTEN` — убрано, оставлен один `MV_CORE_ADDR` с пометкой про compose/T-408). Записи прошлого помечены: `architecture/consolidation.md:50` (G-11), `architecture/contracts.md:105` (changelog v0.2).

**`MV_GATEWAY_CLIENTS` → `MV_GATEWAY_CLIENT_IDS`/`MV_GATEWAY_ACTOR_KIND_CLIENTS`.** Все места из списка задания (`components/gateway-and-bot.md:363,370,891`, `epics/EPIC-004-gateway-bot/design.md:82`) уже были приведены в порядок ранее (T-411/T-416) — правок по существу не потребовалось. Запись прошлого помечена: `diagrams/c4-component-gateway-and-bot.md:110` («Формулировки, записанные при рисовании»).

**`WithEncounterStub` → `FakeEncounter`.** Заменено в действующем тексте: `testing/strategy.md` (3 места, включая пару «заглушка ↔ реализация» и критерий выхода из интеграции — переформулирован на «удаление хука `MV_SWARM_FAKE`», т.к. `WithEncounterStub` не строился вовсе); `epics/EPIC-002-state-mechanics/design.md` (строки 105, 175 — таблица интерфейсов и риск, заменены на `testkit/swarm.FakeEncounter`; строка 15 и 89 уже были верны). `epics/EPIC-001-foundation/design.md` — при проверке обнаружил, что §5 (таблица v0, строка 104), §5.1 (line 109, описание режима `WithEncounterStub(rules)`) и §12 (риск «прижилась», строка 193) остались текстом первоначального плана волны 0, хотя §6 (строка 121) того же документа уже нёс исправленную версию (решение сведения 2 «не делается»). Это расхождение внутри одного документа — исправил все три места на факт (`FakeEncounter`, EPIC-003, T-219), не дожидаясь отдельного решения: правило задания требует замены действующего текста дизайнов эпиков, а не оставления двух версий одного факта в одном файле. Прочие места (`contracts.md:84`, `plan/epics.md:61,154`, `plan/ownership.md:62`, `plan/roadmap.md:97`, `epics/EPIC-003-swarm-llm-laws/design.md:298,326`, `epics/EPIC-003-swarm-llm-laws/tasks.md` — индекс, не трогал) уже корректны или вне области (индексы задач). Записи прошлого помечены: `architecture/consolidation.md` (строка «d» §11, покрывает и §12/§13 того же запроса).

**`levels.go` как источник таблицы владения → `shared/contracts/ownership.go` (ADR-025).** Действующие места (`epics/EPIC-002-state-mechanics/design.md:90`, `epics/EPIC-003-swarm-llm-laws/design.md:19,64,254,299`, `architecture/components/{foundation,state-and-mechanics}.md`, `architecture/diagrams/{domains,c4-component-swarm-llm-laws}.md`, `plan/ownership.md` §1) уже корректны — либо называют `levels.go` файлом реестра уровней (что допустимо: он «реестр уровней агентов», не таблица владения), либо явно говорят «истина — `ownership.go`, ADR-025». Записи прошлого помечены: `architecture/consolidation.md:215` (TL2-3); `architecture/contracts.md:95` (changelog v0.2, экспорт из `levels.go`). ADR-025 сама — решение своего времени, объясняющее отмену прежней процедуры, не переписывал.

**Отдельно.**
1. `plan/ownership.md` §1 — уже исправлен (строка про `cmd/multiverse/fake_contexts.go` явно называет два временных импорта `shared/testkit` и ссылку на T-410/ADR-001 доп. 2026-09-11); правок не потребовалось. ADR-001 доп. п. 8 — решение своего времени, добавлена пометка со ссылкой на дополнение 2026-09-11 п. 2 (которое уже объясняет текущие два импорта) и на T-416.
2. `shared/testkit/swarm/README.md` — раздел «Повтор доставки» дополнен абзацем «Цена позднего запоминания»: дубль нарратива под новым `id` при потерянном подтверждении публикации либо при конкурентной обработке одного события; второе исключено предусловием «`Narrate`/`Observe` не вызываются конкурентно для одного события» (держат `membus`, kafka-адаптер, `eventbus.Delivery`); для настоящего рассказчика (T-229) риск снимает `WithCauseID` (ADR-027, T-417). Формулировка сверена с комментариями `fake_narrator.go` (поле `told`, `Narrate`, тип `window`) — код не менял.
3. `plan/backlog.md` — добавлены T-417 (EPIC-001, S, до T-229), T-418 (EPIC-001, S, после приёмки T-255), T-419 (EPIC-003, M, до T-230), T-420 (EPIC-001, S, 2-й слот M5) в §4 «Порядок реализации»: строка M0+ дополнена `→ T-418` (и упомянут T-413, там же по заданию), строка M5 — `T-417` перед `T-229` и `T-419` перед `T-230`, во 2-м слоте M5 добавлен `T-420` (tech-writer).

**Проверка (DoD).** Финальный грэп по `Docs/` на пять имён — вне записей прошлого (ADR, `journal.md`, `dev-log.md`, `review.md`, `state.js`, `dashboard.html`, `consolidation.md`, `decomposition-review.md`, changelog-секции `contracts.md` версий v0.2–v0.4, tasks.md/tasks/T-*.md любого эпика) и вне `swarm-llm-laws.md` (не трогал по запрету) — пусто. Полный список см. в отчёте оркестратору.

## Ревью (code-reviewer)
Не требуется: документы; проверка — грэп оркестратора.

## Приёмка (оркестратор)
Не принята.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 11.09 14:46 | 11.09 15:01 | 15m |
| **Всего** | | | **15m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
