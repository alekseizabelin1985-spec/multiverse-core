# T-460: `eventbus.Permanent` и `Policy.World` по C-01 v1.10

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / TEAM-1 |
| Волна | 1 (из T-456) |
| Размер | XS–S |
| Исполнитель | developer#2 (Opus) |
| Статус | done |
| Итераций ревью | 1 |
| Начата / завершена | 2026-09-13 / 2026-09-13 |
| Метка | `contract-change`: Go-API `shared/eventbus` расширяется строго по C-01 v1.10 (`contracts.md` v0.14, T-456); нулевые значения — прежнее поведение |
| Ветка / папка | `task/T-460-permanent-policy-world` от `epic/EPIC-001-foundation` (`a0d45d2`), `.worktrees/T-460` |

## Описание и DoD
Раздела T-460 в `../tasks.md` нет — его добавит tech-lead#1 при приёмке. Источник DoD — карточка `T-456.md`, «Строки DoD для владельцев задач», блок «EPIC-001 (tech-lead#1; T-460, `contract-change`, XS–S)» (итерация 2, Ma-2 ревью #1 T-456). Норма — `contracts.md` C-01 v1.10: «Мир в конверте по политике типа», «Окончательная ошибка обработчика»; C-02 v1.7; КД State §9; §16 п. 8.

DoD (перенесено из T-456 без изменений):
- `shared/eventbus`: `ErrPermanent`, `Permanent(err error) error` (`nil` → `nil`; `errors.Is(w, ErrPermanent)` и `errors.Is(w, cause)` истинны). `Delivery.Deliver` при живом контексте: один вызов, парковка сразу, `attempts` = номер вызова, лог `Error handled=true` без `panic`/`stack`. **При отменённом контексте подписки `Permanent` не паркуется**: `Deliver` возвращает `ctx.Err()`, событие не фиксируется; проверка отмены — раньше парковки *(итерация 2, Ma-2)*. README пакета — раздел «Окончательная ошибка»: только дефект события, который повтор не исправит ни в каком процессе; состояние получателя (остановленный мир) — не окончательно.
- Contract-тест `shared/testkit/contract` на `membus` и kafka: окончательная ошибка при живом контексте — один вызов, `dead_letters` с `attempts = 1`, без пауз (ручные `Timers` не двигаются); при отменённом контексте — `ctx.Err()`, dead letter нет, событие выдаётся снова новой подпиской той же группы. Мутанты «`Permanent` как обычная ошибка» и «парковка под отменой» — красные.
- `Policy.World WorldRule` (`WorldOptional` — нулевое значение, `WorldRequired`): `Check` отвергает `World == nil` и пустой `World.Entity.ID` с `ErrPolicyViolation`. Тесты: `Publish` на `membus`; проверка при чтении — dead letter с `attempts = 0`. `All()` реестра до и после совпадает — строк типов задача не ставит.
- Поставка — в develop, оттуда синхронизация EPIC-002.

Связанные строки других эпиков (не в этой задаче): T-056 ставит `WorldRequired` у `entity.create.proposed`/`entity.update.proposed` и отвечает `ErrWorldStopped`, а не `Permanent`; T-055 — `Applier` читает `ev.World`.

## Выполнение (developer)
developer#2 (Opus), TEAM-1, ветка `task/T-460-permanent-policy-world`, папка `.worktrees/T-460`. Не закоммичено: коммит делает оркестратор.

### Итог по пунктам DoD

| # | Пункт | Что сделано | Чем проверено |
|---|---|---|---|
| 1 | `ErrPermanent`, `Permanent` | `delivery.go`: `ErrPermanent`; `Permanent(nil)` — `nil`, иначе `*permanentError` с `Unwrap() []error{ErrPermanent, cause}` и `Error()` = текст причины. Отсюда `errors.Is` по обоим, `errors.As` до причины, текст письма = текст причины (C-01: «ошибка — текст причины») | `TestPermanentKeepsTheCauseAndItsText` (в том числе повторная обёртка `fmt.Errorf("…: %w")`) |
| 2 | `Deliver` при живом контексте | в цикле после проверки `ctx.Err()` — `errors.Is(lastErr, ErrPermanent)` → `parkPermanent`: письмо с `attempts` = номер вызова; после успешной записи — `Error` «event handler failed permanently; the event is parked in dead letters without a retry» с полями события, `attempts`, `handled=true`, `error`, без `panic`/`stack`. Неудачная запись — прежний путь `deadLetter`: ошибка из `Deliver`, строка «not parked», `handled=false` | `TestDeliverParksAPermanentErrorAtOnceWithoutAPause` (ручные таймеры, `DefaultBackoff`), `…WithTheNumberOfItsCall` (попытка 2 → `attempts = 2`), `…LogsAPermanentErrorAsHandledWithoutAStack`, `…ReportsAFailingSinkForAPermanentError` |
| 3 | Отменённый контекст | проверка `ctx.Err()` стоит **раньше** проверки `ErrPermanent` — `Deliver` возвращает `ctx.Err()`, письма нет, лога нет. Паника не тронута: по-прежнему паркуется сразу, в том числе под отменой, с `ErrHandlerPanic` | `TestDeliverDoesNotParkAPermanentErrorUnderCancellation`, `TestDeliverKeepsAPanicApartFromAPermanentError` (паника значением `Permanent(…)` — письмо `ErrHandlerPanic: …`) |
| 4 | Contract-тест на обе шины | `contract.go`: кейсы `APermanentErrorIsParkedAtOnceWithoutAPause` и `APermanentErrorUnderCancellationIsDeliveredAgain`. Первый — на «застывшей» шине (новое поле `Target.Stalled`: та же реализация с `StalledBackoff` на `clock.Manual`, который никто не двигает): один вызов, письмо подписки и письмо журнала (`ReadRange`) с `attempts = 1` и текстом причины, следующее событие обработано; в конце — проверка, что обычная ошибка на этой шине за 300 мс не повторяется (иначе «без пауз» ничего не доказывает). Второй — обработчик ждёт отмены и возвращает `Permanent`: после `stop` письма нет, новая подписка той же группы получает событие снова. `Stalled` добавлен в `membus_test.go` (своя шина, письма из её лога) и `redpanda_integration_test.go` (второй клиент брокера, письма из `dead_letters` брокера) | `TestBusContractOnMembus` — ok; `TestBusContractOnRedpanda` — ok, оба кейса пройдены (1,43 с и 0,59 с) |
| 5 | `Policy.World` | `registry.go`: `WorldRule` (`WorldOptional` = 0, `WorldRequired`), поле `Policy.World`, ветка в `Check`: `World == nil` → «world is required for <type>», пустой `World.Entity.ID` → «world.entity.id is empty for <type>», обе с `ErrPolicyViolation`. `Route` и `Delivery.validate` уже вызывают `Check` — отдельной проверки нет, deprecated-типы по-прежнему политику не проверяют | `policy_world_test.go` (`eventbus`): `Check` по трём случаям и нулевому значению, `Route`, `Deliver` (письмо `attempts = 0`, обработчик не вызван); `membus/policy_world_test.go`: `Publish` без мира и с пустым id → `ErrPolicyViolation`, `End` = 1; чтение — письмо `attempts = 0`; `Lenient` пропускает к обработчику |
| 6 | `All()` реестра | строки реестра не менялись. Сверено: тест-дамп `All()` (тип, топик, версия, `ActorKinds`, `Agent`, `Deprecated`, владелец, издатели, потребители, `Reserved`, `Since`; в ветке — и `World`, если он не нулевой) в копии `a0d45d2` и в копии ветки — 65 строк, файлы совпали побайтно; ненулевого `World` нет | `cmp` дампов в scratch |
| 7 | README | `shared/eventbus/README.md`: пункт «Мир в конверте по политике типа», раздел «Окончательная ошибка» (что окончательно, живой контекст, отменённый контекст, паника, якоря contract-набора), строка про окончательную ошибку в «Исходы `Delivery` в логе». CRLF сохранён | чтение; `file` — CRLF |

### Мутанты
Копии рабочей папки в scratch (`t460-<имя>`, `git ls-files -co --exclude-standard` → `tar`), без `go test -overlay`; мутация — скриптом точной заменой (`assert count == 1`). Прогон каждой копии: `go test -short -count=1 ./shared/eventbus/... ./shared/testkit/contract/`. Копии удалены по точным путям. Kafka-вариант под мутантами не гонялся — разрешён один интеграционный прогон.

| # | Мутант | Результат |
|---|---|---|
| M0 | контрольный: копия без изменений | **зелёный** (3 пакета ok) |
| M1 | «`Permanent` как обычная ошибка»: ветка `ErrPermanent` удалена | **красный**: `…AtOnceWithoutAPause` (Deliver не вернулся за 5 с), `…WithTheNumberOfItsCall` (4 вызова), `…LogsAPermanentError…` (WARN, нет `handled`); contract `APermanentErrorIsParkedAtOnceWithoutAPause` на membus (15 с) |
| M2 | «парковка под отменой»: проверка `ErrPermanent` перед `ctx.Err()` | **красный**: `TestDeliverDoesNotParkAPermanentErrorUnderCancellation`; contract `APermanentErrorUnderCancellationIsDeliveredAgain` на membus («was parked (attempts 1 …)») |
| M3 | пауза `backoff[0]` перед парковкой | **красный**: `…AtOnceWithoutAPause`; contract «waited for a retry» |
| M4 | ветка `WorldRequired` не срабатывает | **красный**: 3 теста `eventbus`, `TestPublishRefuses…` и `TestReadParks…` на membus |
| M5 | пустой `World.Entity.ID` проходит | **красный**: `Check` с пустым id, `Deliver` (письма нет), `Publish` с пустым id (ошибка схемы, не `ErrPolicyViolation`) |
| M6 | `StalledBackoff` без пауз (сторож кейса) | **красный**: «an ordinary error on the stalled bus was called 4 times within 300ms» |

### Изменённые файлы
- `shared/eventbus/delivery.go`: `ErrPermanent`, `Permanent`, `permanentError`, ветка в `Deliver`, `parkPermanent`, doc `Deliver`.
- `shared/eventbus/registry.go`: `WorldRule`, `WorldOptional`, `WorldRequired`, `Policy.World`, ветка в `Check`; doc `Policy` — «policy of a type» вместо «topic policy of a type».
- `shared/eventbus/README.md`: см. п. 7.
- `shared/eventbus/permanent_test.go`, `shared/eventbus/policy_world_test.go`, `shared/eventbus/membus/policy_world_test.go` — новые.
- `shared/testkit/contract/contract.go`: `Target.Stalled`, `StalledBackoff`, `StalledBus`, два кейса, `checkItStalls`, `findLetter`, `checkPermanentLetter`.
- `shared/testkit/contract/membus_test.go`, `redpanda_integration_test.go`: `Stalled` у целей.
- `Docs/dev-team/epics/EPIC-001-foundation/tasks/T-460.md` (эта карточка), `dev-log.md` (запись в конце).

### Прогоны (go1.26.8 windows/amd64, golangci-lint 2.13.2)
- `go build ./... && go vet ./...` — 0; `go vet -tags integration ./shared/...` — 0; `gofmt -l shared/` — пусто.
- `go test -short -count=1 ./...` — 27 пакетов ok, 0 FAIL.
- `golangci-lint run ./...` — 0 issues; `golangci-lint run --build-tags integration ./shared/eventbus/... ./shared/testkit/contract/...` — 0 issues.
- `go run ./cmd/mvctl contracts check` — «65 types, 8 topics, 58 schema files checked».
- `make test` — 0 (без `-race`: нет cgo; порог покрытия `internal/mechanics` 94,9 %).
- Интеграция, один прогон: `go test -tags integration -count=1 -run TestBusContractOnRedpanda -v ./shared/testkit/contract/` — ok, 26 кейсов за 19,98 с. До прогона `docker ps -a --filter label=org.testcontainers=true` пуст, после — пуст. `make test-integration` не запускался.

### Отклонения от дизайна
- Нет по существу. Решения, которых нет в тексте C-01 (подробно — «Открытые вопросы» отчёта): текст `ErrPermanent` («eventbus: permanent handler error»); текст строки лога; `Permanent` пишет текст причины без префикса — так письмо несёт «текст причины» без отдельной распаковки в `Deliver`.
- Сверх DoD: contract-кейс проверяет и журнал (`ReadRange`), как кейс паники, — C-01 говорит «обе реализации читают через `Delivery`».
- `Target` contract-набора получил обязательное поле `Stalled`: без него «без пауз» на kafka не проверить (цели собраны с нулевыми паузами ради остальных кейсов).

### Предложения в бэклог
1. `Policy.Check` на уровне contract-набора (обе шины) для `WorldRequired` не проверяется: в реестре нет типа с правилом, а цели набора собраны с `contracts.Default()`. После T-056 (первые строки `WorldRequired`) можно добавить кейс «предложение без мира» в `contract.go` — на брокере проверка при чтении идёт тем же `Delivery`, так что выигрыш мал; решение за тимлидом.
2. Строка `logPanic` «event handler panicked; the event goes to dead letters» и новая строка окончательной ошибки сформулированы по-разному («goes to» / «is parked»); выровнять вместе с бэклогом T-441, если тимлид сочтёт нужным.

## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Critical / Major / Minor / Nit | Где |
|---|---|---|---|---|---|
| 1 | 2026-09-13 | code-reviewer#2 (TEAM-1) | принять | 0 / 0 / 2 / 1 | `review.md`, «T-460 · ревью #1»: Mi-1 фикстуры правила (г) `mvctl contracts check` без `World` — ложный красный при `WorldRequired` на правленом топике; Mi-2 тест паники со значением `Permanent` не проверяет лог, мутант R6 (живой контекст) выжил; N-1 `StalledBackoff` — изменяемая экспортированная переменная |

## Приёмка (tech-lead)

tech-lead#1 (TEAM-1), 2026-09-13, модель Opus. **Решение: принята.**

**Итог.** В `shared/eventbus` есть окончательная ошибка обработчика `Permanent` (без повторов при живом контексте, без парковки под отменой) и правило мира `Policy.World`, обе шины проверены одним contract-набором; три замечания ревью закрыты при приёмке тестами.

Основание: ревью #1 code-reviewer#2 (TEAM-1) — «принять», Critical 0, Major 0, Minor 2, Nit 1 (`review.md`, «T-460 · ревью #1»). Итераций ревью — 1.

**DoD** (карточка T-456, блок EPIC-001; раздел T-460 в `../tasks.md`). Сверял по `git diff a0d45d2` и неотслеживаемым файлам рабочей папки `.worktrees/T-460`; коммитов в ветке нет.

| # | Пункт | Чем подтверждён при приёмке |
|---|---|---|
| 1 | `ErrPermanent`, `Permanent` | `delivery.go:28`–`:56`: `Permanent(nil)` — `nil`, `Unwrap() []error{ErrPermanent, cause}`, текст — текст причины. `TestPermanentKeepsTheCauseAndItsText` зелёный; мутанты R1, R2 ревьюера — красные |
| 2 | `Deliver` при живом контексте | `delivery.go:147`–`:191`: паника → `ctx.Err()` → `ErrPermanent` → `parkPermanent`; строка `Error` с `handled=true` — после записи письма, без `panic`/`stack`. Тесты `…AtOnceWithoutAPause`, `…WithTheNumberOfItsCall`, `…LogsAPermanentErrorAsHandledWithoutAStack`, `…ReportsAFailingSinkForAPermanentError` зелёные |
| 3 | Отменённый контекст; README | проверка `ctx.Err()` стоит раньше `errors.Is(lastErr, ErrPermanent)`; `TestDeliverDoesNotParkAPermanentErrorUnderCancellation` зелёный, M2 исполнителя — красный. README: «Мир в конверте по политике типа» (`:184`), «Окончательная ошибка» (`:250`), остановленный мир — не окончательно (`:256`) |
| 4 | Contract-тест на обе шины | кейсы `APermanentErrorIsParkedAtOnceWithoutAPause` и `APermanentErrorUnderCancellationIsDeliveredAgain` на membus зелёные в моих прогонах. Kafka: один прогон исполнителя на Redpanda (26 кейсов, ok); интеграцию при приёмке не запускал, код шины не менялся |
| 5 | `Policy.World` | `registry.go`: `WorldRule`, `WorldOptional` = 0, `WorldRequired`; `Check` → `ErrPolicyViolation` для `World == nil` и пустого id. `policy_world_test.go` (`eventbus`, `membus`) зелёные; M4, M5, R3 — красные |
| 6 | `All()` реестра | в `shared/contracts` дифф пуст; `mvctl contracts check` — «65 types, 8 topics, 58 schema files checked», как до задачи. Побайтная сверка дампов — у исполнителя |
| 7 | Общий DoD | прогоны ниже; запись `<!-- dev-log T-460 -->` в `dev-log.md` есть и совпадает с диффом; «Выполнение» и «Ревью» заполнены |

**Правки при приёмке** (tech-lead#1, в рабочей папке, не закоммичено):
- **Mi-2** — `shared/eventbus/permanent_test.go`: `TestDeliverKeepsAPanicApartFromAPermanentError` стал табличным (живой и отменённый контекст) и проверяет лог. Запись ровно одна, в ней есть `panic` и `stack`, `handled=false`, строки «failed permanently» нет. `delivery.go` **не менялся**, поэтому повторный интеграционный прогон из-за этой правки не нужен.
- **Mi-1** — `cmd/mvctl/internal/contracts/check.go`: у всех восьми фикстур `policyFixtures` теперь есть мир (`world-1`), комментарий объясняет причину. В `check_test.go` (`TestPolicyFixtures`) два новых случая. Первый: `WorldRequired` у типов на `player_events` и `llm_records` при политике топика не даёт находок. Второй: `WorldRequired` вместе с пустой политикой по-прежнему даёт находку. Пакет — зона EPIC-001 (`ownership.md` v0.7, §1, строка `cmd/mvctl/internal/{cli,contracts,env,storage,privacy}/**`).
- **N-1** — `shared/testkit/contract/contract.go`: `StalledBackoff()` стал функцией и на каждый вызов возвращает новый срез. Вызовы обновлены в `membus_test.go` и `redpanda_integration_test.go`. Интеграционный файл изменился на одну строку: он собран `go vet -tags integration` и проверен линтером с `--build-tags integration`, но не запускался.
- **Тексты** `ErrPermanent` («eventbus: permanent handler error») и строки лога окончательной ошибки оставлены как есть: тексты — на подтверждении system-architect (T-458, подраздел «Попутно: T-460»).
- `../tasks.md`: раздел T-460 пришёл из T-461 (кончик `3f34195`), в базе `a0d45d2` его нет. Файл рабочей папки взят с кончика `3f34195` (`git cat-file --filters`, CRLF), в разделе T-460 изменены статус (`done`) и добавлены строка приёмки и бэклог. Остальной файл побайтно совпадает с кончиком.

**Мутанты приёмки.** Копия рабочей папки (`git ls-files -co --exclude-standard` → `tar`) в scratch `t460acc-mut`, без `-overlay`. Замена — `perl` с проверкой числа вхождений, после каждого мутанта файл возвращался из рабочей папки. Прогон — `go test -short -count=1` по пакетам мутанта. Копия и скрипт удалены по точным путям.

| # | Мутант | Результат |
|---|---|---|
| M0 | контрольный, без изменений (первым; повторён после мутантов) | **зелёный**: `eventbus`, `membus`, `contract`, `mvctl/internal/contracts` |
| R6 | ревью: `if panicked && (ctx.Err() != nil \|\| !errors.Is(err, ErrPermanent))` | **красный**: `…KeepsAPanicApartFromAPermanentError/live_context` — две строки лога («panicked» и «failed permanently») |
| A1 | паника со значением `Permanent` уходит в ветку окончательной ошибки при любом контексте (`if panicked && !errors.Is(err, ErrPermanent)`) | **красный**: `live_context` (две строки лога), `cancelled_context` (`deliver = context canceled`) |
| A2 | строка паники с `handled=true` | **красный**: оба случая нового теста; также `TestDeliverLogsAPanicAsAnErrorWithItsStack` |
| A3 | строка паники без `stack` | **красный**: оба случая нового теста; также `TestDeliverLogsAPanicAsAnErrorWithItsStack` |
| A4 | фикстуры правила (г) без мира (`World` = `nil`) | **красный**: `TestPolicyFixtures/a_type_that_requires_a_world_keeps_the_policy_of_its_topic` (ложные «policy refuses …») и второй новый случай |
| A5 | мир фикстур с пустым `Entity.ID` | **красный**: оба новых случая `TestPolicyFixtures` |
| A6 | правило (г) пропускает типы с `WorldRequired` (`if !ruled \|\| spec.Policy.World == eventbus.WorldRequired`) | **красный**: `…a_type_that_requires_a_world_and_drops_the_policy_of_its_topic` |

**Прогоны** (рабочая папка после правок; go1.26.8 windows/amd64, golangci-lint 2.13.2):
- `go build ./... && go vet ./...` — 0; `go vet -tags integration ./shared/...` — 0; `gofmt -l shared/ cmd/` — пусто.
- `go test -short -count=1 ./shared/... ./cmd/mvctl/...` — все пакеты ok.
- `golangci-lint run ./...` — 0 issues; `golangci-lint run --build-tags integration ./shared/eventbus/... ./shared/testkit/contract/...` — 0 issues.
- `go run ./cmd/mvctl contracts check` — «65 types, 8 topics, 58 schema files checked».
- `make test` — 0, 27 пакетов ok, FAIL нет. Прогон без `-race` (нет cgo), порог покрытия `internal/mechanics` — 94,9 %.
- Интеграция не запускалась (по заданию).

**Слияние с кончиком эпика.** `git archive 3f34195` в scratch `t460acc-merge`. Файлы ветки положены поверх, для файлов, изменённых на обеих сторонах, — `git merge-file` (база `a0d45d2`). Копия удалена по точному пути.
- Код и `.golangci.yml` не пересекаются: файлы кода ветки на кончике не менялись, `.golangci.yml` взят с кончика.
- `tasks.md` — 2 конфликтных фрагмента, оба внутри раздела T-460: заголовок со статусом и строки приёмки и бэклога. Разрешение — целиком сторона ветки (`--ours`). Рабочая версия — это кончик плюс эти правки, остальные изменения T-461 совпадают и сливаются чисто. Если до слияния кончик ещё раз поменяет раздел T-460, сторону ветки брать нельзя: нужно перенести строки вручную.
- `dev-log.md` и `review.md` — по одному конфликту: дописывание в конец (T-460 и T-461). Их разрешает драйвер `appendtail`, он настроен в `.git/config` основной папки.
- На слитом дереве: `go build ./... && go vet ./...` и `go vet -tags integration ./shared/...` — 0; `go test -short -count=1 ./shared/eventbus/... ./shared/testkit/contract/ ./cmd/mvctl/...` — ok; `golangci-lint run ./...` с `.golangci.yml` T-461 — 0 issues; `mvctl contracts check` — ok.

**Бэклог (из T-460, 2026-09-13)** — не блокирует:
1. tech-lead#1 (п. 2 ревью, п. 1 исполнителя): contract-кейс «предложение без мира» в `shared/testkit/contract` после T-056 (первые строки `WorldRequired`). Выигрыш мал: обе шины проверяют при чтении через тот же `Delivery`, а R3 уже ловят unit-тесты `eventbus` и `membus`.
2. system-architect (п. 3 ревью): C-01 не говорит, что делать с голым `ErrPermanent` без причины. Сейчас такое событие паркуется с текстом «eventbus: permanent handler error». Нужно явно разрешить или запретить.
3. tech-lead#1 (п. 2 исполнителя): строка паники говорит «goes to dead letters», строка окончательной ошибки — «is parked». Выровнять вместе с бэклогом T-441, после подтверждения текстов system-architect (T-458).

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 19:18 | 13.09 19:35 | 17m |
| Ревью #1 | 13.09 19:35 | 13.09 19:47 | 12m |
| Приёмка | 13.09 19:47 | 13.09 19:57 | 10m |
| Слияние | 13.09 19:57 | 13.09 19:57 | 0m |
| **Всего** | | | **39m** |
