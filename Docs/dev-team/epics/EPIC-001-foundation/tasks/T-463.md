# T-463: Devops — срок архивов `make backup`, маскировка значения в отказах LLM-скриптов, `MV_STATE_WORLDS` и `MV_TELEGRAM_*` в compose

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / TEAM-1 |
| Волна | 1 (бэклог) |
| Размер | M (заведена как S; после дополнения R2-Mi-1 к части (а) — M) |
| Исполнитель | devops-engineer (экземпляр назначает оркестратор) |
| Ревьюер | code-reviewer (назначает оркестратор) |
| Статус | done |
| Итераций ревью | 2 |
| Создана / начата / завершена | 2026-09-13 / 2026-09-13 / 2026-09-13 |
| Ветка / папка | `task/T-463-backup-retention-endpoint-mask` от `epic/EPIC-001-foundation` (`3f34195`), `.worktrees/T-463`; часть (в) — T-464 |
| Зависит от | (а), (б) — нет; (в) — T-055 (EPIC-002) и T-310 (EPIC-004) в ветке задачи, см. «Зависимости» |
| Связи | T-318: M-1 ревью безопасности #1, R2-Mi-1 ревью безопасности #2, решение оркестратора «архивы бэкапа — не дольше 30 дней» (`journal.md`, 2026-09-13); `contracts.md` v0.14, C-15 v1.5 «Печать значения в отказах», ADR-005; T-450; T-456 (приёмка, Mi-1); T-055 «Отметка владельца», О-1; T-310 «Отметка владельца», замечание 1; `infrastructure.md` §5.6, §6.3.1; SEC-22 |

## Описание
Три независимые части. Все три — эксплуатационные хвосты ревью и отметок владельца, Go-код платформы и контракты не меняются.

### (а) Срок хранения архивов `make backup`
**Сейчас.** Цель `backup` (`Makefile`) кладёт в `$(BACKUP_DIR)` архивы `minio-<штамп>.tgz` и `redpanda-<штамп>.tgz` и дописывает их суммы в `SHA256SUMS`. Старые архивы не удаляются, поэтому копия данных игроков живёт дольше 30 дней (ревью безопасности T-318, M-1). Решение оркестратора: архивы бэкапа — не дольше 30 дней. Текст уведомления FR-009 (T-318) обещает «ещё до 30 дней в копиях».

Копии `links.db` и `gateway.db` (`infrastructure.md` §5.6: `links-<date>.db.age` и `gateway-<date>.db` в `%USERPROFILE%\multiverse-backups\links\`) по §5.6 тоже хранятся не дольше 30 дней, но скрипта очистки ещё нет. Сама команда `multiverse db backup` появится с `internal/gateway` (EPIC-004). Ревью безопасности #2 T-318 (R2-Mi-1) уточнило: удаление только внутри `make backup` не годится — если бэкапы перестанут делать, старые копии останутся навсегда.

**Что сделать.**
- **Удаление по возрасту, а не по числу.** Удаляется всё, что строго старше 30 суток на момент запуска. Правил «оставить последние N» или «не трогать последний архив» нет: каталог, где остались только старые копии, очищается полностью.
- **Отдельный ежедневный запуск.** Удаление — отдельная точка входа: скрипт, например `scripts/backup-prune.sh` (имя выбирает исполнитель), и цель `make backup-prune`. На стенде его запускает ежедневное задание Планировщика задач Windows. `make backup` может вызывать ту же очистку в конце успешного бэкапа, но срок хранения от этого вызова не зависит.
- **Что покрывается** (всё — внутри `$(BACKUP_DIR)`):
  - архивы томов `minio-<штамп>.tgz` и `redpanda-<штамп>.tgz` и их строки в `SHA256SUMS` (остальные строки не меняются);
  - копии `links.db` и `gateway.db` на хосте по §5.6: `links/links-<штамп>.db.age` и `links/gateway-<штамп>.db`.

  Файлы с другими именами, прочие подкаталоги и `backups/legacy-*` (`make archive-legacy`) не трогаются.
- **Возраст.** Возраст считается по штампу в имени, формат `%Y%m%d-%H%M%S`, как у `make backup`: копирование меняет mtime, а штамп — нет. Формат имён копий SQLite записать в runbook, чтобы бэкап `links.db`/`gateway.db` в EPIC-004 писал имена в нём же. Если у файла покрываемого префикса штамп не разбирается, возраст берётся по mtime — иначе такой файл жил бы вечно. «Сейчас» задаётся параметром только для теста (например `--now`); в обычном запуске это время системы.
- **Удаление по точным путям.** Каждый файл удаляется по своему полному пути внутри каталога бэкапов, найденному перечислением каталога: без `rm` по шаблону, без рекурсивного удаления, без перехода по ссылкам за пределы каталога. Удалённые файлы перечисляются в выводе.
- **30 дней** — константа в скрипте с комментарием-ссылкой на решение и SEC-22. Переменная окружения не вводится: новая `MV_*` потребовала бы манифеста.
- **Реализация** — одна, на bash (на стенде задание вызывает её через Git Bash). Если исполнитель выбирает две половины, sh и ps1, их паритет проверяется в стенде `testdata/script-parity`.
- **Runbook.** `Docs/ops/runbook.md`, раздел бэкапа: срок 30 дней; что удаляется; точная команда регистрации ежедневного задания (`schtasks /Create … /SC DAILY`) и проверки (`schtasks /Query`); как понять, что задание не выполнялось. Регистрирует задание на стенде владелец по runbook: исполнитель в Планировщике стенда ничего не создаёт.
- **Копии в томе.** Открытые копии в томе `gateway-data` (`/data/backup/*`) удаляет сама процедура бэкапа §5.6 сразу после `docker cp`. Эта задача их не чистит; строка для runbook T-317 (EPIC-004) — в «Выполнении».

### (б) Маскировка значения в `llm_endpoint_judge` и `LlmEndpoint.psm1`
**Сейчас.** `llm_endpoint_judge` (`scripts/lib/llm-endpoint.sh`, около `:647`) строит фразы отказа из `$LLM_EP_RAW` и `$LLM_EP_HOST`, те же тексты — в `LlmEndpoint.psm1` (около `:601`). Разбор отвергает userinfo в authority (T-450, сценарии H61–H66), но `@` после `/` до этой проверки не доходит. Значение `http://fakepw/x@127.0.0.1:8888` разбирается как хост `fakepw` (однословное имя, `local`) без порта, и отказ «has no port» печатает значение целиком, вместе с `fakepw/x` и `127.0.0.1`. C-15 v1.5: «Если `@` есть где угодно в значении, ни одна фраза отказа не называет хост»; данные пользователя не печатаются нигде. В C-15 и ADR-005 стоит пометка «Код расходится».

**Что сделать.**
- Если в значении есть `@` (в любом месте), все фразы `llm_endpoint_judge` печатают значение только схемой (`MV_LLM_URL=http://…`, как в H65) и не называют хост. Это ветки any-address, прочего `invalid`, «has no port» для loopback/localhost/docker-host и «has no port» для прочих `local`. Без `@` фразы не меняются.
- `LlmEndpoint.psm1` — то же; тексты обеих половин совпадают побайтно (стенд паритета).
- Маскировка доходит и до `llm_endpoint_classify` (`LLM_CLASS_ERROR`), и до правила 6 `compose-lint`: оба берут фразу из `llm_endpoint_judge`.
- Комментарий `LLM_EP_RAW` (`llm-endpoint.sh:152`, «with any userinfo masked») привести к факту (бэклог приёмки T-450, п. 3).
- Классы и `kind` таблицы `testdata/llm/local-endpoints.tsv` не меняются.
- Стенд LLM владельца (127.0.0.1:8888) не используется: в сценариях стенда паритета — порт двойника `{PORT}`.

### (в) Переменные состояния и бота в compose
**Сейчас.** Сервис `core` в `docker-compose.yml` не передаёт `MV_STATE_WORLDS`, хотя передаёт соседнюю `MV_SNAPSHOT_EVERY_FACTS` (отметка T-055, О-1). Сервис `telegram-bot` в `docker-compose.bot.yml` не передаёт `MV_TELEGRAM_ACTION_KEY_SALT` и `MV_TELEGRAM_COMMANDS_PER_MIN` (отметка T-310, замечание 1). Значения из `.env` в контейнеры не попадают, и процессы молча берут умолчания манифеста.

**Что сделать.**
- `docker-compose.yml`, `core`: `MV_STATE_WORLDS` рядом с `MV_SNAPSHOT_EVERY_FACTS`.
- `docker-compose.bot.yml`, `telegram-bot`: `MV_TELEGRAM_ACTION_KEY_SALT` и `MV_TELEGRAM_COMMANDS_PER_MIN` рядом с остальными `MV_TELEGRAM_*`.
- Форма — та, что принимают правила 3 и 8 `compose-lint`, умолчание — как в манифесте ветки (`shared/env/vars.go`): `${MV_STATE_WORLDS:-dark-forest-world}` и `${MV_TELEGRAM_COMMANDS_PER_MIN:-20}`. Соль — секрет с пустым умолчанием и без `:?` (переменная необязательна): `${MV_TELEGRAM_ACTION_KEY_SALT:-}` или ключ без значения — что принимает правило 3. Выбор записать с доводом в «Выполнении».

### Не входит
- Срок хранения Qdrant, Neo4j и ChromaDB (T-318 M-2 — отдельные решения), архивы `make archive-legacy`.
- Сама команда онлайн-бэкапа SQLite (`multiverse db backup`, EPIC-004) и шифрование `age`. Здесь — только срок хранения готовых копий на хосте.
- Регистрация задания в Планировщике задач на стенде (делает владелец по runbook).
- Go-реализация правила «локальный адрес» (T-451, EPIC-003).
- Правка `contracts.md` и ADR-005 — только system-architect.
- `.env.example` и `shared/env/**`: объявления переменных сделаны в T-055 и T-310.

### Зависимости
- (а) и (б) можно начинать сразу.
- (в) требует объявлений в манифесте ветки задачи. `MV_STATE_WORLDS` сейчас есть только в `epic/EPIC-002-state-mechanics` (`shared/env/vars.go:139`, T-055), `MV_TELEGRAM_ACTION_KEY_SALT` и `MV_TELEGRAM_COMMANDS_PER_MIN` — только в `epic/EPIC-004-gateway-bot` (`vars.go:271`, `:274`, T-310). Без них правило 8 `compose-lint` не найдёт умолчание манифеста. Поэтому (в) — после слияния EPIC-002 и EPIC-004 с этими задачами в develop и синхронизации `epic/EPIC-001-foundation`, но не позже T-390 (стенд бота). Если (а) и (б) готовы раньше, оркестратор выносит (в) в отдельную ветку.
- С T-460 и T-461 пересечение по файлам только в `dev-log.md`.

## DoD
**(а)**
1. Удаление — строго по возрасту: всё старше 30 суток удаляется. «Оставить последние N» и «не трогать последний архив» не реализованы; каталог, где остались только старые копии, очищается полностью. Удаление не привязано к `make backup`: есть отдельная точка входа (`make backup-prune` или скрипт).
2. Ежедневный запуск — задание Планировщика задач Windows на стенде. В `Docs/ops/runbook.md` — команда регистрации (`schtasks /Create … /SC DAILY`), проверка (`schtasks /Query`) и признак того, что задание не выполнялось. Регистрирует задание владелец; исполнитель проверяет команду разбором (`schtasks /Create /?`) или на копии без регистрации на стенде.
3. Тест удаления по возрасту без Docker, на временном каталоге, с поддельным «сейчас» (`--now`) или mtime файлов:
   - `minio-`, `redpanda-`, `links/links-…db.age`, `links/gateway-…db` возрастом 31 день удалены, строки архивов убраны из `SHA256SUMS`;
   - те же файлы возрастом 29 дней — на месте;
   - файл покрываемого префикса без разбираемого штампа удаляется по mtime (31 день) и остаётся при mtime 29 дней;
   - каталог только со старыми архивами очищен полностью (мутант «оставить последний» — красный);
   - чужие файлы (`notes.txt`, `postgres-<штамп>.tgz`, прочий подкаталог, `legacy-*`) — на месте;
   - файл вне временного каталога, на который указывает ссылка изнутри, — на месте;
   - удаление идёт только по точным путям внутри временного каталога.

   Контрольный мутант первым; мутанты «порог 0 дней» и «оставить последний» — красные.
4. Тест входит в `make ci` и выполняется в одном из заданий `.github/workflows/go.yml` на Linux; `bash -n` — ok.
5. `infrastructure.md` §5.6 согласован с решением оркестратора «архивы бэкапа — не дольше 30 дней». Сейчас там у архивов томов срок не назван, периодичность «еженедельно (Task Scheduler → `pwsh scripts/backup.ps1`)», у `links.db` — «скрипт чистит старше». *Пометка:* текст раздела принадлежит system-architect и devops (`ownership.md`). Исполнитель правит только эксплуатационную часть (срок, ежедневная очистка, имя скрипта), а расхождения по существу передаёт строкой system-architect в «Выполнении».
6. Копии `links.db` и `gateway.db` на хосте — не старше 30 дней: они покрыты тем же удалением (п. 3). Формат имён записан в runbook. Строка для T-317 (EPIC-004, runbook бота и gateway): бэкап SQLite пишет имена в этом формате, открытая копия в томе удаляется сразу после `docker cp`.

**(б)**

7. Сценарии `scriptServer` стенда паритета `testdata/script-parity` по образцу H61–H66 — по одному на каждую ветку фразы `llm_endpoint_judge`, в том числе `MV_LLM_URL=http://fakepw/x@127.0.0.1:{PORT}`. В `Absent` — `fakepw`, `127.0.0.1` и хост ветки. Обе половины отвечают одинаково. Сценарий без `@` показывает, что значение печатается как прежде.
8. Мутант «маскировка снята» краснеет и в половине sh, и в половине pwsh; контрольный мутант первым; `make parity-mutants` — зелёный.
9. Фикстура `compose-lint` со значением вида `http://fakepw/x@…` отвергается правилом 6, текст отказа не содержит ни хоста, ни `fakepw`.
10. `make scripts-parity` (включая `T01`/`T02` по таблице — классы не изменились), `bash -n`, разбор `.ps1` — зелёные.
11. В «Выполнении» — строка для system-architect: после слияния T-463 снять пометку «Код расходится» в C-15 v1.5 и в ADR-005. `contracts.md` и ADR исполнитель не правит. *Изменено при приёмке* (решение оркестратора, вариант (б) по Ma-2 ревью #2): пометку не снимать, а сузить до `llm-bench` и строк успеха и `health` `llm-server`; остальное — T-468.

**(в)**

12. Строки добавлены в `docker-compose.yml` (`core`) и `docker-compose.bot.yml` (`telegram-bot`); умолчания равны манифесту ветки.
13. `make compose-lint` — ok (число сервисов и правил — в отчёте), `--fixtures` — ok. В модели `docker compose … config` с env-файлом пробы (не `.env` владельца) три значения доходят до `environment` своих сервисов; без строки в env-файле пробы действует умолчание манифеста.

**Общее**

14. `make ci BASE=epic/EPIC-001-foundation` — зелёный (`test-race` на Windows — SKIPPED); `make secrets-scan BASE=epic/EPIC-001-foundation` — rc=0; `gitleaks dir --redact` по копии изменённых файлов — no leaks.
15. Запись в `dev-log.md`, секция «Выполнение» заполнена; `.env` не открывался.

## Выполнение (developer)
devops-engineer#1 (TEAM-1, Opus), 2026-09-13. Части (а) и (б). Часть (в) по решению оркестратора вынесена в T-464 и здесь не делалась: `docker-compose*.yml`, `.env.example` и `shared/env/**` не менялись. Не закоммичено, `git add` не делал: коммит делает оркестратор. `.env` не открывался. Docker не запускался (`docker compose config` внутри `compose-lint` — только разбор файлов), `make backup` целиком не выполнялся, реальный каталог бэкапов и Планировщик задач не трогались.

### (а) Срок хранения архивов

**Что сделано.**
- `scripts/backup-prune.sh` (новый, bash, одна реализация — половины `.ps1` нет, стенд паритета не нужен). `--dir <каталог>` обязателен; `--dry-run`; `--log <файл>` дописывает вывод и для задания Планировщика; `--now <YYYYMMDD-HHMMSS | @epoch>` — только для теста. Коды выхода: `0` — успех, в том числе «нечего удалять»; `1` — файл не удалился или `SHA256SUMS` не переписался; `2` — ошибка вызова или каталога нет.
  - Покрываются `<dir>/minio-<x>.tgz`, `<dir>/redpanda-<x>.tgz`, `<dir>/links/links-<x>.db.age`, `<dir>/links/gateway-<x>.db` с непустой серединой `<x>`. Если `<x>` — штамп `%Y%m%d-%H%M%S`, возраст берётся по нему (местное время, штамп проходит проверку туда-обратно через `date`, поэтому месяц 13 и 30 февраля — не штамп). Иначе возраст — по mtime.
  - Удаляется строго старше `RETENTION_DAYS=30` суток (константа с комментарием: решение 2026-09-13, T-318 M-1/R2-Mi-1, SEC-22). Ровно 30 суток, моложе и штамп из будущего — остаются. «Последних N» нет.
  - Каталог перечисляется (`"$root"/*`, `"$root/links"/*`). Кандидаты копятся в массив, затем каждый удаляется `rm -f -- <полный путь>` с проверкой, что файла больше нет. Каталоги и символьные ссылки с подходящим именем пропускаются с сообщением. `links`, если это ссылка, не обходится. Другие подкаталоги не читаются. Путь с `\` (из Планировщика) приводится к `/`, корень — `cd -P; pwd -P`.
  - `SHA256SUMS`: удаляются только строки удалённых архивов (формы `<hash>  name`, `<hash> *name`, `./name`). Остальные строки сохраняются как были и в том же порядке. Запись идёт через временный `SHA256SUMS.prune-<pid>` в том же каталоге и `mv -f`.
  - Каждый удалённый файл печатается с возрастом и источником (`by its stamp` / `by its mtime`), в конце — `done: N removed, M kept`.
- `scripts/backup-prune-test.sh` (новый): тест без Docker во временном каталоге `mktemp -d`. Каталог удаляется в конце по точному пути, который вернул `mktemp`. `TZ=UTC`, `--now 20260913-120000`, mtime подделываются `touch -d @…`. Каждый `rm` скрипта идёт через подмену `rm` на `PATH`, которая пишет аргументы. Тест требует ровно `rm -f -- <путь>`: путь внутри каталога, без символов шаблона, и набор путей совпадает с ожидаемым. Случаи:
  - `mixed` — возраст по штампу (31 сут., 30 сут. + 1 с — удалены; ровно 30, 29, штамп из будущего — на месте; mtime нарочно противоречит штампу); по mtime (`minio-copy.tgz` и `links-old.db.age` 31 сут. и `redpanda-20260230-000000.tgz` — удалены; `redpanda-copy.tgz`, `minio-20261301-000000.tgz`, `gateway-old.db` 29 сут. — на месте). Чужие файлы: `notes.txt`, `postgres-<штамп>.tgz`, `.tgz.bak`, `.tar.gz`, `minio-.tgz`, `links-…db.age` и `gateway-…db` в корне, `links/links-…db`, `links/gateway-…db.age`, `links/sub/`, `other/`, `legacy-20200101/`, каталог `minio-<штамп>.tgz/`, `backup-prune.log`. Все старые — на месте. `SHA256SUMS` сверяется целиком: ушли строки удалённых архивов, строки `postgres-…`, живых архивов и отсутствующего файла остались.
  - `only-old` — только старые копии: удалены все шесть, `SHA256SUMS` пуст.
  - `dry-run` — ничего не удалено, `rm` не вызывался, `SHA256SUMS` не изменён.
  - `links` — ссылка `minio-<штамп 40 сут.>.tgz` на файл вне каталога и `links` → каталог вне: цели и сами ссылки на месте, `rm` не вызывался. **На этой машине SKIPPED**: Windows без Developer Mode не создаёт символьную ссылку, а копию вместо ссылки тест не принимает (`MSYS=winsymlinks:nativestrict`). Случай выполняется в CI на Linux.
  - `refusals` — нет `--dir`, каталога нет, неверный `--now`, неизвестный аргумент: код 2.
  - `clock` — обычный запуск по системным часам и `--log`.
  - `--mutants` — копия скрипта портится по одному якорю (ровно одно вхождение, иначе BROKEN). Первым идёт контрольный мутант, вторым — тождественный.
- `Makefile`: цели `backup-prune` (`DRY_RUN=1` — только список), `backup-prune-test` (`bash -n` обоих скриптов и тест), `backup-prune-mutants`. `backup` в конце вызывает `backup-prune.sh --dir "$(BACKUP_DIR)"`. `ci` включает `backup-prune-test`.
- `.github/workflows/go.yml`: шаг `make backup-prune-test` в задании `scripts-parity` (`ubuntu-latest`). Новое задание не заводилось: имена обязательных проверок не меняются.
- `Docs/ops/runbook.md`, раздел 4 — новый подраздел «Срок хранения копий: 30 дней (T-463)». В нём: правило «возраст, а не число»; таблица покрываемых имён; формат штампа (обязателен для бэкапа SQLite EPIC-004); что не трогается; команды и коды выхода; предупреждение про старый архив, принесённый в каталог ради восстановления. Дальше — «Ежедневное задание Планировщика задач»: `schtasks /Create /TN "multiverse\backup-prune" /SC DAILY /ST 10:00 /RL LIMITED /F /TR …` в `cmd.exe`, оговорки (`bin\bash.exe` сам ставит `PATH`, предел 261 символ у `/TR`, без `/RU` — только при входе пользователя), `Set-ScheduledTask … -StartWhenAvailable` для пропущенного запуска, проверка (`schtasks /Run`, `schtasks /Query … /V /FO LIST`), признаки невыполнения («Время прошлого запуска» старше суток или `30.11.1999`, «Прошлый результат» `267011`/`1`/`2`, журнал старше суток, `DRY_RUN=1` с возрастом ≥ 32 дней), снятие задания. Отдельный абзац — копии в томе `gateway-data`. Раздел 7 — еженедельная проверка задания.
- `infrastructure.md` §5.6 — только эксплуатационная часть, с пометкой о владельце под таблицей: у архивов томов «последние 4 еженедельных + все предрелизные за 30 дней» заменено на «≤ 30 дней по штампу, строго по возрасту». В периодичность добавлена ежедневная очистка `scripts/backup-prune.sh`, у `links.db` «скрипт чистит старше» заменено именем скрипта и форматом имён.

**Проверка команды Планировщика** (задание не регистрировалось):
- разбор `schtasks /Create /?` и `schtasks /Query /?` на этой машине: ключи `/TN`, `/TR`, `/SC DAILY`, `/ST ЧЧ:мм`, `/RL LIMITED`, `/F`; `/V`, `/FO LIST` существуют в этом виде;
- строка `/TR` с профилем владельца (`C:\Users\Алексей`) — около 218 символов, меньше 261;
- `C:\Program Files\Git\bin\bash.exe`, запущенный с пустым окружением (`env -i SYSTEMROOT=… PATH=/c/Windows/System32`), находит `/usr/bin/date`, `rm`, `mv`, `touch`;
- ручной запуск на каталоге в scratch с путём в форме Windows (`cygpath -w`) и `--log` с обратными слешами удалил ровно старые архивы, `minio-weird.tgz` — по mtime; `SHA256SUMS` и `legacy-x/` проверены.

**Строки для system-architect** (§5.6, расхождения по существу, текст не правил):
1. Периодичность архивов томов: «еженедельно (Task Scheduler → `pwsh scripts/backup.ps1`)». Скрипта `scripts/backup.ps1` в репозитории нет, бэкап сейчас только ручной (`make backup`, в том числе из `make deploy`). От этого зависит и RPO «≤ 7 дней при еженедельном». Решить: убрать упоминание или завести задачу на задание бэкапа.
2. Столбец «Проверка restore»: `make restore FILE=… TARGET=scratch`. У цели `restore` параметра `TARGET` нет.
3. В таблице тома названы `minio_data`/`redpanda_data` (имена as-is). Целевые — `minio-data`/`redpanda-data` (§5.5, правило именования T-409).
4. Откат миграции SQLite опирается на «предрелизную копию» (ADR-019, `Down` нет). Её срок теперь тоже 30 дней: откат из неё возможен в течение 30 дней после `make deploy`. Проверить §8 и ADR-019.

**Строка для T-317** (EPIC-004, runbook бота и gateway): бэкап SQLite пишет в `%USERPROFILE%\multiverse-backups\links\` имена `links-<штамп>.db.age` и `gateway-<штамп>.db`, штамп — `date +%Y%m%d-%H%M%S` по местному времени. Открытая копия в томе `gateway-data` (`/data/backup/*`) удаляется сразу после `docker cp`. Открытая копия `links-<штамп>.db` на хосте удаляется сразу после `age`: `backup-prune.sh` такое имя не покрывает, покрыт только `.db.age`. Срок хранения копий на хосте держит `scripts/backup-prune.sh` (runbook, раздел 4).

### (б) Маскировка значения

**Что сделано.**
- `scripts/lib/llm-endpoint.sh`, `llm_endpoint_judge`: `local masked` = в `$LLM_EP_RAW` есть `@`. У каждой из четырёх фраз (any-address, прочий `invalid`, «has no port» для loopback/localhost/docker-host и для прочих `local`) есть вторая форма. Она печатает значение только схемой — `$LLM_EP_VAR=$LLM_EP_SCHEME://…`. Хост не называется: «names the any-address», «names a host which is not an address…». Добавлен хвост «(the rest of the value and its host are not printed: the value holds an @, and what stands in front of it may be a key)», как у `aboutTheHost` в `internal/llm/config.go` (кончик EPIC-003, только чтение). Порт в «has no port» — умолчание схемы, а не часть значения, поэтому он остаётся. Без `@` фразы прежние побайтно. Маскировка доходит до `llm_endpoint_classify` (`LLM_CLASS_ERROR`) и правила 6 `compose-lint`: оба берут фразу из судьи.
- `scripts/lib/LlmEndpoint.psm1`, `Set-LlmEndpointClass`: то же, `$masked = $Endpoint.Raw.Contains('@', [StringComparison]::Ordinal)`. Тексты совпадают побайтно: P01 — 79 общих шаблонов, было 75.
- Комментарии `LLM_EP_RAW` (`llm-endpoint.sh:152`) и `Raw` (`LlmEndpoint.psm1`) приведены к факту: значение обрезано, но НЕ замаскировано; принятое значение может содержать `@` после первого `/`.
- Стенд `testdata/script-parity`:
  - `Expect.AbsentFold` — `Absent` в любом регистре (`scenario.go`, `compare.go`). Хост печатается в нижнем регистре, поэтому `FakePW` в значении должен ловиться и как `fakepw`. Прежний `Absent` не менялся.
  - `H67` — `http://fakepw/x@127.0.0.1:{PORT}` (однословный хост без порта), `AbsentFold`: `fakepw`, `127.0.0.1`, `/x@`.
  - `H68` — `HTTP://FakePW/x@127.0.0.1:{PORT}`, `AbsentFold`: `fakepw`, `127.0.0.1`.
  - `H69` — `https://localhost/fakepw@127.0.0.1:{PORT}` (запускаемые виды, порт схемы 443), `AbsentFold`: `fakepw`, `127.0.0.1`, `localhost`.
  - `H70` — `http://0.0.0.0:{PORT}/fakepw@10.0.0.5:{PORT}` (any-address), `AbsentFold`: `fakepw`, `0.0.0.0`, `10.0.0.5`.
  - `H71` — `http://fakehost/x` без `@`: фраза и значение прежние, `AbsentFold`: `are not printed`.
  - Мутанты `M29` (bash: `*@*) masked=0`) и `M30` (pwsh: `$masked = $false`) — маскировка снята в одной половине, ловят `H67`–`H70`.
- `scripts/compose-lint.sh --fixtures`: новая строка фикстуры `# expect-absent: <фрагмент>` — фрагмент, которого в отказе не должно быть ни в каком регистре. При нарушении отчёт не повторяется: в нём этот фрагмент. Фикстура `testdata/compose-lint/bad-llm-url-at-after-path.yml`: `MV_LLM_URL: http://fakepw/x@127.0.0.1:8888` и `MV_OLLAMA_URL: HTTP://FakePW/x@127.0.0.1:11434`, `expect-rule: 6`, `expect-text` с `MV_LLM_URL=http://… has no port`, `expect-absent`: `fakepw`, `127.0.0.1`, `/x@`. Строка добавлена в `testdata/compose-lint/README.md`.
- `infrastructure.md`, с отметкой T-463 в тексте: §3 (состав стенда — 110 сценариев, `H67`–`H71`; мутанты M18–M30; правило `# expect-absent:` фикстур) и §6.3.1 (строка в таблице изменений правила).

**Решения и отклонения.**
- Ветка «прочий `invalid`» (`kind=malformed`) сегодня недостижима: `llm_parse_url` отвергает такой хост раньше. Сценария на неё нет, фразу держит статическая сверка сообщений (P01).
- В `H70` вместо `127.0.0.1` после `@` стоит `10.0.0.5`: фраза any-address сама советует «write 127.0.0.1», поэтому `127.0.0.1` не может быть в `Absent` этой ветки.
- Регистр проверяется отдельным полем `AbsentFold`, а не заменой смысла `Absent`: чужие сценарии со словами вроде «nothing to stop» не должны менять поведение.

**Строка для system-architect** (итоговая — сужена при приёмке, см. «Итерация 2» и «Приёмка (tech-lead)»). После слияния T-463 пометку «Код расходится» в C-15 v1.5 («Печать значения в отказах») и в ADR-005 **не снимать, а сузить**. При `@` в значении хост больше не называют:
- фразы `llm_endpoint_judge` и `LlmEndpoint.psm1`;
- облачная фраза правила 6 `compose-lint`;
- отказ `up` «refusing to start a second server» в `llm-server`.

Код скриптов расходится с правилом только в двух местах:
- три отказа `llm-bench`: `scripts/llm-bench.sh:844-846`, `scripts/llm-bench.ps1:478-482`;
- строки успеха и `health` в `llm-server`.

Вся маскировка вывода скриптов — T-468 (EPIC-001, devops). Она ждёт ответа system-architect: распространяется ли правило C-15 на весь вывод. `contracts.md` и ADR исполнитель не правил.

### Прогоны

| # | Команда | Итог |
|---|---|---|
| 1 | `bash -n scripts/lib/llm-endpoint.sh scripts/compose-lint.sh scripts/backup-prune.sh scripts/backup-prune-test.sh` | ok |
| 2 | разбор `LlmEndpoint.psm1` (`[Parser]::ParseFile`, pwsh 7.6.6) | 0 ошибок |
| 3 | `go run ./testdata/script-parity -run '^(H1[58]\|H22\|H6[1-9]\|H7[01]\|D0[78]\|P01)$'` | 20 passed, 0 failed |
| 4 | `make scripts-parity` (bash и pwsh, режим full) | 112 passed, 0 failed, 2 known-failing (K01, K02 — прежние), 110 сценариев; P01 — 79 общих шаблонов; T01/T02 — по 123 случая, классы не изменились |
| 5 | `go run ./testdata/script-parity -mutants -run '^M(00\|01\|29\|30)$'` | M00 KILLED, M01 GREEN, M29 KILLED (H67–H70), M30 KILLED (H67–H70) |
| 6 | `make parity-mutants` (все 31) | контрольный M00 первым — KILLED, M01 GREEN, M02–M30 — KILLED (в том числе M29 и M30), rc=0 |
| 7 | `make compose-lint` | ok — 15 сервисов в 3 файлах, 8 правил; fixtures ok — 59 bad, 11 good |
| 8 | копия `scripts`/`build`/`.github`/`shared/env`/`testdata/llm`/compose в scratch, `--fixtures` на новой фикстуре и `good-llm-url-local.yml`; затем мутант `masked=0` в копии `llm-endpoint.sh` | без мутанта — ok; с мутантом — отказ по `expect-text` и по трём `expect-absent` (`fakepw`, `127.0.0.1`, `/x@`), rc=1 |
| 9 | `make backup-prune-test` (TMPDIR в scratch) | 0 failures, 1 skipped (`links`, нет символьных ссылок на Windows) |
| 10 | `make backup-prune-mutants` | P00 KILLED (контрольный, первым), P01 GREEN, P02 «порог 0 дней» KILLED, P03 «оставить последний» KILLED, P04 `-ge` KILLED, P05 без mtime KILLED, P06 любой префикс KILLED, P07 строки `SHA256SUMS` остаются KILLED, P09 `rm` по маске KILLED; P08 (обход ссылки `links`) и P10 (удаление ссылки) — SKIPPED без символьных ссылок, проверяются в CI на Linux |
| 11 | `go build ./... && go vet ./... && go vet ./testdata/script-parity`, `gofmt -l testdata/script-parity` | ok, пусто |
| 12 | `golangci-lint run ./...` | 0 issues |
| 13 | `go test -short -count=1 ./...` | ok — 27 пакетов, 0 FAIL |
| 14 | `gitleaks dir --no-banner --redact --config=.gitleaks.toml .` по копии 17 изменённых и новых файлов (с карточкой и `dev-log.md`) с `.gitleaks.toml`/`.gitleaksignore` ветки | no leaks found; копия удалена по точному пути |

`make ci` целиком не запускался: `secrets-scan` делает оркестратор, `test-race` на Windows — SKIPPED, `vuln` и `test-e2e` задача не затрагивает. Временные каталоги scratch (`t463-prunetest`, `t463-lintmut`, `t463-manual`, `t463-leaks`) удалены по точным путям. Временные каталоги тестов удаляются самими тестами по пути из `mktemp`.

**Изменённые файлы.** `scripts/backup-prune.sh` (новый), `scripts/backup-prune-test.sh` (новый), `Makefile`, `.github/workflows/go.yml`, `Docs/ops/runbook.md`, `Docs/dev-team/architecture/infrastructure.md`, `scripts/lib/llm-endpoint.sh`, `scripts/lib/LlmEndpoint.psm1`, `scripts/compose-lint.sh`, `testdata/compose-lint/bad-llm-url-at-after-path.yml` (новый), `testdata/compose-lint/README.md`, `testdata/script-parity/{scenario,compare,scenarios,mutants}.go`; карточка и `dev-log.md`.

**Для коммита.** Новые `.sh` созданы на Windows. Если бит исполнения нужен в индексе, как у соседних скриптов, — `git update-index --chmod=+x scripts/backup-prune.sh scripts/backup-prune-test.sh` после `git add`. Makefile и CI вызывают их через `bash`, так что без бита всё работает.

**Предложения в бэклог.**
1. Принятое значение с `@` после первого `/` (например `http://pw:2024/x@10.0.0.5:8080`: хост `pw`, порт 2024, `local`) по-прежнему печатается целиком строками успеха и не-отказов `llm-server`: `probing … (from MV_LLM_URL=…)`, `is not an address of this machine`, `starting … the platform calls it at …`. Правило 6 `compose-lint` для облачного вердикта печатает хост (`points at 'pw.com'`). У платформы `LogValue` хост такого значения скрывает (T-451). Нужна отдельная задача: те же правила печати для принятых значений в обеих половинах, сценарии и фикстура.
2. `make health` показывает возраст новейшего архива, но не состояние задания `backup-prune`. Можно добавить строку о последней записи `backup-prune.log`.
3. На стенде Windows случай символьных ссылок пропускается. Если включить Developer Mode (решение владельца), тест и мутанты P08/P10 пойдут и локально.

### Итерация 2
devops-engineer#1 (TEAM-1, Opus), 2026-09-13. Основание — ревью #1 (`review.md`, «T-463 · ревью #1»: 0/1/5/6) и решения оркестратора: Ma-1 закрыть здесь (вариант а), Mi-2…Mi-5, N-1…N-4, N-6 — в этой итерации, Mi-1 — в бэклог. Не закоммичено, `git add` не делал. `.env` не открывался. Docker и стенд LLM 127.0.0.1:8888 не трогались, `make backup` не запускался, реальный каталог бэкапов и Планировщик задач не трогались.

**Ma-1 — хост не называется ни в одном отказе при `@`.**
- `scripts/compose-lint.sh`, правило 6. Цикл bash передаёт вместе с вердиктом седьмое поле: `1`, если в значении есть `@`. Хост в вердикте не обнуляется. Python при `cloud` и флаге `1` пишет свою фразу без хоста и без значения: `{service}: {key} points at a host which is not a local address (…); a cloud endpoint needs MV_LLM_CLOUD_ENABLED=true … (the value and its host are not printed: the value holds an @, and what stands in front of it may be a key)`. Проверка формата вердиктов — группы по 7. Без `@` фраза прежняя (`bad-llm-url-dotted-name` — зелёная).
- Фикстура `testdata/compose-lint/bad-llm-url-at-cloud.yml`: `MV_LLM_URL: http://fakepw.example/x@10.0.0.5:8080` и `MV_OLLAMA_URL: HTTPS://FakePW.Example/x@10.0.0.5:8080`. `expect-rule: 6`, три `expect-text`, `expect-absent`: `fakepw`, `10.0.0.5`, `/x@`. Строка в `testdata/compose-lint/README.md`.
- `scripts/llm-server.sh` и `scripts/llm-server.ps1`, отказ `up` «refusing to start a second server». Если в `Raw` есть `@` (`case *@*` / `.Contains('@', Ordinal)`), печатается переменная вместо пробы: `llm: the address of MV_LLM_URL (http://…) already answers <код> at <путь пробы> and ops/llm-server.pid records no process of ours — … (the rest of the value and its host are not printed: …)`. Без `@` фраза прежняя (`U11` — зелёный). Тексты половин совпадают: P01 — 80 общих шаблонов, было 79.
- Стенд паритета: сценарий `U19` — `MV_LLM_URL=http://127.0.0.1:{PORT}/fakepw@x`, чужой сервер на адресе. Двойник отвечает 404 на неизвестный путь, это тоже «уже отвечает». `AbsentFold`: `fakepw`, `@x`. Обе половины печатают одинаково, `Via` — `/health`.
- Мутанты: `M31` (bash, `*@*)` → `*@*@*)`) и `M32` (pwsh, условие → `$false`) — ловит `U19`. Мутанты маски `compose-lint` — в копии дерева в scratch, у `compose-lint` нет своего реестра мутантов: контрольный (синтаксическая ошибка) — rc=2; флаг `llm_masked=0` в bash — фикстура отвергнута по трём `expect-text` и по `expect-absent: fakepw`, rc=1; Python игнорирует флаг (`and False`) — отказ по `expect-absent: fakepw`, rc=1. Без мутанта на той же копии — `fixtures ok`.
- Строка для system-architect исправлена при приёмке по решению оркестратора (вариант (б), Ma-2 ревью #2). Прежний текст «снять пометку» не действует. Итоговый текст — в разделе (б) выше: пометку «Код расходится» в C-15 v1.5 и ADR-005 не снимать, а сузить до трёх отказов `llm-bench` (`scripts/llm-bench.sh:844-846`, `scripts/llm-bench.ps1:478-482`) и строк успеха и `health` `llm-server`. Маскировка всего вывода скриптов — T-468. Строки успеха и не-отказов `llm-server` (Mi-1) здесь не правились.

**Mi-2 — `--dir`.** `backup-prune.sh` после замены `\` на `/` отвергает с кодом 2:
- путь не абсолютный — не `/…` и не `X:/…` (`.`, `./`, `c:`, `backups`, `backups\empty`);
- корень диска: `/`, `//`, `C:\`, `C:/`, `/c`, `/c/`;
- путь после `cd -P; pwd -P` снова корень, или ни один его компонент не содержит слова `backup` в любом регистре (`$work/plain`, `<backups>/..`).

Проверки пути идут до обращения к диску и повторяются после разрешения пути. **Отклонение от буквы решения:** «компонент `backup*`» понят как «компонент содержит `backup`». Буквальный шаблон `backup*` отверг бы каталог по умолчанию `$(HOME)/multiverse-backups` (Makefile, runbook, `/TR` задания). Компоненты разбираются через `read -a`, без подстановки шаблонов. Тест `refusals`: все перечисленные формы; `rm` не вызывался; старый архив в `backups/empty` при относительном `--dir` из родителя остался. Приняты: `Nightly-BACKUP` и путь в форме Windows (`cygpath -w`). Все запуски теста идут из `$work`. Поэтому относительный `--dir`, даже у мутанта, который его примет, не разрешается от каталога `make`, где в репозитории лежит `backups/`. Сами каталоги случаев лежат в `<mktemp>/backups/`, а у `mktemp` шаблон `prune-test.XXXXXX` без слова `backup`, чтобы отказ «нет компонента» можно было проверить.

**Mi-3 — штамп из будущего.** Если штамп позже «сейчас» больше чем на сутки (`FUTURE_SLACK=86400`), ему не верят: возраст по mtime, в вывод и журнал — `WARNING <путь>: its stamp is more than a day ahead of now; the stamp is not trusted, the file is aged by its mtime`. Опережение ровно на сутки — штамп в силе. Тест `mixed`: `minio-<+2 сут.>.tgz` с mtime 31 сут. — удалён «by its mtime», его строка ушла из `SHA256SUMS`; `redpanda-<+10 сут.>.tgz` с mtime 0 — на месте, предупреждение есть; `redpanda-<+1 сут.>.tgz` с mtime 400 сут. — на месте, предупреждения нет. Итог `done: 9 removed, 9 kept`.

**Mi-4.**
- `Makefile`, `backup`: `bash scripts/backup-prune.sh --dir "$(BACKUP_DIR)" || echo "backup: WARNING backup-prune exited $$? — the archives above are made; …" >&2`. Проверено на мини-Makefile в scratch с теми же `SHELL`/`.SHELLFLAGS`/`.ONESHELL` и поддельным скриптом: код 1 — предупреждение с кодом, зависимая цель `deploy` продолжается, rc=0. Реальный `make backup` не запускался.
- `Docs/ops/runbook.md`, раздел 4: комментарий к `make restore` в блоке команд; абзац «Архив для восстановления старше 30 дней» — держать вне `$BACKUP_DIR` (`%USERPROFILE%\multiverse-restore\`), передавать полным путём; то же — в «Восстановлении на чистой машине». Там же — абзац о нефатальной очистке в `make backup`.

**Mi-5.**
1. Подмена `rm` в тесте отказывает (код 1, файл не трогает) для пути из `BACKUP_PRUNE_TEST_RM_FAIL`. Случай `stuck`: код 1, `could not remove …`, неудалённый архив и его строка `SHA256SUMS` на месте, второй архив удалён, `done: 1 removed`. Мутант ревьюера R3 — теперь `P11`, KILLED; добавлен `P12` (неудачный `rm` считается удалённым) — KILLED.
2. Флаг `--require-links` у `backup-prune-test.sh` (и в режиме `--mutants` передаётся в дочерние прогоны): пропуск части со ссылками становится отказом. `Makefile`: `BACKUP_PRUNE_TEST_ARGS`. CI: `make backup-prune-test BACKUP_PRUNE_TEST_ARGS=--require-links`. На Windows с флагом — `FAIL links-file …`, rc=1: флаг работает.
3. CI, задание `scripts-parity`: новый шаг `make backup-prune-mutants BACKUP_PRUNE_TEST_ARGS=--require-links`, так что P08/P10 выполняются на Linux. У обоих шагов очистки — `if: ${{ !cancelled() }}`: красный паритет не скрывает их. `timeout-minutes` задания 15 → 20. Время шага на Linux оценено, не измерено: 17 прогонов теста, на Windows весь набор — 5 мин 46 с, основное — fork в Git Bash. Имена заданий не менялись.

**N-1.** Случай `gap`: зона `CET-1CEST,M3.5.0,M10.5.0/3` — строка POSIX, в Git for Windows нет zoneinfo, и `Europe/Berlin` молча стал бы UTC. `redpanda-20260329-023000.tgz` (время в разрыве) с mtime 29 сут. — на месте: возраст по mtime, а по штампу было бы 168 сут. `minio-20260329-013000.tgz` — удалён «168 days old by its stamp». GNU date 8.32 сам отвергает время в разрыве, поэтому мутант R2 (без проверки туда-обратно) по-прежнему выживает. Проверка признана защитной, комментарий в `stamp_epoch` приведён к факту. Тест держит итог, какой бы из двух слоёв ни отверг штамп.

**N-2.** `now_epoch=$((10#${now#@}))`; тест: `--now @0<epoch>` — код 0, `run at 2026-09-13 12:00:00 +0000`.

**N-3.** Случай `links` разделён на две части, каждая пропускается сама по себе: `links-file` (ссылка на файл) и `links-dir` (`links/` — ссылка на каталог вне). Если символьная ссылка на каталог не создаётся, делается junction: `MSYS2_ARG_CONV_EXCL='*' cmd /c mklink /J`, без этой переменной Git Bash превращает `/J` в путь. Junction снимается `cmd /c rmdir` до `rm -rf` временного каталога, а цель и так лежит внутри него. На этой машине `links-dir` выполняется через junction и мутант `P08` — KILLED; `links-file` и `P10` — SKIPPED, как раньше.

**N-4, N-6** — строки в runbook: не запускать `make backup` около 10:00 (гонка с `SHA256SUMS`); при «Отказано в доступе» из-за папки `multiverse\` — имя `multiverse-backup-prune` и те же подстановки в `/Run`, `/Query`, `/Delete`, `Set-ScheduledTask`; окно консоли при каждом запуске. **N-5** — в бэклог.

**Новые мутанты очистки:** `P11`…`P16` — R3; неудачный `rm` как удалённый; штамп из будущего снова в силе (Mi-3); относительный `--dir` принят; путь без `backup` принят; корень не распознаётся (Mi-2). `P05` — якорь перенесён на новую строку отката к mtime.

**`infrastructure.md`** (отметки T-463 в тексте): §3 — 111 сценариев, `up`/`down` — 19 (`U19`), «33 мутанта (M00–M32)» вместо устаревших «29 (M00–M28)», M18–M32; §6.3.1 — M31/M32 и строка таблицы изменений правила (облачная фраза правила 6 и отказ «второй сервер»).

#### Прогоны итерации 2

| # | Команда | Итог |
|---|---|---|
| 1 | `bash -n` `scripts/backup-prune.sh`, `scripts/backup-prune-test.sh`, `scripts/compose-lint.sh`, `scripts/llm-server.sh`, `scripts/lib/llm-endpoint.sh` | ok |
| 2 | `[Parser]::ParseFile` `scripts/llm-server.ps1`, `scripts/lib/LlmEndpoint.psm1` (pwsh 7) | 0 ошибок |
| 3 | `make backup-prune-test` (TMPDIR в scratch) | 0 failures, 1 part skipped (`links-file`: нет символьных ссылок на Windows); `links-dir` выполнен через junction; случаи `mixed`, `only-old`, `dry-run`, `stuck`, `gap`, `links`, `refusals`, `clock` |
| 4 | `make backup-prune-mutants` (TMPDIR в scratch) | 17 мутантов: P00 KILLED первым, P01 GREEN, P02–P09 и P11–P16 KILLED (в том числе P08 — через junction), P10 SKIPPED (ссылка на файл); rc=0; около 6 мин на Windows |
| 5 | `bash scripts/backup-prune-test.sh --require-links` на Windows | `FAIL links-file … (--require-links: a skip is a failure here)`, rc=1 — ожидаемо |
| 6 | `make scripts-parity` (bash и pwsh, full) | 111 сценариев; 113 passed, 0 failed, 2 known-failing (K01, K02 — прежние); `U11`, `U19` PASS; P01 — 80 общих шаблонов; T01/T02 — по 123 случая |
| 7 | `go run ./testdata/script-parity -mutants -run '^M(00\|01\|31\|32)$'` | M00 KILLED первым, M01 GREEN, M31 KILLED (U19), M32 KILLED (U19) |
| 8 | `make parity-mutants` | 33 мутанта: M00 KILLED первым, M01 GREEN, M02–M32 KILLED (в том числе M31, M32), rc=0 |
| 9 | `make compose-lint` | ok — 15 сервисов в 3 файлах, 8 правил; fixtures ok — 60 bad, 11 good |
| 10 | копия `scripts`/`build`/`.github`/`shared/env`/`testdata/llm`/compose в scratch, `--fixtures` на `bad-llm-url-at-cloud`, `bad-llm-url-dotted-name`, `good-llm-url-local`; три мутанта маски | без мутанта — ok; контрольный — rc=2; `llm_masked=0` — rc=1 (три `expect-text` и `expect-absent: fakepw`); Python без флага — rc=1 (`expect-absent: fakepw`) |
| 11 | мини-Makefile в scratch: `backup`-подобная цель с поддельным скриптом (`RC=1`/`RC=0`) и зависимая `deploy` | предупреждение с кодом 1, `deploy` продолжается, rc=0; при `RC=0` — без предупреждения |
| 12 | `golangci-lint run ./...` | 0 issues |
| 13 | `go test -short -count=1 ./...` | ok — 27 пакетов, 0 FAIL |
| 14 | `gofmt -l testdata/script-parity`, `go vet ./testdata/script-parity`; разбор `.github/workflows/go.yml` (`yaml.safe_load`) | пусто, ok, ok |

Временные каталоги scratch (`t463i2-lintmut`, `t463i2-junction`, `t463i2-mk`, `script-parity-2387756784` от `-keep`) удалены по точным путям. Каталоги тестов удаляют сами тесты по пути из `mktemp`.

**Изменённые файлы итерации 2.** `scripts/backup-prune.sh`, `scripts/backup-prune-test.sh`, `scripts/compose-lint.sh`, `scripts/llm-server.sh`, `scripts/llm-server.ps1`, `testdata/compose-lint/bad-llm-url-at-cloud.yml` (новый), `testdata/compose-lint/README.md`, `testdata/script-parity/scenarios.go`, `testdata/script-parity/mutants.go`, `Makefile`, `.github/workflows/go.yml`, `Docs/ops/runbook.md`, `Docs/dev-team/architecture/infrastructure.md`; карточка и `dev-log.md`.

**Предложения в бэклог (итерация 2).**
1. Mi-1 ревью #1: строки успеха и не-отказов `llm-server` (`probing … (from VAR=RAW)`, `is not an address of this machine`, `the platform calls it at …`) печатают принятое значение с `@` целиком. Задача devops, после ответа system-architect, распространяется ли правило C-15 на весь вывод скриптов.
2. N-5: срок журнала `backup-prune.log` (обрезать до 30 дней) и строка о последнем запуске очистки в `make health`.
3. У `compose-lint` нет реестра мутантов. Мутанты маски правила 6 выполнены вручную в копии. Если правил печати станет больше, завести `--mutants` по образцу `script-parity`.
4. Проверка штампа туда-обратно (`stamp_epoch`) с GNU date недостижима (мутант R2 выживает). Если на стенде появится другой `date`, добавить сценарий с подменой `date`.


## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Critical / Major / Minor / Nit | Где |
|---|---|---|---|---|---|
| 1 | 2026-09-13 | code-reviewer#1 (TEAM-1) | вернуть | 0 / 1 / 5 / 6 | `review.md`, «T-463 · ревью #1»: удаление по точным путям безопасно, маска четырёх фраз судьи в обеих половинах (14 мутантов ревьюера по ожиданию, R2/R3 выжили — N-1/Mi-5); Ma-1 C-15 v1.5 при `@` всё ещё называет хост в отказе облака правила 6 `compose-lint` и в «refusing to start a second server» `llm-server`, а строка для system-architect просит снять «Код расходится»; Mi-1 строки успеха — Minor (бэклог); Mi-2 `--dir /`, корень диска и относительный путь принимаются; Mi-3 штамп из будущего побеждает mtime; Mi-4 сбой очистки роняет `backup`/`deploy`, строки восстановления без предупреждения; Mi-5 ветка «не удалился» не покрыта, CI не замечает пропуска случая ссылок, P08/P10 не выполняются нигде |
| 2 | 2026-09-13 | code-reviewer#1 (TEAM-1) | вернуть | 0 / 1 / 0 / 2 | `review.md`, «T-463 · ревью #2»: Ma-1, Mi-2…Mi-5, N-1…N-4, N-6 закрыты. `--dir` не обходится junction на корень, `..`, UNC и `/proc/cygdrive`. Отступление «содержит `backup`» обосновано. Мутанты ревьюера R0–R4, P13/P14/P16, M31/M32 и C0–C2 правила 6 — по ожиданию. Ma-2: отказы `llm-bench` (`llm-bench.sh:844-846`, `.ps1:478-482`) при `@` печатают хост и значение целиком, а строка для system-architect просит снять «Код расходится» в C-15. Выбор: (а) маска в обеих половинах или (б) сузить строку для system-architect. N-7: mtime из будущего без предупреждения. N-8: `--fixtures` повторяет отчёт с поддельным значением при отказе `expect-text` |

## Приёмка (tech-lead)
tech-lead#1 (TEAM-1, Opus), 2026-09-13. Рабочая папка `.worktrees/T-463`, не закоммичено, `git add` не делал. `.env` не открывался. Docker, стенд LLM 127.0.0.1:8888, реальный каталог бэкапов и Планировщик задач не трогались, `make backup` не запускался.

**Решение: принять.** Итераций ревью — 2. Части (а) и (б) выполнены; часть (в) — T-464.

**Итог.** Очистка бэкапов старше 30 дней по штампу или mtime с тестом и мутантами, маска значения с `@` в отказах судьи, правила 6 `compose-lint` и «второго сервера» `llm-server`. Остальной вывод скриптов — T-468.

**Замечания ревью #2 (0/1/0/2).**
- **Ma-2** — решение оркестратора, вариант (б). Код `llm-bench` в этой задаче не менялся. Строка для system-architect сужена в трёх местах: карточка, раздел (б) и «Итерация 2»; DoD 11; обе записи T-463 в `dev-log.md`. Пометку «Код расходится» в C-15 v1.5 и ADR-005 после слияния не снимать, а сузить до трёх отказов `llm-bench` (`scripts/llm-bench.sh:844-846`, `scripts/llm-bench.ps1:478-482`) и строк успеха и `health` `llm-server`. Вся маскировка вывода скриптов — T-468 (EPIC-001, devops, `todo`, карточка `tasks/T-468.md`). Она ждёт ответа system-architect, распространяется ли правило C-15 на весь вывод. Строки в коде сверены по рабочей копии.
- **N-7** — закрыт при приёмке.
  - `scripts/backup-prune.sh`, ветка mtime: при `epoch - now > FUTURE_SLACK` пишется `WARNING <путь>: its mtime is more than a day ahead of now; the file is aged by it and stays until its mtime plus 30 days — check the clock`. Файл остаётся: опереться больше не на что. Шапка скрипта дополнена.
  - Тест, случай `mixed`: `minio-ahead.tgz` (mtime +400 сут.) — на месте, с предупреждением. `redpanda-ahead.tgz` (mtime ровно +1 сут.) — на месте, без предупреждения. У файлов со штампом из будущего предупреждения о mtime нет. Итог `done: 9 removed, 11 kept`.
  - Мутанты: `P17` (предупреждения нет) и `P18` (`-ge` — ровно сутки уже с предупреждением) — KILLED.
  - Runbook, раздел 4: формат штампа и признак в журнале.
- **N-8** — закрыт при приёмке. Комментарий `--fixtures` в `scripts/compose-lint.sh` приведён к факту: отказ `expect-absent` отчёт не повторяет, а отказ `expect-text` той же фикстуры повторяет. При сломанной маске фрагмент попадает в вывод, но это поддельное значение закоммиченной фикстуры. Поведение не менялось.

**Сверка DoD.**

| # | Итог |
|---|---|
| 1, 3, 6 | Подтверждены по ревью #1–#2 и прогонам ниже: удаление строго по возрасту, без «последних N»; отдельная точка входа `make backup-prune`; тест на временном каталоге с `--now`; `links/` и SQLite-копии покрыты; строка для T-317 — в «Выполнении» |
| 2 | Команды `schtasks /Create … /SC DAILY`, `/Query`, признаки невыполнения — runbook, раздел 4; задание не регистрировалось |
| 4 | `backup-prune-test` входит в `make ci` (около 14 с на Windows) и в задание CI `scripts-parity` (Linux, `--require-links`). Шаг мутантов `backup-prune-mutants` добавлен **только в CI**, в `make ci` его нет (цель `ci` в `Makefile` — без него). Поэтому `make ci` дольше не стал на эти 14 с, а не на 5,5 мин мутантов; выносить нечего. `timeout-minutes` задания — 20 |
| 5 | `infrastructure.md` §5.6 — эксплуатационная часть; четыре расхождения по существу — строками system-architect в «Выполнении» |
| 7–10 | `H67`–`H71`, `U19`, M29–M32 — PASS/KILLED (ревью #2 и `make scripts-parity` ниже); фикстуры `bad-llm-url-at-after-path` и `bad-llm-url-at-cloud` — в `fixtures ok` |
| 11 | Изменён решением оркестратора (вариант (б)): пометку сузить, см. Ma-2 |
| 12–13 | Часть (в) — T-464, не входит |
| 14 | `make ci` целиком не запускался: `secrets-scan` по диапазону ветки делает оркестратор перед коммитом; `test-race` на Windows — SKIPPED; `vuln` и `test-e2e` задача не затрагивает. Составные части, которые задача меняет, прогнаны ниже |
| 15 | `dev-log.md` и «Выполнение» заполнены; запись приёмки — в `dev-log.md` |

**Прогоны при приёмке** (рабочая папка, после правок N-7/N-8).

| # | Команда | Итог |
|---|---|---|
| 1 | `bash -n scripts/backup-prune.sh scripts/backup-prune-test.sh scripts/compose-lint.sh scripts/llm-server.sh scripts/lib/llm-endpoint.sh` | ok |
| 2 | `make backup-prune-test` (TMPDIR в scratch) | `0 failure(s), 1 part(s) skipped` (`links-file` — нет символьных ссылок на Windows; `links/` — через junction), ok, 14 с |
| 3 | `make backup-prune-mutants` (TMPDIR в scratch) | 19 мутантов: P00 KILLED первым, P01 GREEN, P02–P09 и P11–P18 KILLED, P10 SKIPPED (ссылка на файл, выполняется в CI), `mutants ok`, rc=0, 5 мин 31 с |
| 4 | `make scripts-parity` | 113 passed, 0 failed, 2 known-failing (K01, K02 — прежние); P01 — 80 общих шаблонов, P02 — 19; T01/T02 — по 123 случая; rc=0, 1 мин 25 с |
| 5 | `make compose-lint` | `ok — 15 services in 3 file(s), 8 rules`; `fixtures ok — 60 bad, 11 good` |
| 6 | `golangci-lint run ./...` | 0 issues |
| 7 | `go test -short -count=1 ./...` | ok, 0 FAIL |
| 8 | концы строк: скрипты — LF, без CR; `.md` рабочей копии — CRLF целиком (`text=auto`), смешения нет | ok |

**Слияние с кончиком `epic/EPIC-001-foundation`** (`3e3c95b`, база ветки `3f34195`). Трёхсторонний `git merge-file` с LF-нормализацией (`tr -d ''` у всех трёх сторон) по копии в scratch `t463acc-merge`: 23 изменённых и новых файла рабочей копии.
- **Без изменений на кончике** (слияние тривиально): `Makefile`, `.github/workflows/go.yml`, `infrastructure.md`, все `scripts/*`, `testdata/script-parity/*`, `testdata/compose-lint/*`, карточки `T-463.md` и `T-468.md` (новая).
- **Чисто, 0 конфликтов:**
  - `Docs/ops/runbook.md` — кончик добавил в раздел 2 подраздел «Том `gateway-data`, созданный до T-303» (T-303/T-458 из develop), задача правит раздел 4;
  - `tasks.md` — кончик: T-460 `done`, приёмка и бэклог T-460; задача: версия 0.1.3, раздел T-463 и новый T-468. Области не пересекаются.
- **Конфликт — только дописывание в конец:**
  - `dev-log.md` — после 8824 строк базы обе стороны дописали записи: задача — T-463 (итерации 1–2 и приёмка), кончик — T-460, T-458 и др.;
  - `review.md` — после 16856 строк: задача — ревью T-463 #1–#2, кончик — чужие ревью.

  Правки внутри записей T-463 (суженная строка для system-architect) лежат в блоке задачи, а не в теле базы.

**Рецепт.** `git merge epic/EPIC-001-foundation` в ветку задачи. Если драйвер `appendtail` (`.git/merge-drivers`) подключён, `dev-log.md` и `review.md` сольются сами. Иначе в обоих файлах взять оба блока целиком: сначала блок кончика, затем блок T-463, без правки текста записей. Остальные файлы сливаются автоматически. После слияния — `make compose-lint`, `make backup-prune-test`, `make scripts-parity`: скрипты кончика не менялись, достаточно одного прогона. Копия `t463acc-merge` удалена по точному пути.

**Бэклог** — в `tasks.md`, раздел T-463:
1. N-5 ревью #1 — срок `backup-prune.log` и строка в `make health`.
2. `--mutants` у `compose-lint`.
3. Сценарий с не-GNU `date`.

Mi-1 ревью #1 и Ma-2 ревью #2 — T-468.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 19:48 | 13.09 20:35 | 47m |
| Ревью #1 | 13.09 20:39 | 13.09 21:01 | 22m |
| Итерация 2 | 13.09 21:01 | 13.09 22:05 | 64m |
| Ревью #2 | 13.09 22:05 | 13.09 22:23 | 18m |
| Приёмка | 13.09 22:23 | 13.09 22:36 | 13m |
| Слияние | 13.09 22:36 | 13.09 22:36 | 0m |
| **Всего** | | | **164m** |
