# T-418: Перенос membus в shared/eventbus/membus

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / TEAM-1 |
| Волна | 1 |
| Размер | S |
| Статус | done (принята tech-lead#1) |

## Описание и DoD
- **Причина (T-416, дополнение ADR-001 от 2026-09-11)**: `membus` — вторая реализация C-01, её гоняет тот же contract-тест, а не двойник; сейчас она в `shared/testkit`, и бинарник импортирует её через узкое исключение depguard (T-410).
- **Что сделать**: перенести пакет (14 импортёров), снять исключение `cmd-multiverse-bus-memory` в `.golangci.yml`, поправить `foundation.md` §9 и CLAUDE.md. Метка `contract-change`.
- **DoD**: `go build`, тесты и линтер зелёные; в бинарнике нет импорта `shared/testkit`, кроме хука T-255; contract-тест шины на новом пути.

## Выполнение (developer)
developer#1 · 2026-09-11 · время начала и окончания в задании не указаны.

**Сделано.**
- `git mv shared/testkit/membus shared/eventbus/membus`: `membus.go` — чистое переименование (R100 до правки doc-комментария), `membus_test.go` — переименование плюс путь импорта; история идёт через `git log --follow`.
- Импортёры: 16 файлов импортировали `shared/testkit/membus`. Один — собственный тест пакета, он переехал вместе с ним. Остаются 15 внешних: `cmd/multiverse/bus_memory.go` (единственный не тест) и 14 тестов — `cmd/multiverse/{fake_contexts,serve}_test.go`, `shared/testkit/contract/membus_test.go`, `shared/testkit/gateway/{combat,harness,reaction,stand}_test.go`, `shared/testkit/state/state_test.go`, `shared/testkit/swarm/{fake_encounter_lifecycle,fake_encounter,fake_narrator_failure,fake_narrator}_test.go`, `test/e2e/stubs_v0_test.go` (тег `e2e`), `test/fixtures/events_test.go`. Заявленные 14 — это тесты без бинарника. В `services/*` путь модуля `multiverse-core.io/shared/testkit` не импортируется (грэп пуст). `internal/*` membus не импортирует.
- `.golangci.yml`: правило `cmd-multiverse-bus-memory` снято целиком, из `no-testkit-in-production` убрано исключение `!**/cmd/multiverse/bus_memory.go`. `desc` называет одно исключение — хук T-255 `fake_contexts.go`, уходит с T-256. Комментарий правила `cmd-multiverse-fake-contexts` больше не ссылается на снятое правило.
- Doc-комментарии: `bus_memory.go` (исключения больше нет, membus — вторая реализация C-01); `membus.go` (вторая реализация C-01 и транспорт `--bus=memory`, «реализует интерфейсы, а не вторую копию правил» вместо «not a second implementation»); `shared/testkit/contract/contract.go` («stub» → membus).
- Документы: `CLAUDE.md` (карта каталогов и раздел «Тестовые заглушки»), `AGENTS.md` (карта каталогов), `shared/eventbus/README.md`, `components/foundation.md` §9 (дерево, путь contract-теста `shared/testkit/contract` вместо несуществующего `shared/eventbus/bus_contract_test.go`, ядро владения testkit без membus) и §1 (раскладка). Отдельного README у membus нет.

**Проверки (дерево после переноса).**
- `go build ./... && go vet ./...` — 0; `go test -short -count=1 ./...` — все пакеты ok, в том числе `shared/eventbus/membus`.
- Contract-тест `TestBusContractOnMembus` с нового пути: 21 из 21 подтестов PASS, `TwoStepDedupRemembersOnlyAfterTheSideEffect` среди них.
- `go test -tags e2e -count=1 ./test/...` — ok (e2e, fixtures); `go vet -tags integration ./shared/testkit/...` — 0.
- `golangci-lint run ./...` (v2.13.2) — 0 issues; `go run ./cmd/mvctl contracts check` — 65 типов, 8 топиков, 58 файлов схем, 0.
- `go list -deps ./cmd/multiverse`: `shared/eventbus/membus` и, через хук, `shared/testkit/{swarm,swarm/template,state,mechanics}`; прямой импорт `shared/testkit*` в не-тестовых файлах `cmd/`, `internal/` — один, `testkit/swarm` (хук T-255).
- Мутанты depguard в изолированной копии (экспорт индекса плюс мои файлы; каталог `mktemp -d`, удалён по точному пути). Контрольный — синтаксическая ошибка в `bus_memory.go` → красный (typecheck, код 7). M1 — `_ "multiverse-core.io/shared/testkit"` в `bus_memory.go` → depguard, 1 находка. M2 — прежний путь `shared/testkit/membus`, пакет восстановлен в копии → depguard, 1 находка. «Было»: M2 под `.golangci.yml` из HEAD — 0 находок (прежнее исключение пропускало), M1 под HEAD — 1. Исходный файл после восстановления — 0 issues.
- Живой запуск: собранный из дерева бинарник `serve --contexts=all --bus=memory`, `MV_CORE_ADDR=127.0.0.1:18917` (порт перед этим свободен) → `/health` `{"status":"ok"}`, все семь контекстов ok. Остановлен свой PID, порт освободился; стек владельца и `:8888` не трогались. Бинарник собран отдельно, а не через `go run`: так останавливается ровно свой процесс, а не обёртка `go run` без дочернего.
- pre-commit в изолированной копии и `gitleaks git --staged` — результаты в `dev-log.md`.

**Отклонения от задания.** Число импортёров — 15 (14 тестов и `bus_memory.go`), а не 14. Кроме §9, в `foundation.md` поправлен §1: раскладка называла membus частью testkit. Поправлены `AGENTS.md` и комментарий `contract.go` — те же устаревшие утверждения.

**Для T-426.** API не меняется. `membus` читает через `eventbus.Delivery.Deliver` (`membus.go`, `deliver`), так что `recover` в `Deliver` покроет membus без правок в самом membus. Сменились пути: тест membus — `shared/eventbus/membus/membus_test.go`, случай contract-теста — по-прежнему `shared/testkit/contract/contract.go`. Одна ловушка: все десять тестов `shared/eventbus` — внутренний `package eventbus`, а `membus` импортирует `eventbus`. Поэтому тест T-426, которому нужен membus, нельзя положить во внутренний тест eventbus (цикл импорта). Место такого теста — `membus_test.go`, contract-тест или отдельный файл `package eventbus_test`.

**Предложения в бэклог.**
1. Правило `no-testkit-in-production` покрывает только `**/internal/**` и `**/cmd/**`, а `shared/eventbus/membus` теперь в бинарнике. Если не-тестовый файл membus импортирует `shared/testkit`, depguard промолчит, а бинарник получит testkit транзитивно. Предлагаю добавить в `files` правила `**/shared/eventbus/**` (решение system-architect, `contract-change`).
2. Документы вне моей области всё ещё называют `shared/testkit/membus` текущим путём — список в отчёте оркестратору и в `dev-log.md`.

**Добавка до ревью (2026-09-11, по решению оркестратора).**
- Предложение 1 принято и выполнено: в `files` правила `no-testkit-in-production` добавлен `**/shared/eventbus/**`. Тестовые файлы по-прежнему исключены (правило исключений `_test\.go$`). `desc` называет охват: internal/*, cmd/*, shared/eventbus/*, включая membus; единственное исключение — хук T-255. Сейчас ни один не-тестовый файл `shared/eventbus/**` не импортирует `testkit`, импорт есть только в `membus/membus_test.go`.
- Мутанты (изолированная копия, `mktemp -d`, удалена по точному пути; линтер по `./shared/eventbus/...`). Контрольный — синтаксическая ошибка в `membus.go` → красный (typecheck, код 7). M3 — `_ "multiverse-core.io/shared/testkit"` в `membus.go` → depguard `no-testkit-in-production`, 1 находка. M3 под `.golangci.yml` из HEAD → 0 (было молча). M4 — тот же импорт в `membus/mutant_test.go` (`package membus`) → 0. После восстановления — 0. Цикла нет: `shared/testkit` импортирует только `clock`, `env`, `eventbus`.
- Внесено в бывшие файлы T-412 (закоммичена в `0e3d795`): `README.md:128` — `, membus/ (в памяти)`; `README.md:139` — убрано `membus, `; `Docs/dev-team/testing/strategy.md:90` — `shared/eventbus/membus (…); shared/testkit: Dedup, …`; `strategy.md:368` — `eventbus/membus`. `infrastructure.md:1138` не тронут: запись F-5t, файл правит T-413.
- Проверки: `golangci-lint run ./...` — 0 issues; `go build ./... && go vet ./...` — 0; `go test -short -count=1 ./shared/eventbus/... ./cmd/...` — все ok; pre-commit в изолированной копии и gitleaks — в `dev-log.md`.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#1) | 2026-09-11 | 0 | 0 | 0 | 2 | принять |

Записи — `../review.md`, раздел «T-418 · ревью #1». Git видит rename (R097/R099), API `membus` не изменился (`go doc -all`), старого пути в коде и конфигах нет. `go list -deps ./cmd/multiverse` отличается от HEAD только путём membus. Правило depguard: мутанты M1 и M3 красные, а под конфигом HEAD проходили; тесты и `test/e2e` не задеты (M2, M6). Contract-тест 21/21, живой запуск на `:18941` — ok. N-1 — `CLAUDE.md:229` не называет `shared/eventbus/**` в охвате правила; N-2 — длинная строка комментария `.golangci.yml:91`.

## Приёмка (tech-lead)
**Решение: принята** · tech-lead#1 · 2026-09-11.

**Итог:** membus переехал в `shared/eventbus/membus` как rename (R097/R099), бинарник импортирует `shared/testkit` только через хук T-255, правило depguard ловит testkit в не-тестовом коде `shared/eventbus/**` и старый путь в `bus_memory.go`, а под конфигом HEAD оба мутанта проходили.

Проверки — в изолированной копии из экспорта индекса (`git checkout-index -a`, каталог `mktemp -d` в scratchpad, удалён по точному пути); рабочее дерево и файлы T-413 не открывались.

| Пункт DoD | Статус | Доказательство |
|---|---|---|
| `go build` зелёный | выполнен | `go build ./...`, `go vet ./...`, `go vet -tags e2e ./test/...`, `go vet -tags integration ./shared/testkit/... ./shared/eventbus/...` — 0 (go1.26.8) |
| тесты зелёные | выполнен | `go test -short -count=1 ./...` — 27 пакетов ok, 0 FAIL, в их числе `shared/eventbus/membus`; `go test -tags e2e -count=1 ./test/...` — `test/e2e` ok, `test/fixtures` ok. `-race` не запускался (T-401) |
| линтер зелёный | выполнен | `golangci-lint run ./...` (v2.13.2) — 0 issues; `go run ./cmd/mvctl contracts check` — 65 types, 8 topics, 58 schema files, 0 |
| в бинарнике нет `shared/testkit`, кроме хука T-255 | выполнен | `go list -deps ./cmd/multiverse`: `shared/eventbus/membus` и `shared/testkit/{mechanics,state,swarm,swarm/template}` — это замыкание хука; не-тестовый импорт `shared/testkit` в `cmd/`, `internal/`, `shared/eventbus/` — один, `cmd/multiverse/fake_contexts.go`; каталога `shared/testkit/membus` нет; грэп `testkit/membus` по `*.go`, `*.yml`, `Makefile`, `*.sh` вне `Docs/` пуст |
| contract-тест шины на новом пути | выполнен | `shared/testkit/contract/membus_test.go:15` импортирует `shared/eventbus/membus`; `TestBusContractOnMembus` — 21 подтест PASS, 0 FAIL, `TwoStepDedupRemembersOnlyAfterTheSideEffect` в их числе |
| документы в области задачи | выполнен | `CLAUDE.md`, `AGENTS.md`, `README.md`, `shared/eventbus/README.md:24`, `foundation.md` §1 и §9 (`:275`; `:276` — пометка «до T-418»), `strategy.md:90`, `:368` называют новый путь; остальные документы архитектора и тимлида — T-428 |

Живой запуск: бинарник из копии, `MV_CORE_ADDR=127.0.0.1:18963 serve --contexts=all --bus=memory` (порт перед этим свободен) → `/health` `{"status":"ok"}`, семь контекстов ok, в логе `bus: memory (--bus)`. Слушатель PID 75184 опознан по netstat, сверен по пути exe (scratchpad) и командной строке; остановлен только он, после этого `:18963` не слушается. Стек и `:8888` не трогались.

Мутанты depguard (правка файла в копии, восстановление со сверкой `cmp`, линтер по `./shared/eventbus/... ./cmd/multiverse/...`):

| # | Мутант | Результат |
|---|---|---|
| M0 | контроль: `func (` в конец `membus.go` | красный, typecheck, exit 7 |
| M1 | `_ "multiverse-core.io/shared/testkit"` в не-тестовом `membus.go` | depguard `no-testkit-in-production`, 1 issue, exit 1 |
| M1-HEAD | M1 под `.golangci.yml` из HEAD | 0 issues — до добавки проходило молча |
| M2 | `bus_memory.go` на старый путь `shared/testkit/membus` (пакет восстановлен по старому пути, сборка ok) | depguard `no-testkit-in-production`, 1 issue, exit 1 |
| M2-HEAD | M2 под `.golangci.yml` из HEAD | 0 issues — прежнее исключение пропускало |
| M3 | тот же импорт в `membus/mutant_test.go` | 0 issues — тесты не задеты |

После восстановления — `cmp` ok по каждому файлу, итоговый прогон — 0 issues.

Правки оркестратора (N-1, N-2) — по диффу индекса. `CLAUDE.md:229-232` называет охват `cmd/**`, `internal/**`, `shared/eventbus/**` (включая `membus`), исключение тестов и единственное исключение — хук T-255 (уходит с T-256); это совпадает с `files` и `desc` правила в `.golangci.yml:79-87`. `.golangci.yml:89-96`: строки комментария 52–78 символов, соседние блоки — 70–81; хвост `MV_SWARM_FAKE=true …` перенесён, смысл не изменился.

Статус `done` не ставится: секция «Учёт времени» ещё не заполнена, её заполняет оркестратор.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 11.09 18:05 | 11.09 18:23 | 18m |
| Ревью | 11.09 18:23 | 11.09 18:33 | 9m |
| **Всего** | | | **28m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
