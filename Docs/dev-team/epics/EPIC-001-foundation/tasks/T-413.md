# T-413: Пограничные случаи правил 3 и 8 линтера композиции

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / TEAM-1 |
| Волна | 1 (после приёмки T-255) |
| Размер | M |
| Статус | done (принята tech-lead#1 после ревью #2 и итерации 3 (Nit по фикстурам)) |

## Описание и DoD
- **Причина (ревью T-411 #1 и #2)**: пять Nit, ни один не проявляется в нынешних файлах композиции, большинство даёт громкий отказ.
  - N-1: `$${MV_X:-d}` — ложное срабатывание; во вложенной подстановке сообщение искажено, внутреннее умолчание не проверяется.
  - N-3: правило 3 не видит ключ в кавычках — `"MINIO_ROOT_USER":` без значения проходит (класс существовал до T-411).
  - N-4: граница «адресная переменная — по форме умолчания манифеста» ошибается в обе стороны: `MV_CORE_ADDR` (адрес прослушивания) принимается как сетевой; форма элемента не сверяется с формой переменной (`MV_MINIO_ENDPOINT:-http://…` проходит, хотя манифест требует без схемы); `MV_LLM_URL`, `MV_TELEGRAM_HEALTH_ADDR` и др. отвергаются лишне. Вариант: явный набор из шести сетевых переменных или сверка формы с умолчанием. Решать вместе с вопросом архитектору об адресах сети. **Решено (T-416, 2026-09-11):** оба варианта сразу — явный набор и сверка формы, см. п. «T-416 п. 15» ниже.
  - N-5: строчный YAML-комментарий после ключа без значения даёт ложный отказ; в строке и в `command:` совет «ключ без значения» неприменим.
  - N-6: `${MV_X:+x}`, `${MV_X+x}` и `$$$MV_X` проходят молча, хотя дают пустую строку при молчащем `.env`.
  - Приёмка T-411: `make compose-lint` проверяет у «плохой» фикстуры только ненулевой код, а не номер правила — фикстура может краснеть от чужого правила. Цель должна сверять ожидаемое правило (например, по имени файла или по строке в фикстуре).
  - **T-416 п. 15 (T-416, 2026-09-11; `contracts.md` §16 п. 5, `infrastructure.md` §3.1.1 п. 8; закрывает N-4).** Адрес сервиса сети compose — значение другого контекста запуска, второго поля умолчания в манифесте не вводим. Правило 8 знает **явный набор из шести переменных**: `MV_KAFKA_BROKERS`, `MV_MINIO_ENDPOINT`, `MV_CORE_URL`, `MV_QDRANT_ADDR`, `MV_NEO4J_URI`, `MV_TELEGRAM_GATEWAY_URL`. Он заменяет эвристику «адресная переменная — по форме умолчания». Значение compose для них обязано иметь ту же форму, что умолчание манифеста: схема ↔ схема, `host:port` ↔ `host:port`. Сетевая переменная вне набора краснит линтер. `MV_CORE_ADDR` (адрес прослушивания) и `MV_MEMORY_URL` (пустое умолчание передаётся как есть) в набор не входят.
  - **T-416 п. 16 (T-416, 2026-09-11; `contracts.md` §16 п. 5).** Манифест берёт для `OLLAMA_*` умолчания — значения настройки `infrastructure.md` §6.4 и loopback-origins SEC-15 (`DeclareExternal` с умолчанием); `RequiredWhen` снимается. Правило 8 сверяет равенство умолчания compose и манифеста и для `DeclareExternal`. Файлы: `shared/env/infra.go`, `shared/env/env_test.go` (сегодня ждёт ошибку без `OLLAMA_MAX_LOADED_MODELS` — ожидание меняется), комментарии `.env.example`, `scripts/compose-lint.sh`.
- **(T-412, 2026-09-11)** Описание COMPOSE_ENV_FILES в манифесте shared/env/infra.go (строка около 21, «env files compose reads») вводит в заблуждение: compose читает эту переменную только из окружения своего процесса, не из .env. Поправить описание (вариант а) — или убрать переменную из манифеста и .env.example (вариант б, contract-change); исполнитель выбирает с обоснованием.
- **(приёмка T-412, 2026-09-11)** У MV_LLM_URL в .env.example нет пометки [required], хотя композиция требует её через :? (T-404), а CLAUDE.md велит заполнять только помеченное — оператор по инструкции её пропустит. Добавить пометку; проверить линтером или тестом, что каждая переменная с :? в композиции помечена [required] в .env.example.
- **DoD**: на каждый случай фикстура (плохая или хорошая), мутант на каждое новое условие зеленит ровно свою фикстуру; настоящие файлы композиции чистые.
  - **(T-416 п. 15, 2026-09-11)** фикстуры на явный набор: переменная набора с формой, отличной от умолчания манифеста (`MV_MINIO_ENDPOINT:-http://…`), — отказ правила 8; `MV_CORE_ADDR` с адресом сервиса сети — отказ; сетевая переменная вне набора — отказ с подсказкой внести её в набор; `MV_LLM_URL` и `MV_TELEGRAM_HEALTH_ADDR` лишне не отвергаются; мутант «убрать переменную из набора» краснит свою фикстуру.
  - **(T-416 п. 16, 2026-09-11)** `mvctl env check` и `go test ./shared/env/...` зелёные с умолчаниями `OLLAMA_*` в манифесте; тест в `env_test.go` проверяет умолчания вместо ошибки; фикстура «умолчание compose для `OLLAMA_*` ≠ умолчанию манифеста» — отказ правила 8.
  - `.env.example` и `shared/env/**` правятся **после приёмки T-255** (developer#1 сейчас держит `shared/env/vars.go` и `.env.example`).
- **Исполнитель**: devops-engineer. Приоритет — низкий, бэклог волны 1.

## Выполнение (devops-engineer)
devops-engineer#1 (Opus), 2026-09-11. Выполнена, передана на ревью. Коммита нет — коммитит оркестратор.

**Итог.** Правило 8 знает явный набор из шести сетевых переменных и сверяет форму элемента с формой умолчания манифеста; эвристика «адресная по форме умолчания» удалена. Правило 8 сверяет умолчания compose и для `DeclareExternal` с умолчанием (`OLLAMA_*`), манифест получил умолчания §6.4 и loopback-origins SEC-15, `RequiredWhen` у блока снят. Разбор интерполяции переписан под поведение compose: `$$`, вложенные `${…}`, `:+`/`+`, YAML-комментарий, ключ в кавычках — N-1, N-3, N-5, N-6 закрыты. Правило 7 проверяет, что каждая `${VAR:?}` файла, загружаемого всегда, помечена `[required]` в `.env.example`. `scripts/compose-lint.sh --fixtures` сверяет у каждой «плохой» фикстуры её правило и причину и прогоняет «хорошие»; Makefile и CI вызывают один и тот же режим. Правил по-прежнему восемь.

| П. | Что | Сделано / доказательство |
|---|---|---|
| 1 | правило 8, явный набор (T-416 п. 15, N-4) | `NETWORK_ADDRESSES` из шести; форма элемента ↔ форма умолчания; вне набора — отказ с подсказкой; `MV_CORE_ADDR`, `MV_MEMORY_URL` — отказ с их причиной. Фикстуры `bad-network-shape`, `bad-network-core-addr`, `bad-network-outside-set`, `good-network-set`; мутанты M10, M11, M12×6, M13, M14 |
| 2 | `OLLAMA_*` (T-416 п. 16) | `shared/env/infra.go`: умолчания `-1`, `2`, `1`, `1`, `f16`, `http://127.0.0.1,http://localhost`, без `RequiredWhen`; правило 8 читает `DeclareExternal` с умолчанием; ключ без значения для них — отказ. Тест `TestOllamaBlockCarriesTheSettingsOfTheStandAsDefaults`, тест про провайдера ollama переписан; фикстуры `bad-ollama-default`, `bad-ollama-passthrough`, `good-ollama-defaults`; мутанты G1–G4, M15, M16 |
| 3 | N-1, N-3, N-5, N-6 | разбор интерполяции как у compose; фикстуры `good-escaped-dollar`, `bad-default-nested-outer`, `bad-default-nested-inner`, `bad-secret-escaped`, `bad-secret-quoted-key`, `bad-secret-quoted-list`, `good-quoted-keys`, `good-comment-after-bare-key`, `bad-nodefault-in-command`, `bad-alternate-colon`, `bad-alternate-plain`, `bad-nodefault-after-escape`; мутанты M01–M09 |
| 4 | цель сверяет правило «плохой» фикстуры | строка `# expect-rule: N` (+ `# expect-text:`) в каждой из 29 «плохих»; `--fixtures` требует ровно это правило и эту причину; проверка самой проверки — пять подложенных дефектов фикстур краснят прогон, контроль зелёный; мутант M18 |
| 5 | описание `COMPOSE_ENV_FILES` | вариант (а): описание и комментарий в `infra.go` говорят, что compose берёт переменную только из окружения своего процесса, строка в `.env` инертна (T-412). (б) не выбран: удаление — contract-change ради документа, а объявление остаётся полезным — это то самое значение, которое экспортирует Makefile и которое `mvctl env check` сверяет с `.env.example` |
| 6 | `[required]` у `MV_LLM_URL` + проверка | пометка была с T-404 (`1493b3d`), но первой строкой блока из 16 строк, далеко от `MV_LLM_URL=`; перенесена в строку прямо над переменной. Проверка — вторая половина правила 7 (`${VAR:?}`/`${VAR?}` файла, загружаемого всегда, кроме пинов `build/versions.env`, ↔ `[required]`); фикстура `bad-required-unmarked` (+ `.env`), мутант M17 |
| 7 | `infrastructure.md` §3.1.1 | восемь правил, суть 3, 7, 8 после T-413, механизм фикстур; убран дубль пунктов 1–2 и абзац «состояние до T-413»; §4.2 п. 2 и §6.5 — `OLLAMA_*` с умолчаниями |

**Было → стало (правила 3, 7, 8).**
- 3: ключ в кавычках и элемент списка в кавычках читались как «не ключ» — теперь ключ; `$${SECRET:?}` засчитывался как верная форма — теперь литерал; хвостовой комментарий после ключа без значения становился «литералом» — теперь срезается.
- 7: только интерполяция на чистой машине — плюс сверка `:?` ↔ `[required]`.
- 8: эвристика «адрес по форме умолчания» — явный набор + сверка формы; только `Declare` — плюс `DeclareExternal` с умолчанием; регэкспы — разбор как у compose (`$$`, вложенность, `:+`/`+`, комментарии); совет «ключ без значения» — только когда подстановка и есть значение ключа.
- Старый линтер на новых фикстурах: из 15 новых «плохих» пропускал 11 (`network-shape`, `network-core-addr`, `ollama-default`, `ollama-passthrough`, `alternate-colon`, `alternate-plain`, `nodefault-after-escape`, `secret-quoted-key`, `secret-quoted-list`, `secret-escaped`, `required-unmarked`), остальные отвергал с искажённым сообщением или неприменимым советом (на `bad-default-nested-outer` — одно сообщение с умолчанием `'${MV_GATEWAY_CLIENT_IDS:-mvctl'` и без внутренней проверки; новый даёт оба отказа); из 5 «хороших» отвергал 2 (`escaped-dollar`, `comment-after-bare-key`).

**Мутанты линтера** (генератор замены с проверкой «якорь найден ровно один раз»; прогон на настоящих файлах и на всех фикстурах; «зелёная» = плохая фикстура прошла, «причина» = отвергнута, но без своей `expect-text`):

| Мутант | Настоящие файлы | Что показал `--fixtures` |
|---|---|---|
| M00 контроль: синтаксическая ошибка | красные | все 34 фикстуры |
| M01 `$$` не экранирование | чистые | `bad-secret-escaped` зелёная, `good-escaped-dollar` отвергнута |
| M02 `$$$` съедает третий `$` | чистые | `bad-nodefault-after-escape` зелёная |
| M03a вложенная не проверяется | чистые | `bad-default-nested-inner` зелёная; `bad-default-nested-outer` — причина (она несёт обе стороны N-1) |
| M03b вложенное умолчание не отдельный отказ | чистые | `bad-default-nested-outer` — причина |
| M04 `:+`/`+` пропускаются | чистые | `bad-alternate-colon`, `bad-alternate-plain` зелёные |
| M05b хвостовой комментарий читается (строка-комментарий по-прежнему пропускается) | чистые | `good-comment-after-bare-key` отвергнута. Грубый M05 «комментарии не срезаются вовсе» краснит и настоящие файлы, и четыре фикстуры — не изолирует условие, заменён на M05b |
| M06 совет всегда «ключ без значения» | чистые | `bad-nodefault-in-command` — причина |
| M07 ключ в кавычках не читается | чистые | `bad-secret-quoted-key` зелёная |
| M08 элемент списка в кавычках не читается | чистые | `bad-secret-quoted-list` зелёная |
| M09 правило 3 на старом регэкспе | чистые | `bad-secret-escaped` зелёная |
| M10 без сверки формы | чистые | `bad-network-shape` зелёная, `bad-default-address-without-port` — причина |
| M11 без проверки «сервис этой сети» | чистые | `bad-default-not-a-service` зелёная |
| M12 ×6: переменная убрана из набора | **красные** (каждая из шести есть в композиции) | `good-network-set` отвергнута |
| M13 `MV_CORE_ADDR` без своей причины | чистые | `bad-network-core-addr` — причина |
| M14 без подсказки «внести в набор» | чистые | `bad-network-outside-set` — причина |
| M15 `DeclareExternal` не читается | чистые | `bad-ollama-default`, `bad-ollama-passthrough` зелёные |
| M16 ключ без значения у `OLLAMA_*` не отказ | чистые | `bad-ollama-passthrough` зелёная |
| M17 без сверки `[required]` | чистые | `bad-required-unmarked` зелёная |
| M18 `--fixtures` не сверяет правило | чистые | на настоящих фикстурах ничего — они отвергаются своими правилами; на проверке самой проверки ST1 (чужое правило) и ST5 (два правила) зеленеют, ST2–ST4 остаются красными |
| M99 тождественный | чистые | ничего |

Проверка самой проверки (`COMPOSE_LINT_FIXTURES` на каталог в scratchpad): ST0 контроль (верная плохая + верная хорошая) — 0; ST1 фикстура краснеет от правила 2, а называет 3 — 1; ST2 без `# expect-rule:` — 1; ST3 чужая `expect-text` — 1; ST4 «хорошая», нарушающая правило, — 1; ST5 два правила сразу — 1. Первый прогон нашёл дефект самой проверки: в каталоге без `bad-*.yml` незаматченный glob ронял `--fixtures` через `set -e` с кодом 2 и без сообщения — исправлено (`[ -e ] || continue`) до мутантов.

**Мутанты `shared/env`** (`go test -overlay`, ключ относительный `shared/env/infra.go`, каталог из `mktemp -d` удалён по точному пути): G0 контроль (синтаксис) — красный; G1 `RequiredWhen` возвращён — красный; G2 умолчание `OLLAMA_NUM_PARALLEL=2` — красный; G3 пустое `OLLAMA_ORIGINS` — красный; G4 `OLLAMA_ORIGINS=*` — красный.

**Стенд владельца** (`COMPOSE_ENV_FILES=.env,build/versions.env`, `.env` не открывался, печатались коды и хеши): `config -q` — 0 до и после; sha256 `config --format json` до/после: без профиля `0870f33d758b0688` = `0870f33d758b0688`, `gpu` `ddc89274a7f7e5b8` = `ddc89274a7f7e5b8`, `gpu+memory+dev` `80c0aac4ebfcbcd7` = `80c0aac4ebfcbcd7`; `up --dry-run --no-build` — 0. Значения `OLLAMA_*` в модели не изменились: умолчания манифеста взяты ровно из композиции. Стек и LLM не трогались.

**Проверки** — см. запись в `dev-log.md` EPIC-001 от 2026-09-11 (T-413).

**Решения и допущения.**
- Сверка `[required]` — только для файла, загружаемого всегда (та же граница, что у первой половины правила 7): обязательные переменные файлов профилей — именно те, что чистой машине не нужны (токен бота, T-397). Пины образов из `build/versions.env` исключены.
- Для `${MV_X:+x}` отказ безусловный — даже при пустом умолчании манифеста это текст compose вместо значения оператора.
- Форма «схема» сверяется как класс, без сравнения самой схемы и порта — ровно так сформулирован контракт (схема ↔ схема, `host:port` ↔ `host:port`).
- Разбор YAML построчный, как и прежде. *Уточнение по ревью #1 N-1:* строки блочных скаляров (`|`, `>`) читаются и проверяются. Не видны только строки внутри них, которые начинаются с `#`: такая строка срезается как комментарий. Остальные расхождения на уровне YAML (`\x24`, `''`, продолжение многострочной строки, поток-маппинг) — T-429.

### Итерация 2 (по ревью #1) · 2026-09-11

**Статус замечаний ревью #1.**

| № | Статус | Что сделано / доказательство |
|---|---|---|
| Ma-1 | закрыто | `MAPPED`: `\1[ \t]*:` — ключ с пробелом перед двоеточием снова ключ для правила 3, для отказа «ключ без значения» у `OLLAMA_*` и для «подстановка — всё значение» правила 8. Фикстуры `bad-secret-spaced-colon` (правило 3: голый `MINIO_ROOT_USER :` и литерал `MINIO_ROOT_PASSWORD :`), `bad-ollama-spaced-colon` (правило 8), `good-spaced-colon`. Мутант K1 (двоеточие вплотную) зеленит обе «плохие». Остальные места с шаблоном ключа: списочная форма `- KEY =v` — compose v5.2 отказывает сам («declared with a trailing space», зонд), тихой дыры нет; `TOP_LEVEL`/`BLOCK` дают только имя места в сообщении; `raw_images` правила 1 при `image :` даёт громкий ложный отказ, а не молчание (правило 1 вне этой задачи) |
| Mi-1 | закрыто | `external` — только `OLLAMA_*` (`EXTERNAL_PREFIX`), как в контракте §16 п. 5. Для `${OLLAMA_X}` без модификатора свой текст: «the container gets an EMPTY … instead of the default of infra.go». Фикстуры `good-compose-project-name` и `bad-ollama-nodefault`. Мутант K3 (снова все `DeclareExternal`) краснит «хорошую», K4 (текст про список допуска) снимает причину у «плохой» |
| Mi-2 | закрыто | q1 — `bad-comment-in-quotes` (`# expect-rule: 8`), мутант K5 (кавычки в `strip_comment` не учитываются) её зеленит. q2 — `bad-required-nested` с парным `.env` (`# expect-rule: 7`), мутант K6 (обход без `walk`) её зеленит |
| Mi-3 | закрыто | `--fixtures`: нет каталога — отказ «does not exist»; ноль «плохих» или ноль «хороших» — отказ «holds N bad and M good fixtures»; итоговая строка «fixtures ok — 34 bad, 7 good». Тест — ST8 (пустой каталог), ST8b (без «хороших»), ST9 (нет каталога): код 1 с этим текстом. Мутанты K7 и K7b |
| N-1 | уточнена формулировка в карточке (выше); сами формы — T-429 | — |
| N-2 | закрыто | две строки `expect-rule` — отказ (ST6, мутант K8); `bad-*.yaml`/`good-*.yaml` — отказ «not read — fixtures end in .yml» (ST7, K9); «as it must be» печатается только при полном совпадении (ST3 проверяет, что строки нет; K10) |
| N-3 | не трогалось — T-429 | — |

**Проверка самой проверки, итерация 2** (`COMPOSE_LINT_FIXTURES`, каталоги в scratchpad; в каждом, кроме ST8/ST8b/ST9, лежит верная «хорошая» фикстура, чтобы проверка нулей не краснила их по чужой причине): ST0 контроль — 0 и «fixtures ok — 1 bad, 1 good»; ST1…ST9 — код 1 и нужный текст, у ST3 нет «as it must be». Под мутантами: K7 портит ST8 и ST8b, K7b — текст ST9 (код остаётся 1 благодаря проверке нулей), K8 — ST6, K9 — ST7, K10 — ST3, M18 — ST1 и ST5; остальные ST не меняются.

**Мутанты итерации 2 на настоящих файлах и фикстурах**: настоящие файлы чистые у всех, кроме контрольного M00 (синтаксис — красное всё). K1 — `bad-secret-spaced-colon`, `bad-ollama-spaced-colon` зелёные; K3 — `good-compose-project-name` отвергнута; K4 — `bad-ollama-nodefault`, причина; K5 — `bad-comment-in-quotes` зелёная; K6 — `bad-required-nested` зелёная; K7…K10 на настоящих фикстурах ничего не меняют — их доказательство выше. Мутанты итерации 1 перепрогнаны на новом скрипте и 41 фикстуре — итог в `dev-log.md`.

**Стенд владельца, итерация 2**: хеши модели до и после совпадают (без профиля `0870f33d758b0688`, `gpu` `ddc89274a7f7e5b8`, `gpu+memory+dev` `80c0aac4ebfcbcd7`), `config -q` — 0. Файлы композиции в итерации 2 не менялись.

### Итерация 3 (по ревью #2, только фикстуры) · 2026-09-11 · developer#1

`compose-lint.sh` не менялся. Изменены только фикстуры и README.

| № | Статус | Что сделано |
|---|---|---|
| N-1 | закрыто | `bad-secret-spaced-colon.yml`: `MINIO_ROOT_USER :` — один пробел, `MINIO_ROOT_PASSWORD      :` — несколько. `bad-ollama-spaced-colon.yml`: `OLLAMA_KEEP_ALIVE :` — один пробел, добавлен `OLLAMA_NUM_PARALLEL      :` — несколько, и для него добавлен второй `expect-text`. Покрыты оба случая: и один пробел, и выравнивание в столбик |
| N-2 | закрыто | Новая фикстура `bad-comment-in-single-quotes.yml` (`# expect-rule: 8`): `ADMIN_NOTE: 'x #${MV_CORE_ADMIN_CLIENTS}'`. Это отдельный файл, потому что строка с двойными кавычками даёт тот же текст отказа |

**Мутации.** Прогон шёл в копиях экспорта индекса (`git checkout-index -a` плюс `git init`). В каждой копии заменялась ровно одна строка скрипта: якорь найден один раз, отличие от оригинала проверено через `cmp`/`diff`. После прогона копии удалены по точному пути.

| Мутант | `--fixtures` | Итог |
|---|---|---|
| base (без мутанта) | 0, «fixtures ok — 35 bad, 7 good» | зелёный |
| m0 контроль (синтаксис в `strip_comment`) | 1: у 33 «плохих» «no rule at all», 7 «хороших» отвергнуты | подмена действует |
| r1 `\1[ \t]*:` → `\1[ \t]?:` (`MAPPED`) | 1: в `bad-secret-spaced-colon` нет «MINIO_ROOT_PASSWORD carries the literal», в `bad-ollama-spaced-colon` нет «OLLAMA_NUM_PARALLEL is passed through with no value» | **убит** |
| r5 `strip_comment` не учитывает `'` | 1: `bad-comment-in-single-quotes` «passed the linter» | **убит** |

**Прогоны.** `make compose-lint` → 0 за 85 с: 15 сервисов в 3 файлах, «fixtures ok — 35 bad, 7 good». `go run ./cmd/mvctl env check` → 0, 67 переменных.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#1) | 2026-09-11 | 0 | 1 | 3 | 3 | вернуть |
| 2 (code-reviewer#1) | 2026-09-11 | 0 | 0 | 0 | 2 | принять |

Записи — `../review.md`, раздел «T-413 · ревью #1». N-1, N-3, N-4, N-5, N-6 и находка приёмки T-411 закрыты: это подтверждено зондами e/g/h и мутантами. Ma-1 — регрессия правила 3 относительно HEAD. Строка `MINIO_ROOT_PASSWORD : литерал` (с пробелом перед двоеточием; compose её принимает) и `MINIO_ROOT_USER :` проходят молча, а старый линтер их отвергал; правка — `[ \t]*` перед `:` в `MAPPED` плюс фикстура. Mi-1 — `${COMPOSE_PROJECT_NAME}` теперь отвергается, хотя это законная форма, а контракт ограничивает сверку `OLLAMA_*`. Mi-2 — два новых условия без фикстур, мутанты выживают. Mi-3 — `--fixtures` без фикстур зелёный.

Итерация 2 — `../review.md`, раздел «T-413 · ревью #2». Ma-1, Mi-1, Mi-2, Mi-3 и N-2 закрыты: зонды ревью #1 повторены точно, законные формы T-411 проходят, `OLLAMA_*` по-прежнему сверяются (мутант `EXTERNAL_PREFIX` убит), самопроверка отказывает во всех подложенных случаях. Nit 2 — сила фикстур: ослабление `[ \t]*` → `[ \t]?` и `strip_comment` без `'` выживают на фикстурах. Правка — несколько пробелов в «плохих» фикстурах Ma-1 и отдельная фикстура с одинарными кавычками.

## Приёмка (tech-lead)
**Решение: принята** · 2026-09-11 · tech-lead#1 (Opus). Итог: правило 8 знает явный набор из шести сетевых переменных и сверяет форму и умолчания `OLLAMA_*`, разбор интерполяции совпадает с compose, а `--fixtures` сверяет правило и причину каждой «плохой» фикстуры. Всё доказано фикстурами, и каждое новое условие убивается мутантом.

**Метод.** Проверялось содержимое индекса (58 путей), а не рабочая копия: `git checkout-index -a` в scratchpad, затем `git init`. Все прогоны шли в этой копии, после неё копия удалена по точному пути. Рабочее дерево по файлам задачи не менялось (`git diff --quiet` → 0). Стенд, контейнеры и LLM не трогались. Хеши модели владельца взяты из итераций 1–2 и ревью #2: файлы композиции в индексе отличаются от HEAD только комментариями шапки.

| Пункт DoD | Чем подтверждено |
|---|---|
| N-1 (`$$`, вложенная подстановка) | фикстуры `good-escaped-dollar`, `bad-secret-escaped`, `bad-default-nested-outer`, `bad-default-nested-inner`, `bad-nodefault-after-escape` отвергнуты своим правилом и причиной; мутанты M01–M03b (карточка, dev-log) |
| N-3 (ключ в кавычках, ключ с пробелом перед `:`) | `bad-secret-quoted-key`, `bad-secret-quoted-list`, `bad-secret-spaced-colon`, `good-quoted-keys`, `good-spaced-colon`. Свой мутант r1 (`\1[ \t]*:` → `\1[ \t]?:`) убит: у `bad-secret-spaced-colon` пропала причина «MINIO_ROOT_PASSWORD carries the literal», у `bad-ollama-spaced-colon` — «OLLAMA_NUM_PARALLEL is passed through». Nit-1 ревью #2 закрыт |
| N-4 / T-416 п. 15 (явный набор) | в скрипте `NETWORK_ADDRESSES` — ровно шесть переменных контракта, `MV_CORE_ADDR` и `MV_MEMORY_URL` — в `NOT_NETWORK_ADDRESSES` со своей причиной. Отказ правила 8 у `bad-network-shape` (`MV_MINIO_ENDPOINT:-http://…`), `bad-network-core-addr`, `bad-network-outside-set` (с подсказкой «add it to the explicit set NETWORK_ADDRESSES»); `good-network-set` пропускает `MV_LLM_URL` и `MV_TELEGRAM_HEALTH_ADDR`. Свой мутант s1 (из набора убран `MV_QDRANT_ADDR`) краснит `good-network-set` и настоящий `docker-compose.yml:399` |
| N-5 (хвостовой комментарий, совет в `command:`) | `good-comment-after-bare-key`, `bad-nodefault-in-command`, `bad-comment-in-quotes`, `bad-comment-in-single-quotes`. Свой мутант r5 (`strip_comment` без `'`) убит: «bad-comment-in-single-quotes broke rule 8 but passed». Nit-2 ревью #2 закрыт |
| N-6 (`:+`, `+`, `$$$`) | `bad-alternate-colon`, `bad-alternate-plain`, `bad-nodefault-after-escape` — правило 8; мутанты M02, M04 |
| Цель сверяет правило «плохой» фикстуры | у каждой из 35 «плохих» есть `# expect-rule:`/`# expect-text:`; прогон печатает «rejected by rule N, as it must be». Самопроверка ST0–ST9 и мутанты M18, K7–K10 — в карточке. **CI = Makefile**: оба вызывают `scripts/compose-lint.sh`, а затем `scripts/compose-lint.sh --fixtures` (`Makefile`, цель `compose-lint`; `.github/workflows/go.yml`, шаг «The fixtures of the linter»). Старый цикл `for bad in …` удалён в обоих местах |
| T-416 п. 16 (`OLLAMA_*`) | `infra.go`: умолчания `-1`, `2`, `1`, `1`, `f16`, `http://127.0.0.1,http://localhost`, без `RequiredWhen`. `TestOllamaBlockCarriesTheSettingsOfTheStandAsDefaults` проверяет умолчания вместо ошибки. Фикстуры `bad-ollama-default`, `bad-ollama-nodefault`, `bad-ollama-passthrough`, `bad-ollama-spaced-colon`, `good-ollama-defaults`, `good-compose-project-name` (граница `OLLAMA_*`, Mi-1). Мутанты G1–G4, M15, M16, K3, K4 |
| `COMPOSE_ENV_FILES` (T-412) | выбран вариант (а) — описание в `infra.go` поправлено. Обоснование отказа от (б) принято: объявление документирует значение, которое экспортирует Makefile |
| `[required]` у `MV_LLM_URL` + проверка | в индексе `.env.example:183` пометка стоит прямо над `MV_LLM_URL=`. Вторая половина правила 7 — фикстуры `bad-required-unmarked`, `bad-required-nested` (с парными `.env`), мутанты M17, K6 |
| Настоящие файлы композиции чистые | `make compose-lint` → 0 за 88 с: «ok — 15 services in 3 file(s), 8 rules», «fixtures ok — 35 bad, 7 good» |
| Тесты, линтер, сборка | `go build ./... && go vet ./...` → 0; `go run ./cmd/mvctl env check` → 0, 67 переменных; `go test -short -count=1 ./shared/env/... ./cmd/mvctl/...` → 7 пакетов ok; `golangci-lint run ./shared/env/...` → 0 issues |
| dev-log заполнен | в индексе `dev-log.md` есть записи T-413 (итерации 1–2 и 3); записи T-426 в том же файле — по плану оркестратора |
| Документы | `infrastructure.md` §3.1.1 (восемь правил, механизм фикстур), §4.2 п. 2, §6.5; шапка `docker-compose.yml`, комментарии `vars.go`/`env.go`, README фикстур |
| Контроль мутантов приёмки | тождественная копия скрипта → «fixtures ok — 35 bad, 7 good», код 0; замена якоря проверена (ровно одно вхождение, `cmp` ≠) |

**Условия коммита (оркестратору, не разработчику).**
1. Раздел «T-413 · ревью #2» в `review.md` (+106 строк) есть только в рабочей копии и не проиндексирован, а карточка на него ссылается. Проиндексировать его с этим коммитом.
2. В индексе лежит `Docs/dev-team/dashboard.html`. Это не файл T-413, а артефакт оркестратора; оставить его в коммите или вынуть — решает оркестратор.
3. Статус `done` не ставлю: «Учёт времени» пуст. Статус в карточке, `tasks.md` и `backlog.md` отмечает оркестратор после заполнения учёта.

**В T-429 (не блокирует):** предложения ревью #2 — многострочный plain-скаляр и `expect-text` «contracts.md §16 p. 5» у `bad-ollama-default` (мутант r3).

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 11.09 18:12 | 11.09 18:53 | 41m |
| Ревью | 11.09 18:53 | 11.09 19:14 | 21m |
| Разработка 2 | 11.09 19:14 | 11.09 19:39 | 24m |
| Ревью 2 | 11.09 19:39 | 11.09 20:03 | 24m |
| **Всего** | | | **1h 51m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
