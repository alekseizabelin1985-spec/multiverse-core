# T-450: Единое правило «локальный адрес» для скриптов и линтера compose

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / TEAM-1 |
| Волна | 1 |
| Размер | M |
| Исполнитель | devops-engineer#2 (Opus) |
| Статус | done (принята TEAM-1/tech-lead#1 после ревью #2; Mi-R2-1, N-R2-1, N-R2-2 закрыты при приёмке) |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-13 / 2026-09-13 |
| Метка | нет: Go-API и контракты не меняются. Правило — решение system-architect#1; Go-сторона (`IsLocalEndpoint`) — T-451, ADR-005 и C-15 — T-449 |

## Описание и DoD
Главный источник — раздел «### T-450» в `tasks.md`. Кратко:
- одна таблица случаев `testdata/llm/local-endpoints.tsv`: URL, ответ `local|cloud|invalid`, причина; не меньше 40 строк;
- функция классификации в `scripts/lib/llm-endpoint.sh` и `LlmEndpoint.psm1`; вызовы `llm-server`/`llm-bench` сохраняют поведение, изменения описаны;
- правило 6 `compose-lint.sh` вызывает функцию скрипта, своего `is_private` у него нет;
- тесты паритета sh и pwsh по таблице; мутанты «однословное имя» и «`0.0.0.0`» краснеют;
- `infrastructure.md` (раздел про адрес модели и гейт облака), `Docs/ops/runbook.md` §3.

## Выполнение (devops-engineer)
devops-engineer#2 (Opus), TEAM-1, ветка `task/T-450-local-endpoint-table` (от эпика `ab6cb1d`, с T-405), папка `.worktrees/T-450`. Не закоммичено: коммит делает оркестратор.

### Итог по пунктам DoD

| # | Пункт | Что сделано | Чем проверено |
|---|---|---|---|
| 1 | Таблица | `testdata/llm/local-endpoints.tsv`: 107 случаев — 36 `local`, 32 `cloud`, 39 `invalid`. В шапке — правило, уточнения исполнителя и формат | стенд читает таблицу и проверяет формат: заголовок, 3 поля, класс из набора, печатный ASCII без пробелов, без повторов, ≥ 40 строк, все три класса |
| 2 | Функция в двух модулях | `llm_endpoint_classify URL [VAR]` → `LLM_CLASS`, `LLM_CLASS_KIND`, `LLM_CLASS_HOST`, `LLM_CLASS_ERROR`. `Get-LlmEndpointClass -Url -Var` → `Class`, `Kind`, `Host`, `Error`. Общий судья — `llm_endpoint_judge` / `Set-LlmEndpointClass`; его вызывают и функция, и `llm_endpoint_finish` / `Complete-LlmEndpoint`. IPv4 и IPv6 разбираются вручную, одним алгоритмом в обеих половинах: `[ipaddress]::TryParse` принимает `127.1` и `0x7f000001` | стенд `T01` (bash), `T02` (pwsh + совпадение `kind`); сценарии `D07`, `D08` |
| 3 | `compose-lint` | Python выносит из модели тройки `сервис, ключ, URL` (разделитель NUL). bash прогоняет каждую через `llm_endpoint_classify` и отдаёт вердикты правилу 6. Облако — прежний текст отказа; `invalid` — «configuration error, not the cloud» с причиной функции. `ipaddress` и `ALLOWED_HOSTS` удалены. Фикстуры `bad-llm-url-any-address`, `bad-llm-url-dotted-name`, `bad-llm-url-no-port`, `good-llm-url-local` | `bash scripts/compose-lint.sh` — ok; `--fixtures` — 55 bad, 11 good, ok; мутанты C1–C4 |
| 4 | Мутанты | стенд: M18–M23; `compose-lint`: C1–C4 в копии дерева в scratch | таблицы ниже |
| 5 | Документы | `infrastructure.md`: новый §6.3.1 (правило, таблица, реализации, изменения поведения, переход до T-451) и строка правила 6 в §3.1.1. `runbook.md` §3: порт у любого локального адреса, `0.0.0.0`, таблица «какой адрес локальный», переходный период, строки таблицы провайдеров. `testdata/script-parity/README.md`, `testdata/compose-lint/README.md`, комментарий правила 6 в `docker-compose.yml`. `.gitattributes`: `testdata/llm/*.tsv text eol=lf` | чтение; концы строк сверены (CRLF-файлы без смешения) |

### Изменения поведения `llm-server` / `llm-bench`

| Вход | Было | Стало | Направление |
|---|---|---|---|
| однословное имя `http://ollama:11434` | облако: гейт краснит `llm-health` | `local`, доверенный, не запускаемый | ослабление |
| `http://0.0.0.0:N`, `http://[::]:N` | «адрес этой машины», `llm-up` поднимал сервер | `invalid`: `names 0.0.0.0, the any-address …`, код 1 | ужесточение |
| локальный адрес без порта: LAN, сервис, при провайдере `ollama` | принимался с портом схемы | `invalid`: `has no port; a local address is written with the port …` | ужесточение |
| `http://127.0.0.1:80` | «нет порта» (порт схемы не считался явным) | порт записан — принят | ослабление |
| `169.254.*`, `fe80::/10` вне префикса `fe80:`, `fc00::/8`, mapped-формы, `0:0:0:0:0:0:0:1`, `*.localhost` | облако | `local` | ослабление |
| `https://host:` (пустой порт), `[localhost]`, `%` в хосте | принимались | `invalid` | ужесточение |
| порт `:008888`; порт из 20 цифр | печатался как есть; переполнение `[ -gt ]` / `[int]` | порт 8888; «outside 1-65535» | уточнение |

Запускаемый адрес (`llm-up`/`llm-down`) — loopback, `localhost`, `host.docker.internal`, записанные без завершающей точки, у `openai_compat`. Доверенный (гейт облака) — любой `local`. Сообщение H18 («the local runtime would bind 80») сохранено для запускаемых видов адреса.

### Мутанты стенда (`make parity-mutants`)
24 мутанта, код 0: M00 убит, M01 зелёный, M02–M17 (прежние) убиты. Новые:

| # | Мутант | Кто поймал |
|---|---|---|
| M18 | `llm-endpoint.sh`: однословное имя — облако | `T01` (класс) и `T02` (`kind` bash ≠ pwsh) |
| M19 | `LlmEndpoint.psm1`: однословное имя — облако | `T02` |
| M20 | `llm-endpoint.sh`: `0.0.0.0` не any-address | `D07`, `T01`, `T02` |
| M21 | `LlmEndpoint.psm1`: `0.0.0.0` не any-address | `D07`, `T02` |
| M22 | `llm-endpoint.sh`: IPv4-mapped формы — облако | `T01`, `T02` |
| M23 | `LlmEndpoint.psm1`: адрес LAN без порта проходит, как до T-450 | `D08`, `T02` |

Однословное имя в `down`/`health` без DNS не проверить. Поэтому M18/M19 ловит таблица, а не сценарий: `T01`/`T02` выполняются при любом `-run`.

### Мутанты `compose-lint` (копия дерева в scratch)
Копия: `git ls-files -co --exclude-standard`, только `scripts/`, `testdata/compose-lint/`, `build/`, `.github/ci.env`, `.env.example`, `docker-compose*`, `shared/env/`. `.env` не копировался. Базовый прогон копии зелёный. Мутации — точная замена, якорь ровно один раз. Копия удалена по сохранённому пути.

| # | Мутант | Результат |
|---|---|---|
| C0 | без изменений | зелёный: fixtures ok — 55 bad, 11 good |
| C1 | `llm-endpoint.sh`: однословное имя — облако | **красный**: `good-llm-url-local` отвергнута; `bad-llm-url-no-port` — не по своей причине; `bad-network-outside-set` получил второе правило 6 |
| C2 | `llm-endpoint.sh`: `0.0.0.0` не any-address | **красный**: `bad-llm-url-any-address` — не по своей причине (облако) |
| C3 | `compose-lint.sh`: правило 6 пропускает `invalid` | **красный**: `bad-llm-url-any-address` и `bad-llm-url-no-port` прошли линтер |
| C4 | `llm-endpoint.sh`: локальный адрес без порта проходит | **красный**: `bad-llm-url-no-port` прошла линтер |

### Прогоны (go1.26, Windows 11, Git Bash, pwsh 7.6.6, Docker Compose v5.2.0 — только `config`)
- `bash -n` по `llm-endpoint.sh`, `compose-lint.sh`, `llm-server.sh`, `llm-bench.sh` — ok. `Parser::ParseFile` по `LlmEndpoint.psm1`, `llm-server.ps1`, `llm-bench.ps1` — 0 ошибок.
- `gofmt -l testdata/script-parity` — пусто; `go vet ./testdata/script-parity` — ok.
- `make scripts-parity`: 99 сценариев — 97 PASS + 2 KNOWN-FAILING; `P01`, `P02`, `T01` (107), `T02` (107) — PASS; порты двойника 34/34.
- `make parity-mutants` — код 0 (таблица выше).
- `bash scripts/compose-lint.sh` — `ok — 15 services in 3 file(s)`; `--fixtures` — `55 bad, 11 good`, ok.
- `make ci BASE=develop` — код 0: lint 0 issues, `test`, `test-race` SKIPPED (нет cgo), `contracts`, `secrets-scan` (no leaks ×2), `privacy-scan`, `vuln`, `compose-lint`, `scripts-parity`, `test-e2e`. Первые два запуска упали на `parallel golangci-lint is running` — линтер параллельного агента; после освобождения блокировки зелёный.
- `gitleaks dir --config=.gitleaks.toml` по таблице, `endpoints.go`, обоим модулям, `compose-lint.sh`, `runbook.md`, `infrastructure.md`, `good-llm-url-local.yml` — no leaks.
- Слушатели TCP до и после прогонов: своих нет. Новый `8081` — `com.docker.backend`/`wslrelay`, не из этих прогонов: контейнеры не запускались, `docker compose` вызывался только как `config`. К `127.0.0.1:8888` запросов не было.

### Отклонения от задачи
- В `infrastructure.md` отдельного раздела «адрес модели и гейт облака» не было. Добавлен §6.3.1 перед §6.4. §6.4 не тронут. §4.2 п. 4 (гейт «loopback / host.docker.internal / RFC1918») тоже не тронут: это зона T-453, строки рядом с её правкой. После слияния T-453 п. 4 стоит заменить ссылкой на §6.3.1.
- §3.1.2 (стенд) не переписан: там «18 мутантов» и «97 сценариев», сейчас 24 и 99. Новое описано в §6.3.1 и в README стенда, чтобы не пересекаться с T-453 в §3.1.
- Сверх состава: `.gitattributes` (`eol=lf` для таблицы) и комментарий правила 6 в `docker-compose.yml`.

### Предложения в бэклог
1. T-451 (EPIC-003): Go-тест `IsLocalEndpoint` читает ту же таблицу и снимает `\r` с конца строки (на checkout без `.gitattributes` из этой ветки). `needsExplicitPort` приводится к правилу «любой `local`», `0.0.0.0`/`::` — к `invalid` (ошибка конфигурации, а не `ErrCloudDisabled`).
2. После слияния T-453: `infrastructure.md` §4.2 п. 4 заменить ссылкой на §6.3.1, в §3.1.2 обновить счётчики (24 мутанта, 99 сценариев, проверки `T01`/`T02`).
3. Architect#1: подтвердить уточнения из шапки таблицы — завершающая точка, `ollama.` → `cloud`, однословное имя с буквы, `:80` явный, `%` и скобки → `invalid`. Строки таблицы меняются вместе с решением.
4. Сценарий `health` с однословным именем, резолвящимся без сети (например, через подменённый резолвер стенда), чтобы гейт облака проверялся запуском, а не только таблицей. Сейчас такого способа нет.

## Итерация 2 (по ревью #1 и решению system-architect#1) · 2026-09-13

devops-engineer#2 (Opus), та же ветка и папка, база `ab6cb1d`. Не закоммичено.

### Что закрыто

| Замечание | Что сделано | Чем проверено |
|---|---|---|
| M-1 | Точка на конце снимается только у зарезервированных имён. `LLM_IPV4_RE` сверяется с `$host` / `$HostName`, а не с `bare`. Итог: `127.0.0.1.` и `0.0.0.0.` — `cloud`, kind `name`. Шапка таблицы, комментарии обеих половин и `infrastructure.md` §6.3.1 переписаны. Строки: `http://127.0.0.1.:8888`, `http://192.168.1.10.:8080`, `http://0.0.0.0.:8888` (решено `cloud`: это имя для DNS, а не any-address) — `cloud`; `http://host.docker.internal.:8888`, `http://api.localhost.:8080` — `local` | мутанты M24 (bash) и M25 (pwsh) по отдельности; фикстура `bad-llm-url-dotted-quad`, мутант compose-lint C6 |
| Mi-1 | Строки `http://134744072:443` и `http://0x08080808:443` — `cloud` | `T01`/`T02` |
| Mi-2 | Хост без скобок с символом, который Go `url.Parse` не пускает в хост (пробел, `\`, `^`, `` ` ``, `{`, `\|`, `}`), — `invalid`, одно сообщение в обеих половинах. Набор взят у Go, а не строже: `~ ! $ & ' ( ) * + , ; =` проходят `url.Parse`, такое имя — `cloud` (строка `http://olla~ma:11434`). Строки с `\`, `^`, `{}`, `\|`, backtick. Пробел формат таблицы не допускает: два случая с пробелом стенд держит сам (`endpointStandCases`) | `T01`/`T02`, мутант M27 |
| Mi-3 | Драйверы печатают третье поле — текст ошибки. `T02` сверяет его между bash и pwsh, как `kind`. Мутант уровня R1: в `.psm1` `%` в скобках уходит в разбор IPv6 — класс и kind те же, меняется только фраза | мутант M26 |
| Mi-4 | runbook §3: врезка риска резолвера (search-домены, RFC 6761, рекомендация IP). Утверждение «однословное имя резолвится только внутри сети compose» исправлено. §6.3.1: от DNS не зависит класс, но зависит место, куда уйдёт запрос | чтение |
| Mi-5 | Блок T-450 в `compose-lint.sh` перенесён выше комментария правила 3. Комментарий правила 3 снова стоит прямо над `minioadmin_hits=`, в диффе с базой он не тронут | `git diff`; мутант compose-lint C5 |
| N-1 | Строка в таблице изменений поведения §6.3.1: запускаемый — любой `kind=loopback`, включая mapped-формы и несжатый `::1`. Комментарий в обеих половинах | чтение |
| N-2 | Отказ на `?`/`#` идёт первым, раньше проверки на не-ASCII. Он печатает значение только до `?`/`#` и пишет «the rest of the value is not printed». Ни одна фраза разбора больше не выводит query, userinfo не выводилась и раньше *(неверно: выводилась в четырёх отказах до проверки `@` — ревью #2 Mi-R2-1, закрыто при приёмке)*. Фикстура `bad-llm-url-query`; в `H28` добавлено `Absent: x=1` | `H28`, фикстура, мутант compose-lint C7 |
| N-3 | В бэклог, см. ниже | — |
| N-4 | runbook §3: до T-451 не включать `MV_LLM_CLOUD_ENABLED=true` ради имени сервиса. Вместо этого указать адрес RFC 1918 сети compose (без закрепления он меняется при пересоздании контейнера) или дождаться T-451. Та же фраза — в §6.3.1 | чтение |
| N-5 | Ветка `invalid` судьи выбирает фразу по kind: `unspecified` — прежний текст про any-address, иначе — «which is not an address a client can call (kind)». Одинаково в обеих половинах | `D07`, `P01` |

Сверх замечаний:
- **Бюджет драйвера `T01`/`T02`.** Первый прогон `parity-mutants` дал `RED` у тождественного мутанта M01: драйвер bash не уложился в 90 с. Под нагрузкой проход по таблице занял 40–56 с (замерено отдельно), а на Windows процесс, убитый по сроку, выходит с кодом 1 молча. Бюджет поднят до 5 минут, превышение печатается отдельной фразой «the machine is slow, this is not a verdict of the rule». Иначе мутант, «убитый» тайм-аутом, выглядел бы пойманным. Прогон отброшен, мутанты перезапущены.
- Фикстуры compose-lint: `bad-llm-url-dotted-quad.yml`, `bad-llm-url-query.yml`, строки в README фикстур.

### Таблица и стенд
- `testdata/llm/local-endpoints.tsv`: 121 случай — 38 `local`, 38 `cloud`, 45 `invalid` (было 107). Ещё 2 случая стенда с пробелом.
- `T01`/`T02`: по 123 случая (121 из таблицы и 2 стенда); `T02` сверяет класс, kind и текст ошибки.

### Мутанты стенда (`make parity-mutants`)
28 мутантов, код 0: M00 убит, M01 зелёный, M02–M23 убиты. Новые:

| # | Мутант | Кто поймал |
|---|---|---|
| M24 | `llm-endpoint.sh`: точка снова снимается у IPv4, `127.0.0.1.` — loopback (R2 ревью) | `T01`, `T02` |
| M25 | `LlmEndpoint.psm1`: то же | `T02` |
| M26 | `LlmEndpoint.psm1`: `%` внутри скобок уходит в разбор IPv6 (R1 ревью) — класс и kind прежние | `T02`, только сверкой текста ошибки. Отдельный прогон на копии `scripts/` в scratch (`-run '^ZZZ$'`): `T01` PASS, `T02` FAIL «line 158: http://[fe80::1%25eth0]:8080 — the error differs» |
| M27 | `llm-endpoint.sh`: `\`, `^` и прочие символы хоста снова классифицируются, а не отвергаются | `T01`, `T02` |

Первый прогон итерации отброшен: M01 `RED` по тайм-ауту драйвера (см. «Сверх замечаний»). Повторный прогон — после поднятия бюджета и последней правки таблицы.

### Мутанты `compose-lint` (копия дерева в scratch, `t450i2-cl`, без `.env`)
Копия — `git ls-files -co --exclude-standard` по `scripts`, `testdata/compose-lint`, `testdata/llm`, `build`, `.github/ci.env`, `.env.example`, `docker-compose*`, `shared/env`. Замена — точная, якорь ровно один раз. Копия удалена по сохранённому пути.

| # | Мутант | Результат |
|---|---|---|
| C0 | без изменений (контрольный, первым) | зелёный: fixtures ok — 57 bad, 11 good |
| C3 | правило 6 пропускает всё, что не `cloud` | **красный**: `bad-llm-url-any-address`, `-no-port`, `-query` прошли линтер |
| C5 | вынос адресов в Python берёт только `MV_LLM_URL` | **красный**: четыре `bad-llm-url-*` с `MV_OLLAMA_URL` прошли |
| C6 | `llm-endpoint.sh`: точка снова снимается у IPv4 | **красный**: `bad-llm-url-dotted-quad` прошла |
| C7 | `llm-endpoint.sh`: отказ на query печатает значение целиком | **красный**: `bad-llm-url-query` отвергнута не с тем текстом |

### Прогоны итерации 2
(go1.26.8, Windows 11, Git Bash, pwsh 7.6.6, Docker Compose — только `config` внутри `compose-lint`)
- `bash -n` по `llm-endpoint.sh`, `compose-lint.sh`, `llm-server.sh`, `llm-bench.sh` — ok. `Parser::ParseFile` по `LlmEndpoint.psm1`, `llm-server.ps1`, `llm-bench.ps1` — 0 ошибок.
- `gofmt -l testdata/script-parity` — пусто; `go vet ./testdata/script-parity` — ok.
- `make scripts-parity` (после последней правки таблицы) — код 0: 101 PASS и 2 KNOWN-FAILING; `T01`/`T02` — по 123 случая; `P01` — 75 общих шаблонов; `H28`, `D07`, `D08` — PASS; порты двойника 34/34.
- `make parity-mutants` — код 0 (таблица выше).
- `bash scripts/compose-lint.sh` — `ok — 15 services in 3 file(s), 8 rules`; `--fixtures` — `57 bad, 11 good`, ok. Мутанты C0–C7 — таблица выше.
- `make secrets-scan BASE=epic/EPIC-001-foundation` — код 0, no leaks ×2. В диапазоне 0 коммитов: изменения не закоммичены, а индекс равен HEAD. Поэтому дополнительно `gitleaks dir --config=.gitleaks.toml` по 13 изменённым и новым файлам — no leaks.
- Слушатели TCP до и после прогонов — своих нет. Появился `127.0.0.1:13000` — `SpaceEngineers.exe` (`D:\SteamLibrary\…`), не из этих прогонов; исчезли два эфемерных порта чужих процессов. К `127.0.0.1:8888` запросов не было, `.env` не открывался.

### Предложения в бэклог (итерация 2)
1. T-451: Go `canonicalHost` снимает завершающую точку до `netip.ParseAddr` — привести к таблице (`127.0.0.1.` — `cloud`). Хост с `\`, пробелом, `^`, `` ` ``, `{`, `|`, `}` Go и так отвергает в `url.Parse`; строки таблицы это закрепляют.
2. N-3: топологическая проверка в `compose-lint` — при `kind=service` имя должно быть сервисом модели или явным алиасом сети. Это отдельное правило, а не копия правила адреса.
3. Скорость `T01`: `llm_parse_url` делает 5–7 fork на значение (`$(llm_trim)`, два `printf | tr`). Под нагрузкой Git Bash тратит на таблицу до минуты. Перейти на встроенные средства bash (`printf -v`, `${var,,}` под `LC_ALL=C`) — после решения, нужна ли совместимость с bash 3.2.
4. Приёмка после слияния с T-453 (из ревью #1): `infrastructure.md` §4.2 п. 4 заменить ссылкой на §6.3.1; счётчики §3.1.2 — 28 мутантов (M00–M27), 99 сценариев, проверки `T01`/`T02`.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#1) | 2026-09-13 | 0 | 1 | 5 | 5 | вернуть — M-1: IPv4 с завершающей точкой `local` вопреки решению architect#1 (`127.0.0.1.` — `cloud`), строк в таблице нет; Mi-1 строки числовых форм `134744072`/`0x08080808`; Mi-2 `\` и пробел в хосте — `cloud`, а не `invalid`; Mi-3 T01/T02 не сверяют текст ошибки (мутант R1 выжил); Mi-4 строка риска резолвера в runbook; Mi-5 блок T-450 разорвал комментарий правила 3 в compose-lint |
| 2 (code-reviewer#1) | 2026-09-13 | 0 | 0 | 1 | 2 | принять — M-1, Mi-1…Mi-5, N-1/N-2/N-4/N-5 закрыты (M24–M27 и свои R3/R4/R6 красные, `0.0.0.0.` → `cloud` совместимо с C-15 v1.3, набор символов хоста сверен с Go 1.26 `url.Parse`); Mi-R2-1 userinfo печатается в отказах query/схемы/не-ASCII, в том числе в отчёте compose-lint, комментарий `compose-lint.sh:1069` неверен; N-R2-1 `tasks.md:1002` про точку устарел; N-R2-2 счётчики §3.1.2 и §4.2 п. 4 `infrastructure.md` можно править уже сейчас — проба слияния с эпиком и T-455 без конфликтов |

## Приёмка (tech-lead)

tech-lead#1 (TEAM-1), 2026-09-13, модель Opus. **Решение: принята.**

**Итог.** Правило «локальный адрес» записано один раз: таблица `testdata/llm/local-endpoints.tsv` (121 случай) и общий судья bash/pwsh. Правило 6 `compose-lint` вызывает функцию скрипта. Ни один отказ разбора адреса больше не печатает данные пользователя из URL.

Основание — ревью #2 code-reviewer#1: «принять»; Critical 0, Major 0, Minor 1, Nit 2 (`review.md`, «T-450 · ревью #2»). Итераций ревью — 2. По решению оркестратора Mi-R2-1, N-R2-1 и N-R2-2 закрыты при приёмке, повторного ревью нет. Сверка шла по `git diff ab6cb1d` рабочей папки: коммитов в ветке нет.

**DoD** (раздел T-450 в `../tasks.md`).

| # | Пункт | Чем подтверждён при приёмке |
|---|---|---|
| 1 | Таблица ≥ 40 строк, все классы и граничные формы | 121 строка: 38 `local`, 38 `cloud`, 45 `invalid`. Формат держит стенд (`readEndpointTable`). Два случая с пробелом — в `endpointStandCases` |
| 2 | Обе реализации отвечают как таблица, `kind` совпадает, `T01`/`T02` при любом `-run` | `make scripts-parity`: `T01` и `T02` — по 123 случая, PASS. `T02` сверяет класс, `kind` и текст ошибки. Своя проба на 25 враждебных URL с userinfo: вывод двух половин побайтно одинаков (`cmp`) |
| 3 | `compose-lint` без своей копии правила; прогоны зелёные; фикстуры отвергаются по своей причине | В `compose-lint.sh` нет `ipaddress`, `is_private`, `ALLOWED_HOSTS`; осталось только упоминание в комментарии `:629`. `bash scripts/compose-lint.sh`: ok, 15 сервисов, 8 правил. `--fixtures`: ok, 58 bad, 11 good (новая фикстура `bad-llm-url-userinfo`) |
| 4 | Мутанты краснеют в обеих половинах и в фикстурах; контрольный первым | Стенд, выборка `-mutants -run '^M(00\|01\|18\|19\|2[0-8])$'`: код 0; M00 убит, M01 зелёный, M18–M28 убиты. `compose-lint` в копии дерева `t450acc-cl`: C0 (контрольный, первым) — ok, 58 bad, 11 good. C8 (`llm-endpoint.sh` снова печатает `user:pass@`) — **красный**: три `expect-text` фикстуры `bad-llm-url-userinfo` не найдены. Полный `parity-mutants` M02–M17 прогнал исполнитель в итерации 2 |
| 5 | `scripts-parity`, `parity-mutants`, `bash -n`, разбор `.ps1`, `make ci` | `make scripts-parity`: код 0, 105 сценариев, 107 PASS и 2 KNOWN-FAILING, порты двойника 34/34. `bash -n` по `llm-endpoint.sh`, `compose-lint.sh`, `llm-server.sh`, `llm-bench.sh` — ok. `Parser::ParseFile` по `LlmEndpoint.psm1`, `llm-server.ps1`, `llm-bench.ps1` — 0 ошибок. `gofmt -l` пусто, `go vet ./testdata/script-parity` — ok. `make ci BASE=develop` исполнитель прогнал в итерации 1 (код 0). В итерации 2 и при приёмке целиком не повторялся: Go-код модуля не менялся, а входящие в `ci` `scripts-parity`, `compose-lint` и `secrets-scan` прогнаны |
| — | dev-log | Записи T-450 итераций 1 и 2 в `dev-log.md` есть |

**Замечания ревью #2, закрытые при приёмке.**

- **Mi-R2-1** — userinfo в текстах отказов. Правка одинаковая в обеих половинах, оператор в оператор: `llm_parse_url` в `llm-endpoint.sh` и `Convert-LlmUrl` в `LlmEndpoint.psm1`. До первого отказа вычисляется печатаемое значение `shown`:
  - значение режется по первому `?`/`#`;
  - схема сохраняется, если в части до первого `://` нет `@`;
  - всё до последнего `@` в остатке заменяется на `…@`;
  - если `@` есть в отрезанном query или фрагменте, печатается только схема.

  Слово схемы в отказе «uses scheme» маскируется так же (`u:p@h://x`). `shown` печатают и все десять следующих отказов разбора: пароль с голым `/` (`u:x/y@h`) до проверки `@` не доходит. Комментарий `compose-lint.sh` над вердиктами переписан.

  Проверено:
  - сценарии стенда `H61`–`H66` с `Absent: FAKEPW123`: query, `ftp://`, без схемы, не-ASCII, пароль с `?`, пароль с `/`;
  - мутант M28 (`LlmEndpoint.psm1` снова печатает `user:pass@`) убит сценариями `H61`–`H64` и `H66`;
  - фикстура `bad-llm-url-userinfo.yml` с четырьмя отказами: в отчёте `compose-lint -f` по ней 0 вхождений `FAKEPW123`;
  - мутант C8 в копии дерева — красный.

  Шаблоны сообщений не изменились: `P01` — PASS, 75 общих шаблонов.
- **N-R2-1**, `tasks.md`, «Решения исполнителя». Точка снимается только у зарезервированных имён. IPv4 с точкой (`127.0.0.1.`, `0.0.0.0.`) и `ollama.` — `cloud`, по решению system-architect#1.
- **N-R2-2**, `infrastructure.md`:
  - §3.1.2 — 29 мутантов (M00–M28) и строка про M18–M28. «Состав (T-405, T-450)» — 105 сценариев (66 `health`, включая `H61`–`H66`; 8 `down`, включая `D07`/`D08`) и проверки `T01`/`T02`;
  - §4.2 п. 4 — вместо «loopback / host.docker.internal / RFC1918» ссылка на §6.3.1 и C-15 v1.3;
  - §6.3.1 — M28 и строка «userinfo в отказах» в таблице изменений поведения.
- Строка N-2 карточки («userinfo не выводилась и раньше») неверна: userinfo выводилась в четырёх отказах до проверки `@`. Строка помечена, запись итерации 2 не переписывалась.

**Для оркестратора: слияние.** Порядок: T-450, затем T-455. Моделирование — `git merge-file` на LF-копиях в scratch `t450acc-merge`, после правок приёмки:

1. T-450 (база `ab6cb1d`) в кончик эпика `cd7ec02`: `compose-lint.sh`, `infrastructure.md`, `testdata/compose-lint/README.md` — 0 конфликтов.
2. Рабочая копия T-455 (база `dcdb530`, предок кончика) поверх результата: те же три файла — 0 конфликтов. `bash -n` объединённого `compose-lint.sh` — ok.
3. `tasks.md` драйвера слияния не имеет: по одному конфликту на шагах 1 и 2. Оба — дописывание разделов в конец файла с обеих сторон. Разрешение: оставить обе стороны в порядке «эпик, T-450, T-455». `dev-log.md` и `review.md` сливает драйвер `appendtail`.

Если при настоящем слиянии `compose-lint.sh` всё же даст конфликт, его разрешает devops-engineer#2: блоки правила 6 — его.

**Концы строк.** CRLF: `LlmEndpoint.psm1`, `infrastructure.md`, `tasks.md`, `testdata/compose-lint/README.md`. Правки в них внесены скриптом точной заменой (`assert count == 1`); число CR равно числу строк. LF: `llm-endpoint.sh`, `compose-lint.sh`, `*.go`, новая фикстура и эта карточка. `git diff --check` — чисто.

**Секреты.**
- `make secrets-scan BASE=epic/EPIC-001-foundation` — код 0, no leaks ×2. В диапазоне 0 коммитов, индекс равен HEAD.
- `gitleaks dir --config=.gitleaks.toml` по копии 25 изменённых и новых файлов (`t450acc-gl`) — no leaks. Запуск из корня копии с `.gitleaksignore` ветки. Без него срабатывает известный отпечаток `infrastructure.md:generic-api-key:549`: строка не сдвинута, правки приёмки на ней не лежат.
- В новых сценариях и фикстуре пароль заведомо фиктивный — `FAKEPW123`.

**Бэклог** (строками в разделе T-450 `../tasks.md`; задачи заводит оркестратор):

1. T-451, строки DoD:
   - Go-тест `IsLocalEndpoint` читает таблицу и `endpointStandCases` (`testdata/script-parity/endpoints.go`, два случая с пробелом) и снимает `\r` с конца строки;
   - `canonicalHost` не снимает точку у IPv4: `127.0.0.1.` и `0.0.0.0.` — `cloud`;
   - `needsExplicitPort` — для любого `local`;
   - `0.0.0.0`/`::` — ошибка конфигурации, а не `ErrCloudDisabled`;
   - тексты ошибок Go не печатают userinfo: `url.Error` от `url.Parse` печатает значение целиком.
2. T-456, строки DoD (текст C-15, system-architect#1):
   - в список `invalid` добавить символы хоста, которые отвергает `url.Parse`: пробел, `\`, `^`, `` ` ``, `{`, `|`, `}`;
   - одной фразой: ни одна из трёх реализаций не печатает данные пользователя в текстах отказов, в том числе в отчёте `compose-lint`.
3. Nit, `llm-endpoint.sh:152`: комментарий `LLM_EP_RAW` («with any userinfo masked») устарел. Значение с userinfo отвергается разбором, маски нет.
4. Остаток Mi-R2-1, принят сознательно. У `http://u:x/y@h` отказ «non-numeric port 'x'» называет `x`. Для любого разбора URL, включая Go, это порт, а не данные пользователя; остальное значение скрыто.

**Ограничения.**
- Не коммитил, `git add` не делал. `.env` не открывался.
- Docker-стек, `llm-up`/`llm-down` и стенд `:8888` не трогались. `docker compose` вызывался только как `config` внутри `compose-lint`.
- Слушатели TCP до и после сверены, своих среди них нет. Новые: `127.0.0.1:60836` — `test.test.exe` из `go-build…` (чужой `go test`), и эфемерный порт процесса, которого уже нет. Исчез `13000` (SpaceEngineers).
- Scratch-копии `t450acc-cl`, `t450acc-gl`, `t450acc-merge` удалены по точным сохранённым путям. Каталог `script-parity-3558231725` стенд удалил сам.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 13:31 | 13.09 14:28 | 57m |
| Ревью #1 | 13.09 14:28 | 13.09 15:02 | 34m |
| Итерация 2 | 13.09 15:26 | 13.09 16:37 | 71m |
| Ревью #2 | 13.09 16:37 | 13.09 16:47 | 10m |
| Приёмка | 13.09 16:47 | 13.09 17:12 | 25m |
| **Всего** | | | **197m** |
