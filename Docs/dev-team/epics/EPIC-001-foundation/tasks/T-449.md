# T-449: Документы по решениям system-architect (ревизия 4, продолжение)

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / system-architect |
| Волна | 1 |
| Размер | M |
| Исполнитель | system-architect#1 (Opus) |
| Статус | done |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-13 / 2026-09-13 |
| Метка | `contract-change` (документы; код — T-053, T-448, T-450, T-451, T-209, T-250) |

## Описание и DoD
Главный источник — раздел «### T-449» в `../tasks.md`. Основание — пакет решений system-architect#1 по T-050, T-053, T-206, T-448 и бэклог приёмки T-444.

## Выполнение (system-architect)
system-architect#1 (Opus), ветка `task/T-449-docs-architect-decisions`, папка `.worktrees/T-449`. Не закоммичено: коммит делает оркестратор.

### Что внесено

| Решение | Где |
|---|---|
| T-050: каноническая форма значения (после `json.Marshal` → `Unmarshal`, три `nil`-контейнера `jsonpath` — пустыми) | КД State §3.3; C-02 (ссылка) |
| T-050: равенство элементов `append`/`remove` — в канонической JSON-форме | КД State §3.2 |
| T-050: `flee` — строка или целое, `null` — «не убегает» | `data-model.md` §3.3 |
| T-050: абзац «Код расходится» — «исправлено T-050» | C-02 «Статус в то же значение» |
| T-053: `Actor.Version`, `Actor.Kind`, `Action.At`, `Rules.FleePosition`; нормы `ChangesFor` | C-03 v1.3 (блок Go, «Поля v1.3 и нормы», «Гарантии», история) |
| T-053: позицию побега предлагает агент встречи | КД State §5.4 (строка `flee`) |
| T-053: `last_damager` — пакет встречи EPIC-003 | C-03 v1.3; ADR-012 п. 4 (пометка) |
| T-206 Mi-4: правило «локальный адрес» | ADR-005 доп. 2 п. 3; C-15 v1.3 |
| T-206 Mi-6: направление зависимостей `internal/llm` | ADR-001 доп. 2026-09-13 п. 1 |
| T-206, бэклог: `limit_money` | C-07 v1.4 |
| T-448: C-02 v1.6 | заготовка «ожидает T-448» в C-02 (в `.worktrees/T-448` карточки `tasks/T-448.md` нет, готового текста нет) |
| приёмка T-444, замечание 1 | КД шлюза §8.1: `group_move` → `cause=move` у сущности группы |
| приёмка T-444, замечание 3 | C-14 — запись истории v1.2a |
| приёмка T-444, замечание 4 | `ownership.md`, строка `test/e2e/**` — запятая |
| шаблон файлов тестов роя | ADR-001 доп. 2026-09-13 п. 3 — `**/internal/swarm/**_test.go` |

Сверка с кодом T-053 (рабочая папка `.worktrees/T-053`, не закоммичено): поля `Actor.Kind`, `Actor.Version`, `Action.At` и отказ `ChangesFor` при `Version <= 0` и пустом `At` в `types.go` и `changes.go` совпадают с текстом C-03 v1.3.

### Строки DoD для тимлидов (в `tasks.md` эпиков не внесены)

**EPIC-002 (tech-lead#1)**
- T-053: код по C-03 v1.3.
  - `Actor.Version` заполняет `ActorFromEntity`; `Actor.Kind` и `Action.At` — по контракту.
  - Отказ при `Version <= 0` у меняемого актора и при пустом `At`, где нужны `died_at` и `acquired_at`.
  - Комментарий `ChangesFor` не обещает `last_damager` и позицию побега. В рабочей папке это уже так.
- design EPIC-002, I1-4: пометка «дополнено C-03 v1.3: `encounter.npcs[].last_damager` пишет пакет встречи EPIC-003, позицию побега — вызывающий через `Rules.FleePosition`». Вносится в ветке EPIC-002.
- T-448: готовый текст C-02 v1.6 положить в карточку `tasks/T-448.md` — форма `changed[]`, обязательный `proposal_id`, запрет целых за ±2^53, повтор `/forget`. Кто заменяет заготовку «ожидает T-448» в `contracts.md`, решает оркестратор.

**EPIC-003 (tech-lead#2)**
- T-230: пакет успешного побега содержит `set position` из `Rules.FleePosition(world, region)` с `cause=flee`; тест.
- T-230: пакет удара пишет `encounter.npcs[].last_damager`, и при выброшенной попытке значение откатывается вместе с пакетом (C-05 п. 1г); тест.
- T-209: правило depguard для `**/internal/llm/{guardian,filter,parser,prompt}/**`.
  - Запрет `internal/*`, кроме своего пакета; запрет распространяется и на корень `internal/llm$`.
  - Для `guardian` дополнительно запрещён `shared/eventbus`.
  - Мутанты: «подпакет импортирует корень», «`guardian` импортирует `eventbus`».
- T-217: у стража свои `Verdict`, `Rejection`, `Status`, `EntityRef{ID, Type string}`; пакет не импортирует `internal/llm` и `shared/eventbus`.
- T-213: шлюз переводит `guardian.Verdict` в `llm.Rejection` с `*eventbus.EntityRef`; тест перевода.
- T-250: схема `llm.output.rejected`.
  - При `kind=cloud` — `budget.limit_money` (`Money`), `limit` запрещён.
  - При `background|turn` — целый `limit`.
  - `MV_LLM_CLOUD_BUDGET_USD_PER_DAY` переводится в центы; больше двух знаков после запятой — ошибка конфигурации.
  - Валидная и невалидная фикстура на каждую ветку.
- T-450 (таблица и скрипты), T-451 (Go):
  - таблица `testdata/llm/local-endpoints.tsv` — по перечню C-15 v1.3;
  - три теста паритета;
  - `compose-lint` вызывает функцию из `llm-endpoint.sh`.

### Проверки
- CRLF: у каждого изменённого файла число CR равно числу строк, байтов NUL нет. Проверка — в скрипте правки до записи и после; `sed -i` не использовался.
- КД роя не менялся: строка таблицы провайдеров — 521-я, inline `gitleaks:allow` на месте.
- `gitleaks dir --redact -c .gitleaks.toml` по изменённым файлам в копии — результат в dev-log.

### Итерация 2 (по ревью #1, 2026-09-13)

Основание: `review.md`, «T-449 · ревью #1 · 2026-09-13 · code-reviewer#2 (TEAM-1)» (0 / 3 / 6 / 5). Поручение оркестратора: C-02 v1.6 внести текстом из карточки T-448 с решением system-architect по T-448; правило «локальный адрес» — по окончательной таблице T-450 (107 случаев: 36 `local`, 32 `cloud`, 39 `invalid`).

| Замечание | Что сделано | Где |
|---|---|---|
| Ma-1 | В §5.1 внесены поля `Actor.Kind` и `Actor.Version`, поле `Action.At`, `Rules.FleePosition`; у `Stats` пояснено «ставит `Kind` (только NPC), `Version` не ставит — 0». Утверждение «совпадают дословно с C-03 v1.2» заменено на «формы, общие с C-03, совпадают с C-03 v1.3; `Attr`, `RollCheck`, `Item` — дополнения КД». Строка C-03 в §8 — v1.3 | КД State §5.1, §8 |
| Ma-2 | `Version <= 0` в тексте нормы, в комментарии поля и в истории C-03 v1.3 | `contracts.md` C-03 |
| Ma-3 | Правило «локальный адрес» переписано по таблице T-450: принцип и классы случаев, ссылка «исчерпывающе — таблица, при расхождении права таблица». Добавлено: однословное имя начинается с латинской буквы; числовые записи хоста — `cloud`; отказы формы адреса — `invalid` (нет схемы или схема не http/https, данные пользователя, пустые `?`/`#`, пустой порт, `%` в хосте, скобки не вокруг IPv6, неверный IPv6-литерал); порт и ведущие нули; any-address — только `0.0.0.0` и `::`; `::127.0.0.1` и NAT64 — не mapped-формы; точка на конце | `contracts.md` C-15 (текст, история, строка сводки); ADR-005 доп. 2 п. 3 |
| Mi-1 | Предложение про `EntityWithName` возвращено в пункт `llm.output.rejected`, из пункта «Лимит бюджета» убрано | `contracts.md` C-07 |
| Mi-2 | No-op в `changed[]`: «путь был и до, и после (или не был ни до, ни после), и каноническая форма значения не изменилась» вместо `reflect.DeepEqual` | КД State §3.2 |
| Mi-3 | У C-02 в заголовке и в истории — v1.5a (каноническая форма, «исправлено T-050») и v1.6; сводка v0.12 — пометка v1.5a | `contracts.md` C-02, сводка |
| Mi-4 | «со здоровьем после предыдущего удара и **той же** версией; изменения одной сущности из нескольких вызовов `ChangesFor` сливаются в один набор (C-02 v1.3)» | `contracts.md` C-03 |
| Mi-5 | Исправлено здесь, а тест — строкой DoD T-353 (ниже). Строка §8.1 разнесена на две. Факт игрока с `cause=move` в групповом scope не ставится в доставку; факт позиции сущности `group` адресуется всем `alive` участникам, по одной доставке на каждого. Признак — scope игрока, а не поиск факта группы по `proposal_id`, потому что факты пакета приходят по одному | КД шлюза §8.1 |
| Mi-6 | «у NPC `kind` обязателен — без него ошибка, как без `hp`/`def`; `kind` без таблицы трофеев законен» | `contracts.md` C-03 |
| N-1 | Перечень случаев в C-15 и ADR-005 — один и тот же текст (вступление, пять пунктов, обоснование). «Совместимость» в ADR-005 перечисляет всё новое `local` относительно T-206: однословные имена, `*.localhost` и `localhost.`, link-local, ULA; и все ужесточения | C-15, ADR-005 |
| N-2 | «без прямого импорта `shared/eventbus`; транзитивно через `shared/entity` он остаётся» | ADR-001 доп. п. 1 |
| N-3 | Запись v1.4 «Код расходится» восстановлена дословно (прежнее время глаголов) под зачёркнутым заголовком; «Исправлено T-050 (v1.5a)» — отдельным предложением перед ней. Пример «второй `/forget`» зачёркнут, рядом — пометка v1.6 | `contracts.md` C-02 |
| N-4 | Для T-250: в ветке `kind=cloud` схема сужает `Money` — `currency` `const: "USD"`, `amount` `minimum: 0` | `contracts.md` C-07 (текст, история) |
| N-5 | Граница — «по модулю от 2^53, допустимо ±(2^53−1)» (решение system-architect по T-448, п. 4) | КД State §3.3; C-02 v1.6 |
| C-02 v1.6 | Заготовка «ожидает T-448» заменена текстом из карточки T-448 (пп. 2–9) с правками решения system-architect: граница 2^53−1; `attributes` создания; `proposal_id` обязателен и в `entity.created`; предложение без него — `Warn` и пропуск; «`a[n]` за концом списка — повреждённый факт»; замена промежуточного узла. Пометка «вступает в силу со слиянием T-448» — в заголовке, у каждого пункта v1.6, в истории и в сводке. Реестр потребителей §17 не менялся (п. 10 карточки T-448 — по желанию) | `contracts.md` C-02 |

**Решения по ходу итерации 2.**
- **Mi-5 — в T-449, а не только строкой DoD.** Двойную доставку внесла сама итерация 1 T-449, когда переписала строку `group_move`. Текст правится там же, где появился дефект, а T-353 получает тест.
- **Уточнения T-450 (вопросы devops-engineer#2).** Подтверждены: однословное имя с буквы, числовые записи — `cloud`; записанный порт явный, ведущие нули незначимы, любой `local`-хост без порта — `invalid`; классы отказа формы; any-address только `0.0.0.0` и `::`, `::127.0.0.1` и NAT64 — не mapped-формы; `localhost.` — `local`, `ollama.` — `cloud`. **Изменено одно:** точка на конце у IPv4-адреса (`127.0.0.1.`) не снимается, ответ — `cloud`. Клиент Go (`netip.ParseAddr`) такую запись адресом не считает и ищет в DNS, а сомнительное — облако. В таблице T-450 строки для этого случая нет, шапка таблицы и функции скриптов снимают точку и у адресов. Строка для devops-engineer#2 — в отчёте system-architect оркестратору; текст C-15 и ADR-005 уже в новой редакции.
- **КД State в двух эпиках.** T-448 (ветка EPIC-002) и T-449 (ветка EPIC-001) правят соседние строки §3.2 (таблица ops, строка no-op) и §8 (строки C-02 и C-03). При слиянии эпиков будет текстовый конфликт, разрешение — объединение правок: формулировки T-448 для C-02, T-449 для no-op и C-03.

**Строки DoD для тимлидов (итерация 2; в `tasks.md` эпиков не внесены).**
- **EPIC-004 (tech-lead#3), T-353:** в описании `entity.updated cause=group_move` заменить на «`entity.updated` сущности `group` с `cause=move` — всем `alive` участникам; факт игрока с `cause=move` в групповом scope не доставляется» (КД шлюза §8.1). Unit адресности: пакет перемещения группы из трёх `alive` участников (4 факта `cause=move`) даёт ровно одну доставку `mechanics` на участника.
- **EPIC-002 (tech-lead#1), T-448, итерация 2:** по разделу «Решение system-architect#1» карточки T-448. Строка итерации 1 «положить готовый текст C-02 v1.6 в карточку» выполнена.
- **EPIC-003 (tech-lead#2), T-451:** Go-тест `IsLocalEndpoint` по таблице T-450, включая строки для IPv4-адреса с точкой на конце (`cloud`) после их добавления в T-450; ошибки формы (`invalid`) — через `parseEndpoint`, одним ответом с таблицей.
- **EPIC-003, T-250:** в ветке `kind=cloud` — `currency` `const: "USD"`, `amount` `minimum: 0`; невалидные фикстуры на `currency: "EUR"` и отрицательную сумму.

**Проверки итерации 2** — в dev-log, запись `<!-- dev-log T-449 iteration 2 -->`.

## Ревью (code-reviewer)
| Итерация | Дата | Ревьюер | Вердикт | Critical / Major / Minor / Nit | Где |
|---|---|---|---|---|---|
| 1 | 2026-09-13 | code-reviewer#2 (TEAM-1) | вернуть | 0 / 3 / 6 / 5 | `review.md`, «T-449 · ревью #1»: Ma-1 КД State §5.1 не приведён к C-03 v1.3; Ma-2 `Version == 0` против `<= 0` в коде и DoD п. 3; Ma-3 «однословное имя» пропускает числовые записи хоста в `local`, отказы формы URL не названы `invalid` (C-15 v1.3, ADR-005) |
| 2 | 2026-09-13 | code-reviewer#3 (TEAM-1) | принять | 0 / 0 / 2 / 6 | `review.md`, «T-449 · ревью #2»: Ma-1…Ma-3, Mi-1…Mi-6, N-1…N-5 закрыты; R2-Mi-1 — у норм v1.6 в «Вход/Выход State» и «Гарантиях» C-02 нет пометки «вступает в силу со слиянием T-448»; R2-Mi-2 — порядок элемента предка (решение по T-448 п. 3) не внесён в правило `changed[]` |

## Приёмка (tech-lead)

tech-lead#1 (TEAM-1), 2026-09-13, модель Opus. **Решение: принята.**

**Итог.** Пакет решений system-architect#1 по T-050, T-053, T-206 и T-448 и бэклог приёмки T-444 записаны в документах: `contracts.md` v0.12, КД State, КД шлюза, `data-model.md`, ADR-001, ADR-005, ADR-012, `ownership.md`.

Основание — ревью #2 code-reviewer#3: «принять», Critical 0, Major 0, Minor 2, Nit 6 (`review.md`, «T-449 · ревью #2»). Ревью #1 (code-reviewer#2, 0/3/6/5) закрыто итерацией 2. Итераций ревью — 2. Приёмка возобновлена после прерывания лимитом API. Частичных записей прежнего запуска в рабочей папке не было: `tasks.md` и карточка стояли в `review`, секция «Приёмка» — «Не принята».

Сравнивал `git diff` рабочей копии с базой `bde9348`: коммитов в ветке нет. Изменены 11 файлов, новый файл — эта карточка.

### DoD (раздел T-449 в `../tasks.md`)

| # | Пункт | Итог | Проверка |
|---|---|---|---|
| 1 | Версия в заголовке и запись в истории у C-03 v1.3, C-07 v1.4, C-14 v1.2a, C-15 v1.3; сводка v0.12 с колонкой «Кто прав» | выполнен | Заголовки и истории на месте. Сводка — `contracts.md:9-24`, колонка «Кто прав» есть. У C-02 — v1.5a и v1.6 в заголовке и истории (Mi-3 ревью #1) |
| 2 | Нет готового текста в карточке T-448 — C-02 v1.6 остаётся заготовкой | выполнен в изменённой постановке | Поручение оркестратора на итерацию 2: внести текст из карточки T-448 вместе с решением system-architect. Внесено с пометкой «вступает в силу со слиянием T-448» в заголовке, у новых пунктов, в истории и в сводке. Пропуски пометки в трёх местах — R2-Mi-1, переданы в T-456 |
| 3 | C-03 v1.3 совпадает с кодом T-053 | выполнен | T-053 слита в `epic/EPIC-002-state-mechanics` (`24f1baf`). Сверено через `git show`: `Actor.Kind` (`types.go:53`), `Actor.Version int64` (`:60`), `Action.At time.Time` (`:140`); `if who.Version <= 0` (`changes.go:209`); `a.At.IsZero()` (`:225`); `Version: e.Version` (`actor.go:30`); `FleePosition(worldID, regionID string) string` (`rules.go:717`). Текст C-03: `Version <= 0` в норме, в комментарии поля и в истории |
| 4 | Перечень случаев «локальный адрес» в C-15 и ADR-005 совпадает дословно | выполнен | Скриптом: пять строк перечня (`contracts.md:753-757`, ADR-005 `:69-73`) совпадают побайтно. Заключительный абзац совпадает на первых 1085 символах, у C-15 добавлен хвост «Решение и совместимость — ADR-005 доп. 2 п. 3.». Различаются только вводные заголовки абзацев |
| 5 | CRLF, NUL, строка КД роя, gitleaks | выполнен | У 12 файлов число CRLF равно числу строк, конец `\r\n`, NUL — 0. `git diff --quiet bde9348 -- swarm-llm-laws.md` — без изменений, в строке 521 `gitleaks:allow`. `gitleaks dir --redact -c .gitleaks.toml` по копии 12 файлов в scratch: с `.gitleaks.toml`/`.gitleaksignore` ветки — 0; с конфигурацией кончика эпика `dcdb530` (T-453 правил `.gitleaksignore`) — 0; без `.gitleaksignore` — 0. Копия удалена по точному пути. `make secrets-scan BASE=epic/EPIC-001-foundation` — rc=0: 0 коммитов в диапазоне, индекс 14,5 МБ без находок. Незакоммиченных правок он не видит, их покрывает скан `gitleaks dir` |
| — | Общие пункты: dev-log, ревью | выполнен | Записи dev-log `<!-- dev-log T-449 -->` и `<!-- dev-log T-449 iteration 2 -->` на месте; оба раздела ревью — в `review.md` |

### Замечания ревью #2 — решение оркестратора

Не правились при приёмке: `contracts.md` правит только system-architect, КД и ADR тимлид не правит. Все замечания переданы в состав T-456, раздел создан в конце `../tasks.md`:
- R2-Mi-1 — пометка «вступает в силу со слиянием T-448» у норм v1.6 в «Вход State», «Выход State», «Гарантиях» (`contracts.md:322`, `:323`, `:356`). Не блокирует: заголовок C-02 объявляет v1.6 не действующей до слияния T-448.
- R2-Mi-2 — порядок элемента предка. В текст v1.6 карточки T-448 он уже внесён итерацией 2 (`T-448.md`, строка «R2-Mi-2 ревью T-449»; мутант K11 красный). В `contracts.md` попадёт при сверке C-02 v1.6 с итоговой карточкой в T-456.
- R2-N-1…R2-N-6 — пунктом 7 состава T-456. Строка DoD T-450 про `127.0.0.1.` (R2-N-6) — пунктом 8. По существу она уже в работе: Ma-1 ревью #1 T-450.

### Строки DoD для тимлидов других эпиков

Файлы других эпиков не правил. Строки — из карточки («Строки DoD для тимлидов», итерации 1 и 2), передаются через оркестратора:
- **Тимлид EPIC-002:** T-053 — код по C-03 v1.3 (в слитом коде выполнено, сверено выше); `design.md` I1-4 — пометка «дополнено C-03 v1.3»; T-448 — итерация 2 по решению system-architect.
- **tech-lead#2 (EPIC-003):**
  - T-230 — `set position` из `Rules.FleePosition` с `cause=flee`; `last_damager` откатывается вместе с пакетом;
  - T-209 — depguard `internal/llm/{guardian,filter,parser,prompt}` с мутантами;
  - T-217 — свои типы стража;
  - T-213 — перевод `guardian.Verdict` в `llm.Rejection`;
  - T-250 — `limit_money`, `currency` `const: "USD"`, `amount` `minimum: 0`, центы из `MV_LLM_CLOUD_BUDGET_USD_PER_DAY`, фикстуры;
  - T-451 — Go-тест по таблице T-450, включая `127.0.0.1.` → `cloud`, `invalid` через `parseEndpoint`.
- **tech-lead#3 (EPIC-004):** T-353 — адресация `entity.updated` сущности `group` с `cause=move`; unit на ровно одну доставку на участника.
- **EPIC-001 (своя):** T-450 — таблица по C-15 v1.3, паритет, `compose-lint` через функцию `llm-endpoint.sh`; `127.0.0.1.` → `cloud` (T-456 п. 8, Ma-1 ревью T-450).

### Совместимость с эпиком

`git diff --stat bde9348 epic/EPIC-001-foundation` (кончик `dcdb530`, T-453 и файлы оркестратора): 13 файлов. С T-449 пересекаются три, все дописыванием в конец:
- `dev-log.md` и `review.md` — драйвер `appendtail`, сольются сами;
- `tasks.md` — драйвера нет, оба ответвления дописали хвост после строки 1069, ожидается текстовый конфликт. Разрешение — объединение: раздел T-453 из эпика, затем разделы T-449 и T-456 из ветки. Ни одна сторона целиком (`--ours`/`--theirs`) не подходит.

`contracts.md`, КД, ADR, `data-model.md`, `ownership.md` в эпике после `bde9348` не менялись. `.gitleaksignore` (T-453) изменён только в эпике, скан с его версией — 0.

Межэпиковый конфликт КД State §3.2 и §8 с T-448 (ветка EPIC-002) записан исполнителем. Разрешение — объединение правок: формулировки T-448 для C-02, T-449 — для no-op и C-03. Его разрешает интеграция эпиков, не эта задача.

### Не делал

`git add`/`commit`, push, merge, docker, стенд `:8888`, `.env`. `contracts.md`, КД, ADR не правил. «Учёт времени» не заполнял — это делает оркестратор.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 13:49 | 13.09 14:00 | 11m |
| Ревью #1 | 13.09 14:00 | 13.09 14:16 | 16m |
| Итерация 2 | 13.09 14:16 | 13.09 14:45 | 29m |
| Ревью #2 | 13.09 14:45 | 13.09 15:01 | 16m |
| Приёмка (прервана лимитом API) | 13.09 15:01 | 13.09 15:22 | 21m |
| Приёмка | 13.09 15:26 | 13.09 15:34 | 8m |
| **Всего** | | | **101m** |
