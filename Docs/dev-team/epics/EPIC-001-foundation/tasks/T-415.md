# T-415: Обходные и беззвучные пути: recover в StartAll, ошибка публикации у заглушки встречи, хук go-fmt вне модуля

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / TEAM-1 |
| Волна | 1 (бэклог) |
| Размер | S |
| Статус | done (принята tech-lead#1, правки EPIC-003 подтверждены tech-lead#2) |

## Описание и DoD
- **Причина (ревью T-410, T-220)**: три мелких пути, где нарушение не видно.
  - `runtime.StartAll` не делает `recover`: паника в `Start` контекста закрывает шину раньше, чем остановлены уже запущенные контексты (обход порядка ADR-023). Нужно: паника → ошибка старта, запущенные останавливаются, шина закрывается после них; тест ленты событий.
  - `shared/testkit/swarm/fake_encounter.go`: неудачная публикация не оставляет следа — хотя бы `log.Error` и ошибка в `Err()` (у нарратора это закрыто итерацией 2 T-220). Правка в принятом файле EPIC-003 — через tech-lead#2.
  - Хук проекта `.claude/hooks/go-fmt.ps1` выдаёт ошибку `go vet` («go.mod not found») на `.go`-файлы вне модуля (временные зонды агентов): пропускать файлы вне корня модуля.
  - Дымовой тест сигналов под Windows (ревью #2 T-410): мутант «`serve` в обход обёртки сигналов» ловит только живой процесс с двойным CTRL_BREAK.
  - Правила depguard `shared-testkit-mechanics` и `shared-testkit-swarm` допускают `internal/mechanics` без `$` — та же дыра по префиксу, что закрыта в T-410 для `membus`; мутант с пакетом `internal/mechanicsx` должен краснеть.
  - Мутант M7c ревью T-410 (подмена конфигурации в строке `eventbus.NewKafka(cfg)`) выживает: две строки `reflect` в `TestOpenBusBuildsTheTransportTheValueNames` либо интеграционный тест `openBus(kafka)` вместе с T-394.
  - Проба e2e не замечает, что дочерний процесс уже вышел, и ждёт 60 с до срока (ревью #1 и #2 T-255, п. 2): проба должна проверять, жив ли процесс, и падать сразу с его кодом выхода.
  - ~~**Вопрос системному архитектору (ревью #1 T-417)**: нужен ли `recover` в `eventbus.Delivery`?~~ **решено T-425: C-01 v1.5 вводит перехват, код — T-426**; пункт DoD этой задачи «мутант на `recover` краснеет» относится к `runtime.StartAll`. Прежний текст: Сейчас паника любого обработчика роняет весь процесс ADR-001; путь по данным есть — `Derive(…, WithCauseID(…))` от конверта без id при `MV_BUS_VALIDATE_ON_READ=false`. Решение — изменение C-01; код — только после решения архитектора.
  - **Вопрос системному архитектору (T-426)**: при сбое записи в dead_letters после паники вернувшуюся ошибку нельзя опознать через errors.Is(err, ErrHandlerPanic) — причина включена через %v; переход на %w меняет цепочку ошибок у всех запаркованных событий, то есть contract-change. Нужно ли это вызывающему — решение архитектора.
  - **(ревью T-426)** К вопросу о %w: простая замена (cause: %v) на %w опасна — stopped в kafka.go считает остановкой любой io.EOF в цепочке ошибки, и если ошибка обработчика оборачивает io.EOF, сбой записи в dead_letters молча закончил бы Subscribe с nil; безопаснее заворачивать только ErrHandlerPanic.
  - **(ревью T-426)** runtime.Goexit в обработчике под membus.Subscribe (например, t.FailNow в тесте) оставляет блокировку группы захваченной, и группа зависает — старое поведение; освобождать блокировку отложенно.
  - **(ревью #2 T-426)** Предостережение про %w стало шире: тип panicError с многозвенным Unwrap делает значение паники видимым для errors.Is; с %w для причины паника значением io.EOF при сбое записи в dead_letters заставит kafka-Subscribe молча вернуть nil при живом контексте (зонд подтвердил). Варианты: оборачивать через %w только ErrHandlerPanic или сузить stopped; нужен тест с паникой значением io.EOF при сбое записи. Ветка DLQ == nil в Deliver оборачивает причину через %w — сейчас недостижима (обе шины всегда задают DLQ), но при изменении действует то же.
- **DoD**: на каждый пункт тест или воспроизводимая проверка; мутант на `recover` краснеет.
- **Исполнитель**: developer. Низкий приоритет, бэклог волны 1.

## Выполнение (developer)
developer#1 (Opus), TEAM-1, ветка `task/T-415-silent-paths`, папка `.worktrees/T-415`. Не закоммичено (коммит — оркестратор после ревью и приёмки).

### Итог по пунктам

| # | Пункт | Что сделано | Чем проверено | Мутант → результат |
|---|---|---|---|---|
| 1 | `recover` в `runtime.StartAll` | `start()` монтирует маршруты и зовёт `Start` под `defer recover`: паника в `Routes`/`Start` превращается в ошибку `start context <имя>: panic: <значение>`, уже запущенные контексты останавливаются в обратном порядке, стек пишется в `Deps.Log` уровнем `Error`. В тексте ошибки стека нет, потому что ошибка уходит одной строкой в stderr. Значение печатает `panicText` с защитой от вложенной паники форматирования: тот же приём, что `eventbus.panicText` (ревью #1 T-426, Mi-1). Комментарий `serve.go` у `run` про панику приведён к коду | `TestStartAllTurnsAPanicIntoAFailedStart`: строка, ошибка, `nil`, паника в `Routes`, непечатаемое значение; `TestStartAllTurnsAPanicIntoAnErrorWithoutALogger`; лента `TestServeClosesTheBusLastWhenTheStartFails/a_context_panics_in_Start` = `start first\|stop first\|bus closed` | MR (`r := recover()` → `var r any`): **красный**, в `runtime` «StartAll let the panic out», в `cmd/multiverse` пакет падает паникой |
| 2 | `FakeEncounter`: след неудачной публикации | `refusedPublication`: ошибка в лог (`Error`, «publication refused by the bus», поля `type`, `event_id`, `cause_id`, `err`) и в `Err()`. Путь один для `publish` и обеих веток `announce`. Ошибки стаба — первая ошибка подписки или публикации — лежат под своим `failMu`: публикация падает под `mu`, а `Err` не должен ждать хода. Здоровье `FakeContext` уже читает `Err()`, поэтому отказ виден как `fail` на `/health` | `fake_encounter_failure_test.go`: отказ `combat.decided` (Act) и отказ `encounter.started` (announce по факту State). Там же закреплено, что повтор шины отвечает `nil`, а след остаётся | ME-err (без `e.fail`): **красный**; ME-log (без строки лога): **красный** |
| 3 | Дымовой тест сигналов | `test/e2e/lifecycle_test.go` `TestTheProcessStopsCleanlyOnASignal`: бинарник в своей группе процессов (`CREATE_NEW_PROCESS_GROUP`), после `/health` отправляется CTRL_BREAK только в эту группу (`GenerateConsoleCtrlEvent`, `signal_windows_test.go`); на остальных ОС — SIGTERM (`signal_other_test.go`). Ждём код 0, строку старта в логе и свободный порт. Если консоли нет (Windows без консоли), тест делает `t.Skip` и пишет, как запустить из терминала | e2e зелёный, тест реально выполнился (PASS, не SKIP) | R5b (`serve` зовёт `run` без `withSignals`): юнит-тесты `cmd/multiverse` **зелёные** (так и было), e2e **красный**: `exit code 3221225786 (0xc000013a) … the process died of it instead of stopping` |
| 4 | depguard, дыра по префиксу | `$` в `allow` правил `shared-testkit-mechanics` и `shared-testkit-swarm`, у каждого комментарий «почему» | исходное дерево — 0 issues | ML (пакет `internal/mechanicsx`, импорт из обоих двойников): **красный**, 2 × depguard с `desc` своего правила; контроль (тот же мутант, `$` снят): 0 issues |
| 5 | M7c (подмена конфигурации перед `NewKafka`) | `TestOpenBusHandsTheKafkaAdapterTheValidationFlag`: `openBus(kafka)` для `""/false/true`, поле `skipValidateOnRead` читается через `reflect`; если поле переименуют, тест упадёт с явным текстом. Проверка с брокером — T-394. Тест вынесен отдельной функцией рядом с `TestOpenBusBuildsTheTransportTheValueNames`, а не двумя строками внутри неё, из-за подкейсов по значению переменной | `cmd/multiverse` зелёный | M7c (`cfg.SkipValidateOnRead = false` перед `NewKafka`): **красный**, `value "false": … = false, want true` |
| 6 | Проба e2e замечает выход процесса | `launch` запускает процесс и следит за ним с момента старта: `Wait` в горутине, канал `exited`. `waitHealthy` в `select` опроса ловит выход и сразу возвращает ошибку с `*exec.ExitError`. `probe` — обёртка с `t.Fatal`. Уборка ждёт `exited`, а не зовёт `Wait` второй раз | `TestTheProbeGivesUpOnAProcessThatExited` (`--contexts=state --bus=memory` → код 2): ≈3 с вместе со сборкой, код 2 и причина в выводе | MP (ветка `exited` удалена): **красный** через 15 с бюджета теста («no answer … want the exit of the process»); с исходной пробой ожидание было бы 60 с |
| 7a | `runtime.Goexit` под `membus.Subscribe` | Один шаг доставки вынесен в `deliverNext`: блокировка курсора группы берётся и снимается через `defer`. Курсор при `Goexit` не сдвигается, событие получает следующая подписка группы (at-least-once). Публичный API `shared/eventbus` не менялся: правка внутри `membus`, неэкспортируемая | `TestAGoexitInAHandlerLeavesTheGroupUsable` | MG (`membus.go` из HEAD): **красный** через 5 с: «the group is stuck» |
| 7b | `%w` для причины при сбое записи в `dead_letters` | **Не меняется.** `%w` для причины сейчас никому не нужен: ошибку `Deliver` разбирает только `stopped` в `kafka.go`. А с `%w` паника значением `io.EOF`/`io.ErrClosedPipe` или ошибка, их оборачивающая, при сбое записи дала бы `stopped=true`, и kafka-`Subscribe` молча вернул бы `nil` при живом контексте. Если различать панику всё же понадобится, заворачивать только `ErrHandlerPanic`. Это изменение цепочки ошибок, то есть contract-change, решает системный архитектор (вопрос пункта T-426 выше). Поставлен сторожевой тест | `shared/eventbus/panic_stop_test.go` `TestAFailedDeadLetterIsNeverTakenForTheEndOfTheReader`: паника `io.EOF`, `io.ErrClosedPipe`, обёрнутый `EOF`, возвращённый обёрнутый `EOF` при сбое записи → `stopped(живой ctx, err) == false` | MW (`(cause: %v)` → `(cause: %w)`): **красный** на всех четырёх подкейсах |
| — | Хук `.claude/hooks/go-fmt.ps1` | **Исключён из T-415 оркестратором**, файл не открывался и не правился. Что нужно владельцу — ниже | — | — |

Контрольный мутант M0 (синтаксическая ошибка в `lifecycle.go`) — красный (сборка). Мутанты выполнялись в копии рабочей папки (`git ls-files -co` → scratch), не через `-overlay`. После каждого мутанта файлы восстановлены из эталонной копии, итоговый `diff -rq` пуст, базовый прогон копии зелёный. Рабочая папка задачи мутантами не затрагивалась.

### Изменённые файлы
- `shared/runtime/lifecycle.go`: `start` с `recover`, `panicText`; `shared/runtime/runtime_test.go`: два теста.
- `cmd/multiverse/serve.go`: только комментарий у `run`; `cmd/multiverse/serve_test.go`: `witness.startPanic`, кейс ленты, тест M7c.
- `shared/testkit/swarm/fake_encounter.go`: `failMu`/`failure`, `fail`, `refusedPublication`, `Err`/`Wait` — doc; новый `shared/testkit/swarm/fake_encounter_failure_test.go`.
- `.golangci.yml`: `$` в двух правилах и комментарии.
- `shared/eventbus/membus/membus.go`: `deliverNext`; новый `shared/eventbus/membus/goexit_test.go`.
- новый `shared/eventbus/panic_stop_test.go`.
- `test/e2e/empty_world_test.go`: `emptyWorldEnv`, `launch`, `process`, `waitHealthy`, `probe(…, p)`; новые `test/e2e/lifecycle_test.go`, `test/e2e/signal_windows_test.go`, `test/e2e/signal_other_test.go`.

### Прогоны (go1.26.8 windows/amd64, golangci-lint 2.13.2)
- `go build ./... && go vet ./...` и `go vet -tags e2e ./test/...`: 0.
- `go test -short -race …`: **`-race` недоступен** на машине («-race requires cgo»), прогоны без него.
- `go test -short -count=1 ./...`: все пакеты ok, кроме одного падения `shared/testkit/swarm` `TestTheFactsOfStateMoveTheVersionTheStubProposesAgainst` (index out of range [-1]). Это **давний флак, не T-415**: на выгрузке HEAD без моих правок он же падает при `-count=30`, в моей папке — 30/30 ok, повторный прогон пакета зелёный. Причина — в самом тесте (`fake_encounter_test.go:853`): `settled` бывает `true` при пустом `proposals`, потому что решение публикуется раньше предложения, и `proposals[len(proposals)-1]` падает. Файл EPIC-003 — в бэклог.
- `go test -tags e2e -count=1 ./test/...`: `test/e2e` ok (≈13 с), `test/fixtures` ok.
- `golangci-lint run ./...`: 0 issues.

### Отклонения от задачи
- Пункт 2 сделан ровно в объёме «хотя бы»: `log.Error` + `Err()`. Повтор шины по-прежнему отвечает `nil`, потому что действие запоминается (`acted.Seen`) до публикации, поэтому письма в `dead_letters` нет. Позднее запоминание, как у нарратора (`Has`/`Add`), здесь не минимальная правка: при повторе полуопубликованный ход бросил бы кости и опубликовал решения второй раз под новыми id. Это решение EPIC-003 (реальный агент, T-229). **Правку в принятом файле EPIC-003 должен подтвердить tech-lead#2.**
- `Err()` теперь «первая ошибка стаба» (подписка или публикация) и липкая: после отказа публикации `/health` контекста `swarm` остаётся `fail` до перезапуска, даже если отказ был разовым. Для заглушки это честно: пакет мог потеряться. `Wait()` и `FakeContext.Stop` возвращают эту же ошибку.
- `shared/runtime` в `ownership.md` — общий код системного архитектора. Правка по поручению T-415 не добавляет экспортируемых имён и не меняет сигнатур (`StartAll` та же). Меняется поведение: паника больше не выходит наружу.

### Что нужно владельцу хука `.claude/hooks/go-fmt.ps1` (пункт исключён из T-415)
Симптом из задачи: на `.go`-файл вне модуля (временные зонды агентов в `%TEMP%`/scratch) хук вызывает `go vet` и получает «go.mod not found». Нужно:
1. Перед `go vet` искать корень модуля, поднимаясь от каталога файла до `go.mod`. Если `go.mod` не найден, `go vet` не запускать и выйти с 0 без сообщения об ошибке. `gofmt` для такого файла можно оставить: ему модуль не нужен.
2. `go vet` запускать из найденного корня (`Push-Location`) по пакету файла, а не по пути вне модуля.
3. Файлы под путями, исключёнными из форматирования в `.golangci.yml` (`.claude/`, `build/.legacy-src`, `services/`), пропускать целиком, как это уже решено 2026-09-11 для форматтеров.
Проверка владельцу: зонд `probe.go` в scratch → хук молчит; файл в `shared/` с нарушением vet → хук сообщает.

### Предложения в бэклог
1. EPIC-003 (tech-lead#2): флак `TestTheFactsOfStateMoveTheVersionTheStubProposesAgainst` — в условии ожидания проверять `len(proposals) > 0` до `proposals[len(proposals)-1]`.
2. EPIC-003: `FakeEncounter` — позднее запоминание действия (`Has`/`Add`) вместе с идемпотентностью полуопубликованного хода, чтобы отказ публикации заканчивался письмом в `dead_letters`, а не только следом.
3. `.golangci.yml`: правила `internal-state` и `internal-swarm` тоже держат `allow: internal/mechanics` без `$` — та же дыра по префиксу. Контекстам подпакеты механики, возможно, нужны, тогда пара `internal/mechanics$` + `internal/mechanics/`. Не проверялось мутантом, в T-415 не входило.
4. Системному архитектору (к вопросу T-426 выше): `%w` только для `ErrHandlerPanic`, если различение паники понадобится. Сторож `panic_stop_test.go` уже стоит.

### Итерация 2 (по ревью #1 code-reviewer#2, решения оркестратора)

| # | Замечание | Что сделано | Чем проверено | Мутант → результат |
|---|---|---|---|---|
| 1 | Mi-1: остановка записывалась как отказ шины | `refusedPublication(ctx, …)`: при `ctx.Err() != nil` (остановка по ADR-023 п. 4) `e.fail` не вызывается, в лог идёт `Info` «publication interrupted by the stop», ошибка возвращается обработчику как раньше. `Err()`/`Wait()`/`FakeContext.Stop` на чистой остановке — `nil`, как обещают их doc-комментарии | `TestAPublicationCutShortByTheStopLeavesNoTrace`: подкейс `Act` и подкейс `announce` при отменённом контексте → ошибка отмены, `Err() == nil`, в логе нет `ERROR`, есть строка про остановку | MI (ветка `ctx.Err()` удалена): **красный**, оба подкейса: липкий `Err` и `ERROR` в логе |
| 2 | N-1: ветка `encounter.ended` в `announce` без теста | `TestARefusedEndLeavesATrace`: бой до закрывающего пакета, отказ `encounter.ended` на его факте | `shared/testkit/swarm` зелёный | MA ревьюера (в ветке `ended` прежний `fmt.Errorf`): **красный** |
| 3 | N-2: смена типа поля в тесте M7c | перед `Bool()` проверка `field.Kind() != reflect.Bool` → `t.Fatalf` с советом | `cmd/multiverse` зелёный | поле `skipValidateOnRead` сделано `int`: **красный** с текстом «is a int now, not a bool: follow the change here», без паники `reflect` |
| 4 | N-3: сигнал мёртвому процессу | перед `interrupt` `select` по `proc.exited` → `t.Fatalf` с ошибкой процесса | e2e зелёный, сигнальный тест выполнен (PASS) | — |
| 5 | N-4: префикс у контекстов | `$` у `internal-state` (`internal/mechanics$`) и `internal-swarm` (`internal/mechanics$`, `internal/laws$`, `internal/llm$`), комментарий «почему» у `internal-state` | исходное дерево — 0 issues | MS: `internal/mechanicsx` из `internal/state` → **красный** (depguard `internal-state`), контроль без `$` — 0 issues. MW: `internal/lawsx`, `internal/llmx`, `internal/mechanicsx` из `internal/swarm` → **красный**, 3 × depguard `internal-swarm`, контроль без `$` — 0 issues |
| 6 | флак `TestTheFactsOfStateMoveTheVersionTheStubProposesAgainst` (EPIC-003) | Поручение — `len(proposals) > 0`. Сделано строже: `len(proposals) > proposedBefore`, число предложений снимается до удара. Проверка «список не пуст» закрыла панику, но не гонку: при 6×40 с ней 1 серия упала по сроку (`timed out waiting for the exchange to be decided and applied`, 3 с). Причина — та же гонка, вторая её сторона. Решение убийственного обмена уже опубликовано, а предложение ещё нет, в списке только прошлые (применённые) предложения. Условие срабатывает рано, тест не ждёт `encounter.ended`, следующий удар уходит в закрывающийся бой. Он откладывается, после конца боя получает молчание (так задумано), и нового решения нет. Условие «появилось предложение этого обмена» закрывает обе стороны | HEAD без правки: 2 из 3 серий по 40 падают (`index out of range [-1]`). Вариант только с `len > 0`: 1 из 6 серий по 40 падает по сроку. Итоговая правка: **0 из 8 серий по 40** (320 прогонов) | — |

**Tech-lead#2 (EPIC-003) подтверждает обе правки в принятых файлах:** `shared/testkit/swarm/fake_encounter.go` (след отказа публикации и исключение остановки) и `shared/testkit/swarm/fake_encounter_test.go` (условие ожидания флакового теста). Новый файл `fake_encounter_failure_test.go` — тоже в пакете EPIC-003.

Мутанты итерации 2 — в копии рабочей папки. Первым шёл контрольный M0 (синтаксис в `fake_encounter.go`) — красный. Файлы после каждого мутанта восстановлены, `diff -rq` пуст, базовый прогон зелёный, копия удалена.

Прогоны итерации 2 (go1.26.8 windows/amd64, golangci-lint 2.13.2): `go build ./... && go vet ./... && go vet -tags e2e ./test/...` — 0; `go test -short -count=1 ./...` — все пакеты ok (без `-race`: нет cgo); `go test -tags e2e -count=1 ./test/...` — `test/e2e` ok, `test/fixtures` ok; `golangci-lint run ./...` — 0 issues (повторён после последней правки теста).

Бэклог ревью, п. 2 (паника в `Stop`, `recover` в `StopAll` — системному архитектору) и п. 3 (нарратор пишет `ERROR` на отмене — EPIC-003) в итерацию не входили.

### Запись для dev-log
> **T-415 · developer#1 (Opus) · 2026-09-11 · обходные и беззвучные пути.** `runtime.StartAll` перехватывает панику в `Routes`/`Start`: это ошибка старта, запущенные контексты останавливаются, шина закрывается последней (ADR-023 п. 4), стек — в лог. `FakeEncounter` оставляет след отказа публикации: `Error` в лог и ошибка в `Err()`/`/health`. Повтор шины по-прежнему `nil`, позднее запоминание — решение EPIC-003; подтверждает tech-lead#2. depguard: `$` у двойников механики и боя. M7c закрыт тестом через `reflect`. Проба e2e падает сразу с кодом выхода процесса. Добавлен дымовой тест остановки по сигналу (CTRL_BREAK в свою группу / SIGTERM): ловит R5b. `membus.Subscribe` снимает блокировку группы отложенно, поэтому `runtime.Goexit` в обработчике не вешает группу. `%w` для причины не вводится, сторожевой тест против `stopped`. Мутанты recover, mechanicsx (контроль без `$` — 0 issues), M7c, Goexit, след, `%w`, проба, R5b — красные; контроль M0 красный. Прогоны: build/vet, `go test -short ./...` (без `-race`: нет cgo; один давний флак EPIC-003 воспроизведён на HEAD), e2e, lint — зелёные. Хук go-fmt исключён, что нужно владельцу — в карточке.
>
> **T-415 · итерация 2 · developer#1 (Opus) · 2026-09-11 · по ревью #1.** `FakeEncounter`: публикация, прерванная остановкой (отменён контекст), — не отказ шины: `Info` в лог, `Err()`/`Wait()`/`Stop` на чистой остановке — `nil` (Mi-1). Тест на отказ `encounter.ended` (N-1). Тест M7c проверяет тип поля перед `Bool()` (N-2). Сигнальный e2e не шлёт сигнал вышедшему процессу (N-3). depguard: `$` у `internal-state` и `internal-swarm`, включая `laws`/`llm` (N-4). Флак EPIC-003 `TestTheFactsOfStateMoveTheVersionTheStubProposesAgainst` закрыт условием «появилось предложение этого обмена». Условие строже поручения `len > 0`: оно закрывало только панику, а не преждевременное завершение ожидания. Доказательство: HEAD — 2 из 3 серий по 40 красные, вариант `len > 0` — 1 из 6, итог — 0 из 8. Мутанты MI, MA, N-2 (тип), MS, MW — красные, контроли без `$` — 0 issues, M0 красный. Build/vet, unit, e2e, lint — зелёные. Правки в файлах EPIC-003 подтверждает tech-lead#2.

## Ревью (code-reviewer)
- Ревью #1 · 2026-09-11 · code-reviewer#2 (Opus) · **принять** · Critical 0 · Major 0 · Minor 1 · Nit 4. Mi-1: `refusedPublication` принимает публикацию, прерванную остановкой (`context.Canceled`), за отказ шины: на чистой остановке липкий `Err()` и `Stop` ≠ nil; исправить до коммита. N-1 ветка `encounter.ended` без теста (мутант выжил), N-2 `reflect` без проверки типа поля, N-3 сигнал без проверки, что процесс жив, N-4 `$` у `internal-state`/`internal-swarm` проще закрыть сейчас. API `shared/runtime` и `shared/eventbus` не менялся, contract-change нет. Флак `fake_encounter_test.go:853` воспроизведён на HEAD, к T-415 не относится. Подробно — `review.md`, «T-415 · ревью #1».
- Ревью #2 (итерация 2) · 2026-09-11 · code-reviewer#2 (Opus) · **принять** · Critical 0 · Major 0 · Minor 0 · Nit 0. Mi-1, N-1…N-4 закрыты, мои мутанты MI, MA и N4 красные, контроль M0 красный. Флак EPIC-003: строгое условие тест не ослабляет (итоговые проверки те же), HEAD — 2 из 2 серий по 40 красные, итерация 2 — 0 из 4, мутант «условие HEAD» — 2 из 2 красные. Гонка Mi-1 (настоящий отказ в момент отмены уходит в `Info`) для двойника приемлема: `Deliver` на отмене не паркует, событие приходит снова; сужение — в бэклог T-229. Подробно — `review.md`, «T-415 · ревью #2».

## Приёмка (tech-lead)
- **Решение: принята** · 2026-09-11 · tech-lead#1 (Opus, ступень ниже плановой Fable). Итог: шесть пунктов задачи и два пункта T-426 закрыты тестами, на каждом краснеет свой мутант. Правки в принятых файлах EPIC-003 ждут подтверждения tech-lead#2.
- **Проверено мной.** Прогоны шли в копии рабочей папки в scratch (`git ls-files -co --exclude-standard`), копия удалена по точному пути. В рабочей папке изменены только этот раздел, поле «Статус» и индекс `tasks.md`. Окружение: go1.26.8 windows/amd64, golangci-lint 2.13.2.
  - `go build ./... && go vet ./... && go vet -tags e2e ./test/...` — 0.
  - `go test -short -count=1 ./...` — все пакеты ok, 0 FAIL. Без `-race`: на машине нет cgo.
  - `go test -tags e2e -count=1 ./test/...` — `test/e2e` ok (11,9 с), 4 теста PASS. `TestTheProcessStopsCleanlyOnASignal` выполнился (PASS, не SKIP). `test/fixtures` ok.
  - `golangci-lint run ./...` — 0 issues.
  - Мутант MR (в `runtime.start` `r := recover()` → `var r any`): **красный**. В `shared/runtime` 5 подкейсов «StartAll let the panic out», в `cmd/multiverse` падает паникой `a_context_panics_in_Start`. После восстановления (`cmp` с рабочей папкой) оба пакета ok.
  - Мутант ML (пакет `internal/mechanicsx`, пустой импорт из `shared/testkit/mechanics` и из `shared/testkit/swarm`): **красный**, 2 × depguard, у каждого `desc` своего правила. Контроль (те же импорты, `$` снят): 0 issues.

| Пункт DoD | Чем подтверждён |
|---|---|
| `recover` в `StartAll`: паника → ошибка старта, откат запущенных, шина закрывается последней; мутант на `recover` краснеет | Тесты `TestStartAllTurnsAPanicIntoAFailedStart`, `TestStartAllTurnsAPanicIntoAnErrorWithoutALogger`; лента `TestServeClosesTheBusLastWhenTheStartFails/a_context_panics_in_Start` = `start first\|stop first\|bus closed`. MR красный: мой прогон, автор, ревью #1. MS (откат) и MK (стек в логе) — ревью #1. Не contract-change: C-01 уже описывает `recover` в `StartAll` (`contracts.md:156`, `:169`), экспортируемые имена и сигнатуры `shared/runtime` не менялись |
| `FakeEncounter`: след неудачной публикации | `fake_encounter_failure_test.go`: отказ `combat.decided`, `encounter.started`, `encounter.ended` (N-1); остановка — не отказ (Mi-1). ME, MA, MI красные (ревью #1, #2). **Файл EPIC-003 — ждёт tech-lead#2** |
| Хук `.claude/hooks/go-fmt.ps1` вне модуля | Исключён оркестратором, в приёмку T-415 не входит. Файл владельца не менялся: в `git status` папки нет ни одного пути под `.claude/`. Что нужно владельцу — в «Выполнении» |
| Дымовой тест сигналов под Windows | `test/e2e/lifecycle_test.go`, `signal_windows_test.go`, `signal_other_test.go`. В моём прогоне PASS, не SKIP. R5b: e2e красный, юнит-тесты зелёные (автор, ревью #1) |
| depguard: `$` у двойников механики и боя | ML — мой прогон (выше). Сверх задачи — N-4 (`internal-state`, `internal-swarm`): ревью #2, 4 × depguard, контроль 0 issues |
| M7c | `TestOpenBusHandsTheKafkaAdapterTheValidationFlag` проверяет вид поля до `Bool()` (N-2). M7c красный (автор, ревью #1). Проверка на брокере — T-394 |
| Проба e2e замечает выход процесса | `TestTheProbeGivesUpOnAProcessThatExited`: 4,0 с в моём прогоне вместо прежних 60 с. MP красный (автор, ревью #1) |
| T-426: `runtime.Goexit` под `membus.Subscribe` | `goexit_test.go`; MG красный (ревью #1). Правка внутри `membus` без экспортируемых имён, не contract-change |
| T-426: `%w` для причины | Код не менялся, решение за system-architect (T-430). Сторож `panic_stop_test.go`; MW красный на 4 из 4 подкейсов (ревью #1) |
| Общее: тесты зелёные, линтер чист, запись для dev-log есть | Мои прогоны выше; две записи для dev-log в карточке (итерации 1 и 2), перенос в `dev-log.md` — за оркестратором |

- **Замечания приёмки** (принять не мешают):
  1. Фраза «Tech-lead#2 (EPIC-003) подтверждает обе правки…» в разделе «Итерация 2» принадлежит исполнителю. Записи подтверждения от tech-lead#2 нет ни в карточке, ни в `review.md`, хотя ревью #2 сослалось на неё как на подтверждение. **Ждут подтверждения tech-lead#2** (оркестратор вызывает его следом):
     - `shared/testkit/swarm/fake_encounter.go`: след отказа и исключение остановки;
     - `shared/testkit/swarm/fake_encounter_test.go`: условие ожидания флакового `TestTheFactsOfStateMoveTheVersionTheStubProposesAgainst`; оркестратор включил эту правку сверх состава задачи;
     - новый `shared/testkit/swarm/fake_encounter_failure_test.go`.

     Если tech-lead#2 отклонит один из этих файлов, возврат коснётся только этой части: правки EPIC-001 от пакета `shared/testkit/swarm` не зависят.
  2. `shared/runtime` — общий код system-architect (`plan/ownership.md`). Поведение `StartAll` изменилось, API — нет, и C-01 это поведение уже описывает. Панику в `Stop` архитектор получает с T-430.
  3. `done` в индексе `tasks.md` поставлен по поручению оркестратора. `done` в этой карточке — после «Учёт времени» и подтверждения tech-lead#2.
- **Бэклог разнесён:**
  - EPIC-001, `tasks.md` эпика, новая **T-430**: `recover` в `StopAll` (ревью #1 п. 2) и вопрос system-architect «`%w` только для `ErrHandlerPanic`» («Предложения в бэклог» п. 4, пункты T-426 в описании).
  - EPIC-003 — **передано tech-lead#2**, индекс EPIC-003 не правился:
    - (а) нарратор пишет `ERROR` на отменённом контексте, `fake_narrator.go:815-817` (ревью #1 п. 3);
    - (б) позднее запоминание у `FakeEncounter` вместе с идемпотентностью полуопубликованного хода → T-229 (предложение п. 2, ревью #1 п. 4);
    - (в) исключение остановки в `refusedPublication` сузить до ошибок отмены → T-229 (ревью #2 п. 1).
  - Флак EPIC-003 (предложение п. 1) закрыт в итерации 2, в бэклог не идёт — входит в подтверждение tech-lead#2.
  - Предложение п. 3 (`$` у `internal-state`/`internal-swarm`) закрыто в итерации 2 (N-4).

### Подтверждение tech-lead#2 (EPIC-003)
- **Решение: подтверждаю, все три файла как есть, возврата нет** · 2026-09-11 · tech-lead#2 (Opus, ступень ниже плановой Fable), владелец принятых двойников `shared/testkit/swarm` (T-219, T-220, T-419). Фраза исполнителя «Tech-lead#2 (EPIC-003) подтверждает обе правки…» из раздела «Итерация 2» подтверждается этой записью.

| Файл | Решение | Почему |
|---|---|---|
| `shared/testkit/swarm/fake_encounter.go` | **подтверждаю** | Успешный путь не тронут. Порядок и состав публикаций по C-05 v1.5 прежние: решения до пакета (C-03), `encounter.started/ended` после факта с id из причины (п. 4–5), `exchange` (п. 7). Правка касается только ветки ошибки в трёх точках публикации: `publish`, `announce` started и `announce` ended. Окно `Seen` до обработки остаётся (ADR-027 п. 3, образец для T-230), поэтому повтор шины по-прежнему отвечает `nil` — ради этого след и нужен. **Липкий `Err()` для двойника I1-α приемлем.** Его читают только `/health` и `Wait`/`Stop`. Потерянный пакет двойника — дефект, и оператор должен видеть его до перезапуска. Двойник снимается T-256. **`Info` на остановке приемлем.** `Deliver` на отменённом контексте событие не паркует (`delivery.go:121-125`): оно придёт в следующем запуске, окно `acted` живёт в памяти, и ответ будет дан заново. Остаточная гонка P3 ревью #2 (настоящий отказ в момент отмены уходит в `Info`) двойнику не вредит. Узкую проверку получит `Emitter` роя (T-223, см. бэклог ниже). Блокировки берутся в порядке `mu → failMu`, обратного пути нет. |
| `shared/testkit/swarm/fake_encounter_test.go` | **подтверждаю** | Доказываемое не ослаблено. Итоговые проверки теста не менялись: State не отказал ни одному предложению, бой закончился, версия волка ≥ 2, волк ранен, `dead_letters` пусты. Условие ожидания только упорядочивает удары: следующий уходит, когда предложение текущего обмена опубликовано и применено. Это и есть сценарий теста. Кроме того, `closes()` теперь смотрит на предложение текущего обмена, а не прошлого. Обмен без предложения условие не спрячет: тест упадёт по сроку `waitFor`. |
| `shared/testkit/swarm/fake_encounter_failure_test.go` (новый) | **подтверждаю** | Покрыты все три выхода публикации и остановка — и для `Act`, и для `Observe`. Тест закрепляет: повтор отвечает `nil`, след остаётся. Это следствие ADR-027 п. 3, а не дефект. Если architect#2 сменит окно встречи (§10 п. 14 индекса EPIC-003), эта проверка изменится вместе с ним. Пакет по-прежнему не зависит от `testify`: файл `_test`, условие DoD T-219 не задето. |

- **Проверено мной.** Копия папки T-415 в scratch (`git ls-files -co --exclude-standard`) и выгрузка HEAD `72a24ec`, обе удалены по точному пути. В папке T-415 изменены только этот подраздел и индекс EPIC-003. Окружение: go1.26.8 windows/amd64, golangci-lint 2.13.2, без `-race` (нет cgo).
  - `go build ./...`, `go vet ./shared/testkit/... ./cmd/...` — 0. `go test -short -count=1 ./shared/testkit/... ./cmd/multiverse/...` — все ok. Тесты следа и остановки `-count=3` — PASS. `go test -tags e2e -count=1 ./test/e2e/` — ok (14 с). `golangci-lint run ./shared/testkit/...` — 0 issues.
  - Флак `TestTheFactsOfStateMoveTheVersionTheStubProposesAgainst`, серии `-count=40`: HEAD — 2 из 3 красные (`index out of range [-1]`), T-415 — 0 из 3.
  - Мутанты в копии; после каждого файл восстановлен и сверен `cmp`, итоговый `diff -rq` пуст. M0 (синтаксис) — красный. **MH** (`FakeContext.Health` не читает `encounter.Err()`) — **выжил** в `shared/testkit/swarm` и `cmd/multiverse`. **MS2** (`fail` запоминает последнюю ошибку вместо первой) — **выжил**. Мутанты ME, MA, MI, MF повторять не стал: они красные у автора и в ревью #1/#2.
- **Замечания** (подтверждению не мешают):
  1. MH: «отказ публикации виден как `fail` на `/health`» верно по коду (`fake_context.go:213`), но тестом не закреплено. Дыра старше T-415: отказ подписки встречи на `/health` тоже не проверен, единственный тест (`TestAContextWhoseNarratorWentDeafIsNotHealthy`) идёт через нарратора. DoD T-415 («хотя бы `log.Error` и ошибка в `Err()`») закрыт тестами. Тест `/health` — в бэклог двойников EPIC-003.
  2. MS2: обещание «первая ошибка» из doc `Err()` тестом не закреплено — туда же.
- **Бэклог из T-415 — решение tech-lead#2**, внесено в `epics/EPIC-003-swarm-llm-laws/tasks.md` с пометкой «(из T-415, 2026-09-11)»:
  - (а) `ERROR` нарратора на отменённом контексте — **принято**. Двойник — §11 п. 1 индекса; настоящий рассказчик — строка DoD в T-229.
  - (б) позднее запоминание у `FakeEncounter` — **для двойника отклонено**. ADR-027 п. 3 прямо оставляет агенту встречи `Seen` до обработки, двойник снимается T-256, а следа отказа (T-415) для I1-α достаточно. Для настоящего агента это не T-229 (там видимость и нарратор), а вопрос architect#2 (§10 п. 14). Кости детерминированы по причине (NFR-060, T-230); если и решения обмена получат id из причины, повтор полуопубликованного хода даст те же события с теми же id, и `Has`/`Add` станет безопасным. Это правка ADR-027 п. 3 — до решения архитектора в задачи не идёт. В T-421 внесён пункт «след отказа публикации переживает повтор».
  - (в) сузить исключение остановки до ошибок отмены — **принято, но в T-223**: `Emitter` — единственная точка публикации роя, и правило общее для всех ролей, включая рассказчика. T-229 и T-421 на него ссылаются.
  - (г) новое, из мутантов MH и MS2 — тест `/health` на отказ встречи и тест «первая ошибка»: §11 п. 2 индекса.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 11.09 20:10 | 11.09 20:28 | 18m |
| Ревью | 11.09 20:28 | 11.09 20:40 | 12m |
| Разработка 2 | 11.09 20:40 | 11.09 20:50 | 9m |
| Ревью 2 | 11.09 20:50 | 11.09 20:56 | 6m |
| Приёмка | 11.09 20:56 | 11.09 21:03 | 6m |
| Приёмка 2 | 11.09 21:03 | 11.09 21:12 | 9m |
| **Всего** | | | **1h 1m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
