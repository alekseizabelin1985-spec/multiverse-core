# T-398: Свести раскол docs/ и Docs/, вычистить устаревшие файлы-инструкции (QWEN.md и др.)

| Поле | Значение |
|---|---|
| Инициатива / эпик / команда | EPIC-001 / EPIC-001 / TEAM-1 |
| Волна | 1 |
| Исполнитель | tech-writer#1 |
| Размер | M |
| Статус | done (принята tech-lead#1 после ревью #2 (Nit закрыты при приёмке)) |
| Итераций ревью | 2 |
| Начата / завершена | 2026-09-13 10:31 / 2026-09-13 11:03 |

> Карточка заведена задним числом при переходе на плагин 0.6.0. Разделы
> «Описание» и «DoD» живут в индексе `tasks.md` — дублировать их сюда значило бы
> завести второй источник истины. Подробности выполнения — в `dev-log.md`,
> замечания и вердикты — в `review.md`, решения — в `journal.md`.

## Описание и DoD
См. раздел T-398 в `../tasks.md`.

## Выполнение (tech-writer)
2026-09-13 · tech-writer#1 (Sonnet). Ветка `task/T-398-docs-case-split`. Подробная запись — `../dev-log.md`.

Раскол `docs/`/`Docs/` в git — только на файловой системе Windows (без различения регистра) это
один каталог; в индексе git это были два разных префикса пути. Все 6 файлов под строчным `docs/`
и 4 корневых файла-инструкции перенесены `git mv` в уже существующий `Docs/archive/` (создан T-002,
владелец — tech-writer, `plan/ownership.md` §1). Двухшаговый приём через промежуточное имя не
понадобился: во всех 10 случаях путь меняется не только регистром верхнего сегмента, но и
подкаталогом (`docs/X.md` → `Docs/archive/X.md`), `git status` после каждого `git mv` сразу
показывал `R` (rename), а не удаление+добавление.

Прочитаны все 10 файлов целиком. Ни один не содержит верного фрагмента, который стоило бы
вычленить в актуальный `Docs/` отдельной ссылкой/абзацем — все десять целиком относятся к
одной из двух прежних картин проекта (архитектура «Entity-Actor» до ADR-001 и работа с одним
ассистентом до перехода на командный процесс), полностью замещённых текущими артефактами.
Решение по каждому файлу и куда он ведёт читателя дальше — таблица ниже (дублирует и раскрывает
причины столбца «Чем заменён» в `Docs/archive/README.md`, куда занесена официальная запись
перенесённых документов).

| Файл | Куда | Почему |
|---|---|---|
| `docs/FEATURES_VERIFICATION.md` | `Docs/archive/FEATURES_VERIFICATION.md` | Матрица верификации требований к отклонённой архитектуре Entity-Actor (нейросетевые веса как состояние, Go 1.22, Redis, ChromaDB) — не пересекается с ADR-001 и текущим кодом; актуальные требования — `Docs/dev-team/requirements/**` |
| `docs/LIVING_WORLDS_ARCHITECTURE.md` | `Docs/archive/LIVING_WORLDS_ARCHITECTURE.md` | Дизайн той же отклонённой архитектуры целиком; текущая архитектура — `Docs/dev-team/architecture/overview.md` и ADR |
| `docs/LIVING_WORLDS_FEATURE_CHECKLIST.md` | `Docs/archive/LIVING_WORLDS_FEATURE_CHECKLIST.md` | Чек-лист приёмки той же отклонённой архитектуры; актуальные критерии готовности — `epics/EPIC-001-foundation/tasks.md`, `foundation.md` §12 |
| `docs/LIVING_WORLDS_INTEGRATION_GUIDE.md` | `Docs/archive/LIVING_WORLDS_INTEGRATION_GUIDE.md` | Интеграция с сервисами (`entity-actor`, `evolution-watcher`, `rule-engine`), которых в сборке нет — `entity-actor` заморожен (`services/entity-actor/FROZEN.md`), остальные не существовали |
| `docs/LIVING_WORLDS_QUICK_START.md` | `Docs/archive/LIVING_WORLDS_QUICK_START.md` | Быстрый старт для той же отклонённой архитектуры (Go 1.22, Redis, ручной Kafka) — актуальный путь запуска — `README.md` «Запуск за 5 команд» |
| `docs/LIVING_WORLDS_SUMMARY.md` | `Docs/archive/LIVING_WORLDS_SUMMARY.md` | Резюме и бизнес-метрики той же отклонённой архитектуры, ничего не переносится в текущую картину |
| `README_LIVING_WORLDS.md` | `Docs/archive/README_LIVING_WORLDS.md` | Корневой README ветки с описанием той же отклонённой архитектуры; статус «production ready» не подтверждён кодом (расхождение уже зафиксировано `requirements/inventory.md` INV-23) |
| `QWEN.md` | `Docs/archive/QWEN.md` | 723 строки прежней картины проекта (Go 1.24, 12+ отдельных сервисов, ChromaDB как основная БД, TimescaleDB/Redis) — противоречит ADR-001 и текущей карте каталогов; актуальная картина — `CLAUDE.md`/`AGENTS.md`/`README.md` |
| `AI_AGENT_INSTRUCTIONS.md` | `Docs/archive/AI_AGENT_INSTRUCTIONS.md` | Инструкция режима работы одного ассистента-соавтора (CONSULT/RESEARCH/ARCHITECTURE/…, стек Java/Python/немного Go) — не тот стек и не тот процесс; текущий процесс — `.dev-team.json`, роли — `Docs/dev-team/plan/teams.md` |
| `AUTOMATION-SETUP.md` | `Docs/archive/AUTOMATION-SETUP.md` | Состав subagent'ов/skills/MCP-серверов на момент установки разошёлся с фактическим (`.claude/agents/*`, `.claude/skills/*`, `.mcp.json` уже другие) и не обновлялся; отдельный обзорный документ не нужен — состав самоописывается перечисленными каталогами/файлом |

**Живые ссылки.** Грэп по `README.md`, `CLAUDE.md`, `Makefile`, `.github/`, `scripts/`,
`.gitleaksignore`, `.gitleaks.toml`, `.golangci.yml`, `.dockerignore`, `.gitattributes` на имена
всех 10 файлов — пусто, править нечего. Единственный «живой» указатель — сам
`Docs/archive/README.md`: таблица «Перенесённые документы» заполнена (10 строк), пункты по этим
файлам убраны из списка «Кандидаты на перенос».

Три места, где имена файлов встречаются, но правка не сделана намеренно:
1. `Docs/dev-team/architecture/components/foundation.md:330` — плановая таблица волны 0, не
   указатель на путь, решение по ней уже исполнено этой задачей; переписывать план прошлой волны
   значило бы стирать историю решения.
2. `Docs/dev-team/requirements/inventory.md`, `Docs/dev-team/project/open-questions.md` — датированные
   записи аудита (BA, 2026-09-09), фиксируют состояние на момент анализа; та же логика, что и для
   журнала/дев-лога/ревью — не переписываются.
3. `services/entity-actor/docs/technical-spec.md:745` — сервис заморожен (`FROZEN.md`): «содержимое
   не правится ни рефакторингом, ни автофиксерами». Ссылка на `docs/LIVING_WORLDS_ARCHITECTURE.md`
   там теперь битая, но это ожидаемое следствие заморозки, а не что-то, что чинит T-398.

Отдельно вне зоны правки, но не проверить нельзя: `.claude/plugins/multiverse-core-plugins/README.md`
ссылается на `AUTOMATION-SETUP.md`, `.qwen/PROJECT_SUMMARY.md` — на `QWEN.md` и
`README_LIVING_WORLDS.md`. Оба — под `.claude/*`/`.qwen/*`, трогать запрещено условиями задачи;
ссылки становятся битыми. Вынесено в риски отчёта, решение — за оркестратором/владельцем.

**Проверки.**
```
git -C <папка> ls-files | grep -E '^docs/'   # пусто
git -C <папка> status --porcelain            # все 10 строк — R (rename), не D+A
```
Оба выполнены, результат — в отчёте оркестратору.

### Итерация 2
2026-09-13 · tech-writer#1 (Sonnet). Та же ветка `task/T-398-docs-case-split`, та же рабочая папка.
Исправлены оба замечания ревью #1 (`../review.md`, «T-398 · ревью #1»).

- **Mj-1.** `Docs/LIVING_WORLDS_IMPLEMENTATION_STATUS.md` — шестой файл семейства `LIVING_WORLDS_*`,
  в HEAD лежавший под заглавным `Docs/`, а не строчным `docs/`, поэтому не попал в перечень 10
  файлов итерации 1 и одновременно выпал из пункта «Кандидаты на перенос». Прочитан целиком (отчёт
  «Core Implementation Complete ✅ ~85%» по ветке `feat/living-worlds-entity-actor`, та же отклонённая
  архитектура Entity-Actor). Перенесён предпочтительным способом из ревью:
  `git mv Docs/LIVING_WORLDS_IMPLEMENTATION_STATUS.md Docs/archive/LIVING_WORLDS_IMPLEMENTATION_STATUS.md`
  (`git status` — `R`, не `D+A`; содержимое не менялось). Добавлена строка в таблицу «Перенесённые
  документы» `Docs/archive/README.md` с тем же обоснованием, что предложил ревьюер (не подтверждено
  кодом — `requirements/inventory.md` INV-23, `project/open-questions.md`), «чем заменён» —
  `Docs/dev-team/architecture/overview.md`. Упоминания в `audit-facts.md:159` и
  `open-questions.md:202` — записи аудита, не тронуты. Грэп по `README.md`, `CLAUDE.md`, `AGENTS.md`,
  `Makefile`, `.github/`, `scripts/`, остальному `Docs/` (кроме записей аудита и самого `review.md`)
  на имя файла — пусто, живых ссылок не было и до переноса.
- **Mi-1.** `Docs/archive/README.md:14-15` — правило «Актуальная документация … в `docs/**`
  (пользовательская документация и runbook, создаётся в F-9)» заменено на «… и в `Docs/**` вне
  `archive/` (пользовательская и эксплуатационная документация, например `Docs/ops/runbook.md`);
  строчного `docs/` в корне нет (T-398)» — формулировка ревьюера применена дословно.
- **Сверка.** Таблица «Перенесённые документы» — теперь 11 строк, ровно 11 фактических `git mv`
  (10 из итерации 1 + этот). Список кандидатов сверен построчно с `git show HEAD:Docs/archive/README.md`:
  из четырёх пунктов HEAD (`Docs/architecture*.md` ×3, `LIVING_WORLDS_*` (6 файлов) +
  `FEATURES_VERIFICATION.md` + `EVENTS-MIGRATION.md`, корневые as-is, `memory/**`/`plans/**`/
  `reports/**`) все либо перенесены (10+1), либо остались в списке кандидатов
  (`Docs/architecture*.md` ×3, `Docs/EVENTS-MIGRATION.md`, `PULL_REQUEST.md`, `memory/**`/`plans/**`/
  `reports/**`) — ни один пункт не потерян.

## Ревью (code-reviewer)
| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#2) | 2026-09-13 | 0 | 1 | 1 | 0 | вернуть |
| 2 (code-reviewer#1) | 2026-09-13 | 0 | 0 | 0 | 2 | принять |

Подробности — разделы T-398 в `../review.md`.

## Приёмка (tech-lead)
**Решение: принята** · tech-lead#1 (Opus) · 2026-09-13. **Итог:** строчного `docs/` в дереве больше нет. Все 11 устаревших документов, которые агенты читали как инструкции, лежат в `Docs/archive/` с сохранённой историей. Ничего не удалено, у каждого файла в README архива есть причина и замена.

Основание: ревью #1 (вернуть; Major 1, Minor 1), итерация 2, ревью #2 (принять; Nit 2) и собственная проверка. Кода в задаче нет, поэтому Go-тесты и golangci-lint не запускались. Вместо них — проверки только чтением и gitleaks. Индекс не менялся, не коммитил, `git add` не делал. `.claude/*`, `.qwen/*`, `.mcp.json` и `Docs/user-stories/` только читались, `.env` не открывался. Стенд и контейнеры не трогались.

**DoD.** В индексе у T-398 нет отдельного списка DoD, только «Состав» и «Причина». Поэтому сверка идёт по ним, по заголовку задачи, по указанию владельца «всё в архив, ничего не удалять» (U-1/OQ-A-17) и по общему DoD команды.

| # | Пункт | Статус | Доказательство (рабочая папка `.worktrees/T-398`) |
|---|---|---|---|
| 1 | Раскол `docs/`/`Docs/` сведён | выполнен | `git ls-files \| grep -c '^docs/'` — 0. На диске один каталог `Docs`. Правило README архива (`:14-16`) называет местом актуальной документации `Docs/**` вне `archive/` и прямо говорит, что строчного `docs/` нет (Mi-1 ревью #1 закрыт) |
| 2 | Устаревшие файлы-инструкции убраны из пути чтения: `QWEN.md`, `AI_AGENT_INSTRUCTIONS.md`, `README_LIVING_WORLDS.md`, `AUTOMATION-SETUP.md` и 6 файлов `docs/` | выполнен, с расширением | `git diff --cached -M --summary` — 11 записей `rename … (100%)`: 4 корневых, 6 из `docs/`, а также `Docs/LIVING_WORLDS_IMPLEMENTATION_STATUS.md`. Одиннадцатый файл сверх «Состава» добавлен по Mj-1 ревью #1. Расширение принимаю: семейство `LIVING_WORLDS_*` целиком (7 файлов вместе с `README_LIVING_WORLDS.md`, `open-questions.md:750`) теперь в архиве, как требуют `foundation.md` §11 и решение A3 |
| 3 | Ничего не удалено, история и содержимое сохранены | выполнен | Для каждой из 11 пар `rev-parse HEAD:<src>` = `rev-parse :<dst>` = `hash-object <dst>`. Файлов в HEAD и в индексе по 968. `--diff-filter=D` пуст и в индексе, и в рабочей копии, неотслеживаемых файлов нет. `git log --follow` по новым путям работает за счёт R100 |
| 4 | Место архива соответствует карте | выполнен | `foundation.md:51` — `Docs/archive/` для исторических документов, `services/_archive/` для кода (`:50`). `plan/ownership.md:30` отдаёт `Docs/archive/**` tech-writer. Корневого `_archive/` карта не знает |
| 5 | README архива соответствует факту | выполнен | Таблица «Перенесённые документы» (`:22-32`) — 11 строк, исходные пути один к одному совпадают с `--name-status`. Пометку «исторический, статусы не подтверждены» из A3 несут расположение в архиве и столбец «Причина». Содержимое файлов по правилу архива не правится. Кандидаты из `HEAD:Docs/archive/README.md` (4 пункта) не потеряны: перенесённое вычеркнуто, остальное (`Docs/architecture*.md` ×3, `Docs/EVENTS-MIGRATION.md`, `PULL_REQUEST.md`, `memory/**`, `plans/**`, `reports/**`) осталось в `:38-43` |
| 6 | Живых ссылок, которые задача ломает в зоне команды, нет | выполнен | Мой `git grep` по всему дереву на имена 11 файлов, без `Docs/archive/`, журнала, dev-log, ревью и карточек. Совпадения есть только в записях прошлого (`audit-facts.md:159`, `inventory.md`, `open-questions.md`, `domain-notes.md:184`), в плановой таблице `foundation.md:330`, в индексе `tasks.md:440`, в `state.js`/`dashboard.html`, в замороженном `services/entity-actor/docs/technical-spec.md:745`, а также в файлах владельца `.claude/plugins/multiverse-core-plugins/README.md:175` и `.qwen/PROJECT_SUMMARY.md`. Решения исполнителя по записям прошлого, плану и замороженному сервису поддерживаю. Маски путей в живых `overview.md:5` и `glossary.md:8` вынесены в бэклог ниже |
| 7 | Гигиена и секреты | выполнен | `git diff --check` и `git diff --cached --check` чисты. Все правленые файлы в CRLF без смешения, `* text=auto`. gitleaks по содержимому индекса (копия `checkout-index` в scratch, `.gitleaks.toml`) — no leaks found. Правленые README архива и карточка тоже чисты. Копия удалена по сохранённому точному пути. Коммитов на ветке сверх эпика нет, поэтому часть `secrets-scan` по диапазону пуста |
| 8 | dev-log заполнен | выполнен | Две записи T-398 в `dev-log.md`: итерация 1 и итерация 2 |

**Nit ревью #2 — исправлены при приёмке** (однострочные правки в артефактах задачи, смысл не меняется):
- **N-1** — `dev-log.md:8286`: `../review.md` → `review.md` (`dev-log.md` и `review.md` лежат в одном каталоге).
- **N-2** — карточка, «Итерация 2»: «из шести пунктов HEAD» → «из четырёх пунктов HEAD». Та же неточность счёта есть в записи итерации 1 (`dev-log.md:8278`, «убраны шесть пунктов»). Её оставляю как запись прошлого: итог сверки верен и подтверждён ревью #2.

**Бэклог** (записан в раздел T-398 индекса; приёмку не блокирует):
- `infrastructure.md:1044`, `:1106` — `docs/ops/runbook.md` строчными, а файл лежит в `Docs/ops/runbook.md`. Владелец — devops-engineer. Удобно закрыть в T-399: она правит тот же файл.
- `architecture/overview.md:5` (system-architect) и `project/glossary.md:8` (BA) называют источником `Docs/LIVING_WORLDS_*.md`. Правка — `Docs/archive/LIVING_WORLDS_*.md`, одна строка при следующем касании документа.
- `Docs/agent-gm-research/` (4 файла) по A3 (`open-questions.md:754`) исторический, но в списке кандидатов `Docs/archive/README.md` его нет. Добавить в список при следующей гигиенической задаче tech-writer вместе с оставшимися кандидатами.
- Битая ссылка `.claude/plugins/multiverse-core-plugins/README.md:175` (`../../../../AUTOMATION-SETUP.md`) — вопрос владельцу, уже передан оркестратором. `.qwen/PROJECT_SUMMARY.md` содержит упоминания, а не ссылки, действий не требует.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 10:31 | 13.09 10:41 | 9m |
| Ревью | 13.09 10:41 | 13.09 10:46 | 5m |
| Разработка 2 | 13.09 10:46 | 13.09 10:49 | 2m |
| Ревью 2 | 13.09 10:54 | 13.09 10:57 | 3m |
| Приёмка | 13.09 10:57 | 13.09 11:03 | 5m |
| **Всего** | | | **26m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
