# T-442: Редакционно: тексты об `--alias`, пине и `ops/models.txt` приведены к слиянию T-437

| Поле | Значение |
|---|---|
| Эпик / команда | EPIC-001 / TEAM-1 |
| Волна | 1 (передача T-437) |
| Размер | XS |
| Статус | done (принята tech-lead#2 после ревью #1 (Mi-1 и N-1 закрыты при приёмке)) |

## Описание и DoD
- **Причина (бэклог T-437: карточка исполнителя п. 4, ревью #1, приёмка tech-lead#2 п. 6)**: T-437 слита в эпик (мерж `c3bfcd5`, коммит `c281632`), а ADR-005 «Уточнение исполнения» п. 6, `baseline.md` §5 и `_model_ids` в `bench-matrix.json` всё ещё описывали её как будущую задачу.
- **DoD** — раздел «### T-442» индекса `tasks.md`: правки трёх текстов по факту T-437; записи прошлого не переписываются; решение T-435 не меняется; T-438 остаётся будущей; грэп по `Docs/`, `ops/`, `README.md` на прочие ссылки на T-437 как на будущее; JSON валиден; `gitleaks dir` по изменённым файлам чист.
- **Исполнитель**: architect#1. Ветка `task/T-442-t437-followup-texts`, папка `.worktrees/T-442`, родитель `epic/EPIC-001-foundation`.

## Выполнение (architect#1)
2026-09-13 · architect#1 (Opus). Правки только текстовые, код и скрипты не менялись.

**Факт T-437, от которого считаются правки.** В single-режиме `scripts/llm-server.{sh,ps1}` передают `-m <файл> --alias <id>`, где id — имя файла без `.gguf`. В router-режиме алиаса нет. `/v1/models` отдаёт ключи матрицы: E → `Qwen3.8-27B-UD-Q3_K_XL`, Q36-A3B → `Qwen3.6-35B-A3B-UD-Q3_K_XL`. Пин `LLAMACPP_BUILD` b10441 → b10878, `ops/models.txt` — E, Q36-A3B, C, A. Проверено на подставном сервере; на настоящем `llama-server` b10878 алиас подтвердит прогон 1 сессии T-438.

1. **ADR-005, «Уточнение исполнения» п. 6** (`architecture/adr/ADR-005-llm-gateway-providers-models.md`). Истории версий у ADR нет, поэтому пометка «редакционно, T-442» стоит в самом пункте, как в T-440.
   - Факт T-402 переведён в прошедшее время: «до T-437 скрипты передавали `-m` без `--alias`». Условие T-438 — «было условием».
   - Ссылки `llm-bench.sh:743`, `llm-bench.ps1:384` устарели после T-434 (сейчас `:893` и `:478`). Их заменил текст сообщения `bench: model … is not in …/v1/models — phase … skipped`. Номера строк снова сдвинет параллельная T-405, а текст сообщения — нет.
   - Добавлен абзац «Исполнено (T-437, 2026-09-13; редакционно, T-442)»: поведение скриптов, id в `/v1/models`, чем проверено, что подтвердит T-438. Два остатка: сервер, поднятый вручную без `--alias`, отдаёт путь, а `make llm-health` не сверяет `/v1/models` (бэклог T-437).
   - Решение пункта («поле-алиас не вводится, id — stem, отдаёт сервер») не менялось. Доп. 3 п. 6 и УИ п. 5 («после T-434 и T-437») не менялись: это перечень задач и порядок, а не утверждение о будущем.
2. **`ops/metrics/baseline.md` §5**, чек-лист «Что меняется вслед за решением»:
   - `ops/models.txt` — `[x]`, «выполнено в T-437». Названо отклонение: список сокращён по решению T-435, не дожидаясь T-438. После исхода T-438 он приводится к `decision`.
   - `LLAMACPP_BUILD` — `[x]`: пин `b10878`, билд зачётных прогонов по `/props`, а не контрольного прогона. В начале T-438 — сверка `build_info` с пином.
   - `--alias` — `[x]`: что отдаёт `/v1/models`, чем проверено и что подтвердит прогон 1 T-438.
   - «Кто и когда» (запись T-435) не переписан: к нему дописана строка состояния «на 2026-09-13 T-434 и T-437 выполнены, сессия T-438 ещё не проведена».
3. **`ops/metrics/bench-matrix.json`**:
   - `_model_ids` переписан по факту T-437. Сервер, поднятый вручную без `--alias`, по-прежнему отдаёт путь. Проверка на настоящем b10878 — за T-438. Поле-алиас не вводится.
   - `updated` → `2026-09-13`; `version` осталась `0.3`, ключи и значения конфигураций не менялись.
   - **Кодом `_model_ids` не читается.** Во всём дереве строка встречается только в самой матрице и в записях прошлого. `llm-bench.{sh,ps1}` берут из матрицы `configs`, `request`, `thresholds`, `n_per_cell`, `runs_required`. Меняется только `matrix_sha256` в отчётах будущих прогонов — это ожидаемо.

### Грэп
Шаблоны: «после T-437», «до T-437», «T-437 добав», «без T-437», «вынесено в T-437», «задача T-437», `after/before/until T-437`, «передают `-m` без», «без `--alias`», `without --alias`, `no --alias`. Области: `Docs/`, `ops/`, `README.md`, `CLAUDE.md`, `scripts/`, `build/`, `Makefile`, `.env.example`. Исключены журнал, ревью, dev-log и карточки.

- **Свои документы после правок.** ADR-005 и `baseline.md` §5 называют T-437 только как выполненную. `baseline.md:425` («так делает `llama-server` без `--alias`», §4 наблюдения T-402) — запись прошлого, верна и сейчас. `infrastructure.md` §6.4 ссылок на T-437 как на будущее не содержит: `--alias` появляется только в §6.2, §6.2.1 и §6.5, и там текст уже написан T-437.
- **Верны по факту, не правились.** `Docs/ops/runbook.md:286`, `ops/models.txt:9`, `:20`, `build/versions.env:97`, `:100`, `infrastructure.md` §6.2 и §6.5. Комментарии в `scripts/llm-server.*` и `scripts/llm-bench.*` — за T-405, их не трогал.
- **Чужой документ, отстаёт** — индекс `tasks.md`, раздел T-438, «Зависимости» (`:845`): «Без T-437 сервер поднимается вручную с `--alias`, иначе скрипт пропустит фазы». T-437 выполнена, условие потеряло смысл. Но суть остаётся: сервер, поднятый не через `make llm-up`, как в T-402, всё равно нужно запускать с `--alias`. Владелец индекса — tech-lead#1; раздел T-438 я не правил.
- **Записи прошлого, не правились**: разделы T-437 и T-435 индекса, `tasks/T-435.md`, `tasks/T-434.md:150`, `ops/metrics/README.md:144` (пин `b10441` в прогонах T-402).

### Проверки
- `py -3 -m json.tool ops/metrics/bench-matrix.json` — ok. `ConvertFrom-Json` в `pwsh` читает матрицу: `version` 0.3, модели E и Q36-A3B на месте.
- `gitleaks dir --config=.gitleaks.toml` по каждому изменённому и созданному файлу (ADR-005, `baseline.md`, `bench-matrix.json`, `tasks.md`, `dev-log.md`, эта карточка) — `no leaks found`. В `.gitleaksignore` записей на эти файлы нет, поэтому сдвиг строк отпечатков не задевает. После `git add` оркестратору стоит повторить `make secrets-scan`.
- Переводы строк. Правленые файлы (ADR-005, `baseline.md`, `bench-matrix.json`, `tasks.md`, `dev-log.md`) остались в CRLF, число CR равно числу строк, смешения нет. Эта карточка создана новым файлом и записана в LF целиком. Репозиторий нормализует переводы строк при `git add` (`* text=auto`, `core.autocrlf=true`), но в рабочей копии она отличается от соседних карточек. Bash у архитектора только на чтение, поэтому перевод в CRLF — на оркестраторе.
- Go и скрипты не менялись, `go test` не запускался.
- Стенд `:8888` не трогался, `make llm-*`, `make up/down`, `make test-integration` не запускались. `.env` не открывался. Не коммитил, `git add` не делал.

### Бэклог (найдено, вне DoD)
1. Индекс `tasks.md`, раздел T-438, `:845`: переписать зависимость от T-437 как выполненную и оставить требование `--alias` для сервера, поднятого вручную. Владелец — tech-lead#1.
2. `infrastructure.md` §6.4 п. 4: «13,1 ГБ весов» E против 13,4 ГБ по байтам файла стенда (приёмка T-437, бэклог п. 7). §6.4 за architect#1, но в XS-передачу T-437 не входит. Взять при ближайшем касании §6.4, например строкой в T-438, где architect#1 правит `baseline.md` и матрицу.
3. `ops/metrics/README.md` (подготовка T-438, рядом со строкой `:89` из DoD T-438): одна фраза о том, что сервер, поднятый не через `make llm-up`, запускается с `--alias`, иначе скрипт пропустит фазы. Сейчас README об этом молчит. Владелец README — devops-engineer.

## Ревью (code-reviewer)

| Итерация | Дата | Critical | Major | Minor | Nit | Вердикт |
|---|---|---|---|---|---|---|
| 1 (code-reviewer#1, Opus) | 2026-09-13 | 0 | 0 | 1 | 1 | принять |

Подробности — раздел «T-442 · ревью #1» в `../review.md`.

## Приёмка (tech-lead)

**Решение: принята** · tech-lead#2 (Opus) · 2026-09-13. **Итог:** ADR-005 УИ п. 6, `baseline.md` §5 и `_model_ids` матрицы описывают T-437 как выполненную, T-438 остаётся будущей, решение T-435 не тронуто. Mi-1 и N-1 ревью #1 я закрыл правкой в несколько слов.

Основание: ревью #1 (принять; 0/0/1/1) плюс моя проверка. Кода в задаче нет, тесты и линтер не гонялись. Проверка — сверка утверждений с деревом, diff, грэп, `json.tool`, `gitleaks dir`. **Стенд `:8888` не трогал**: ни одного запроса, `make llm-*` не запускал, `.env` не открывал. Не коммитил, `git add` не делал.

**DoD** (строка DoD этой карточки; «Состав» раздела T-442 в `../tasks.md`)

| # | Пункт | Статус | Чем подтверждён |
|---|---|---|---|
| 1 | Три текста приведены к факту T-437 | выполнен | Сверил с деревом ветки, мерж `c3bfcd5` в ней есть. `--alias "$model_id"` стоит только в ветке single (`llm-server.sh:596`, `Get-ModelAlias` в `.ps1:603`). Пин `LLAMACPP_BUILD=b10878` (`versions.env:103`). В `ops/models.txt` `gguf` — E и Q36-A3B в комментариях, теги `ollama` только за C/A. Сообщение о пропуске фазы дословно совпадает с `llm-bench.sh:893` и `llm-bench.ps1:478`. Id в текстах равны `configs.E.tick/narrative` и `configs.Q36-A3B.tick/narrative` |
| 2 | Записи прошлого не переписываются | выполнен | В ADR-005 один hunk (`:114`), в `baseline.md` три (§5: чек-лист и хвост «Кто и когда»). Запись T-435 «Кто и когда» сохранена дословно, состояние дописано отдельной помеченной фразой. Наблюдения §1–§4 не тронуты. В `tasks.md` и `dev-log.md` только добавления в конец |
| 3 | Решение T-435 не меняется | выполнен | Пословный diff матрицы: изменены только `updated` и текст `_model_ids`. `decision_order` `["E", "Q36-A3B", "C", "A"]`, `decision.chosen` `E`, `decision.target` `Q36-A3B`, `thresholds`, `results`, `configs` без изменений. ADR-005 п. 5 и доп. 3 вне diff |
| 4 | T-438 остаётся будущей | выполнен | Все три текста говорят «проверено на подставном сервере» и «подтвердит прогон 1 сессии T-438». Нигде не сказано, что алиас проверен на настоящем b10878 или что сессия проведена. После N-1 ADR прямо называет `--alias` действующим условием T-438 |
| 5 | Грэп на T-437 как будущую | выполнен | Мой грэп («вынесено в T-437», «без/Без T-437», «до T-437», `after/until/before T-437`, «передают `-m` без») по `Docs/`, `ops/`, `README.md`, `CLAUDE.md` без журнала, dev-log, ревью, карточек и дашборда. Выдача: `tasks.md:845` (раздел T-438, передан в бэклог ниже) и `:876`, `:878` (сам раздел T-442, в прошедшем времени) |
| 6 | JSON валиден | выполнен | `py -3 -m json.tool ops/metrics/bench-matrix.json` — ok. `_model_ids` кодом не читается (грэп ревьюера по `scripts/`, `shared/`, `cmd/`, `internal/`, `build/`, `Makefile`) |
| 7 | `gitleaks dir` по изменённым файлам чист | выполнен | После моих правок `gitleaks dir --config=.gitleaks.toml` по ADR-005, `baseline.md`, `bench-matrix.json`, `tasks.md` и этой карточке — `no leaks found` у каждого. На эти файлы записей в `.gitleaksignore` нет |
| — | dev-log заполнен | выполнен | Запись `<!-- dev-log T-442 -->` в `../dev-log.md` |

**Замечания ревью #1, закрыты мной**

- **Mi-1.** «Ключи матрицы» заменены на «id моделей»: ADR-005 `:115` — «отдаёт в `/v1/models` id моделей из матрицы: E → `…`, Q36-A3B → `…`», `baseline.md:535` — «отдаёт id модели из матрицы (`…`, `…`)». Ключи `E`/`Q36-A3B` в ADR остаются только слева от стрелки, как ключи. Строку 18 этой карточки не правил: это запись исполнителя, и термин в ней уточнён этим пунктом.
- **N-1.** ADR-005 `:114`: вместо «оно было условием сессии T-438» теперь «это условие сессии T-438 (выполнено T-437; алиас на настоящем билде подтверждает прогон 1 этой сессии)». Условие не снято, а исполнено, и T-438 по-прежнему поднимает сервер с `--alias` (`tasks.md:822-823`).

После правок: `git diff --check` чист. ADR-005 и `baseline.md` остались в CRLF (CR = LF: 115 и 597). Строка `baseline.md:535` стала длиннее соседних на 8 символов, перенос абзаца не менял, чтобы не сдвигать строки.

**Бэклог** (строками в раздел T-442 `../tasks.md`, отдельные задачи, не блокируют):
1. `tasks.md:845`, раздел T-438, «Зависимости»: «Без T-437 сервер поднимается вручную с `--alias`…». Записать зависимость от T-437 как выполненную и оставить требование `--alias` для сервера, поднятого не через `make llm-up`. Владелец — tech-lead#1.
2. `ops/metrics/README.md`, к подготовке T-438: одна фраза о том, что сервер, поднятый не через `make llm-up`, запускается с `--alias`, иначе скрипт пропустит фазы. Владелец — devops-engineer.
3. Вес E «13,1 ГБ» в `infrastructure.md` §6.4 п. 4 против 13,4 ГБ по байтам файла стенда (`ops/models.txt`, `baseline.md` §1). Правит architect#1 в T-438; §2 и §6.2 — devops-engineer (бэклог T-437 п. 7).

**Вне задачи, оркестратору** (ревью #1, «Предложения в бэклог» п. 2): `review.md` эпика перемешан при разрешении конфликта мержа T-437 (`c3bfcd5`). Хвост раздела «T-434 · ревью #1» оказался под заголовком T-437, а Nit и «Риски» T-437 — в конце файла у «T-440 · ревью #2». Содержимое цело. Восстанавливать порядок — отдельной правкой по веткам T-434 и T-437, не в T-442.

**Замечания к слиянию.** Ветка идёт от `c3bfcd5`. Код и скрипты не менялись, с параллельной T-405 (`scripts/llm-*`, `Makefile`) файлы не пересекаются. Ожидаемые конфликты — хвосты `dev-log.md`, `review.md` и `tasks.md`; разрешает оркестратор, сохраняя обе записи. Карточка в LF, нормализуется при `git add` (`* text=auto`). После `git add` повторить `make secrets-scan`.

## Учёт времени (оркестратор)
| Этап | Начало | Окончание | Длительность |
|---|---|---|---|
| Разработка | 13.09 10:45 | 13.09 10:53 | 8m |
| Ревью | 13.09 10:57 | 13.09 11:21 | 23m |
| Приёмка | 13.09 11:21 | 13.09 11:27 | 5m |
| **Всего** | | | **37m** |

Время посчитано по меткам `activity` в `state.js`: у оркестратора есть начало и
конец каждого вызова агента, у агентов надёжных часов нет.
