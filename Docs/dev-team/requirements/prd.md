# Требования: multiverse-core — платформа живых миров

**Инициатива:** PROJECT — аудит платформы, архитектурное видение, декомпозиция на эпики
**Этап:** A1 Discovery, шаги 3–4 (формализация, повторный проход после доменного ревью)
**Версия:** 0.3 · 2026-09-09 · бизнес-аналитик (dev-team)
**Статус:** утверждено на G1 2026-09-09 (без замечаний; решения гейта — `journal.md`, запись «G1 УТВЕРЖДЁН пользователем»)

**Изменения v0.3 (G1, точечно):** FR-050 зафиксирован как минимальная проверка категории (a) fail-closed, задержанный ответ → `llm.output` со статусом `quarantined` без `response_raw` (OQ-D-16; вариант A снят); BR-08 — 7 пунктов, пометка «предложение DE» снята (OQ-D-17); допущения A-1, A-5, A-10…A-16 → «решено (G1)» как стартовые значения (OQ-R-18…21, OQ-D-18/19, словарь команд); §1.2, §7, §10, §11 приведены в соответствие.

**Изменения v0.2 (кратко):**

- **Раунды 4–5 (пользователь):** GM — не один агент, а иерархический *рой* на одной архитектуре Agent GM Core (§5.13 FR-120…FR-128, BR-12, BR-16, G-6); *фоновая жизнь мира* — в MVP-1, допущение A-3 отклонено (FR-037, FR-124…FR-127, BR-10); первый мессенджер — Telegram (FR-004, A-6 решено); ввод — команды с опциональным текстом (FR-002, A-1 решено); целевое железо RTX 4090 24 ГБ / 128 ГБ RAM / i9-13900 (§9, NFR); список абсолютных запретов принят как 4 пункта (BR-08).
- **Доменное ревью (существенные DR-01…DR-10):** BR-02 «пробой законов» описан как механика с явным объёмом MVP-1; разведены пробой / эволюция правил / immutable-законы (BR-01, FR-046); модель раунда группы (BR-13, FR-025); правила боя v0.1 как данные + действие `rest` (FR-020, FR-024, приложение A); seed = hash(event_id, roll_index) (FR-021); «один аккаунт ↔ один живой персонаж» (FR-023, A-7); два варианта FR-050 (решение на G1); согласие и подтверждение 18+ при `/start` (FR-009); явный ввод имени персонажа (FR-060). Замечания DR-11…DR-21 внесены там, где это формулировка требования (FR-045, FR-056, FR-013, BR-04, BR-05, BR-06, §2), остальное — ссылка «учесть на архитектуре».
- **Метрики (metrics.md §8):** сквозная трасса `correlation_id` (FR-084), поля `llm.output` (FR-032) и `llm.output.rejected` (FR-034), сессии и `actor_kind` (FR-085), события аналитики и журнал инцидентов (FR-086…FR-088), `notice_shown_at` (FR-060), формат артефакта прогона (FR-101).

Источник решений — `Docs/dev-team/journal.md` (записи «Discovery раунд 1/2/3», «Допущения оркестратора», «Подтверждение видения, раунд 4», «Уточнения к видению (раунд 5)»). Всё, что помечено «допущение», подтверждается на G1. Термины — по `project/glossary.md` v0.2. Открытые вопросы — `project/open-questions.md`. Доменное ревью — `requirements/domain-review.md` (`DR-NN`). Метрики — `project/metrics.md` (`PA §N`).

---

## 1. Контекст и цель

multiverse-core — бэкенд-платформа «живых миров»: символическое состояние мира
(сущности, события, законы, канон) — источник истины; LLM принимает решения в
рамках правил и рассказывает; мир помнит действия игроков, живёт между сессиями,
восстанавливается после сбоя и эволюционирует. Игровые домены (культивация, города,
Планы) — расширения поверх платформы, не её ядро (раунд 1).

Сегодня в репозитории 15 сервисов и три поколения дизайна GM, но ни один игровой
сценарий не проходит сквозь систему из клиента (`requirements/scenarios.md`).
Цель этого PRD — задать проверяемые требования для двух горизонтов:

- **MVP-1 «Тёмный лес»** — первый играбельный инкремент: вход в регион → встреча →
  бой → нарратив, 30 ходов без поломки мира, соло и группой 2–6, через Telegram-бота;
  мир живёт между сессиями (фоновые события глобального и регионального GM);
- **целевое видение** — платформа с Living Worlds, эволюцией законов через пробой,
  культивацией и Планами, городами, генерацией миров и авторскими инструментами —
  как основа для декомпозиции на эпики.

### 1.1. Цели

| # | Цель | Как проверяем |
|---|---|---|
| G-1 | Сквозной играбельный цикл существует и проходит автоматически | e2e-тест «Тёмный лес» на поднятом стеке (US-002) |
| G-2 | Мир не ломается в сессии | 30 ходов без противоречий состояния (NFR-012) |
| G-3 | Мир переживает рестарт | восстановление из снапшота + replay без повторных вызовов LLM (US-011) |
| G-4 | В мир можно играть вдвоём-вшестером | групповая сессия из 3 игроков (US-006, US-007) |
| G-5 | Есть эталонный клиент | Telegram-бот (US-008) |
| G-6 | Одна архитектура GM — рой агентов | все GM (глобальный, региональный, персональный) — экземпляры Agent GM Core по блупринтам; legacy `GMInstance` выведен или рендер (FR-010, FR-120, US-037) |
| G-7 | Мир живёт без игроков | между сессиями в регионе происходят фоновые события, видимые игроку при входе (FR-124…FR-127, US-036) |
| G-8 | Основа для целевого видения зафиксирована | требования целевого горизонта (§4.2) достаточны для брифов эпиков; контракт пробоя законов зарезервирован (FR-045) |

### 1.2. Не-цели (этого цикла и MVP-1)

- Массовый мультиплеер, шардирование, «1000 игроков/шард» — снято из целей и NFR.
- Графический или веб-UI (`hasUI=false`); клиент — бот и API.
- Монетизация, публичный запуск, внешние разработчики фронтов.
- Обучение нейросетевых Entity-Actor (TinyML) в MVP; Entity-Actor как автономная сущность — эпик E-A (фоновая жизнь мира через GM-рой — уже в MVP-1, см. FR-124).
- Реальный фильтр контента 18+ («стены») и аутентификация внешних игроков в MVP (только точки вставки; в MVP-1 фильтр проверяет лишь категорию (a) — FR-050, решение G1).
- Механика пробоя законов (фаза пробоя, напряжение, ревью) в MVP-1 — только контракт (FR-045, эпик E-B).
- Очистка git-истории (`git filter-repo`) — отдельная задача по явной команде пользователя.
- Юридическая экспертиза (152-ФЗ, 436-ФЗ, 243-ФЗ) — только ориентиры из `domain-notes.md`.

---

## 2. Пользователи и роли

| Роль | Кто это (MVP-1) | Что нужно | Истории |
|---|---|---|---|
| **Игрок (соло)** | Сам разработчик; затем 2–6 тестеров | Войти, действовать, получать механический результат и нарратив, чувствовать память и жизнь мира | US-001…US-005, US-009, US-036 |
| **Игрок в группе** | 2–6 тестеров в одном scope `group` | Видеть общую картину, действовать в раундах без рассинхрона | US-006, US-007 |
| **Оператор** | Разработчик на своей машине с GPU | Поднять стек одной командой, видеть здоровье, нагрузку LLM и отчёт по сессии, восстановить после сбоя, не утечь секретами | US-010…US-014, US-038 |
| **Автор блупринтов / мира** | = оператор (допущение A-4, OQ-R-17/OQ-P-11): файлы в Git + CLI, без UI | Описать регион/NPC/тики блупринтом, получить ошибки валидации, увидеть трассу решений агента | US-015, US-016 |
| **Исследователь / разработчик** | = разработчик | Воспроизводимые прогоны (seed + записанные LLM-выводы), сравнение вариантов, аудит промптов | US-017…US-019, US-037 |
| **Ревьюер** (целевое) | Автор мира как *человек* в контуре пробоя законов, канона и эволюции правил | Видеть предложения изменений законов/правил/канона, утверждать или откатывать | US-020, US-021, US-023 |
| **Страж** (автомат) | Запрет Мира — автоматический валидатор предложений LLM и действий против законов/канона (DR-17) | — | FR-042, BR-02 п. 7 |
| **Внешний разработчик фронта** (целевое) | Не в MVP | Публичный API с аутентификацией | US-030 |

Термины «страж» (автомат) и «ревьюер» (человек) разведены по DR-17; в v0.1 они употреблялись как одна роль.

Аккаунт и персонаж в MVP-1 не разделяются: один внешний аккаунт (Telegram) ↔ один
**живой** персонаж ↔ один `player_id`; после смерти персонажа тот же аккаунт создаёт
нового персонажа с новым `player_id` (FR-023, A-7, DR-06). Разделение аккаунт/персонаж
как отдельных сущностей — целевое (US-030).

---

## 3. Сценарии (кратко; детали в `user-stories.md`)

| Сценарий | Роль | Инкремент | История |
|---|---|---|---|
| SC-01 Регистрация, согласие 18+ и вход | игрок | MVP-1 | US-001, US-008 |
| SC-03 «Тёмный лес»: вход → встреча → бой → нарратив, 30 ходов | игрок | MVP-1 | US-002…US-005 |
| SC-05 Совместная игра группы (3 игрока, раунды) | группа | MVP-1 | US-006, US-007 |
| Бот как эталонный клиент (Telegram) | игрок | MVP-1 | US-008 |
| Псевдонимизация и уведомление об ИИ | игрок | MVP-1 | US-009 |
| Фоновая жизнь мира между сессиями | игрок | MVP-1 | US-036 |
| Рой GM: иерархия событий глобальный → регион → персональный | исследователь | MVP-1 | US-037 |
| SC-09 Запуск, здоровье, восстановление, деградация без LLM, секреты, отчёт по сессии | оператор | MVP-1 | US-010…US-014, US-038 |
| SC-07 Блупринт региона, точка вставки фильтра | автор/оператор | MVP-1 | US-015, US-016 |
| SC-10 Воспроизводимый прогон, запись LLM-выводов, фича-флаг GM | исследователь | MVP-1 | US-017…US-019 |
| SC-08 Пробой законов, канон, эволюция правил | ревьюер | целевое (контракт — MVP-1) | US-020…US-023 |
| SC-04 Культивация, Планы, Ω | игрок | целевое | US-024, US-025 |
| Города/экономика, GM города | игрок | целевое | US-026 |
| SC-06 Генерация мира, законы, онтологии | автор | целевое | US-027…US-029 |
| SC-02 Свободный текст → намерение | игрок | целевое | US-032 |

---

## 4. Объём по инкрементам

### 4.1. MVP-1 «Тёмный лес» (обязательное)

Один мир, один регион «Тёмный лес» (`dark-forest-01`), один тип NPC (волк), один
игрок или группа 2–6. Цикл: вход в регион → активация GM региона по блупринту →
встреча с волком (Task-агент) → бой (детерминированная механика, правила v0.1 как
данные) → нарратив (LLM, асинхронно, через персональный GM игрока) → последствия
(HP, трофей, смерть) → выход. 30 ходов подряд без поломки мира.

**Рой GM в MVP-1 (обязательно):** глобальный GM мира (погода и события мира по
тикам), GM региона «Тёмный лес» (наблюдает территорию, порождает события и встречи
по тикам — и с игроками, и без), персональный GM каждого игрока (события в
видимости игрока → его нарратив). GM города — целевое (E-D).

**Фоновая жизнь в MVP-1:** глобальный и региональный GM тикают по расписанию без
игроков в дешёвом режиме (без LLM или малая модель, лимит вызовов); мир меняется
между сессиями; игрок при входе видит последствия.

Клиент — Telegram-бот (Discord — следующий инкремент). Оператор поднимает стек
одной командой на целевой машине (RTX 4090 24 ГБ, 128 ГБ RAM, i9-13900),
восстанавливает после рестарта, работает без LLM в деградированном режиме, получает
отчёт по сессии. Исследователь воспроизводит прогон по seed и записанным выводам LLM.

**Контракты, зарезервированные в MVP-1 без механики:** пробой законов (`laws@v1`,
`laws_version`, типы событий пробоя — FR-045), точка вставки фильтра входа и выхода
(FR-050, FR-056), Entity-Actor (FR-110).

### 4.2. Целевое видение — эпики-кандидаты (после MVP-1)

| Кандидат | Содержание | Требования |
|---|---|---|
| E-A Living Worlds / Entity-Actor | Автономное поведение *сущностей* (Entity-Actor как расширение через зарезервированные контракты), эволюция правил с ревью человека до применения; расширение фоновой жизни (LOD, бюджеты, Monitor-агенты аномалий) | FR-110, FR-115, FR-017, US-022, US-023 |
| E-B Эволюция законов через пробой | Механика BR-02 целиком: напряжение закона, фаза пробоя, автопроверка стража, `pending_review`, окно ревью, откат-ретрокон; промоушен канона | FR-040…FR-044, FR-046, BR-01, BR-02, US-020, US-021 |
| E-C Культивация и Планы | Гибрид: ступени (реалмы) с бутылочными горлышками внутри мира + «портрет дао» для перехода между Планами; Ω — финал персонажа | FR-111, FR-112, US-024, US-025 |
| E-D Города и экономика | GM города (Domain-уровень роя), минимальный леджер (валюта, предметы — сущности), квесты, репутация | FR-113, FR-120 (город), US-026 |
| E-E Генерация миров | Законы вселенной (immutable/default, версии), онтологии-дуальности, генерация мира по описанию/seed с проверкой наследования законов | FR-114, US-027…US-029 |
| E-F Авторские инструменты | API/CLI автора, горячая загрузка блупринтов, трасса решений агента, ревью канона и пробоев | FR-091…FR-094, US-015 (расширение), US-021 |
| E-G Наблюдаемость и операции | Метрики (Prometheus/Grafana или коллектор `analytics.*`), алерты, бюджеты LLM, CI с интеграционными тестами, LOD/бюджет на действие | FR-082, FR-072, FR-017, US-033, US-034 |
| E-H Контент и доступ | Реальный фильтр входа/выхода 18+, аутентификация, разделение аккаунт/персонаж, публичный API, полное право на забвение, второй мессенджер (Discord) | FR-053, FR-054, FR-062, FR-004 (Discord), US-030, US-031, US-035 |

Нумерацию и границы эпиков задаёт планирование; здесь — только связь с требованиями.

### 4.3. Вне объёма (обоих горизонтов, до пересмотра видения)

MMO-масштаб и шардирование; графический UI; монетизация; облачный деплой/K8s;
обучение TinyML-акторов на данных игроков; мультиязычный контент (английский —
только вторичный язык промптов при необходимости); `git filter-repo`.

---

## 5. Функциональные требования

Приоритеты MoSCoW заданы отдельно для MVP-1 и для целевого горизонта. «Won't» в
MVP-1 означает «сознательно не делаем в этом инкременте». Источник: `р1…р5` —
раунды discovery/уточнений в `journal.md`; `доп.` — допущения оркестратора; `INV-NN`,
`SC-NN` — `inventory.md`, `scenarios.md`; `DN §N` — `domain-notes.md`; `DR-NN` —
доменное ревью; `PA §N` — `metrics.md`.

### 5.1. Доступ игрока и клиент

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-001 | Игрок создаёт персонажа в существующем мире, **вводя имя персонажа явно** (данные профиля мессенджера для имени не используются — DR-10), и при повторном входе получает сохранённое состояние (HP, инвентарь, позиция, статус жив/мёртв). Стартовая позиция — `outside:{world}` («Опушка»: позиция вне регионов, без NPC; DR-19). Вход в несуществующий мир отклоняется с понятной ошибкой | Must | Must | SC-01, INV-06, DR-10, DR-19 |
| FR-002 | Игрок передаёт действие как структурированный тип с опциональным свободным текстом. Словарь MVP-1 (р4, DR-17): игровые `enter <регион>`, `leave` (регион → `outside`), `look`, `status`, `attack <цель>`, `flee`, `say <текст>`, `rest`; групповые `group create`, `group join <id>`, `group leave`; служебные `/start`, `/help`, `/forget`. Неизвестный тип или несуществующая цель отклоняются без изменения состояния и без вызова LLM | Must | Must | р4 (OQ-R-07 решено), DN §5, DR-17 |
| FR-003 | Нарратив и результаты доставляются адресно: игрок получает только вывод своего персонального GM (события своего scope и адресованные ему события уровней выше); чужие scope не видны | Must | Must | INV-17, SC-02, р5 |
| FR-004 | Эталонный клиент — **Telegram-бот** (р4): регистрация, выбор мира, действия командами, получение механики и нарратива отдельными сообщениями, согласие и уведомление об ИИ (FR-009), группа. Интерфейс бота к game-service единый; Discord-бот — следующий инкремент (E-H) поверх того же интерфейса | Must | Must | р1, р4, OQ-P-04 |
| FR-005 | Групповая сессия 2–6 игроков: создание группы, присоединение (только вне активной встречи), выход; группа = один scope `group` с лидером (BR-13); действия участников обрабатываются одним GM региона/Task-агентом с единым состоянием сцены, нарратив раунда — один на scope | Must | Must | р1, DR-03, DR-12 |
| FR-006 | Игрок видит список доступных миров и своё текущее состояние по запросу (`look`/`status`) | Should | Must | SC-01 |
| FR-007 | Свободный текст игрока распознаётся в намерение (Intent Recognition) с подтверждением перед необратимыми действиями | Won't | Should | SC-02, OQ-D-10 |
| FR-008 | Аутентификация внешних клиентов (токен/ключ), разделение аккаунта и персонажа, публичный API для сторонних фронтов | Won't | Should | INV-06, INV-37, OQ-A-14 |
| FR-009 | **Согласие и подтверждение возраста.** Первый `/start` показывает единое сообщение: контент создаётся ИИ; игра 18+ — подтвердите возраст; введённый текст обрабатывается для генерации (при облачном провайдере — передаётся ему); `/forget` удаляет связку. Создание персонажа возможно только после явного подтверждения; даты `notice_shown_at`, `consent_at`, `age_confirmed_at` пишутся в связку (FR-060). Повторный показ — по `/help` и после 30 дней неактивности (Should) | Must | Must | DR-09, PA §8 п. 10, DN §10 |

### 5.2. Игровой цикл и GM

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-010 | **Agent GM Core — единственная архитектура GM.** Все решения по миру и scope принимаются агентами роя по блупринтам (§5.13); иных путей принятия решений (legacy `GMInstance`, Entity-Actor) в активном цикле нет | Must | Must | р1, р5, OQ-R-13/OQ-P-10/OQ-A-02 |
| FR-011 | GM региона (Domain-агент) активируется по блупринту при создании мира/регистрации региона и живёт постоянно (тики без игроков — FR-124); вход игрока не пересоздаёт контекст, а добавляет игрока в наблюдаемый scope | Must | Must | AGM US-001, INV-27, р5 |
| FR-012 | При обнаружении NPC в радиусе восприятия игрока GM региона порождает Task-агента встречи с контекстом {игрок(и), NPC, регион}; встреча завершается смертью NPC или выходом всех игроков из боя (`agent.child_resolved`) | Must | Must | AGM US-002, INV-28, DR-03 |
| FR-013 | Двухфазный конвейер: Phase 1 — решение (строгий JSON, механика через Rule Engine), Phase 2 — нарратив (асинхронно). Результат Phase 1 публикуется как событие до начала Phase 2. `combat.decided` несёт `correlation_id`, `trigger.event.id`, `gm_path: agent\|legacy`, `phase1_mode: rules\|llm`, `lod?` (PA §8 п. 7). Для scope `group` Phase 2 запускается по закрытию раунда и описывает все действия раунда одним текстом (DR-12) | Must | Must | AGM US-002/003, INV-28, PA, DR-12 |
| FR-014 | narrative-orchestrator становится тонким слоем рендера текста либо выводится из эксплуатации; на время миграции — фича-флаг `agent_mode` с откатом; после миграции флаг удаляется | Must | Must | р1, INV-34 |
| FR-015 | При недоступности или таймауте LLM цикл продолжается: механика применяется, нарратив заменяется шаблонным текстом с пометкой деградации | Must | Must | SC-09, AGM US-003 |
| FR-016 | Персональные GM и Task-агенты без игроков переходят в сон/удаляются по TTL и не потребляют LLM; для scope `group` TTL отсчитывается от выхода последнего участника (`idle`-участник не считается вышедшим — DR-24). Глобальный и региональные GM по TTL не удаляются (FR-124) | Must | Must | SC-03 шаг 5, р5, DR-24 |
| FR-017 | LOD/бюджет на действие: желаемая детализация задаётся значимостью события, потолок — бюджетом LLM; понижение LOD не теряет критических событий | Won't (MVP-1: фиксированные LOD — бой LOD 1, фоновые тики LOD 1–2) | Should | AGM US-005, OQ-D-11 |
| FR-018 | Механический результат — источник истины: нарратив не может изменить HP, инвентарь, позицию, статус; расхождение нарратива с механикой — дефект | Must | Must | DN §5, BR-05 |

### 5.3. Механика

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-020 | Правила боя «Тёмного леса» v0.1 описаны как данные (`rules/dark-forest.yaml` или эквивалент), не в Go-коде агента: атака (d20 + atk ≥ def → урон dmg; нат. 20 — урон ×2; нат. 1 — промах), защита, HP, смерть, бегство, трофей, отдых, детерминированный выбор цели NPC. Числа — приложение A (предложение DR-04, уточняются после первых прогонов). Формат правил поддерживает `damage_formula` (кубик), а не только константу | Must | Must | INV-19, SC-03, DR-04 |
| FR-021 | Генератор случайных чисел детерминирован: `seed_i = hash(event_id, roll_index)`, где `roll_index` — порядковый номер броска внутри события-причины; разные броски одного события независимы; один и тот же (event_id, roll_index) даёт одинаковый результат | Must | Must | р2, DN §4, DR-05 |
| FR-022 | Каждый бросок публикуется как событие `dice.rolled` (`seed`, `formula`, `result`, `roll_index`, `correlation_id`, `trigger.event.id`, `replay: bool`) | Must | Must | р2, OQ-D-01, PA §4.3 |
| FR-023 | Смерть персонажа — терминальное состояние персонажа: дальнейшие действия отклоняются с предложением создать нового персонажа; при статусе `dead` повторная регистрация того же внешнего аккаунта создаёт **новый `player_id`** (связка обновляется; старый `player_id` остаётся в истории и графе, исключается из scope и целей NPC — DR-26) | Must | Must | SC-03, DR-06, DR-26 |
| FR-024 | Действие `rest` вне встречи восстанавливает HP до `hp_max` и занимает ход; во встрече недоступно (отклоняется без изменения состояния). Иных источников восстановления HP в MVP-1 нет; нарратив не лечит (FR-018) | Must | Must | DR-04 |
| FR-025 | **Раунд scope.** В `solo` раунд = ход. В `group` раунд закрывается, когда все `active` участники сделали по действию **или** истёк таймаут раунда (блупринт; по умолчанию 60 с; в автотесте — явное `round.close`). Порядок действий внутри раунда — по времени приёма; NPC действует один раз в раунд после игроков; молчащий участник получает авто-действие `defend`, после двух подряд пропущенных раундов — статус `idle` (не действует, не ждётся, не цель), возвращается в `active` первым действием. Событие `round.closed` записывается (BR-04) | Must | Must | DR-03, DR-12, DR-13 |

### 5.4. Состояние, память, восстановление

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-030 | entity-manager — единственный писатель состояния сущностей. Остальные компоненты публикуют команды/предложения (`entity.update.proposed`); entity-manager применяет, разрешает конфликты (версия сущности) и публикует факт `entity.updated` (`correlation_id`, `entity{entity{id,type}}`, `version`, `changed[]`, `timestamp`) | Must | Must | р2, OQ-A-03/OQ-D-08, PA §4.3 |
| FR-031 | Снапшот состояния — источник истины при восстановлении; снапшоты пишутся периодически и по завершении сессии; включают `laws_version` (FR-045); после старта сервис загружает последний снапшот и догоняет события после него; итог восстановления публикуется как `analytics.replay.completed` (FR-087) | Must | Must | р2, DR-11, PA §8 п. 6 |
| FR-032 | Record-replay: каждый вызов LLM записывается как событие `llm.output` **до** использования ответа. Обязательные поля: `provider`, `model`, `params`, `prompt_hash`, `response_raw` (кроме статуса `quarantined` — FR-050), `validation_status`, `correlation_id`, `caused_by`, `laws_version`, `agent{id, level, blueprint}`, `phase: decision\|narrative\|tick\|other`, `attempt`, `latency_ms`, `tokens{prompt, completion}`, `cost_usd` (0 для Ollama), `gm_path`, `lod?`, `replay: bool`. При replay LLM не вызывается, читается записанное | Must | Must | р2, OQ-D-01, DR-11, PA §8 п. 2 |
| FR-033 | Потребители событий идемпотентны: повторная доставка события с тем же `event_id` не меняет состояние второй раз | Must | Must | DN §4 |
| FR-034 | Ссылки на сущности в выводах LLM проверяются против реестра существующих `entity_id`; отклонённый вывод записывается как `llm.output.rejected` с `reason` из перечисления: `unknown_entity`, `player_agency` (BR-06), `level_violation` (BR-16), `schema_invalid`, `language` (NFR-090), `filter_error` (FR-050), `budget_exceeded` (FR-072), `other`; для `unknown_entity` — отсутствующая ссылка | Must | Must | DN §2, PA §8 п. 3 |
| FR-035 | Семантическая память индексирует события (включая фоновые) и отдаёт контекст агенту; каждый факт помечен источником (`authored` / `validated` / `generated`) и слоем консистентности | Should | Must | OQ-D-09, INV-12 |
| FR-036 | Все публикуемые типы событий зарегистрированы в реестре со схемой; событие неизвестного типа отклоняется на входе в шину. Типы `world.law_breach.*` (BR-02 п. 11) регистрируются в MVP-1 без обработчиков | Should | Must | OQ-A-08/09, DR-11 |
| FR-037 | **Мир живёт и без игроков (р5; допущение A-3 отклонено):** глобальный и региональные GM тикают по расписанию независимо от присутствия игроков (FR-124…FR-127); персональные GM и Task-агенты живут только при игроках (FR-016). Автономное поведение отдельных сущностей (Entity-Actor) — целевое (E-A, FR-110) | Must | Must | р5 (OQ-R-15 решено) |

### 5.5. Консистентность и законы мира

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-040 | Три слоя консистентности (BR-01): законы мира, канон, сиюминутное. В MVP-1 законы (`laws@v1`) и канон «Тёмного леса» задаются в блупринте и попадают в промпты слоями; промоушен фактов в канон — целевое | Must (слои в промптах) | Must | р2, OQ-R-11 |
| FR-041 | «Пробой законов»: неординарное событие (`world.law_breach.proposed`), изменяющее законы мира, применяется только через механизм BR-02: инициаторы, напряжение закона, автопроверка стража, `pending_review`, окно ревью, откат-ретрокон, версии законов | Won't (механика) | Must | р2 (ключевое доменное требование), DR-01 |
| FR-042 | Страж целостности (Запрет Мира) — автомат: проверяет предложения LLM и действия против законов и канона; нарушение превращается в последствие в духе мира (не «наказание»), с видимой игроку причиной (`reason`); выполняет автопроверку пробоя (BR-02 п. 7). В MVP-1 роль стража ограничена валидацией FR-034/BR-06/BR-16 и инвариантами NFR-020 | Should (валидация) | Must | INV-10, DN §5, OQ-D-12, DR-17 |
| FR-043 | Промоушен факта в канон: предложение → валидация (страж или ревьюер) → запись с версией; конфликт с существующим каноном отклоняется или оформляется явным событием ретрокона | Won't | Must | OQ-D-09, DN §1 |
| FR-044 | Слияние scope (merge) с конфликтующими канонами не выполняется автоматически: конфликт фиксируется и передаётся на ревью; при слиянии scope с разными `laws_version` действует старшая версия | Won't | Should | DN §1, DR-01 |
| FR-045 | **Контракт пробоя в MVP-1 (DR-11):** у мира есть документ законов `laws@v1` (из блупринта); `laws_version` присутствует в контексте агента, в каждом `llm.output`, в снапшоте и в событии `world.laws.changed`; типы `world.law_breach.proposed / rejected / applied / review_decided / rolled_back` и `world.laws.changed` зарегистрированы (FR-036); инварианты NFR-020 оформлены как первые законы `laws@v1`; ручная правка законов автором создаёт `laws@vN+1` со статусом `approved` | Must | Must | DR-11, A-8 |
| FR-046 | **Разведение механизмов изменения правил (DR-02):** пробой (BR-02) меняет *законы мира* и `default`-законы вселенной; `immutable`-законы вселенной не пробиваются (допущение A-10); *числовые правила механики* (формулы Rule Engine) пробой не меняет — катаклизм, требующий числовых изменений, порождает предложение по FR-115 с ревью *до* применения; эволюция правил (FR-115) не меняет законы мира | Won't (контракт) | Must | DR-02 |

### 5.6. Контент и безопасность

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-050 | **Точка вставки фильтра на выходе** (`NarrativeFilter`): единый интерфейс, через который проходит весь текст перед доставкой игроку; при ошибке реализации — fail-closed (шаблон с пометкой, ошибка в логе); журнал вызовов. **Реализация в MVP-1 (решено на G1, OQ-D-16):** минимальная проверка только категории (a) BR-08 по словарю/регулярным выражениям, fail-closed. Задержанный фильтром текст игроку не доставляется (шаблон с пометкой), соответствующий `llm.output` записывается со статусом `quarantined` **без** `response_raw`, инцидент — в журнал фильтра. Остальные категории BR-08 в MVP-1 не фильтруются (решение р3 «без фильтров» для приватной игры сохраняется; полный фильтр — FR-053). Словарь категории (a) — в конфигурации, не в коде (как FR-051); ложных срабатываний на золотом наборе — 0 (NFR-065) | Must | Must | р3, DR-08, G1 |
| FR-051 | Список абсолютных запретов (BR-08) включён в системный промпт каждого нарративного вызова в **короткой позитивной формулировке** («мир вымышлен, персонажи — взрослые, реальные инструкции не даём») без перечисления «как именно нельзя» (DR-07: перечисление тем в промпте малой модели повышает их появление); полный список — в контракте фильтра. Оба хранятся в конфигурации, не в коде | Must | Must | р3, DR-07 |
| FR-052 | Каждый нарративный ответ помечен `generated_by: llm` (шаблон — `rules`); бот показывает уведомление «контент создан ИИ» при первом взаимодействии (в составе FR-009) | Must | Must | р3, OQ-D-14 |
| FR-053 | Реальный фильтр входа (текст игрока) и выхода (нарратив) по политике 18+ «стены»: приватная сессия — только абсолютные запреты; групповой/публикуемый контент — полный фильтр | Won't | Must | р3, DN §9 |
| FR-054 | Журнал инцидентов контента и канал жалоб; протокол реагирования на признаки реального намерения самоповреждения в тексте игрока — ответ вне игры (контакты помощи), не нарратив (BR-08 (e), принято на G1; сам протокол — целевое) | Won't | Should | DN §9, DR-07, G1 |
| FR-055 | Текст игрока и имена сущностей попадают в промпт как данные (экранирование/разметка), а не как инструкции; попытка инъекции не меняет механику и не запускает пробой законов (фаза пробоя не читает текст игрока — BR-02 п. 4) | Should | Must | DN §2, DR-01 |
| FR-056 | **Точка вставки фильтра на входе** (`InputFilter`) — тот же принцип, что FR-050: единый интерфейс для текста игрока (`say`, имя персонажа) перед публикацией в шину; MVP-1 — пустышка с журналом. Добавляется сейчас, потому что в группе текст `say` виден другим игрокам (UGC), а переделка контракта действия позже дорога | Should | Must | DR-15 |

### 5.7. Персональные данные

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-060 | Псевдонимизация на входе game-service: во внутренние события, хранилища и логи попадает только `player_id`; внешний идентификатор (Telegram ID, username, имя профиля) хранится в отдельном хранилище связок с полями `external_platform`, `external_id`, `player_id`, `notice_shown_at`, `consent_at`, `age_confirmed_at`. **Бот не использует данные профиля мессенджера (имя, username, фото) ни для чего, кроме доставки**; имя персонажа вводится игроком явно; при совпадении имени персонажа с username бот предупреждает и просит подтвердить (DR-10) | Must | Must | р3, OQ-D-04, DR-10, PA §8 п. 10 |
| FR-061 | Связка «внешний ID ↔ player_id» удаляема по запросу игрока (`/forget`) или оператора; после удаления восстановить внешний ID по `player_id` невозможно; игровая история по `player_id` остаётся | Must | Must | р3 |
| FR-062 | Полное право на забвение (удаление/шреддинг истории игрока в лог-хранилищах), retention по топикам | Won't | Should | DN §10.1 |

### 5.8. LLM-провайдеры и стоимость

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-070 | Абстракция провайдера LLM: единый интерфейс, реализации Ollama (по умолчанию) и облачные (OpenAI/Anthropic/DeepSeek); выбор — конфигурацией без изменения кода; облачные ключи — только через переменные окружения. Модель задаётся **на уровень роя/фазу** (блупринт): предложение — `qwen3:8b` для Phase 1 и фоновых тиков, `qwen3:30b-a3b` (или `qwen3:32b`) для Phase 2; обе загружены одновременно (NFR-076) | Must (интерфейс + Ollama) | Must (+ облако) | р2, р5, OQ-A-05/OQ-P-07/OQ-D-13 |
| FR-071 | Учёт вызовов LLM и токенов на ход игрока, на раунд, на сессию и на фоновые тики (по уровню агента); доступен оператору | Must | Must | доп. (OQ-R-10), р5 |
| FR-072 | Лимит частоты/бюджета вызовов LLM с предупреждением при превышении (`llm.output.rejected`, `reason=budget_exceeded`); отдельный лимит на фоновые тики (FR-125); при облачном провайдере — денежный лимит | Should (фоновый лимит — Must) | Must | SC-09, р5 |

### 5.9. Эксплуатация и наблюдаемость

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-080 | Весь стек MVP-1 поднимается одной командой на одной машине с GPU; без GPU стек стартует в режиме деградации (FR-015) | Must | Must | р1, SC-09 |
| FR-081 | У каждого сервиса есть `/health`, отражающий доступность его зависимостей | Must | Must | SC-09 |
| FR-082 | Оператор видит: глубину очередей событий, латентность и число вызовов LLM (по фазам и уровням роя), активные агенты/scope, фоновые тики за период, ошибки за период | Should | Must | SC-09, INV-38 |
| FR-083 | Секреты не хранятся в репозитории: `.env`, `.mcp.env` исключены из индекса, есть `.mcp.env.example`; отозванные токены не используются | Must | Must | р3, OQ-A-11 |
| FR-084 | **Сквозная трасса.** Корреляционный идентификатор действия игрока (или фонового тика) проходит через всю цепочку событий: действие/тик → решение → броски → `llm.output` → `entity.updated` → нарратив. Требование — единый детерминированный путь поля во всех событиях, проставляемый game-service (действие) или агентом (тик) и копируемый всеми звеньями; конкретный путь (предложение PA: `payload.correlation_id` + `trigger.event.id`) — решение архитектора (OQ-A-09) | Must | Must | DN §5, PA §8 п. 1, NFR-031 |
| FR-085 | **Сессии и род актора.** game-service ведёт сессии: сессия — серия ходов в одном scope от первого принятого действия до выхода всех участников, смерти всех участников или простоя ≥ 30 мин (допущение A-12); `session.id = "{scope.id}:{started_at_unix}"`; поле `actor_kind: human\|ci\|sim` задаётся клиентом при регистрации/действии (по умолчанию `human`; бот — `human`, тест-харнесс — `ci`, нагрузка — `sim`). game-service публикует `analytics.session.started`, `analytics.session.ended`, `analytics.turn.completed` с полями по `metrics.md` §4.2 (тайминги, статус хода, `gm_path`, `phase1_mode`, `generated_by`, `delivery`). В аналитике нет внешних ID, текста игрока и нарратива | Must | Must | PA §0, §4.2, §8 п. 4 |
| FR-086 | **Нарушения консистентности как событие.** Проверка инвариантов после хода (тест-харнесс/страж) и сверка снапшота с агрегатом лога публикуют `analytics.consistency.violated {violation{code, layer, severity, detected_by, entity?, expected?, actual?}}`; список инвариантов «Тёмного леса» — часть блупринта/правил (NFR-020); значения — числа/идентификаторы, не текст | Must | Must | PA §8 п. 5 |
| FR-087 | Завершение восстановления/replay публикует `analytics.replay.completed {run_id, mode, snapshot_id, events_replayed, llm_calls, dice_rolled_new, duration_ms, state_hash_before?, state_hash_after, identical?, events_hash_match?, incomplete_record}` | Must | Must | PA §8 п. 6 |
| FR-088 | **Отчёт по сессии и журнал инцидентов оператора.** CLI `session-report` считает сводку сессии из событий шины (`analytics.*`, `llm.output`, `dice.rolled`) и дописывает строку в `ops/metrics/sessions.csv`; флаг `--audit` запускает сверку снапшота с логом (FR-086), `--json` сохраняет артефакт прогона (FR-101). Оператор ведёт `ops/metrics/incidents.csv` (`date, session_id, correlation_id, kind: world_break\|defect\|content\|other, manual_fix, description`) — единственный источник «ручной правки состояния» для критерия S7. Топик/маршрут `analytics.*` (отдельный `analytics_events` vs `system_events` с игнорированием в replay) — решение архитектора (OQ-A-07/08) | Should (CSV — Must) | Must | PA §6.2, §8 п. 8, 11 |

### 5.10. Авторские инструменты

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-090 | Регион, NPC, законы `laws@v1`, правила боя и расписание тиков описываются Markdown/YAML-блупринтом (один утверждённый формат); валидатор сообщает об ошибках с указанием поля; новый регион (со своим GM региона) добавляется без изменения Go-кода | Must | Must | SC-07, INV-35, р5 |
| FR-091 | Блупринт подхватывается работающей системой без перезапуска | Should | Must | SC-07 |
| FR-092 | Трасса решения агента (входные события, промпт-хэш, решение, инструменты, уровень агента, `laws_version`) доступна автору по `correlation_id` | Should | Must | SC-07 |
| FR-093 | CLI автора/оператора: создать мир из блупринтов, зарегистрировать регион, посмотреть состояние scope и активных агентов роя | Could | Should | доп. (OQ-R-17) |
| FR-094 | API автора: CRUD вселенных, законов (immutable/default, версии), онтологий-дуальностей, блупринтов; ревью канона и пробоев | Won't | Should | US-001/002 (старые), INV-01/02 |

### 5.11. Исследование и воспроизводимость

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-100 | Сценарий запускается с фиксированным seed и записанными `llm.output`; повторный прогон даёт идентичные броски и события без сетевых вызовов LLM; время и таймеры при replay берутся из записанных событий (`tick.fired`, `round.closed`), не из `time.Now()` | Must | Must | SC-10, р2, DR-13 |
| FR-101 | Артефакт прогона: JSON-сводка `session-report --json` (`ops/metrics/sessions/<session_id>.json`): список событий, нарративы, метрики (латентность, токены, вызовы по фазам/уровням) — сохраняется и сравним с другим прогоном | Should | Must | SC-10, PA §8 п. 9 |
| FR-102 | Сравнение двух вариантов (промпт/модель/провайдер) на одном сценарии с одинаковым seed | Could | Should | SC-10 |

### 5.12. Целевые домены (для брифов эпиков)

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-110 | Living Worlds: отдельные сущности действуют автономно (Entity-Actor как расширение через зарезервированные контракты), поверх фоновой жизни роя GM (FR-124); архитектура MVP-1 эти контракты резервирует | Won't (контракты — Should) | Must | р2, р5 |
| FR-111 | Культивация — гибрид: ступени (реалмы) внутри мира с бутылочными горлышками (накопление + постижение + испытание с риском), убывающий прирост внутри ступени | Won't | Must | р3, OQ-D-02 |
| FR-112 | Переход между Планами — по «портрету дао» (LLM-оценка + числовой минимум) через испытание с ограниченным набором исходов; достижение Плана Ω — финал персонажа (ретирмент). LLM-оценка портрета — единственное явное исключение из BR-05 (enum-исход, `llm.output`, максимальный штраф) | Won't | Must | р3, OQ-D-02/06, DR-16 |
| FR-113 | Города: GM города (Domain-уровень роя), минимальный леджер (валюта и предметы — сущности), LLM предлагает награды в лимитах, движок выдаёт; квесты и репутация — из состояния | Won't | Should | OQ-D-07, р5 |
| FR-114 | Генерация мира по описанию/seed из законов вселенной и онтологий с проверкой наследования законов; результат просматривается и правится автором до открытия игрокам | Won't | Should | SC-06, INV-01…04 |
| FR-115 | Эволюция правил: обнаружение устойчивых аномалий (Monitor-агент), предложение правила, **ревью человека до применения**, журнал и откат; нарративное объявление изменения игрокам | Won't | Should | INV-21, SC-08, DR-02 |

### 5.13. Рой GM и фоновая жизнь мира (р5)

Уровни в терминах кода `shared/agent.AgentLevel`: `global`, `domain`, `task`,
`object`, `monitor`. Соответствие ролей роя — таблица:

| Роль GM | Уровень кода | Кол-во | Что наблюдает | Что порождает | MVP-1 |
|---|---|---|---|---|---|
| Глобальный GM | `global` | 1 на мир | все события мира (сводки регионов) | события мира: погода, время суток/сезон, эпохи, редкие явления; трансляция пробоя (целевое) | **обязателен** |
| GM региона | `domain` | 1 на регион | события своей территории, игроков в ней, события уровня выше | локальные события региона, спавн/движение NPC, Task-агенты встреч | **обязателен** («Тёмный лес») |
| GM города | `domain` | 1 на город | город, его NPC, экономику | квесты, события города | целевое (E-D) |
| Task-агент встречи/квеста | `task` | по событию | встреча {игроки, NPC} | `combat.decided`, `agent.child_resolved` | обязателен (в составе FR-012) |
| Персональный GM игрока | `monitor` (долгоживущий, TTL = сессия игрока) либо `task` — выбор архитектора | 1 на игрока | события в радиусе восприятия игрока + адресованные его scope события уровней выше | нарратив для игрока (Phase 2), контекст памяти игрока; **никаких изменений состояния** | **обязателен** |
| Monitor аномалий (Evolution Watcher) | `monitor` | 1 на мир | статистика событий | предложения правил (FR-115) | целевое (E-A) |

| ID | Требование | MVP-1 | Целевое | Источник |
|---|---|---|---|---|
| FR-120 | **Рой GM.** GM реализуется как иерархия агентов на одной архитектуре Agent GM Core по таблице выше; каждый агент — экземпляр по своему блупринту с `max_instances = 1` на (уровень, scope). Обязательны в MVP-1: глобальный GM, GM региона «Тёмный лес», персональный GM каждого игрока; GM города — целевое | Must | Must | р5 |
| FR-121 | **Иерархия событий.** События идут вверх (регион → глобальный: сводки, аномалии) и вниз (глобальный → регион → персональный: погода, эпоха, катаклизм) только через шину; каждый агент подписан на события своего scope и родителя (`parent` в блупринте); прямых вызовов между агентами нет. Событие уровня выше доходит до игрока через его персональный GM, а не напрямую | Must | Must | р5 |
| FR-122 | **Состояние только своего уровня.** Каждый GM меняет состояние только сущностей своего уровня и только предложениями по BR-03: глобальный — атрибуты мира (погода, эпоха, `laws_version` через BR-02); региональный — регион и NPC региона; Task — NPC и игроков во встрече через `combat.decided`; персональный — ничего (только нарратив и свой контекст). Предложение изменить сущность чужого уровня отклоняется стражем с `reason=level_violation` (FR-034) | Must | Must | р5, BR-16 |
| FR-123 | **Персональный GM не действует за игрока.** Он не публикует `player.*`, не инициирует встречи и не меняет состояние (BR-06); его вывод — нарратив одного адресата с `generated_by`, построенный из событий видимости игрока. В группе в MVP-1 персональные GM участников доставляют один общий нарратив раунда (один вызов Phase 2 на раунд scope — FR-013, DR-12) без отдельного вызова LLM на игрока; индивидуальная перспектива — целевое (допущение A-13) | Must | Must | р5, DR-12 |
| FR-124 | **Фоновые тики.** Глобальный и региональные GM срабатывают по расписанию (`trigger.type=timer` блупринта) независимо от присутствия игроков и не удаляются по TTL. Интервалы задаёт блупринт; предложение по умолчанию (допущение A-14): глобальный — 1 раз в 60 мин, региональный — 1 раз в 30 мин; при игроках в регионе региональный тик учащается до интервала обнаружения (FR-012). Каждый тик публикует `tick.fired {agent, level, scope, scheduled_at}` (BR-04) | Must | Must | р5 (OQ-R-15 решено), DR-13 |
| FR-125 | **Бюджет фоновой жизни.** Тики без игроков в scope работают в LOD 1 (таблицы событий/правила без LLM) либо LOD 2 с малой моделью (`qwen3:8b`), с лимитом ≤ B вызовов LLM в час на мир суммарно по всем фоновым агентам (допущение A-14: B = 4); при исчерпании — тик без LLM (шаблон/таблица), событие `llm.output.rejected reason=budget_exceeded`. Персональные GM и Task-агенты без игроков не выполняются (FR-016) | Must | Must | р5 |
| FR-126 | **Фоновые события — обычные события мира.** Результаты тиков — зарегистрированные типы (`world.weather_changed`, `world.time_advanced`, `region.event_occurred`, `npc.moved`, `npc.spawned` — состав уточняет архитектор), записанные по BR-04 (включая `tick.fired` и `llm.output` тика с `phase=tick`), применяемые entity-manager; они индексируются семантической памятью и попадают в контекст персональных GM. Replay воспроизводит их без LLM и без таймеров | Must | Must | р5, DR-13 |
| FR-127 | **Последствия для входящего игрока.** При `enter` в регион и при первом действии после паузы ≥ 30 мин (граница сессии, FR-085) персональный GM получает сводку фоновых событий мира и региона с момента последнего присутствия игрока (из журнала событий/семантической памяти, не из LLM-памяти) и отражает её в нарративе входа; нарратив не противоречит фактам фоновых событий (например, погода в нарративе = текущая погода мира) | Must | Must | р5 |
| FR-128 | **Ограничения фоновой жизни.** Фоновые тики не порождают пробой законов без авторского триггера (BR-02 п. 4), не публикуют `player.*` и не изменяют HP/инвентарь/позицию/статус персонажей игроков вне их сессии (нет `combat.decided` против игрока без его действия — инвариант NFR-020); NPC, убитый игроком, не возрождается фоновым тиком в течение TTL региона из блупринта (по умолчанию — не в течение 24 ч) | Must | Must | р5, BR-06, DR-01 |

---

## 6. Бизнес-правила

| ID | Правило | Область |
|---|---|---|
| BR-01 | **Слои консистентности.** (1) *Законы мира* (`laws@vN`) — неизменны в обычном игровом цикле; меняются только автором (новая версия `approved`) или пробоем (BR-02). (2) *Канон* — факты мира/региона; добавляются и меняются только через утверждение (страж или ревьюер), с версией. (3) *Сиюминутное* — атмосфера, реплики, детали сцены; LLM свободно, без утверждения, с TTL. Таблица «кто что меняет»: автор — все слои и `immutable`/`default`-законы вселенной; ревьюер (человек) — канон, решение по пробою и по правилам FR-115; страж (автомат) — только отклоняет/помечает; LLM — сиюминутное напрямую, канон и законы только предложением (пробой — только в фазе пробоя); механика — числовое состояние сущностей; **пробой** — законы мира и `default`-законы вселенной, не `immutable` и не формулы механики; **эволюция правил (FR-115)** — формулы механики с ревью до применения, не законы (DR-02, FR-046) | консистентность |
| BR-02 | **Пробой законов** (редакция по DR-01; механика — эпик E-B, в MVP-1 — только п. 12). (1) *Объект:* закон мира — декларативное ограничение слоя 1, документ `laws@vN`; *пробой* — событие, отменяющее ≥ 1 закон и/или вводящее ≥ 1 новый в обход авторской правки. (2) *Что пробивается:* законы мира и `default`-законы вселенной; `immutable` — нет (допущение A-10); формулы механики — нет (FR-046). (3) *Инициаторы:* (а) LLM-агент уровня `global`/`domain` — только в отдельной *фазе пробоя*, запускаемой по п. 4 (в Phase 1/2 схема ответа пробоя не содержит); (б) автор — вручную (сразу `approved`); (в) игрок — косвенно, действиями, повышающими напряжение; прямой команды нет (BR-06). Инициатор и события-причины записываются в предложение. (4) *Условие «неординарности» — напряжение закона:* по каждому закону ведётся счётчик (отклонённые стражем предложения против закона, аномалии Monitor-агента, нарушения игроков, авторские триггеры блупринта); фаза пробоя запускается только при напряжении ≥ порога автора или по авторскому триггеру; текст игрока фаза не читает (защита бюджета и от инъекций). (5) *Частота:* не более одного пробоя `pending_review` на мир; кулдаун после решения ≥ K ходов и ≥ T дней (по умолчанию 100 ходов, 7 дней). (6) *Контракт:* `world.law_breach.proposed {initiator, cause_event_ids[], strain, laws_removed[], laws_added[], canon_invalidated[], narrative_hook, laws_version_base}`; `canon_invalidated` обязательно. (7) *Автопроверка стража до применения (синхронно):* не трогает `immutable`; новые законы непротиворечивы между собой и с оставшимися; все конфликты с каноном перечислены; нет изменений формул; ссылки только на существующие сущности (FR-034). Провал → `world.law_breach.rejected` (записан, не применён, напряжение не сбрасывается). (8) *Применение:* прошедшее автопроверку предложение становится `laws@vN+1` со статусом `pending_review` и вступает в силу с границы следующего хода/раунда во всех scope мира (не ретроактивно); событие пробоя транслируется в нарратив всех активных scope с `reason` (BR-11) через глобальный GM → персональные GM; агенты перезагружают законы; промпты, `llm.output`, снапшоты несут `laws_version = vN+1`. (9) *Постфактум-контроль:* ревьюер (человек) получает diff законов, `canon_invalidated`, число ходов/фактов под vN+1 и решает `approved` / `rolled_back` в окне ревью T_r (по умолчанию 14 дней или 200 ходов — что раньше); по истечении без решения — `approved` автоматически (допущение A-11; альтернатива — автооткат). (10) *Откат = ретрокон:* создаётся `laws@vN+2` с содержимым vN (история не удаляется); факты под vN+1 получают статус `retconned` (не удаляются); числовое состояние сущностей (смерти, предметы, HP) не откатывается — компенсации только явными событиями мира (BR-11); откат — нарративное событие, видимое игрокам. (11) *Replay:* все шаги — события (`proposed → rejected \| applied → review_decided → rolled_back`); при replay фаза пробоя LLM не вызывает (BR-04). (12) **MVP-1:** фаза пробоя отключена; обязательны `laws@v1`, `laws_version` в контексте/`llm.output`/снапшоте, зарегистрированные типы событий, инварианты NFR-020 как первые законы (FR-045). Краевые случаи — `domain-review.md` §3.1 (бой, другой scope, второй пробой, инъекция, рестарт, слияние scope) — вход брифа E-B | консистентность |
| BR-03 | **Единственный писатель.** Состояние сущностей меняет только entity-manager; любой другой компонент (включая все агенты роя) публикует предложение и ждёт факта `entity.updated`. Last-writer-wins недопустим для HP, инвентаря, позиции | состояние |
| BR-04 | **Record-replay.** Результат каждого недетерминированного источника — LLM, бросок, внешний API, **время и таймеры** (тики, таймауты раунда, TTL) — записывается как событие до его использования (`llm.output`, `dice.rolled`, `tick.fired`, `round.closed`); replay читает записанное и не вызывает LLM/RNG/часы повторно; производные события наследуют timestamp причины. Снапшот — истина, лог событий — способ догнать (DR-13) | состояние |
| BR-05 | **Механика — истина.** Числовые исходы определяет Rule Engine по декларативным правилам; нарратив описывает, но не переписывает. Phase 2 не публикует событий, меняющих состояние. *Исключение* — явно перечисленные LLM-оценки с enum-исходом, записанные как `llm.output` и ограниченные числовым минимумом/максимальным штрафом (портрет дао FR-112, испытание); в MVP-1 исключений нет (DR-16) | механика |
| BR-06 | **Агентность игрока.** Ни один агент роя (в том числе персональный GM) не публикует события `player.*` от имени игрока и не описывает действия персонажа, которых тот не совершал. Перемещение группы лидером оформляется событием группы (`group.entered_region {leader, members[]}`) или `player.entered_region` с `cause: group_move, by: <leader>` — формат выбирает архитектор (DR-20); «от имени игрока — без его действия или действия лидера его группы» | механика |
| BR-07 | **Псевдонимизация.** Единственный идентификатор игрока внутри платформы — `player_id`; внешние ID, username и имя профиля живут в отдельном удаляемом хранилище связок; в шину, индексы, логи (включая логи бота) и промпты внешние ID не попадают; данные профиля не используются для имени персонажа | ПДн |
| BR-08 | **Абсолютные запреты** — *утверждено на G1 (OQ-D-17), 7 пунктов:* (a) сексуализация несовершеннолетних в любой форме (включая «взрослые персонажи, описанные как дети»); (b) реальные, применимые инструкции по причинению вреда (оружие, взрывчатка, наркотики, взлом) — вымышленные «алхимия/заклинания» допустимы; (c) контент, направленный на реальных людей (травля, дипфейк-сценарии, сексуальные сцены с реальными лицами); (d) материалы, запрещённые к распространению в юрисдикции РФ — расшифровка как перечня категорий (экстремизм, оправдание терроризма, пропаганда наркотиков, категории 436-ФЗ/149-ФЗ, ФЗ-478) *составляется с юристом*; (e) поощрение самоповреждения/суицида как реальных действий; при признаках реального намерения в тексте игрока — протокол ответа вне игры, не нарратив (FR-054); (f) реальные ПДн третьих лиц, вставленные игроком, не воспроизводятся и не индексируются; (g) охранный принцип «мир вымышлен» — нарратив не ссылается на реальные события, организации, страны и политиков. Полный список — в контракте фильтра; в промпте — короткая позитивная формулировка (FR-051); фильтр MVP-1 проверяет только (a) (FR-050) | контент |
| BR-09 | **Уведомление об ИИ и согласие.** Каждый сгенерированный текст помечен `generated_by`; клиент обязан показать единое сообщение (ИИ, 18+, обработка текста, `/forget`) при первом взаимодействии и получить явное подтверждение до создания персонажа (FR-009) | контент |
| BR-10 | **Фоновая жизнь дёшева.** Без игроков LLM тратят только глобальный и региональные GM по расписанию и в бюджете FR-125 (без LLM или малая модель, ≤ B вызовов/час/мир); персональные GM и Task-агенты без игроков не живут; фоновый тик никогда не блокирует и не задерживает обработку действия игрока (приоритет — действию) | LLM/стоимость |
| BR-11 | **Последствия, не наказания.** Нарушение законов игроком превращается в событие в духе мира с видимой причиной (`reason`), не в отказ «ошибкой» и не в штраф вне мира | консистентность |
| BR-12 | **Одна архитектура GM — много агентов.** Все GM (глобальный, региональные, городские, персональные, Task) — экземпляры Agent GM Core по блупринтам; legacy `GMInstance` narrative-orchestrator выводится (или остаётся тонким рендером); фича-флаг сосуществования допустим только на период миграции и удаляется по её завершении. «Один GM» в видении = одна архитектура, не один агент (р5) | архитектура |
| BR-13 | **Группа и раунд.** Группа — 2–6 игроков в одном scope `group`; *лидер* — создатель группы; только его `enter`/`leave` перемещают группу (`enter` от не-лидера отклоняется с подсказкой); при выходе лидера лидерство переходит к старейшему участнику; группа распадается с выходом последнего; группа всегда в одной позиции. Раунд — по FR-025; действия внутри раунда — по времени приёма; NPC действует один раз в раунд. `flee` индивидуален (успех → `out_of_combat`, игрок остаётся в группе, не цель; провал → свободная атака NPC); `group leave` во время активной встречи = `flee`; присоединение — только вне встречи; трофей — нанёсшему последний удар (один раз); выход участника не прерывает сессию остальных; вышедший переходит в `solo` (DR-03) | игровой цикл |
| BR-14 | **Деградация без LLM.** Отсутствие LLM никогда не блокирует механику; игрок получает шаблонный текст с пометкой, а не ожидание | надёжность |
| BR-15 | **Локальный по умолчанию.** Базовая конфигурация — Ollama без внешних затрат; облачный провайдер включается явно, с лимитом и с записью в журнал; при наличии в мире игроков, кроме владельца, облако по умолчанию недоступно (NFR-046, DR-14) | LLM/стоимость |
| BR-16 | **Правила роя GM** (р5). (1) Каждый GM меняет состояние только своего уровня и только событиями-предложениями (FR-122); (2) события идут вверх и вниз по иерархии через шину, прямых вызовов между агентами нет (FR-121); (3) персональный GM игрока не публикует `player.*` и не действует за игрока (FR-123); (4) конфликт двух GM за одну сущность разрешает entity-manager по версии (BR-03), пересечение уровней — страж (`level_violation`); (5) один агент на (уровень, scope) — `max_instances = 1`; (6) фоновый тик уступает действию игрока (BR-10) | архитектура |
| BR-17 | **Сессия и учёт.** Сессия — серия ходов в scope с простоем ≤ 30 мин между ходами (FR-085); в критерии S7 считаются только сессии с `actor_kind = human` (допущение A-12, PA) | продукт/метрики |

---

## 7. Данные

| Данные | Что храним | Откуда | Ограничения |
|---|---|---|---|
| Сущности (игрок, NPC, регион, мир) | иерархический payload с историей, версия; статусы `player`: `alive / dead / ascended_final` (+ `idle` в группе); `dead`/`ascended_final` остаются в графе, исключаются из scope и целей NPC (DR-26); атрибуты мира: погода, время, `laws_version` | entity-manager (MinIO) | единственный писатель (BR-03); `player_id` — псевдоним |
| События | `id, type, timestamp, source, world, scope, payload, relations, correlation_id`; иерархический payload (`world{entity}`, `scope{id,type}`, `entity{entity,name}`, `trigger{event}`) | шина (Redpanda) | тип зарегистрирован (FR-036); retention достаточен для replay от последнего снапшота и для сводки фоновых событий FR-127 |
| Снапшоты | состояние сервиса на момент времени + `laws_version` | MinIO | периодичность и триггеры — архитектура; истина при восстановлении |
| LLM-выводы `llm.output` | поля FR-032 (провайдер, модель, параметры, хэш промпта, сырой ответ, статус, фаза, попытка, латентность, токены, стоимость, агент/уровень, `laws_version`, `replay`) | конвейер агента | обязательны до использования ответа (BR-04); промпт целиком — по флагу; статус `quarantined` — без `response_raw` (FR-050) |
| Броски `dice.rolled` | `seed`, `formula`, `result`, `roll_index`, `correlation_id`, причина | Rule Engine | `seed = hash(event_id, roll_index)` |
| Тики и раунды `tick.fired`, `round.closed` | агент, уровень, scope, `scheduled_at` / участники раунда, причина закрытия | агенты роя / game-service | источник времени при replay (BR-04) |
| Связки внешний ID ↔ `player_id` | `external_platform`, `external_id`, `player_id`, `notice_shown_at`, `consent_at`, `age_confirmed_at` | game-service (отдельное хранилище) | удаляемы; не реплицируются в шину и индексы; username/имя профиля не хранятся дольше, чем нужно для доставки |
| Блупринты, правила, законы, канон | Markdown/YAML, версия; `laws@vN`; `rules/dark-forest.yaml`; расписание тиков | Git (MVP-1) | один формат блупринта; версии законов для BR-02 |
| Семантический индекс | эмбеддинги и граф связей событий (включая фоновые) | semantic-memory (проекция) | перестраивается из лога; источник факта помечен |
| События аналитики `analytics.*` | сессии, ходы с таймингами, нарушения консистентности, итог replay (`metrics.md` §4.2) | game-service, тест-харнесс, компонент восстановления | без внешних ID, текста игрока и нарратива; топик — архитектура |
| Журнал сессий и инцидентов | `ops/metrics/sessions.csv`, `ops/metrics/incidents.csv` | CLI `session-report`, оператор | только `player_id` и счётчики; коммитятся в git |

---

## 8. Интеграции и внешние зависимости

| Система | Роль | Направление | Примечание |
|---|---|---|---|
| Telegram Bot API | эталонный клиент MVP-1 | бот ↔ game-service | внешний ID не покидает бота/хранилища связок; логи бота без пейлоадов (NFR-041); Telegram не даёт механизма 18+ для ботов — возраст подтверждается в `/start` (FR-009, DR-23) |
| Discord API | второй клиент (E-H) | тот же интерфейс game-service | следующий инкремент |
| Ollama (+ Qwen3) | LLM по умолчанию | сервисы → Ollama | GPU RTX 4090 24 ГБ; две модели одновременно: `qwen3:8b` (решения, тики) + `qwen3:30b-a3b`/`qwen3:32b` (нарратив); режим structured output; thinking off для Phase 1; окончательный выбор — после замера (NFR-076) |
| OpenAI / Anthropic / DeepSeek | облачные LLM (опционально) | через абстракцию провайдера | передача текста за рубеж — учесть в ПДн (только `player_id`; при внешних игроках — юрист, NFR-046) |
| Redpanda | шина событий | все сервисы | retention для replay и сводок фоновых событий |
| MinIO | сущности, снапшоты, блупринты-артефакты | entity-manager, агенты | — |
| ChromaDB или Qdrant | векторный индекс | semantic-memory | одна БД, выбор за архитектором (OQ-A-05) |
| Neo4j | граф связей | semantic-memory | — |
| TimescaleDB, Redis | по решению архитектора (OQ-A-07) | — | сейчас без потребителей / заглушка; для MVP-1 метрики — CSV + CLI (FR-088) |

---

## 9. Ограничения и допущения

**Ограничения (решения пользователя):** одна машина с GPU — **RTX 4090 24 ГБ VRAM,
128 ГБ RAM, Intel i9-13900** (р5); один разработчик, инкременты 2–4 недели, дедлайна
нет; русский язык контента и документов; стек — Go workspace + Redpanda + MinIO +
Chroma/Qdrant + Neo4j + Ollama; юрисдикция РФ по умолчанию; целевой рейтинг 18+;
нулевые внешние затраты на LLM в базовой конфигурации; клиент MVP-1 — Telegram.

**Допущения (G1 2026-09-09 утверждён без замечаний; статус — по строкам):**

| # | Допущение | Источник | Статус / если неверно |
|---|---|---|---|
| A-1 | Форма ввода — команды бота с опциональным текстом (словарь FR-002); распознавание намерения — целевое | р4, G1 | **решено** (OQ-R-07); состав словаря DR-17 (`status`, `rest`, `group *`) — **решено (G1)**, стартовое значение, уточняется по первым сессиям |
| A-2 | Латентность: механика ≤ 0,5 с, нарратив асинхронно ≤ 5 с; цифры фиксируются после замера на целевой машине | оркестратор (OQ-R-09), р5 | NFR-001/002 пересматриваются |
| A-3 | ~~Мир живёт только в активных scope~~ — **отклонено пользователем (р5)**: фоновая жизнь в MVP-1 (FR-124…FR-128) | р5 (OQ-R-15) | — |
| A-4 | Автор мира в MVP = оператор через файлы в Git и CLI | оркестратор (OQ-R-17/P-11) | FR-093/094 поднимаются в MVP |
| A-5 | Список абсолютных запретов BR-08 — 7 пунктов (a)–(g) | **решено (р4 — 4 пункта; G1 — расширен до 7, OQ-D-17)** | расшифровка (d) — с юристом; фильтр MVP-1 проверяет только (a) (FR-050) |
| A-6 | Первый бот — Telegram; Discord — следующий инкремент | **решено (р4)** | — |
| A-7 | Один аккаунт ↔ один **живой** персонаж ↔ один `player_id`; после смерти — новый `player_id` для того же аккаунта | BA по DR-06 | FR-008 частично в MVP |
| A-8 | Пробой законов в MVP-1: контракт (FR-045) и точка расширения; механика (BR-02 п. 1–11) — эпик E-B | BA по р2, PO | FR-041 в MVP |
| A-9 | Открытость репозитория без изменений; горизонт — один разработчик | оркестратор (OQ-P-12/13) | — |
| A-10 | `immutable`-законы вселенной не пробиваются пробоем | DE (DR-02), OQ-D-19 | **решено (G1)**, стартовое значение; уточняется в брифе E-B (механика пробоя) |
| A-11 | По истечении окна ревью пробоя без решения — `approved` автоматически (автопроверка пройдена); альтернатива — автооткат | DE (DR-01), OQ-D-18 | **решено (G1)**, стартовое значение; окно T_r и поведение уточняются в брифе E-B |
| A-12 | Сессия = серия ходов с простоем ≤ 30 мин; в S7 считаются только `actor_kind = human`; числа S7 — 8/3 | PA (metrics.md §0, §10), OQ-R-21 | **решено (G1)**, стартовое значение, уточняется замером (FR-085, BR-17) |
| A-13 | Персональный GM в группе в MVP-1 не делает отдельного вызова LLM на игрока: один нарратив на раунд scope, персональные GM доставляют его; индивидуальная перспектива — целевое | BA по DR-12 + р5, OQ-R-19 | **решено (G1)**, стартовое значение, уточняется замером (NFR-005/006) |
| A-14 | Параметры фоновой жизни: тик глобального GM — 60 мин, регионального — 30 мин; бюджет ≤ 4 вызова LLM/час/мир без игроков; модель тиков — `qwen3:8b` или без LLM | BA, OQ-R-20 | **решено (G1)**, стартовое значение, уточняется замером (NFR-007, «Что измерить первым» п. 8) |
| A-15 | Параметры раунда: таймаут 60 с, `idle` после 2 пропущенных раундов; S2 = «30 раундов группы из 3 (≥ 60 действий игроков)» | BA по DR-03, OQ-R-18 | **решено (G1)**, стартовое значение, уточняется замером (FR-025, BR-13; формулировку S2 в видении обновляет PO) |
| A-16 | Правила боя v0.1 (приложение A) — числа как предложение DR-04, калибруются после первых прогонов; e2e-seed выбирается так, чтобы игрок выжил, плюс прогон с гибелью | DE (DR-04) | **решено (G1)**, стартовое значение, уточняется замером (первые прогоны; FR-020) |

---

## 10. Вне объёма

См. §1.2 и §4.3. Дополнительно явно вне объёма MVP-1: города и GM города, культивация,
Планы, генерация миров, Entity-Actor/TinyML, эволюция правил, механика пробоя
законов (только контракт FR-045), фильтр контента 18+ (только точки вставки FR-050/056;
в MVP-1 фильтр проверяет лишь категорию (a) — G1), аутентификация внешних клиентов, Discord-бот,
английский контент, продуктовые метрики retention (только сессии/ходы по FR-085).

---

## 11. Открытые вопросы

Полный реестр со статусами — `project/open-questions.md`, раздел «Сводка после
discovery».

**Решено на G1 (2026-09-09):** OQ-D-16 — фильтр MVP-1 = минимальная проверка категории (a), `quarantined` без `response_raw` (FR-050); OQ-D-17 — BR-08 расширен до 7 пунктов, (d) — с юристом; OQ-D-18 (A-11), OQ-D-19 (A-10), OQ-R-18 (A-15), OQ-R-19 (A-13), OQ-R-20 (A-14), OQ-R-21 (A-12) и словарь команд (A-1) — приняты по рекомендациям как стартовые значения, уточняются замером на этапе архитектуры.

Оставшиеся вопросы (не блокируют архитектуру):

| ID | Вопрос | Владелец | Допущение/варианты в PRD |
|---|---|---|---|
| OQ-D-05 | Единый список scope-типов | архитектура | `solo, group, city, region, quest, world` |
| OQ-D-11 | Направление адаптации LOD | архитектура (целевое) | FR-017 «двухосный» |
| OQ-D-13 | Модель Qwen3 для каждой фазы/уровня и режим thinking | архитектура + замер на целевой машине | FR-070, NFR-076 |
| OQ-A-05, OQ-A-07 | Векторная БД; Redis/TimescaleDB; топик `analytics_events` | архитектура | FR-088 |
| OQ-A-08, OQ-A-09 | Реестр событий, формат контрактов, путь `correlation_id` | архитектура | FR-036, FR-084 |
| OQ-A-10, OQ-A-12, OQ-A-13, OQ-A-15, OQ-A-16 | Тесты, workspace, канон документации, гигиена, коммит user-stories | архитектура / планирование | — |
| OQ-D-15 | Лицензия и права на контент | пользователь (фон) | — |
| Архитектура | Уровень кода для персонального GM (`monitor` vs `task`); формат события перемещения группы (BR-06); состав типов фоновых событий (FR-126); формат правил с `damage_formula` | архитектор (ADR) | FR-120, FR-126, FR-020 |

---

## Приложение A. Правила боя «Тёмного леса» v0.1 (данные; предложение DR-04, допущение A-16)

| Параметр | Игрок | `wolf-alpha` |
|---|---|---|
| `hp_max` | 10 | 10 |
| `atk` (бонус попадания) | +2 | +3 |
| `def` (класс защиты) | 12 | 11 |
| `dmg` | d6 | d4 |
| `flee` (бонус бегства) | +2 | не бежит |

- **Атака:** `d20 + atk ≥ def цели` → попадание, урон `dmg`; натуральная 20 — урон ×2; натуральная 1 — промах. HP всегда в [0, hp_max] (clamp — инвариант NFR-020).
- **Ответ NPC:** после действий игроков раунда волк, если жив и есть цель, атакует один раз. Цель: последний игрок, нанёсший ему урон; иначе игрок с минимальным HP; при равенстве — по возрастанию `player_id`. `idle` и `out_of_combat` — не цели. Без вызовов LLM в Phase 1 боя (LOD 1).
- **Бегство:** `d20 + flee ≥ 10 + число живых противников` → игрок выходит из встречи в позицию `outside:{world}`; провал — волк немедленно атакует его вне очереди (свободная атака), игрок остаётся.
- **Смерть:** HP = 0 → `dead`. Игрок — терминально (FR-023). Волк — `dead`, трофей «волчья шкура» → нанёсшему последний удар; волк не возрождается в сессии и в течение TTL региона (FR-128).
- **Отдых:** `rest` вне встречи — HP до `hp_max`, занимает ход; во встрече недоступен (FR-024).
- **Броски на ход:** до 4 `dice.rolled` (попадание, урон, ответ волка: попадание, урон), `roll_index` 0…3, seed по FR-021.
- **Калибровка (ожидание):** шанс попадания игрока 0,60, средний урон 3,5 → волк падает за ~5 ходов; волка 0,60 × 2,5 → игрок за ~6,7 ходов; соло-победа ~60–65 %; группа из 3 — ~2 раунда, ≤ 1 укус на игрока. Замер на первых прогонах.
- **E2E «30 ходов соло»:** seed фиксирован и выбран так, чтобы игрок выжил (отмечено в тесте): `enter` → `look` → тик (встреча) → `attack`×k → волк мёртв → `look` → `leave` → `enter` (волк не появляется) → `rest` → …; отдельные прогоны: seed с гибелью игрока (FR-023) и с провальным `flee`.
