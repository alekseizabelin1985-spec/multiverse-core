# FROZEN — `services/ontological-archivist`

Сервис **заморожен** в EPIC-001 F-3 (задача T-002): остаётся на месте as-is, но выведен из сборки,
`Makefile`, `docker-compose.yml` и линтера. Это **заморозка, а не архив** — код возвращается в работу
в своём эпике (EPIC-006…010).

| Поле | Значение |
|---|---|
| Статус | заморожен (не собирается, не запускается, не развивается) |
| Причина | хранилище онтологических схем as-is; схемы событий ведёт `shared/contracts` |
| Заморожен в | задача T-002, ветка `epic/EPIC-001-foundation` (база `04a3a15`) |
| Последний рабочий коммит | `476be3e` |
| Эпик возврата | EPIC-006…010 |
| Собственный `go.mod` | остаётся автономным; после удаления `go.work` (T-003, F-2) сервис может не собираться — это ожидаемо |

Правила заморозки (`architecture/components/foundation.md` v0.2 §11,
`architecture/infrastructure.md` v0.3 §4.6):

- содержимое **не правится** — ни рефакторингом, ни автофиксерами; изменения только в своём эпике;
- сервис не входит ни в один профиль `docker-compose.yml` и ни в одну цель `Makefile`;
- зависимости сервиса могли уйти в `services/_archive/` (см. `services/_archive/README.md`) —
  восстановление сборки делает эпик возврата;
- разморозка — решение архитектора вместе с владельцем эпика.
