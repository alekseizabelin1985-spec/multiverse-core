# ops/models.txt — модели платформы (infrastructure.md v0.3 §6.5)
#
# Формат задан Makefile: каждая НЕзакомментированная строка — тег для
# `ollama pull` (`make models`) и для прогрева (`make warm`); обе цели работают
# только при MV_LLM_PROVIDER=ollama. Файлы .gguf для llama-server качаются
# вручную в MV_LLM_MODELS_DIR, поэтому секция gguf ниже — комментарии: иначе
# `make models` попытался бы тянуть их из реестра Ollama.
#
# Список сокращён по решению T-435 (ADR-005 дополнение 3, T-437) до
# конфигураций `decision_order` матрицы ops/metrics/bench-matrix.json:
# E → Q36-A3B → C → A. Сверяется с блупринтами: валидатор C-11 (правило 7а)
# отклоняет модель, которой нет у выбранного провайдера. Эмбеддинг-модели
# (nomic-embed-text, bge-m3) и тиковая модель E+ (qwen3-8b) из списка убраны:
# они вне decision_order и вернутся сюда вместе с решением, которое их выберет
# (эмбеддинги — architect#1 в 005-memory, ADR-005 доп. 2 п. 8).

# --- gguf: llama-server, провайдер openai_compat (по умолчанию) --------------
# Скачиваются вручную с Hugging Face в MV_LLM_MODELS_DIR. Имя модели в запросе,
# в блупринте и в GET /v1/models = имя файла без .gguf: в single-режиме его
# отдаёт `--alias`, который ставят scripts/llm-server.{sh,ps1} (T-437).
# Размер — байты файла со стенда (ops/metrics/baseline.md §1), в ГБ по 10^9.
#
#   Qwen3.8-27B-UD-Q3_K_XL        unsloth/Qwen3.8-27B-GGUF   13,4 ГБ   конфигурация E — базовая на переход, LLM_MODEL_DEFAULT
#   Qwen3.6-35B-A3B-UD-Q3_K_XL    unsloth (Hugging Face)     17,2 ГБ   конфигурация Q36-A3B — целевая нарратива, запасная № 1

# --- ollama: только при MV_LLM_PROVIDER=ollama -------------------------------
# Запасные конфигурации C (№ 2) и A (№ 3) матрицы; других тегов здесь нет.
qwen3:30b-a3b
qwen3:8b
qwen3:14b
