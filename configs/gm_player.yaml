scope_type: "player"
focus_entities: ["{{.player_id}}"]
time_window: "2m"
context_depth:
  canon: 1
  history: 2
  entities: 1
include:
  world_facts: false
  entity_emotions: true
  location_details: true
  temporal_context: true
triggers:
  time_interval_ms: 5000
  narrative_triggers:
    - "player.used_skill"
    - "player.died"
    - "player.got_item"

# 👇 Новое: параметры видимости
perception: 1.0  # Базовое восприятие (1.0 = 200 м)
# Можно динамически менять через события: player.perception_changed